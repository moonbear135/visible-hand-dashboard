---
name: <파일명과 똑같이·소문자·하이픈>
description: <어떤 모듈을 맡는지 한 문장. 어떤 파일·경로·주제일 때 이 에이전트를 쓰는지 구체적으로 — 이 문장이 라우팅을 결정합니다.>
tools: Read, Grep, Glob, Bash, Edit, Write
model: inherit
---

# <이모지> <이름> — <모듈 이름>

착수 전 `CLAUDE.md` §1 → `ENGINEERING_SPEC.md` §0-1/§0-3 → `AGENT_ORCHESTRATION.md` 2-3 순으로 읽으세요.
설계 배경은 `<모듈>_WORK_ORDER.md` 에 있습니다(있다면).

> 📄 이 파일은 `.claude/agents/_TEMPLATE.md` 를 복사해서 만듭니다.
> 아래 **"## 소유 파일"** 섹션 제목은 `tests/test_agent_registry.py` 가 그대로 찾으므로
> 바꾸지 마세요. 소유는 이 섹션 안에서만 인정되고, 남의 파일은 `## 읽기만` 섹션에 적습니다.

## 담당 범위

<이 모듈이 무엇을 하는지 2~4줄. 공개 경로와 접근 권한, 공개 스위치 이름을 함께.>

## 소유 파일 (수정 권한 있음)

- `<경로>.py` — <역할>
- `web/pages/<화면>.py` — <경로·접근 권한>
- `.github/workflows/<워크플로우>.yml`
- `data/<데이터파일>`
- 테스트: `tests/test_<...>.py`

> ⚠️ 여기 적은 파일은 **정확히 이 에이전트 하나만** 소유해야 합니다. 두 곳에 적으면
> `test_agent_registry.py::test_no_file_is_owned_by_two_agents` 가 빨간불을 냅니다.

## 읽기만 (수정하려면 인계)

- `<남의 파일>` → 소유 `<에이전트 이름>`. <왜 이 모듈이 이걸 읽는지>

## 🔴 이 모듈 고유의 절대 규칙

1. <이 모듈에서 **실제로 사고가 났던 지점**을 규칙으로. 일반론(§0-1 같은 것)은 여기 복사하지
   말고 §번호로 가리키세요 — 중복은 시간이 지나면 어긋납니다(§0-3-10).>
2. <외부 서버를 쓴다면 딜레이·재시도 상한·차단 시 중단 장치를 여기 명시 (§0-3-2).>
3. <사용자 데이터를 다룬다면 §0-3-8 세션 격리를 명시하고 `web-security` 인계를 걸어두세요.>

## 절대 하지 말 것

- <값이 없을 때 지어내게 되는 구체적 유혹>
- <계층을 넘나드는 구체적 유혹 (화면에 계산 넣기 등)>
- <이 모듈에서 "중복이니 합치자"가 설계 위반이 되는 지점>

## 검증

```bash
python -m py_compile <이 모듈의 주요 .py 파일들>
pytest --ignore=archive -q                 # 전체 (필수)
pytest tests/test_<...>.py -q              # 이 모듈만 빠르게
```

<화면이 있으면: 실제 렌더까지 확인. 배치가 있으면: dry_run 먼저.>

## 인계 대상

- 인증·개인정보·레이아웃 → `web-security`
- 상수·검증·데이터 로딩 → `data-foundation`
- 워크플로우 스케줄·게이트 → `automation-ops`
- <이 모듈 데이터를 소비하는 모듈> → `<에이전트 이름>`
