# 📊 "내 성적표" v1 신규 구축 — 실행 지시서 (오푸스5 최고성능용)

> 이 문서는 **다른 세션·다른 모델(오푸스)이 이 대화의 맥락 없이 처음부터 읽어도 작업을 시작할 수 있도록**
> 만든 독립 실행 지시서입니다. 2026-08-06 밤 1차 검토 메모(`MY_SCORECARD_MODULE_NOTES.md`)와
> 2026-08-10~11 오너 대화에서 확정된 내용을 종합해 정리했습니다.
> 작업 시작 전 반드시 `ENGINEERING_SPEC.md` 전체, `PROJECT_STATUS.md`, `MY_SCORECARD_MODULE_NOTES.md`를
> 먼저 읽으세요(§0-2 절차). `US_STOCKS_WORK_ORDER.md`도 같은 지시서 스타일이니 참고하면 좋습니다.

---

## 0. 한 줄 요약

기존 두 모듈("사실 이 가격이에요" 코스피/미국주식)에 이어 **3번째 모듈 "내 성적표"**를 신규 구축합니다.
로그인한 사용자가 자신의 보유 종목·수량·매입가를 **직접 입력**하면(v1 = 수동입력만, OCR은 v2로 보류),
보유 비중 원형차트를 보여주고 기존 PEGY 밸류에이션 데이터와 연동해 "내가 산 가격이 지금 밸류에이션상
싼지 비싼지"를 판단하게 돕는 게 목표입니다. 기존 두 모듈은 절대 건드리지 않고 완전히 별도 모듈로 만듭니다
(§0-3-6 신규 기능 모듈 관리 원칙).

---

## 1. 🔴 가장 중요 — 데이터 저장 아키텍처가 기존과 완전히 다릅니다

지금 이 프로젝트의 모든 데이터(`data/*.json`, `market_history.csv`)는 **공개 GitHub 저장소에 그대로
커밋**됩니다. "내 성적표"는 로그인·비밀번호·보유종목·매입가 같은 **개인 금융정보**를 다루므로 이 방식을
**절대 재사용하면 안 됩니다.**

- **DB/Auth는 Supabase를 씁니다** (오너 확정, 2026-08-11). 오너가 Supabase 프로젝트를 만들고 URL/anon key를
  발급하면, 그 값은 **git에 커밋하지 않고 Streamlit Community Cloud의 자체 Secrets 관리 화면**
  (앱 대시보드 → Settings → Secrets, TOML 형식)에 등록합니다. **이건 GitHub Actions Secrets
  (`KRX_OPENAPI_KEY`가 등록된 곳)와 완전히 다른 곳**이니 혼동하지 마세요 — 이 모듈은 스케줄 크롤링이
  아니라 사용자가 접속할 때마다 실시간으로 Streamlit 앱이 직접 Supabase를 호출하는 구조이기 때문입니다.
- 코드에서는 `st.secrets["SUPABASE_URL"]`, `st.secrets["SUPABASE_ANON_KEY"]` 형태로 읽으세요.
  **service_role 키는 절대 이 앱(사용자 대면 프론트엔드)에 넣지 마세요** — anon key + Row Level
  Security(RLS)만으로 충분해야 하고, 그렇게 설계하는 것 자체가 이 기능의 핵심 안전장치입니다.
- **로그인/비밀번호는 직접 구현하지 말고 Supabase Auth(email+password)를 그대로 쓰세요.** 이 저장소에
  이미 `bcrypt`가 있지만(관리자 단일 비밀번호용, `views/admin_view.py`) 그건 사용자 1명짜리 단순 인증이고,
  다중 사용자 회원가입/로그인/비밀번호 재설정은 검증된 Auth 서비스에 맡기는 게 훨씬 안전합니다.
- **RLS(Row Level Security)를 반드시 켜세요.** "사용자는 자기 자신의 holdings 행만 읽고 쓸 수 있다"는
  규칙을 애플리케이션 코드가 아니라 **DB 정책 레벨**에서 강제해야 합니다. 스키마를 만들 때 RLS policy
  SQL도 같이 작성해서 문서(§6)에 남기세요 — 오너가 Supabase 대시보드에서 그대로 실행할 수 있게.
- 코드는 `try/except ImportError`로 `supabase` 파이썬 패키지를 감싸서, 아직 Secrets이 설정 안 됐거나
  패키지가 없어도 **기존 두 모듈은 정상 작동하고 "내 성적표" 메뉴만 "준비중" 안내를 보여주도록** 하세요
  (`requirements.txt`의 기존 optional dependency 패턴 — `# yfinance` 등 주석 처리된 부분 — 을 참고).

---

## 2. v1 범위 (2026-08-11 오너 확정 — 이것만 만들면 됩니다)

**포함:**
1. 로그인/회원가입 (Supabase Auth, 이메일+비밀번호)
2. 보유 종목 수동 입력 (종목/수량/매입가 직접 입력 — **방법1만**, 스크린샷 OCR인 방법2는 v2로 보류)
3. 같은 종목을 여러 번(여러 증권사 계좌 가정) 입력하면 **수량 가중평균으로 매입가 자동 재계산**
   (마인드맵 예시: A증권 10주/100만원 + B증권 3주/21만원 → 13주/121만원 → 평균 93,076원).
   이건 난이도가 낮고 입력방식(수동/OCR)과 무관하게 필요한 로직이라 v1에 포함합니다.
4. 원형차트 2종: 계좌 총액 대비 종목별 비중 / 종목별 수익 비중
5. **기존 "사실 이 가격이에요" 연동**: 보유 종목이 코스피 상위200(`data/kospi200_pegy_latest.json`) 또는
   미국 상위550(`data/us_stocks_latest.json`) 안에 있으면, 리스트에서 마우스를 올렸을 때(또는 탭/클릭 시
   — Streamlit 툴팁 제약에 따라 적절히) 그 종목의 PEGY 밸류에이션 요약을 보여줍니다. 두 JSON 파일은
   이미 존재하는 최신 데이터를 **읽기 전용으로 그대로 재사용**하세요(구조는 아래 §5 참고).
6. 한국/미국 종목 섹터(리스트) 분리 관리, **원/달러 환율 변환 절대 안 함** — 각 통화 그대로 표시하고
   화면에 "환율 변환 없음" 경고 문구를 명확히 둡니다.

**v1에서 명시적으로 제외(다음 버전으로 미룸, 지금 코딩하지 마세요):**
- 스크린샷 업로드 + AI OCR (v2 — 벤더 미정, `MY_SCORECARD_MODULE_NOTES.md` §추가확인 참고)
- 로그인 없이도 매일 백그라운드로 수익률을 누적하는 기능, 주/월/분기/반기/연 리포트, 벤치마크 비교 (v3)
- 코스피 상위200/미국 상위550 밖에 있는 임의 종목의 실시간 시세 조회 (v1은 그 유니버스 밖 종목은
  "밸류에이션 정보 없음"으로만 표시하고 시세 자체도 안 가져옵니다 — 전체 시장 실시간 조회는 범위 밖)
- "1년 미접속 시 데이터 삭제" 실제 자동삭제 로직 (v1은 회원가입 시 안내 문구만 표시, 실제 삭제 배치는
  나중에)

---

## 3. 파일 구조 (완전히 별도 모듈, §0-3-6)

기존 파일(`views/pegy_view.py`, `views/us_stocks_view.py`, `utils/scoring.py`, `utils/constants.py`,
`collector_kospi200.py`, `collector_us_stocks.py`)은 **절대 건드리지 마세요.** 새로 만들 파일:

- `views/scorecard_view.py` — 화면 렌더링(로그인 폼, 보유종목 입력 폼, 원형차트, PEGY 툴팁)
- `utils/scorecard_db.py` — Supabase 클라이언트 래퍼(회원가입/로그인/로그아웃, holdings CRUD, 가중평균
  재계산 함수). Streamlit 앱 코드가 Supabase SQL을 직접 흩뿌리지 않고 이 모듈을 통해서만 호출하게 하세요.
- `sql/scorecard_schema.sql` (신설 폴더) — 오너가 Supabase SQL 에디터에 그대로 붙여넣을 테이블 생성 +
  RLS 정책 스크립트. 코드가 아니라 **오너가 수동으로 1회 실행하는 설정 스크립트**입니다.
- `requirements.txt`에 `supabase` 추가하되, 반드시 `try/except ImportError` 가드와 함께 코드를 작성해
  패키지가 없어도 앱이 안 죽게 하세요(§1 마지막 항목 참고).
- `visiblehand.py`의 사이드바 라우팅에 새 메뉴("📊 내 성적표")를 **기존 두 메뉴 로직은 그대로 두고 옆에
  추가**하는 방식으로 넣으세요(`MARKET_KR`/`MARKET_US` radio 옆에 별도 섹션으로 두는 걸 권장 — 성격이
  다른 기능이라 같은 radio 그룹에 넣지 않는 게 좋아 보입니다. 다만 최종 UI 배치는 오너 확인 후 조정
  가능).

---

## 4. 지켜야 할 아키텍처 원칙 (기존 프로젝트에서 이미 확립된 것들 — 새 모듈도 동일하게)

### 4-1. "지어내지 않기" (§0-1, 최우선, 예외 없음)
- 가격 정보는 절대 사용자가 입력한 매입가나 OCR값을 "현재가"로 오인해 쓰지 마세요. 현재가는 반드시
  `data/kospi200_pegy_latest.json`/`data/us_stocks_latest.json`의 기존 수집값을 조회해서 씁니다. 그
  유니버스 밖 종목은 현재가를 "알 수 없음"으로 정직하게 표시하고 임의 추정하지 마세요.
- Supabase 호출이 실패하면(네트워크 오류, RLS 거부 등) 빈 값으로 조용히 넘어가지 말고 에러를 사용자에게
  명확히 보여주세요.

### 4-2. 민감정보 처리
- 비밀번호는 절대 코드/로그에 평문으로 남기지 않습니다(Supabase Auth가 처리하므로 우리 코드는 애초에
  평문 비밀번호를 저장할 일이 없어야 합니다).
- `SUPABASE_ANON_KEY`도 KRX 키와 마찬가지로 로그에 남기지 마세요(다만 anon key는 설계상 클라이언트에
  노출되는 게 정상인 키라는 점은 KRX 키와 다릅니다 — RLS가 실제 방어선입니다. 이 차이를 코드 주석에
  남겨 헷갈리지 않게 하세요).

### 4-3. 신규 기능은 완전히 별도 모듈로, 스테이징 후 오너 승인 (§0-3-6)
- 다 만들고 나면 **바로 프로덕션에 반영하지 말고, 오너 스테이징 승인을 받은 뒤 사이드바에 노출**하세요.
  (기존 미국주식 모듈도 이 절차를 거쳐 공개 전환됨 — `PROJECT_STATUS.md` 참고)

### 4-4. 네트워크 제약 (샌드박스 한계, 반복 확인된 사항)
- 이 개발 샌드박스는 대부분의 외부 도메인(github.com, openapi.krx.co.kr 등)에 접근할 수 없습니다.
  Supabase도 마찬가지일 가능성이 높습니다. **실제 Supabase 연결 테스트는 이 세션에서 불가능하다고
  가정하고 작업하세요.** 대신:
  - `utils/scorecard_db.py`의 로직은 Supabase 클라이언트를 모킹(mock)한 합성 데이터로 오프라인
    유닛테스트를 작성하세요(기존 `tests/test_*.py` 패턴 그대로).
  - 가중평균 재계산 함수처럼 순수 계산 로직은 네트워크 없이도 완전히 검증 가능하니 꼼꼼히 테스트하세요.
  - 실제 Supabase 프로젝트 연결 확인은 오너가 Secrets를 등록한 뒤 직접 앱을 열어보는 방식으로 진행됩니다
    (KRX API 실서버 검증과 같은 패턴).

---

## 5. 기존 데이터 연동 참고 (실측 확인 완료)

### `data/kospi200_pegy_latest.json`
```
{"metadata": {...}, "stocks": [
  {"rank": 1, "is_visible": true, "name": "삼성전자", "code": "005930", "price": 230000.0, ...}
  ...
]}
```
- 종목 조회 키는 `code`(6자리 문자열, 앞자리 0 보존 주의).

### `data/us_stocks_latest.json`
```
{"metadata": {...}, "stocks": [
  {"symbol": "NVDA", "name": "NVIDIA Corporation Common Stock", "rank": 1, "price": 223.96, ...}
  ...
]}
```
- 종목 조회 키는 `symbol`(티커).

두 파일 다 **읽기 전용**으로만 쓰세요 — "내 성적표"가 이 파일을 수정하는 일은 절대 없어야 합니다(기존
크롤링 파이프라인 전용 산출물입니다). 각 종목 딕셔너리 안에 PEGY·목표주가·퀀트스코어 등 툴팁에 쓸만한
필드가 이미 다 있으니, 필요한 필드만 골라 요약 카드/툴팁을 구성하세요.

---

## 6. 착수 단계에서 만들어야 할 것 (우선순위 순)

1. **`sql/scorecard_schema.sql`** — `profiles`(또는 Supabase Auth의 `auth.users`를 그대로 쓰고 확장
   프로필만 별도 테이블), `holdings`(user_id, market, ticker, stock_name, quantity, avg_purchase_price,
   currency, updated_at) 테이블 + RLS 정책(본인 행만 select/insert/update/delete 가능). 오너가 Supabase
   SQL 에디터에 붙여넣기만 하면 되게 완결된 스크립트로 작성하세요.
2. **`utils/scorecard_db.py`** — Supabase 클라이언트 초기화(secrets 없으면 `None` 반환, 에러 아님),
   회원가입/로그인/로그아웃 래퍼, holdings CRUD, 가중평균 재계산 함수. 순수 계산 함수는 Supabase
   클라이언트 없이도 단독 테스트 가능하게 분리하세요.
3. **`tests/test_scorecard.py`** — 가중평균 계산, 통화 분리(원/달러 섞임 방지), PEGY 데이터 조회(합성
   JSON으로), Supabase 클라이언트 없을 때 "준비중" 폴백이 정상 작동하는지 등을 오프라인으로 검증.
4. **`views/scorecard_view.py`** — 로그인 폼 → 보유종목 입력 폼 → 원형차트 2종 → PEGY 연동까지 최소
   동작 버전.
5. **`visiblehand.py`** 라우팅에 메뉴 추가(§0-3-6에 따라 오너 승인 전까지는 노출 안 함 — 코드는 만들되
   기본값은 숨김 처리하거나 관리자만 미리보기 가능하게 하는 것을 권장).
6. 1~5가 안정적으로 동작하면(오프라인 테스트 통과 기준), 오너에게 진행상황 보고 후 Supabase 프로젝트
   생성·Secrets 등록·실제 연결 테스트를 진행하세요 — 한 번에 다 만들려 하지 말고 여기서 일단 끊어서
   보고할 것.

---

## 7. 절대 금지사항 (재확인, 이 프로젝트 전체 공통 규칙)

- **git 명령 절대 실행 금지** (add/commit/push/pull 전부). 오너가 cmd.exe에서 직접 수동으로 커밋합니다.
- Supabase URL/anon key 등 어떤 크레덴셜도 코드·문서·로그에 실제 값으로 적지 마세요.
- 지시받지 않은 기존 파일(코스피/미국주식 모듈, `utils/constants.py`, 매크로 관련 파일 전부)을
  건드리지 마세요. **특히 매크로(§4, 방공망 지표) 관련 파일은 오너가 명시적으로 중단 지시를 내린
  상태이니 절대 손대지 마세요** (`PROJECT_STATUS.md` 최상단 참고).
- 값을 지어내지 마세요(§0-1). 확실하지 않으면 로그/주석에 정직하게 남기세요.

---

## 8. 완료 후 보고 형식

작업을 마치면 `TASK_HISTORY.md`에 새 항목을 추가하고, `PROJECT_STATUS.md`에 "내 성적표" 진행 상황을
새 섹션으로 추가하세요(§4 매크로 섹션과 별도로, 예를 들어 §9). 무엇을 만들었고, 무엇을 오프라인
테스트로 검증했고, 오너가 다음에 무엇을 해야 하는지(Supabase 프로젝트 생성, Secrets 등록, SQL 스크립트
실행 순서 등)를 명확히 남기세요. `git diff -w --stat`로 변경 범위가 이 지시서의 §3 파일 목록을 벗어나지
않는지 스스로 확인하고 보고에 포함하세요.
