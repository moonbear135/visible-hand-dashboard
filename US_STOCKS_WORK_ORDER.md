# 🇺🇸 미국주식 페이지 신규 구축 — 실행 지시서 (오푸스용)

> ⚠️ **이 문서는 v1 빌드 당시(2026-08-06, Streamlit) 기준의 역사 기록입니다 — 지금 아키텍처가
> 아닙니다.** 이 지시서대로 새로 만들지 마세요: **미국주식 화면은 이미 완성돼 `/us`로 공개
> 실서비스 중**입니다(2026-08-08 공개 전환). 화면은 `views/us_stocks_view.py`가 아니라
> **`web/pages/us_stocks_page.py`(NiceGUI)** 가 그리고, 메뉴 배선도 `visiblehand.py` 사이드바가
> 아니라 `web/layout.py`의 `_MENU`에서 합니다. 수집기·스코어링(`collector_us_stocks.py`,
> `utils/scoring_us.py`, `utils/constants_us.py`)은 이 지시서대로 만들어져 **지금도 그대로
> 쓰입니다.** 현재 아키텍처는 `PROJECT_STATUS.md` §0을 보세요.

> 이 문서는 **다른 세션·다른 모델(오푸스)이 이 대화의 맥락 없이 처음부터 읽어도 작업을 시작할 수 있도록** 만든
> 독립 실행 지시서입니다. 2026-08-06 밤, 소넷과의 대화에서 조사·확정된 내용을 정리했습니다.
> 작업 시작 전 반드시 `ENGINEERING_SPEC.md` 전체와 `PROJECT_STATUS.md`를 먼저 읽으세요(§0-2 절차).

---

## 0. 한 줄 요약

"사실 이 가격이에요"(현재 코스피 200 PEGY 밸류에이션 리포트)에 이어, **미국 주식(나스닥+뉴욕 상장 시가총액
상위 약 550종목) 버전을 신규 페이지로 추가**합니다. 기존 코스피 페이지는 절대 건드리지 않고, 완전히 별도
모듈로 만듭니다(§0-3-6 신규 기능 모듈 관리 원칙).

---

## 1. 배경 — 오늘(2026-08-06) 세션에서 있었던 일 (참고용, 몰라도 작업 가능)

이번 세션은 원래 코스피 200 페이지의 하드코딩 감사(`AUDIT_REPORT_V2.md`, 27건) 후속 조치로 시작했습니다.
타임존(UTC/KST) 버그 수정, 목표가 상한(캡) 배지 문구 개선, PEGY 성장률 보정 배지 등 UI 디테일을 여러 건
고친 뒤, 오너가 "슬슬 미국주식 시작해보자"고 지시했고, 소넷이 데이터 소스를 조사해 이 문서로 정리했습니다.
지금 토큰 한계에 가까워져 이 지시서를 만들어두고, 다음 세션은 오푸스가 바로 기초틀 구축에 착수합니다.
전체 경과는 `TASK_HISTORY.md`(오늘 날짜 항목들, #33~#43)와 `PROJECT_STATUS.md` §8을 참고하세요.

---

## 2. 범위 (오너 마인드맵 원안)

- 나스닥 + 뉴욕(NYSE) 상장, 시가총액 상위 약 550종목
- ETF 제외
- 표시 정보: 티커, 영문명, 한글명(직접 매핑 필요 — 자동 번역 소스 없음, 회사명 사전 만들거나 주요 종목만
  수동 매핑 후 나머지는 영문명 그대로 노출하는 것도 고려)
- 화면 상단에 4개 지수(예: S&P500, 나스닥종합, 다우존스, 러셀2000 등 — 오너와 확정 필요, 이 문서 작성 시점
  미확정)를 2x2 배치
- 30개씩 페이지네이션 (코스피 페이지는 20개씩이니 다름에 주의)
- 수집 시점: 미국 장마감 + 30분 (아래 §5-2 타임존 항목 필독 — KST 아님)

---

## 3. 확정된 결정사항 (오너 확정, 2026-08-06 밤)

1. **통화 표시: 달러(USD)로만.** 원화 환산/병기 없음. 가격·시가총액·목표주가 등 전부 수집 원본 통화 그대로.
2. **종목 유니버스 필터링 3단계** (아래 §4-1의 CSV에 적용):
   - **채권/노트류 제외**: 종목명에 이자율("%")과 "Notes"/"Debentures"/"Bond" 패턴이 같이 나오면 제외.
     예: `"AT&T Inc. 5.350% Global Notes due 2066"`, `"Duke Energy Corporation 5.625% Junior Subordinated
     Debentures due 2078"`.
   - **미국식 우선주 제외**: 종목명에 "Preferred Stock"이 명시되면 제외. 예: `"Alphabet Inc. Depositary
     Shares representing a 1/20th Interest in a Share of Series A Mandatory Convertible Preferred Stock"`
     (티커 GOOGM/GOOGN). **한국 우선주(삼성전자우 등)와 성격이 다름** — 미국 우선주는 고정배당·채권에
     가까운 상품이라 이 프로젝트의 "성장주 밸류에이션" 취지와 안 맞습니다. 코스피 우선주 상속
     (`inherited_from_common`) 같은 로직을 미국 우선주에 적용하지 마세요 — 아예 목록에서 제외입니다.
   - **동일 회사 복수 보통주 클래스는 전부 포함 (중복제거 안 함).** GOOGL/GOOG(Alphabet 클래스A/C),
     BRK/A/BRK/B(Berkshire Hathaway) 같은 경우 회사명이 동일하게 찍히지만 **우선주가 아니라 의결권
     차이만 있는 보통주**이므로 위 두 필터에는 안 걸립니다. "대표 1개만 남기기"는 시도했다가
     **명시적으로 철회**된 방침입니다 — 클래스별 가격이 크게 다를 수 있고(BRK/A ≈ 74만 달러 vs
     BRK/B ≈ 495달러), 의결권 차이는 해외투자자에게 중요하지 않다는 오너 판단. 각 클래스를 별도
     종목으로 취급해 550개 슬롯에 모두 포함시키세요.

---

## 4. 데이터 소스 (실측 검증 완료, 2026-08-06 밤)

### 4-1. 종목 유니버스(순위표)

```
https://raw.githubusercontent.com/Ate329/top-us-stock-tickers/main/tickers/all.csv
```

- 무료, 인증 불필요, GitHub Actions로 **매일 자동 갱신**(NASDAQ Stock Screener API 기반).
- 컬럼: `symbol,name,price,marketCap,volume,industry`
- **시가총액 내림차순 정렬됨** — 위 §3-2 필터를 적용한 뒤 상위 550행만 취하면 됩니다.
- 나스닥·뉴욕 상장 종목이 섞여 있음을 실측 확인(JPM/XOM/WMT 등 NYSE 종목 존재) — "나스닥+뉴욕" 요구사항 충족.
- 참고: `tickers/top_200.csv`, `tickers/top_50.csv` 등 사전 필터링된 파일도 있지만 550개짜리는 없으므로
  `all.csv`를 받아 직접 슬라이싱하세요.
- 주의: 이 저장소는 제3자 개인 프로젝트입니다(공식 NASDAQ API 아님). 매일 자동 갱신이 언젠가 멈출 수
  있으니, 수집 스크립트에 "행 수가 비정상적으로 적으면(예: 550개 미만) 실패로 처리하고 이전 데이터 유지"
  가드레일을 넣으세요(§0-1 원칙 — 조용히 이상한 데이터로 채우지 말 것).

### 4-2. 펀더멘털 (Trailing + Forward)

```
https://stockanalysis.com/stocks/{TICKER}/statistics/
```

- 종목별 개별 페이지 스크래핑 (네이버 크롤링과 동일한 패턴 — `collector_kospi200.py` 참고).
- **소형주(시가총액 500위권, RBC Bearings)로 실측 테스트 완료** — 아래 필드가 전부 한 페이지에서
  확인됨(즉 대형주뿐 아니라 소형주까지 커버리지 충분해 보임, 하지만 550개 전수 테스트는 아직 안 함):
  - 밸류에이션: PE Ratio(Trailing), Forward PE, PEG Ratio, PS/Forward PS, PB Ratio, EV/EBITDA
  - 수익성: ROE, ROIC, ROA, ROCE, 마진(Gross/Operating/Profit/EBITDA)
  - 성장: EPS(Trailing), Revenue Growth Forecast(3Y), EPS Growth Forecast(3Y)
  - 배당: Dividend Per Share, Dividend Yield (무배당 종목은 "n/a"로 명시 — None 처리할 것, 0으로 채우지
    말 것)
  - 기타: 시가총액, 발행주식수, 장부가(Book Value Per Share, PBR 바닥가 계산용), 애널리스트 목표주가
    (Price Target) 및 컨센서스(Buy/Hold/Sell), Altman Z-Score, Piotroski F-Score
- **주의**: 제3자 애그리게이터(데이터 출처는 S&P Global Market Intelligence)이므로 `ENGINEERING_SPEC.md`
  §0-3-2 크롤링 매너 원칙(종목당 정중한 슬립, 과도한 동시요청 금지)을 코스피와 동일하게 적용하세요.
- **아직 안 한 것**: 550종목 전체를 정중하게 순회했을 때 실제 소요 시간 실측. 소형주 1종목만 테스트한
  상태입니다. 착수 초반에 10~20종목 샘플(대형주+중형주+소형주 골고루)로 먼저 타이밍을 재보고, 그 결과로
  550종목 전체 소요시간을 재추정하세요. (참고: 코스피 200종목이 종목당 정중한 슬립 포함 50분+ 걸렸음 —
  단순 비례로는 550종목이 2~3배지만, stockanalysis.com 페이지 구조/응답속도가 네이버와 다를 수 있어
  실측이 필요합니다.)

### 4-3. 보류된 소스 (지금 당장 필요 없음, 참고만)

- **Finnhub**(무료 API, 분당 60콜) — 회원가입+API 키 발급 필요, Forward 데이터가 무료 티어에 포함되는지
  미확인. stockanalysis.com 단일 소스로 충분해 보여 1차 개발에서는 사용 안 함. 나중에 stockanalysis.com이
  막히거나(anti-bot 등) 데이터가 부실한 종목이 많이 나오면 보완 소스로 재검토.
- **Stooq** — 가격 히스토리 벌크 다운로드용(API 아님, 이번 세션엔 fetch 실패). "3개월 이동평균" 같은
  자체 히스토리 기반 기능을 나중에 만들 때 재검토.

---

## 5. 반드시 지켜야 할 아키텍처 원칙 (이번 세션에 확립됨 — 새 기능도 처음부터 이렇게)

### 5-1. `utils/constants.py` 패턴을 재사용하되, 값은 새로 캘리브레이션

`utils/constants.py`를 읽어보면 코스피용 상수(`ROE_PREMIUM_BASELINE_PCT=12.0`, `TARGET_PER_CAP=25.0` 등)에
전부 한국어 근거 주석이 달려있습니다. **이 값들을 미국 종목에 그대로 갖다쓰지 마세요** — 미국 시장은 평균
PER/ROE 자체가 코스피와 다릅니다. `utils/constants_us.py`(가칭)로 별도 파일을 만들고, 값은 "TODO: 미국
시장 실제 분포 확인 후 재조정 필요"라고 명시한 잠정값으로 시작해도 됩니다(단, 반드시 그렇게 명시할 것 —
확정된 값처럼 보이게 하드코딩만 하지 말 것).

### 5-2. 타임존 — 미국 동부시간(America/New_York) 전용 처리 (★★★ 가장 중요, 오늘 실제로 겪은 버그와 동급)

오늘 세션에서 `datetime.now()`가 GitHub Actions 서버(UTC 기본)에서 naive UTC를 반환해 `last_updated_at`이
9시간 어긋나는 버그를 발견하고 전면 수정했습니다(`TASK_HISTORY.md` #37 참고). 미국주식 페이지는 **같은
버그를 새로 만들 위험이 더 큽니다**:

- "장마감(4:00 PM ET) + 30분"을 계산할 때 **반드시 `zoneinfo.ZoneInfo("America/New_York")`를 써서 ET
  기준으로 날짜/시각을 계산**하세요. 서머타임(3월~11월)과 표준시가 자동으로 반영됩니다.
- KST로는 미국 장마감이 서머타임 기간엔 오전 5시, 표준시 기간엔 오전 6시로 **날짜 자체가 바뀝니다**.
  크론(GitHub Actions cron)을 KST 기준으로 설계하지 말고, ET 기준 "몇 시에 장이 닫히는가"부터 계산한 뒤
  그 시각을 UTC cron 표현식으로 역산하세요.
- 화면에 보여줄 때는 ET 원본 시각과 KST 환산 시각을 둘 다 표기하는 것을 권장(오너 확인 시 헷갈리지 않게).
- 참고 코드 패턴: `collector_kospi200.py`의 `_now_kst()` 함수를 그대로 본떠 `_now_et()`를 만드세요.

### 5-3. 처음부터 "고정 임계값" 대신 절대거리/z-score 비례 스케일링

오늘 세션 내내 잡은 버그 패턴이 "임계값을 넘으면 무조건 고정 배수/점수를 적용하고, 그걸 마치 계산된
값처럼 화면에 보여준다"는 것이었습니다(`ENGINEERING_SPEC.md`에 "flat 하드코딩을 계산값처럼 보여주는
패턴"으로 기록). 미국주식 스코어링 로직을 새로 짤 때 처음부터:
- 어떤 지표가 기준선을 넘으면, 얼마나 넘었는지에 비례해서 점수/배수를 스케일링하세요(윈저라이즈 상한 포함).
- 화면에 어떤 숫자를 보여줄 땐 "이게 실제 계산값인지, 상한에 걸려 절단된 값인지"를 반드시 구분해서 표기
  (예: 오늘 만든 "🧮 상한 적용값" 배지 패턴 재사용).

### 5-4. 히스테리시스 버퍼 재사용

`collector_kospi200.py`의 `apply_hysteresis_buffer()` / `_load_previously_tracked_codes()` 로직을 그대로
재사용하세요 — 진입 기준(예: 550위)과 이탈 기준(예: 600위)을 다르게 둬서, 순위가 경계선에서 왔다갔다하는
종목이 매일 들어왔다 나갔다 하지 않게 합니다.

### 5-5. Forward 데이터는 "없으면 마스킹" — 처음부터 이렇게 설계

`ENGINEERING_SPEC.md` §0-1의 "섹션별 마스킹" 예외 조항을 그대로 적용하세요. stockanalysis.com에서 특정
종목의 Forward 데이터(Forward PE, 성장전망 등)가 없으면 종목 카드 전체를 막지 말고 Forward 섹션만
마스킹하고 Trailing은 정상 노출(코스피 페이지의 기존 마스킹 패턴 그대로 재사용).

### 5-6. "지어내지 않기" 원칙 (§0-1, 최우선 규칙 — 예외 없음)

- stockanalysis.com에 없는 필드(예: 무배당 종목의 배당수익률)를 0으로 채우지 말고 None/데이터없음으로.
- 수집 실패한 종목을 조용히 건너뛰지 말고, 실패 목록을 `metadata`에 남기세요(코스피의 `failed_codes` 패턴).
- 계산할 수 없는 지표를 임의로 만들어내지 마세요.

### 5-7. 신규 기능은 완전히 별도 모듈로 (§0-3-6)

- 기존 `collector_kospi200.py`, `views/pegy_view.py`, `utils/scoring.py`, `utils/constants.py`는 건드리지
  마세요. 새 파일(`collector_us_stocks.py`, `views/us_stocks_view.py`, `utils/scoring_us.py`,
  `utils/constants_us.py` 등, 이름은 임의 조정 가능)로 완전히 분리하세요.
- `visiblehand.py`의 사이드바 라우팅에 새 메뉴를 추가할 때도 기존 코스피 라우팅 로직은 그대로 두고
  옆에 추가하는 방식으로 하세요.
- 다 만들고 나면 **바로 프로덕션에 반영하지 말고, 오너 스테이징 승인을 받은 뒤 반영**하세요(§0-3-6).

---

## 6. 이번 착수 단계에서 만들어야 할 것 (우선순위 순)

1. **종목 유니버스 수집+필터 스크립트** — §4-1 CSV를 받아 §3-2 필터 3단계를 적용해 최종 ~550개 리스트를
   뽑는 함수. 필터링 결과(제외된 채권 수, 제외된 우선주 수, 최종 종목 수)를 로그로 남기세요.
2. **샘플 10~20종목으로 stockanalysis.com 수집 프로토타입** — 대형주/중형주/소형주 골고루 섞어서, 실제
   필드 추출이 잘 되는지, 종목당 몇 초 걸리는지 측정. 이 결과로 550종목 전체 소요시간을 재추정해서
   `PROJECT_STATUS.md` §8을 업데이트하세요.
3. **`utils/constants_us.py` 초안** — TODO 주석과 함께 잠정값으로 작성.
4. **`_now_et()` 헬퍼 + ET 기준 수집 시점 계산 로직**.
5. 위 1~4가 안정적으로 동작하면, 오너에게 진행상황 보고 후 다음 단계(전체 550종목 수집, 페이지 UI 구축,
   4개 지수 2x2 배치 등)를 논의하세요 — 한 번에 다 만들려 하지 말고 여기서 일단 끊어서 보고할 것.

---

## 7. 착수 전/도중 오너에게 확인해야 할 것

- 화면 상단에 배치할 "4개 지수"가 정확히 무엇인지(예: S&P500 / 나스닥종합 / 다우존스 / 러셀2000?) —
  이 문서 작성 시점에 미확정.
- 회사명 한글 표기를 어떻게 할지(수동 매핑 사전을 만들지, 주요 종목만 하고 나머지는 영문 그대로 둘지).
- 페이지 UI가 코스피 페이지와 완전히 똑같은 레이아웃(카드 형태)을 쓸지, 다른 구성을 원하는지.

---

## 8. 완료 후 보고 형식

작업을 마치면 이 세션의 `TASK_HISTORY.md`에 새 항목을 추가하고, `PROJECT_STATUS.md` §8을 실제 진행 상황에
맞게 업데이트하세요(오늘 만든 §8-4/§8-5 구조를 그대로 이어서 쓰면 됩니다). 오너가 나중에 소넷과 대화를
이어갈 때 바로 이해할 수 있도록, 무엇을 만들었고 무엇을 검증했고 무엇이 아직 안 됐는지 명확히 남기세요.
