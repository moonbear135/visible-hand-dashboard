# 🧊 데이터 신선도 감시 — 전체 흐름 지시서

> **2026-09-08 (#219) 신설.** 오너 지시로 "어제와 통째로 같은 데이터"를 잡는 눈을
> 저장소 전체에 붙이고, **경고가 수습 가능한 시각에 도착하도록** 배선을 바꾼 작업입니다.
>
> - 절대준수 규칙: `ENGINEERING_SPEC.md` (§0-1 · §0-3-10 · §0-3-15 · §0-3-16)
> - 현재 상태: `PROJECT_STATUS.md`
> - 작업 이력: `TASK_HISTORY.md` #219
> - 담당 에이전트: `data-foundation` (섀도 쪽 소비는 `kr-stocks`)

---

## 1. 왜 이 작업을 했는가 — 실제로 난 사고

**2026-09-04**, 코스피 수집기가 **"오늘 날짜 라벨 + 어제 내용물"** 스냅샷을 남기고 정식
수집을 건너뛰었습니다(#195). 전 종목 주가가 하나도 바뀌지 않았는데도 —

| 감시망 | 그날 반응 | 이유 |
|---|---|---|
| `watch_schedule_health.yml` | 🟢 조용 | "돌았는가"만 봅니다. 돌긴 돌았습니다 |
| `test_suite.yml` (pytest) | 🟢 조용 | 이미 아는 검사만 합니다 |
| `utils/data_sanity.py` | 🟢 조용 | 🔴 **결측 없음 · 건수 같음 · 중앙값 이동 0** → **오히려 "너무 정상"** |
| 결투 (`duel_rules`) | 🔴 **잡음** | 유일하게 **내용**을 봤습니다 |

오너: *"결투, 성적표, 사실 이 가격이에요 — 전부 다 적용이 안 되던 걸 찾았지."*

**값이 "그럴듯한가"만 보면, 어제 것을 그대로 다시 받은 날이 가장 정상적인 날로 보입니다.**
그래서 내용을 보는 눈이 필요하고, 그 눈은 **한 곳에만** 있어야 합니다 — 한 곳에만 있었기
때문에 나머지가 전부 뚫린 것이 이번 사고였습니다.

---

## 2. 두 번째 문제 — **경고를 쓸 수가 없었습니다**

오너: *"매일 오전 9시 30분이라는 시간은 참 애매한 시간대인데, 크롤링이 실패했어도 수습할
수 있는 시간도 아니고, 그날 장은 시작했으니까 장 종료까지 기다려야 하고."*
*"문제가 생긴 것을 수습할 시간을 벌기 위한 경고문들인데, 그 경고문들을 쓸 수가 없네."*

판정은 **수집 직후에 이미 나와 있었습니다.** 각 수집기가 저장 직후
`data_sanity.check_dataset()` 을 부르니까요. 09:30 은 그걸 **읽어주기만** 하는 시각이었습니다.

| 시각 (KST) | 무슨 일 |
|---|---|
| 16:05 | 코스피 수집 → **판정 완료** |
| 17:00 | 보조지표 수집 → 판정 완료 |
| 05:35 | 미국주식 수집 → 판정 완료 |
| **09:30** | **알림** ← 판정이 나온 지 최대 **17시간** 뒤 |

09:00 에 장이 열리므로, 09:30 에 알아도 **그날은 손쓸 수 없습니다** — 다시 수집하면
장중 가격이 그날 종가로 저장되는 사고가 나서(이 저장소가 실제로 겪은 것) 15:30 마감까지
기다려야 합니다. **경고가 가장 쓸모없는 시각에 도착하고 있었습니다.**

그리고 오너: *"이 부분을 미국장 한국장을 분리해서 돌려야 하는 게 맞다고 생각해, 이게
커버가 안 돼."* — 두 시장의 마감이 12시간 가까이 다른데 알림은 하루 한 번뿐이라,
어느 쪽이든 한쪽은 **반드시** 늦게 알게 됩니다.

---

## 3. 전체 흐름 (지금 이렇게 돕니다)

```
   ┌─ 코스피 수집 16:05 ─┐   ┌─ 보조지표 17:00 ─┐   ┌─ 미국주식 05:35 ─┐
   │ collector_kospi200  │   │ collector_       │   │ collector_us_    │
   │                     │   │  indicator_kr    │   │  stocks          │
   └──────────┬──────────┘   └────────┬─────────┘   └────────┬─────────┘
              │ 저장 직후 바로              │                      │
              ▼                           ▼                      ▼
        data_sanity.check_dataset()  ← 판정은 **여기서** 끝납니다
              │  · 건수 급감/급증 · 결측·0 비율 · 상수 채움 · 중앙값 급변
              │  · 🧊 내용 지문 비교  ← #219 신설
              ▼
        data/<이름>_sanity.json  (판정 + 내일의 기준값. 수집 워크플로우가 커밋)
              │
              ├──────────────► 화면 (`사실 이 가격이에요`)
              │                 판정을 **읽어서 배너로 표시만** 합니다
              │
              └── workflow_run(완료) ──► watch_data_sanity.yml
                                          디스코드 알림 + 잡 실패(빨간 X)
                                          🔴 각 장이 **닫혀 있는 시각**에 도착
```

**판정 계산은 `utils/data_freshness.py` 한 곳에만 있습니다**(§0-3-10).
`data_sanity`·섀도 관찰·화면이 전부 그 하나를 씁니다.

---

## 4. 만든 것

| 파일 | 역할 |
|---|---|
| `utils/data_freshness.py` | 🆕 **공용 판정** — `judge_frozen()` · `compare_unchanged()` · `fingerprint()` · `is_frozen()` |
| `utils/data_sanity.py` | 요약에 **내용 지문** 한 줄 추가 + 검사 ⑦ `frozen_content` 신설 + 화면용 `frozen_notice()` |
| `run_naver_api_shadow.py` | `check_frozen_data()` 를 **공용 모듈 호출로 교체**(자체 판정 제거) |
| `web/pages/pegy_page.py` | 얼어붙은 날 배너 표시 (**판정하지 않고 읽기만**) |
| `.github/workflows/watch_data_sanity.yml` | 09:30 cron → **각 수집기 완료 이벤트**. cron 은 안전망으로만 |
| `tests/test_data_freshness.py` | 🆕 14검사 |

---

## 5. 🔴 정직하게 남기는 한계 (§0-1)

### 5-1. 결투와 계산이 **두 곳에 남았습니다**

오너 결정으로 **결투 파일은 한 글자도 건드리지 않았습니다.** 결투 판정은 사용자 주문의
체결 여부를 직접 좌우하는 자리라, 공용화하다 그 경로를 흔드는 위험을 받지 않기로 했습니다.

그리고 결투 규칙을 **그대로 불러다 쓸 수도 없었습니다**:
`duel_rules.check_crawl_freshness()` 는 **지수 2개(KOSPI·KOSDAQ)를 앵커로 요구**합니다.
지수가 움직였는지를 봐야 "휴장일이라 다 같은 것"과 "수집이 실패해 다 같은 것"을 구분할 수
있기 때문인데, 이 모듈을 쓰는 쪽(섀도·산티체크)에는 **지수 값이 없습니다.** 지수를 새로
받아오려면 상대 서버에 요청을 더 보내야 하고(§0-3-2), 없는 값을 지어내는 것은 §0-1 위반입니다.

→ 결과적으로 **"무변동을 세는 계산"이 결투와 공용 모듈 두 곳에 있습니다.**
   완전히 하나로 합치려면 결투 규칙 파일을 고쳐야 하고, **그건 오너가 따로 판단할 사항**입니다.

### 5-2. 휴장일에는 **정상적으로 걸립니다**

휴장일이든 사고든 "어제 값을 그대로 들고 있다"는 사실은 같고, 이 모듈에는 그 둘을 구분할
앵커가 없습니다. 그래서 사유 문장에 **"휴장일이면 정상입니다"** 를 같이 적어, 한 번 보고
넘길 수 있게 했습니다. 국내 휴장일은 연 10~15일이므로 그만큼 알림이 옵니다.
**놓쳐서 하루를 통째로 잃는 것보다 낫다**는 것이 오너 판단입니다.

### 5-3. 지문은 **"몇 개가 바뀌었는지"는 못 셉니다**

`data_sanity` 는 원본 값을 안 들고 있고(매일 커밋되는 파일이라 작아야 합니다), 지문은
"통째로 같다/아니다"만 압니다. 종목 단위 개수가 필요하면 섀도처럼 `judge_frozen()` 에
값 dict 를 직접 넘기면 됩니다.

### 5-4. 알림이 하루 여러 번 옵니다

문제가 이어지는 동안 수집기가 끝날 때마다(하루 최대 3~4회) 알림이 옵니다.
같은 문제를 여러 번 알리는 셈이지만, **"아직 안 고쳐졌다"는 사실은 매번 참**입니다.

---

## 6. 검증

- **테스트**: 전체 **2,512검사 통과**(로컬, 실패 0). 신설 `test_data_freshness.py` 14검사,
  `test_data_sanity.py` 39 → **51검사**, `test_pegy_page.py` 22 → **25검사**.
- **사보타주 26종 전부 잡힘** (공용 모듈 7 · 산티체크 6 · 섀도 4 · 워크플로우 5 · 화면 4).
  그중 **1건이 진짜 구멍을 찾았습니다**:
  - 🔴 "지문이 결측을 포함하는가" 테스트가 **아무것도 안 지키고 있었습니다.** 처음 쓴 판은
    숫자 하나를 `None` 으로 바꿔 비교했는데, 그러면 어차피 숫자 묶음도 달라져서
    지문을 **"숫자만"** 내도 그대로 통과했습니다. **숫자 묶음이 완전히 같고 결측만 다른**
    경우(결측 행이 더 붙은 날 / `None` → 빈 문자열)로 고쳐 쓰니 잡힙니다.
  - (직전 #218 에서도 같은 계열의 구멍이 나왔습니다 — `compare_with_production` 이
    `check_frozen_data()` 결과를 **버려도** 테스트가 전부 통과하던 것. 검사가 **있는 것**과
    **불려서 읽히는 것**은 다릅니다.)
- **연쇄 확인**: 공용 모듈에서 `frozen` 판정을 없애는 사보타주를 넣자 **섀도 테스트가
  같이 빨간불**이 됐습니다. 오너가 걱정한 *"하나 망가지면 연쇄로 박살"* 은, 테스트가
  깔려 있으면 **"연쇄로 들킴"** 으로 나타납니다 — 조용히 틀리는 것보다 낫습니다.

---

## 7. 아직 안 한 것 (오너 판단 필요)

1. **결투와의 완전한 통합** — 5-1 참고. 결투 규칙 파일을 고쳐야 합니다.
2. **미국주식 화면·결투 화면에도 같은 배너** — 이번에는 "사실 이 가격이에요"만 붙였습니다.
3. **휴장일 달력** — 붙이면 5-2 의 오탐이 사라집니다. 다만 달력 출처가 또 하나의 외부
   의존성이 되므로(§0-3-2) 별도 판단이 필요합니다.
