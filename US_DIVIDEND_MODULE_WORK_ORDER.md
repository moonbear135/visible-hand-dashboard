# 🇺🇸 미국 배당 모듈 작업지시서 (US_DIVIDEND_MODULE_WORK_ORDER.md)

> 이 문서는 **아이디어 채팅방에서 오너와 나눈 브레인스토밍 결과를 정리한 지시서**입니다.
> 실제 구현은 이 문서를 들고 별도의 "배당금 모듈" 작업 채팅방에서 시작합니다.
> 여기 적힌 것은 **방향 확정 사항**이고, 세부 구현(파일명·함수 구조·환경변수 이름 등)은
> 작업 채팅방에서 `ENGINEERING_SPEC.md`·`DIVIDEND_MODULE_WORK_ORDER.md`(한국판)·
> `PROJECT_STATUS.md` §11을 먼저 읽고 그 관례를 그대로 따라 확정할 것.

---

## 0. 배경 — 왜 이 작업을 하는가

지금 "배당 달력"(`/dividend`, 사이드바 '💰 배당 달력' 그룹)은 **한국 상장사만** 다룹니다.
오너 판단: 한국 쪽은 밸류에이션(PEGY)·배당 모두 어느 정도 틀이 잡혔는데, **미국 주식은
같은 규모로 아직 부족**합니다 — 이미 만들어둔 미국주식 인프라(`collector_us_stocks.py`,
`/us` 화면, 결투 USD 트랙 등)를 다 활용하지도 못한 상태에서, 다음 우선순위는 **미국
배당 확보**로 정했습니다.

이 문서는 그 논의에서 나온 데이터 소스 검토·수집 범위·노출 방식 결정을 정리합니다.

---

## 1. 확정된 것 (오너 확정, 이견 없음)

1. **1차 수집 범위는 지금 밸류에이션 대상인 상위 550종목으로 한정.** 전종목
   (`data/us_all_market_prices.json`의 ~5,607종목)·ETF(`data/us_all_etf_prices.json`)
   확장은 **보류** — 별도 오너 승인 없이 먼저 손대지 않음(§0-3-6).
2. **한국/미국을 완전히 분리된 화면·라우트로 노출.** 한 달력에 섞지 않음(통화·건수
   혼동 방지). 사이드바도 탭/토글이 아니라 **완전히 독립된 사이드바 항목**으로 분리.
3. **"낯선 종목이 너무 많이 보인다"는 문제는 신경 쓰지 않는다.** 한국 배당 달력도
   이미 코스피+코스닥 전 상장사 공시를 그대로 보여주고 검색창으로 찾게 하는 방식이라,
   "익숙한 종목만 필터링" 같은 개인화 기능은 미국 쪽에도 **새로 만들지 않음** — 기존
   검색 필터(`matches_filters`) 방식을 그대로 재사용.
4. **단일 출처(2차 교차검증 없음)로 시작하는 것을 허용**하되, 이 사실을 화면·문서에
   정직하게 표시(§0-1). 2차 교차검증 소스 추가는 나중 과제로 남김.

---

## 2. 데이터 소스 — 검토 과정과 최종 판단

### 2-1. 채택 — 확정 배당금(과거 지급 확정치): **SEC EDGAR XBRL Frames API**

- 엔드포인트 예: `https://data.sec.gov/api/xbrl/frames/us-gaap/CommonStockDividendsPerShareDeclared/USD-per-shares/CY2026Q2.json`
- **실측 확인 완료(2026-08-27, 이 채팅방에서 직접 호출)**: 이 URL 하나로 **919개 기업**의
  해당 분기 주당 배당금이 `entityName`·`cik`·`start`/`end`·`val`·`accn`(원문 공시
  접수번호) 필드로 한 번에 옴.
- 채택 이유: 미국 연방정부(SEC) 공식 API, 무료, 인증 불필요, 라벨 기반 키워드 파싱
  가능(§0-1), 한 분기당 요청 1번으로 벌크 수집 — **DART `alotMatter.json`과 성격이
  가장 유사한 소스.**
- **한계 (작업 채팅방에서 반드시 검토)**:
  - 배당락일·기준일·지급일 같은 "일정" 데이터는 없음 — 오직 "확정 금액"만 줌.
  - 기업마다 쓰는 XBRL 개념(concept) 태그가 다를 수 있음
    (`CommonStockDividendsPerShareDeclared` 외 `...CashPaid` 등 변형 존재 가능) —
    상위 550종목 커버리지를 실제로 몇 % 잡는지 실측 필요. 태그 여러 종을 같이 돌려
    합치는 방식이 필요할 수 있음(한국 `collector_dividend_payment_kr.py`가 report_nm
    6종을 표본으로 도출한 것과 같은 접근).
  - 프레임이 역년 분기(CY2026Q2 등) 기준이라, 결산월이 12월이 아닌 회사는 실제 회계
    분기와 어긋날 수 있음 — "정당하게 다를 수 있는 차이"로 문서에 남길 것(§0-1, KR
    교차검증 때의 `cross_source_notes`와 같은 정신).
  - 개별 기업 단위로 더 볼 때는 `companyconcept` API
    (`data.sec.gov/api/xbrl/companyconcept/CIK{cik}/us-gaap/{concept}.json`)도 있음 —
    필요시 보강용으로 검토.
  - **자동 접근 시 User-Agent에 연락처(이름/이메일)를 명시하라는 SEC 정책 있음** —
    §0-3-2(외부 서버 매너)에 맞춰 반드시 지킬 것.

### 2-2. 채택 — 배당 일정(배당락일/기준일/지급일): **stockanalysis.com 종목별 페이지**

- 이미 `collector_us_stocks.py`가 가격 수집에 쓰고 있는 **기존 신뢰 출처**(§0-3-2 매너
  목록에 이미 등재).
- 실측 확인(KO 코카콜라 종목 페이지): 배당락일·지급일·기준일·배당금·주기(Quarterly)·
  과거 이력 20건·다음 예정 배당까지 **표 하나에 전부** 있음.
- **한계**: 전종목을 한 번에 주는 벌크 스크리너가 없음(`/dividend-screener/` 확인 결과
  404). **종목별 페이지를 하나씩 열어야 함** — 550종목 기준 종목당 2~3초 슬립(§0-3-2
  매너)으로 약 20~25분, 기존 550종목 전수 수집기와 비슷한 규모라 무리 없음.
- 이 소스가 **유일한 일정 데이터 출처**이므로, 화면에 "이 일정 정보는 단일 출처
  기준입니다"라고 정직하게 표시할 것(§0-1, 위 1-4항).

### 2-3. 기각된 후보와 기각 사유 (재검토 시 참고)

| 후보 | 기각 사유 |
|---|---|
| **NYSE/ICE 공식 API** | 무료 공개 API 자체가 없음. 실시간·과거 데이터 전부 ICE Data Services 등 상업 벤더를 통해서만 유료 제공. |
| **Nasdaq Dividend Calendar** (`api.nasdaq.com/api/calendar/dividends?date=...`) | **실측으로 데이터 품질은 최고 수준임을 확인**(하루치 요청 하나로 여러 종목의 배당락일/지급일/기준일/금액을 전부 벌크로 줌, `timeframe`이 1988~2104까지 커버). 그러나 `api.nasdaq.com/robots.txt`가 `User-agent: * / Disallow: /`로 **API 서버 전체를 크롤링 금지** 명시. §0-3-2("들어오지 말라면 안 들어간다")·오너 명시적 판단(2026-08-27)으로 **후보에서 제외**. |
| **dividendhistory.org** | 무료 배당 캘린더 사이트지만, 프로그램적 접근 1회 시도에 바로 403 — 정기 수집 대상으로 신뢰하기 어려움. |
| **Alpha Vantage (무료 티어)** | ① 이용약관상 "personal, non-commercial use"만 허용, "다른 사용자가 정보를 열람하는 상업적 활동"은 명시적으로 금지 — 우리 서비스(광고 붙은 공개 웹사이트)는 여기 해당돼 **약관 위반**. ② 설사 약관 문제가 없어도 무료 티어가 **하루 25회 요청**으로 550종목 커버에 규모 자체가 안 맞음(한 바퀴에 22일). 유료 전환($49.99/월~, 분당 75회·일일 무제한)은 **기술·법적으로는 가능**하나 매달 실비용이 발생하는 결정이라 오너가 별도 판단할 백로그 항목으로 남김. |

---

## 3. 아키텍처 방향 (참고용 — 작업 채팅방에서 최종 설계)

- 한국 배당 모듈처럼 "확정 배당"과 "지급 일정"을 **별개 산출물·별개 수집기**로 분리하는
  기존 원칙(§0-3-3 raw/가공 분리, §0-3-10 중복 금지)을 유지. 다만 미국은 소스 성격이
  달라서 파일 이름·저장 정책(append-only 여부 등)은 한국 그대로 베끼지 말고 새로
  판단할 것 — 특히 SEC 데이터는 "상태"가 아니라 "그 분기에 확정 신고된 값"의 성격이
  강해서, `collector_dividend_kr.py`보다는 `collector_dividend_payment_kr.py`의
  append-only 정신에 더 가까울 수 있음(작업 채팅방에서 실제 데이터 갱신 패턴을 보고
  결정).
- 기존 코스피 파이프라인(`collector_kospi200.py` 등)과 기존 한국 배당 파이프라인
  (`collector_dividend_kr.py`, `collector_dividend_payment_kr.py`) 둘 다 **import·수정
  하지 않음**(§0-3-6 신규 기능 모듈 분리 원칙 — `collector_us_stocks.py` 파일 상단
  주석이 이미 이 원칙을 선언한 전례 그대로 따름).
- 종목 유니버스(티커 목록)는 새로 만들지 않고 `collector_us_stocks.py`가 이미 관리하는
  상위 550종목 목록을 재사용.

---

## 4. 노출 방식 (확정)

### 4-1. 라우트

- `/dividend` — 기존 한국 배당 달력, **그대로 유지**.
- `/dividend/us` — 신규 미국 배당 달력. (정확한 경로명은 작업 채팅방에서 최종 확정
  가능하나, 기존 `/` ↔ `/us` 관례를 따르는 이 형태가 자연스러움.)

### 4-2. 사이드바 (`web/layout.py`)

지금 구조:
```python
('💰 배당 달력', [
    ('/dividend', '💰 투자 감사합니다!', DIVIDEND_MENU_ADMIN_ONLY),
]),
```

PEGY(`💡 재무제표 읽기` 그룹의 `/` ↔ `/us`, 라벨 앞에 🇰🇷/🇺🇸 국기 프리픽스)가 이미
쓰고 있는 **같은 그룹 안에 한국/미국 두 항목** 패턴을 그대로 재사용:

```python
('💰 배당 달력', [
    ('/dividend', '🇰🇷 투자 감사합니다!', DIVIDEND_MENU_ADMIN_ONLY),
    ('/dividend/us', '🇺🇸 투자 감사합니다!', DIVIDEND_US_MENU_ADMIN_ONLY),
]),
```

- 새 그룹을 만들지 않고 **기존 '💰 배당 달력' 그룹에 항목 추가**.
- `DIVIDEND_US_ENABLED`/`DIVIDEND_US_MENU_ADMIN_ONLY` 등 **완전히 독립된 새 환경변수/
  스위치**를 씀 — 기존 `/dividend`가 이미 전체 공개라고 해서 `/dividend/us`도 같이
  공개로 시작하지 않음. §0-3-6대로 **관리자 전용 → 단계적 공개 → 전체 공개** 절차를
  처음부터 다시 밟을 것(2026-08-27 채팅방에서 오너가 직접 재확인한 사항).

### 4-3. 달력 UI

- 기존 `/dividend`의 `_render_calendar()` 패턴을 그대로 재사용/재구현:
  - 달력 칸에는 **회사 이름이 아니라 건수만** 표시(`count = len(grouped.get(key, ()))`).
  - 칸 클릭 시 아래에 상세 리스트가 펼쳐지고, **이미 페이지네이션(`day_page`)이 있음.**
  - 이 설계는 "한 날짜에 여러 회사가 겹칠 수 있다"를 이미 전제로 만들어져 있어서,
    미국 쪽 배당 밀도(분기배당이 기본이라 한국보다 이벤트가 촘촘함)가 높아져도 달력
    그리드 자체는 그대로 버팀 — **새로 설계할 필요 없음.**
- 필터: 기존 검색창(`matches_filters`, 종목명/티커 검색) 방식 재사용. "내 보유종목만
  보기" 같은 개인화 필터는 **만들지 않음**(위 1-3항, 한국과의 일관성).

### 4-4. 경고 배너 (§0-3-13)

화면 상시 배너에 최소 아래 두 가지는 명시:
- 지금은 **상위 550종목 한정**이며 전종목·ETF는 아직 다루지 않는다는 점.
- 배당 일정(배당락일/지급일)은 **stockanalysis.com 단일 출처** 기준이라는 점(§0-1).

---

## 5. 작업 채팅방에서 이어서 정할 것 (미결정 사항)

1. SEC XBRL concept 태그를 몇 종 돌려야 550종목을 실제로 몇 %나 커버하는지 실측.
2. 확정 배당금(SEC)과 지급 일정(stockanalysis.com) 두 데이터를 화면에서 어떻게
   묶어 보여줄지 — 티커 기준 조인 방식, 둘 중 하나만 있을 때(예: 일정은 있는데 SEC
   태그가 아직 안 잡힌 경우) 처리 방침.
3. 정확한 파일명(`collector_dividend_us.py` 등 가칭)·환경변수 이름·수집 배치 주기
   (GitHub Actions 워크플로우 신설 여부).
4. SEC 요청 시 User-Agent에 넣을 연락처 문구.
5. `TASK_HISTORY.md`/`PROJECT_STATUS.md` 등록 — §0-3-7대로 작업 시작 시 반드시.

---

## 6. 백로그로 남긴 것 (지금 진행 안 함)

- 전종목(5,607종목)·ETF 확장 — 오너 별도 승인 필요.
- 2차 교차검증 소스 추가(단일 출처 한계 보완).
- Alpha Vantage 유료 전환 여부 — 매달 $49.99~ 실비용 발생, 오너가 별도 판단.
