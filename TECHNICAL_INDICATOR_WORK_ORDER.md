# 보조지표 모듈 작업 지시서 — 「여기서부터는 신앙입니다」

> 상태: **검토 완료, 착수 전** (2026-08-24, 오푸스 검토 2회 기반)
> 이 문서는 실제 구현에 들어가기 전 오너와의 논의를 정리한 작업 지시서입니다. 아직 코드는 한 줄도 작성되지 않았습니다.
> 모듈 번호: **7번째** (1 코스피 PEGY · 2 미국주식 · 3 내 성적표 · 4 사장님 보고서 · 5 결투다! · 6 배당금(진행 중, 이 모듈과 무관) · **7 여기서부터는 신앙입니다**)

---

## 0. 한 줄 요약

기존에 이미 매일 돌고 있는 `fdr.DataReader` 호출(현재는 코스피200 변동성 판정용)을 재활용해서, 코스피+코스닥 통합 시가총액 상위 **500종목**에 대해 RSI·MACD·볼린저밴드를 매일 계산·누적·다운로드 가능하게 하고, 세 지표를 합친 종합 판정은 100% 결정론적 파이썬으로, 개별 종목 해설만 사용자가 실제로 그 종목을 열 때 온디맨드로 제미나이를 호출해 서버 캐싱하는 구조로 만든다.

---

## 1. 이름·테마·경고 문구

- **이름**: 「믿습니까!」 폐기 → **「여기서부터는 신앙입니다」** 확정.
- **경고 문구는 다른 모듈보다 확실히 세게.** 다른 모듈의 "참고용입니다" 수준이 아니라:
  - 화면 **상단·하단 둘 다** 큰 글자·경고색 배너로 고정 배치(툴팁·작은 글씨로 숨기지 않음).
  - **문장마다 줄바꿈으로 확실하게 끊어서 보여줍니다.** 아래처럼 한 문장씩 줄을 바꿔서, 훑어봐도 각 경고가 따로따로 눈에 들어오게 합니다(한 문단으로 붙여쓰지 않음):

    ```
    이 화면의 숫자는 과거 데이터를 계산한 결과입니다.
    미래 주가를 맞히지 않습니다.
    매수·매도 판단의 근거로 쓰지 마세요.
    이 정보로 인한 어떤 손실도 개발자/이 프로젝트는 책임지지 않습니다.
    ```

  - **책임 소재 명시 필수**: 위 예시의 마지막 줄("이 정보로 인한 어떤 손실도 개발자/이 프로젝트는 책임지지 않습니다")에 해당하는 문구를 반드시 별도 줄로 포함.
  - AI 해설이 붙는 자리에는 그 옆에 축소판 경고를 한 번 더(이것도 한 줄로 욱여넣지 말고 짧게 줄바꿈).
- **✅ 확정(2026-08-24)**: 이 문구의 **내용(투자 판단 프레이밍 금지 취지)** 은 프로젝트 전체 원칙으로는 가지 않고 **이 모듈에 한정해서 다른 모듈보다 유난히 강하게** 간다.
  - 근거 확인: `ENGINEERING_SPEC.md`를 "투자 조언/투자 권유/매수매도 추천/면책/disclaimer"로 직접 재검색했으나 **전부 0건**. 이 프로젝트에 있는 기존 경고(배당 캘린더의 "배당받는 날 아님", 전역 "후행지표 전용", "실시간 아님" 배너 등)는 전부 **"데이터 시점"에 대한 사실 고지**일 뿐, "이 정보로 투자 판단을 내리지 말라"는 해석·행동 리스크 경고는 이 프로젝트 어디에도 없었음. **이 모듈이 그런 경고가 필요한 첫 사례.**
  - 따라서 다른 모듈의 배너 문구를 재활용하면 안 되고, 이 모듈 전용으로 새로 작성해야 함(위 문구 취지 참고). 상하단 배너 + 책임소재 명시 + AI 해설 옆 축소판 경고, 3곳 모두 이 모듈만의 문구 사용.
  - **단, 배너의 "배치·가시성·줄바꿈" 규칙 자체는 방금 `ENGINEERING_SPEC.md`에 §0-3-13(2026-08-24 신설)로 프로젝트 전체 기본 원칙으로 등록했습니다** — 모든 모듈이 유의사항을 상하단에 눈에 띄게, 문장 단위로 줄바꿈해서 보여줘야 한다는 것. 이 모듈의 강한 문구 내용은 그 위에 얹는 예외적으로 강한 버전입니다. (자세한 건 `ENGINEERING_SPEC.md` §0-3-13 참고.)

---

## 2. 범위 확정

| 항목 | 결정 |
|---|---|
| 지표 | RSI, MACD, 볼린저밴드 (3종) |
| 대상 시장 | 한국 주식만 (종목별 결과물이 필요해 미국은 이번 범위 제외) |
| 종목 수 | **코스피+코스닥 통합 시가총액 상위 500종목** (코스피200보다 넓게, 전 종목 2,874보다는 좁게 — 논의 끝에 절충) |
| 종목 선정 소스 | `data/kr_all_market_prices.json` (이미 존재, 4,296건 수록) 랭킹 활용 가능성 있음 — **착수 시 이 파일의 실제 구조·정렬 기준을 먼저 확인해야 함(미검증)** |
| **리밸런싱 주기** | **✅ 확정(2026-08-24): 3개월(분기) 단위**로 top 500 목록을 다시 산정한다. 매일 갱신하지 않음 |
| **목록 이탈 종목 추적** | **✅ 확정: 리밸런싱으로 top 500에서 빠진 종목도 최소 1년간은 데이터 수집·추적을 계속한다.** 화면 노출은 top 500만이지만, 이탈 직후 바로 데이터가 끊기지 않게 함(과거 종목의 이력이 갑자기 사라지는 것을 방지) |
| 데이터 종류 | 종가(Close)만 사용. OHLC 전체는 불필요 |
| 지표 계산 기간 | 제한 없이 넉넉하게(아래 §3 참고) — MACD 신뢰 구간 확보를 위해 최소 100~120봉 이상 확보 |

> 💡 **구현 힌트**: "3개월마다 재산정하되 이탈 종목도 1년은 계속 추적"하는 이 구조는, `ENGINEERING_SPEC.md`가 이미 설명하는 코스피 PEGY 200종목의 **히스테리시스 버퍼**(`_load_previously_tracked_codes()`, 버퍼 구간은 `is_visible=False`로 계속 수집만 하고 화면엔 숨김)와 정확히 같은 패턴이다. 새로 설계하지 말고 그 패턴을 그대로 재사용하면 된다(§0-3-10 중복 금지와도 부합).

---

## 3. 핵심 기술 발견 — 반드시 알아야 할 것

### 3-1. 기간을 늘리는 건 완전히 공짜다 (소스 코드로 실증 확인)

`FinanceDataReader/naver/data.py`의 실제 구현:

```python
url = 'https://fchart.stock.naver.com/sise.nhn?timeframe=day&count=6000&requestType=0&symbol='
r = requests.get(url + symbol)
...
return df.loc[start:end]   # 필터링은 받아온 뒤 로컬에서
```

요청 URL에 `count=6000`이 고정돼 있어서, 우리가 몇 일치를 요청하든(`timedelta(days=120)`이든 `days=400`이든) **매번 종목당 최대 6,000개 일봉(약 24년치) 전체를 한 번에 받아온 뒤 파이썬에서 잘라 쓰는 구조**다. 즉 기간 확장은 네트워크 요청·응답 용량·소요 시간에 **전혀 영향을 주지 않는다.** `collector_kospi200.py`의 `fetch_recent_volatility()`가 이미 이 방식으로 매일 종목당 1회씩 호출 중이므로, 이 모듈은 **새 크롤링을 추가하는 게 아니라 이미 받고 있던 데이터에서 더 많은 구간을 활용하는 것**에 가깝다.

⚠️ 이건 소스 코드 분석으로 확인한 것이고, **실제 GitHub Actions 환경에서의 응답 시간까지 실측하지는 못했다**(이 검토를 진행한 샌드박스는 `fchart.stock.naver.com`으로 나가는 네트워크가 막혀 있어 403으로 실패함). 착수 전 실측이 필요하다(§7 0단계).

### 3-2. 시간을 좌우하는 건 종목 수다

한 종목당 요청 1번이 나가는 구조라, 부하·시간은 순전히 **몇 종목을 도느냐**에 비례한다.

| 커버리지 | 종목 수 | 대략 소요(딜레이 포함) |
|---|---|---|
| 현재(코스피200, 변동성용) | 200 | 이미 기존 배치에 포함 |
| **이번 모듈(확정)** | **500** | ~5~10분 추정 — 기존 20~30분 배치에 얹어도 부담 적음 |
| 전 종목 참고치 | ~2,874 (코스피+코스닥+코넥스) | ~15~30분 추정 — 별도 워크플로우 필요 수준 |

500종목이면 기존 아침 배치(`scrape.yml`)에 얹는 것도 가능해 보이지만, **독립 모듈 원칙(§0-3-6)을 지키려면 여전히 별도 수집 스크립트/함수로 분리**하는 게 맞다. `collector_kospi200.py` 루프 안에 섞어 넣지 말 것.

---

## 4. 아키텍처 결정 사항

### 4-1. 판정(verdict)과 해설(commentary)을 반드시 두 층으로 분리한다

| 층 | 방식 | 이유 |
|---|---|---|
| **지표 원값 + 개별 판독**(RSI 과매수/과매도, MACD 골든/데드크로스, 볼린저 밴드 위치) | 100% 결정론적 파이썬 | 닫힌 형태 수식, AI 개입 시 §0-1 위반 |
| **종합 판정**(3개 지표 합산 → 매수우위/중립/매도우위 등급) | 100% 결정론적 파이썬 (가중합산 규칙표) | 이 저장소에 AI가 종합점수를 만든 선례가 없음(PEGY·매크로 모두 결정론). 판정을 AI가 쥐면 같은 입력에 다른 답이 나올 수 있어 시계열 저장의 의미가 붕괴하고, API 장애 시 모듈 전체가 인질로 잡힘 |
| **개별 종목 해설**(문장으로 풀어주는 것) | 제미나이, 온디맨드 | `macro_ai.py`의 `generate_macro_commentary()`와 같은 성격 — AI가 유일하게 정당하게 기여하는 자리 |

전 종목 매일 판정은 **500종목 전부 항상 계산**한다(파이썬이라 비용 문제 없음). AI 해설만 아래 캐싱 구조를 따른다.

### 4-2. AI 해설 캐싱 — "사람 수"가 아니라 "그날 실제로 열어본 서로 다른 종목 수"에 비용이 비례하도록

- 캐시는 **브라우저/사용자 단위가 아니라 서버(DB)에 종목+날짜 단위**로 저장한다.
- 오늘 어떤 종목의 카드를 A가 처음 열면 그때 1회 호출해 해설을 생성하고 "YYYY-MM-DD + 종목코드" 키로 DB에 저장. B, C, ... 몇 명이 같은 날 같은 종목을 열어도 캐시를 그대로 보여주고 재호출하지 않는다(`macro_ai.py`의 "오늘 것이 이미 있으면 재호출 안 함" 패턴 재사용).
- 최악의 경우(그날 500종목이 전부 한 번씩 조회됨)에도 하루 상한은 500회로, "전 종목 매일 배치 호출"과 같아지지만 실제로는 훨씬 적을 가능성이 높음.
- ⚠️ **동시성 함정**: 아직 캐시가 없는 종목을 두 사용자가 거의 동시에 열면 둘 다 캐시 미스를 보고 동시에 API를 2번 부를 수 있다. 구현 단계에서 "먼저 쓴 게 이기고 나중 건 버림" 같은 잠금/유니크 제약 처리가 필요하다(배당 모듈의 `merge_delta_output()`이 델타 중복 병합을 거부하는 방식과 같은 정신).
- 실패 시 문구를 남기되 날짜를 갱신하지 않는 `macro_ai.py` 패턴 그대로: "(2026-08-20 생성 해설)"처럼 **언제 만든 해설인지 항상 명시** — 지표는 매일 바뀌는데 해설만 그저께 것이면 화면이 모순되므로 이 모듈에서는 특히 더 중요함.

### 4-3. 단일 출처 원칙(§0-3-10)

- 상수(`RSI_PERIOD`, `MACD_FAST/SLOW/SIGNAL`, `BB_PERIOD/STD`, 과매수·과매도 임계치)는 전부 `utils/constants.py` 한 곳.
- 계산 함수는 `utils/indicators.py` 하나로 통합, 배치와 화면이 같은 함수를 공유(`stock_history.py`가 이미 지키고 있는 "화면 프레임워크를 import하지 않는다" 관례를 그대로 따름 — 오프라인 단위 테스트 가능해야 함).
- AI 함수 경계는 `generate_indicator_commentary(indicator_readings, verdict) -> str | None` 하나로, `AI_PROVIDER` 환경변수로 교체 가능하게(§0-3-11).

### 4-4. MACD가 뭔지, 데이터 관리는 어떻게 하면 되는지 (2026-08-24 추가, 오너 질문에 대한 답)

**MACD(Moving Average Convergence Divergence, 이동평균 수렴·확산)** 는 "최근 주가 흐름이 얼마나 강한 추세인지, 그 추세가 지금 강해지는 중인지 약해지는 중인지"를 보는 지표다. 종가만 있으면 계산되고, 계산 순서는 이렇다.

1. **MACD선** = 12일 지수이동평균(EMA) − 26일 지수이동평균(EMA). 단기 평균이 장기 평균보다 위에 있으면 양수(상승 추세), 아래면 음수(하락 추세).
2. **시그널선** = MACD선 자체의 9일 지수이동평균. MACD선의 "평균적인 흐름"을 보여주는 매끄러운 선.
3. **히스토그램** = MACD선 − 시그널선. MACD선이 시그널선을 위로 뚫으면(골든크로스) 히스토그램이 음수→양수로 바뀌고, 반대면(데드크로스) 양수→음수로 바뀐다. 화면에서 흔히 막대그래프로 표시하는 게 이 값이다.

지수이동평균(EMA)은 단순평균과 달리 **최근 값에 더 큰 가중치**를 준다. 그래서 시장 변화에 더 빠르게 반응하지만, 대신 "계산을 시작한 지 얼마 안 된 시점"에는 초기화 편향이 남아있다(§3에서 말한 "워밍업 구간" 문제가 여기서 나온 것).

**장기 데이터 관리에 대한 의견**: 좋은 소식은, §3-1에서 확인했듯 **우리는 원본 종가 시계열을 직접 쌓아둘 필요가 없다는 것**이다. `fdr.DataReader`를 부를 때마다 네이버가 종목당 최대 6,000봉(약 24년치)을 매번 통째로 돌려주므로, MACD가 필요로 하는 워밍업 구간(100~120봉)은 **상장한 지 100~120영업일이 넘은 종목이라면 이 모듈을 시작하는 첫날부터 이미 충족**돼 있다. 즉 "우리가 데이터를 몇 달 더 모아야 MACD를 쓸 수 있다"는 걱정은 이 모듈에는 해당하지 않는다 — 그건 우리 자체 누적 파일(`kospi200_stock_history.csv` 같은)을 원본으로 쓸 때만 생기는 문제였다.

그래서 "장기 데이터 관리"가 실제로 의미하는 건 원본 가격이 아니라 **우리가 매일 계산해서 남기는 결과값**(종목별 RSI/MACD/볼린저 원값 + 판정)이다. 이건 종목당 하루에 숫자 몇 개뿐이라 용량 부담이 크지 않고, §5-3에서 정리한 것처럼 이 결과값만 매일 누적하면 된다. 예외는 딱 하나 — **상장한 지 얼마 안 된 신규 종목**은 네이버가 애초에 짧은 역사만 갖고 있으므로 MACD가 자연스럽게 "산출 불가"로 마스킹된다(§6-6 결정 참고, 지어내지 않고 이유를 표시).

---

## 5. 오너가 재확인한 4가지 원칙 — 이 모듈에 구체적으로 어떻게 적용되는지

### 5-1. 가짜 데이터 절대 금지 (§0-1)

- 봉 수가 부족한 종목(RSI 15봉 미만, MACD 35봉 미만, 볼린저 20봉 미만)은 **중립값(RSI=50 등)으로 채우지 않고 `None` + "산출 불가(N봉 보유, M봉 필요)" 문구**로 표시. RSI의 중립값이 하필 50이라 더 그럴듯해 보이는 게 더 위험하므로 특히 주의.
- MACD처럼 EMA 워밍업 구간에서는 **숫자가 나오더라도 초기화 편향에 오염돼 있을 수 있음** — 각 레코드에 `bars_used`(실제 사용한 봉 수)를 반드시 남기고, 임계치 미달이면 값을 안 내보내거나 최소한 "⚠️ 워밍업 부족" 배지.
- AI 해설에는 프롬프트 단계에서 "매수/매도/보유 권유 금지, 미래 주가·목표가 언급 금지" 지시를 명시하고, **출력 후 파이썬으로 금지어 필터링**해서 걸리면 그 해설을 버리고 "해설 생성 실패"로 처리(지어낸 조언을 그대로 통과시키지 않음).
- 교차검증(가능하면): 같은 배치 안에서 이미 스크래핑 중인 네이버 당일가와 FDR 시계열 마지막 종가를 대조 — 추가 요청 없이 FDR 지연·수정주가 소급조정 이슈를 잡아낼 수 있음. 불일치는 배당 모듈의 `cross_source_notes`처럼 **기록만 하고 값은 고치지 않음**.

### 5-2. 상대 서버 예의 (§0-3-2)

- **기간을 늘리는 건 공짜**지만(§3-1), **종목 수를 500으로 늘리는 건 진짜 부하 증가**다 — 반드시 종목별 요청 사이에 딜레이를 둘 것.
- `fetch_recent_volatility()`가 이미 있다면 **그 호출을 재사용**해서 변동성+RSI+MACD+볼린저를 한 번에 뽑는 구조로(§0-3-10과 동시 만족). 지표용으로 FDR을 별도로 한 번 더 부르지 말 것.
- GitHub도 남의 서버다(§0-3-2). 매일 raw 데이터를 통째로 커밋하면 배당 모듈이 이미 겪은 `data/` 19MB 문제를 몇 배 규모로 재현한다 — 아래 5-3과 연결.
- 제미나이 호출도 대상이다: 배치로 500종목 전부 매일 부르면 안 되고, §4-2의 온디맨드 캐싱 구조를 지킬 것.
- 착수 전 반드시 GitHub Actions 수동 실행 1회로 **500종목을 도는 실제 소요 시간과 네이버가 짧은 간격 요청을 막는지 여부**를 실측할 것(§7 0단계).

### 5-3. 데이터 저장은 항상 진행될 수 있게 (신뢰성/가용성)

- 종목 하나의 조회 실패가 **전체 배치를 막으면 안 됨** — 개별 종목 try/except로 감싸고, 실패한 종목은 "데이터 없음"으로 표시한 채 나머지는 정상 저장.
- 비거래일 필터링: 지금 `kospi200_stock_history.csv`에 2026-08-09(일요일) 행이 섞여 있는 사고가 실제로 있었음 — 이 모듈의 누적 파일에는 같은 실수가 재발하지 않게 거래일 판정을 명확히 할 것.
- append-only 누적이되, 배당 모듈처럼 raw jsonl을 매일 전체 재수록하지 말 것 — **원본 시계열은 FDR에서 매번 다시 받으면 되므로 저장할 이유가 없고**, 저장이 필요하다면 "그날 받은 최신 1봉 + 체크섬" 정도로 최소화(용량 문제 방지). 이건 §6 열린 질문 2번과 연결.
- 다운로드는 항상 가능해야 함: `utils/stock_history.py` + `utils/stock_export.py`의 기존 패턴 재사용(종목별 다운로드보다 §0-3-3이 요구하는 "전체 파일 다운로드"가 우선).
- 체크포인트/재시작: 배당 모듈의 `build_run_key()` + `load_checkpoint(max_age_days=...)` 패턴처럼, 500종목 도중 실패해도 이어받을 수 있는 구조를 고려(500종목이면 필수는 아니지만, 전 종목으로 확장할 걸 대비해 미리 설계해두면 좋음).

### 5-4. 스파게티 코드 절대 금지 — 필요하면 오푸스 높음으로 검토 (2026-08-24 추가)

- 이 모듈은 층이 여러 개 얽히기 쉽다: 지표 계산(파이썬) · 판정 합산(파이썬) · AI 해설(온디맨드+캐싱+동시성) · raw/가공 분리 저장 · 500종목 배치 · 화면 배너. **층 사이 경계가 흐려지면 스파게티 코드로 가는 가장 흔한 경로**이므로 특히 조심할 것.
- `ENGINEERING_SPEC.md` §0-3-10(코드는 단순하게·중복 금지)이 이미 있는 원칙이지만, 이 모듈은 얽힐 지점이 많아 그것만으로 부족할 수 있음 — 아래를 기본으로 지킬 것.
  - 지표 계산(`utils/indicators.py`) · 판정 합산 · AI 해설 함수는 **서로 직접 호출하지 않고, 각자 입력→출력이 명확한 순수 함수로 분리**한다. 화면·배치는 이 함수들을 조합만 할 뿐, 로직을 화면 코드 안에 다시 풀어쓰지 않는다.
  - 한 함수 안에서 "네트워크 호출 + 계산 + 저장 + 화면용 문자열 조립"을 전부 하지 않는다. 이 넷은 각자 다른 함수/파일에 둔다.
  - 캐싱·동시성 처리(§4-2)처럼 로직이 복잡해지는 지점은 특히 함수를 작게 쪼개 이름만 보고도 뭘 하는지 알 수 있게 한다.
- **복잡도가 애매하거나 여러 층이 한 커밋에 같이 바뀌는 경우, 병합 전에 오푸스 높음(reasoning effort 높음)으로 별도 리뷰를 돌린다.** 이전 감사(`AUDIT_REPORT.md`/`_V2.md`)와 배당 모듈의 오푸스 높음 서브에이전트 리뷰가 이미 이 방식으로 실제 문제(변조된 부호, 캡 흔적 없음 등)를 찾아낸 선례가 있음 — 같은 수준의 검토를 이 모듈에도 기본으로 적용.

---

## 6. 오너 결정 사항 (2026-08-24, 1차 정리)

1. **✅ 종목 선정 방식 — 확정.** 코스피+코스닥 통합 시가총액 상위 500종목. **리밸런싱은 3개월(분기) 단위**로 하고, 목록에서 빠진 종목도 **최소 1년간 데이터 추적은 계속**한다(§2 표 참고, PEGY 200의 히스테리시스 버퍼 패턴 재사용). 다만 `kr_all_market_prices.json`의 실제 컬럼·정렬 기준 확인은 여전히 착수 시 필요(미검증).
2. **✅ raw 저장 정책 — 화면 UX 기준으로 확정.** 오너가 원하는 그림은: 화면 기본 노출은 **가장 최신 1봉의 결과값**이고, **-1일·-2일·-3일 정도는 화면에서 간단히 "뒤로 넘겨보기"** 로 볼 수 있으면 되고, 그보다 긴 장기 데이터는 **사용자가 파일을 직접 다운로드**해서 본다. 이걸 저장 구조로 옮기면:
   - 화면에서 즉시 넘겨볼 수 있어야 하는 최근 3~4일치는 **가공(처리 완료) 결과값을 별도로 얕게 캐싱**해두는 게 자연스럽다(매번 재계산하지 않도록).
   - 그보다 긴 장기 이력(다운로드용)은 계속 **일별로 누적**해야 한다 — "raw를 최신 1봉만 저장"하는 쪽으로 가면 안 되고, §5-3의 "가공 결과값은 매일 누적"이 이 요구사항과 맞다. 다만 **raw(FDR 원본 시계열) 자체는 여전히 매번 새로 받아오면 되므로 저장할 필요 없음**(§4-4에서 확인) — 저장·누적이 필요한 건 계산이 끝난 **가공 결과값**(지표 원값 + 판정)이다.
   - 정리: raw = 매번 재조회(저장 안 함) / 가공 결과값 = 매일 누적 저장(다운로드용) + 최근 3~4일은 화면 넘겨보기용으로 바로 서빙.
3. ~~경고 조항을 프로젝트 전체 원칙으로 신설할지~~ → **✅ 확정: 이 모듈 한정 배너로, 단 다른 모듈보다 강하게(§1 참고).**
4. **✅ MACD 기초 — §4-4에서 설명함, 랭킹 질문도 닫힘.** 종가만 있으면 계산 가능하다는 게 맞고(확인됨), 원본 가격을 우리가 오래 쌓아둘 필요도 없다(FDR이 매번 충분한 과거치를 통째로 줌). **남은 절반**(MACD는 원 단위라 종목 간 비교가 무의미함, 정규화된 비교 필드를 만들지 여부)은 **오너가 확인**: "여러 종목의 MACD를 서로 비교하는 랭킹 기능, 애초에 각각의 종가로 만드는 건데 이걸 서로 비교하는 의미가 있어?" → **없음. 랭킹 기능 자체를 안 만들기로 확정.** 정규화 필드도 불필요.
5. **✅ 파생 지표 배지 — 확정.** 기존 🧮(계산값) 배지를 재활용하지 않고, RSI/MACD/볼린저 전용 **새 배지**를 만든다(예: "📐 파생지표"). 정확한 이모지·문구는 화면 구현 단계에서 확정.
6. **✅ 워밍업 부족 종목 처리 — 확정.** 이 프로젝트의 기본 원칙(가짜 데이터·상상 금지, §0-1)을 그대로 적용: **산출이 안 되면 보여주지 않는 게 기본**이고, 대신 **왜 안 되는지 이유는 반드시 명확하게 표기**한다(예: "MACD 산출 불가 — 상장 후 42거래일, 최소 35거래일 필요" 같은 문구). 지표별로 부분 표시(볼린저는 되고 MACD는 안 되는 상태를 각각 따로 표시)하는 방식이 이 원칙과 맞다 — 종목 전체를 숨기지 않고, 안 되는 지표만 그 자리에 사유와 함께 마스킹.
7. **⏳ 보류(2026-08-24, 화면 다 만든 뒤 최종 결정) — 결과값 기반 필터링.** 지표값으로 종목 랭킹을 세우는 건 필요 없음(#4에서 확정, 시가총액 순서면 충분)으로 정리됨. 대신 **결과값 기반 필터링**(예: RSI 과매수/과매도만 보기, 오늘 골든·데드크로스 발생 종목만 보기, 볼린저 상·하단 터치 종목만 보기, 종합판정별 보기)은 랭킹과 달리 종목마다 자기 자신의 값·임계치만 비교하면 되므로 단위 문제 없이 안전하게 만들 수 있음. **지금 결정하지 않는다 — 화면까지 다 완성된 뒤 실제로 보면서 필요 여부를 최종 결정.**

---

## 7. 착수 로드맵

### ✅ 0단계 — 착수 전 실측 — 완료 (2026-08-25, 오너가 GitHub Actions 수동 실행)

`probe_indicator_universe_timing.py` + `.github/workflows/probe_indicator_timing.yml`(진단 전용, 1회성)로 실측. **결과 전부 청신호:**

| 항목 | 결과 |
|---|---|
| 대상 | 500종목(`kr_all_market_prices.json` 상위 500) |
| 요청 기간 | 400일 |
| 딜레이 | 종목당 0.5초 |
| 성공/실패 | **500 / 0** — 네이버가 이 속도에서 요청을 막지 않음 |
| 총 소요 | **12분 52초(771초)** |
| 호출 1건당 응답시간 | 평균 1.043초 / 중앙값 1.139초 / 최대 1.668초 |
| 받아온 봉 수 | 평균 261봉 / 최소 28봉 / 최대 267봉 — 400일 요청과 정확히 맞아떨어짐(§3-1 가설 확인됨) |

- 771초 중 딜레이 몫(500×0.5초=250초)을 빼면 **순수 호출 시간이 약 520초(8.7분)** — 시간의 대부분은 딜레이가 아니라 응답 자체. 딜레이를 더 줄여도 얻는 게 크지 않고, 실패 0건이 나온 지금 설정(0.5초)을 그대로 실전에 가져가도 안전해 보임.
- **⚠️ 새로 확인된 것**: 이 모듈만 독립적으로 돌리면 **약 13분**이 걸린다. 기존 `scrape.yml` 아침 배치(20~30분)에 그대로 얹으면 전체가 **35~45분**까지 늘어날 수 있다는 뜻 — §5-2에서 "500종목이면 기존 배치에 얹어도 부담 적다"고 썼던 추정보다 실측이 더 크게 나왔다. §7-1(git push 충돌 창)이 그만큼 넓어지므로, **독립 워크플로우로 분리하는 쪽을 권장**(원래도 §0-3-6상 권장이었는데, 실측으로 근거가 더 확실해짐).

### ✅ 1단계 — 계산 엔진 (네트워크 불필요) — 완료 (2026-08-24)

`utils/indicators.py`(RSI·MACD·볼린저밴드·종합판정 4개 함수) + `utils/constants.py`에 §5 신규 상수 섹션 추가. `tests/test_indicators.py` — 합성 시계열(단조증가/단조감소/급락/횡보/데이터 부족)로 오프라인 단위테스트 **23개 전부 통과**. 확인된 것:
- 봉이 부족하면 `available=False` + `reason`으로 정직하게 빠짐(중립값으로 안 채움, §0-1).
- 산출은 되지만 워밍업(EMA 초기화 편향) 구간이면 `warmup_insufficient=True`로 표시됨.
- 종합판정에서 산출 불가한 지표는 0점(중립)으로 슬쩍 섞이지 않고 `skipped`에 남아 합산에서 제외됨.
- MACD 데드/골든크로스, 볼린저 %B(무차원이라 가격대 달라도 같은 스케일), RSI 과매수·과매도 전부 합성 시나리오로 검증.
- `python3 -m py_compile`로 두 파일 다 문법 오류 없음 확인.
- 화면·수집기(`web/`, `collector_kospi200.py`) 어디에도 아직 연결 안 함 — 계산 엔진만 독립적으로 완성된 상태(§0-3-10 관례대로 프레임워크 미의존).

**남은 것**: 이 함수들을 실제 배치(`collector_kospi200.py` 또는 별도 수집기)에 연결하는 건 **0단계 실측이 먼저**다. 지금 상태로는 오너가 로컬에서 `python -m pytest tests/test_indicators.py -v`로 직접 재현·확인할 수 있다.

### ✅ 2단계 — 수집기 통합 — 완료 (2026-08-25, 오프라인 검증까지)

독립 수집기(`collector_indicator_kr.py`)로 만들었습니다(§7 0단계 실측 결과에 따라 `collector_kospi200.py`에 얹지 않고 완전 분리, 워크플로우도 별도). 새로 생긴/바뀐 파일:

| 파일 | 역할 |
|---|---|
| `utils/indicator_universe.py` | 500종목 유니버스(3개월 리밸런싱 + 이탈 종목 1년 추적) — PEGY 200의 히스테리시스 버퍼 패턴 재사용 |
| `collector_indicator_kr.py` | 일간 수집기: FDR 호출 → `utils/indicators.py` 계산 → 이력 기록 |
| `utils/stock_history.py` | `INDICATOR_HISTORY_FILENAME`/`INDICATOR_HISTORY_FIELDS` 추가(신규 저장 모듈 안 만들고 기존 범용 함수 재사용, §0-3-10) |
| `utils/constants.py` | `INDICATOR_FETCH_DAYS=400`, `INDICATOR_REQUEST_DELAY_SECONDS=0.5`(0단계 실측 근거) 추가 |
| `tests/test_indicator_universe.py` | 11개 |
| `tests/test_collector_indicator_kr.py` | 14개(네트워크 불필요 부분만 — FDR 실호출은 0단계 실측이 이미 검증) |

**오프라인 테스트 총 51개(계산엔진 23 + 유니버스 11 + 수집기 14) 전부 통과, `py_compile` 클린.**

확정된 것들을 코드로 옮긴 방식:
- **raw 미저장(§6-2 확정)**: `fetch_and_calculate()`가 FDR 시계열을 받아 계산만 하고 버립니다. 저장되는 건 계산이 끝난 결과값(`indicator_kr_history.csv`)뿐.
- **500종목 선정(§6-1 확정)**: `kr_all_market_prices.json`(시가총액 순서 보존)에서 `kr_ticker_master.json`의 `type=="STOCK"`만 걸러 상위 500 선정 — ETF가 안 섞이게.
- **종목 하나 실패해도 전체 안 막힘(§5-3)**: 종목별 try/except, 실패 목록은 로그에만 남고 나머지는 정상 기록.
- **교차검증(§5-1)**: 오늘 네이버 당일가(이미 갖고 있는 값)와 FDR 마지막 종가를 대조, 5%p 넘게 벌어지면 로그로만 경고(값은 안 고침). ⚠️ 1차 구현은 콘솔 로그 + 요약 카운트까지만이고, 종목별 불일치를 파일 컬럼으로 영구 기록하는 건 후속 과제로 남김.
- **신선도 경고**: `kr_all_market_prices.json`이 오늘 자가 아니면 경고만 찍고 계속 진행(지표 계산 자체는 이 파일에 의존하지 않으므로 차단할 이유는 없음 — 영향은 유니버스 후보 목록·교차검증 기준가 신선도뿐).
- **저장 산출물**: `data/indicator_universe_kr.json`(유니버스), `data/indicator_kr_history.csv`(일별 누적, 다운로드용), `data/indicator_kr_latest.json`(오늘자 스냅샷, 3단계 화면이 빠르게 쓸 수 있게).

**✅ 2026-08-25 실운영 검증 완료** — `indicator_kr.yml`을 오너가 실제로 2회 실행:
1. `limit=20` 1차 실행에서 `KeyError: 'reason'` 발견(`append_daily_history()`가 안 돌려주는 키를 마지막 출력 줄에서 읽으려 함) → 즉시 수정 + 회귀 테스트 2개 추가(전체 `run()`을 모킹된 FDR로 끝까지 실행하는 통합 테스트, 총 53개 통과) → 재실행으로 수정 확인.
2. **`limit=500` 본운영 실행 — 성공 500 / 실패 0 / 교차검증 불일치 0건, 소요 11분 52초** (0단계 실측치 12분 52초와 거의 일치). `data/indicator_universe_kr.json`·`data/indicator_kr_latest.json`·`data/indicator_kr_history.csv` 3개 파일 모두 정상 생성·커밋 확인. git push 3회 재시도 로직도 실전에서 실제로 동작(동시 커밋 충돌 → 자동 rebase 재시도 → 성공) 확인.

### ✅ 3단계 — 화면(`/indicator`), 관리자 전용 플래그로 먼저 — 코드 작성 완료 (2026-08-25, 실기기 확인 전)

결정론적 판정 + 3지표 원값 + 경고 배너(상하단) + 다운로드. **AI 없이 여기까지 완성해서 세운다.** 새로 생긴/바뀐 파일:

| 파일 | 역할 |
|---|---|
| `web/pages/indicator_page.py` | 새 화면 본체. 검색(종목명/코드) → 종합판정 + RSI/MACD/볼린저 카드 → 상하단 강한 경고 배너 → 다운로드 |
| `web/layout.py` | `INDICATOR_ENABLED`/`INDICATOR_MENU_ADMIN_ONLY` 스위치 추가(배당금 모듈과 완전히 같은 패턴), 사이드바 메뉴 그룹 추가 |
| `main.py` | `web.pages.indicator_page` import 등록 |

구현 방식:
- **기존 화면 관례 그대로 재사용(§0-3-10)**: `web/pages/dividend_page.py`의 게이트 패턴(`if not X_ENABLED: warning_banner(...); return`), `web/components/widgets.py`의 `error_banner`/`warning_banner`/`info_banner`(줄바꿈 `\n`→`<br>` 자동 처리 — §0-3-13 요구사항을 새 코드 없이 그대로 충족), `web/components/stock_download.py::render_stock_download_tool()`(종목별 CSV/JSON 다운로드, pegy·us_stocks와 동일 도구)를 그대로 가져다 씀. 이 화면만의 새 코드는 카드 렌더링과 검색 필터뿐.
- **상하단 강한 경고(§1 확정 문구)**: `STRONG_WARNING_TEXT` 상수 하나를 화면 맨 위(검색창 앞)와 맨 아래(다운로드 도구 뒤) 두 번 그대로 호출 — 문구가 어긋날 일이 없음.
- **판정에 색을 입히지 않음**: 매수 우위=초록/매도 우위=빨강 같은 방향성 색을 의도적으로 안 씀 — "판단 근거로 쓰지 말라"는 경고와 정면으로 배치되는 시각 신호를 주지 않기 위해서(§0-1 취지 확장 적용, 오너 승인 필요한 판단이었음 — 아래 참고).
- **산출 불가 지표는 사유를 그대로 노출(§0-1)**: `unavailable_reasons`("RSI:종가 14봉 보유, 최소 15봉 필요" 형태)를 파싱해 해당 지표 자리에 값 대신 노란색 "⚠️ 산출 불가 — 사유" 문구로 표시. 값을 비우거나 0/중립으로 채우지 않음.
- **오프라인 검증**: 로컬 테스트 하네스에 실제 repo의 `web/`·`utils/` 패키지를 그대로 옮겨 `import` 성공 확인, `INDICATOR_ENABLED` on/off에 따른 메뉴 노출 여부 확인, 정상/부분산출불가 두 케이스로 카드 렌더링 함수(`_render_stock_card`)를 NiceGUI 위젯을 가짜로 바꿔치기해 예외 없이 도는 것까지 확인. **NiceGUI 서버를 실제로 띄운 브라우저 확인은 아직 안 함** — 아래 "오너가 해야 할 것" 참고.

**오너가 해야 할 것 (배포 전 필수)**:
1. `git add`/`commit`/`push` (아래 명령 참고)
2. Render 대시보드 → Environment → `INDICATOR_ENABLED=true` 추가(코드에는 `INDICATOR_MENU_ADMIN_ONLY=True`가 이미 박혀 있어, 이 값을 켜도 관리자 계정으로 로그인해야만 메뉴/화면이 보임 — 배당금 모듈과 동일한 2단계 상태)
3. 배포 후 관리자로 로그인 → `/indicator` 접속 → 실제 종목(예: 삼성전자/005930) 검색 → 카드·다운로드 버튼 눈으로 확인
4. 판정에 색을 안 입힌 것, 경고 문구 배치·줄바꿈이 의도대로 보이는지 특히 확인해 주세요 — 이건 코드로 자동 검증할 수 없는 부분입니다.

### 4단계 — AI 해설 레이어 (선택, §4-2 캐싱 구조 적용)

이 단계를 통째로 건너뛰어도 모듈은 완전하게 동작해야 함(§0-3-11의 진짜 의미).

### 4단계 — AI 해설 레이어 (선택, §4-2 캐싱 구조 적용)

이 단계를 통째로 건너뛰어도 모듈은 완전하게 동작해야 함(§0-3-11의 진짜 의미).

### 5단계 — 오너 실기기 확인 → 공개 (§0-3-6 3단계 절차: admin-only → 실기기 검증 → 전체 공개)

---

## 8. 확인하지 못한 것 (정직하게 명시)

- FDR 500종목 실측 응답 시간·안정성 (샌드박스 네트워크 제약으로 미실측, §7 0단계 필요)
- `data/kr_all_market_prices.json`의 실제 정렬·컬럼 구조
- `web/layout.py`의 실제 스테이징 플래그 명명 관례(배당 모듈의 `DIVIDEND_ENABLED` 패턴을 따를 것으로 추정하나 미확인)
- 이 문서는 이 세션(검토 전용 채팅)에서 작성된 것으로, 실제 코드 작업은 아직 시작되지 않았음
