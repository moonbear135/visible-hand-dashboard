# 📊 "내 성적표" v2 — 스크린샷 업로드 + AI OCR 진행 스케줄 (초안)

> 이 문서는 다른 세션·다른 모델이 맥락 없이 읽어도 착수할 수 있도록 만든 진행 스케줄 초안입니다.
> `SCORECARD_WORK_ORDER.md`(v1 지시서), `MY_SCORECARD_MODULE_NOTES.md`(원안 검토 메모),
> `ENGINEERING_SPEC.md`(전체 원칙)를 근거로 작성했습니다. **실제 착수 전 오너 확인이 필요한
> 항목은 각 단계에 🔴로 표시했습니다 — 이 문서만 보고 바로 코딩 시작하지 말고, 0단계부터
> 순서대로 확인하세요.**

---

## 0. 왜 이 순서인가

v1(수동 입력)은 이미 완료·공개 전환까지 끝났습니다. v2는 입력 방법을 하나 추가하는 것뿐이라 —
로그인, holdings 테이블, 가중평균 재계산, 원형차트, PEGY 연동 — **기존 인프라는 전부 재사용**합니다.
새로 필요한 건 "이미지 → 종목/수량/매입가 텍스트 추출" 한 조각뿐입니다. 그래서 스케줄도 이
한 조각에 필요한 선행조건(오너 액션) → 설계 확정 → 코딩 → 오프라인 검증 → 실검증 → 공개전환
순서로 짰습니다.

**이미 확보돼 있어 다시 안 만들어도 되는 것** (v1·이후 작업에서 완료됨):
- 여러 계좌 통합·가중평균 재계산 로직(`utils/scorecard_db.py`) — OCR 결과도 이 함수에 그대로 넣으면 됩니다.
- 코스피 상위200/미국 상위550 **밖** 종목의 현재가 조회 — `data/kr_all_market_prices.json`
  (코스피+코스닥 전체), `data/us_all_market_prices.json`(미국 상장 전체), `data/us_all_etf_prices.json`
  (미국 ETF 전체) 세 파일이 이미 존재·검증 완료(#84·#92·#93). 즉 OCR로 사용자가 유니버스 밖
  종목을 스캔해도 현재가는 이미 조회 가능합니다 — v1 문서에 "범위 밖"이라 적힌 건 낡은 정보입니다.
- Gemini API 호출 패턴(`utils/macro_ai.py`) — `GEMINI_API_KEY` 환경변수 읽기, 미설정 시 안전하게
  건너뛰기, 모델 폴백(2.5→1.5) 구조를 그대로 참고하면 됩니다.

---

## 1단계 — ✅ 완료(2026-08-17) — 오너 선행 조치

이건 코드로 할 수 없는 행정 작업이었습니다. 완료됐으니 이제 5단계(실검증)로 진행 가능합니다.

1. **✅ 완료 — Gemini Developer API 결제 활성화.** `aistudio.google.com`에서 기존
   `visiblehand` 프로젝트(AI 코멘트용 `GEMINI_API_KEY`와 같은 프로젝트, 재사용 권장대로 됨)에
   Cloud Prepay 결제 연결 완료. ₩25,000 선불 충전, 잔액 ₩25,000 미만 시 자동 재충전,
   월 한도 ₩25,000. 본인인증 단계에서 "이름을 확인하고 다시 시도" 오류가 났었는데, 원인은
   `pay.google.com`의 결제 프로필 이름·주소가 실제 본인 정보와 안 맞아서였음 — 결제 프로필을
   맞춰 넣은 뒤 통과. (참고: 이 프로젝트는 선불만 가능했음 — 공식 문서상 후불도 있다고 나와
   있지만 실제로는 계정별로 선불만 뜨는 경우가 있는 듯. 문제 없음, 진행에 지장 없음.)
2. **Zero Data Retention(ZDR) 신청 — 참고: 2026-08-17 확인 결과 개인 개발자에게는 공개된 신청
   절차가 없고 사실상 엔터프라이즈 전용에 가깝습니다(구글 공식 포럼 다수 문의에도 공개 답변 없음).
   유료 티어 자체가 이미 "프롬프트·응답을 제품 개선에 안 씀"을 보장하므로, ZDR을 필수 관문으로
   두지 않습니다 — 신청 채널이 열리면 병행 시도하되 못 받아도 진행합니다.**
3. **모델 확정**: ~~`gemini-2.5-flash-lite`~~ → **`gemini-3.5-flash-lite`로 정정
   (2026-08-17 실검증 중 발견)** — 원래 모델이 신규 프로젝트에 더 이상 제공되지 않아 Render
   로그에서 404 NotFound 확인, 구글이 안내한 대체 모델로 교체(입력 $0.30/출력 $2.50/백만
   토큰 — 기존보다 조금 올랐지만 여전히 저렴, ENGINEERING_SPEC.md §0-3-12 적용 사례).

> **다음 세션에서 제일 먼저 확인할 것**: 위 1번이 끝났는지. 안 끝났으면 2~4단계(설계·코딩·오프라인
> 테스트)까지는 진행해도 되지만 5단계(실검증)는 막힙니다.

---

## 2단계 — 설계 확정 (오너 확인 완료, 2026-08-17)

1. **업로드 전 안내 문구** — 업로드 버튼 바로 위에 상시 노출(체크박스 동의 없이도 항상 보임),
   "계좌번호·잔고 등은 폰 기본 마크업 기능으로 가려주세요" + 원본 이미지 미저장 고지.
2. **잘린 스크린샷 재질문 UX** — 부분 실패 항목만 기존 수동 입력 폼으로 넘겨 사용자가 직접
   채우게 함(새 UI 안 만듦).
3. **OCR 추출 스키마** — 종목명/수량/매입가 3개만(§0-1 원칙 — 현재가는 절대 OCR값을 안 씀):
   ```json
   {"items": [{"raw_name": "삼성전자", "quantity": 10, "avg_price": 71000, "confidence": "high|low"}]}
   ```
   `confidence: low`인 행은 자동 반영하지 않고 사용자 확인을 거치게 합니다.
4. **자체 마스킹(이미지 편집) 도구는 만들지 않음** — 일반 사용자에게 편집 도구 제공은 구현
   난이도가 높아 범위 밖으로 결정(2026-08-17). 대신 위 1번의 OS 기본 마크업 안내 + 원본 이미지
   즉시 폐기로 대체.
5. **화면 촬영 시각 EXIF 메타데이터로 "신뢰도"를 올리는 아이디어는 채택하지 않음** — 메신저를
   거치면 대부분 메타데이터가 사라지고, 남아있어도 일반인도 쉽게 위조 가능해 신뢰도 신호로는
   부적합(§0-3-9와 반대 방향). 이 기능은 개인 전용 화면이라 위조 유인 자체가 낮다는 점도 고려.
6. **외부 AI provider는 항상 교체 가능하게** — `ENGINEERING_SPEC.md` §0-3-11 신설(2026-08-17,
   오너 지시). 특정 회사에 종속되지 않는 함수 경계로 설계.

---

## 3단계 — 코드 구현 (오프라인 가능, API 키 없어도 모킹으로 진행)

우선순위 순. 기존 파일(`views/pegy_view.py`, `views/us_stocks_view.py`, scorecard v1 파일들의
기존 로직)은 그대로 두고 아래만 추가/확장합니다.

0. **provider 교체 가능 설계 — `ENGINEERING_SPEC.md` §0-3-11 준수 필수.**
   AI 호출 부분을 특정 회사 이름이 아니라 "무엇을 하는지"로 부르는 함수 하나
   (`extract_holdings_from_image(image_bytes)`) 뒤에 감춥니다. 실제 provider(지금은 Gemini
   하나뿐)는 그 함수 안에서만 갈리고, 어느 걸 쓸지는 환경변수(`OCR_PROVIDER=gemini`)로
   정합니다. 지금 다른 provider(GPT/Grok 등)를 미리 구현하라는 뜻이 아니고, 나중에 바꿀 때
   이 함수 경계 안쪽만 고치면 되게 자리를 남겨두라는 뜻입니다(§0-3-10 YAGNI와 충돌 없음).
1. **`utils/scorecard_ocr.py`(신규)** — 위 0번의 `extract_holdings_from_image()` 함수를
   이 파일에 두고, 내부에서 Gemini 클라이언트 초기화(`macro_ai.py` 패턴 재사용,
   `try/except ImportError` + `GEMINI_API_KEY` 미설정 시 `None` 반환), 이미지 1장을 위 스키마로
   파싱합니다. **현재가는 절대 이 모듈이 반환하지 않습니다** — 종목명/수량/매입가만 반환.
2. **`web/pages/scorecard_page.py` 확장** — NiceGUI `ui.upload`로 이미지 업로드 위젯 추가, OCR
   결과를 기존 수동 입력 폼에 **프리필**(자동 저장 금지 — 사용자가 검토·수정 후 직접 "저장" 눌러야
   반영). `confidence: low` 행은 시각적으로 강조(예: 노란 배지)해서 재확인 유도.
3. **현재가 재조회 배선** — OCR이 추출한 종목명을 기존 종목 조회 함수(코스피/코스닥/미국 전종목
   매칭 로직 — v1·#83·#84·#92·#93에서 이미 구현된 `resolve_stock_query()`류 함수)에 그대로 넘겨
   현재가·PEGY 데이터를 붙입니다. **OCR이 준 숫자를 현재가로 쓰는 코드 경로가 하나라도 있으면
   §0-1 위반** — 리뷰 시 반드시 확인.
4. **가중평균 재계산 연동** — OCR로 추가된 종목도 기존 `utils/scorecard_db.py`의 가중평균 함수를
   그대로 통과하게(같은 종목 중복 입력 시 동일하게 병합). 새 로직 불필요, 배선만.
5. **`requirements.txt`** — `google-generativeai`(주석 처리됨)를 활성화하되 `try/except
   ImportError` 가드로 패키지 없어도 앱이 안 죽게(기존 optional dependency 패턴 유지). 새 기능도
   기본 숨김(`SCORECARD_OCR_ENABLED=False`)이므로 이 패키지가 없어도 앱 전체는 항상 정상 작동.

---

## 4단계 — 오프라인 테스트 (`tests/test_scorecard_ocr.py` 신규)

API 키 없이도 전부 가능 — Gemini 클라이언트를 모킹해서:
- 정상 응답(다건 종목) 파싱
- `confidence: low` 행이 자동 저장 안 되고 확인 대기로 표시되는지
- 빈 응답/파싱 실패 시 에러가 사용자에게 명확히 보이는지(조용히 무시 금지, §0-1)
- OCR 결과가 현재가 필드를 채우지 않는지(회귀 테스트로 고정 — 나중에 실수로 채워지면 바로 잡히게)
- `SCORECARD_OCR_ENABLED=False`일 때 업로드 위젯이 안 보이는지
- 기존 스위트(scorecard 포함) 전부 회귀 없음

---

## 5단계 — 🔴 실검증 (진행 중, 2026-08-17 착수)

1. Render 환경변수에 `GEMINI_API_KEY` 확인(이미 있으면 생략).
2. 실제 브로커 앱 스크린샷 2~3종(포맷 다른 것으로, 예: 증권사 앱 캡처 + 모바일 웹 캡처)으로
   업로드 테스트 — 정상 인식, 부분 잘림, 완전 실패 시나리오 각각 1회 이상.
3. 인식된 매입가가 OCR값이 아니라 우리 시세 데이터에서 온 현재가와 실제로 다른 필드인지 화면에서
   육안 확인(가장 중요한 확인 포인트).

### 5단계 진행 상황 (2026-08-17~18)

**✅ 이미 확인된 것**
- **정상 인식 시나리오 통과** — 오너가 실제 증권사 앱 스크린샷(ISA중개형 주식잔고, 연금저축
  ETF 잔고, 금현물 잔고)을 `visiblehand.co.kr/scorecard`에 업로드해 종목명·수량·매입가가
  실제로 추출되는 것을 확인했습니다.

**🔧 실검증에서 발견해 고친 결함 2건**
1. **모델 404** — 원래 넣었던 `gemini-2.5-flash-lite`가 신규 프로젝트에 더 이상 제공되지
   않아 Render 로그에서 `404 NotFound` 확인 → 구글이 안내한 `gemini-3.5-flash-lite`로 교체.
   (§0-3-12 외부 의존성 능동 점검 적용 사례. 위 1단계 3번에도 기록됨.)
2. **여러 장 연속 업로드 시 이전 인식 결과가 사라짐** — 스크린샷 3장을 한 장씩 연달아 올리면
   먼저 처리된 장의 결과가 화면에서 사라지고 마지막 장 결과만 남았습니다. 원인은 업로드마다
   `extracted_box.clear()`로 결과 목록을 통째로 지우고 "이번 장 결과만" 다시 그린 것.
   → 인식 결과를 누적 목록(`extracted_items`, `@ui.page` 지역 변수라 접속마다 분리 — §0-3-8)에
   쌓고, **실패 배너(`error_slot`)와 남은 횟수(`quota_label`)를 각각 전용 자리로 분리**해
   해결했습니다. 실패 배너 하나 때문에 이미 성공한 이전 장의 결과가 지워지던 경로도 함께
   사라졌습니다. 회귀 테스트 `test_multiple_uploads_accumulate_instead_of_replacing` 추가.
   (병합 커밋 `580bc95`. `pytest tests/test_scorecard_ocr.py tests/test_scorecard.py` 74개 통과.)

**✅ 남은 실검증 3건 — 전부 완료 (2026-08-18, Cowork 세션 실기기 테스트)**
- **부분 잘림/저확신 스크린샷 시나리오** ✅ — 실제 업로드에서 스페이스X·EDV·SOL AI반도체 등
  일부 행이 노란 테두리 + `⚠️ 확인 필요` 배지로 강조되는 것을 오너가 실제 화면 스크린샷으로
  확인. `confidence: low` 판정과 배지 렌더링이 실사용 조건에서 정상 동작함을 확인.
- **완전 실패 스크린샷 시나리오** ✅ — 보유 종목이 없는 화면(일반 스크린샷)을 여러 차례
  업로드해, 조용히 넘어가지 않고 사람이 읽을 실패 문구가 뜨는 것을 오너가 반복 확인.
- **매입가 vs 현재가 육안 확인** ✅ — 여러 실제 브로커 앱 스크린샷(ISA중개형, 연금저축 ETF,
  금현물 등)을 올려 화면에 보이는 매입가(OCR값)와 현재가(시세 데이터, 예: TIGER 미국배당
  다우존스 8/18 종가 15,345원)가 서로 다른 필드에서 온 게 맞는지 오너가 직접 대조·확인.
  **5단계에서 가장 중요한 확인 포인트였는데, 완료됐습니다.**

**➡️ 5단계(실검증) 완전히 종료.** 아래 6단계로 넘어갑니다.

**✅ 알려진 잔여 결함 — 전부 처리 완료 (2026-08-18)**

아래는 코드로 정리했습니다(`pytest tests/test_scorecard_ocr.py tests/test_scorecard.py`
74 → **76개 통과**, 신규 회귀 테스트 2건 포함). **5단계 자체가 끝난 것은 아닙니다** — 바로 위
"아직 안 해본 것" 3건은 여전히 오너의 실기기 확인이 필요합니다.

- ✅ **실패 시 잔여 횟수 표시** — 업로드가 실패해도 한도는 이미 1회 차감된 상태인데
  (`consume_ocr_quota`가 유료 호출 *전에* 차감), 실패 경로에서 `quota_label`을 갱신하지 않아
  화면에 **실제보다 1 많은 숫자**가 남던 문제(§0-1 — 화면이 사실과 달라짐). `except OcrError`
  분기에서 갱신하도록 고쳤고, 문구 조립은 새 지역 함수 `_set_quota_label()` **한 곳**으로 모아
  성공/실패 경로가 서로 어긋날 수 없게 했습니다(§0-3-10).
  ⚠️ `except ScorecardError`·`except Exception` 분기는 **그대로 뒀습니다** — `quota`가 아직
  바인딩 안 됐을 수 있어 건드리면 `UnboundLocalError`이고, 그 경로들은 애초에 차감이 없었으므로
  화면 숫자가 이미 맞습니다. 이 "건드리지 않음"도 회귀 테스트로 고정했습니다.
- ✅ **이미지가 아닌 파일도 한도를 소모하던 문제** (2026-08-18 추가 발견) — 형식 판별이
  `extract_holdings_from_image()` **안쪽**(= 한도 차감 뒤)에서만 일어나서, PDF 같은 파일을
  올리면 외부 API로는 아무것도 안 나가는데(돈은 안 나감) 한도만 1회 깎였습니다. 이는
  `consume_ocr_quota` 독스트링이 이미 약속한 *"유료 호출이 없었던 업로드는 한 번으로 세지
  않는다"* 와 어긋나는 상태였습니다. `utils/scorecard_ocr.py`에 공개 함수
  `ensure_supported_image_format()`을 두고 화면이 **한도 차감 전에** 먼저 부르도록 순서를 바꿔
  해결했습니다(판정 로직은 기존 `_sniff_mime_type()` 그대로 재사용 — 기준이 두 벌이 되지 않게).
- ✅ **부정확한 주석 정정** — `web/pages/scorecard_page.py`의 원인 설명 주석이 *"`ui.upload`가
  여러 장 동시 선택을 허용해서"* 라고 적혀 있었으나, NiceGUI 3.16 `Upload.__init__` 확인 결과
  `multiple` 기본값은 `False`라 사실이 아니었습니다. 실제 재현 경로는 **한 장씩 연달아 업로드**
  이며 그렇게 정정했습니다(§0-1 — 주석도 사실이어야 합니다). 코드와 기존 회귀 테스트는 그대로
  유효하고 바뀐 건 원인 설명뿐입니다.
- ✅ (부수) `ENGINEERING_SPEC.md` §0-3-11 "첫 적용 사례"에 남아 있던 옛 모델명
  `gemini-2.5-flash-lite`를 실제 값 `gemini-3.5-flash-lite`로 갱신했습니다.

---

## 6단계 — ✅ 스테이징 → 오너 승인 → 공개 전환 (완료, 2026-08-18)

5단계 실검증 3건이 전부 통과해 오너가 공개 전환을 승인했습니다. Render Environment의
`SCORECARD_OCR_ENABLED` 값을 `true`로 켜서(2026-08-18, 오너 직접 확인) 업로드 위젯이
`/scorecard`를 방문하는 누구에게나 보이는 상태입니다. `web/layout.py`의 `_MENU`는 이미
`/scorecard`가 공개 상태였으므로 메뉴 추가 작업은 없었습니다.

**➡️ "내 성적표" v2(스크린샷 OCR) 전체 완료.** 1~6단계 모두 끝났습니다.

---

## 참고 — 이번엔 명시적으로 범위 밖 (v2에서 하지 않음)

- 로그인 없이 백그라운드 수익률 누적, 주/월/분기/반기/연 리포트, 벤치마크 비교 — **이미 v3(사장님
  보고서)로 별도 완료됨**, 이 스케줄과 무관.
- "1년 미접속 시 데이터 자동 삭제" 실제 배치 — 여전히 v1 안내 문구만, 이번에도 착수 안 함.
- 자체 이미지 마스킹(가리기) 편집 도구 — 위 2단계 4번 참고.
- 스크린샷 EXIF 메타데이터 기반 신뢰도 판정 — 위 2단계 5번 참고.

---

## 다음에 이 문서를 다시 열 때

**2026-08-18 기준 진행 상황**: **1~6단계 전부 완료. "내 성적표" v2(스크린샷 OCR) 정식 공개
전환까지 끝났습니다.**

- 1단계(오너 API 결제 설정) ✅ 완료 — Gemini Developer API 유료 등급 활성화, 모델
  `gemini-3.5-flash-lite`로 확정.
- 2~4단계(설계 확정·코드 구현·오프라인 테스트) ✅ 완료 — 하루 15회 업로드 제한 포함
  (오너 결정으로 100 → 15 → 200 → 최종 15로 조정됨).
- 5단계(실검증) ✅ **완료** — 정상 인식 시나리오, 부분 잘림/저확신 시나리오, 완전 실패
  시나리오, 매입가 vs 현재가 육안 대조까지 오너가 전부 실기기로 확인했습니다. 검증 과정에서
  발견한 결함(모델 404, 여러 장 연속 업로드 시 결과 소실, 남은 횟수 표시 오차, 이미지 아닌
  파일의 한도 소모, 스페이스X 등 미국주식 한글명 검색 실패)은 전부 고쳐 회귀 테스트로
  고정했습니다.
- 6단계(스테이징 → 오너 승인 → 공개 전환) ✅ **완료** — Render `SCORECARD_OCR_ENABLED=true`
  로 확인, 업로드 위젯이 누구에게나 보이는 정식 공개 상태입니다.

**다음 세션에서 이 모듈을 다시 열 때는 새 버그 제보나 v3 이후 로드맵 논의부터 시작하면
됩니다** — 이 문서에 남은 v2 작업은 없습니다.

> ⚠️ **작업 시작 전에 반드시 `git pull`.** 2026-08-18 이 파일들(`web/pages/scorecard_page.py`,
> `tests/test_scorecard_ocr.py`)을 서로 다른 AI 세션 두 곳이 거의 동시에 고쳐 병합 충돌이
> 났습니다. 오너가 여러 창을 오가며 쓰므로, 코드에 손대기 전에 로컬이 origin 최신인지 먼저
> 확인하세요 — 자세한 규칙은 `PROJECT_STATUS.md` §7-1 참고.

**📌 오너 결정(2026-08-17): `google-generativeai` → `google-genai` 마이그레이션은 5단계
실검증이 끝나고 안정된 뒤에 진행합니다(안전 우선).** 검증 전에 미리 옮기지 마세요 — 자세한 배경은
`ENGINEERING_SPEC.md` §0-3-12, `PROJECT_STATUS.md` §0-5-6 참고.
