"""
utils/constants_us.py
🇺🇸 미국주식("미국 주식은 이가격") 페이지 전용 상수 단일 출처 (Single Source of Truth)

⚠️ 왜 utils/constants.py 를 재사용하지 않고 파일을 새로 만들었나
   `utils/constants.py` 의 값들은 **코스피 시장 분포에 맞춰 캘리브레이션된 값**입니다
   (예: TARGET_PER_CAP=25배는 "코스피 PER 중앙값 10~13배의 약 2배"라는 근거로 정해짐).
   미국 시장은 평균 PER·ROE 수준 자체가 달라 그대로 쓰면 전 종목이 한쪽으로 쏠립니다.
   US_STOCKS_WORK_ORDER.md §5-1 지시대로 파일을 분리하고, 값은 처음엔 전부 **잠정값 + TODO**로
   두었다가 2026-08-07 실측 분포로 확정했습니다(아래 ✅ 항목 참고).

⚠️ ENGINEERING_SPEC §0-1: 여기 있는 값은 전부 "실데이터가 맞는지 판별/스케일링하기 위한
   기준선"이며, 실데이터를 대체하는 더미 기본값이 아닙니다.

📌 원래 계획했던 확정 절차: 550종목 1회 전수 수집 → 실제 횡단면 분포(중앙값/사분위)를 출력 →
   그 숫자를 근거 주석과 함께 여기에 반영. 아래 ✅ 대로 2026-08-07에 완료했습니다.

✅ 2026-08-07 밤: 첫 550종목(정확히는 548종목, CCZ·BTSGU 개별 실패) 전수 수집 실측 완료
   (`data/us_stocks_latest.json`, 2026-08-07 00:36 ET, valid_ratio 99.8%). 아래 §5 임계값들을
   실제 횡단면 분포(중앙값/p75/p90/p95)를 근거로 재캘리브레이션했습니다 — 항목별로 "✅ 확정"
   표기와 실측 수치를 남깁니다.

✅ 2026-08-08: 위 임계값 확정 + GitHub Actions 무인 수집(`.github/workflows/scrape_us.yml`)
   연결이 모두 끝나 오너 승인으로 미국주식 화면을 **공개 전환**했습니다(§0-3-6 절차 종료).
   화면 상단의 "🧪 베타 · 검토중" 배너도 이때 함께 내렸습니다.
   ⚠️ 다만 §5 값들은 "한 시점의 횡단면 분포"를 근거로 정한 것이라 영구 고정값이 아닙니다 —
   데이터가 몇 달치 쌓이면 같은 방식으로 한 번 더 점검하는 것을 권장합니다.
"""

# =============================================================================
# 0. 데이터 소스 (US_STOCKS_WORK_ORDER.md §4)
# =============================================================================
# 종목 유니버스: 무료·무인증·매일 자동 갱신(제3자 개인 프로젝트, NASDAQ Stock Screener 기반)
US_UNIVERSE_CSV_URL = (
    "https://raw.githubusercontent.com/Ate329/top-us-stock-tickers/main/tickers/all.csv"
)
# 펀더멘털: 종목별 개별 페이지 (데이터 원천은 S&P Global Market Intelligence)
US_STATISTICS_URL_TEMPLATE = "https://stockanalysis.com/stocks/{ticker}/statistics/"

# 유니버스 CSV의 필수 컬럼 (하나라도 없으면 소스 구조 변경으로 보고 수집 중단)
US_UNIVERSE_REQUIRED_COLUMNS = ("symbol", "name", "price", "marketCap")

# =============================================================================
# 1. 유니버스 크기 / 히스테리시스 버퍼
# =============================================================================
# 오너 마인드맵 원안: 나스닥+뉴욕 시가총액 상위 약 550종목.
US_TARGET_UNIVERSE_SIZE = 550
# 히스테리시스(진입 550위 / 이탈 600위) — collector_kospi200.py 의 200/230 패턴을
# 그대로 옮긴 것입니다(진입:이탈 = 1 : 1.09 비율도 코스피와 동일 수준).
US_HYSTERESIS_ENTRY_RANK = 550
US_HYSTERESIS_EXIT_RANK = 600

# 소스 건전성 가드레일 (§4-1 주의사항: 제3자 저장소의 자동 갱신이 언젠가 멈출 수 있음).
# 필터로 제외되는 채권·우선주가 상위권에도 섞여 있으므로 원본 행 수는 목표치보다 넉넉해야 합니다.
# 이 값 미만이면 "CSV가 깨졌다"로 보고 수집을 중단하고 기존 스냅샷을 유지합니다.
US_UNIVERSE_MIN_RAW_ROWS = 700

# =============================================================================
# 2. 크롤링 매너 (ENGINEERING_SPEC §0-3-2) — 코스피와 동일 수준 이상 유지
# =============================================================================
US_CRAWL_DELAY_MIN_SEC = 2.0   # collector_kospi200.py 의 random.uniform(2.0, 3.0) 과 동일
US_CRAWL_DELAY_MAX_SEC = 3.0
US_REQUEST_TIMEOUT_SEC = 10.0
US_MAX_RETRIES = 3             # 일시적 네트워크 오류 흡수용. '차단'에는 재시도하지 않음(아래 참고)
US_RETRY_BASE_DELAY_SEC = 1.0  # 지수 백오프 기준 (1s → 2s → 4s + 지터)
# 아래 응답 코드는 "상대 서버가 우리를 막았다"는 신호이므로 재시도를 반복하지 않고
# 즉시 실패로 기록하고 중단합니다 (§0-3-2: 조용히 우회 시도 금지).
US_BLOCK_STATUS_CODES = (401, 403, 429, 503)

# 2026-08-07: 오너 로컬 전수수집 실측에서 187종목째(약 11분 경과)에 stockanalysis.com 이
# HTTP 429 로 차단 — 종목당 슬립(2~3초)만으로는 부족했습니다. §0-3-2 원칙상 차단된 요청을
# 재시도하지는 않되(그대로 유지), 애초에 차단당할 확률을 낮추기 위해 일정 종목마다 더 긴
# '휴지기'를 추가로 둡니다. ⚠️ stockanalysis.com 의 정확한 rate limit 정책(임계값·시간창)은
# 공개돼 있지 않아 이 값은 추정 완화책이지 "재발 방지 보장"이 아닙니다 — 재발하면 배치
# 크기를 더 줄이거나 휴지기를 늘려야 합니다.
US_CRAWL_BATCH_SIZE = 40          # 이 개수마다 한 번 더 쉽니다
US_CRAWL_BATCH_COOLDOWN_SEC = 120  # 배치 사이 추가 휴지기(초)
# 2026-08-07 2차 실측: 위 값(60/90초)으로도 290종목째(187종목 때보다는 개선됐지만 여전히 550
# 미만)에서 다시 HTTP 429 — 정확한 rate limit 규칙을 모르는 채로 값만 계속 키우는 건 도박이라,
# 아래 '이어하기(체크포인트)' 기능을 주된 대응책으로 삼고 이 값은 보조 완화책으로만 소폭 조정.
US_COLLECT_CHECKPOINT_FILENAME = "us_collect_checkpoint.json"  # 차단 시 진행상황 저장(같은 세션 날짜에서만 재사용)
# 우리가 누구인지 숨기지 않되, 일반 브라우저와 동일한 UA를 사용합니다.
US_USER_AGENT = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
)

# =============================================================================
# 3. 타임존 / 수집 시점 (US_STOCKS_WORK_ORDER.md §5-2 — 이번 프로젝트 최대 함정)
# =============================================================================
US_MARKET_TIMEZONE = "America/New_York"   # 서머타임(EDT)/표준시(EST) 자동 반영
US_MARKET_CLOSE_HOUR = 16                 # 정규장 마감 4:00 PM ET
US_MARKET_CLOSE_MINUTE = 0
# 오너 요구사항: "장마감 + 30분" 시점 수집 (마감 직후 데이터 정리 시간을 감안)
US_COLLECT_AFTER_CLOSE_MINUTES = 30

# =============================================================================
# 4. 교차검증 허용 오차 (ENGINEERING_SPEC §0-3-3 다중 출처 원칙)
# =============================================================================
# 유니버스 CSV(NASDAQ 스크리너 기반)와 stockanalysis.com(S&P Global 기반)의
# 시가총액을 대조합니다. 두 소스의 갱신 시점이 달라 정확히 같을 수는 없으므로,
# "같은 회사를 보고 있는가"를 확인하는 수준의 느슨한 허용 오차입니다.
# 이 범위를 넘으면 값을 버리지 않고 discrepancy 를 기록해 화면에 표시합니다.
US_MARKETCAP_CROSSCHECK_TOLERANCE = 0.15   # ±15%
US_PRICE_CROSSCHECK_TOLERANCE = 0.15       # ±15%

# =============================================================================
# 5. 밸류에이션 임계값 — 2026-08-07 밤 실측 550종목(548종목) 분포로 재캘리브레이션 완료
# =============================================================================
# 실측 분포 참고치(548종목 시각화 대상, f_per/t_roe/roic/beta/f_pegy/growth 순 — 전부
# data/us_stocks_latest.json 2026-08-07 00:36 ET 스냅샷 기준):
#   Forward PER   : 중앙값 20.2 / p75 28.8 / p90 41.4 / p95 52.5 / p99 158.8 / 최댓값 490.7(WBD 1건)
#   Trailing ROE  : 중앙값 16.0% / p25 9.2% / p75 30.7%
#   ROIC          : 중앙값 11.5% / p25 6.2% / p75 20.7%
#   베타(5Y)       : 중앙값 0.96 / p90 1.70 / p95 2.02
#   Forward PEGY  : 중앙값 1.40 / p75 2.02 / p90 2.81 / p95 3.99
#   3년 EPS 성장률 : 중앙값 12.7% / p95 47.5% / p99 90.4%

# 데이터 오염 판정선(Forward PER). 실측 결과 이 값을 넘는 종목은 548개 중 WBD 1건뿐 —
# "데이터가 깨졌다"가 아니라 "숫자 자체가 무의미할 만큼 극단적"인 경우만 정확히 걸러내는
# 걸로 확인돼 코스피와 같은 300배를 그대로 확정합니다. ✅ 확정(2026-08-07 실측)
US_PER_EXTREME_MAX = 300.0

# ✅ 확정(2026-08-07 실측, 오너 결정): PEGY 하드컷오프.
# 애초 피터 린치 원전 해석 그대로 2.0(코스피와 동일)을 썼는데, 실측해보니 미국은 PEGY
# 분포 자체가 코스피보다 높은 쪽으로 넓게 퍼져 있어(p75=2.02) 2.0 기준이 "상위 25%"와
# 거의 같아지고, 전체 550종목 중 115개(21%)가 전부 "🔴 극단적 고평가(위험)" 하드컷오프를
# 받는 문제가 있었습니다. "극단적" 배지치고 비율이 너무 높다고 판단해, 오너 확정으로
# **미국만** 3.0으로 상향(코스피 2.0은 그대로 유지 — 시장 규모·분포가 다르다는 오너 판단).
# 3.0 기준으로는 47개(8.6%)만 걸려 "상위 1할 정도의 진짜 극단치"라는 배지 의도에 맞습니다.
US_PEGY_EXTREME_OVERVALUED = 3.0
# Forward PER 100배 이상은 548종목 중 11개(2.0%, p97~98 수준)만 해당 — 이미 "진짜 극단"
# 구간에 정확히 위치해 조정 없이 확정합니다. ✅ 확정(2026-08-07 실측)
US_FPER_EXTREME_OVERVALUED = 100.0

# ✅ 확정(2026-08-07 실측): 목표주가 폭주 방지 캡.
# 35배 캡 적용 시 실제 목표가 캡 발동 비율은 548종목 중 16개(2.9%)로, "가끔 걸리는 안전판"
# 이라는 원래 의도에 부합해 조정 없이 확정합니다. 목표가 배수 캡(2.5배)은 시장 무관한
# 논리(외삽 방지)라 코스피와 동일하게 유지합니다.
US_TARGET_PER_CAP = 35.0
US_TARGET_PRICE_CAP_MULTIPLE = 2.5

# ✅ 확정(2026-08-07 실측): 자본효율성 기준선(목표가 품질 프리미엄·스코어링 공용).
# ROE 중앙값이 실측 16.0%로 잠정값(15%)과 거의 일치해 16.0%로 소폭만 조정합니다.
# ROIC는 잠정값(12%)이 이미 실측 p75(20.7%)와 중앙값(11.5%) 사이 합리적 지점이라 유지.
US_ROE_PREMIUM_BASELINE_PCT = 16.0
US_ROIC_PREMIUM_BASELINE_PCT = 12.0   # WACC(미국 대형주 8~10%) 상단 + 여유, 실측 중앙값(11.5%)과 근접

# ✅ 확정(2026-08-07 실측): 착시 저평가(value trap) 판정 기준선.
# ROE는 잠정값(9%)이 실측 p25(9.2%)와 사실상 일치해 그대로 확정. ROIC도 같은 논리로
# "하위 25% 지점"에 맞춰 잠정값(8%)을 실측 p25(6.2%)에 더 가깝게 6.5%로 조정합니다.
US_VALUE_TRAP_ROE_PCT = 9.0
US_VALUE_TRAP_ROIC_PCT = 6.5

# PEGY 밴드 경계 — 피터 린치 원전 해석이라 시장과 무관하며, 코스피와 동일 값입니다.
# (1.0 = 성장률만큼 PER을 주는 이론적 적정가 / ±35% 를 저평가·고평가 경계로 사용)
US_PEGY_BAND_STRONG_UNDER = 0.65
US_PEGY_BAND_UNDER = 0.95
US_PEGY_BAND_FAIR = 1.35

# -----------------------------------------------------------------------------
# 5-1. 실효성장률(g_eff) 산출 파라미터 — 2026-08-07 추가 (utils/scoring_us.py 용)
# -----------------------------------------------------------------------------
# ENGINEERING_SPEC §5-1 의 2중 Cap 개념을 그대로 씁니다. "성장률 35%p / 주주환원 10%p /
# 합계 40%p" 는 특정 시장의 통계가 아니라 "PEGY 분모가 발산하지 않게 하는 상한"이라는
# 모델 안전장치라서 코스피와 동일 값을 유지합니다(시장 무관 상수).
US_GROWTH_CAP_PCT = 35.0
US_SH_RETURN_CAP_PCT = 10.0
US_GEFF_TOTAL_CAP_PCT = 40.0
# 실효성장률이 이 값 미만이면 PEGY 공식이 성립하지 않는 구간(사실상 무성장)으로 보고
# 산출을 생략합니다 — 코스피 PEGY_MIN_DENOMINATOR_PCT 와 같은 개념·같은 근거입니다.
US_PEGY_MIN_DENOMINATOR_PCT = 0.5

# ✅ 확정(2026-08-07 실측): 고성장 컨센서스 보수 반영 구간.
# 코스피와 동일하게 "성장률 100%p 초과분에 비례해 PEGY '점수'만" 깎습니다(f_pegy 값 자체는
# 절대 건드리지 않음 — 2026-08-06 코스피에서 잡은 자기모순 버그 재발 방지). 실측 결과
# 548종목 중 100%를 넘는 건 4개(MU/BE/LITE/ROKU, 0.7%)뿐이라 "정말 드문 예외" 취지에
# 맞아 조정 없이 확정합니다.
US_GROWTH_ADJ_THRESHOLD_PCT = 100.0
US_GROWTH_ADJ_SEVERITY_CAP_PCT = 200.0
US_GROWTH_ADJ_SCORE_RATIO_MIN = 0.20
US_GROWTH_ADJ_SCORE_RATIO_MAX = 1.00

# -----------------------------------------------------------------------------
# 5-2. 베타(변동성) 기준선 — 미국 페이지 전용
# -----------------------------------------------------------------------------
# ⚠️ 코스피는 20거래일 일간수익률 표준편차를 직접 계산했지만, 미국 수집기는 가격 시계열을
#    받아오지 않고 stockanalysis.com 이 제공하는 **Beta (5Y)** 실측값만 있습니다.
#    베타는 개별 종목의 절대 변동성이 아니라 시장 대비 민감도라 성격이 다르므로,
#    코스피처럼 실효성장률(g_eff) 분모에 벌점 배수를 곱하지 않습니다
#    (PEGY·목표주가 전체가 흔들리고, 개념이 달라 근거를 댈 수 없기 때문).
#    대신 점수 카테고리 하나(5점)에서만 반영합니다 — 이중 계상도 자동으로 없습니다.
US_BETA_NEUTRAL = 1.0        # 시장과 같은 민감도
# ✅ 확정(2026-08-07 실측): 베타 실측 p95=2.02로 2.0 근처와 정확히 일치 — "상위 5% 변동성"
# 만 최저점 처리한다는 의도에 맞아 조정 없이 확정합니다.
US_BETA_SEVERITY_CAP = 2.0   # 베타 2.0 이상(시장의 2배 이상 변동)은 동일하게 최저점 처리

# -----------------------------------------------------------------------------
# 5-3. 재무 건전성(피오트로스키 F-Score) — 0~9점 실측 지표
# -----------------------------------------------------------------------------
# ⚠️ 코스피는 "Trailing 안정성 10점"을 Trailing ROE 로 채점했는데, 미국 쪽은 자본효율성
#    카테고리에서 이미 Trailing ROE 를 쓰기 때문에 그대로 베끼면 같은 지표를 두 번
#    계상하게 됩니다. stockanalysis.com 은 피오트로스키 F-Score(회계학 표준 재무건전성
#    스코어, 0~9)를 실측으로 제공하므로 이 카테고리를 F-Score 로 대체합니다.
US_PIOTROSKI_MAX_SCORE = 9

# -----------------------------------------------------------------------------
# 5-4. 수집 품질 상태 판정 임계값
# -----------------------------------------------------------------------------
# ✅ 확정(2026-08-07 실측): 550종목(548종목) 전수 수집 실제 valid_ratio가 99.8%로
# 0.90 기준을 여유 있게 통과 — 너무 헐겁지도 너무 빡빡하지도 않은 정책값으로 확인돼
# 조정 없이 확정합니다(원래 시장 분포가 아니라 "데이터 손실 허용 정책"이라 애초에
# 시장별로 다를 이유는 없는 값입니다).
US_VALID_RATIO_SUCCESS = 0.90
US_VALID_RATIO_DEGRADED = 0.70

# 2026-08-29 재감사 H1: 직전 스냅샷 대비 노출 종목 수가 이 비율 밑으로 급감하면
# (예: 550종목 → 8종목) 소스 대량 실패 가능성이 높다고 보고, 프로덕션 산출물을
# 덮어쓰지 않고 중단합니다(collector_kospi200.py 의 소스 건전성 가드와 같은 정신).
# valid_ratio/collect_ratio 의 DEGRADED 문턱과 같은 값으로 시작 — 실측 후 조정 가능.
US_SNAPSHOT_SHRINK_GUARD_RATIO = 0.70

# 2026-08-29 재감사 S6: 위 축소 가드는 "직전 스냅샷이 있을 때"만 발동합니다(previous_symbols
# 가 비어 있으면 건너뜀 — 첫 실행 안전 목적). 그런데 그 말은 직전 스냅샷 자체가 비정상적으로
# 작았거나 없었던 상태에서는 아무리 결과가 작아도 가드가 없다는 뜻입니다. `us_stocks_latest.json`
# 을 읽는 5개 소비처(§7 문서 참고)가 항상 의지할 수 있는 **절대 하한**을 따로 둡니다 —
# US_TARGET_UNIVERSE_SIZE(550)의 상당수 미만이면 소스가 심각하게 망가진 것으로 보고
# 프로덕션 산출물을 덮어쓰지 않습니다.
US_SNAPSHOT_MIN_GUARANTEED_COUNT = 400

# 발행주식수 sanity range check 하한 (파싱 오염 탐지 — 코스피 MIN_OUTSTANDING_SHARES 와 같은 개념)
US_MIN_OUTSTANDING_SHARES = 1_000_000

# 2026-08-29 재감사 M7: 수집 실패 종목 목록이 접힌 아코디언 안에만 있으면 §0-3-13 위반
# (유의사항을 접힌 곳에 숨기지 않기)입니다. 실패 비율이 이 값 이상이면 목록은 그대로
# 아코디언에 두되, 상단에 펼쳐진 경고 배너를 추가로 띄웁니다. 목표 유니버스의 2%(약 11종목,
# 550 기준)를 문턱으로 시작 — 실측 후 조정 가능.
US_FAILED_TICKERS_BANNER_RATIO = 0.02

# 2026-08-29 재감사 M4: 화면 상단 "마지막 동기화"에 담긴 거래일이 오늘로부터 이 값(달력일
# 기준) 이상 지났으면 일반 사용자에게도 노후 경고를 띄웁니다. 통상적인 금~월 3일 주말
# 간격에서는 오탐이 나지 않도록 여유를 둔 값 — 미국 시장 휴장일 캘린더가 없어 "영업일"이
# 아니라 "달력일"로 계산합니다(§0-1 — 있지도 않은 정밀도를 주장하지 않음).
US_STALE_SESSION_WARNING_DAYS = 4

# -----------------------------------------------------------------------------
# 5-5. 착시 저평가/금융업종 판정용 업종 키워드
#      (SPEC §2-2 — 개별 티커 하드코딩 금지. 유니버스 CSV 의 `industry` 컬럼으로만 판정)
# -----------------------------------------------------------------------------
US_FINANCIAL_INDUSTRY_KEYWORDS = (
    "bank", "banks", "capital markets", "insurance", "financial",
    "asset management", "credit services", "mortgage", "reinsurance",
)
US_REIT_INDUSTRY_KEYWORDS = ("reit", "real estate")

# =============================================================================
# 6. 화면(UI) 관련 — 오너 확정 사항
# =============================================================================
US_DISPLAY_CURRENCY = "USD"        # 원화 환산/병기 없음 (오너 확정, PROJECT_STATUS §8-5)
US_PAGE_SIZE = 30                  # 페이지네이션 30개씩 (코스피 페이지는 20개 — 다름에 주의)

# =============================================================================
# 7. 산출물 경로
# =============================================================================
# 2026-08-29 재감사 S6: `us_stocks_latest.json`(US_SNAPSHOT_FILENAME) 은 이 수집기 하나가
# 쓰고 여러 화면·배치가 읽는 **공용 계약**인데, 그 계약이 문서화된 곳이 없었습니다(그래서
# H1의 "550종목 중 542종목 실패"가 미국주식 화면만이 아니라 결투 USD 야간배치까지 멈췄을 때,
# 왜 배치가 영향받는지 코드를 다 뒤져야 알 수 있었습니다). 실제 소비처(2026-08-29 grep 확인,
# archive/ 제외):
#   · web/pages/us_stocks_page.py                — 화면 표시 (원 소비처)
#   · web/pages/dividend_us_page.py / dividend_us_logic.py
#       — 미국 배당 화면의 **유일한 입력**(새 수집기를 따로 만들지 않고 이 파일의 필드만 씀)
#   · web/pages/duel_page.py / utils/duel_batch_usd.py
#       — 결투 USD 배치의 신선도 점검·체결가(rank/price)
#   · web/pages/scorecard_page.py (utils/scorecard_db.py 의 종목명 매핑 경유)
#       — 스코어카드 종목명(name_kr) 표시
#   · utils/report_db.py — 사장님 보고서의 거래일 정합성 점검(session_hint 만 읽음, 가벼운 참조)
# 최소 보증(수집기 자기검증 — 아래 run_us_collector() 의 자기검증 블록이 강제):
#   · `stocks` 는 최소 US_SNAPSHOT_MIN_GUARANTEED_COUNT 개 이상(첫 실행이라 직전 스냅샷이
#     없어도 H1 축소 가드와 별개로 지켜지는 절대 하한 — 예: 소스가 완전히 바뀌어 1종목만
#     긁혔는데 직전 스냅샷 자체가 없던 상황).
#   · 각 종목 dict 는 최소 `symbol`·`rank`·`is_valid` 를 가짐(위 5개 소비처가 공통으로
#     의존하는 필드).
#   · `metadata.status` 는 "SUCCESS"/"DEGRADED"/"FAILED" 중 하나(값의 의미는 §5-4 위 참고 —
#     valid_ratio·collect_ratio 를 둘 다 통과해야 SUCCESS).
# "모델이 무엇을 목표가라고 부를 것인가"(BPS 바닥값 vs PEGY 역산값, H4)는 코드가 아니라
# 오너 정책 결정 사안이라 이 계약에는 넣지 않았습니다(H4의 "두 값 병기"가 정책 확정 전
# 임시 방어입니다).
US_SNAPSHOT_FILENAME = "us_stocks_latest.json"          # 가공(processed) 스냅샷
US_RAW_SNAPSHOT_FILENAME = "us_stocks_raw_latest.json"  # 크롤링 직후 raw 보관 (§0-3-3)
US_UNIVERSE_FILENAME = "us_universe_latest.json"        # 필터링된 종목 유니버스
US_SAMPLE_DIRNAME = "us_sample"                         # 프로토타입 샘플 결과 보관 폴더
US_SUMMARY_HISTORY_FILENAME = "us_summary_history.json"  # 상단 요약 지표 누적 이력
US_ALL_MARKET_PRICES_FILENAME = "us_all_market_prices.json"  # 미국 전 종목 현재가(§9, 가격 전용)
US_ALL_ETF_PRICES_FILENAME = "us_all_etf_prices.json"        # 미국 상장 ETF 현재가(§9-1, 가격 전용)

# =============================================================================
# 8. 상단 지수 3종 (오너 확정 2026-08-07: S&P500 / 나스닥종합 / 다우존스, 러셀2000 제외)
#
# 2026-08-07 수정: 애초 시도했던 Stooq EOD CSV 엔드포인트(`^spx` 등)가 오너 로컬 실행에서
# 3종 전부 HTTP 404 — 무료·무인증이라 골랐지만 실제로는 살아있는 소스가 아니었습니다.
# 대안으로 **ETF 프록시** 방식으로 전환합니다: stockanalysis.com 은 종목 통계 페이지와
# 완전히 동일한 "At close: ..." 종가 블록 구조를 ETF 페이지(/etf/{ticker}/)에도 씁니다 —
# 즉 이미 검증된 `extract_close_price()` 파서를 그대로 재사용할 수 있습니다(실측 확인:
# /etf/spy/, /etf/oneq/ 라이브 응답에서 동일 구조 확인, 2026-08-07).
#
# ⚠️ 중요한 두 가지 한계 (오너 확정, §0-1 정직 표기):
#   1) ETF 주가(예: SPY $768)는 실제 지수 포인트값(S&P500 ≈ 6,598)과 **숫자가 다른 별개
#      값**입니다 — 그래서 화면에는 절대가(포인트/주가)를 보여주지 않고 **당일 등락률(%)만**
#      표시합니다(오너 확정 2026-08-07). ETF 등락률은 배당 재투자 시점 등으로 인한 극히 미세한
#      추적오차를 빼면 실제 지수 등락률과 사실상 동일합니다.
#   2) 나스닥종합(Nasdaq Composite)은 QQQ(나스닥100 추종)로는 정확히 대응되지 않아, 대신
#      ONEQ(Fidelity Nasdaq Composite Index ETF — "Index Tracked: NASDAQ Composite Index"를
#      페이지에서 직접 확인함)를 씁니다.
#   각 지수 데이터에는 `is_etf_proxy=True`와 실제 대응 여부를 재확인하는
#   `tracked_index_verified`(페이지의 "Index Tracked" 라벨 값이 기대 문구를 포함하는지)를
#   함께 기록해, 소스가 나중에 다른 ETF로 바뀌거나 페이지 구조가 바뀌어도 조용히 틀린 값을
#   보여주지 않습니다.
#
# §0-3-3(다중 출처): 지금은 지수 3종에 대해 여전히 단일 출처(stockanalysis.com)입니다 —
#    2차 출처는 추후 보완 대상(TODO)으로 남겨둡니다.
# =============================================================================
US_INDEX_PROXY_URL_TEMPLATE = "https://stockanalysis.com/etf/{symbol}/"
US_INDEX_DEFINITIONS = (
    # (내부 키, 화면 표기(한글), 화면 표기(영문), 프록시 ETF 티커, "Index Tracked" 기대 문구(소문자 부분일치))
    ("sp500",  "S&P 500",   "S&P 500",          "spy",  "s&p 500"),
    ("nasdaq", "나스닥 종합", "Nasdaq Composite", "oneq", "nasdaq composite"),
    ("dow",    "다우존스",   "Dow Jones",        "dia",  "dow jones"),
)

# =============================================================================
# 9. 미국 상장 전 종목 현재가 (2026-08-12 신설, TASK_HISTORY #92)
#
# 무엇을 위한 것인가 — "내 성적표"에서 상위 550 유니버스 **밖** 미국 종목이 계속
# "현재가 없음"으로만 뜨던 문제를 줄이기 위한 **가격 전용** 보조 목록입니다.
# 코스피 쪽 `kr_all_market_prices.json`(TASK_HISTORY #84)과 정확히 같은 역할이며,
# 밸류에이션(PER/PEGY/퀀트점수)은 여전히 상위 550 안에서만 제공됩니다(§0-1 —
# "가격은 있는데 밸류에이션은 전부 None"인 오탐을 만들지 않으려고 파일을 분리).
#
# 왜 종목별 페이지가 아니라 스크리너인가 — 기존 `US_STATISTICS_URL_TEMPLATE` 경로는
# 종목 하나당 요청 하나라서 550종목에 40~56분이 걸립니다(그래서 550이 한계였음).
# stockanalysis.com 자체 스크리너는 **한 번의 요청**으로 미국 상장 전 종목의
# 티커/회사명/현재가를 한꺼번에 돌려줍니다.
#
# ⚠️ 기술적 함정(2026-08-12 실응답으로 확인) — 스크리너 화면(/stocks/screener/)의
#    페이지 넘김은 자바스크립트(SvelteKit)가 처리해서 `?p=2` 같은 쿼리로는 HTML이
#    바뀌지 않습니다. 대신 SvelteKit 데이터 엔드포인트(`__data.json`)를 직접 부르면
#    **필터·페이지와 무관하게 전 종목이 한 응답에 통째로** 들어옵니다(실응답에서
#    `count=5607` + 5,607개 행 인덱스를 직접 확인). 다만 이 응답은 평범한 JSON이
#    아니라 SvelteKit 의 "devalue" 직렬화 포맷(값들을 평평한 배열에 담고 서로
#    인덱스로 참조 — 같은 값은 한 번만 저장)이라 전용 디코더가 필요합니다
#    (`collector_us_stocks.py` §3-2 참고).
#
# 라이선스/매너: 이미 이 프로젝트가 쓰고 있는 같은 사이트이고(robots.txt 는 일반
# 크롤러 전부 허용), 요청 수는 550회 → 사실상 1회로 **줄어듭니다**.
# =============================================================================
US_SCREENER_DATA_JSON_URL = "https://stockanalysis.com/stocks/screener/__data.json"
# 위 엔드포인트가 지금은 전 종목을 한 번에 주지만, 사이트가 나중에 진짜 페이지네이션으로
# 바뀔 수도 있어 수집기는 페이지 루프 구조를 유지합니다. 이 값은 그 루프가 폭주하지
# 않게 막는 안전장치일 뿐, "실제로 이만큼 요청한다"는 뜻이 아닙니다(현재 실측 1회).
US_ALL_MARKET_MAX_PAGES = 300
# devalue 디코더가 순환 참조/기형 응답을 만났을 때 무한 재귀에 빠지지 않게 하는 상한.
US_DEVALUE_MAX_DEPTH = 30
# 스크리너 한 행(row)의 키 이름 — 실응답에서 확인한 그대로입니다.
#   {"s":"NVDA","n":"NVIDIA Corporation","marketCap":...,"price":217.5,"change":...}
# ⚠️ §2-1(위치 인덱스 파싱 금지)과 같은 정신으로, "몇 번째 열"이 아니라 키 이름으로 집습니다.
US_SCREENER_SYMBOL_KEY = "s"
US_SCREENER_NAME_KEY = "n"
US_SCREENER_PRICE_KEY = "price"
# 소스가 알려주는 "전체 종목 수" 키 후보(스크리너는 count, 목록 페이지는 resultsCount).
# 이 값에 도달하면 다음 페이지를 더 요청하지 않고 멈춥니다(불필요한 요청 방지).
US_SCREENER_TOTAL_COUNT_KEYS = ("count", "resultsCount")

# =============================================================================
# 9-1. 미국 상장 ETF 현재가 (2026-08-12 신설, TASK_HISTORY #93)
#
# 오너 지시(원문) — "아니 그냥 ETF까지도 넓히자 긁어오는데 문제가될까? 미국주식은 개별주식을
# 가지고 있는 사람도 많겠지만 ETF로 투자하는 비율도 소수라고 할 수 없을 만큼 많기 때문에
# 무시할 수는 없어". 그동안 이 파일 §3(상품 유형 분류)과 위 §9 는 오너 원안대로 ETF를
# **의도적으로 제외**해 왔는데, 이번에 오너가 그 제외 정책을 명시적으로 뒤집었습니다.
# 다만 뒤집힌 것은 "현재가를 보여줄 범위"뿐이고, **밸류에이션(PER/PEGY/퀀트점수) 유니버스는
# 여전히 상위 550 보통주 전용**입니다 — ETF에는 EPS/ROE 같은 기업 재무제표가 없어서
# PEGY를 계산하는 것 자체가 §0-1 위반(없는 값을 만들어내는 것)이기 때문입니다.
#
# 소스는 같은 사이트의 **ETF 스크리너** SvelteKit 데이터 엔드포인트입니다. 응답 포맷도
# 주식 스크리너와 완전히 동일한 devalue 직렬화라서 §9의 디코더를 그대로 재사용합니다.
#
# ⚠️ 2026-08-12 실응답 확인 결과(추측 아님, 실제 받아서 눈으로 확인):
#     · 화면에는 "Page 1 of 279"로 표시되지만 주식 스크리너와 똑같이 **한 번의 요청**에
#       전 종목이 들어옵니다(응답의 `count`=5480, 평평한 배열 인덱스가 32,000번대까지).
#     · 한 행의 키 이름:
#         {"s":"VOO","n":"Vanguard S&P 500 ETF","assetClass":"Equity","aum":1033499074065,
#          "price":691.59,"change":0.13,"volume":1180435,"holdings":519}
#       → 티커(s)·이름(n)·현재가(price) **세 키가 주식 스크리너와 글자 그대로 같습니다**.
#         그래서 US_SCREENER_SYMBOL_KEY / _NAME_KEY / _PRICE_KEY 를 그대로 씁니다
#         (ETF 전용 키인 assetClass/aum/volume/holdings 는 '가격 전용' 파일 목적상
#          일부러 저장하지 않습니다 — 주식 쪽 파일과 구조를 동일하게 유지).
#     · 컬럼 순서(위치 인덱스)에는 어차피 의존하지 않습니다(§2-1 정신 — 키 이름으로 집음).
# =============================================================================
US_ETF_SCREENER_DATA_JSON_URL = "https://stockanalysis.com/etf/screener/__data.json"
