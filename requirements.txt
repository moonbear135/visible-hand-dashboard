streamlit==1.50.0
altair>=5.4
typing_extensions>=4.12
plotly
pandas
finance-datareader
# ⚠️ `requests` 는 수집기(collector_*.py)뿐 아니라 **웹앱 실행 중에도** 쓰입니다 —
#    utils/data_source.py 가 data/*.json 을 raw.githubusercontent.com 에서 읽어옵니다
#    (NICEGUI_MIGRATION_PLAN.md §8-5 B안, 2026-08-17). 컷오버 때 의존성을 정리하더라도
#    이 줄은 지우지 마세요.
requests
beautifulsoup4
lxml
schedule
bcrypt

# 2026-08-11 — 📊 "내 성적표" 모듈(3번째 모듈, 스테이징)용 Supabase 클라이언트.
# 이 패키지가 없어도 앱은 정상 작동합니다: utils/scorecard_db.py 가 try/except ImportError
# 로 감싸고 있어, 없으면 "내 성적표" 화면만 "준비중" 안내를 띄우고 기존 두 모듈
# (한국/미국 밸류에이션)은 아무 영향 없이 그대로 돕니다.
supabase

# 2026-08-16 — Streamlit → NiceGUI 이전(NICEGUI_MIGRATION_PLAN.md) 0단계.
# `streamlit`/`altair`는 컷오버 전까지 지우지 않습니다(듀얼런, ENGINEERING_SPEC.md §0-3-10 ·
# NICEGUI_MIGRATION_PLAN.md §11-1) — main.py(NiceGUI)와 app.py(Streamlit)가 당분간 같은
# 저장소에서 나란히 삽니다. 2026-08-17 컷오버 완료 — 지금은 롤백 안전장치로 유지 중.
# 실제 제거 대상·시점은 NICEGUI_MIGRATION_PLAN.md 부록 B(가장 구체적) 또는
# PROJECT_STATUS.md §0-2·§0-5-4(지금 상태 요약)를 보세요 — 이 줄엔 날짜를 복사해두지 않습니다.
nicegui

# 2026-08-10 (TASK_HISTORY #70) — KRX OPEN API 연동에 **새 의존성을 추가하지 않았습니다.**
# 후보였던 `pykrx-openapi`(PyPI)의 소스를 직접 읽고 비교한 결과, ① 인증키를 공식 규격(HTTP
# 헤더 AUTH_KEY)이 아니라 쿼리스트링에 실어 보내고(키가 URL·로그에 남음), ② 우리가 쓰는 건
# 31개 엔드포인트 중 3개뿐이라 requests 호출 몇 줄이면 충분해서, `utils/krx_openapi.py` 에
# 직접 구현했습니다. 자세한 판단 근거는 그 파일 상단 주석에 있습니다.
#
# 아래 패키지들은 일부러 requirements.txt 에 넣지 않았습니다.
# 코드에서 전부 try/except ImportError 로 감싸져 있어, 없으면 해당 기능만
# (교차검증/AI 코멘트/드라이브 백업/pykrx 보조수집) 조용히 꺼지고 앱 자체는 정상 작동합니다.
# 빌드 안정성을 위해 필요할 때만 아래 주석을 풀어 하나씩 추가하는 걸 권장합니다
# (2026-08-17 정정: 원문은 "Streamlit Cloud 빌드 안정성"이었으나 지금 실배포는 Render/Docker —
# 이 문단의 try/except 가드 원칙 자체는 배포처와 무관하게 여전히 유효합니다).
# yfinance
# google-api-python-client
# google-auth-oauthlib
# google-auth-httplib2
# pykrx

# 2026-08-17 — "내 성적표" v2(스크린샷 OCR 프리필, 스테이징)용 Gemini 클라이언트.
# `SCORECARD_V2_OCR_WORK_ORDER.md` · ENGINEERING_SPEC.md §0-3-11(외부 AI는 항상 교체
# 가능하게) 참고. 위 목록에서 빠져나와 실제로 추가되지만, 이 패키지가 없어도 앱은 정상
# 작동합니다: `utils/scorecard_ocr.py` 가 (`utils/scorecard_db.py`의 supabase 가드와 같은
# 패턴으로) try/except ImportError 로 감싸고 있어, 없으면 "스크린샷으로 채우기" 기능만
# 사람이 읽을 오류 문구로 실패하고 기존 두 모듈(코스피/미국 밸류에이션)은 영향받지 않습니다.
# 이 기능 자체도 `SCORECARD_OCR_ENABLED` 플래그로 기본 숨김입니다(§0-3-6).
#
# 🔴 오너 확인 필요 (2026-08-17 검토에서 발견 — §0-3-9 "의존성은 방치하지 않는다"):
#    이 패키지는 구글이 **지원 종료(EOL)** 를 선언했고, import 할 때마다
#    "All support for the `google.generativeai` package has ended ... please switch to
#    the `google.genai` package" FutureWarning 을 냅니다. 지금 동작에는 문제가 없지만
#    앞으로 보안 패치가 나오지 않습니다. 후속 조치는 이 파일만 고쳐서는 안 되고
#    `utils/macro_ai.py`(배치 AI 코멘트)가 같은 패키지를 쓰고 있으므로 **두 곳을 한
#    번에** `google-genai` 로 옮겨야 패턴이 갈라지지 않습니다(§0-3-10). 이번 v2 작업
#    범위(작업지시서 3단계 = macro_ai.py 패턴 재사용) 밖이라 임의로 바꾸지 않고
#    기록만 남깁니다 — 옮길 때는 `utils/scorecard_ocr.py` 의 provider 분기
#    (`OCR_PROVIDER`) 안쪽만 고치면 화면·테스트는 그대로입니다(§0-3-11 설계 의도).
google-generativeai
