# 배당금 모듈 ("투자 감사합니다!") — 작업 현황 및 인수인계

> ⚠️ **이 문서는 2026-08-23~24 빌드 당시 기준의 역사 기록입니다 — 지금 상태가 아닙니다.**
> 이 지시서대로 새로 만들지 마세요: **배당금 모듈은 이미 완성돼 `/dividend`(배당 달력)로
> 실서비스 중이고, 2026-08-25 전체 공개로 전환됐습니다.** 지금 아키텍처·공개 상태·열려있는
> 일은 `PROJECT_STATUS.md` **§11**을 보세요(리포트·성적표 모듈이 완성 후 밟은 것과 같은
> 관례 — `REPORT_WORK_ORDER.md`/`SCORECARD_WORK_ORDER.md` 맨 위 배너 참고). 다만 **왜
> 이렇게 설계했는지**(§0-1~§0-3-N 조항별 적용 근거, 초기 발견 문제 10가지의 상세 배경 등)는
> 지금도 유효한 참고자료라 아래는 그대로 남겨둡니다.

작성일: 2026-08-23
작성 세션: 배당금 모듈 전담 (다른 창의 모듈4/대결 세션과는 완전히 분리된 별도 세션)
목적: 다른 채팅창(모듈4 등)과 작업이 섞일 때 서로 충돌하지 않도록, 이 모듈이 어디까지 됐고 어떤 파일을 쓰는지 미리 정리해둔 문서.

---

## 🚨 2026-08-23 (같은 날, 다른 채팅창 — 모듈4 세션) — 외부 검토 결과 추가

이 문서는 모듈4(결투다!/내 성적표) 담당 세션이 오너 요청으로 **처음부터 끝까지 다시 읽고, 실제
코드·워크플로우·다른 두 모듈의 작업지시서와 대조 검토**했습니다(Opus 모델, 높은 추론 노력).
검토 방식: 문서 정독 + 코드 정독 + **직접 실행 검증**(`py_compile` 클린, `pytest` 69개 통과 재확인,
워크플로우 YAML 직접 읽고 대조) + 다른 모듈 작업지시서와 관례 비교. 아래에 발견한 것 전부를
심각도와 함께 정리합니다 — **다음에 이 모듈을 여는 세션은 아래 전부를 먼저 읽어주세요.**

📌 **모듈 번호 표기(2026-08-24 오너 확정, 재번호 완료)**: 검토 중 번호가 겹치는 걸 발견해
오너께 확인받았습니다. 최종 확정: "내 성적표" 3번째(`PROJECT_STATUS.md` §9, 변경 없음),
"리포트(사장님 보고서)" **4번째**(원래 5번째였다가 재번호, `PROJECT_STATUS.md` §10),
"결투다!" **5번째**(원래 4번째였다가 재번호, `DUEL_MODULE_WORK_ORDER.md`), 그리고 **이 배당금
모듈("투자 감사합니다!")이 6번째**입니다. 리포트·결투다! 양쪽 실제 코드 파일(주석/독스트링)과
`TASK_HISTORY.md`의 과거 기록까지 전부 새 번호로 맞춰 고쳤습니다(오너 승인). 이전 대화나
과거 문서 어딘가에 "결투다! = 4번째"라고 남아있으면 그건 재번호 이전 기준입니다 — 지금부터는
항상 6번째로 부르면 됩니다.

🔴 **이 모듈에서 절대 잊으면 안 되는 네 가지**
① 모르는 값은 절대 지어내지 않는다 — 수집 실패·NO_DATA·UNMAPPED는 전부 화면까지 도달해야 한다
(§0-1). ② 실시간은 없다 — 모든 값에 "언제 기준"(`stlm_dt`·`collected_at_kst`)을 붙인다(§0-3-1).
③ 화면을 만드는 순간 `esc()` — DART가 준 모든 문자열(종목명·사유 문구 등)은 우리가 만든 값이
아니다(§0-3-9, 아래 [항목 5] 참고). ④ 세션을 끝내기 전 `PROJECT_STATUS.md`를 갱신한다 —
그게 §0-3-7이 말하는 "작업완료서"다(아래 [항목 2] 참고, 지금까지 이걸 안 한 게 이 검토에서 나온
가장 중요한 문제입니다).

### 발견한 문제 — 심각도순

**[1] 🔴 치명적 — 2026년 수집 대상이 "전 상장사"가 아니라 "과거 배당 이력 파일"입니다.**
`.github/workflows/collect_dividend_kr.yml`의 `ARGS="--universe data/dividend_history_kr_2023_2025.json ..."`
— 이 문서와 코드 docstring은 "코스피·코스닥·코넥스 전 상장사 약 2,700종목"이라고 말하지만, 실제로
도는 대상은 **2023~2025년에 배당 이력이 있던 종목뿐**입니다. 2026년에 신규 상장했거나 2026년에
처음 배당하는 회사는 **조회 자체가 시도되지 않고, 리포트 어디에도 "빠졌다"는 흔적이 남지 않습니다**
— §0-1("모르는 건 지어내지 않는다") 이전에 "빠진 것 자체를 모르는" 상태라 더 심각합니다. 저장소에
**이미 올바른 전 종목 목록이 있습니다**: `data/kr_ticker_master.json`(4,033건 = KOSPI 942 /
KOSDAQ 1,771 / KOSDAQ GLOBAL 50 / KONEX 109 / ETF 1,161, 키 이름 `code`). `load_universe()`의
`_CODE_KEYS`에 `"code"`가 이미 있어 **`--universe` 값을 이 파일로 바꾸고 ETF만 걸러내면 그대로
됩니다.**

**[2] 🔴 치명적 — `PROJECT_STATUS.md`를 한 글자도 안 건드린 것 자체가 규칙 위반입니다.**
이 문서 §2가 "절대 건드리지 않은 파일: `PROJECT_STATUS.md`, `TASK_HISTORY.md`"라고 적었는데, 이건
자랑이 아니라 위반 신고입니다. `ENGINEERING_SPEC.md` §0-2는 작업 종료 시 "최근 작업 로그" 추가 +
"지금 열려있는 일" 정리 + "파일 구조" 표 갱신을 **필수**로 규정하고, §0-3-7은 *"작업지시서/
작업완료서 요구사항은 이미 `PROJECT_STATUS.md`로 충족되고 있다 — 별도 문서를 새로 만들 필요는
없다"*고 못박습니다. 즉 **이 인수인계 문서(`DIVIDEND_MODULE_WORK_ORDER.md`)는 §0-3-7을 대신할 수
없습니다.** 다음 세션은 `PROJECT_STATUS.md`에 배당금 모듈을 위한 새 절을 만들고, 파일 구조 표와
"지금 열려있는 일"에 이 모듈을 등록해야 합니다. §7-1 규칙(여러 창 동시 작업 시 손대기 **전**에
`🔒 작업 중` 한 줄을 먼저 커밋·push)도 지금까지 지켜지지 않았습니다.

**[3] 🔴 치명적(보안) — 워크플로우 입력값이 셸에 그대로 새어 들어갑니다(GitHub Actions script
injection, 이미 널리 알려진 공격 유형 — §0-3-9 대상).**
`.github/workflows/collect_dividend_kr.yml`:
```
MAX_MIN="${{ github.event.inputs.max_runtime_minutes }}"
...
ARGS="$ARGS --max-runtime-sec $((MAX_MIN * 60))"
if [ -n "${{ github.event.inputs.limit }}" ]; then
  ARGS="$ARGS --limit ${{ github.event.inputs.limit }}"
fi
```
`${{ }}`는 셸이 실행되기 **전에** 텍스트로 그대로 치환되므로, `limit` 입력값에 `1; curl ...` 같은
문자열을 넣으면 그대로 실행됩니다. 이 잡은 `contents: write` 권한과 `DART_API_KEY` 시크릿을 함께
갖고 있어 영향이 작지 않습니다. 처방: `env: LIMIT: ${{ github.event.inputs.limit }}`로 받아서
`"$LIMIT"`로 인용해 쓰기(다른 값들도 동일하게).

**[4] 🟠 중요 — 수집이 중간에 죽으면 체크포인트가 통째로 사라집니다(§0-3-2 정면 위반 소지).**
"산출물 커밋" 스텝에 `if:` 조건이 없어(기본값 `success()`), 수집 도중 예상 못 한 예외·러너 강제
종료·시간 초과가 나면 이 스텝 자체가 건너뛰어져 **체크포인트가 저장소에 올라가지 못하고
사라집니다.** 코드 주석은 "체크포인트가 저장되니 이어서 실행하라"고 약속하는데, 그 약속이 인프라
설정에서 깨져 있습니다. 처방: 체크포인트를 커밋하는 스텝만이라도 `if: always()`로.

**[5] 🔴 치명적(아직 미착수라 지금 안 고치면 잊기 쉬움) — §0-3-9 XSS 방어가 문서에 한 글자도
없습니다.** 다음 단계가 바로 화면(배당 캘린더 페이지) 구현인데, 이 문서에 `esc()`/XSS 언급이
0회입니다. 이 모듈이 화면에 그릴 문자열은 거의 전부 **우리가 만들지 않은 값**입니다 —
`corp_name`(DART가 준 회사명), `status_reason`, `parse_notes`, `unknown_se_labels`(DART 원문
라벨). `web/components/html.py`의 docstring이 "크롤링해 온 종목명·사유 문구 등 외부 문자열이
섞이는 자리는 반드시 `esc()`"라고 이미 규정했고, `tests/test_web_session_isolation.py`가 화면
파일마다 `esc(` 사용을 **자동으로 강제 검사**합니다(안 쓰면 곧바로 테스트 빨간불). 화면을 만들기
전에 이 프로젝트의 다른 화면 파일(`web/pages/scorecard_leaderboard_page.py` 등)이 `esc()`를 어떻게
쓰는지 먼저 보고 따라 하세요.

**[6] 🟠 중요 — `README_상황보고.md`를 세 곳에서 인용하는데 그런 파일이 저장소에 없습니다.**
`collector_dividend_kr.py:123`, `.github/workflows/collect_dividend_kr.yml:16`,
`tests/test_dividend_collector.py:20` 세 곳이 "요청량 산수" 절·"§8"을 그 문서에서 인용한다고
적었는데, 저장소 전체를 찾아도 그런 파일이 없습니다. 그 내용을 실제로 어딘가에(이 문서 안이든
새 파일이든) 옮기거나, 인용 세 곳을 지워야 다음 사람이 헤매지 않습니다.

**[7] 🟡 사소하지만 확인 필요 — 테스트가 저장소 표준 명령으로는 안 돌아갑니다.**
`tests/test_dividend_collector.py`가 `sys.path.insert(0, os.path.dirname(__file__))`만 써서
`tests/`만 경로에 넣는데, 저장소의 다른 모든 테스트 파일은 `sys.path.append(str(REPO_ROOT))`를
씁니다. 그 결과 `pytest tests/test_dividend_collector.py`를 **단독으로** 실행하면
`ModuleNotFoundError: corp_code_mapper`로 죽습니다(`python -m pytest`로 CWD를 경로에 넣거나,
전체 스위트를 돌려 알파벳순으로 앞선 다른 파일이 REPO_ROOT를 넣어준 덕에 우연히만 통과). 다른
테스트 파일과 같은 방식으로 고쳐두세요.

**[8] 🟡 사소 — 단위 변환을 라벨의 단위 토큰 없이 필드명으로 단정합니다.**
`classify_se`가 `"총액"+"배당"+"현금"` 같은 키워드만 보고 분류하는데, 출력 필드명(`cash_total_mkrw`
등)은 "백만원" 단위를 이미 확정해버립니다. `ENGINEERING_SPEC.md` §2-4는 "원문에서 단위 키워드
확인 후 변환"을 명시적으로 요구합니다 — 회사가 실제로는 "천원" 단위로 적었다면 1000배 틀린 값이
조용히 정상값으로 저장될 수 있습니다.

**[9] 🟡 사소 — `data/`에 커밋되는 부산물(체크포인트 ZIP, append-only raw jsonl)의 실제 용량을
아직 아무도 재지 않았습니다.** 첫 완주 후 실제 바이트 수를 확인하고, 필요하면 `.gitignore` +
Actions artifact로 빼는 걸 검토하세요(저장소·배포 이미지가 계속 무거워지면 §0-3-2 "GitHub에도
무리 주지 않는다"에 걸립니다).

**[10] 참고 — 화면을 만들 때 반드시 알아야 할 것 두 가지:**
- 데이터 로딩은 `open()`이 아니라 `await web.state.load_json_file_async(...)`를 쓸 것. 동기
  `load_json_file()`을 `@ui.page` 안에서 부르면 이벤트 루프를 막아 **전 접속자가 끊기는 사고**가
  이미 한 번 있었습니다(2026-08-21, `web/state.py:80-102`에 전문 기록).
- 화면 파일을 `web/pages/`에 새로 만드는 순간 `tests/test_web_session_isolation.py`가 자동으로
  검사 대상에 넣습니다. 모듈 최상위에 dict/list 전역을 만들면 그 파일의
  `ALLOWED_MUTABLE_GLOBALS`에 "왜 사용자 데이터가 담길 수 없는지" 사유와 함께 등록해야 합니다.

**정정 하나** — 검토 중 `data/dividend_history_kr_2023_2025.json`이 검토용 사본에는 없어서 한때
"파일이 없는 것 아니냐"는 의심이 있었는데, 오너 컴퓨터에서 직접 확인해보니 **실제로는 존재합니다**
(4,069,510바이트 — 이 문서 §2가 말한 8,202건 규모와 맞습니다). 이건 검토용 사본을 준비할 때 이
파일을 안 옮겨서 생긴 착오였고, 배당금 모듈 쪽 문제는 아닙니다.

*(위 발견 사항의 상세 근거(파일·줄 번호·인용문)는 검토를 진행한 세션 기록에 남아 있습니다. 필요하면
오너에게 요청해 전달받으세요.)*

---

## 1. 이 모듈이 뭔지

배당금 캘린더 모듈. 테마는 **"투자 감사합니다!"**. 코스피·코스닥·코넥스 전체 상장사의 배당 정보를
연도별로 모아서 보여주는 게 목표. ETF 분배금은 "기왕이면" 넣고 싶었으나 구조화된 공식 데이터 소스가
없어(§6 참고) 오너가 명시적으로 **보류** 결정.

기존에 이미 있는 "사실 이 가격이에요"(PEGY) 페이지에 날짜별 배당금·배당수익률을 연결하고 싶다는 게
최종 그림이지만, 이 연결 작업은 **오너가 명시적으로 나중으로 미뤄둠**. 지금은 완전히 독립된 모듈로만
만드는 중이고, 기존 페이지/파일은 건드리지 않음.

---

## 2. 이 모듈이 소유한 파일 (전부 신규 파일 — 기존 파일 수정 없음)

| 경로 | 내용 | 상태 |
|---|---|---|
| `collector_dividend_kr.py` | DART OpenAPI `alotMatter.json` 기반 2026년 개별종목 배당 수집기 (~1,260줄) | 완성, 테스트 통과 |
| `corp_code_mapper.py` | DART `corpCode.xml` → 종목코드↔corp_code 매핑 유틸 | 완성 |
| `tests/test_dividend_collector.py` | 오프라인 pytest 69개 | 전부 통과 확인 |
| `.github/workflows/collect_dividend_kr.yml` | 수집기 실행용 워크플로우 (`workflow_dispatch` 수동 트리거만, 스케줄 없음) | 완성, 실행 중(§4) |
| `data/dividend_history_kr_2023_2025.json` | 2023~2025 배당 기준선 데이터 (오너가 KIND에서 직접 받아온 원본 9개 xls 정리, 8,202건) | 완성 |
| `data/dividend_kr_2026_latest.json` | 2026년 수집 결과(가공본) | 수집기가 생성 중 |
| `data/dividend_kr_2026_raw.jsonl` | 2026년 수집 결과(원본, §0-3-3 원칙) | 수집기가 생성 중 |
| `data/cache/dart_corpcode_*`, `data/cache/dividend_kr_2026_checkpoint.json` | 수집기 캐시/체크포인트 (완주하면 체크포인트는 자동 삭제) | 수집기가 생성 중 |
| `DIVIDEND_MODULE_WORK_ORDER.md` | 이 문서 | - |
| (예정) `web/pages/` 안의 새 페이지 파일 | "투자 감사합니다!" 배당 캘린더 화면 | **아직 미착수** |

**절대 건드리지 않은 파일**: `PROJECT_STATUS.md`, `TASK_HISTORY.md`, 모듈4(대결/duel) 관련 파일 전부
(`duel_*.py`, `.github/workflows/duel_daily.yml` 등). 이 세션은 이 파일들을 읽기만 했을 뿐 수정한 적
없음. `git add`도 항상 위 표의 신규 파일만 개별 지정해서 했고, `git add -A`나 `.` 같은 일괄 추가는
쓰지 않았음.

---

## 3. 데이터 원칙 (프로젝트 ENGINEERING_SPEC.md 준수 — 조항별 현황, 2026-08-23 검토로 확장)

- §0-1 지어내지 않기: 파싱 실패/애매한 값은 침묵하고 넘어가지 않고 명시적으로 실패 기록.
- §0-3-2 외부 서버 매너: 종목당 2~3초 랜덤 딜레이, 차단되면 즉시 중단(우회 절대 안 함 — 아래 §7 참고).
- §0-3-3 원본/가공 분리: raw jsonl과 가공 json을 따로 저장. **다만 §0-3-3의 나머지 절반("최소 2개
  출처 + 교차검증", "raw·가공 둘 다 사용자 다운로드 가능")은 아직 안 지켜집니다 — 아래 표 참고.**
- §0-3-6 신규 기능은 독립 모듈로, 정식 반영 전 단계적으로.
- 오직 공식 출처만 사용(DART, KIND/KRX). 네이버금융 등 외부 집계 사이트는 절대 사용 안 함.

**여기 적힌 것 말고도 지켜야 할 §0-3-N 조항이 더 있습니다.** 위 [항목 1~10]을 못 봤다면 먼저
읽으세요. 아래는 각 조항이 이 모듈에 실제로 적용되는지, 지금 어떤 상태인지 한눈에 보는 표입니다.

| 조항 | 상태 | 다음 세션이 할 일 |
|---|---|---|
| §0-1 지어내지 않기 | 대체로 준수. 단 [항목 1] 유니버스 누락은 "빠진 것 자체를 모르는" 상태 | [항목 1] 고치고, 화면은 `summary.by_status`/`unmapped_stock_codes`를 반드시 노출 |
| §0-3-1 후행지표 전용 | 준수(`stlm_dt`·`collected_at_kst`) | 화면 문구에 "실시간"·"최종 동기화: 지금" 류 금지 |
| §0-3-2 외부 서버 매너 | 준수(딜레이·차단 시 중단). **GitHub도 대상** — [항목 9] | 첫 완주 후 커밋 용량 실측 |
| §0-3-3 raw/가공 분리 | 절반만(raw↔가공 분리는 ✅, 2개 출처·교차검증·다운로드는 ❌) | 2023~2025 KIND 데이터와 `prev_dps_cash_common` 등 대조하는 검증 함수 추가, 그때까지 `PROJECT_STATUS.md`에 "단일 출처" 명시 |
| §0-3-4 화면에 코드 노출 금지 | 화면 미착수 | `status_reason`에 `corp_code=…`/`HTTP 404` 같은 내부 문자열이 그대로 있음 — 사람이 읽는 문장으로 매핑할 것 |
| §0-3-5 push까지 완료 | 이 세션은 push 권한 없음(§6) | 아래 §6-2 참고 |
| §0-3-6 모듈 분리 + 스테이징 | 준수 | 화면은 오너 승인 전까지 기본 숨김(`DIVIDEND_ENABLED` 환경변수 + 메뉴 조건부 삽입 — 결투/성적표와 같은 패턴) |
| §0-3-7 작업지시서/작업완료서 | **미충족** — [항목 2] | `PROJECT_STATUS.md` 갱신이 정본, 이 문서는 보조일 뿐 |
| §0-3-8 개인정보 격리 | 해당 없음(공개 시세만) | 단, 화면 파일이 생기면 `test_web_session_isolation.py`가 자동 검사 |
| §0-3-9 XSS 방어 | **미충족(화면 미착수)** — [항목 5], 그리고 워크플로우 자체의 셸 인젝션 — [항목 3] | 화면 만들 때 `esc()` 필수 |
| §0-3-10 단순·중복 금지 | 대체로 준수 | 화면 카드/표는 `web/components/`의 공용 함수부터 확인 |
| §0-3-11 AI 교체 가능 | **해당 없음** — 이 모듈에 외부 AI 호출이 없어 억지로 인용하지 않음 | — |
| §0-3-12 외부 의존성 점검 | 신규 패키지 0(`requests`만) | — |

### 배당수익률을 직접 계산해서 보여줄 경우 (§0-1 "대수적 역산 허용 예외")
`(누적 주당배당금 ÷ 확정 종가)`처럼 사칙연산으로 계산한 값을 쓰려면 4조건을 지켜야 합니다: ①
실측값을 먼저 시도, ② 순수 사칙연산, ③ 입력 둘 다 실측, ④ **데이터에 `_calculated`/`_source`
플래그를 남기고 화면에 "🧮 계산값" 배지를 실측 배지와 다르게 붙일 것.** DART 실측 수익률과
절대 섞지 마세요.

### "작년 배당율입니다" 표시 (§0-1 예시 4 — Forward Fill)
화면 라벨만으로는 부족합니다. 데이터 쪽에도 `dividend_basis_year`/`data_quality` 같은 필드를
남겨서, 화면 문구가 없어져도 "이 값이 작년 것"이라는 사실이 데이터에 남게 하세요.

---

## 4. 현재 상태 (2026-08-23 기준)

1. **2023~2025 기준선 데이터**: 완료. `data/dividend_history_kr_2023_2025.json`
2. **2026년 개별종목 수집기**: 코드/테스트 완성 → repo에 반영(로컬 커밋 `ed48cd0`) → 오너가 직접
   `git push`로 GitHub에 반영 완료 → GitHub Actions "Dividend KR Collector"
   워크플로우를 오너가 수동으로 `Run workflow` 실행 → **현재 진행 중** (오늘 시작, 최악의 경우
   5~6시간 예상, 5시간 예산 초과 시 자동으로 멈추고 체크포인트 저장 → 이어서 재실행 가능).
3. **ETF 분배금**: 조사 완료. DART/KRX/공공데이터포털 모두 구조화 API 없음 확인. KIND 웹페이지에
   유일하게 구조화된 공시(ETF이익금분배신고)가 있으나 봇 차단(`kind.krx.co.kr`, 403)됨 →
   **오너 결정으로 일단 보류**. 우회 시도 안 함.
4. **"투자 감사합니다!" 독립 페이지**: 아직 미착수. 2026 수집이 끝나야 실제 데이터로 만들 수 있음.
5. **PEGY 페이지 연동**: 명시적으로 나중 과제. 지금은 시작 안 함.

---

## 5. 다음에 할 일 (우선순위 순 — 2026-08-23 검토로 0번대 추가)

0-1. 🔴 **[항목 1] 유니버스를 `data/kr_ticker_master.json`(ETF 제외)로 교체** — 지금 상태로 화면을
     만들면 "전 상장사"라고 표시하면서 실제로는 일부 종목이 조용히 빠진 상태가 됩니다.
0-2. 🔴 **[항목 3] 워크플로우 셸 인젝션 수정** — `env:`로 받아서 인용 부호로 감싸기.
0-3. 🔴 **[항목 2] `PROJECT_STATUS.md`에 배당금 모듈 등록** — §0-3-7 미충족 상태를 해소.
0-4. 🟠 **[항목 4] 커밋 스텝을 `if: always()`로** — 중단 시 체크포인트 유실 방지.
0-5. 🟠 **[항목 6] `README_상황보고.md` 인용 정리** — 내용을 옮기거나 인용을 지움.
0-6. 🟡 **[항목 7] 테스트 `sys.path` 수정** — 저장소 관례(`sys.path.append(str(REPO_ROOT))`)로.

1. 2026 수집기 실행 결과 확인/검증 (완료까지 대기 중)
2. `data/dividend_kr_2026_*.json` 기준으로 "투자 감사합니다!" 배당 캘린더 페이지 구현
   (2026년은 실제 지급된 배당만, 아직 지급 안 한 종목은 "작년 배당율입니다" 표시 — **화면을
   만들기 전에 위 §3 "§0-3-9 XSS 방어"·[항목 5]·[항목 10]을 반드시 먼저 읽을 것**)
3. 데이터 정확성 + 기존 모듈과의 격리 최종 검증 — **아래 검증 체크리스트로**
4. (보류 중) ETF 분배금 — 재개할 경우 봇 차단 우회 없이 사용자 승인된 방식으로만
5. (나중 과제) PEGY 페이지와 연결

### 검증 체크리스트 (코드를 고칠 때마다, 저장소 표준)
1. `python -m py_compile collector_dividend_kr.py corp_code_mapper.py` — 클린 확인
2. `python -m pytest tests/test_dividend_collector.py -q` — 통과 개수 기록
   (⚠️ `python -m` 없이 `pytest tests/test_dividend_collector.py`만 단독 실행하면 [항목 7] 때문에
   `ModuleNotFoundError`로 죽습니다 — 고치기 전까지는 항상 `python -m pytest`로.)
3. **저장소 전체 스위트**를 돌려서 실패·오류 목록이 이 세션 이전과 같은지(회귀 0) 확인. 이
   저장소엔 nicegui 렌더 순서 때문에 원래 흔들리는 flaky 5건(`test_*_render_smoke` 계열)이
   있으니 그것과 새로 생긴 실패를 구분할 것.
4. 화면을 만들었다면: `esc()` 자리를 직접 읽어서 확인 + 악성 문자열(`<img onerror=...>`)을 실제
   `corp_name`에 넣고 실행해서 이스케이프되는지 직접 확인(서브에이전트나 스스로의 보고를 그대로
   믿지 말고 직접 실행) + `tests/test_web_session_isolation.py` 통과 확인.

---

## 6. 특이사항 / 다른 세션이 알아야 할 것

- 이번 작업 중 `.git/index.lock` 잔여 파일(다른 프로세스가 비정상 종료하며 남긴 것으로 추정)이
  발견돼 git 명령이 한동안 막혀 있었음. 2026-08-23에 오너가 직접 삭제해서 **해결됨**. 혹시 또
  이런 "Unable to create .git/index.lock: File exists" 에러가 나면, 다른 git 프로세스가 실제로
  돌고 있는 게 아니라면 그냥 그 파일 지워도 안전함.
- 이 세션(cloud/device_bash 경로)에는 **GitHub push 권한도 `gh` CLI도 없음** — 실제 push는 항상
  오너가 본인 자격증명으로 직접 함. 앞으로도 이 세션이 "커밋했다"고 하면 로컬 커밋까지만이고,
  push는 오너 확인 필요.
- `DART_API_KEY`는 GitHub Secrets에만 등록돼 있고 코드 어디에도 하드코딩 안 돼 있음.
- **작업 시작 전에 반드시 `git pull`.** 오너가 여러 창(다른 모듈 세션 포함)을 동시에 돌리는 일이
  실제로 있고, 2026-08-18에 실제로 병합 충돌이 난 적 있습니다. 코드에 손대기 전
  `git status --short`로 로컬이 origin과 같은지 확인하세요.
- **`.github/workflows/`는 device_bash/디바이스-브릿지 경로로 파일을 못 옮기는 환경이 있습니다.**
  워크플로우 파일을 고치거나 새로 만들 때 그런 제약에 부딪히면, 오너 컴퓨터에 직접 쓰는 방식
  (터미널 heredoc 등) + 전송 전후 체크섬 대조로 우회하세요(다른 모듈들이 이미 쓴 방법).
- **오너는 코딩을 전혀 모릅니다.** 터미널 명령을 오너에게 줄 때는 **항상 Windows cmd.exe에서 그대로
  복붙되는 형태**여야 합니다(`&&` 체인·리눅스 전용 경로·PowerShell 전용 문법 금지). git push를
  부탁할 때는 아래 두 줄을 그대로 주세요 — 다른 우회 방법을 새로 제안하지 마세요.
  ```
  cd /d "C:\Users\crown\.gemini\antigravity\scratch\visible_hand"
  git push
  ```

---

*이 문서는 배당금 모듈 작업 세션이 스스로 남긴 인수인계용 기록입니다. 다른 세션이 이 프로젝트를
건드리기 전에 한 번 읽어보면 좋습니다.*
