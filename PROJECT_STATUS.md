# 프로젝트 현황판 (PROJECT_STATUS.md)

> **다음에 이 프로젝트를 다시 열 때는 (나 자신이든, 제미나이든, 다른 클로드 세션이든) 전체 파일을 처음부터 다 읽지 말고 이 문서부터 읽으세요.**
> 이 문서 하나로 "지금까지 뭘 했고, 지금 뭐가 문제고, 다음에 뭘 해야 하는지"를 알 수 있게 유지하는 게 목적입니다.
> 작업할 때마다 이 문서의 **최근 작업 로그**와 **지금 열려있는 일** 섹션을 업데이트해주세요.
> 완료된 작업의 상세 이력(번호별 전체 목록)은 `TASK_HISTORY.md`를 참고하세요. 이 문서는 요약만 남깁니다.

마지막 업데이트: 2026-08-08 — **⬇️ "종목별 데이터 다운로드" 도구 신설 (TASK_HISTORY #63).** 코스피·미국 두 화면의 검색/필터 UI 바로 아래에, 종목 하나를 검색해 고르면 **그 종목의 전체 레코드(수집 원본 + 파생 계산 + 내부 진단 필드 전부)** 를 CSV·JSON으로 받는 별도 섹션을 추가했습니다. 공통 로직은 신설 모듈 `utils/stock_export.py`(streamlit 미의존 순수 함수)에 두어 양쪽 파일 형식이 동일합니다 — CSV는 한 종목짜리라 세로형(`항목,값` 2컬럼)이고 엑셀 한글 깨짐을 막기 위해 **UTF-8 BOM(`utf-8-sig`)**, JSON은 `ensure_ascii=False`/`indent=2`, 파일명은 `삼성전자_005930_20260808.csv` 형식(`BRK/B`의 `/` 등은 `_`로 치환). 다운로드용 검색창은 **전용 위젯을 새로 두어** 기존 카드 목록 필터링 동작은 한 글자도 바뀌지 않았습니다(두 화면 모두 추가만 있고 기존 줄 변경 0).

이전 업데이트: 2026-08-08 — **✅ 🇺🇸 미국주식 화면 공개 전환 완료(§0-3-6 절차 종료, TASK_HISTORY #61).** 오너의 명시적 승인으로, 관리자 인증 후에만 보이던 "🇺🇸 미국 주식은 이가격"을 사이드바 **공개 메뉴**로 올렸습니다(`visiblehand.py`). 화면 상단의 "🧪 [베타 · 검토중] 아직 공개되지 않은 화면입니다" 배너도 제거(`views/us_stocks_view.py`) — 그 배너가 알리던 "임계값 잠정값" 문제는 #55 재캘리브레이션으로 이미 해소됐습니다. 승인 전제조건 2가지(임계값 확정 = `utils/constants_us.py` §5 전 항목 "✅ 확정", 자동화 연결 = `scrape_us.yml`이 실제로 GitHub 서버에서 돌아 봇 커밋 `0907c19`/`55ba92e` 생성)를 코드·데이터로 재검증한 뒤 진행했습니다. 매크로 방공망은 종전 그대로 관리자 전용입니다. **이로써 처리 순서 1번(해외주식 페이지)이 사실상 마무리 — 다음은 2번(매크로 재설계)입니다.**

그 이전 업데이트: 2026-08-07 밤 — **🇺🇸 미국주식 GitHub Actions 자동화 연결 완료(TASK_HISTORY #60).** `.github/workflows/scrape_us.yml` 신설(서머타임 대응 cron 2줄 + 수집기 `--skip-if-not-ready` 사전 점검으로 실제 수집은 하루 한 번). 기존 코스피 워크플로우 `scrape.yml`은 무수정. **이제 오너가 매일 로컬 PC에서 수동으로 `collect`를 돌릴 필요가 없습니다.** (당시 남은 관문이던 공개 메뉴 전환은 위 2026-08-08 항목으로 완료.) 상세는 §5 / §8-10.

더 이전 업데이트: 2026-08-07 — **🇺🇸 미국주식 모듈 2단계 완료(전수 수집 CLI + 스코어링 + 화면 + 한글 종목명)**. `utils/scoring_us.py` / `views/us_stocks_view.py` / `utils/company_names_kr.py` / `tests/test_us_scoring.py` 신설, `collector_us_stocks.py`에 `collect`·`indices` 서브커맨드 추가, `visiblehand.py` 사이드바에 관리자 전용 베타 메뉴 연결. 기존 코스피 파일은 무수정. 검증 78건 통과(실데이터 12종목 + 합성 극단케이스 + end-to-end 배선 + 화면 렌더링). ⚠️ **실제 550종목 전수 수집은 오너가 로컬에서 직접 실행해야 합니다** — 이번 세션도 샌드박스 네트워크가 프록시 차단(403)이라 실행 불가. 상단 지수 소스(Stooq)는 응답을 받아본 적이 없어 **파서 로직만 검증**된 상태. 상세는 §8-8/§8-9.

---

## 1. 이 프로젝트는 무엇인가

"잘 보면 보이는 손 (The Visible Hand)" — 코스피 시장 위험도 + 코스피 시가총액 상위 200개 종목 PEGY(가치) 점수를 매일 자동으로 계산해서 보여주는 대시보드.

> ⚠️ **명칭 주의**: 여기서 "200개 종목"은 **KRX 공식 "코스피 200 지수" 편입종목이 아니라, 순수 시가총액 순위 상위 200개**입니다(2026-08-06 오너 확인 — 공식 지수 전환은 하지 않기로 함). 공식 지수는 유동주식 시총·업종 안배·유동성 심사를 거쳐 연 2회만 리밸런싱되지만, 이 프로젝트는 매일 시가총액만으로 순위를 매깁니다. 화면·다운로드 파일의 사용자 노출 문구는 모두 "시가총액 상위 200"으로 표기하도록 통일함(파일명 `collector_kospi200.py`/`kospi200_pegy_latest.json` 등 내부 이름은 하위 호환을 위해 유지).

- 실제 서비스 주소: `visiblehand.co.kr` (커스텀 도메인, `index.html`이 아래 Streamlit 앱을 iframe으로 감싸는 구조)
- 실제 앱이 돌아가는 곳: **Streamlit Community Cloud** (`https://visible-hand-dashboard-2vmzz6tk63wsac7n345ord.streamlit.app/`)
- 코드 저장소: `github.com/moonbear135/visible-hand-dashboard` (public repo)
- 데이터 자동 수집: **깃허브 액션(GitHub Actions)** — PC를 꺼놔도 매일 자동으로 돈다
- 공개 화면은 **"사실 이 가격이에요"(코스피 PEGY 밸류에이션, 기본 화면)** 와 **"🇺🇸 미국 주식은 이가격"(2026-08-08 공개 전환, 사이드바 상단 메뉴)** 두 개. "잘 보면 보이는 손"(매크로 방공망)은 관리자 전용으로 전환됨 (2026-08-05, 아래 참고)

## 2. 파일 구조 한눈에 보기 (다시 다 안 읽어도 되게)

| 파일/폴더 | 역할 |
|---|---|
| `scrape_daily.py` | 코스피 지수/환율/수급 데이터 수집 → `market_history.csv`에 누적. 백필 모드 지원 |
| `collector_kospi200.py` | 코스피 시가총액 상위 200개 종목 PEGY 데이터 수집 → `data/*.json`. 상장주식수(FDR 우선), Trailing EPS 계산값 예외, 그레이엄 넘버 등이 여기 있음 |
| `.github/workflows/scrape.yml` | **코스피 자동화 워크플로우.** 매일 평일 KST 16:05 실행 |
| `.github/workflows/scrape_us.yml` | **🇺🇸 미국주식 자동화 워크플로우 (2026-08-07 신설).** 서머타임 대응으로 cron 2개(20:35/21:35 UTC)를 걸고, 수집기의 `--skip-if-not-ready` 사전 점검이 하루 한 번만 실제 수집되게 걸러냄 |
| `market_history.csv` | 날짜별 종합 위험 점수 이력 |
| `data/kospi200_pegy_latest.json`, `data/pegy_summary_history.json` | 종목별 최신 데이터 + 요약 이력 |
| `views/pegy_view.py` | **공개** PEGY 밸류에이션 화면. Forward 데이터 없으면 해당 섹션만 마스킹 처리 |
| `views/macro_view.py` | **관리자 전용** 매크로 방공망 화면 (2026-08-05부로 비공개 전환) |
| `views/admin_view.py` | 관리자 인증 + 관리자 전용 메뉴(매크로 화면 진입 체크박스 등) |
| `utils/guardrail.py` | 종목 차단/마스킹 판정 (필수 조건은 이제 `price`뿐, Forward 결측은 차단 아님) |
| `utils/scoring.py` | 퀀트 스코어링. 만점(score_max)은 종목마다 실제 산출 가능한 항목만 합산(고정 100 아님, 배지엔 %도 함께 표시). 역성장/적자·PER이상·극단고평가 하드컷오프는 오늘 수집분 전체 분포 대비 z-score로 심각도 스케일링 |
| `utils/db.py` / `utils/gdrive_helper.py` | DB 저장, 구글드라이브 백업(백업 폴더ID는 `GDRIVE_TARGET_FOLDER_ID` 환경변수로 오버라이드 가능) |
| `collector_us_stocks.py` | (2026-08-06 신설) 🇺🇸 미국주식 수집기 **기초틀**. 유니버스 CSV 수집·필터(550개), 상품유형 분류, 히스테리시스(550/600), stockanalysis.com 통계 페이지 파서, `_now_et()`/ET 세션 계산, 샘플 프로토타입 CLI. **아직 전수 수집·스코어링·화면은 없음** |
| `utils/scoring_us.py` | (2026-08-07 신설) 🇺🇸 미국주식 전용 퀀트 스코어링 + 파생 밸류에이션(PEGY/목표가/그레이엄) + 미국판 가드레일. 배점: PEGY 35 + ROE·ROIC 30 + 주주환원 20 + F-Score 10 + 베타 5 |
| `views/us_stocks_view.py` | (2026-08-07 신설) 🇺🇸 미국주식 화면. **공개 화면**(2026-08-08 오너 승인으로 §0-3-6 스테이징 종료, 베타 배너 제거). 상단 지수 3종 + 코스피와 동일 카드 구조 + USD 단독 표기 + 30개 페이지네이션 |
| `utils/company_names_kr.py` | (2026-08-07 신설) 미국 종목 한글 표기. 정식 한글명 사전(티커 키, 오너가 직접 수정) 우선 + 규칙 기반 음역 폴백(완벽하지 않음을 코드에 명시) |
| `tests/test_us_scoring.py` | (2026-08-07 신설) 미국주식 스코어링·전수수집 배선 오프라인 검증 78건 (실데이터 12종목 + 합성 극단케이스 + end-to-end + 화면 렌더링) |
| `utils/constants_us.py` | (2026-08-06 신설) 미국주식 전용 상수. 밸류에이션 임계값은 전부 🚧잠정값+TODO🚧 (코스피 값을 그대로 쓰지 않음) |
| `tests/test_us_stocks.py` | (2026-08-06 신설) 미국주식 기초틀 오프라인 검증(네트워크 불필요). 실제 CSV 종목명·실제 페이지 원문 라벨로 74개 체크 |
| `utils/constants.py` | (2026-08-06 신설) 프로젝트 전역 임계값·가중치 단일 출처. PER 오염 판정, 목표가 캡, ROE 기준선, 매크로 `RISK_WEIGHTS`/`INVESTOR_WEIGHTS` 등 |
| `utils/macro_scoring.py` | (2026-08-06 신설) 매크로 종합점수 계산(z-score 정규화+시그모이드 변환+동시충격 증폭기) 단일 모듈. `scrape_daily.py`(저장)와 `views/macro_view.py`(표시)가 공용으로 호출 |
| `app.py` / `visiblehand.py` | 앱 진입점. 공개 메뉴는 코스피 PEGY 화면(기본) + 🇺🇸 미국주식(사이드바 상단 체크박스, 2026-08-08 공개 전환) 두 개. 매크로는 관리자 인증 후에만 |
| `utils/scheduler.py` | 앱 내장 스케줄러(기본 비활성화, `ENABLE_INAPP_SCHEDULER=1`일 때만). 정식 경로는 GitHub Actions. (구버전 레거시 `scheduler.py`/`run_scheduler.bat`는 2026-08-06 삭제됨) |
| `utils/stock_export.py` | (2026-08-08 신설) "종목별 데이터 다운로드" 공용 모듈. 종목 1건 레코드 → CSV(세로형 `항목,값` · **UTF-8 BOM**, 엑셀 한글 깨짐 방지) / JSON(`ensure_ascii=False`) 바이트 + 파일명 안전화(`BRK/B`→`BRK_B`). streamlit 미의존 순수 함수라 코스피·미국 양쪽이 같은 형식을 공유 |
| `ENGINEERING_SPEC.md` | 코딩 원칙 문서. **하드코딩·더미 데이터 금지가 최우선 규칙.** §0-3(절대 준수 메뉴얼: 후행지표 전용, 크롤링 매너 등), 예시2-보충(계산값 허용 예외), 예시2-보충2(섹션별 마스킹 원칙)까지 포함 |
| `AUDIT_REPORT.md` | 2026-08-05 오푸스 코드베이스 감사 보고서 (13개 파일, 50여 건) |
| `TASK_HISTORY.md` | Cowork 작업 목록 위젯에서 정리(삭제)한 완료 항목 전체 이력 |
| `credentials.json` 등 | 구글 API 비밀키. `.gitignore` 등록되어 git에는 안 올라감 (확인 완료) |

## 3. 최근 작업 로그 (요약, 최신순)

- **2026-08-06 밤(8차)** — **🇺🇸 미국주식 모듈 기초틀 착수(`US_STOCKS_WORK_ORDER.md` §6의 1~4번 완료).**
  신규 3개 파일만 추가하고 기존 코스피 파일은 한 줄도 안 건드림(§0-3-6). ① `collector_us_stocks.py`:
  유니버스 CSV 수집·파싱(컬럼 변경/행 수 부족 시 즉시 중단), 종목명 키워드 기반 상품유형 분류
  (채권·미국식 우선주·ETF + 오너 규칙 밖 워런트/라이츠/하이브리드유닛/구조화신탁), 550개 컷 필터
  (복수 보통주 클래스는 전부 포함), 히스테리시스 버퍼(550/600), stockanalysis.com 통계 페이지 파서
  (표의 **라벨 키워드**로만 매칭 — 열 번호 고정 금지, 41개 필드), **장마감 종가 전용 파서**(프리마켓
  절대 미사용), 무배당 확정 vs 수집 실패 구분, 두 출처 시총 교차검증, 샘플 프로토타입(소요시간 실측 +
  raw/가공 분리 저장). ② `_now_et()` + ET 기준 "장마감+30분" 세션 계산(서머타임/표준시에 따라 KST
  05:30/06:30으로 날짜가 바뀌는 것까지 테스트로 고정, 휴장일 캘린더는 지어내지 않고 미적용 명시 —
  실제 세션 날짜는 페이지의 "At close: …" 타임스탬프에서 확보). ③ `utils/constants_us.py`: 코스피
  상수를 베끼지 않고 미국 시장 기준 잠정값 + TODO 주석. ④ `tests/test_us_stocks.py`: 실제 CSV 종목명과
  실제 8종목(ROK/BRK.B/CAVA/IBRX/MAC/JPM/ET/PLXS) 페이지 원문으로 74개 체크 전부 통과.
  **한계(정직 기록)**: 이번 세션 샌드박스는 외부 네트워크 직결이 막혀(프록시 403) 실제 크롤링을 못 돌려
  ① HTML→라벨/값 추출 계층 ② 종목당 실제 소요시간을 실측하지 못함. 550종목 소요시간은 슬립 상수 기반
  추정(약 27~41분)이며 오너 로컬 1회 실행으로 확정 필요. 아직 git 커밋 전.
- **2026-08-06 밤(7차)** — **타임존(UTC/KST) 버그 전면 수정 (오너가 run #13 실데이터 검증 중 발견:
  JSON `last_updated_at`이 "08:09" — 실제 KST 완료 17:09와 9시간 차이).** GitHub Actions/Streamlit
  서버가 기본 UTC라 `datetime.now()`(naive)가 UTC를 반환하는 게 원인. `collector_kospi200.py`는
  KST 처리가 아예 없었고, `scrape_daily.py`/`utils/db.py`는 KST 변수를 이미 정의해두고도 정작
  `Collected_At` 저장에는 안 쓰고 있었음. 데이터 저장(collector/scrape_daily/db)과 화면 표시
  (pegy_view 신선도 계산, macro_view "오늘 날짜"·다운로드 파일명), 그리고 기본 비활성 상태인
  `utils/scheduler.py`(16시 트리거)까지 전부 `datetime.now(KST)`로 통일. 오너 지시("데이터 쌓아가는데
  문제가 될 수 있는 표기는 바로바로 처리")에 따라 발견 즉시 처리. 전체 컴파일 통과, 아직 커밋 전.
- **2026-08-06 밤(6차)** — **`AUDIT_REPORT_V2.md` 27건 전체 처리 완료.** 19건(collector_kospi200.py/
  utils/scoring.py/utils/guardrail.py/views/pegy_view.py + `utils/constants.py` 신설)은 Opus 에이전트가
  처리, 나머지 8건(scrape_daily.py/macro_view.py)은 이어서 직접 처리. 핵심: `utils/macro_scoring.py`
  신설로 매크로 종합점수 로직(가중치·z-score+시그모이드 정규화·동시충격 증폭기)을 scrape_daily.py/
  macro_view.py 공용 단일 모듈로 통합 — 예전엔 두 파일이 가중치도 다르고(5-1) 척도도 달라서(5-2,
  원시값 0~1 vs 시그모이드 변환 0~100) 화면 표의 기여점수 합이 실제 종합점수와 안 맞았음. 이제
  scrape_daily.py가 수집 시점에 서브점수(`SubScore_*`)·증폭배율(`Multiplier`)을 CSV에 함께 저장하고,
  macro_view.py는 과거 날짜 조회 시 재계산 대신 이 값을 그대로 읽음(구버전 행은 근사치 폴백). 동시
  충격 증폭기도 flat 1.3/1.15 → 극단신호 비율 비례 스케일링으로 교체(4-1, 오늘 고친 VOL_PENALTY와
  동일 계열). 그 외 4-2(모멘텀 결측시 0.0 대입 제거)·4-3(개인 순매도 규모 일부 반영)·4-4(변화율
  결측시 0.0 대입 제거)·4-5(네이버 시세 괴리시 변화율 재계산)·5-3(감사이력 v1.5.0 추가) 완료.
  전체 컴파일·import·합성테스트 통과. **AUDIT_REPORT_V2.md 27건 전부 완료 — 아직 커밋 전.**
- **2026-08-06 밤(5차)** — **"고성장률 PEGY 보정" 하드코딩 제거 (오너가 run #12 실데이터로 카드 9장을 직접 대조 검증하다 발견).**
  삼성전자우·SK하이닉스가 목표가 대비 "+150% 상승여력"(초록, 저평가 신호)인데 정작 배지는 "🔴 고평가 관망"으로
  뜨는 자기모순을 오너가 스크린샷으로 정확히 짚어냄. `utils/scoring.py` 추적 결과, "성장률 100% 이상이면
  f_pegy를 max(f_pegy*2, 1.5)로 강제 덮어쓴다"는 옛 방어로직(기저효과 왜곡 방지 목적)이 원인이었음 — 이
  덮어쓰기가 배지 판정에만 반영되고 화면의 목표가·적정가 갭은 원래(안 덮어써진) f_pegy로 계산되다 보니
  서로 다른 방향을 가리키는 모순이 생겼음. `_growth_pegy_score_ratio()` 신설로 교체: f_pegy 자체는 절대
  건드리지 않아 배지·목표가가 항상 일관되고, 대신 PEGY 카테고리 "점수"만 성장률이 100%를 얼마나 초과했는지에
  비례해(절대거리 기준, per_extreme·VOL_PENALTY와 동일 원리) 100%(무보정)~20%(최대보정) 사이로 윈저라이즈.
  `views/pegy_view.py`에 "⚠️ 고성장 추정 보수반영" 배지+툴팁을 추가해 왜 저평가 배지인데 점수는 낮은지
  설명. 실제 데이터로 재현 테스트 — 수정 전 "고평가 관망"이던 두 종목이 수정 후 "강력 저평가"로 화면과
  일관되게 바뀌고, 점수는 여전히 보수적(38/85, 45/85)으로 반영됨(예전처럼 무조건 5/35로 뭉개지지 않음)
  확인. **아직 커밋/푸시 전 — 다음 크롤링에서 실데이터 재검증 필요.**
- **2026-08-06 밤(4차)** — **Forward ROE 실제 구현 + VOL_PENALTY 하드코딩 세분화 + 크롤링 검증 정리.**
  ① **Forward ROE**: 진단 로그(오너가 붙여넣은 GitHub Actions 실행 로그)로 네이버 "주요재무제표" 표에
  `ROE(지배주주)` 행과 연간추정(`2026.12(E)`류) 컬럼이 실제로 존재함을 확인 → `collector_kospi200.py`에
  `DataValidator.classify_header_timeframe()`으로 `ANNUAL_EST` 컬럼만 정확히 골라 `f_roe` 추출하는 로직 구현
  (분기추정 컬럼과 혼동 안 함, 범위 이상치 |값|>300% 제외). 진단 전용 파라미터(`diag_roe_check`) 제거.
  `views/pegy_view.py`에 Trailing 대비 Forward가 비정상적으로 크게 벌어지면(2.5배 이상 또는 +25%p 이상) 값은
  그대로 두고 노란 경고 배지+툴팁만 추가(데이터 조작·평활화 없음, SPEC 원칙 준수). 실제 로그와 동일한 합성
  테스트로 컬럼 선택 검증 완료.
  ② **VOL_PENALTY 세분화** (오너가 삼성전자우 카드 스크린샷에서 "1.18x가 전 종목 고정값인데 왜 고정했나,
  이건 하드컷오프가 아니라 단순 하드코딩이잖아"라고 정확히 지적 — 참고로 오너가 예전에 참고했던 "3개월
  이동평균" 설명은 실제로는 틀린 정보였음, 실제 변동성 윈도우는 20거래일이고 1.18은 애초에 평균값이 아니라
  flat 상수였음). 기준선(2.0%) 초과폭에 비례해 1.05~1.40배로 선형 스케일링하는 `compute_vol_penalty()`로 교체
  (+10%p 초과분은 1.40배로 윈저라이즈). 스코어링처럼 population z-score 2차 패스를 쓰기엔(PEGY/목표가에 인라인으로
  바로 쓰이는 값이라) 구조 변경이 너무 커서 절대거리 기반 스케일링을 대안으로 선택(코드 주석에 트레이드오프 명시).
  UI 안내 문구("1.18x 벌점"→"1.05~1.40x 비례 벌점")도 수정. 입력값 9종 테스트로 스케일링 순서 검증 완료.
  ③ **크롤링 검증 정리**: run #11 로그로 히스테리시스 버퍼(`tracked_count:201`/`hidden_buffer_count:1`),
  `market_history.csv` 저장 성공, Forward ROE 컨센서스 존재까지 3가지 확정 → Cowork 작업 목록에서 완료 처리.
  **forward_available 배지 재라벨과 퀀트 스코어 심각도 세분화는 원격 JSON이 너무 커서(샌드박스 fetch 용량
  제한) 실데이터로 확인 못 함 — 여전히 미확인 상태(아래 4번 섹션 참고).** 이 배치(ROE+VOL_PENALTY)는 아직
  git 커밋/푸시 전 — 다음 할 일은 오너가 직접 push.
- **2026-08-06 밤(3차)** — **AUDIT_REPORT.md 보류 항목 정리 (오너 지시: "무해한 것들은 치우는 방향으로").**
  ① 죽은 파일 삭제: `scheduler.py`(레거시 로컬 배치, `utils/scheduler.py`와는 다른 파일 — 이건 앱이 실제 import하므로 보존),
  `run_scheduler.bat`, `.python-version`, `runtime.txt`(둘 다 Streamlit Cloud가 무시해서 효과 없었음). `patch.py`/
  `patch_macro.py`/`update_snapshot.py`/`error.log`/`output.log`/`git_history_target.txt`/`templates/test_dashboard.html`은
  확인 결과 이미 존재하지 않아(이전에 정리됨) 별도 조치 불필요. ② `TARGET_FOLDER_ID`(구글드라이브 폴더 ID) 하드코딩을
  환경변수(`GDRIVE_TARGET_FOLDER_ID`) 오버라이드 가능하게 전환 — 값 안 넣으면 기존과 동일하게 동작(`utils/gdrive_helper.py`,
  `scrape_daily.py`). ③ `views/macro_view.py`의 미사용 `PYKRX_AVAILABLE`/`from pykrx import stock` 완전 제거(둘 다 코드
  어디서도 참조 안 됨 확인) — 나중에 매크로 재설계(4번 항목)에서 pykrx 실제로 쓰게 되면 그때 다시 추가하면 됨.
  ④ **비밀번호 해시 SHA-256 → bcrypt 전환 지원 추가**: `views/admin_view.py`에 해시 형식을 자동 판별하는
  `_verify_admin_password()` 신설(`$2a$/$2b$/$2y$`로 시작하면 bcrypt, 아니면 기존 SHA-256 — 하위호환 완전 유지, 지금 당장
  아무것도 안 바꿔도 로그인 안 끊김). `requirements.txt`에 `bcrypt` 추가. 오너가 원할 때 로컬에서
  `python -c "import bcrypt,getpass;print(bcrypt.hashpw(getpass.getpass().encode(),bcrypt.gensalt()).decode())"`
  직접 실행(평문 비밀번호가 어디로도 전송 안 됨) → 결과값을 Streamlit Secrets의 `ADMIN_PASSWORD_HASH`에 넣으면 전환 완료.
  구 SHA-256/신 bcrypt 양쪽 다 유닛테스트 통과. ⑤ **git 히스토리 옛 비밀번호 제거(`git filter-repo`)는 보류** —
  지금 크롤링이 진행 중/자동실행 예정이라 동시에 force-push 하면 충돌 위험이 있어, 크롤링이 완전히 끝난 안전한
  시점에 진행하기로 함(task #10). SPEC §5-4 역성장 처리 방식 불일치는 오늘 scoring.py 개편으로 자연 해소.
  총배당금 실제 교차검증은 새 데이터 소스가 필요해 계속 보류.
- **2026-08-06 밤(2차)** — **퀀트 스코어 심각도 세분화 + 만점 계산 통일 + %표시 (오너 지적: "묘하게 점수가 높아서 게임처럼 보인다", "만점이 35점인 것도 있고 100점인 것도 있다").**
  ① `utils/scoring.py`: 역성장/적자·PER 이상치 하드컷오프가 예전엔 flat 15점/10점 + 만점 100(또는 None) 고정이었던 걸,
  다른 정상 채점 경로와 동일하게 "실제 산출 가능한 항목만" 만점(score_max)을 계산하도록 통일. 그 위에서 점수 상한(cap)은
  오너 요청("랜덤한 가중치 말고 금융공학적 표준")대로 **오늘 수집된 종목 전체(횡단면) 분포 대비 z-score + 윈저라이즈**
  (Barra/Fama-French류 퀀트 팩터 모델 표준 기법)로 심각도에 비례해 5~25%(역성장) / 2~12%(PER 오염) / 5~20%(극단고평가)
  사이로 스케일링. population 통계(평균/표준편차)가 없으면 중간값으로 안전하게 대체(크래시·임의값 없음).
  ② `collector_kospi200.py`: 이 z-score 계산에 필요한 population 통계를 얻기 위해 스코어링을 2차 패스로 분리
  (1차: 종목별 raw 지표 수집 → 전체 모인 뒤 평균/표준편차 계산 → 2차: 일괄 스코어링). 표본 5개 미만이면 population
  통계 생략. ③ `views/pegy_view.py`: 퀀트 스코어 배지에 원점수(X점/Y점)뿐 아니라 달성률(%)을 색상과 함께 크게 표시해
  만점이 종목마다 달라도 한눈에 비교되게 함. 합성 데이터 10개 시나리오(역성장 경중 3단계, PER 오염 경중 2단계, 극단고평가
  경중 2단계, Forward결측 정상케이스, population통계 없는 폴백, 정상 저평가 회귀케이스)로 z-score 방향성·순서·폴백 전부 검증 완료.
  ④ 배지에 % 설명을 툴팁에 한 줄 추가했다가 툴팁 박스가 옆/아래 카드로 튀어나가는 렌더링 버그 발생(오너 제보) →
  %가 배지에 이미 보이므로 툴팁 문구는 원래 길이로 되돌려 해결.
- **2026-08-06 밤(2차)** — **Forward ROE 수집 가능성 진단 로그 추가 (task #4, 미완료).** 오너가 "Forward ROE도 모으라고
  했었다"며 스코어링의 영구 결측 항목(Forward ROE·ROIC 30점, 한 번도 수집 로직이 없었음) 보완 필요성 언급.
  `collector_kospi200.py`가 이미 매 종목 fetch하는 네이버 페이지 안에 있는 "주요재무제표" 표(현재는 DPS만 뽑아 씀)에
  ROE 행 + 추정치(E) 컬럼이 같이 있다면 **추가 크롤링 요청 없이** Forward ROE를 채울 수 있을 가능성이 있음 — 다만 이
  세션에서는 샌드박스 네트워크 제한으로 실제 HTML을 열어 확인하지 못했음(오너도 "200종목을 빠르게 훑어보면 실수
  발생 우려"로 직접 확인 보류). 대신 `fetch_naver_item_dps_and_eps()`에 `diag_roe_check` 파라미터를 추가해 **처음
  5종목만** 이 표의 컬럼명과 ROE 행 유무를 로그로 출력하도록 심어둠(수집 로직·네트워크 요청에는 전혀 영향 없음).
  **다음 수집 실행(자동/수동 무관) 로그에서 `[진단/ROE컨센서스확인]`으로 시작하는 줄을 확인하면 Forward ROE 실제 구현
  여부를 판단할 수 있음.**
- **2026-08-06 밤** — 오너가 GitHub Actions 수동 수집(`workflow_dispatch`)을 직접 실행해두고 휴식. 이번 수집 결과가 완료되면 아래 3가지를 **한 번에 검증**할 수 있음: ① 히스테리시스 버퍼(`tracked_count`/`hidden_buffer_count`, 화면 정확히 200장), ② `Collected_At` 값이 매크로 화면에 정상 표시되는지, ③ `scoring.py`의 `forward_available` 버그 수정 이후 시프트업(176)/CJ대한통운류 종목들이 "🔴 실적 역성장/적자"로 정확히 재라벨되는지(수정 전 스크린샷에서는 구버전 JSON이라 여전히 옛 배지가 보였음 — 정상, 재수집 전이라 당연함). **다음 세션 시작 시 이 3가지부터 확인.**
- **2026-08-06** — **버그 수정: "Forward 데이터 없음" 배지와 실제 Forward 카드 표시가 모순되던 문제(CJ대한통운 사례, 오너 제보).** `utils/scoring.py`의 `forward_available` 판정이 `f_pegy is not None`까지 조건에 넣고 있어서, Forward PER·EPS·성장률은 다 있는데 성장률이 마이너스라 PEGY 계산식(분모)이 성립 안 하는 경우(f_pegy=None)까지 "Forward 데이터 없음"으로 오판정 → 배지는 "🔵 Trailing만 검증됨(Forward 데이터 없음)"인데 화면엔 실제 Forward 수치가 버젓이 표시되는 자기모순 발생. `forward_available`을 `f_per`/`growth` 존재 여부만으로 재정의하고, "성장률 마이너스" 케이스는 기존 Guardrail 1(역성장 컷오프)이 정확히 잡아내도록 수정("🔴 실적 역성장/적자" 배지 + 점수 15/100). f_pegy 관련 계산부에 None 방어 코드도 추가. 3개 시나리오(역성장/진짜결측/정상) 회귀 테스트 통과.
- **2026-08-06** — **PEGY 카드 전체 차단 범위 대폭 축소 (오너 지적 반영).** "역성장 종목(롯데지주)", "데이터 검증 실패 종목(롯데케미칼)" 스크린샷을 보고 오너가 "재무제표가 이미 있는 종목까지 전체를 가리면 과거 데이터로 공부를 못 한다"고 지적 → 코드 확인 결과 DataValidator 3단계 하네스는 Trailing(price/t_per/t_eps)끼리의 교차검증만 하고 PBR·DPS·EV-EBITDA·ROE는 전혀 안 건드리는데도 실패 시 카드 전체를 가리고 있었음(설계상 과한 차단). `views/pegy_view.py`를 개편해 **진짜 카드를 못 그리는 2가지(price 없음, 상장주식수 파싱 오류)만 전체 차단**하고, 나머지(역성장·Forward PER 극단치·g_eff 산출불가·일반 하네스 실패)는 Trailing은 정상 노출한 채 "🚀 Forward" 카드 자리에만 사유별 색상(보라/빨강/회색/노랑) 마스킹으로 완화. 단, 하네스는 통과했는데 g_eff만 마이너스인 기존 케이스는 원래대로 정상 카드+빨간 배지 유지(퀀트점수도 그대로 계산됨) — 이번 변경으로 영향받지 않음. 합성 데이터 10개 시나리오로 분기 로직 검증 완료.
- **2026-08-06** — **긴 문장 가독성 개선(줄바꿈 추가).** 오너 스크린샷 피드백(3곳 지적 + 전반적 점검 요청) 반영. `views/pegy_view.py`의 상단 투자주의 배너 2개, 퀀트 스코어/착시저평가 가이드 박스, 그레이엄 넘버 설명 2종, 배당 데이터 미확정 안내문, PER 극단치·역성장·데이터 검증 실패 마스크 카드 설명문에 `<br>` 삽입해 한 줄로 길게 이어지던 문장을 자연스러운 지점에서 줄바꿈. 컴팩트 데이터 행(PER|EPS|PBR 등)과 호버 툴팁(이미 `max-width` 제약 있음)은 실제 문제가 아니라 손대지 않음.
- **2026-08-06** — **PEGY 카드 UI 개선 2건 (오너 스크린샷 피드백 기반).** ① 그레이엄 넘버(Trailing 전용 참고 목표가) 박스가 "🚀 Forward 데이터 없음" 마스크 박스 안에 중첩되어 있던 걸 Trailing 섹션 바로 아래 독립 박스로 이동(`views/pegy_view.py`, `graham_box_html` 분리) — 표시 조건(Forward 결측 시에만)은 그대로. ② 배당 필수 업종(리츠/인프라/금융)인데 DPS·배당수익률이 0으로 수집되면 종목 전체를 차단하던 로직 완화 — 오너 지적("국내 상장사는 아직 주주환원율이 안 높고 실제 무배당 기업도 많아서 이렇게 막으면 과거 지표로도 판단을 못 한다") 반영. `utils/guardrail.py`가 더 이상 `is_unverified=True`로 전체 차단하지 않고 `dividend_data_unverified`/`dividend_unverified_reason` 플래그만 남기며(Trailing·퀀트 점수는 정상 산출), `views/pegy_view.py`는 **"🚀 Forward" 카드 자리에만** 노란색(amber) "주주환원 데이터 검증 대기 중" 배지를 표시(Trailing은 그대로 노출). 합성 데이터로 로직 검증 완료(리츠 DPS=0 → 더 이상 전체 차단 안 됨 / 일반 업종은 플래그 안 켜짐).
- **2026-08-06** — **KOSPI 200 히스테리시스 버퍼 도입 (진입 200위 / 이탈 230위).** GitHub Actions 수집 성공 확인(58분 24초, run #9) 후 착수. `collector_kospi200.py`: ① 순위 수집 상한을 200→250으로 올려 버퍼 판정용 여유분 확보(`fetch_kospi200_real_market_data`), ② 오늘자 파일을 덮어쓰기 전에 `_load_previously_tracked_codes()`로 "어제 추적 중이었던 종목 코드"를 먼저 읽어옴, ③ `apply_hysteresis_buffer()` 신설 — 200위 이내 신규 진입 또는 어제부터 추적 중이고 아직 230위 이내인 종목만 추적 유지, 그 외 제외. 화면 노출 여부는 `is_visible`(순위 200위 이내만 True) 필드로 구분. `views/pegy_view.py`는 로드 직후 `is_visible` 기준으로 필터링해 **화면에는 항상 정확히 200개만** 표시(오너 확정 — 버퍼 구간 종목은 계속 수집만 되고 화면엔 안 보임). 품질지표(`total_count`/`valid_count`)와 요약 중앙값 이력(`pegy_summary_history.json`)은 모두 화면 노출 200개 기준으로만 집계해 배너 숫자가 어긋나지 않게 함. 합성 데이터로 진입/유지/이탈 3가지 경계 케이스 + 부트스트랩(첫 실행) 케이스 전부 유닛 테스트 통과. **다음 자동 수집부터 실제 동작 확인 필요** (오늘 처음 도입이라 아직 실제 운영 데이터로는 검증 안 됨).
- **2026-08-06** — **"코스피 200" 명칭 정직화.** 오너가 "지금 수집하는 게 진짜 KOSPI 200 지수가 아니라 시가총액 상위 종목 아니냐"고 지적 → 코드 확인 결과 정확함(`collector_kospi200.py`가 KRX 공식 지수가 아니라 네이버 `sise_market_sum.naver` 시가총액 순위 페이지를 스크래핑함). 오너 결정: 진짜 지수로 전환하지 않고(범위 밖), **화면·다운로드 파일에 노출되는 "코스피 200" 문구만 전부 "시가총액 상위 200"으로 교체**. 대상: `views/pegy_view.py`(공개 페이지 부제·에러 메시지·다운로드 버튼), `collector_kospi200.py`(JSON `description` 필드·로그·docstring, KRX 공식지수와 다르다는 경고 주석 추가), `.github/workflows/scrape.yml`(수집 스텝 이름), `utils/guardrail.py`(docstring), 이 문서. 파일명(`collector_kospi200.py`, `kospi200_pegy_latest.json`)은 하위 호환을 위해 그대로 유지.
- **2026-08-06** — "동기화/업데이트" 문구 통일 + "실시간" 문구 전면 제거 (오너 지시: "확실하게 절대로 써지면 안되는거야"). ① `market_history.csv`에 `Collected_At`(데이터 수집 완료 시각) 컬럼 신설 — `scrape_daily.py`/`utils/db.py`가 행 저장 시점에 기록. ② `views/macro_view.py`의 "최종 동기화" 표시가 지금까지 **파일 mtime(배포·재시작 시각)** 을 썼던 걸 이 새 컬럼 기반으로 교체 — `views/pegy_view.py`와 **완전히 같은 문구**("📅 마지막 동기화: {시각} (크롤링 완료 후 장마감 데이터 적용)")로 통일. ③ 코드베이스 전수 grep으로 `.py`/`index.html`에 남아있던 "실시간" 표현 전부 제거(주석·로그·툴팁·SEO 메타 태그 포함, 총 11곳) — `scrape_daily.py`, `collector_kospi200.py`, `visiblehand.py`, `views/admin_view.py`, `views/macro_view.py`, `views/pegy_view.py`, `index.html`. `ENGINEERING_SPEC.md`/`AUDIT_REPORT.md` 등 내부 문서에 남은 "실시간"은 규칙 설명·의도된 나쁜 예시·과거 감사기록이라 그대로 둠. **⚠️ `Collected_At`는 새 컬럼이라 다음 GitHub Actions 수집이 최소 1회 돌아야 매크로 화면에 실제 값이 찍힘 — 그 전까지는 "수집 완료 시각 기록 없음(구버전 데이터)"으로 정직하게 표시됨(지어내지 않음).**
- **2026-08-06** — 매크로 화면을 관리자 전용으로 내리면서 공개 화면에서 코스피 지수/환율을 아예 볼 수 없게 된 문제 보완. `views/pegy_view.py`(공개 PEGY 화면) 최상단에 코스피 지수·원/달러 환율 카드 2개 추가. `market_history.csv`(`utils/db.py`의 `HISTORY_FILE`/`COL_MAP` 재사용)에서 최신 값만 가볍게 읽어옴 — 매크로 화면의 14개 지표 로직은 안 건드림. 일반 사용자에겐 소수점이 불필요하다는 오너 판단에 따라 정수로 반올림 표기(예: 6,598 / 1,423원). 데이터 없으면 카드 자체를 숨김(지어내지 않음).
- **2026-08-06** — `views/pegy_view.py`: 배당 표기 정직화. "배당수익률 상세"를 **"작년 배당률 (확정)"**으로 명확히 라벨링(배당은 지급돼야 확정되는 값이라 올해 실시간 지급 내역이 아니라는 점을 툴팁에도 명시). 기존에 존재했지만 UI 마크가 빠져있던 `dps_source == "derived_from_div_yield"`(재무제표 확정 DPS가 없어 배당수익률로 역산한 경우)에도 "🧮 계산값" 배지 추가 — `collector_kospi200.py` 데이터 필드 변경 없이 순수 표시 계층만 수정이라 재수집 불필요.
- **2026-08-06** — GitHub Actions 수집 작업(25분 소요)이 마지막 "깃허브 푸시" 단계에서 `rejected` 에러로 실패. 원인: 25분 크롤링하는 동안 사람이 직접 다른 커밋을 푸시해서 원격이 앞서나가 있었음(수집 자체는 100% 성공, 데이터 유실 아님 — 마지막 업로드만 실패). `.github/workflows/scrape.yml`의 푸시 단계를 **최대 3회 자동 fetch+rebase+재시도**하도록 수정해서 앞으로 이런 타이밍 충돌이 나도 자동 복구되게 함.

모든 항목은 `git log origin/main..HEAD`가 비어있음을 기준으로 **전부 깃허브에 푸시 완료된 상태**입니다.

- **2026-08-05 (오후~야간)** — PEGY 화면 개선 4종 연속 작업: ① 그레이엄 넘버(Trailing 전용 참고 목표가, 적자기업은 산출불가로 정직표기·금융주는 강한경고) 추가, ② Forward 데이터 결측 시 종목 전체 차단 대신 **Forward 섹션만 마스킹**하도록 guardrail/scoring/UI 개편, ③ 대수적 역산(계산값) 허용 예외를 지침서에 신설하고 Trailing EPS 16종목 복구, ④ PEGY 53종목 결측 원인 규명(버그 아닌 네이버 무료 데이터 한계) + DPS "-" 셀 가짜 에러 로그 버그 수정. 상세는 `TASK_HISTORY.md` #15~18.
- **2026-08-05 (오후)** — 오너가 작성한 "절대 준수 메뉴얼"을 `ENGINEERING_SPEC.md` §0-3으로 반영(후행지표 전용 원칙, 크롤링 매너, 신규기능 스테이징 승인 절차 등). 이 원칙에 따라 매크로 방공망 화면을 공개 메뉴에서 내리고 관리자 전용으로 전환.
- **2026-08-05 (오후)** — 배포 크래시 2건 연속 대응: streamlit 버전 미고정으로 인한 GZip 미들웨어 크래시(streamlit==1.50.0 고정), altair가 Python 3.14와 안 맞아 위험지수 차트가 죽던 문제(Plotly로 교체). 위험 기여도 표가 고정높이 iframe에 잘리던 버그도 수정.
- **2026-08-05 (오전~낮)** — PEGY 상장주식수 파싱 오류(외국인소진율 오인) 수정 — FinanceDataReader 구조화 데이터를 1차 출처로 도입. 타임존 버그, 자동화 워크플로우 통합, 죽은 파일 정리, 하드코딩·더미데이터 전수 감사 및 수정(`AUDIT_REPORT.md`) 완료.
- 그 이전 항목(자동화 미작동 진단, 8/4 백필 등)은 `TASK_HISTORY.md` #1~9 참고.

## 4. 지금 열려있는 일 (다음에 반드시 확인/처리할 것)

0. **🟢 `AUDIT_REPORT_V2.md` 27건 + 타임존(UTC/KST) 버그 — 데이터 레벨 검증 전부 완료 (run #14, 2026-08-06 17:59).**
   확인된 것: ① SKC 등 적자 기업 PER 부호유실 버그 해소(t_per/t_eps/graham_target 정직하게 None),
   ② 목표가 캡(현재가×2.5) 걸린 종목 36개 전부 `f_target_capped=True` 정확히 플래그, ③ market_history.csv에
   `SubScore_*`/`Multiplier` 14개 컬럼 실제값으로 정상 생성, ④ 삼성전자우/SK하이닉스 등 고성장 종목 배지
   "🟢 강력 저평가"로 화면 목표가 갭과 일관(`growth_score_capped=True`, 점수만 보수 반영), ⑤ **타임존 버그
   확정 해소** — `last_updated_at`이 "2026-08-06 17:59"로 실제 크롤링 완료 시각과 정확히 일치(KST 정상).
   **참고만 남겨둠(급하지 않음)**: 목표가 캡 종목 "상한 도달" 배지의 화면 스크린샷 육안 확인, 매크로
   화면(관리자 전용) 기여점수 합 재확인은 코드상 이미 반영되어 있어 우선순위 낮음. 상세는
   `AUDIT_REPORT_V2.md`/`TASK_HISTORY.md` #36~38 참고.
1. **🟢 해외주식("미국 주식은 이가격") 페이지 — 550종목 전수 수집 실측 성공(2026-08-07 00:36 ET, 소요 55.5분).** 상세는 §8-8/§8-9/TASK_HISTORY #52·#53·#54 참고.
   - **✅ 전수 수집 완료 확인**: 548종목 추적(+버퍼 0) / 검증 통과 547 (99.8%) / 상태 SUCCESS. 지수 3종도
     경고 없이 정상 표시(등락률 S&P500 -0.16%, 나스닥종합 -0.04%, 다우존스 -0.85%). 실패 2종목(CCZ,
     BTSGU, 둘 다 개별 HTTP 404)은 유니버스 CSV상 상장폐지/티커변경 추정, 정상적으로 실패 목록에
     기록되고 나머지 진행에 영향 없음(설계대로 동작). `data/us_stocks_latest.json`/
     `us_stocks_raw_latest.json`/`us_summary_history.json` 전부 생성 확인.
   - **✅ 임계값 재캘리브레이션 완료 (2026-08-07 밤)**: 실제 548종목 분포 근거로 `utils/constants_us.py`
     §5를 잠정값→확정값으로 전환. 가장 중요한 발견은 PEGY 하드컷오프(피터린치 원전 2.0, 코스피와
     동일값 유지)가 미국은 상위 25%와 거의 같아 550종목 중 21%가 전부 "극단적 고평가" 배지를
     받던 문제 — 오너 확정으로 **미국만** 3.0으로 상향(코스피 2.0은 유지), 8.6%로 정상화.
     상세는 TASK_HISTORY #55.
   - **✅ 한글 음역 품질 검토 완료 (2026-08-07 밤)**: 360개 음역 결과 전수 검토 → 음역기 자체의
     "er/ir" 모음 규칙 누락 버그를 발견·수정(68종목 한 번에 개선) + 잘 알려진 브랜드 48개
     `KR_NAME_OVERRIDES` 정식 등재(스페이스X/클라우드플레어/나스닥/이베이 등). 정식 한글명
     비율 34%→43%로 상승. 상세는 TASK_HISTORY #56.
   - **🟡 화면 육안 확인 진행 중 (2026-08-07 밤, 오너가 실제 배포 사이트에서 직접 검토)**:
     이 과정에서 발견·수정한 것들 —
     ① "고평가/주의 종목 그룹" 프리셋이 345/548(63%)로 과다 표시 → 원인은 키워드 매칭이
     데이터 상태 안내 배지("Trailing만 검증됨" 등)까지 잘못 포함시킨 버그, 수정 후 311개
     (TASK_HISTORY #57, 커밋·푸시 완료).
     ② 버크셔해서웨이 B 같은 저성장 자본집약형 우량주에서 "모델 목표가"가 비정상적으로
     낮게 나오는 구조적 문제(PEGY 역산 공식의 한계) 발견 → ROE·ROIC 우량 게이트를 통과한
     종목에 한해 BPS(장부가)를 참고 하한으로 쓰도록 보정, value trap 종목은 미적용
     (TASK_HISTORY #58, US 전용). **다음 `collect` 재실행부터 화면 반영.**
     ⚠️ 위 임계값·음역 재캘리브레이션(TASK_HISTORY #55·#56)도 마찬가지로 다음 `collect`
     재실행부터 반영됩니다 — 지금 디스크의 `data/us_stocks_latest.json`은 옛 기준 그대로.
   - **✅ GitHub Actions 자동화 연결 완료 (2026-08-07 밤, TASK_HISTORY #60)**: `.github/workflows/scrape_us.yml`
     신설. 서머타임(EDT/EST) 때문에 cron 을 두 줄(20:35 / 21:35 UTC) 걸고, 수집기의
     `collect --skip-if-not-ready` 사전 점검이 "이번에 담길 거래일이 이미 스냅샷에 있으면
     아무것도 안 하고 정상 종료"하게 해 실제 수집은 하루 한 번만 일어나게 했습니다.
     기존 `scrape.yml`(코스피)은 한 줄도 건드리지 않았습니다. 상세는 §5 / §8-10.
   - **✅ 공개 전환 완료 (2026-08-08, 오너 승인 — TASK_HISTORY #61)**: 마지막 관문이던 §0-3-6
     스테이징 절차가 끝났습니다. `visiblehand.py` 사이드바 **상단 공개 메뉴**에서 인증 여부와
     무관하게 미국주식 화면에 진입할 수 있습니다. 화면의 "🧪 [베타 · 검토중]" 배너 제거.
     매크로 방공망은 종전대로 관리자 전용 유지(무수정).
   - **✅ 사이드바 메뉴를 대분류/중분류 구조로 개편 (2026-08-08, TASK_HISTORY #62)**: 공개 전환 당시의
     "미국주식 체크박스 1개"(옵트인 부가기능처럼 읽힘)를 오너 요청으로 **택1 라디오**로 바꿨습니다 —
     대분류 "💡 사실 이 가격이에요" 아래 중분류 "🇰🇷 한국 주식은 이가격이에요"(기본값, 코스피) /
     "🇺🇸 미국 주식은 이가격이에요"(미국). 한국·미국을 대등한 두 리포트로 보여주는 것이 목적이며,
     라우팅은 `st.session_state["selected_market"]` 기준(옛 `view_us_stocks` 키는 폐기).
   - **완료**: 기초틀 실측 검증(2026-08-07 오전) → 전수 수집 CLI(`collect`)·스코어링(`utils/scoring_us.py`)·
     화면(`views/us_stocks_view.py`)·한글 종목명(`utils/company_names_kr.py`)·사이드바 연동까지 코드 완성.
     오프라인 검증 84+81건 통과(실데이터 12종목 전 구간 통과 + 합성 극단케이스 9종 + end-to-end 배선 + 화면 렌더링).
   - **✅ 2026-08-07 오후 1차 수정**: 오너가 로컬 전수수집을 처음 실행하며 발견한 문제 2건.
     ① 지수 3종 소스가 Stooq CSV에서 전부 HTTP 404 → stockanalysis.com ETF 프록시(SPY/ONEQ/DIA)로 교체,
     화면엔 절대가 대신 **당일 등락률(%)만** 표시(ETF 주가≠지수 포인트값 혼동 방지, 오너 확정).
     ② 550종목 전수수집이 187종목째에서 HTTP 429 차단 → 60종목마다 90초 추가 휴지기 도입.
   - **✅ 2026-08-07 오후 2차 수정**: 오너가 1차 수정본을 바로 재실행하며 또 발견한 문제 2건.
     ① `indices` 실행 시 등락률은 정상인데 "'Index Tracked' 라벨을 못 찾음" 경고가 매번 뜸 —
     원인은 그 라벨이 통계 표(`<table>`)가 아니라 표 밖 "About" 섹션에 있었던 것, 표 밖 라벨도
     찾는 폴백 스캐너 추가로 해결(경고 사라짐 확인은 오너 재실행으로 확인 필요).
     ② 배치 휴지기를 늘렸는데도 이번엔 290종목째에서 다시 HTTP 429 — 정확한 rate limit 규칙을
     모르는 채 값만 계속 키우는 건 도박이라, **체크포인트 기반 이어하기**를 주 대응책으로 도입:
     차단되면 진행상황을 `data/us_collect_checkpoint.json`에 저장하고 **다음 실행이 처음부터 다시
     하지 않고 남은 종목만 이어서** 진행합니다(같은 세션 날짜에서만 재사용). 완주하면 자동 정리.
   - **🔴 오너가 로컬에서 다시 실행해서 확인해야 할 것**: ① `python collector_us_stocks.py indices`
     (경고 없이 3종 다 정상 표시되는지, 10초), ② `python collector_us_stocks.py collect` — 이번에
     또 HTTP 429로 끊기면 당황하지 말고 **잠시 후 같은 명령을 그대로 다시 실행**하면 체크포인트에서
     이어서 진행됩니다. 여러 번 재실행해서라도 결국 550종목이 다 모이는지 확인 필요 → 그 결과로
     잠정 임계값 재캘리브레이션.
   - **남은 것**: 🟢 **이 페이지는 사실상 마무리됐습니다.** 참고 항목으로 상단 지수 2차 출처 확보
     (§0-3-3 미충족 상태)만 남아있습니다 — 오너 판단으로 공개 블로커가 아니며, 화면에 이미
     "근사치입니다" 고지가 붙어 있습니다. (임계값 확정·GitHub Actions 연결·공개 전환은 각각
     TASK_HISTORY #55 / #60 / #61 로 완료)
2. **🔴 매크로 14개 위험 지표 재설계.** "추정 프록시" 라벨링만으로는 부족 — 후행지표(장마감 후 확정 데이터)로 실제 연결 가능한 것만 붙이고, 안 되는 지표는 화면에서 제거 검토. 조사된 무료 출처: KRX Open API(옵션 미결제약정), pykrx(VKOSPI·공매도, 단 공매도는 T+2 지연), 한국은행 ECOS(외환 스왑). ELS 낙인위험은 매칭되는 공개 데이터가 없을 가능성 높음. (매크로 화면 자체가 지금 비공개라 급하지 않음. **위 1번 해외주식 페이지 완료 후 착수, 오너 확정 2026-08-06**)
3. **🟡 관리자 비밀번호(`ADMIN_PASSWORD_HASH`) 설정 완료됨 (2026-08-05 확인).** 로그인 정상 작동 확인함 — 별도 조치 불필요, 참고로만 남겨둠.
4. **🟢 `AUDIT_REPORT.md` 보류 항목 대부분 정리 완료 (2026-08-06).** 죽은 파일 삭제, `TARGET_FOLDER_ID` 환경변수화,
   `PYKRX_AVAILABLE` 미사용 코드 제거, bcrypt 비밀번호 해시 지원 추가(하위호환 유지) 완료. SPEC §5-4 불일치는 scoring.py
   개편으로 자연 해소. **✅ git 히스토리 옛 비밀번호 제거 완료 (2026-08-07 밤, task #10)**: 오너가 이미 비밀번호를
   다른 값으로 로테이션해둔 것 확인 후 진행. `git filter-repo --replace-text`로 커밋 `80bb132` 등 전체 히스토리(214개
   커밋)에서 평문 비밀번호(`MOONvisiblehand9118`)와 그 SHA-256 해시를 치환·제거, `git push --force`로 GitHub에 반영
   완료. 재작성 전 전체 폴더 백업 후 진행, 재작성 후 히스토리 전수 검색으로 잔존 여부 0건 확인. 브랜치가 `main` 하나뿐이고
   다른 클론(협업자) 없는 프로젝트라 강제 푸시 리스크 낮음. **남은 것**: 총배당금 실제 교차검증(새 데이터 소스 필요해
   계속 보류, 급하지 않음)만 남음.
5. **⚪ 참고만** — `GEMINI_API_KEY` 미등록이어도 자동화 자체는 정상 작동(AI 코멘트 기능만 건너뜀).

> **처리 순서(오너 확정, 2026-08-06): 0번 완료 → 4(AUDIT 잔여, git filter-repo) → 1(해외주식 페이지) → 2(매크로 재설계).**
> **✅ 4번(git filter-repo) 완료(2026-08-07 밤).** 3번은 이미 완료 상태라 순서에서 제외.
> **✅ 1번(해외주식 페이지) 사실상 완료(2026-08-08, 공개 전환 — TASK_HISTORY #61).**
> **➡️ 다음 작업은 2번(매크로 14개 위험 지표 재설계)입니다.**

## 5. 자동화 동작 방식 요약

**🇰🇷 코스피 — `.github/workflows/scrape.yml`**

- 트리거: 매일 평일 KST 16:05 (cron), 또는 Actions 탭에서 수동(`Run workflow`)
- 순서: `collector_kospi200.py`(종목 데이터) → `scrape_daily.py`(매크로 데이터) → 변경사항 `git commit & push`
- 수동 실행 시 `target_date` 입력칸에 `YYYY-MM-DD`를 넣으면 그 날짜만 콕 집어 보정(백필) 가능. 이때는 실시간 시세 조회를 건너뛰고 그 날짜의 실제 종가만 사용함 (오늘 시세로 과거가 오염되는 걸 방지)

**🇺🇸 미국주식 — `.github/workflows/scrape_us.yml`** (2026-08-07 신설, TASK_HISTORY #60)

- 트리거: 매일 평일 **20:35 UTC / 21:35 UTC 두 번** (cron 2줄), 또는 Actions 탭에서 수동(`Run workflow`)
- **왜 두 번인가**: 우리가 수집해야 할 시각은 언제나 "미국 정규장 마감 16:00 ET + 30분 = 16:30 ET"인데,
  GitHub Actions의 cron은 UTC 고정이라 서머타임에 따라 대응 UTC 시각이 1시간 움직입니다.
  EDT(3~11월) → 20:30 UTC, EST(11~3월) → 21:30 UTC (둘 다 `describe_collection_schedule()`을
  실제 호출해 확인한 값. 정각 혼잡을 피해 5분씩 뒤로 밀어 20:35/21:35로 설정).
- **그래도 실제 수집은 하루 한 번**: 수집기가 `--skip-if-not-ready` 사전 점검을 합니다. 판정 기준은
  "이번에 담기게 될 거래일(session_date)을 기존 스냅샷이 이미 담고 있는가" —
  이미 있으면 아무것도 하지 않고 정상 종료(exit 0)합니다. 서머타임 전환일에도 자동으로 맞습니다.
- **덤**: 앞쪽 실행이 HTTP 429로 끊기면 스냅샷이 갱신되지 않으므로 뒤쪽 실행이 같은 날 자동 재시도합니다
  (체크포인트 `data/us_collect_checkpoint.json`에서 이어받아 처음부터 다시 하지 않음).
- 순서: `collector_us_stocks.py collect --skip-if-not-ready` → `data/us_*.json` 변경분 `git commit & push`
  (푸시 거부 시 fetch+rebase 최대 3회 재시도 — 코스피 워크플로우와 동일 패턴)
- 수동 실행 시 `force` 체크박스를 켜면 사전 점검을 무시하고 같은 거래일도 다시 수집합니다.
- 시크릿 불필요(`GEMINI_API_KEY` 등 안 씀 — 미국주식 수집기에는 AI 코멘트 기능이 없습니다).

## 6. 다음에 코드를 또 고칠 때 지켜야 할 원칙

`ENGINEERING_SPEC.md`의 최우선 규칙: **데이터를 못 가져오면 하드코딩된 값이나 그럴듯한 가짜 숫자로 조용히 채우지 말고, 명확한 오류를 내거나 화면에 "데이터 없음"으로 표시할 것.** 단, 아래 두 가지 예외는 오너 승인으로 허용됨(둘 다 §0-1에 조건 명시):

- **계산값 예외**: 실측 불가 + 순수 사칙연산 + 입력값도 실측일 때만, 대수적 역산 허용(반드시 "계산값" 마크).
- **섹션별 마스킹**: 일부 데이터(주로 Forward/미래 추정)만 없으면 종목 전체를 막지 말고, 그 섹션만 마스킹하고 나머지는 정상 노출.

이 프로젝트는 **후행지표 전용**입니다(§0-3-1) — 실시간 데이터를 다루지 않고, 장마감 후 확정된 데이터만 사용합니다.

## 7. 로컬-GitHub 데이터 동기화 원칙 (2026-08-06 추가)

자동 크롤링(GitHub Actions)은 GitHub 원격 저장소에만 데이터를 쌓고, 로컬 작업 폴더는 오너가 수동으로
`git pull` 해야만 최신 상태가 됩니다. 시간 기반 자동 동기화는 오너 컴퓨터가 항상 켜져 있지 않아서
적용하지 않기로 함(2026-08-06 논의). 대신: **세션 시작 시, 또는 크롤링 결과 확인·데이터 검증 등 데이터
관련 작업을 요청받으면, 먼저 로컬이 GitHub 최신 상태인지 확인하고 뒤처져 있으면 pull부터 안내할 것.**
오늘 로컬/원격(raw.githubusercontent CDN 캐시)/실제 서비스(Streamlit) 세 곳이 서로 다른 시점을 보여줘서
혼선을 겪었던 것이 이 원칙을 세운 계기.

## 8. 해외주식 페이지 작업지시서 (2026-08-06 밤 초안 — 착수 전 조사 결과)

### 8-1. 범위 (오너 마인드맵 원안, 그대로 유효)
나스닥+뉴욕 시가총액 상위 약 550개(ETF 제외), 티커/영문명/한글명 표기, 4개 지수 2x2 배치,
30개씩 페이지네이션, 장마감+30분 시점 수집.

### 8-2. 데이터 소스 조사 결과
- **종목 유니버스(순위표)**: `https://raw.githubusercontent.com/Ate329/top-us-stock-tickers/main/tickers/all.csv`
  — 무료·무인증·매일 자동 갱신(GitHub Actions, NASDAQ Stock Screener API 기반), 시가총액 내림차순 정렬,
  나스닥·뉴욕 상장 종목 전부 포함(JPM/XOM 등 NYSE 종목도 확인됨). 다만 채권/우선주/노트류(예:
  "5.350% Global Notes", "Junior Subordinated Notes")와 복수 상장클래스(BRK/A·BRK/B, GOOGL·GOOG)가
  섞여 있어 우리 쪽에서 "보통주 1클래스만" 필터링하는 로직이 필요함(코스피 우선주 처리 로직과 유사한 패턴).
- **펀더멘털(Trailing+Forward)**: `stockanalysis.com`의 종목별 `/stocks/{ticker}/statistics/` 페이지를
  네이버 크롤링과 동일한 방식(개별 페이지 스크래핑)으로 수집. **시가총액 순위 ~500위권 소형주(RBC Bearings)로
  실측 테스트한 결과** PER/Forward PER/PEG/ROE/ROIC/EPS/EPS 3개년 성장 전망/배당/시가총액/장부가/EV-EBITDA/
  애널리스트 목표주가까지 한 페이지에서 전부 확인됨 — 소형주라고 데이터가 부실하지 않았음(오너가 제안한
  "정보 적은 소형주부터 테스트" 방식이 실제로 이 판단의 근거가 됨). 다만 3자 애그리게이터라 크롤링 매너
  원칙(ENGINEERING_SPEC §0-3-2, 종목당 정중한 슬립) 동일 적용 필요.
- **보류(당장 불필요)**: Finnhub(무료 API, 분당 60콜, 회원가입+키 발급 필요, Forward 데이터 무료 포함
  여부 미확인) — stockanalysis.com 단일 소스로 커버가 충분해 보여 1차 개발에서는 보류. 필요해지면
  대체/보완 소스로 재검토.
- **보류(당장 불필요)**: Stooq(가격 히스토리 벌크 다운로드, API 아님, 이번 세션엔 접근 실패) — 나중에
  "3개월 이동평균" 같은 자체 히스토리 기반 기능을 만들 때 재검토.

### 8-3. 이번 세션에서 확립된, 새 기능에도 그대로 적용해야 할 원칙
- **`utils/constants.py` 패턴 재사용, 단 값은 미국 시장용으로 별도 캘리브레이션.** ROE 프리미엄 기준선(12%),
  목표 PER 캡(25배) 등은 코스피 기준 상수라 그대로 재사용 불가 — 미국 시장 전용 상수 세트를 새로 만들 것.
- **타임존은 미국 동부시간(America/New_York, 서머타임 있음) 기준으로 별도 처리.** 오늘 KST 버그를 겪은
  것과 같은 실수(naive `datetime.now()`)를 처음부터 만들지 않도록, `zoneinfo.ZoneInfo("America/New_York")`로
  전용 헬퍼(`_now_et()` 등)를 처음부터 도입.
  - **함정 주의**: 미국 장마감(4:00 PM ET)이 서머타임 여부에 따라 KST로는 오전 5시(서머타임)~오전 6시
    (표준시)로 날짜가 바뀐다. "장마감+30분 수집"을 KST 크론으로 스케줄링할 때 날짜 경계 버그가 나기 쉬우니
    반드시 ET 기준으로 날짜를 계산하고, KST 변환은 화면 표시용으로만 쓸 것.
- **처음부터 고정 임계값 대신 절대거리/z-score 비례 스케일링으로 설계**(이번 세션 내내 잡은 "flat 하드코딩을
  계산값처럼 보여주는" 패턴 재발 방지 — 나중에 감사해서 고치지 말고 처음부터 이렇게 설계).
- **히스테리시스 버퍼**(진입/이탈 기준 다르게) 그대로 재사용 가능 — `apply_hysteresis_buffer()` 참고.
- **Forward 데이터 없으면 섹션만 마스킹**(ENGINEERING_SPEC §0-1 예외 조항) — 오너 지시대로 Trailing부터
  우선 출시하고 Forward는 되는 대로 채우는 방식으로 처음부터 설계.
- **"지어내지 않기" 원칙**(§0-1)은 당연히 동일 적용 — 특히 stockanalysis.com에 없는 필드(예: 배당 없는
  종목의 배당수익률)를 0으로 채우지 말고 None/데이터없음으로 정직하게 표시.

### 8-4. 소요시간 추정 현황 (2026-08-06 밤 갱신)
- **여전히 실측 전입니다.** 이번 세션 샌드박스가 외부 네트워크 직결이 막혀(프록시 403) `requests` 기반
  실행을 못 했습니다. 대신 소요시간을 자동으로 재추정해 출력하는 하네스를 코드에 심어뒀습니다.
- **현재 추정치: 약 27~41분 / 550종목** — 근거는 "종목당 = 페이지 요청 시간 + 정중한 슬립(2.0~3.0초,
  코스피와 동일 상수)". 요청 시간을 0.5~1.5초로 잡으면 종목당 3.0~4.5초 → 550종목 27~41분입니다.
  ⚠️ 이건 **실측이 아니라 슬립 상수 기반 계산**이므로 확정값으로 인용하지 마세요.
  (참고: 코스피 200종목이 50~58분 걸린 건 종목당 페이지 fetch 외에 FDR 변동성 조회·재무제표 표
  파싱까지 하기 때문이며, 미국 쪽은 현재 종목당 페이지 1회 요청 설계입니다.)
- **확정 방법**: 오너가 로컬에서 `python collector_us_stocks.py sample --count 12` 1회 실행 →
  종목당 평균/중앙값/최대 요청시간과 "550종목 재추정 X분"이 화면에 바로 출력되고,
  `data/us_sample/sample_raw_*.json` / `sample_processed_*.json`에 raw·가공 결과가 분리 저장됩니다.

### 8-5. 확정된 결정 (오너 확정, 2026-08-06 밤)
- **통화 표시: 달러(USD)로만 표시.** 원화 환산/병기 없음 — 코스피 페이지처럼 원화로 바꾸지 않고,
  미국 주식 페이지는 가격·시가총액·목표주가 등 전부 수집된 원본 통화(USD) 그대로 노출.
- **종목 유니버스 필터링 3단계 (오너 확정, "한국 우선주랑 느낌이 다르다"는 지적 반영):**
  1. **채권/노트류 제외** — 이름에 "%"(이자율)와 "Notes"/"Debentures"/"Bond" 패턴이 같이 나오면 채권으로
     판정해 제외(예: "AT&T Inc. 5.350% Global Notes due 2066", "Duke Energy ... Junior Subordinated
     Debentures due 2078").
  2. **미국식 우선주 제외** — 이름에 "Preferred Stock"이 명시된 종목 제외(예: "Alphabet Inc. ... Series A
     Mandatory Convertible Preferred Stock", 티커 GOOGM/GOOGN). 한국 우선주(삼성전자우 등, 실거래 활발한
     준-보통주 성격)와 달리 미국 우선주는 고정배당·채권에 가까운 성격이라 이 프로젝트의 "PEGY 성장주
     밸류에이션" 취지와 안 맞음 — 코스피처럼 우선주 상속(`inherited_from_common`) 로직 적용 대상이 아님.
  3. **동일 회사 복수 보통주 클래스는 전부 포함(중복제거 안 함) — 오너 정정 지시(2026-08-06 밤).**
     GOOGL/GOOG(Alphabet), BRK/A/BRK/B(Berkshire Hathaway)처럼 회사명이 동일하게 찍히는 복수 클래스는
     **우선주가 아니라 의결권 차이만 있는 보통주**라서 1·2번 필터에는 안 걸림 — 이건 맞음. 다만 처음
     제안했던 "대표 1개만 남기기"는 **철회**: 클래스별로 가격이 크게 다를 수 있고(BRK/A ≈ 74만 달러 vs
     BRK/B ≈ 495달러), 의결권 차이는 해외투자자 입장에선 중요하지 않으므로 각 클래스를 별도 종목으로
     전부 포함. 즉 한 회사가 550개 슬롯 중 여러 자리를 차지하는 것을 허용함.

### 8-6. 이번 세션 산출물 (2026-08-06 밤, 오푸스 — 기초틀 구축)

**만든 파일 (전부 신규. 기존 코스피 파일은 한 줄도 수정하지 않음)**

| 파일 | 내용 |
|---|---|
| `collector_us_stocks.py` | 유니버스 수집·필터, 상품유형 분류, 히스테리시스(550/600), 통계 페이지 파서(41필드), 장마감 종가 파서, `_now_et()`/ET 세션 계산, 샘플 프로토타입, CLI 3종 |
| `utils/constants_us.py` | 미국 전용 상수. 밸류에이션 임계값은 전부 🚧잠정값 + TODO(전수 수집 후 재캘리브레이션)🚧 |
| `tests/test_us_stocks.py` | 오프라인 검증 74건 (네트워크 불필요, `python tests/test_us_stocks.py`) |

**CLI**
```
python collector_us_stocks.py schedule            # ET 기준 수집 시점 + UTC cron 힌트
python collector_us_stocks.py universe            # 유니버스 CSV → 필터 → data/us_universe_latest.json
python collector_us_stocks.py sample --count 12   # 샘플 수집 + 소요시간 실측 + 550종목 재추정
```

**실데이터로 확인된 사실 (유니버스 CSV 상위 929행 기준)**
- 제외 대상: 우선주 26 · 채권/노트 15 · 하이브리드 유닛 2 · 워런트 1 · 구조화신탁 1 = **45건(4.8%)**
- **550위 컷오프는 시가총액 약 $15.2B 부근**이고, 그 구간 안에서 제외되는 건 우선주 19 · 채권 6 · 유닛 2 = 27건
- 실제로 섞여 있던 것들: `GOOGM/GOOGN`(알파벳 우선주), `TBB`(AT&T 5.350% Notes), `AGNCN~AGNCM`(우선주 6종),
  `SOMN`/`PPLC`(Corporate Units), `GJS`(STRATS 신탁), `CORZZ`(워런트) →
  **MLP 보통유닛(`ET`/`MPLX`/`PAA` 등)과 ADR(`SE`/`TME`/`RTO` 등)은 보통주로 판정해 포함**
- stockanalysis.com 커버리지: 시총 $7.3B 소형주(PLXS)까지 PER/Forward PER/PEG/ROE/ROIC/EPS/BPS/배당/
  목표주가/3년 성장전망 전부 제공 — 대형주와 차이 없음 (8종목 실측: ROK·BRK.B·CAVA·IBRX·MAC·JPM·ET·PLXS)
- 티커 표기 차이 확인: 유니버스 CSV는 `BRK/B`(슬래시), stockanalysis.com은 `BRK.B`(점) → 변환 로직 반영
- 적자기업은 PER을 음수로 주지 않고 `n/a`로 주며, EPS 라벨이 `Earnings Per Share (EPS)` → `Loss Per Share`로
  **바뀝니다**(네이버와 다름). 리츠는 `Price/FFO Ratio` 행이 추가되고 PER은 n/a. 은행은 ROIC/EV·EBITDA가 n/a.
  `Graham Number`/`Lynch Fair Value`는 무료 페이지에서 `Upgrade`(유료)로 표시 → 숫자로 오인하지 않도록 처리함.

**이번 세션에서 하지 못한 것 (정직 기록, §0-1)**
- 샌드박스 외부 네트워크가 프록시에서 차단(403)돼 `requests` 실행을 못 했습니다. 그래서
  ① HTML → (라벨, 값) 추출 계층(실제 태그 구조 대응)과 ② 종목당 실제 소요시간은 **실측하지 못했습니다.**
  검증한 것은 "실제 페이지 원문 라벨/값"을 입력으로 하는 매핑·파싱 계층과 필터·타임존 로직입니다.
- 따라서 **첫 로컬 실행 시 확인해야 할 것**: `sample` 로그에서 core 필드 커버리지가 12/12 종목 모두
  높게 나오는지, `price=` 값이 채워지는지(비면 종가 블록 셀렉터를 실제 HTML에 맞게 보정 필요),
  `⚠️` 표시가 뜨는 종목이 있는지.

### 8-7. 다음 단계에서 오너에게 확인할 것 (기존 §7 항목 + 이번에 새로 생긴 것)

1. **✅ 확정(오너, 2026-08-07)**: 워런트·라이츠·하이브리드유닛(Corporate Units)·신탁(STRATS 등)은
   전부 제외. 오너 판단 기준: "우선주 느낌으로 제공되는 건 다 빼도 된다" — 워런트/라이츠는 실적·배당
   개념 자체가 없는 옵션형 파생상품, 하이브리드유닛은 워런트가 섞인 번들 상품, 신탁은 세금·법률
   목적으로 신탁 껍데기를 씌운 사실상의 채권형 우선주라 전부 이 기준에 부합. 코드 기본값과 일치 —
   추가 수정 불필요.
2. **✅ 확정(오너, 2026-08-07)**: MLP(Energy Transfer 등 Common Units)와 ADR(Sea·TME 등)은 포함.
   오너 판단 기준: MLP는 이름만 "유닛"이지 실제로는 진짜 사업체(주로 에너지 파이프라인)의 정상적인
   소유권이며 법적 형태만 파트너십일 뿐(PER/배당수익률 등 정상 산출됨, ET 실측: PER 13.93·배당수익률
   6.69%) — "우선주 느낌"이 아니라 일반 주식에 가까움. ADR은 나스닥/뉴욕에 상장된 해외기업 보통주로
   이해. 코드 기본값과 일치 — 추가 수정 불필요.
3. **✅ 확정(오너, 2026-08-07)**: 상단 지수는 **S&P500 / 나스닥종합 / 다우존스 3개**(러셀2000 제외).
   원래 마인드맵 "4개 지수 2x2 배치"에서 변경 — **2x2 그리드 강제 안 함, 가독성 우선**으로 3개를
   보기 좋게 배치(1행 3열 등 자유 구성).
4. **✅ 확정(오너, 2026-08-07)**: 회사명 한글 표기는 **하이브리드 방식**으로 진행 — 삼성전자/애플/
   엔비디아처럼 한국에서 이미 널리 쓰이는 정식 한글명이 있는 종목은 그대로 사용, 나머지(한국에 잘
   안 알려진 하위권 소형주 등)는 회사명을 발음대로 한글 음역(예: "RBC Bearings Incorporated" →
   "RBC 베어링스") — 번역이 아니라 기계적 음역이라 지어내기(§0-1) 위반 아님, 오너가 나중에 개별
   종목 이름을 직접 고칠 수 있게 설계할 것.
5. **✅ 확정(오너, 2026-08-07)**: 페이지 UI는 코스피 카드 레이아웃과 동일하게 진행.

**§8-7 전체 5개 항목 확정 완료 → 2026-08-07 세션에서 전부 코드로 반영 완료(§8-8 참고).**
다음은 오너 로컬 실행(§8-9)입니다.

### 8-8. 2차 세션 산출물 (2026-08-07, 오푸스 — 전수 수집 + 스코어링 + 화면)

**만든 파일 (전부 신규. 기존 코스피 파일은 한 줄도 수정하지 않음)**

| 파일 | 내용 |
|---|---|
| `utils/scoring_us.py` | 파생 밸류에이션(`derive_valuation`) + 퀀트 스코어(`calculate_us_quant_score`) + 2차 패스 드라이버(`score_all`) + 미국판 가드레일(`apply_us_guardrail`) |
| `views/us_stocks_view.py` | 미국주식 화면(당시 관리자 전용 베타 → **2026-08-08 공개 전환**). 상단 지수 3종 + 코스피 동일 카드 구조 + USD 단독 표기 |
| `utils/company_names_kr.py` | 정식 한글명 사전(약 190개, 티커 키) + 규칙 기반 음역 폴백 |
| `tests/test_us_scoring.py` | 오프라인 검증 (`python tests/test_us_scoring.py`, 2026-08-07 ETF 프록시 반영 후 전체 통과) |

**수정한 파일**

| 파일 | 내용 |
|---|---|
| `collector_us_stocks.py` | `collect`(전수 수집)·`indices`(지수 점검) 서브커맨드, `run_us_collector()`, `fetch_index_quotes()`/`fetch_one_index_quote()`(2026-08-07 ETF 프록시로 교체), `_polite_sleep_with_batch_cooldown()`(2026-08-07 429 대응), `update_us_summary_history()` 추가 |
| `utils/constants_us.py` | g_eff 2중 캡·고성장 보수화·베타 기준선·F-Score·수집 품질 임계값·업종 키워드·지수 소스 정의(2026-08-07: Stooq→ETF 프록시) + 배치 휴지기 상수(2026-08-07) 추가 (전부 🚧잠정값+TODO🚧) |
| `visiblehand.py` | 당시엔 관리자 인증 후에만 보이는 "🇺🇸 미국주식 (베타, 검토중)" 사이드바 체크박스 + 라우팅 → **2026-08-08 공개 메뉴로 이동, 라벨도 "🇺🇸 미국 주식은 이가격 (미국 밸류에이션 리포트)"로 변경** |

**CLI (추가분)**
```
python collector_us_stocks.py indices              # 상단 지수 3종 소스 점검 (ETF 프록시, 2026-08-07 실측 검증)
python collector_us_stocks.py collect --limit 5    # 전수 수집 경로 리허설(5종목만)
python collector_us_stocks.py collect              # 550종목 전수 수집 (약 40분, 배치 휴지기 포함)
```

**설계상 코스피와 다르게 한 것(그리고 그 이유)**
- **재무 건전성 10점을 Trailing ROE → 피오트로스키 F-Score 로 교체**: 미국은 자본효율성 카테고리에서 이미 Trailing ROE를 쓰므로, 코스피 배점을 그대로 베끼면 **같은 지표를 두 번 계상**하게 됩니다. F-Score는 소스가 실측으로 제공합니다.
- **변동성을 g_eff 분모에 곱하지 않음**: 코스피는 20거래일 실측 변동성이 있지만 미국 수집기는 가격 시계열을 받지 않고 **베타(5Y)** 만 있습니다. 베타는 절대 변동성이 아니라 시장 민감도라 성격이 달라, 근거를 댈 수 없는 배수를 PEGY·목표가에 곱하는 대신 **점수 항목(5점)으로만** 반영했습니다(이중 계상도 자동으로 없음).
- **주주환원율에 자사주 매입 포함**: 소스가 Shareholder Yield(배당+자사주)를 실측 제공합니다. 미국은 배당보다 자사주 비중이 큰 기업이 많고, **희석 시 음수가 될 수 있어**(실측 12종목 중 GLW -0.09%, INCY -3.09%) 0으로 자르지 않고 그대로 반영합니다.
- **Forward EPS는 계산값**: 소스가 Forward EPS를 직접 주지 않아 `장마감 종가 ÷ Forward PER`로 역산합니다. §0-1 예시2-보충의 4개 조건(실측 불가 / 미래추정 아닌 순수 사칙연산 / 입력 둘 다 실측 / 계산값 마킹)을 전부 충족하며, `f_eps_calculated` 플래그와 화면 "🧮 계산값" 배지로 실측값과 구분합니다.
- **애널리스트 목표주가를 별도로 노출**: 소스가 주는 실측 컨센서스 목표가와 우리 PEGY 모델 목표가는 **다른 값**이라, 하나로 합치지 않고 카드에 나란히 보여주고 툴팁에 "어느 쪽도 정답이 아니다"라고 명시했습니다.

**이번 세션에서 하지 못한 것 (정직 기록, §0-1)**
- 샌드박스 외부 네트워크가 여전히 프록시 차단(403)이라 **실제 550종목 수집을 실행하지 못했습니다.** 검증은 실데이터 12종목(오너가 로컬에서 받아온 결과) + 합성 극단케이스 + `_http_get`만 가짜로 바꾼 end-to-end 배선 테스트로 대신했습니다.
- **지수는 현재 단일 출처**라 §0-3-3(신규 지표는 2개 이상 출처)을 아직 못 지켰습니다. 2차 출처를 실측 확인 없이 지어내지 않고 비워뒀습니다 — 추후 보완 대상.
- ~~`utils/constants_us.py`의 밸류에이션 임계값은 **여전히 전부 잠정값**입니다. 화면 상단에 "🧪 베타 · 검토중" 배너로 이 사실을 노출하고 있습니다.~~ → **✅ 해소**: 2026-08-07 실측 재캘리브레이션으로 §5 전 항목 확정(#55), 2026-08-08 공개 전환과 함께 배너 제거(#61).

**2026-08-07 오후 추가 수정 (오너 로컬 실행에서 실제로 드러난 문제 2건)**
- 상단 지수 소스(Stooq EOD CSV)가 오너 로컬 실행에서 3종 전부 HTTP 404로 확인됨 → stockanalysis.com ETF
  프록시(SPY/ONEQ/DIA)로 교체, 실측 검증 완료(자세한 내용은 §4 항목1, TASK_HISTORY #52 참고).
- 550종목 전수수집이 187종목째에서 HTTP 429 차단(설계대로 안전 중단됨) → 배치 휴지기 추가.

### 8-9. 오너가 로컬에서 실행/확인해야 할 것 (2026-08-07 신설)

**실행 순서 (앞에서부터 차례대로)**

1. `python tests/test_us_scoring.py` 및 `python tests/test_us_stocks.py` — 오프라인 검증 (네트워크 불필요, 수 초)
2. `python collector_us_stocks.py indices` — **상단 지수 소스 확인 (2026-08-07 ETF 프록시로 교체, 10초).**
   3개 다 `등락률=` 값이 찍히면 정상입니다. 실패하면 화면 지수 카드가 "데이터 없음"으로 뜨므로
   에러 사유를 확인해야 합니다.
3. `python collector_us_stocks.py collect --limit 5` — 전수 경로 리허설(약 1분). 실제 HTML로
   수집→스코어링→저장이 도는지 확인하고, `data/us_stocks_latest.json`이 생기는지 봅니다.
4. **미국 장마감 30분 이후에** `python collector_us_stocks.py collect` — 550종목 전수 수집. ⚠️ 장중에
   돌리면 종가가 아니라 직전 거래일 값이 담깁니다(스크립트가 경고를 출력합니다). ⚠️ 이전 두 번의
   실행에서 187종목·290종목째 각각 HTTP 429 차단이 있었습니다 — 이번에도 차단되면 **당황하지 말고
   잠시(몇 분) 기다렸다가 같은 명령을 그대로 다시 실행**하세요. 체크포인트(`data/us_collect_checkpoint.json`)에서
   이어서 진행되므로 처음부터 다시 하지 않습니다. 여러 번 재실행해야 할 수도 있습니다 — 최종적으로
   550종목이 다 모이는지가 중요합니다.
5. Streamlit 화면 사이드바 상단 "🇺🇸 미국 주식은 이가격 (미국 밸류에이션 리포트)" 체크 → 카드 육안 확인
   (2026-08-08 공개 전환 전에는 관리자 로그인이 필요했지만 이제 불필요합니다)
   (상단 지수 3개가 등락률(%)만 표시되고 절대가/포인트값은 안 보이는 게 정상입니다).

**확인 후 오너에게 물어봐야 할 것**
- **한글 종목명 음역 품질**: 상위 100~150개는 정식 사전으로 덮여 있지만, 그 아래 종목은 자동 음역이라
  어색한 이름이 나옵니다(예: Plexus → "플레크서스", 실제 통용은 "플렉서스"). 몇 개나 손볼지, 아니면
  하위권은 영문명만 볼지 결정 필요. 고치는 방법은 `utils/company_names_kr.py`의 `KR_NAME_OVERRIDES`에
  `"티커": "원하는 한글명"` 한 줄 추가가 전부입니다.
- **~~잠정 임계값 재캘리브레이션~~ → ✅ 2026-08-07 완료(TASK_HISTORY #55).** 실측 548종목 분포로
  `utils/constants_us.py` §5 전 항목을 확정했습니다. 이 전제조건이 풀리면서 "🧪 베타 · 검토중"
  배너도 2026-08-08 공개 전환과 함께 내렸습니다(#61).
- **~~GitHub Actions 자동화 연결 여부~~ → ✅ 2026-08-07 완료.** 권장했던 방식(cron 2개 + 스크립트
  방어 로직) 그대로 구현했습니다. 상세는 아래 §8-10 / TASK_HISTORY #60. 오너는 더 이상 로컬에서
  매일 `collect`를 돌릴 필요가 없습니다(원하면 언제든 수동 실행은 가능).
- **공개 전환 시점(§0-3-6) → ✅ 2026-08-08 완료(TASK_HISTORY #61).** 오너가 명시적으로 승인해
  `visiblehand.py` 사이드바 **공개 메뉴**로 올렸고, 화면의 "🧪 [베타 · 검토중]" 배너를 제거했습니다.
  전제조건 2가지(임계값 확정 #55 / 자동화 연결 #60)가 실제로 충족돼 있는지 코드·데이터로 재검증한
  뒤 진행했습니다. **§0-3-6 스테이징 절차 종료 — 미국주식 화면은 이제 정식 공개 서비스입니다.**

### 8-10. GitHub Actions 자동화 연결 (2026-08-07 밤, 오푸스)

**만든/고친 파일**

| 파일 | 내용 |
|---|---|
| `.github/workflows/scrape_us.yml` | 🆕 미국주식 전용 워크플로우. cron 2줄(20:35/21:35 UTC) + `workflow_dispatch`(`force` 체크박스) + `timeout-minutes: 90` + `concurrency`(겹침 방지) + fetch/rebase 3회 재시도 푸시 |
| `collector_us_stocks.py` | `snapshot_covered_session_dates()` / `evaluate_collection_readiness()` 신설, `collect` 서브커맨드에 `--skip-if-not-ready` 옵션 추가 |
| `tests/test_us_stocks.py` | `[6] 무인 자동화 사전 점검` 테스트 12건 추가(두 크론 시각을 EDT/EST 양쪽으로 시뮬레이션) |

⚠️ 기존 코스피 워크플로우 `.github/workflows/scrape.yml` 과 코스피 코드는 한 글자도 수정하지 않았습니다.

**핵심 설계 판단 — 왜 `is_ready_now` 만으로는 안 되는가 (§0-1: 코드를 실제로 돌려 확인한 사실)**

기존 `resolve_collection_session_et()` 은 "마감+30분이 지난 가장 최근 평일"을 찾을 때까지 하루씩
거슬러 올라갑니다. 루프 탈출 조건 자체가 `now >= ready_at` 이라 그 결과의 `is_ready_now` 는
**어떤 시각을 넣어도 항상 True** 입니다(장중 09:35 ET로 호출해도 False가 되지 않고 그냥
'어제 세션'을 가리킴 — 실제로 여러 시각으로 호출해 확인). 즉 그 플래그로는 중복 실행을 막을 수 없습니다.

그래서 실제 판정 기준을 **"이번에 담기게 될 거래일(session_date)을 기존 스냅샷이 이미 담고 있는가"**
로 잡았습니다. 이 하나로 서머타임 양쪽이 자동으로 맞습니다:

| 기간 | 20:35 UTC 크론 | 21:35 UTC 크론 |
|---|---|---|
| EDT(3~11월, UTC-4) | 16:35 EDT → 대상=당일, 스냅샷엔 전일까지 → **✅ 수집** | 17:35 EDT → 대상=당일, 방금 넣음 → ⏭️ 건너뜀 |
| EST(11~3월, UTC-5) | 15:35 EST(장중) → 대상=전 거래일, 이미 있음 → ⏭️ 건너뜀 | 16:35 EST → 대상=당일, 아직 없음 → **✅ 수집** |

부수 효과 2가지: ① 앞 크론이 HTTP 429로 끊겨 스냅샷이 갱신 안 되면 뒤 크론이 같은 날 자동 재시도
(체크포인트에서 이어받음), ② 오너가 로컬에서 이미 수집해 푸시해둔 날은 액션이 같은 데이터를 다시
크롤링하지 않음(§0-3-2 정중한 크롤링).

**안전 원칙 — "모르면 건너뛰기"가 아니라 "모르면 수집한다"**

스냅샷 파일이 없거나 / JSON이 깨졌거나 / `status`가 `FAILED`면 '이미 수집됨'으로 치지 않고
수집을 진행합니다. 건너뛰면 데이터가 하루 통째로 비지만, 수집하면 최악이라도 중복 크롤링 1회이기 때문입니다.

**아직 검증 못 한 것 (정직 기록, §0-1)**
- 이 워크플로우가 **GitHub 서버에서 실제로 돈 적은 아직 없습니다.** 오너가 푸시한 뒤 Actions 탭에서
  `Run workflow`로 한 번 수동 실행해 실측 확인이 필요합니다.
- GitHub Actions 러너에서의 실제 소요시간은 미측정입니다(오너 로컬 실측 40~56분 기준으로 90분 타임아웃 설정).
  러너 IP에서 stockanalysis.com이 HTTP 429를 더 자주 낼지도 아직 모릅니다 — 그래서 체크포인트/재시도
  구조를 그대로 살려뒀습니다.
