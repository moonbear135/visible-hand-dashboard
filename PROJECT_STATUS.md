# 프로젝트 현황판 (PROJECT_STATUS.md)

> **다음에 이 프로젝트를 다시 열 때는 (나 자신이든, 제미나이든, 다른 클로드 세션이든) 전체 파일을 처음부터 다 읽지 말고 이 문서부터 읽으세요.**
> 이 문서 하나로 "지금까지 뭘 했고, 지금 뭐가 문제고, 다음에 뭘 해야 하는지"를 알 수 있게 유지하는 게 목적입니다.
> 작업할 때마다 이 문서의 **최근 작업 로그**와 **지금 열려있는 일** 섹션을 업데이트해주세요.
> 완료된 작업의 상세 이력(번호별 전체 목록)은 `TASK_HISTORY.md`를 참고하세요. 이 문서는 요약만 남깁니다.

> 🛑 **오너 지시 (2026-08-10): 매크로(§4, 방공망 지표) 관련 작업은 여기 지시서(§4)에만 기록으로 남겨두고 지금부터 중단합니다.**
> §4에 남아있는 미완료 항목(FX_Swap_Point/Put_OTM_OI 실측 전환 등)을 **오너가 먼저 명시적으로 지시하지 않는 한** 먼저 나서서 조사·코딩·제안하지 마세요. "다음엔 뭘 해야 하나" 같은 질문에도 매크로를 기본 추천으로 꺼내지 마세요.
> 중단 직전 확인: `market_history.csv` 자동 누적 정상 작동 확인함(2026-08-10) — VKOSPI·선물베이시스 실측값 정상 기록 중, #72(공매도 2종 제외) 코드는 08-11부터 자동 수집에 반영됨. 그러니 중단해도 데이터 파이프라인 자체는 계속 건강하게 쌓입니다.

마지막 업데이트: 2026-08-17 — **🚀 Streamlit → NiceGUI 이전 전체 완료 + 컷오버 + 공개 전환 +
전체 코드 감사(TASK_HISTORY #131~#142).** 자세한 내용과 지금 상태는 **아래 새 §0 섹션**을
먼저 읽으세요 — §1 이하는 전부 그 이전(Streamlit 단일 스택 시절) 기록이라 지금은
참고용으로만 유효합니다.

(2026-08-13 이전 마지막 업데이트 — Streamlit 시절 기록 요약) "내 성적표"는 실서버 연결 검증·실사용
UX 개선·유니버스 밖 가격/이름 커버리지 확장(미국 ETF 포함)까지(#73~#94), 리포트 모듈은 v1 구축과
배치 파이프라인 실검증까지(#95~#97), 비밀번호 찾기(재설정)는 코드 완성까지(#109) 진행된
상태였습니다(이후 실사용 검증까지 완료 — §9-7). #79(정렬)·#80(빨강/파랑 통일)이 배포 화면에 반영
안 되던 소동은 코드 문제가 아니라 Streamlit Cloud 재배포/Reboot 지연으로 확인돼 해결됐고
(2026-08-11), 매크로(§4)는 2026-08-10 오너 지시로 #72까지 완료된 상태에서 중단 유지.
자세한 일자별 이력은 `TASK_HISTORY.md` #73~#110 참고.

> 더 오래된 일자별 업데이트 로그는 `TASK_HISTORY.md`에 번호 순으로 전부 남아있습니다(#1~#142).
> 이 문서는 "지금 상태"만 요약해서 유지합니다 — 과거 항목을 여기 계속 쌓지 않습니다(2026-08-09 정리).

---

## 0. 🚀 지금 상태 — NiceGUI 이전 완료 + 컷오버 + 공개 전환 (2026-08-17, 여기부터 읽으세요)

> **§1 이하는 전부 Streamlit 단일 스택 시절 기록입니다.** 파일 경로·배포 방식·공개 전환
> 절차 등은 지금과 다릅니다. 지금 코드가 궁금하면 이 §0만 읽고, 과거 의사결정 배경이나
> 계산 로직(PEGY·퀀트 스코어·매크로 등)의 유래가 궁금할 때만 §1 이하를 참고하세요.
> 상세 이력은 `TASK_HISTORY.md` #131~#142.

### 0-1. 무슨 일이 있었나 (요약)

Streamlit Community Cloud의 무료 호스팅 제약(무접속 시 재우기, `st.columns()` 반응형
쌓기가 CSS로 못 이기는 프레임워크 자체 한계 — #119~#130에서 여러 번 부딪힘)을 근본적으로
벗어나기 위해 **NiceGUI + Render**로 프레임워크·호스팅을 전면 전환했습니다. 0~7단계
로드맵(계획서 `NICEGUI_MIGRATION_PLAN.md` — 착수 당시엔 저장소 밖에 있었으나 2026-08-17
문서 정리 중 저장소 안으로 옮김)을 전부 마쳤고, 도메인
DNS 전환(컷오버)과 "내 성적표"·"사장님 보고서" 공개 전환, 저장소 전체 코드 감사+수정까지
전부 끝났습니다.

### 0-2. 지금 실제 서비스되는 곳

- **`https://visiblehand.co.kr`** → **Render**(NiceGUI, `main.py` + `web/` 전체)가 서빙.
  가비아 DNS: `A @ 216.24.57.1`, `CNAME www → visible-hand-dashboard.onrender.com`.
- **구 Streamlit 앱**(`https://visible-hand-dashboard-2vmzz6tk63wsac7n345ord.streamlit.app/`)은
  **롤백 안전장치로 2026-08-31까지(최소 2주) 계속 살려둡니다.** 이 기간 동안은 내리지
  마세요. `views/`·`visiblehand.py`·`app.py`도 그때까지 저장소에 그대로 둡니다(코드
  수정 금지 — 지금은 아무도 안 건드리는 참고용 사본일 뿐).
- GitHub Pages 커스텀 도메인 설정은 해제됨(`CNAME` 파일도 삭제됨) — 더 이상 도메인을
  안 씀.

### 0-3. 화면별 공개 상태 (지금 기준)

| 화면 | 경로 | 공개 상태 |
|---|---|---|
| 코스피 PEGY | `/` | 공개 |
| 미국주식 | `/us` | 공개 |
| 내 성적표 | `/scorecard` | **공개** (2026-08-17 전환, 로그인 필요) |
| 사장님 보고서 | `/report` | **공개** (2026-08-17 전환, 로그인 필요, 성적표와 로그인 공유) |
| 매크로 방공망 | `/admin/macro` | 관리자 전용, **개발 중단 상태 유지**(§4 옛 지시 그대로 유효) |
| 관리자 콘솔 | `/admin` | 관리자 전용 |

### 0-4. 새 파일 구조 (NiceGUI 쪽 — §2의 Streamlit 표와 별개)

```
main.py                  진입점 (@ui.page 등록용 import 모음)
web/
  theme.py                전역 CSS (f-string 조립 절대 금지 — 과거 #129 사고 원인)
  auth.py                 app.storage(로그인 세션)를 다루는 유일한 곳
  auth_ui.py              로그인/가입/비번재설정 폼 (scorecard·report 공용)
  layout.py               헤더+드로어 뼈대, 전역 "데이터 최신 아님" 배너 자리
  state.py                읽기전용 시장데이터 로더 (내부에서 data_source 경유)
  components/             카드·툴팁·배지·다운로드버튼 등 화면 5개 공용 UI
  pages/
    pegy_page.py           / (공개)
    us_stocks_page.py      /us (공개)
    scorecard_page.py      /scorecard (공개, 로그인 필요)
    report_page.py         /report (공개, 로그인 필요)
    macro_page.py           /admin/macro (관리자 전용, 개발 중단)
    admin_page.py           /admin
utils/data_source.py     원격 데이터 로드 (DATA_SOURCE_BASE_URL 미설정 시 기존과 100% 동일)
tests/test_web_session_isolation.py   §0-3-8(개인정보 격리) 자동 검증 — 가장 중요한 테스트
```
`views/`·`utils/db.py`·`utils/*_db.py` 등 기존 Streamlit·계산 계층 파일들은 **§2 표가
여전히 정확**합니다(둘 다 씁니다 — 계산 로직은 이전하지 않고 그대로 재사용했습니다).

### 0-5. 지금 열려있는 일 (다음에 반드시 확인/처리할 것) — §4는 낡았으니 여기부터

1. **✅ 완료 — Render Build Filters에 `market_history.csv` 추가 (2026-08-17, 오너 직접
   처리).** Ignored Paths에서 빼서 Included Paths로 옮김(루트 파일이라 `data/**` 규칙과
   무관). 이제 평일 자동 커밋마다 불필요한 재배포가 안 발생함.
2. **✅ 완화 — 무료 티어 "15분 무접속 시 재시작" 문제 (2026-08-17).** 기존
   `keep_awake.yml`의 `render_healthz` job을 별도 워크플로우
   `.github/workflows/render_keep_awake.yml`로 분리해 **10분 간격**(정시 혼잡 회피용
   3,13,23,33,43,53분) 크론으로 촘촘하게 핑을 보내도록 바꿈 — Streamlit(12시간 기준,
   `keep_awake.yml`에 그대로 남음)과 슬립 기준이 10배 이상 달라 스케줄을 분리한 것.
   public repo라 Actions 사용 시간 무제한이라 비용 부담 없음. ⚠️ **100% 보장은
   아님** — GitHub Actions 스케줄은 워크로드 몰릴 때 지연될 수 있어 가끔 콜드스타트가
   한 번씩 있을 수 있음(지어내지 않기). 완전한 상시 기동이 필요해지면 그때는 유료
   (Starter, 월 $7선) 승격이 유일한 확실한 해법 — 지금은 이 정도로 충분해 보류.
3. **✅ 실검증 완료 — 원격 데이터 로드(`DATA_SOURCE_BASE_URL`) (2026-08-17).** Render
   Environment의 `DATA_SOURCE_BASE_URL` 끝에 일부러 오타(`x`)를 붙여 재배포한 뒤 실제
   확인: ① `/scorecard`에 실패 사유(HTTP 404)와 폴백 사본 시각까지 정확히 담긴 빨간
   배너 정상 노출, 서비스는 로컬 사본으로 안 끊김. ② Render Logs에서 `kospi200_pegy_
   latest.json`이 12:28:04에 1회 실패한 뒤 같은 파일로 22초 동안 페이지를 5번
   새로고침해도(07·23·24·25·26초) 재요청 로그가 **한 번도 안 찍힘** — `RETRY_BACKOFF_
   SECONDS = 60.0`이 실제로 작동함을 확인. ③ 오타 제거 후 재배포 → 배너 정상 소멸,
   실제 보유 종목 데이터 정상 표시까지 확인. 테스트 중 겪은 1회성 로그아웃은 재배포로
   인스턴스가 재시작되며 메모리 세션이 초기화된 것으로, 버그 아님(정상 동작).
4. **⏳ 2026-08-31 이후 정리 작업** — Streamlit 앱 정지, `views/`·`visiblehand.py`·
   `app.py`·`index.html`·`CNAME`·`keep_awake_ping.py`의 Streamlit 관련 부분을
   `archive/`로 이동, `requirements.txt`에서 streamlit·altair 등 제거,
   `.github/workflows/keep_awake.yml`의 `streamlit_wake` job 삭제(코드 주석에 표시돼
   있음).
5. **✅ 완료 — "내 성적표" v2(스크린샷 OCR) (2026-08-17 착수 → 2026-08-18 정식 공개 전환
   완료).**
   자세한 스케줄·설계 결정은 `SCORECARD_V2_OCR_WORK_ORDER.md`(레포 루트) 참고. 1~4단계(오너
   결제 설정·설계·코드 구현·오프라인 테스트) **전부 완료**, 하루 업로드 제한(사용자별,
   KST 자정 리셋, 오너 결정으로 100 → 15 → 200 → **최종 15회**로 조정)까지 구현·커밋
   완료. `visiblehand` 프로젝트에 Gemini API 유료 등급 활성화 확인됨(2026-08-17, AI Studio
   "설정 완료" 화면 확인).
   **5단계(실기기 실검증)는 이미 착수했고 1회차에서 결함 2건을 잡았습니다(2026-08-17~18):**
   - ① 구모델(`gemini-2.5-flash-lite`)이 신규 프로젝트에 미제공 → Render 로그에서 404
     확인 후 `gemini-3.5-flash-lite`로 교체(§0-3-12 적용 사례). **수정 완료.**
   - ② **여러 장을 연달아 올리면 앞서 인식된 장의 결과가 화면에서 사라짐** — 업로드마다
     `extracted_box.clear()`로 결과 목록을 통째로 지우고 "이번 장 결과만" 다시 그린 게 원인.
     결과를 누적 목록(`extracted_items`)에 쌓고, 실패 배너(`error_slot`)·남은 횟수
     (`quota_label`)를 각각 전용 자리로 분리해 해결. **수정 완료**(커밋 `580bc95` 병합,
     `pytest tests/test_scorecard_ocr.py tests/test_scorecard.py` 74개 통과).
   - ③ **잔여 결함 정리 완료(2026-08-18)** — ㉮ 인식이 실패해도 한도는 이미 차감된 상태인데
     실패 경로가 남은 횟수 표시를 갱신하지 않아 화면에 **실제보다 1 많은 숫자**가 남던 것을
     `except OcrError` 갈래에서 갱신하도록 수정(문구는 새로 만든 `_set_quota_label()` 한
     곳에서만 조립 — 성공/실패 경로가 서로 어긋날 수 없게). ㉯ **이미지가 아닌 파일**을
     올리면 유료 호출은 없는데 한도만 1회 깎이던 것을, 형식 판별
     (`ensure_supported_image_format()` 신설)을 한도 차감 **앞으로** 옮겨 해결(데이터 계층이
     독스트링에서 이미 약속한 동작에 코드를 맞춘 것). ㉰ ②의 원인을 *"`ui.upload`가 여러 장
     동시 선택을 허용해서"* 라고 잘못 적어둔 주석 정정(`multiple` 기본값은 `False`, 실제
     재현 경로는 한 장씩 연달아 업로드). 회귀 테스트 2건 추가 — 74 → **76개 통과**.
     `ENGINEERING_SPEC.md` §0-3-11에 남아 있던 옛 모델명(`gemini-2.5-flash-lite`)도 함께
     갱신했습니다.
   - ④ **5단계 남은 실검증 3건 — 전부 완료(2026-08-18, Cowork 세션 실기기 테스트)**: 부분
     잘림/저확신 스크린샷(스페이스X·EDV·SOL AI반도체 등에서 노란 `⚠️ 확인 필요` 배지 실제
     확인), 완전 실패 스크린샷(보유 종목 없는 화면, 사람이 읽을 실패 문구 확인), 매입가
     vs 현재가 육안 대조(여러 실제 브로커 앱 스크린샷으로 서로 다른 필드임을 확인) — 전부
     오너가 실기기로 직접 검증 완료.
   - ⑤ **부수 — 미국주식 한글명 검색 버그 수정**: "스페이스X"·"애플" 등 한글 이름으로 미국
     종목을 찾으면 실패하던 버그(영문 `name`만 비교하고 이미 있던 `name_kr` 필드는 검색에
     안 씀). `utils/scorecard_db.py::find_ticker_by_name`이 `name_kr`도 함께 보도록 수정.
   **✅ 6단계(스테이징 승인 → 공개 전환) 완료** — Render `SCORECARD_OCR_ENABLED=true`로
   확인(2026-08-18, 오너 직접 확인). 업로드 위젯이 `/scorecard`를 방문하는 누구에게나 보이는
   정식 공개 상태입니다. **v2 전체(1~6단계) 완료.**
   > ⚠️ **2026-08-18 충돌 사고 기록**: 같은 두 파일(`web/pages/scorecard_page.py`,
   > `tests/test_scorecard_ocr.py`)을 **서로 다른 AI 세션 두 곳이 거의 동시에 수정**해
   > 병합 충돌이 났습니다 — 한쪽(Claude in Chrome)은 오너를 통해 **GitHub 웹 편집기**로
   > 직접 커밋(`7721d10`·`9889668`, committer가 `GitHub`으로 찍힘), 다른 쪽(Cowork 세션)은
   > 로컬 폴더에서 커밋(`57eda8e`, committer가 오너 본인). 검토 결과 웹 편집기 쪽 구현이
   > 책임 분리(결과/실패/한도 각각 전용 자리)와 회귀 테스트 면에서 우세해 그쪽을 채택하고
   > 로컬 구현을 버리는 방식으로 병합(`580bc95`). 재발 방지는 아래 §7 참고.
6. **✅ 완료 — `google-generativeai` → `google-genai` 마이그레이션 (2026-08-17 판단,
   2026-08-18 실행).**
   `google-generativeai`는 구글이 지원 종료(EOL) 선언한 패키지(`ENGINEERING_SPEC.md`
   §0-3-12 참고). `utils/macro_ai.py`(운영 중)와 `utils/scorecard_ocr.py`(v2 OCR) 둘 다
   이 패키지를 써서 §0-3-10(중복 금지) 원칙대로 **두 파일을 한 번에** 옮겼습니다. v2 OCR
   5·6단계가 끝나 안정된 뒤 오너 지시로 진행(원래 판단대로 검증 전에는 미루지 않음).
   API 모양이 통째로 바뀌어(`genai.configure()` + `genai.GenerativeModel(name)` →
   `genai.Client(api_key=...)` + `client.models.generate_content(model=..., contents=...,
   config=...)`) 두 파일의 실제 호출부와 `tests/test_scorecard_ocr.py` 의 가짜 SDK 스텁을
   함께 고쳤습니다. 네트워크가 되는 별도 환경에 실제 `google-genai` 패키지를 설치해 새
   호출부가 (인증 이전 단계까지) SDK 시그니처와 정확히 맞는지 구조적으로 확인했고,
   `requirements.txt` 도 `google-genai` 로 교체했습니다.
7. **🟠 진행 중 — 💰 배당금 모듈("투자 감사합니다!", 6번째 모듈) (2026-08-23 착수, 2026-08-24
   수집 완주 + 화면 구현 + 6번째 모듈로 확정).** 수집기·워크플로우·테스트·화면(`/dividend`
   배당 캘린더)까지 전부 만들어졌고 GitHub Actions 첫 전수 실행이 **완주**했습니다(상세는
   아래 §11). 지시서 원본은 `DIVIDEND_MODULE_WORK_ORDER.md`. **화면은
   `DIVIDEND_ENABLED` 기본 꺼짐이라 아직 아무에게도 안 보입니다** — 오너가 실 데이터로
   직접 켜서 확인한 뒤(관리자 전용 2단계) 전체 공개(3단계) 여부를 결정해야 합니다(결투·
   성적표와 같은 절차, §0-3-6). 지금 남아 있는 일(우선순위 순):
   - 🟢 **유니버스가 "전 상장사"가 아님 — 79종목 코드 형식 문제는 해소, 실제 실행만 남음
     (2026-08-24).** 워크플로우가 넘기는 `data/dividend_history_kr_2023_2025.json`
     은 2023~2025년에 배당한 적 있는 2,734종목입니다. 2026년 신규 상장사·신규 배당 회사는
     **조회 자체를 안 해서 리포트에 흔적조차 없습니다.** 이 사실은 화면 상단 고지문으로
     이미 노출돼 있습니다(§0-1 최소 방어). `data/kr_ticker_master.json`(4,034건 중
     `type == "STOCK"` 2,873건)으로 교체하는 방안이 검토 대상인데, 그중 79종목의 코드
     형식 문제를 직접 까본 결과 성격이 다른 3그룹(우선주 23·스팩 30·진짜 신규상장
     26)이었고, ✅ **코드 형식 문제 자체는 오늘 해결**했습니다 — `normalize_stock_code()`를
     실제 KRX 6자리 영숫자 형식만 정확히 추가로 받아들이도록 좁게 확장(기존 숫자 경로는
     완전히 그대로)하고, DART 표에 직접 안 잡히는 경우를 위해 회사명 2차 매칭
     (`build_corp_name_index()`, 정확히 일치할 때만, 유사매칭 없음)을 신설했습니다 —
     79개를 하나하나 처리하는 게 아니라 앞으로도 통하는 구조적 해법. 상세는 §11-4 참고.
     이미 끝낸 2,734종목을 다시 안 돌리는 **델타 실행 결과를 기존 파일에 합치는 도구
     (`merge_delta_output()`, `--merge-delta`/`--force-merge` CLI, 오푸스 높음으로 신설,
     테스트 25개 포함)도 완성**했습니다 — 병합 시 종목코드 중복이면 무조건 거부(§0-1),
     같은 델타를 두 번 합치면 매니페스트로 걸러냄, 실패 시 원본 파일은 절대 부분쓰기
     안 함까지 검증 완료. 델타 수집을 별도 `--out-dir`로 돌릴 때 실수로 기존 완주본 위에
     바로 덮어쓰는 사고도 별도 가드(`--allow-overwrite` 없으면 거부, 아래 항목)로
     막아뒀습니다. **델타 유니버스 파일도 완성**(`data/dividend_kr_2026_universe_delta.json`,
     신규 29종목) — 그중 **23개(79%)가 리츠**로, 기존 유니버스에 리츠가 하나도 없었다는
     중요한 발견입니다. 남은 건 **실제 델타 수집 실행(오너가 GitHub Actions로 트리거)**
     뿐이며, 유니버스 파일 자체(`data/dividend_history_kr_2023_2025.json`)는 이번 작업에서
     손대지 않았습니다. 상세는 §11-4 참고.
   - 🟡 **화면(`/dividend`) 정보 밀도 재구성 — 완료, 오너 재확인 대기 (2026-08-24).**
     Render 실배포 후 오너가 "정보량이 너무 많다, 달력이 먼저 보이는 단순한 구조를
     원했다"고 피드백. 문구는 한 글자도 안 지우고 배경 설명을 달력 아래 접이식 패널로
     모으고, 남긴 문단 글자 크기를 키웠습니다. 상세는 §11-4 6번 참고.
   - ✅ **체크포인트 이어하기 날짜 버그 — 완료(2026-08-24, 오푸스 높음).**
     `run_key`에서 실행 날짜를 뺐습니다(`build_run_key()`로 분리) — 하루 넘겨 재실행해도
     이제 체크포인트를 버리지 않고 이어받습니다. 대신 `load_checkpoint()`에
     `max_age_days=14`(기본값) 신선도 검사를 추가해, 정말 오래된(2주 초과) 체크포인트는
     여전히 "다른 실행"으로 간주해 새로 시작합니다(무한정 옛 상태를 이어받는 사고 방지).
   - ✅ **기존 산출물 덮어쓰기 방지 가드 — 완료(2026-08-24, 오푸스 높음).** 델타 병합 도구
     작업 중 발견된 반대 방향 구멍 — `latest.json`이 이미 있는데 체크포인트가 없거나
     다른 실행조건이면 `run_collection()`이 그냥 덮어썼습니다. `--allow-overwrite` 없이는
     거부하도록 막았고(진짜 이어하기는 영향 없음), 이 프로젝트 자체 워크플로우
     (`.github/workflows/collect_dividend_kr.yml`)는 완주 때마다 체크포인트를 지우는
     구조라 다음 분기 갱신이 이 가드에 막힐 뻔한 것도 같이 발견해 워크플로우에
     `--allow-overwrite`를 추가로 반영했습니다(안전 — 이 워크플로우는 항상 같은
     out-dir·universe만 쓰도록 고정돼 있어 가드가 막으려는 사고 자체가 날 수 없음).
   - ✅ **§0-3-3 — 완료(2026-08-24).** raw/가공 분리 ✅, 사용자 다운로드 ✅
     (`web/pages/dividend_page.py`, `4b8f000`), **2개 출처 교차검증도 ✅ 신설**
     (`build_kind_baseline_index()`/`check_cross_source()`, DART 응답에 공짜로 오는
     전기·전전기 값을 2023~2025 KIND 기준선과 대조, 결과는 `cross_source_notes` 필드에만
     추가·값은 절대 안 고침). `--history-baseline` 플래그로 옵트인(기존 워크플로우는
     안 건드림 — 안 켜면 동작 그대로). **실 데이터로 실행해보니 진짜 정보를 찾았습니다**:
     비교 가능한 971건 중 522건(53.8%)에서 불일치 — DPS 자체 불일치는 70건(3.9%)뿐이지만
     그중 하나(067900 와이엔텍)는 "주당배당금(원)" 칸에 총배당금액이 잘못 들어간 것으로
     보이는 **진짜 데이터 결함**으로 보입니다(단위검증(§2-4)은 라벨의 "(원)" 표기만 보므로
     못 잡는 종류의 오류 — 교차검증이 아니었으면 몰랐을 것). ⚠️ **다만 주의할 점 2가지**:
     ① DPS 불일치 70건 중 36건은 "전기=year-1"이 아니라 "전기=year"로 놓고 비교하면 정확히
     맞아떨어짐 — DART 전기/전전기와 KIND `fiscal_year`가 가리키는 회계기간이 일부 종목에서
     서로 다르게 해석될 가능성이 있어(비12월 결산 때문만은 아님, 6건만 해당), 이 522건 중
     상당수는 "진짜 오류"가 아니라 "연도 매핑 방식 차이"일 수 있습니다. ② 배당성향 불일치
     943건(56.7%)은 오차가 너무 커서(p90 16.3%p) 단순 반올림이 아니라, DART(연결 우선 채택)와
     KIND(자체 계산 방식)가 **애초에 서로 다른 걸 계산**하고 있을 가능성이 있습니다. 지금은
     "발견만 하고 고치지 않는다"는 원칙대로 노트에만 남겼지만, **이 두 가지는 오너가 실제로
     들여다보고 판단해야 신뢰할 수 있는 신호인지 결정할 수 있습니다** — §11-4 참고.
   - ✅ **`se` 라벨 단위 검증(§2-4) — 완료(2026-08-24, `f25223d`)**. `extract_unit_token()`
     ·`EXPECTED_UNIT_TOKENS`·`check_unit_token()`을 신설해 원문 라벨의 단위 키워드
     (백만원/원/주)와 필드가 기대하는 단위를 대조하고, 불일치는 새 필드
     `unit_mismatch_notes`에만 기록합니다(값 변환·보정 없음, §0-1). 실제 raw 5,484줄
     전수 재실행 결과 **불일치 0건** — 지금 데이터는 이미 단위가 맞다는 뜻이며, 이 기능은
     향후 회귀 방지용 안전장치입니다. 회귀 테스트 12개 추가로 69 → **81개 전부 통과**.
   - 🟡 **커밋 용량** — 이 모듈이 `data/` 에 올리는 파일이 실측 약 **19MB**입니다
     (latest.json 6.8MB + raw.jsonl 9.4MB + corpcode zip 3.6MB). raw jsonl 은 **append-only**
     라 실행할 때마다 커집니다 — 아래 §11-4 참고.
   - ✅ **화면 구현 시 실제로 지킨 것(2026-08-24)**: DART/KIND 문자열 전부 `esc()`(§0-3-9,
     23회) · 데이터 로딩은 `await web.state.load_json_file_async(...)`(§0-3-1 회귀 방지) ·
     모듈 최상위 mutable 전역 0개(`tests/test_web_session_isolation.py` 등록 불필요) ·
     달력 날짜 칸에는 확정분(155건)만, 폴백분은 절대 안 섞이도록 구조적으로 분리.
8. **참고**: §4(매크로 재설계) 이하 Streamlit 시절 "지금 열려있는 일" 목록은 이제
   대부분 낡았거나(성적표·보고서 공개는 새 사이트 기준으로 §0-3 참고) 매크로처럼
   여전히 유효한 오너 지시(중단 상태 유지)만 남아있습니다 — 필요할 때만 개별 대조하세요.

## 1. 이 프로젝트는 무엇인가

"잘 보면 보이는 손 (The Visible Hand)" — 코스피 시장 위험도 + 코스피 시가총액 상위 200개 종목 PEGY(가치) 점수를 매일 자동으로 계산해서 보여주는 대시보드.

> ⚠️ **명칭 주의**: 여기서 "200개 종목"은 **KRX 공식 "코스피 200 지수" 편입종목이 아니라, 순수 시가총액 순위 상위 200개**입니다(2026-08-06 오너 확인 — 공식 지수 전환은 하지 않기로 함). 공식 지수는 유동주식 시총·업종 안배·유동성 심사를 거쳐 연 2회만 리밸런싱되지만, 이 프로젝트는 매일 시가총액만으로 순위를 매깁니다. 화면·다운로드 파일의 사용자 노출 문구는 모두 "시가총액 상위 200"으로 표기하도록 통일함(파일명 `collector_kospi200.py`/`kospi200_pegy_latest.json` 등 내부 이름은 하위 호환을 위해 유지).

- 실제 서비스 주소: `visiblehand.co.kr` (커스텀 도메인, `index.html`이 아래 Streamlit 앱을 iframe으로 감싸는 구조)
- 실제 앱이 돌아가는 곳: **Streamlit Community Cloud** (`https://visible-hand-dashboard-2vmzz6tk63wsac7n345ord.streamlit.app/`)
- 코드 저장소: `github.com/moonbear135/visible-hand-dashboard` (public repo)
- 데이터 자동 수집: **깃허브 액션(GitHub Actions)** — PC를 꺼놔도 매일 자동으로 돈다
- 공개 화면은 **"사실 이 가격이에요"(코스피 PEGY 밸류에이션, 기본 화면)** 와 **"🇺🇸 미국 주식은 이가격"(2026-08-08 공개 전환, 사이드바 상단 메뉴)** 두 개. "잘 보면 보이는 손"(매크로 방공망)은 관리자 전용으로 전환됨 (2026-08-05, 아래 참고)

## 2. 파일 구조 한눈에 보기 (다시 다 안 읽어도 되게)

| 파일/폴더 | 역할 |
|---|---|
| `scrape_daily.py` | 코스피 지수/환율/수급 데이터 수집 → `market_history.csv`에 누적. 백필 모드 지원 |
| `collector_kospi200.py` | 코스피 시가총액 상위 200개 종목 PEGY 데이터 수집 → `data/*.json`. 상장주식수(FDR 우선), Trailing EPS 계산값 예외, 그레이엄 넘버 등이 여기 있음 |
| `.github/workflows/scrape.yml` | **코스피 자동화 워크플로우.** 매일 평일 KST 16:05 실행 |
| `.github/workflows/scrape_us.yml` | **🇺🇸 미국주식 자동화 워크플로우 (2026-08-07 신설).** 서머타임 대응으로 cron 2개(20:35/21:35 UTC)를 걸고, 수집기의 `--skip-if-not-ready` 사전 점검이 하루 한 번만 실제 수집되게 걸러냄 |
| `.github/workflows/keep_awake.yml` | (2026-08-09 신설) Streamlit 무료 호스팅이 **12시간 무방문 시 앱을 재우는 정책** 대응 — 8시간마다 실제 Streamlit 앱 URL(커스텀 도메인 아님)을 방문해 타이머 리셋. 데이터 수집과 무관, 실패해도 영향 없음 |
| `collector_dividend_kr.py` | (2026-08-23 신설) 💰 **배당금 모듈(6번째 모듈)** 수집기. DART OpenAPI `alotMatter.json`("배당에 관한 사항")을 종목별로 불러 **당해 사업연도의 가장 최근 확정 누적치**(사업보고서→3분기→반기→1분기 순으로 찾아 첫 번째 쓸 수 있는 것에서 멈춤)를 수집 → `data/dividend_kr_2026_latest.json`(가공) + `data/dividend_kr_2026_raw.jsonl`(원본, §0-3-3). 파싱은 전부 `se` 라벨 **키워드 매칭**(위치 인덱스 없음, §0-1). 실패·무데이터 종목도 **같은 스키마의 레코드로** 남김 |
| `corp_code_mapper.py` | (2026-08-23 신설) 종목코드(6자리) ↔ DART 고유번호(`corp_code` 8자리) 매핑. `corpCode.xml` ZIP 을 받아 `data/cache/` 에 캐시 |
| `.github/workflows/collect_dividend_kr.yml` | (2026-08-23 신설) 💰 **배당 수집 워크플로우.** DART 정기보고서는 1년에 4번만 갱신되므로 **수동(`workflow_dispatch`) 전용** — 매일 돌리면 같은 값을 다시 받는 §0-3-2 위반. 5시간 예산에서 스스로 멈추고 체크포인트를 남김(job timeout 340분) |
| `web/pages/dividend_page.py` | (2026-08-24 신설) 💰 **배당 캘린더 화면** (`/dividend`, 공개·로그인불필요). 결산기준일 기준 월간 달력 그리드 + 미확정 종목 "작년 배당율" 폴백. `DIVIDEND_ENABLED`(기본 꺼짐) 게이트는 `web/layout.py` |
| `market_history.csv` | 날짜별 종합 위험 점수 이력 |
| `data/kospi200_pegy_latest.json`, `data/pegy_summary_history.json` | 종목별 최신 데이터 + **시장 전체** 요약 이력 (⚠️ `*summary_history.json` 은 중앙값 요약이지 종목별 이력이 아닙니다 — 종목별 이력은 2026-08-09 신설된 `*_stock_history.csv`) |
| `views/pegy_view.py` | **공개** PEGY 밸류에이션 화면. Forward 데이터 없으면 해당 섹션만 마스킹 처리 |
| `views/macro_view.py` | **관리자 전용** 매크로 방공망 화면 (2026-08-05부로 비공개 전환) |
| `views/admin_view.py` | 관리자 인증 + 관리자 전용 메뉴(매크로 화면 진입 체크박스 등) |
| `utils/guardrail.py` | 종목 차단/마스킹 판정 (필수 조건은 이제 `price`뿐, Forward 결측은 차단 아님) |
| `utils/scoring.py` | 퀀트 스코어링. 만점(score_max)은 종목마다 실제 산출 가능한 항목만 합산(고정 100 아님, 배지엔 %도 함께 표시). 역성장/적자·PER이상·극단고평가 하드컷오프는 오늘 수집분 전체 분포 대비 z-score로 심각도 스케일링 |
| `utils/db.py` / `utils/gdrive_helper.py` | DB 저장, 구글드라이브 백업(백업 폴더ID는 `GDRIVE_TARGET_FOLDER_ID` 환경변수로 오버라이드 가능) |
| `collector_us_stocks.py` | (2026-08-06 신설) 🇺🇸 미국주식 수집기 **기초틀**. 유니버스 CSV 수집·필터(550개), 상품유형 분류, 히스테리시스(550/600), stockanalysis.com 통계 페이지 파서, `_now_et()`/ET 세션 계산, 샘플 프로토타입 CLI. **아직 전수 수집·스코어링·화면은 없음** |
| `utils/scoring_us.py` | (2026-08-07 신설) 🇺🇸 미국주식 전용 퀀트 스코어링 + 파생 밸류에이션(PEGY/목표가/그레이엄) + 미국판 가드레일. 배점: PEGY 35 + ROE·ROIC 30 + 주주환원 20 + F-Score 10 + 베타 5 |
| `views/us_stocks_view.py` | (2026-08-07 신설) 🇺🇸 미국주식 화면. **공개 화면**(2026-08-08 오너 승인으로 §0-3-6 스테이징 종료, 베타 배너 제거). 상단 지수 3종 + 코스피와 동일 카드 구조 + USD 단독 표기 + 30개 페이지네이션 |
| `utils/company_names_kr.py` | (2026-08-07 신설) 미국 종목 한글 표기. 정식 한글명 사전(티커 키, 오너가 직접 수정) 우선 + 규칙 기반 음역 폴백(완벽하지 않음을 코드에 명시) |
| `tests/test_us_scoring.py` | (2026-08-07 신설) 미국주식 스코어링·전수수집 배선 오프라인 검증 78건 (실데이터 12종목 + 합성 극단케이스 + end-to-end + 화면 렌더링) |
| `utils/constants_us.py` | (2026-08-06 신설) 미국주식 전용 상수. 밸류에이션 임계값은 전부 🚧잠정값+TODO🚧 (코스피 값을 그대로 쓰지 않음) |
| `tests/test_us_stocks.py` | (2026-08-06 신설) 미국주식 기초틀 오프라인 검증(네트워크 불필요). 실제 CSV 종목명·실제 페이지 원문 라벨로 74개 체크 |
| `tests/test_stock_history.py` | (2026-08-09 신설) 종목별 시계열 이력·다운로드 오프라인 검증 98건. 필드 스펙(내부 필드 유출 0/한국어 라벨), 상태 가드, **실제 수집기 함수를 직접 호출한 실패·차단 시나리오**, 같은 날 중복 방지, 실데이터 end-to-end(BOM·pandas 재파싱·종목코드 0 보존) |
| `utils/constants.py` | (2026-08-06 신설) 프로젝트 전역 임계값·가중치 단일 출처. PER 오염 판정, 목표가 캡, ROE 기준선, 매크로 `RISK_WEIGHTS`/`INVESTOR_WEIGHTS` 등 |
| `utils/macro_scoring.py` | (2026-08-06 신설) 매크로 종합점수 계산(z-score 정규화+시그모이드 변환+동시충격 증폭기) 단일 모듈. `scrape_daily.py`(저장)와 `views/macro_view.py`(표시)가 공용으로 호출. 2026-08-10(#68) **실측 지표 정규화 함수**(`measured_downside_risk` 등) 추가, (#70) 값이 클수록 위험인 지표용 `measured_upside_risk` + 이력 컬럼 분포 생성기 `history_column_population` 추가 |
| `utils/krx_openapi.py` | (2026-08-10 신설, TASK_HISTORY #70) **KRX OPEN API 최소 클라이언트.** VKOSPI 지수값 / KOSPI200 지수 종가 / KOSPI200 선물 근월물 종가만 가져와 매크로 2지표에 씁니다. 인증키는 환경변수 `KRX_OPENAPI_KEY`에서만 읽고 **HTTP 헤더 `AUTH_KEY`로만** 전달(URL에 실으면 서버 로그에 남음). 외부 래퍼 라이브러리 **미채택**(근거는 파일 상단 주석·#70). 실패는 전부 `None` → 그 지표만 배점 제외. **⚠️ 실서버 응답으로 검증된 적 없음** — 지수명·선물 근월물 식별이 미확인이라, 못 찾으면 응답에 있던 이름을 전부 로그로 출력합니다 |
| `app.py` / `visiblehand.py` | 앱 진입점. 공개 메뉴는 코스피 PEGY 화면(기본) + 🇺🇸 미국주식(사이드바 상단 체크박스, 2026-08-08 공개 전환) 두 개. 매크로는 관리자 인증 후에만 |
| `utils/scheduler.py` | 앱 내장 스케줄러(기본 비활성화, `ENABLE_INAPP_SCHEDULER=1`일 때만). 정식 경로는 GitHub Actions. (구버전 레거시 `scheduler.py`/`run_scheduler.bat`는 2026-08-06 삭제됨) |
| `utils/stock_history.py` | (2026-08-09 신설) **종목별 시계열 이력의 단일 출처.** 다운로드에 내보낼 필드 목록 + 한국어 라벨(`KOSPI_HISTORY_FIELDS` 26개 / `US_HISTORY_FIELDS` 40개 — 카드에 실제로 보이는 재무 지표만, 내부 진단·색상 필드 전부 제외) + 이력 append/조회. `record_daily_history()` 가 수집 상태를 보고 **SUCCESS/DEGRADED 일 때만** 기록하고 같은 날짜는 중복 없이 교체. streamlit 미의존 |
| `data/kospi200_stock_history.csv`, `data/us_stocks_history.csv` | (2026-08-09부터 생성) **종목별 날짜 이력**(하루 한 종목 = 한 줄). 컬럼명은 영문 키(라벨 문구가 바뀌어도 과거 행이 안 깨지게), 인코딩 `utf-8-sig`. ⚠️ **과거 소급 없음 — 도입 후 첫 수집분부터 쌓입니다.** 워크플로우의 `git add data/` 범위에 자동 포함 |
| `utils/stock_export.py` | (2026-08-08 신설 → 2026-08-09 재작성) "종목별 데이터 다운로드" 내보내기 모듈. 이력 행 목록 → **날짜=행 / 지표=열** CSV(한국어 헤더 · **UTF-8 BOM**, 엑셀 한글 깨짐 방지) / JSON(`ensure_ascii=False`, 숫자 컬럼만 숫자로 복원해 종목코드 `005930` 이 안 깨짐) + 파일명 안전화(`BRK/B`→`BRK_B`). streamlit 미의존 순수 함수라 코스피·미국 양쪽이 같은 형식을 공유 |
| `ENGINEERING_SPEC.md` | 코딩 원칙 문서. **하드코딩·더미 데이터 금지가 최우선 규칙.** §0-3(절대 준수 메뉴얼: 후행지표 전용, 크롤링 매너 등), 예시2-보충(계산값 허용 예외), 예시2-보충2(섹션별 마스킹 원칙)까지 포함 |
| `AUDIT_REPORT.md` | 2026-08-05 오푸스 코드베이스 감사 보고서 (13개 파일, 50여 건) |
| `MACRO_REDESIGN_PROPOSAL.md` | (2026-08-09 신설, TASK_HISTORY #66) 매크로 14개 위험 지표의 실측 소스 조사 결과와 재설계 제안. 지표별 A/B/C 판정 · 소스/필드/지연(T+n)/난이도 · 가중치 재분배안 · 구현 순서. **§5 구현순서 1번(#68, 실측 2지표)·2번(#69, C 6개 제외+가중치 재분배)·3·4번(#70, VKOSPI·선물 베이시스, 실서버 검증 완료)·6번(#72, 공매도 2종을 실측 불가로 재분류 — 오너 결정 옵션②)까지 반영됨. 남은 건 5번(SMBS 스왑포인트)과 `Put_OTM_OI`. §4-3 가중치 재설계는 미적용(활성 6개 전부 실측 전환 후)** |
| `collector_us_indices.py` | (2026-08-12 신설, #95 / 파싱 버그 수정 #96) 📈 리포트용 **미국 벤치마크 일별 종가** 수집기. stockanalysis.com 의 ETF 프록시(SPY=S&P500, ONEQ=나스닥종합) 과거주가 엔드포인트를 요청 1회씩 불러 `data/us_index_history.json` 에 누적(요청 1회당 125행 확인). ⚠️ 지수 포인트가 아니라 **ETF 종가**(키 이름에 PROXY 명시). devalue 디코더·HTTP 헬퍼는 `collector_us_stocks.py` 에서 import 재사용. 행 탐색은 **깊이 우선 재귀 + 날짜(`t`) 필수** — 최상위만 훑던 #95 버전은 실응답에서 0행이었음 |
| `utils/report_db.py` | (2026-08-12 신설, #95) 📈 리포트 데이터 계층. 기간(일/주/월/분기/반기/연) 경계 계산 · 스냅샷 행 생성 · 기간 집계와 데이터 부족 판정 · 벤치마크 기간 수익률 · Supabase 접근. **⚠️ 2026-08-19부터 더 이상 유일하지 않음 — `utils/duel_db.py`·`run_duel_daily_batch.py`·`run_duel_publish_batch.py`(2026-08-20 신설, 5단계 발행 배치)도 같은 `service_role` 격리 패턴으로 씀(아래 결투 모듈 항목 참고)**(환경변수에서만 읽고 streamlit 미import) |
| `views/report_view.py` | (2026-08-12 신설, #95) 📈 리포트 화면. **사용자에게 보이는 이름은 "📈 사장님 보고서"**(2026-08-13 #107 개명 — 파일명·함수명·플래그는 그대로 `report_*`). 기본 숨김 `REPORT_ENABLED`, `visiblehand.py` 에서 "📊 내 성적표"의 하위 메뉴로 배선됨(#102·#105) |
| `sql/report_schema.sql` | (2026-08-12 신설, #95) `portfolio_daily_snapshots` 테이블 + **RLS select 정책 1개**(사용자는 읽기만, 쓰기는 배치 전용). 오너가 Supabase SQL Editor 에서 1회 실행 |
| `.github/workflows/scrape_report_snapshots.yml` | (2026-08-12 신설, #95) 평일 23:20 UTC — 미국 벤치마크 수집 → 사용자별 스냅샷 적재(Supabase) → `data/us_index_history.json` 커밋 |
| `data/us_index_history.json` | (#95, **아직 실제 수집 미실행**) 미국 벤치마크(SPY/ONEQ) 일별 종가 누적 파일. 공개 데이터라 기존 `data/` 관례대로 GitHub 에 커밋 |
| `tests/test_report.py` | (2026-08-12 신설, #95 / #96 에서 실응답 픽스처로 교체) 리포트 모듈 오프라인 검증 **270체크**(기간 경계·데이터 부족 판정·스냅샷 생성·벤치마크·**실제 응답 원문**으로 수집기 실행·가짜 Supabase 배선·범위 확인) |
| `TASK_HISTORY.md` | Cowork 작업 목록 위젯에서 정리(삭제)한 완료 항목 전체 이력 |
| `credentials.json` 등 | 구글 API 비밀키. `.gitignore` 등록되어 git에는 안 올라감 (확인 완료) |

## 3. 최근 작업 로그 (요약, 최신순 — 2026-08-09 정리, 상세는 전부 TASK_HISTORY.md에 있음)

- **2026-08-24 (계속 이어서, 델타 유니버스 파일 완성)** — 오너 지시로 실제 델타 유니버스
  파일을 만들었습니다(`data/dividend_kr_2026_universe_delta.json`, 네트워크 요청 0건 —
  이미 있는 `kr_ticker_master.json`·`dividend_history_kr_2023_2025.json` 두 파일만 대조).
  `kr_ticker_master.json`의 STOCK 타입 2,873건 중 이미 유니버스에 있는 2,734건을 뺀
  139건을 분류: 스팩은 전부(71건) 이미 유니버스에 있어서 델타에 안 걸림, 113건은 우선주로
  추정되는데 그중 110건은 본주가 이미 유니버스에 있어 배당 데이터가 이미 수집돼 있을
  가능성이 높아 제외(감사 기록은 `dividend_kr_2026_universe_delta_excluded_preferred.json`
  에 전부 남김 — 왜 뺐는지, 무엇을 뺐는지 나중에도 확인 가능), 애매한 3건(패턴은 우선주
  같은데 본주를 이름으로 못 찾음)은 확신이 없어 빼지 않고 포함. **최종 델타 29종목** —
  그런데 **이 중 23개(79%)가 리츠(REITs)**였습니다(맥쿼리인프라, SK리츠, 롯데리츠,
  신한알파리츠 등). 리츠는 법적으로 이익 대부분을 배당해야 하는 구조라 배당 가능성이
  거의 확실한 종목군인데, 지금 유니버스에는 **단 한 종목도 없었습니다** — 애초에 조사
  대상이던 "79개 이상한 코드"보다 훨씬 큰 실질적 공백이었다는 뜻입니다. 나머지 6개는
  진짜 신규상장(삼성에피스홀딩스 등)과 애매한 우선주 3건. `collector_dividend_kr.py`
  의 `load_universe()`/`load_universe_name_map()` 로 실제 파싱까지 확인 완료(29종목 정상
  인식). ⚠️ 아직 실제 DART 조회는 한 건도 안 했습니다 — 파일만 준비된 상태이고, 실제
  델타 수집 실행(GitHub Actions)과 그 결과를 `--merge-delta` 로 합치는 건 오너 결정·
  실행 대기입니다. 기존 워크플로우(`collect_dividend_kr.yml`)는 유니버스 파일이
  하드코딩돼 있어 이 델타 파일로 실행하려면 워크플로우 쪽 손질이 별도로 필요합니다
  (아직 안 함 — 오너가 다음에 요청하면 진행).
- **2026-08-24 (계속 이어서)** — 유니버스 확장의 "79종목 문제"를 직접 까본 결과, 79개가
  아니라 성격이 다른 3그룹이었음을 확인(§0-1 — 추측 대신 실측): **23개는 우선주**(SK우,
  삼성물산우B 등)인데 본주(SK, 삼성물산 등)가 이미 유니버스에 있고 DART 보고서 하나에
  보통주·우선주 배당이 같이 들어있어 **이미 수집돼 있었음**(실측: SK `dps_cash_preferred`
  =1500원 등 20건 확인). **30개는 스팩**(기업인수목적회사)으로 구조상 배당이 없어 원래
  빠지는 게 맞음. **26개만 진짜 신규상장 회사**(삼성에피스홀딩스, 대신밸류리츠 등, 배당
  가능성 있음). 오너 승인 받아 오푸스 높음 서브에이전트 2건으로 대응:
  ① `corp_code_mapper.normalize_stock_code()`를 좁게 확장 — 79개 전부가 정확히 6자리
  영숫자(예 `0126Z0`)인 실제 KRX 코드 형식임을 확인하고 이 한 가지 패턴만 추가로
  받아들이도록 함(기존 숫자 6자리 경로는 완전히 그대로). ② 그래도 DART 표에 직접
  안 잡히는 경우를 위해 `build_corp_name_index()` + `map_stock_codes()`의 회사명 2차
  매칭(정확히 일치할 때만, 절대 유사매칭 안 함) 신설 — 특정 79개를 하나하나 처리하는
  게 아니라 앞으로도 계속 통하는 구조적 해법. `load_universe_name_map()`으로 유니버스
  파일의 회사명을 함께 읽어 자동으로 연결(새 CLI 플래그 없음). **실제 확인된 사실**:
  현재 2,734종목 유니버스에서 이미 UNMAPPED였던 55종목 전부가 "형식 오류로 DART에
  아예 물어보지도 못한" 경우였음(DART가 거절한 게 아니라 우리 쪽에서 미리 걸렀던 것) —
  이번 수정만으로 55종목 전부가 다음 실행부터는 최소한 DART에 물어는 보게 됨.
  ③ 별도로, 이미 수집돼 있었지만 화면에 한 번도 안 보이던 우선주 배당 데이터
  (`dps_cash_preferred` 등, 실제 20건)를 `dividend_page.py`에 노출 — 보통주와 절대
  안 섞이게 별도 줄로 표시, 값이 여러 개(우선주 종류 2개 이상)면 하나로 뭉개지 않고
  전부 나열. 테스트 156 → **188개 전부 통과**(`corp_code_mapper.py`,
  `collector_dividend_kr.py`, `tests/test_dividend_collector.py`,
  `web/pages/dividend_page.py` 4개 파일만 수정). ⚠️ 실제 유니버스 파일 교체(더
  이상 손대지 않음, 여전히 `data/dividend_history_kr_2023_2025.json`)와 실제 델타
  수집 실행은 오너 결정 대기 — 이번 작업은 "수집기가 이런 형식을 다룰 수 있게" 만든
  것뿐입니다.
- **2026-08-24 (한 번 더 이어서)** — 델타 병합 도구 작업 중 발견된 구멍 하나를 오푸스
  높음으로 마저 메움: `run_collection()`이 기존 `latest.json`이 있는데도(체크포인트가
  없거나 다른 실행조건이면) 그냥 덮어써버리는 경로가 있었음 — `merge_delta_output()`이
  막는 "델타 결과를 잘못 합치는" 사고와는 반대 방향의, "애초에 다른 실행이 기존 완주본
  위에 그냥 덮어써버리는" 사고. `peek_checkpoint_run_key()` + `--allow-overwrite` 플래그로
  가드 신설(§0-1 — 조용히 덮어쓰지 않고 멈춤) — 진짜 이어하기(같은 run_key)는 전혀
  영향 없음, 뮤테이션 테스트로 가드가 실제로 작동하는지까지 확인(테스트 148 → **156개
  전부 통과**). ⚠️ 부작용 하나 발견해서 같이 고침: 이 워크플로우는 완주할 때마다
  체크포인트를 지우므로, 다음 분기 갱신 때 파라미터가 완전히 같아도 "체크포인트가 없다"는
  이유만으로 새 가드에 막힐 뻔했음 — `.github/workflows/collect_dividend_kr.yml`에
  `--allow-overwrite`를 추가해 해결(이 워크플로우는 항상 같은 out-dir·같은 universe
  파일만 쓰도록 하드코딩돼 있어 가드가 막으려는 사고 자체가 날 수 없는 구조라 안전).
  `collector_dividend_kr.py`·`tests/test_dividend_collector.py`·
  `.github/workflows/collect_dividend_kr.yml` 3개 파일만 수정.
- **2026-08-24 (더 이어서)** — 배당금 모듈 "기초 작업" 3건을 오푸스 높음 서브에이전트 2건으로
  처리: ① 체크포인트 `run_key` 날짜 버그 수정(`build_run_key()`로 날짜 제거 +
  `load_checkpoint(max_age_days=14)` 신선도 검사 추가) — 하루 넘겨 재실행해도 이제
  이어받음. ② §0-3-3 "2개 출처 교차검증" 신설(`build_kind_baseline_index()`/
  `check_cross_source()`, `--history-baseline` 옵트인) — DART 전기·전전기 값을 기존
  2023~2025 KIND 기준선과 대조해 `cross_source_notes`에만 기록(값 안 고침). **실 데이터로
  돌려보니 진짜 신호를 찾음**: 비교 가능 971건 중 522건 불일치, 그중 067900 와이엔텍은
  단위검증으로는 못 잡는 진짜 데이터 결함으로 의심됨 — 다만 연도 매핑 방식·배당성향 계산
  방식 차이일 가능성도 있어 오너 확인 필요(§11-4 참고, 아직 신뢰 여부 미확정). ③ 델타
  실행(신규 종목만 수집) 결과를 기존 파일에 안전하게 합치는 `merge_delta_output()` +
  CLI `--merge-delta`/`--force-merge` 신설 — 종목코드 중복 시 무조건 거부, 중복 병합은
  매니페스트로 차단, 실패 시 원본 파일 부분쓰기 없음까지 테스트로 확인. 테스트
  81 → **148개 전부 통과**(`collector_dividend_kr.py`, `tests/test_dividend_collector.py`
  만 수정 — 두 서브에이전트 모두 직접 재검증 완료, 다른 파일 무변경 확인).
  유니버스 확장 자체(79종목 처리 방침, 실제 델타 수집 실행)는 여전히 오너 결정·실행
  대기.
- **2026-08-24 (이어서)** — 배당금 모듈 미뤄뒀던 항목 2건 처리: ① `web/pages/dividend_page.py`에
  전체 파일 다운로드 버튼 3종(가공 JSON·raw JSONL·기준선 JSON) 추가 — 지시서 §0-3-3이 요구하는
  건 종목별이 아니라 **전체 파일** 다운로드임을 원문 재확인 후 그에 맞게 구현(`4b8f000`).
  ② §2-4 단위 키워드 검증 신설 — `collector_dividend_kr.py`에 `extract_unit_token()`·
  `EXPECTED_UNIT_TOKENS`·`check_unit_token()`을 추가해 `se` 라벨의 단위(백만원/원/주 등)가
  필드가 기대하는 단위와 일치하는지 확인하고, 불일치 시 새 필드 `unit_mismatch_notes`에만
  기록(값은 절대 변환·보정하지 않음, §0-1). 실제 수집 원본 5,484줄 전수 재실행 결과
  **불일치 0건**으로 확인 — 지금 데이터는 이미 단위가 전부 맞다는 뜻이며, 이 기능은 향후
  회귀를 잡기 위한 안전장치. 회귀 테스트 12개 추가로 `tests/test_dividend_collector.py`가
  69 → **81개 전부 통과**. `unit_mismatch_notes`는 기존 `notes`/`unknown_se_labels`와 나란히
  추가된 것으로 기존 필드는 하나도 안 바뀜(`f25223d`). 오푸스 높음 서브에이전트 2건으로 작업,
  둘 다 제출 전 직접 재검증(`py_compile`·`pytest`) 완료.
- **2026-08-24** — 💰 **배당금 모듈("투자 감사합니다!")을 6번째 모듈로 정식 등록 + 첫 화면(`/dividend` 배당 캘린더) 구현.** 2026년 개별종목 배당 수집기(`collector_dividend_kr.py`, DART `alotMatter.json` 기반, 2026-08-23 신설)를 GitHub Actions로 첫 전수 실행 완주(유니버스 2,734종목 / 요청 5,484건 / 4.8시간 / OK 2,572·NO_DATA 107·UNMAPPED 55, `completed: true`). 이어서 다른 세션의 외부 검토(오푸스 높음) 10건 중 워크플로우 셸 인젝션·체크포인트 유실 방지(`if: always()`)·없는 문서 인용 정리·테스트 `sys.path` 버그 4건을 수정(`5e18400`). 검토 중 발견된 모듈 번호 겹침을 오너가 확정 — **리포트 4번째**(기존 5번째)·**결투다! 5번째**(기존 4번째)로 재번호 12개 파일 반영, 배당금 모듈은 **6번째로 확정**. 이어서 오푸스 높음으로 배당 캘린더 화면(`web/pages/dividend_page.py`)을 신규 구현 — 달력 날짜는 결산기준일(`stlm_dt`)이며 실제 지급일이 아님을 상시 배너로 고지, 2026년 미확정 종목은 "○○년 배당율입니다" 폴백(2023~2025 기준선 중 배당액이 실제로 있는 가장 최근 연도), 유니버스가 전 상장사가 아니라는 사실도 화면 상단에 그대로 노출(§0-1). 재번호 12건 + 화면 3개 파일 = 총 15개 파일 커밋·푸시 완료(`4444dd3`). `DIVIDEND_ENABLED` 기본 꺼짐이라 아직 아무에게도 안 보임 — 유니버스 전 상장사 교체·체크포인트 날짜버그·단위검증은 미해결(§11-4).
- **2026-08-18** — "내 성적표" v2 OCR 5단계 실검증 착수: 정상 인식 통과, 결함 2건 발견·수정(모델 404 → `gemini-3.5-flash-lite` 교체 / 여러 장 연속 업로드 시 이전 결과 소실). 같은 두 파일을 서로 다른 AI 세션이 동시 수정해 병합 충돌 → 웹 편집기 쪽 구현 채택으로 병합(`580bc95`), 재발 방지 규칙 §7-1 신설.
- **2026-08-10** — 매크로 재설계 1단계(#68, 실측 2지표를 과거 분포 대비 정규화) → 2단계(#69, 실측 불가 6개를 점수에서 제외하고 4개는 "공부용 참고" 섹션으로 분리·2개는 개념 중복이라 완전 삭제, 남은 8개 가중치 비례 재분배로 합계 100.00) → 3·4단계(#70, `VKOSPI_Skew`·`Synthetic_Futures`를 KRX OPEN API 실측으로 교체 — `utils/krx_openapi.py` 신설, 오너 Actions 수동 실행으로 **실서버 검증 완료**) → #71(실서버 검증 정리 + ECOS 스왑레이트 조사, 코드 미수정) → **6단계(#72, 공매도 2종을 "실측 불가"로 재분류 — 점수 제외 + 공부용 참고 섹션 이동 + 가중치 재분배로 지표 8개 → 6개)**. Streamlit 잠들기 방지 워크플로우 실전 검증(#67).
- **2026-08-09** — 매크로 14개 지표 실데이터 연결 조사 + 재설계 제안서 작성(#66, 코드 미수정 · `MACRO_REDESIGN_PROPOSAL.md` 신설). 종목별 시계열 이력 신설(#64) + UI 폴리시 6건(#65: 국기 이모지 깨짐, 툴팁 오버플로우, 안내박스 흐림 2건, 코스피 지수 가독성, 신선도 경고 관리자 전용화, 미국 화면 학습용 안내 박스).
- **2026-08-08** — 미국주식 페이지 공개 전환(#61), 사이드바 대분류/중분류 라디오 개편(#62), 종목별 데이터 다운로드 도구 신설(#63, 오너 피드백으로 #64에서 시계열로 재설계).
- **2026-08-07** — 미국주식 550종목 전수 수집 성공, 임계값 재캘리브레이션(#55), 한글 음역 품질 개선(#56), GitHub Actions 자동화 연결(#60), 프리셋 필터 버그 수정(#57), BPS 목표가 바닥값 도입(#58).
- **2026-08-06** — 미국주식 모듈 기초틀 구축, 타임존(UTC/KST) 버그 전면 수정, `AUDIT_REPORT_V2.md` 27건 처리 완료, 퀀트 스코어 심각도 세분화(z-score 기반), KOSPI 200 히스테리시스 버퍼(200/230) 도입.
- **2026-08-05 이전** — PEGY 화면 초기 구축, 매크로 방공망 관리자 전용 전환, 배포 크래시 2건 대응, 상장주식수 파싱 오류 수정. (TASK_HISTORY #1~18)

> 각 항목이 왜 필요했는지, 어떻게 검증했는지, 어느 파일을 고쳤는지의 전체 서술은 `TASK_HISTORY.md` #1~#72에 번호 순으로 전부 남아있습니다. 이 섹션은 날짜 감각만 잡는 용도입니다 — 여기 계속 새 문단을 쌓지 말고, 새 작업은 한 줄 요약만 맨 위에 추가하세요.

모든 항목은 `git log origin/main..HEAD`가 비어있음을 기준으로 **전부 깃허브에 푸시 완료된 상태**입니다.

## 4. 지금 열려있는 일 (다음에 반드시 확인/처리할 것)

> **📌 2026-08-13 세션 종료 메모** — 오너 주간 토큰 한도로 세션이 여기서 멈췄었고, 바로 아래
> 항목(#114)의 오너 확인 3가지가 그때부터 지금까지 확인 대기 상태입니다.

- **🟡 오너 확인 대기 — 리포트 3종 세트 (2026-08-13, TASK_HISTORY #114 / §10-9).**
  ① '이전/최신/다음 기간' **버튼 버그 수정** ② 화면 **문구 대폭 간소화** ③ 📊 **종목별 비중(%) +
  비중 변화 표 신설**. **오너가 실행할 SQL 은 없습니다**(비중은 이미 저장된 값으로 조회 시점에
  계산 — 새 테이블 없음). git commit/push 후 재배포하면 바로 보입니다.
  **다음 세션에서 오너에게 확인받을 것 3가지**:
  ㉮ 일간으로 두고 '◀ 이전 기간' 을 눌렀을 때 **기준일 달력의 날짜가 실제로 바뀌는지**,
  ㉯ 화면이 "한눈에 들어오게" 줄었는지(더 지울 문구가 있는지),
  ㉰ 비중 변화 표의 비교 시작점이 **"이 기간 첫 기록일"** 인 게 괜찮은지 — 오너의 수기 표처럼
     **직전 달 마지막 주와 비교**하려면 종목별 스냅샷 조회 범위를 기간 이전까지 넓히는 별도
     작업이 필요합니다(지금은 없는 값을 끌어오지 않으려고 기간 안에서만 비교).
- **✅ 완료 — 종목별 일일 스냅샷 신설(#113) + 종가 수집 시각 표시(#112) (2026-08-13).** 새 테이블
  `portfolio_holding_snapshots`(종목별 히스토리 — §10-8)와 `price_as_of_kst` 컬럼(§10-7)을
  추가했고, **오너가 Supabase SQL Editor에서 `sql/report_schema.sql` 전체 실행 + git push까지
  완료** — 다음 배치부터 종목별 스냅샷·수집 시각이 쌓입니다. 자세한 이력은 `TASK_HISTORY.md`
  #112·#113 참고.
- **✅ 완료 — "내 성적표"·"사장님 보고서" 공개 전환(#106·#107) + 비밀번호 찾기 실사용
  검증(#109·#110) (2026-08-13).** 공개 전 보안 최종 점검(발견 2건·수정 2건 — §9-6) 후 오너가
  Streamlit Cloud Secrets에 `SCORECARD_ENABLED`/`REPORT_ENABLED` 2줄을 켜서 공개 절차 완전 종료
  (캡처로 확인). 비밀번호 찾기는 Resend Custom SMTP 연결·실사용(실측 8자리 코드로 변경·재로그인·
  보유 종목 유지)까지 오너가 직접 확인 — §9-7. 자세한 이력은 `TASK_HISTORY.md` #106~#110 참고.
- **✅ 종목별 시계열 이력 검증 완료 (2026-08-09, TASK_HISTORY #64).** 코스피 200종목·미국 548종목 둘 다 첫 기록 확인.
- **🟡 매크로 14개 위험 지표 재설계 — 1~4·6단계 완료 후 오너 지시로 중단 상태 (TASK_HISTORY #66 제안서 기반).**
    - **✅ 완료(2026-08-10, TASK_HISTORY #68~#72)** — 실측 2지표 z-score 정규화(#68) → 실측 불가
      6개 제외 + 가중치 비례 재분배(#69) → `VKOSPI_Skew`·`Synthetic_Futures`를 KRX OPEN API
      실측으로 교체 + 실서버 검증(#70·#71) → 공매도 2종 "실측 불가" 재분류(#72, 오너 옵션②
      확정 — 데이터가 없어서가 아니라 pykrx의 로그인 우회가 §0-3-2와 충돌해 **안 쓰기로 한** 것).
      단계별 가중치 산정·검증 로그·삭제/이동 사유 전문은 `TASK_HISTORY.md` #66~#72 참고.
    - **📌 현재 매크로 상태 (2026-08-10 최종)** — 활성 **6개** 중 **4개가 실측**: `KOSPI_5D_Return`·
      `Stock_Net_Sell`(#68) + `VKOSPI_Skew`·`Synthetic_Futures`(#70, KRX OPEN API 실서버 검증
      완료). **아직 프록시는 2개**: `FX_Swap_Point`(환율 레벨) · `Put_OTM_OI`(코스피 등락률).
      점수에서 뺀 8개 = 공부용 참고 섹션 6개 + 개념 중복 완전제외 2개(`Foreign_Broker_Dump`·
      `Put_Buy_Simple`). 신규 실측 지표 정규화는 이력 20행이 쌓일 때까지 중립(0.5)으로
      나옵니다(표본 부족 시 지어내지 않음 — 버그 아님).
    - **⏸️ 남은 프록시 2개는 둘 다 보류(오너 결정, 2026-08-10).** ⑤ `FX_Swap_Point` — ECOS·SMBS
      경로 조사까지 끝났으나(#71) "일이 너무 커진다"로 보류. 재개 옵션: ① 오너가 브라우저
      Network 탭으로 SMBS 실제 데이터 엔드포인트 확인 후 공유, ② `appdev@smbs.biz` 정식 문의,
      ③ 6단계처럼 실측 불가 재분류. ⑥ `Put_OTM_OI` — 행사가 파싱 시도 또는 풋/콜 미결제약정
      비율 재라벨링(지표 의미가 바뀌므로 **오너 승인 필요**) 중 미착수. 둘 다 실측되면 그때
      `MACRO_REDESIGN_PROPOSAL.md` §4-3(가중치 재설계)을 한 번에 적용. **현재 매크로 관련 활성
      작업 없음.**
    - **✅ 선행 조건 전부 완료(2026-08-10)** — KRX OPEN API 인증키 발급(**유효기간 2026-08-10 ~
      2027-08-09**), 4개 서비스 이용신청 승인, GitHub Secret `KRX_OPENAPI_KEY` 등록·배선 완료.
      **인증키 값 자체는 어떤 문서·코드·로그에도 남기지 않습니다**(환경변수에서만 읽고 HTTP
      헤더로만 전달 — URL에 실으면 서버 로그에 남기 때문).
    - **⚠️ 약관 주의 — KRX OPEN API 제6조②: "비상업적 목적으로만 이용 가능, 결과에 대한 대가를
      제3자에게 청구 불가."** 오너가 사이트에 **광고를 붙일 계획**이 있음(2026-08-10 확인) —
      매크로 화면이 비공개인 지금은 충돌 없지만, 이 화면을 공개 전환하거나 광고 붙은 페이지에 이
      데이터를 섞으려면 ① 그 페이지 광고 제외 ② KRX 유료 상업 라이선스 전환 중 하나를 선택해야
      합니다. **공개 전환 전에 반드시 이 조항을 재검토할 것.**
- **⚪ 참고만** — 미국주식 페이지 상단 지수가 아직 단일 출처(§0-3-3 미충족, 화면에 "근사치입니다" 고지 있어 공개 블로커 아님). `GEMINI_API_KEY` 미등록이어도 자동화는 정상 작동(AI 코멘트만 생략).

> 완료된 항목(AUDIT_REPORT_V2 27건, git filter-repo 히스토리 정리, 해외주식 페이지 구축 전체, bcrypt 지원 등)은 전부 `TASK_HISTORY.md`에 남아있습니다.
> **처리 순서(오너 확정): 0(AUDIT_REPORT_V2) → 4(git filter-repo) → 1(해외주식 페이지) 전부 완료. 다음은 2번(매크로 재설계)입니다.**

## 5. 자동화 동작 방식 요약

**🇰🇷 코스피 — `.github/workflows/scrape.yml`**

- 트리거: 매일 평일 KST 16:05 (cron), 또는 Actions 탭에서 수동(`Run workflow`)
- 순서: `collector_kospi200.py`(종목 데이터) → `scrape_daily.py`(매크로 데이터) → 변경사항 `git commit & push`
- 수동 실행 시 `target_date` 입력칸에 `YYYY-MM-DD`를 넣으면 그 날짜만 콕 집어 보정(백필) 가능. 이때는 실시간 시세 조회를 건너뛰고 그 날짜의 실제 종가만 사용함 (오늘 시세로 과거가 오염되는 걸 방지)

**🇺🇸 미국주식 — `.github/workflows/scrape_us.yml`** (2026-08-07 신설, TASK_HISTORY #60)

- 트리거: 매일 평일 **20:35 UTC / 21:35 UTC 두 번** (cron 2줄), 또는 Actions 탭에서 수동(`Run workflow`)
- **왜 두 번인가**: 우리가 수집해야 할 시각은 언제나 "미국 정규장 마감 16:00 ET + 30분 = 16:30 ET"인데,
  GitHub Actions의 cron은 UTC 고정이라 서머타임에 따라 대응 UTC 시각이 1시간 움직입니다.
  EDT(3~11월) → 20:30 UTC, EST(11~3월) → 21:30 UTC (둘 다 `describe_collection_schedule()`을
  실제 호출해 확인한 값. 정각 혼잡을 피해 5분씩 뒤로 밀어 20:35/21:35로 설정).
- **그래도 실제 수집은 하루 한 번**: 수집기가 `--skip-if-not-ready` 사전 점검을 합니다. 판정 기준은
  "이번에 담기게 될 거래일(session_date)을 기존 스냅샷이 이미 담고 있는가" —
  이미 있으면 아무것도 하지 않고 정상 종료(exit 0)합니다. 서머타임 전환일에도 자동으로 맞습니다.
- **덤**: 앞쪽 실행이 HTTP 429로 끊기면 스냅샷이 갱신되지 않으므로 뒤쪽 실행이 같은 날 자동 재시도합니다
  (체크포인트 `data/us_collect_checkpoint.json`에서 이어받아 처음부터 다시 하지 않음).
- 순서: `collector_us_stocks.py collect --skip-if-not-ready` → `data/us_*.json` 변경분 `git commit & push`
  (푸시 거부 시 fetch+rebase 최대 3회 재시도 — 코스피 워크플로우와 동일 패턴)
- 수동 실행 시 `force` 체크박스를 켜면 사전 점검을 무시하고 같은 거래일도 다시 수집합니다.
- 시크릿 불필요(`GEMINI_API_KEY` 등 안 씀 — 미국주식 수집기에는 AI 코멘트 기능이 없습니다).

## 6. 다음에 코드를 또 고칠 때 지켜야 할 원칙

`ENGINEERING_SPEC.md`의 최우선 규칙: **데이터를 못 가져오면 하드코딩된 값이나 그럴듯한 가짜 숫자로 조용히 채우지 말고, 명확한 오류를 내거나 화면에 "데이터 없음"으로 표시할 것.** 단, 아래 두 가지 예외는 오너 승인으로 허용됨(둘 다 §0-1에 조건 명시):

- **계산값 예외**: 실측 불가 + 순수 사칙연산 + 입력값도 실측일 때만, 대수적 역산 허용(반드시 "계산값" 마크).
- **섹션별 마스킹**: 일부 데이터(주로 Forward/미래 추정)만 없으면 종목 전체를 막지 말고, 그 섹션만 마스킹하고 나머지는 정상 노출.

이 프로젝트는 **후행지표 전용**입니다(§0-3-1) — 실시간 데이터를 다루지 않고, 장마감 후 확정된 데이터만 사용합니다.

## 7. 로컬-GitHub 데이터 동기화 원칙 (2026-08-06 추가)

자동 크롤링(GitHub Actions)은 GitHub 원격 저장소에만 데이터를 쌓고, 로컬 작업 폴더는 오너가 수동으로
`git pull` 해야만 최신 상태가 됩니다. 시간 기반 자동 동기화는 오너 컴퓨터가 항상 켜져 있지 않아서
적용하지 않기로 함(2026-08-06 논의). 대신: **세션 시작 시, 또는 크롤링 결과 확인·데이터 검증 등 데이터
관련 작업을 요청받으면, 먼저 로컬이 GitHub 최신 상태인지 확인하고 뒤처져 있으면 pull부터 안내할 것.**
오늘 로컬/원격(raw.githubusercontent CDN 캐시)/실제 서비스(Streamlit) 세 곳이 서로 다른 시점을 보여줘서
혼선을 겪었던 것이 이 원칙을 세운 계기.

### 7-1. 여러 AI 세션이 같은 저장소를 건드릴 때 (2026-08-18 추가 — 실제 충돌 사고 후)

이 프로젝트는 오너가 여러 창(Cowork 세션, Claude in Chrome, 그 밖의 코딩 도구)을 오가며 씁니다.
2026-08-18 실제로 **같은 두 파일을 서로 다른 세션이 몇 분 차이로 고쳐 병합 충돌**이 났습니다(위
§0-5 항목 5의 충돌 사고 기록 참고). 한쪽은 GitHub 웹 편집기로, 다른 쪽은 로컬 폴더에서 커밋해
서로를 전혀 모르는 상태였습니다. 재발을 줄이기 위한 규칙:

1. **코드 수정 전에도 `git pull`.** §7은 원래 "데이터 관련 작업" 기준으로 쓰였지만, 이제
   **코드 파일을 고치기 전에도** 로컬이 origin 최신인지 먼저 확인합니다. 로컬에만 커밋을 오래
   쌓아두지 말고 작업이 끝나면 바로 push 하세요 — 로컬에 머무는 시간이 길수록 충돌 창이 커집니다.
2. **"지금 작업 중" 표시를 코드보다 먼저 push.** 여러 창을 동시에 열어 쓸 때는 이 문서 §0-5 맨
   위에 `🔒 작업 중: <파일 경로> — <세션 이름> — <시각 KST>` 한 줄을 **먼저 커밋·push** 하고
   시작하고, 끝나면 지웁니다. §0-2가 "모든 AI는 이 문서를 먼저 읽어라"를 이미 강제하므로 다른
   세션에도 실제로 보입니다.
3. **한 번에 한 창에서만 같은 파일을 고칩니다.** 특히 "이 코드를 GitHub 웹 편집기에 붙여넣어
   커밋해줘" 방식은 로컬 저장소를 우회하므로(커밋의 committer가 `GitHub`으로 찍힘) 로컬에서
   작업 중인 다른 세션과 반드시 충돌합니다 — 쓰려면 다른 창의 작업이 끝난 뒤에 하세요.

> 🔎 **커밋 출처를 구분하는 법**: `git log -5 --format="%h %an | Committer: %cn"` 을 보면,
> committer가 `GitHub`이면 GitHub 웹사이트(웹 편집기·API)에서 만든 커밋이고, 오너 이름이면
> 로컬 `git commit`으로 만든 커밋입니다. 어느 경로로 들어온 변경인지 추적할 때 쓰세요.

## 8. 해외주식(미국주식) 페이지 — 설계 원칙 요약 (완료된 기능, 참고용만 유지 — 2026-08-09 정리)

미국주식 페이지는 2026-08-08 공개 전환으로 완료됐습니다. 조사 과정·확정 이력·수치 전체 서술은
`TASK_HISTORY.md` #52~#65에 남아있습니다. 여기는 **비슷한 신규 시장을 또 추가할 때 재사용할 설계
원칙만** 요약해 남깁니다.

- **범위**: 나스닥+뉴욕 시가총액 상위 약 550개. 채권/노트류, 미국식 우선주(Preferred Stock 명시) 제외.
  동일 회사 복수 보통주 클래스(BRK/A·BRK/B, GOOGL·GOOG)는 전부 포함(대표 1개로 합치지 않음). MLP
  보통유닛·ADR은 일반 주식으로 취급해 포함. 통화는 USD 단독 표기(원화 환산 없음).
- **데이터 소스**: 유니버스는 `Ate329/top-us-stock-tickers` CSV(무료·자동 갱신), 펀더멘털은
  stockanalysis.com 종목별 statistics 페이지(소형주까지 커버리지 확인됨).
- **코스피와 다르게 설계한 부분과 이유**: 재무 건전성은 Trailing ROE 대신 F-Score 사용(ROE 중복 계상
  방지) · 변동성은 실측 시계열이 없어 베타를 배수가 아닌 점수 항목(5점)으로만 반영 · 주주환원율에
  자사주매입 포함(희석 시 음수 허용, 0으로 자르지 않음) · Forward EPS는 계산값(종가÷Forward PER,
  "🧮 계산값" 배지로 실측과 구분) · 애널리스트 컨센서스 목표가는 모델 목표가와 합치지 않고 나란히 노출.
- **타임존**: America/New_York 전용 헬퍼(`_now_et()`)로 서머타임까지 처리 — KST 크론 스케줄링 시
  날짜 경계 버그(장마감이 서머타임 여부로 KST 오전 5~6시 사이에서 날짜가 바뀜) 재발 방지용.
- **임계값 설계 원칙**: 처음부터 flat 하드코딩 대신 z-score/절대거리 비례 스케일링으로 설계(코스피
  쪽에서 나중에 감사로 고친 패턴을 처음부터 반영) — 실측 548종목 분포로 확정 완료(TASK_HISTORY #55).
- **남은 참고사항**: 상단 지수가 아직 단일 출처(§0-3-3 미충족, 화면에 "근사치" 고지로 공개 블로커
  아님으로 처리) — §4 참고.

## 9. 📊 "내 성적표" (3번째 모듈) — 진행 상황 (2026-08-11 신설, TASK_HISTORY #73)

> 이 섹션은 §4(매크로)와 **완전히 별개**입니다. 매크로는 오너 지시로 중단 상태이고, 지금 진행 중인
> 신규 작업은 이 §9입니다. 지시서 원본은 `SCORECARD_WORK_ORDER.md`(단일 진실 소스),
> 최초 검토 메모는 `MY_SCORECARD_MODULE_NOTES.md`.

### 9-1. 지금 상태 = 🔓 **공개 승인 완료 + 공개 전환 전 최종 점검 완료** (2026-08-13, #106)

> **⚡ 현재 상태 한 줄 요약 (2026-08-13 갱신)** — 오너가 2026-08-12 "둘 다 공개로 전환해줘"로
> §9·§10 두 모듈의 공개를 최종 승인했고, 공개 전 보안/견고성 최종 점검과 사이드바 레이아웃
> 정리까지 끝났습니다(#106). **코드 쪽에 남은 일은 없습니다** — 오너가 Streamlit Cloud Secrets 에
> `SCORECARD_ENABLED = "1"` / `REPORT_ENABLED = "1"` 2줄을 추가하면 그 즉시 공개됩니다
> (→ 이후 오너가 실제로 추가해 공개 절차 완료 — §4·§9-6). 자세한 점검 결과는 **아래 §9-6** 참고. (아래 9-1 본문은 2026-08-11 시점 기록입니다.)

#### (2026-08-11~12 시점 기록 요약 — 전체 서술은 `TASK_HISTORY.md` #73~#94)

v1 골격(#73) 후 **오너가 실제 Supabase 프로젝트 연결(RLS 8개 정책 적용 확인)과 실사용 테스트까지
직접 완료**했고, 실사용 피드백을 그때그때 반영해 입력/편집 UX를 완성했습니다(#74~#82: Enter 실수
제출 방지·콤마 입력 파싱·종목별 인라인 ✏️수정/🗑️삭제·코드/티커/이름 **통합 단일 입력칸**·국내
관례(빨강=오름/파랑=내림) 색상 통일·표 정렬·이름 일부만 쳐도 되는 빠른 검색·모바일 가로 스크롤
대응). 참고: #79·#80이 배포 화면에 반영 안 되던 소동은 코드 문제가 아니라 Streamlit Cloud
재배포/Reboot 지연으로 확인돼 해결됐습니다(2026-08-11 오너 스크린샷 확인).

이후 가격·이름 커버리지를 유니버스 밖으로 확장했습니다 — #83 코스피+코스닥+ETF 전체 상장종목
마스터 목록(이름 검색), #84 한국 전 종목 종가(네이버 목록형 페이지 재사용 — KRX 공식 API·공공
데이터포털은 약관상 광고 수익화와 충돌해 미채택, §9-3 원칙 참고), #92 미국 상장 전 종목
현재가(stockanalysis.com 스크리너의 devalue 직렬화 응답 전용 디코더, 요청 1회로 5,607종목),
#93 미국 ETF까지(**오너가 "ETF 제외" 원안을 명시적으로 뒤집음** — 뒤집은 건 현재가 표시 범위뿐,
밸류에이션은 §0-1대로 여전히 상위 200/550 보통주 전용). 실사용 버그로는 "종목 추가" 버튼이
조용히 씹히던 selectbox 상태 문제(#85 — nonce 동적 키로 근본 수정, 오푸스 별도 검토로 같은
원리의 잠재 버그 2건 추가 수정)와 가독성 다듬기(#86~#89, "$"가 마크다운 수식으로 오인되던 "VS"
배너 렌더링 버그 포함)를 처리했습니다.

테스트는 187 → **496체크**(#93 시점, 실측)까지 확장됐고 매 단계 기존 스위트 회귀 없음. 기존
공개 모듈·매크로 파일의 기존 수집·스코어링 경로는 한 줄도 바꾸지 않고 새 함수·상수만
덧붙였습니다(각 단계의 파일 범위·검증 서술은 `TASK_HISTORY.md` 해당 번호 참고).

### 9-2. 만든 것

| 파일 | 역할 |
|---|---|
| `sql/scorecard_schema.sql` | **오너가 Supabase SQL Editor에서 1회 수동 실행**할 설정 스크립트. `profiles`/`holdings` 테이블 + **RLS 정책 8개**(본인 행만 select/insert/update/delete) + `anon` 권한 회수 + 트리거. 여러 번 실행해도 안전, `DROP TABLE` 없음 |
| `utils/scorecard_db.py` | 데이터 계층 단일 출처. 순수 계산(가중평균·비중·수익비중)은 Supabase 없이 단독 호출 가능하게 분리. 기존 스냅샷(`data/*.json`)은 **읽기 전용** 재사용. `load_kr_ticker_master()`(전체 상장종목 로더)·`resolve_stock_query(..., broad_index=)`(3단계 폴백)·`load_kr_all_market_prices()`/`load_us_all_market_prices()`/`load_us_all_etf_prices()`(#93, 가격 전용 보조 목록 로더, `make_price_lookup(..., broad_kr_prices=, broad_us_prices=)`의 2차 폴백 — 미국은 화면에서 주식+ETF를 합쳐 넘김) 포함. #109 부터 비밀번호 재설정 2종(`send_password_reset_code()` / `reset_password_with_code()` — 6자리 OTP, 계정 존재 여부 비노출, 1회용 세션 자동 로그아웃)도 여기에 있음. `supabase`/`streamlit` 임포트는 `try/except ImportError` |
| `views/scorecard_view.py` | 로그인/회원가입/**비밀번호 찾기**(#109, 6자리 코드) → 보유종목 **통합 단일 칸**(코드/티커/이름, 전체 상장종목 대상 이름검색) 입력 → 통화별 표·요약(정렬·색상) → **원형차트 2종**(비중/이익 기여) → 선택 종목 PEGY 요약 카드 → 종목별 인라인 ✏️수정/🗑️삭제, 모바일 대응 |
| `collector_kospi200.py` | (기존 공개 수집기, #83부터 보조 함수 추가) `run_kr_ticker_master_collector()` — 코스피+코스닥+국내ETF 전체 상장종목 코드↔이름을 `data/kr_ticker_master.json`으로 저장(가격 없음, 이름 검색 보조 전용) |
| `data/kr_ticker_master.json` | (#83 신설, 아직 실제 수집 미실행) 코스피+코스닥+국내ETF 전체 상장종목 코드↔이름 목록. GitHub Actions가 기존 `git add data/` 글롭으로 자동 커밋 |
| `data/kr_all_market_prices.json` | (#84 신설, 2026-08-12 실검증 완료 — 4,296건) 코스피+코스닥 전 종목(ETF 포함) 코드↔이름↔종가만. 마찬가지로 `git add data/` 글롭으로 자동 커밋 |
| `collector_us_stocks.py` | (기존 공개 수집기, #92부터 보조 함수 추가) `run_us_all_market_prices_collector()`(주식) / `run_us_all_market_etf_prices_collector()`(#93, ETF) — stockanalysis.com 스크리너의 SvelteKit 데이터 엔드포인트를 **요청 1회** 불러 티커↔이름↔현재가만 저장. 두 진입점은 공용 본체 `_run_us_screener_prices_collector()`에 소스 URL·파일명·라벨만 바꿔 끼우는 구조. 응답이 devalue 직렬화 포맷이라 전용 디코더(`decode_sveltekit_data_json()`)도 같은 파일 §3-2에 있음. 기존 550종목 수집 경로는 한 줄도 안 바뀜 |
| `data/us_all_market_prices.json` | (#92 신설, 아직 실제 수집 미실행) 미국 상장 전 종목(실측 5,607) 티커↔이름↔현재가만. 밸류에이션 없음(§0-1). `git add -A data/` 글롭으로 자동 커밋 |
| `data/us_all_etf_prices.json` | (#93, 2026-08-12 실검증 완료) 미국 상장 **ETF**(실수집 5,567) 티커↔펀드명↔현재가만. 주식 파일과 **일부러 분리**(소스가 다른 두 요청 — 한쪽 실패가 다른 쪽을 지우지 않도록). 밸류에이션 없음(§0-1) |
| `tests/test_scorecard.py` | 오프라인 **382체크** (네트워크·Supabase 접속 불필요, 가짜 클라이언트+합성 데이터+실제 스냅샷). #109 에서 비밀번호 재설정 검증 46체크 추가 |
| `tests/test_stock_history.py` | 오프라인 **173체크** (기존 시계열/다운로드 검증 + `run_kr_ticker_master_collector()`(#83)·`run_kr_all_market_prices_collector()`(#84)·`run_us_all_market_prices_collector()`(#92) 가짜 응답 기능 테스트 + devalue 디코더 실응답 픽스처 검증 포함) |
| `tests/fixtures/us_screener_data_json_head.json` | (#92 신설) 2026-08-12 stockanalysis.com 스크리너 데이터 엔드포인트에서 **실제로 받은 응답의 앞부분을 그대로 잘라낸** devalue 픽스처(값 무수정, 뒤쪽 5,600여 행·화면 메타데이터만 제거). 네트워크 없이 디코더 회귀 검증용 |
| `tests/fixtures/us_etf_screener_data_json_head.json` | (#93 신설) 같은 방식으로 잘라낸 **ETF 스크리너** 실응답 픽스처(`count=5480`, VOO~VUG 7행, 값 무수정). 주식 스크리너와 행 키가 동일함을 회귀로 못 박아 둠 |
| `requirements.txt` | `supabase` 추가 (없어도 기존 두 모듈은 정상 작동) |
| `visiblehand.py` | 라우팅 추가. 임포트를 `try/except`로 감싸 이 모듈이 깨져도 기존 두 공개 화면은 살아남음 |

기존 파일(`views/pegy_view.py`, `views/us_stocks_view.py`, `utils/scoring.py`, `utils/scoring_us.py`,
`utils/constants.py`, 매크로 관련 전부)은 **한 줄도 수정하지 않았습니다**(테스트로 확인).
수집기 2개(`collector_kospi200.py` #83·#84 / `collector_us_stocks.py` #92·#93)와
`utils/constants_us.py`(#92·#93)에는 **새 함수·새 상수를 덧붙이기만** 했고 기존 수집·스코어링
경로는 건드리지 않았습니다(기존 스위트 회귀 없음으로 확인).

### 9-3. 이 모듈에서 특별히 지킨 원칙

- **개인 금융정보는 git에 안 올린다** — 기존 데이터는 전부 공개 저장소에 커밋되지만 이 모듈의 데이터는
  Supabase에만 저장됩니다. 접속 정보는 **Streamlit Cloud Secrets**(GitHub Actions Secrets가 아님)에서
  `st.secrets`로만 읽습니다. 소스·문서·로그 어디에도 실제 값이 없습니다.
- **RLS가 진짜 방어선** — anon key는 설계상 브라우저에 노출되는 게 정상인 키라 KRX 인증키와 성격이
  다릅니다. 그래서 "사용자는 자기 행만 본다"를 앱 코드가 아니라 **DB 정책**으로 강제합니다.
  `service_role` 키를 읽는 코드는 없습니다(RLS를 통째로 우회하므로 앱에 절대 넣으면 안 됨).
- **로그인 세션 공유 사고 방지** — Supabase 클라이언트를 `@st.cache_resource`로 캐시하면 한 사람의
  로그인이 모든 방문자에게 공유됩니다. 캐시하지 않고 방문자별 `st.session_state`에만 둡니다.
- **환율 변환 없음** — 원화/달러를 끝까지 분리해 계산하고, 둘을 합친 "총 자산" 숫자는 만들지 않습니다
  (DB의 CHECK 제약으로도 혼용을 차단). 화면 상단에 고지 상시 노출.
- **지어내지 않기(§0-1)** — 현재가는 기존 수집 스냅샷의 실측값만 사용. 유니버스(코스피 상위200/미국
  상위550) 밖 종목은 현재가·평가금액·수익률을 전부 비우고 "현재가 없음"으로 표시하며 비중 계산에서도
  제외합니다(0%로 속이지 않음). Supabase 호출 실패는 빈 값으로 삼키지 않고 `st.error`까지 도달시킵니다.
- **가중평균 표기** — 오너 마인드맵 예시(10주 100만원 + 3주 21만원 → 13주 평균 93,076원)와 화면 숫자가
  어긋나지 않도록, 저장·계산은 전체 정밀도(93,076.923…)로 하고 **원화 표기만 절사**합니다.
- **시세 데이터 상업적 이용 리스크 (2026-08-11, #84 조사로 확정된 원칙)** — KRX 공식
  OPEN API(`openapi.krx.co.kr`, 지금 매크로 화면의 VKOSPI·선물베이시스가 쓰는 바로 그
  API)는 이용약관 제6조②에 "비상업적 목적으로만 이용 가능, 대가를 제3자에게 청구 불가"가
  명시돼 있어 **광고 수익화와 양립 불가**임을 원문으로 확인했습니다. 공공데이터포털의
  금융위 계열 API도 같은 이유(공공누리 2유형)로 보류. 그래서 광고를 붙일 계획이 있는
  기능에는 KRX 공식 API를 새로 쓰지 않고, 이미 쓰고 있는 네이버 금융 스크래핑 방식을
  유지·확장하는 쪽으로 갑니다 — 이건 "완전히 안전하다고 확인된" 것은 아니고 오너가 감수
  하기로 한 리스크입니다(네이버 자체 약관 원문은 조사에서 확인 못함). 나중에 매크로 화면을
  다시 켜고 광고까지 붙이게 되면, VKOSPI·선물베이시스는 이 원칙과 충돌하니 그때 다시
  점검이 필요합니다.
- **모바일 우선 고려 (2026-08-11 오너 지시로 신설된 원칙)** — "모바일이 기본인 세상이니 미리미리
  고려하면서 디자인하자." 앞으로 이 화면(§9)에 새 UI를 만들 때는 데스크톱에서만 확인하고 끝내지
  않고, 좁은 화면(≤640px)에서 깨지지 않는지도 함께 고려합니다. 표처럼 칸이 많은 요소는 억지로
  욱여넣거나 글자를 줄이지 말고 **가로 스크롤**로 유지하는 쪽을 기본 원칙으로 삼습니다(#82 사례).
  다만 이 샌드박스는 실제 모바일 화면을 렌더링해 볼 수 없어, 최종 확인은 항상 오너의 실제 기기로
  해야 합니다.

### 9-4. ▶️ 오너가 다음에 할 일 — 전부 종결 (상세는 TASK_HISTORY #90·#91·#94, #119~#130)

**1~5번(Supabase 프로젝트 생성 → SQL·RLS 확인 → Email 인증 → Secrets 등록 → 실사용 테스트)은
2026-08-11 완료.** 이후 검증 항목도 아래처럼 전부 끝났고, 마지막 남았던 모바일 실기기 확인은
Streamlit 프레임워크 한계 확인으로 이어져 **§0의 NiceGUI 전면 이전으로 귀결**됐습니다.

1. ✅ **한국 전 종목 종가(#84) 실검증 완료(2026-08-12, #91)** — `data/kr_all_market_prices.json`
   2026-08-11 22:09 수집분, 코스피 2,477 + 코스닥 1,819 = **4,296건**, 가격 결측 0건. 오너가
   화면에서 직접 입력·확인했던 종목 가격들과 정확히 일치.
   ✅ **전체 상장종목 마스터 목록(#83)도 실제 워크플로우 실행으로 검증 완료(2026-08-11)** —
   `data/kr_ticker_master.json` **4,030건**(주식 2,870 + ETF 1,160, 코스피/코스닥/코스닥글로벌/
   코넥스 포함) 정상 생성·커밋, 컬럼 구조 경고 없음.
2. ✅ **미국 전 종목(#92)·ETF(#93) 실검증 완료(2026-08-12 force 재크롤링, #94)** — 같은
   회차(16:58 KST)에 주식 `count=5607` / ETF `count=5567`, 둘 다 `pages_fetched=1`·실패 0·차단
   없음. 화면에서 보유 ETF **KORU 현재가 $17.64 정상 표시**(이 작업의 출발점이던 "현재가 없음"
   사례 해결). `Confirm email`도 켜져 있음을 스크린샷으로 확인 완료(#90).
3. 🔚 **모바일 실기기 확인(2026-08-16 착수, TASK_HISTORY #119~#130) — Streamlit 한계 확인으로
   종결.** 사촌 실기기 시연에서 발견된 신고 중 `white-space: nowrap` 가로 밀림(#119·#122~#125)과
   보유종목 표 세로 쌓임(#126~#128 순수 HTML `<table>`+CSS flex 교체, #129 핫픽스, #130 선택자
   보강)은 수정했지만, **"Built with Streamlit" 하단 바는 제거 불가로 최종 결론**(#120 →
   #121 긴급 롤백 — Community Cloud가 iframe 임베드 시 `embed=true`를 사실상 요구),
   `st.columns()` 반응형 쌓기는 CSS로 못 덮는 프레임워크 자체 한계(공식 이슈 #6592)로
   확정됐습니다. **이 한계들이 §0의 NiceGUI 전면 이전의 직접 계기**이며, 구 Streamlit 화면은
   이제 참고용 사본이라 잔여 실기기 확인 항목은 더 진행하지 않습니다.

### 9-5. v1 이후 로드맵 (2026-08-12 오너 지시로 순서 변경)

> **2026-08-12 오너 지시**: v2(OCR)·v3(리포트) 순서를 바꿉니다 — §9-4가 끝나는 대로 **리포트
> 쪽을 먼저** 착수합니다. 리포트는 "내 성적표" 안에 다 표기하기엔 정보가 너무 많아서, "내
> 성적표" **아래에 별도 모듈**(기존 코스피/미국주식/내 성적표처럼 독립된 화면)로 만드는 게
> 낫다고 확정. 착수 시점에 이 모듈 전용 지시서(`SCORECARD_WORK_ORDER.md`처럼)를 새로 만들
> 예정 — 아직 코드는 없습니다.
>
> ⚠️ **오너 지시 — 리포트 모듈은 코딩할 때 오푸스(Opus)를 높은 추론 강도로 써서 작업할
> 것.** (지금까지 "내 성적표" 버그 수정·가독성 다듬기는 대부분 소넷으로 충분했지만, 리포트는
> 새 모듈을 처음부터 설계하는 작업이라 난이도가 다름 — 착수 시점에 이 지시를 반드시
> 다시 확인할 것.)

- **다음 순서 — 리포트 모듈** (이전 계획의 v3, "내 성적표"와 별도 화면/메뉴로 신설 예정): 매일
  백그라운드 수익률 누적, 일간·주간 결산 리포트, 벤치마크 비교. §9-4가 끝나면 착수. **코딩은
  오푸스(높은 추론 강도)로 진행.**
- **그다음 — 스크린샷 업로드 + AI OCR** (이전 계획의 v2, Gemini Developer API 별도 결제
  프로젝트 필요, ZDR 신청 권장. `MY_SCORECARD_MODULE_NOTES.md` 참고): 리포트 모듈 이후로 순연.
- 코스피 상위200 / 미국 상위550 **밖 종목의 실시간 시세 조회** (v1은 "밸류에이션 정보 없음"으로만
  표시 — #83으로 이름 검색 범위는 코스피+코스닥+국내ETF 전체로, #84로 한국 종목은 현재가까지
  코스피+코스닥 전체로, #92로 미국 종목도 현재가가 미국 상장 전 종목(약 5,600)으로,
  #93으로 **미국 ETF(약 5,480)까지** 넓어졌습니다.
  다만 ① 여전히 "실시간"이 아니라 수집 시점 종가일 뿐이고, ② PEGY/퀀트 밸류에이션은 상위
  200/550 밖은 여전히 없으며(ETF는 EPS·ROE 자체가 없어 앞으로도 없음), ③ **미국은 이름 검색
  범위가 아직 상위 550 안**입니다 — 티커를 직접 입력하면 가격이 나오지만 회사명으로 찾는 건
  #83의 미국판이 아직 없어서 안 됩니다
  (`us_all_market_prices.json`/`us_all_etf_prices.json`에 이름도 들어있으니 나중에
  `resolve_stock_query()`의 `broad_index`에 연결하면 되는, 작은 후속 작업))
- "1년 미접속 시 데이터 삭제" 자동 배치 (v1은 회원가입 화면 안내 문구만)

### 9-6. 🔓 공개 전환 — 완료 (2026-08-13, TASK_HISTORY #106·#107)

오너가 2026-08-12 **"둘 다 공개로 전환해줘"** 로 §9(내 성적표)·§10(사장님 보고서) 두 모듈의
공개를 최종 승인했고(§0-3-6), 공개 전 최종 점검(#106)은 **발견 2건·수정 2건·"문제 없음" 확인
4건**으로 끝났습니다:

- **🔧 수정 ①(XSS)** — 사용자 소유 컬럼 `stock_name`·`ticker`가 `unsafe_allow_html`로 그려지고
  있어 `_row_label_html()`에 `html.escape()` 적용(RLS 덕에 남을 공격하는 XSS는 아니었지만 본인
  세션 토큰 탈취용 사회공학이 가능했음).
- **🔧 수정 ②(입력 검증)** — `float()`가 `"nan"`/`"inf"`/`"1e400"`을 통과시키고 특히 수정(✏️)
  경로는 검증이 아예 없었음 → 공유 함수 `_parse_positive_number()`에 유한성 + 상한
  (`MAX_INPUT_VALUE`, DB `numeric(20,6)`에서 유도) 검사 추가.
- ✅ RLS(두 SQL 파일)·`service_role` 격리(`utils/report_db.py` 한 곳뿐)·가입 남용/이메일 인증
  우회·에러 메시지 내부 정보 노출은 전부 문제 없음(SQL 변경 없음).

이후 오너가 Streamlit Cloud Secrets에 `SCORECARD_ENABLED = "1"` / `REPORT_ENABLED = "1"`을
추가해 **공개 절차 완료**(2026-08-13 캡처로 확인 — §4). 같은 날 "리포트"를 "📈 사장님 보고서"로
개명하고 사실과 달라진 스테이징 배너 2개를 삭제, RLS 안내는 `st.info`로 톤만 낮춰 유지(#107).
자세한 점검 서술은 `TASK_HISTORY.md` #106·#107 참고. (현재 공개 상태의 단일 출처는 새 사이트
기준 **§0-3**입니다.)

### 9-7. 🔑 비밀번호 찾기(재설정) — 실사용 검증까지 완료 (2026-08-13, TASK_HISTORY #109·#110)

오너 요청("간단하게 아이디 비밀번호 찾는 과정은 필요할 것 같애 그래야 계정무한생성을 막을 수
있어")으로 로그인 화면에 **"비밀번호 찾기" 탭**을 추가했습니다. **'아이디 찾기'는 없습니다** —
이 앱의 로그인 ID가 이메일 그 자체라 필요한 건 비밀번호 재설정 하나뿐입니다.

- **방식 = 이메일 링크가 아니라 숫자 코드(OTP)** — Streamlit이 URL 해시 프래그먼트를 읽을 수
  없어, Supabase 템플릿 변수 `{{ .Token }}` + `verify_otp(type="recovery")`로 **리다이렉트가 전혀
  필요 없는** 흐름(①이메일로 코드 발송 → ②코드+새 비밀번호로 즉시 변경)을 택했습니다(등록할
  Redirect URL 없음). 계정 존재 여부 비노출 문구, 1회용 클라이언트로 기존 로그인 세션과 분리
  (검증 후 자동 로그아웃), 1회용 코드를 헛되이 소모하지 않는 사전 검증 포함.
- **⚠️ 실측 정정(#110)** — 문서상 "6자리" 코드가 실제로는 **8자리**로 와서 화면이 정상 코드를
  거부하던 버그를 수정(**6~10자리 범위**로 완화, 정오답 최종 판단은 Supabase 서버). 실측 8자리
  코드 통과를 회귀 테스트로 고정.
- **오너 쪽 설정·실사용 검증 전부 완료(2026-08-13)** — Resend Custom SMTP 연결 +
  `mail.visiblehand.co.kr` 도메인 인증(DKIM/SPF/DMARC) + Reset Password 템플릿에 `{{ .Token }}`
  추가, 실제 메일 수신 → 8자리 코드로 비밀번호 변경 → 새 비밀번호 로그인 → 보유 종목 유지까지
  직접 확인. **더 할 일 없음.** 자세한 서술은 `TASK_HISTORY.md` #109·#110 참고.

### 9-8. 👁️ 통화 요약 카드 — 동기화 시각·수익률 가시성 개선 (2026-08-16, TASK_HISTORY #118)

오너 지적("가시성이 좀 많이 떨어진다 … 글자를 키우던 색을 좀 넣던")으로 ① 동기화 시각 캡션을
굵고 진하게(🕒 아이콘 포함), ② 요약 카드 수익률 %를 전용 `_colored_pct_lg()`(같은 빨강/파랑
규칙, 1.35rem·굵게)로 확대했습니다 — 값·계산·색 규칙 무변경, 렌더링 크기만 조정. 7개 스위트
회귀 없음. 자세한 이력은 `TASK_HISTORY.md` #118 참고.

## 10. 📈 "리포트" (5번째 모듈) — 진행 상황 (2026-08-12 신설, TASK_HISTORY #95)

> 이 섹션은 §4(매크로, 중단 상태)와 완전히 별개이고, §9("내 성적표")와도 **별도 모듈**입니다.
> 지시서 원본은 `REPORT_WORK_ORDER.md`(단일 진실 소스). 코딩은 오너 지시대로 오푸스(높은 추론 강도).

### 10-1. 지금 상태 = 🔓 공개 완료 (2026-08-13 승인·전환 — 상세 이력은 TASK_HISTORY #95~#114)

- 오너가 2026-08-12 **"둘 다 공개로 전환해줘"** 로 공개를 최종 승인 → 공개 전 보안/견고성 최종
  점검(#106, `service_role` 격리·RLS select 1개 재확인 포함) → Secrets 2줄 추가로 공개 완료.
  절차·점검 결과는 **§9-6**에 한 곳으로 모았습니다(두 모듈이 같은 절차라 중복하지 않음).
- **#112·#113의 Supabase SQL(`sql/report_schema.sql` 전체 실행)은 오너가 2026-08-13 실행 완료**
  (§4) — 다음 배치부터 종목별 스냅샷·수집 시각이 쌓이는 상태로 마무리됐습니다. 실행 전 날짜의
  값은 소급해서 채우지 않습니다(§0-1).
- **🧾 2026-08-13 개명(#107)** — 사용자에게 보이는 이름만 "📈 리포트" → **"📈 사장님 보고서"**.
  파일명·함수명·컬럼명·플래그(`report_view.py`·`REPORT_ENABLED` 등)와 문서 내부 용어 "리포트
  모듈"은 그대로 둡니다(코드 추적용 이름이라 바꾸면 추적만 어려워짐).
- **첫 자동 실행 실패 2건(#96) → 배치 파이프라인 전 구간 실검증 완료(2026-08-12, #97).**
  ① 파싱이 응답 최상위만 훑던 버그는 깊이 우선 재귀 + **날짜(`t`) 있는 스키마만** 인정으로 수정
  — ⚠️ 시세 공급자 이름은 조건으로 쓰지 않습니다(SPY=`tiingo`, ONEQ=`spg`로 서로 다름).
  ② 파일 없으면 `git add`가 잡을 죽이던 워크플로우는 "있으면만 add"로 수정. 이후 `SUPABASE_URL`
  시크릿 오등록(`PGRST125`, §10-4 2번 참고)까지 정정한 재실행에서 벤치마크 수집·보유 종목
  8행/사용자 1명 조회·스냅샷 2행(KR+US) 저장·파일 커밋 전부 정상.
- ⚠️ 리포트는 스냅샷이 **며칠 쌓여야** 의미 있는 숫자가 나옵니다. 신규 가입자에게 "📭 아직
  저장된 스냅샷이 없습니다"가 보이는 건 버그가 아니라 설계이며(과거를 역산해 만들지 않음 —
  §0-1), 화면 상단 안내문("이 보고서는 **시간이 지나면 채워집니다**")이 직접 설명합니다.

### 10-2. 만든 것 (신규 파일 6개, 기존 파일 **한 줄도 수정 안 함**)

| 파일 | 역할 |
|---|---|
| `sql/report_schema.sql` | **표 2개** — `portfolio_daily_snapshots`(사용자 × 시장 × 거래일 = 1행, 합계) + `portfolio_holding_snapshots`(§8, 2026-08-13 추가 — 사용자 × 시장 × **종목** × 거래일 = 1행, 상세) + 각각의 RLS. **정책이 select 하나뿐**인 게 `holdings` 와 다른 점 — 쓰기는 배치(service_role)만 하고 사용자는 자기 과거 기록을 못 고칩니다. `priced_count > 0` CHECK 로 "그날 가격을 하나도 모르면 행 자체가 안 생기게" DB 레벨에서도 강제 |
| `collector_us_indices.py` | 미국 벤치마크(S&P500·나스닥) 일별 종가 수집기 — 아래 §10-3 소스 조사 결과 참고 |
| `utils/report_db.py` | 기간 경계 계산 · 스냅샷 행 생성 · 기간 집계/데이터 부족 판정 · 벤치마크 수익률 · Supabase 접근. **⚠️ 2026-08-19부터 더 이상 유일하지 않음 — `utils/duel_db.py`·`run_duel_daily_batch.py`·`run_duel_publish_batch.py`도 같은 service_role 격리 패턴으로 씀(§ 결투 모듈 항목 참고)** |
| `views/report_view.py` | 리포트 화면(기간 선택·이전/다음 기간·시장별 블록·벤치마크 비교·데이터 부족 안내). 기본 숨김 |
| `.github/workflows/scrape_report_snapshots.yml` | 평일 23:20 UTC 실행(코스피·미국 수집이 끝난 뒤) |
| `tests/test_report.py` | 오프라인 **547체크** (네트워크·Supabase 불필요. 2026-08-16 #117 기준 — #112 +41, #113 +113, #114 +47, #115 +23, #116 +21, #117 +24). #96 부터 파싱 검증은 **실제 응답 원문 픽스처**(`tests/fixtures/us_index_history_{spy,oneq}_data_json_head.json`, 2026-08-12 캡처)로 합니다 |

### 10-3. 🔍 벤치마크 소스 — 실응답으로 확인한 결과와 채택 근거 (§0-1)

- **한국(KR)**: `market_history.csv` 에 "코스피 종가"가 이미 매일 쌓이고 있어 **읽기 전용으로 재사용**
  합니다(새 크롤링 없음). ⚠️ 매크로 파이프라인 산출물이라 **쓰기 금지** — 테스트가 이 파일의 바이트가
  변하지 않는지까지 확인합니다. 코스닥 지수는 이 파일에 없어 **v1 은 코스피만**(후속 과제).
- **미국(US)**: 이 프로젝트에 미국 지수 데이터가 아예 없어 새로 조사했습니다.
  - stockanalysis.com 에는 **지수 자체 페이지가 없음**을 확인(`/quote/index/SPX/__data.json` 이
    데이터가 아니라 symbol-lookup 리다이렉트를 돌려줌).
  - 대신 이미 화면 상단 지수에 쓰고 있는 **추종 ETF 프록시**의 과거주가 엔드포인트
    `https://stockanalysis.com/etf/{symbol}/history/__data.json` 가 살아 있고, **요청 1회로 약
    6개월치 일별 종가**를 돌려줍니다(2026-08-12 실응답 재확인 — SPY·ONEQ 모두 125행,
    2026-02-11 ~ 2026-08-11. 행 `{"a":104.16,"c":104.16,...,"t":"2026-08-11",...}`). 응답이 #92 와
    같은 devalue 포맷이라 **디코더를 그대로 재사용**했습니다.
    - 응답 구조(#96 에서 실측 확정): 행 목록은 최상위가 아니라 한 겹 안쪽
      `{"data":{"symbol":"SPY","source":"tiingo","updated":"…","data":[행…]}, "news":[…], …}`.
      **ETF 이름은 이 엔드포인트에 없습니다**(v1 주석의 `"name":"Fidelity …"` 는 다른 페이지 값
      이었음) → 종목 확인은 응답이 스스로 말하는 `symbol` 로 합니다. 시세 공급자는 종목마다 달라
      (SPY=`tiingo`, ONEQ=`spg`) **조건으로 쓰지 않습니다.**
  - **FRED 는 채택하지 않았습니다(중요)**: `fredgraph.csv?id=SP500` 로 **지수 실값** 일별 종가를
    무료로 받을 수 있음을 실응답으로 확인했지만, 같은 응답에 S&P Dow Jones Indices 의 재배포 제한
    ("Reproduction of S&P 500 in any form is prohibited except with the prior written permission of
    S&P Dow Jones Indices LLC")이 명시돼 있습니다. **#84 에서 KRX OPEN API 를 배제했던 것과 같은
    기준**(오너의 광고 수익화 계획과 충돌)으로 보류했고, 근거를 `collector_us_indices.py` 상단에
    남겼습니다. 나중에 "지수 실값이 꼭 필요하다"고 판단되면 그 주석을 근거로 재검토하면 됩니다.
  - Stooq / Yahoo chart API / Nasdaq 공식 API 는 이 개발 환경에서 응답 자체가 오지 않아,
    살아있는지도 확인 못 한 소스라 코드에 넣지 않았습니다(§0-1).
- ⚠️ **그래서 미국 벤치마크 값은 "지수 포인트"가 아니라 "ETF 종가"입니다**(S&P500 약 6,600pt vs
  SPY 약 750달러). 이 사실이 숨겨지지 않도록 키 이름부터 `SP500_PROXY_SPY` / `NASDAQ_PROXY_ONEQ`
  로 두고 DB 코멘트·화면 문구에도 그대로 적었습니다. 리포트가 쓰는 건 절대값이 아니라 **기간
  수익률**이라 목적은 달성되지만, 운용보수·추적오차·배당락 때문에 지수와 완전히 같지는 않습니다.
  배당 조정가가 아니라 **미조정 종가**를 저장합니다(조정가는 과거 값이 소급해 바뀌어 기록이 흔들림).

### 10-4. ▶️ 오너가 다음에 할 일 — 전부 완료 (2026-08-12~13, 상세는 TASK_HISTORY #97~#106)

1. ✅ **Supabase SQL 실행 완료** — `sql/report_schema.sql` 전체(#113 확장 포함, 여러 번 실행해도
   안전) 실행 완료(§4).
2. ✅ **GitHub Actions Secrets(`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`) 등록 완료.**
   ⚠️ 다시 등록할 일이 있으면 `SUPABASE_URL`에는 Supabase 대시보드의 "API URL"(`/rest/v1/`
   경로 포함)이 아니라 **베이스 "Project URL"**(경로·끝 슬래시 없음)을 넣어야 합니다 —
   #97에서 겪은 `PGRST125` 오류의 원인. 🔴 service_role 키는 RLS를 통째로 우회하므로
   앱 쪽 Secrets에는 절대 넣지 마세요.
3. ✅ **워크플로우 실검증 완료(2026-08-12, #97)** — 벤치마크 수집·보유 종목 8행/사용자 1명
   조회·스냅샷 2행(KR+US) 저장·파일 커밋까지 전 구간 실제 운영 확인.
4. ✅ **화면 배선 + 사이드바 최종 형태 확정(#98→#105, 흔들림 문제는 #106에서 해결).** 최종 구조:
   맨 위 대분류 라디오 → 각 대분류 중분류 → 맨 아래 관리자 로그인. CSS 주입·매직넘버 없이 공개
   API만 사용(반복 조정 과정은 `TASK_HISTORY.md` #98~#106 참고).
5. 스냅샷은 평일 23:20 UTC 자동 실행으로 자연히 쌓입니다(과거 역산 없음 — 쌓이기 전에는 화면이
   정상적으로 "데이터 부족"만 보여줍니다).

### 10-5. 이 모듈에서 특별히 지킨 원칙

- **service_role 격리** — `utils/report_db.py` 한 파일만 이 키를 쓰고, **환경변수에서만** 읽으며
  streamlit 을 아예 import 하지 않습니다(앱 설정에서 읽히는 경로 자체를 만들지 않음). 화면 코드에는
  `create_service_client` 가 등장하지 않습니다(테스트로 강제).
- **읽기는 전체 사용자, 쓰기는 스냅샷 한 곳** — 배치는 `holdings` 를 select 만 합니다.
- **데이터 부족은 주 컨텐츠로** — 작은 경고 한 줄 붙인 정상 리포트로 위장하지 않고, 부족하다는 사실과
  **실제로 비교한 날짜**를 화면 한가운데에 보여줍니다. 몇 %가 아니라 날짜·일수 같은 사실로 말합니다.
- **소급 계산 금지** — 기능을 켠 날부터 쌓입니다. 벤치마크가 없는 날은 NULL 이고 전날 값을 복사하지
  않습니다. 비교 기준일의 벤치마크 종가가 없으면 가까운 날로 대체하지 않고 "비교 불가"로 둡니다.
- **환율 변환 없음** — 원화/달러는 시장별 행으로 끝까지 분리(DB CHECK 제약으로도 차단).
- **v1 은 단순·정직한 지표** — 구간 시작/종료 스냅샷의 단순 비교이고, 구성이 바뀐 구간에는 "매매
  영향이 섞여 있다"는 안내를 반드시 붙입니다. TWR 같은 정교한 계산은 v1 범위 밖(지어내지 않기).
- **화면 숫자와 스냅샷이 같은 계산** — 평가금액은 "내 성적표"의 `make_price_lookup()`/
  `evaluate_holding()` 을 그대로 재사용합니다(두 번 구현하면 언젠가 반드시 어긋납니다).
- **합계와 종목별이 같은 계산** (2026-08-13 추가, §10-8) — 종목별 스냅샷 표를 덧붙이면서도
  합계를 따로 계산하는 경로를 만들지 않았습니다. **종목별 행을 먼저 만들고 그것을 그대로 더해
  합계 행을 만듭니다.** 화면에서도 매번 두 값을 대조해 어긋나면 숨기지 않고 표시합니다.

### 10-6. v1 에서 제외한 것(다음 순서)

- 시간가중수익률(TWR) 등 정교한 수익률 계산 · 코스닥 벤치마크 · 리포트 PDF/이메일 발송·알림
- 미국 지수 **실값**(FRED 등 재배포 제한 소스)의 사용 여부 판단 — §10-3 참고, 오너 결정 사항
- 사이드바 배선/공개 전환 — §0-3-6 오너 승인 후

### 10-7. 🕐 "이 리포트는 언제 종가로 만들어졌나" 표시 (2026-08-13, TASK_HISTORY #112)

오너 피드백(미국장 마감 시차 때문에 "언제쯤 종가로 만들어진건지 눈에 확 들어오질 않아")으로
`portfolio_daily_snapshots`에 `price_as_of_kst` 컬럼(text, nullable)을 추가해, 배치가 이미 읽고
있던 수집기 메타데이터의 시:분(KR=`last_updated_at`, US=`last_updated_at_kst` — 수집기가 변환해
둔 KST 재사용, **앱이 시차·서머타임 계산을 새로 하지 않음**)을 버리지 않고 저장·표시합니다.
`timestamptz`가 아니라 `text`인 이유 = 원본이 분 단위까지만 기록이라 없는 초·정밀도를 만들지
않기 위함(§0-1). 기능 도입 전 행은 "시각 정보 없음"으로 정직 표기하고 소급하지 않으며, 화면
코드에 `date.today()`/`datetime.now()`가 없음을 테스트가 강제합니다. ALTER 1줄은 오너가 실행
완료(§4). 자세한 서술·검증(신규 41체크)은 `TASK_HISTORY.md` #112 참고.

### 10-8. 🧾 종목별 일일 스냅샷 저장 (2026-08-13, TASK_HISTORY #113)

오너 결정 원문 요지: *"내가 제일 중시하는 데이터의 품질관리를 하려면 … 깨끗하게 잘 정리된 상태의
많은 데이터가 필요해 … 들쑥날쑥하면 세상의 모두가 힘들어져."* — 합계 1줄만으로는 "이 달 수익이
어느 종목에서 났는지"를 영원히 알 수 없고 과거 종목별 값은 소급 계산이 불가능해(§0-1),
**`portfolio_holding_snapshots` 테이블(사용자 × 시장 × 종목 × 거래일 = 1행)** 을 신설했습니다.
늘린 건 기간 축이 아니라 **종목 축**입니다 — 기간별 테이블은 여전히 만들지 않고 합계 표의 날짜
범위 쿼리로 계산합니다. **저장량 증가(사용자당 연 약 2,500행 / 0.5MB 안팎, 무료 티어 500MB 기준
수백 사용자·수년 여유)는 오너가 설명을 듣고 감수한 사항**이며, 한계에 가까워지면 종목별 행만
오래된 것부터 지우는 보존 정책을 그때 도입하면 됩니다(합계 표·기간 리포트는 무영향).

- **"들쑥날쑥" 원천 차단** — 합계를 따로 계산하는 코드 경로가 저장소에 없습니다:
  `build_snapshot_rows_with_holdings()`가 `evaluate_holding()`('내 성적표' 화면과 **같은 함수**)로
  종목별 행을 먼저 만들고 **그것을 그대로 더해(Decimal)** 합계 행을 만듭니다. 화면은 매번 두 값을
  대조해(허용오차 0.01 — 배정밀도 한계 실측 근거는 #113) 어긋나면 숨기지 않고 표시합니다.
- **부분 실패 안전** — Supabase REST에는 다중 테이블 트랜잭션이 없어 **합계 먼저 → 종목별
  나중** 순서로 저장 ⇒ "종목별은 있는데 합계가 없는 날"은 구조적으로 안 생깁니다. 실패는
  삼키지 않고 배치를 빨갛게 실패시키며, upsert라 다음 실행이 같은 날짜를 다시 채웁니다.
- **화면** — 시장 블록 안 `🧾 종목별 상세`(기간 내 마지막 기록일 하루를 평가금액 큰 순 한 표 +
  합계 한 줄, 종목 선택 시 일별 추이 펼침). 날짜를 섞지 않고, 가격을 몰랐던 날은 "가격 모름"으로
  표기합니다(전날 값 안 끌어옴).
- DDL 전문·자가 점검 쿼리는 `sql/report_schema.sql` §8~§9에 있고 **오너가 2026-08-13 실행
  완료**(§4). ⑨ "합계 = 종목별 합" 대조 쿼리는 항상 0행이어야 정상입니다(표 생성 전 날짜는
  종목별 행이 없어 나오는 게 정상 — 소급하지 않음). 전체 서술은 `TASK_HISTORY.md` #113 참고.

### 10-9. 🔧 버튼 버그 수정 · 문구 간소화 · 비중(%) 표 (2026-08-13, TASK_HISTORY #114)

오너 지적 3건을 한 번에 처리 — ① '이전/최신/다음 기간' 버튼이 눌러도 반응 없던 버그 수정,
② 화면 문구 대폭 간소화(기준="처음 보는 방문자" — **§0-1 고지는 표현만 압축하고 뜻은 하나도
삭제하지 않음**, `⚖️ 데이터 대조 통과` 같은 개발자용 문구는 어긋났을 때만 경고), ③ 📊 종목별
비중(%) + 기간 시작 대비 비중 변화 표 신설(#113 데이터로 **조회 시점 계산** — 새 테이블 없음,
가격 모르는 종목은 0%가 아니라 분모 제외+"모름" 표기, 단위는 %p, 비교 시작점은 "이 기간 안의
첫 기록일"). 상세 원인 분석·검증(신규 47체크)은 `TASK_HISTORY.md` #114 참고.

- ⚠️ **위젯 관례(앞으로도 준수)** — 버튼으로 날짜/기간 위젯 값을 바꿀 때는 반드시 "pending 표시
  → `st.rerun()` → 다음 렌더 맨 앞(위젯 생성 전)에 **위젯의 진짜 키**에 대입" 3단계(#85와 동일
  관례). 위젯이 만들어진 뒤 그 키에 대입하는 것, 그리고 `value=` 병용은 금지(노란 경고 상자).
- 🟡 오너 확인 3가지(㉮㉯㉰)는 §4 첫 항목에 정리돼 있고 아직 확인 대기입니다.

### 10-10. 🇺🇸 미국 종목명 한글 표기 (2026-08-16, TASK_HISTORY #115)

오너 요청("스페이스 X 애플, 엔비디아 … 이런식으로"). 근본 원인은 기능 부재가 아니라 **배선
누락** — '내 성적표'는 이미 한글 표기 중이었는데 리포트 화면만 스냅샷의 영문 원문(`stock_name`)을
그대로 뿌리고 있었습니다. `views/report_view.py`가 `_display_name()`을 읽기 전용으로
import(scorecard_view 무수정)하고, 상위 550 유니버스의 미리 계산된 `name_kr`을 같은 로더로
재사용해 공개 미국주식 화면·내 성적표와 글자 단위로 같은 표기를 보장 — 종목명이 나오는 4자리
전부 동시 적용. **DB 저장값은 영문 그대로**(표시 시점에만 변환 — 마이그레이션 불필요, 되돌리기
가능), 한글명을 못 만들면 지어내지 않고 영문/티커로 폴백(§0-1). 계산·한국 종목 무영향.
자세한 서술·검증(신규 23체크)은 `TASK_HISTORY.md` #115 참고.

### 10-11. ➗ 벤치마크 비교에 "미국 두 지수 평균" 한 줄 (2026-08-16, TASK_HISTORY #116)

오너가 수기로 관리하던 "2025년수익률 비교 - 연말 성적표.csv"의 **"VOO / QQQ 평균 수익률"** 줄을
옮긴 것. 기존 벤치마크 비교 글머리표 아래 `_render_benchmark_average()` 한 줄만 덧붙여 모든
기간(일/주/월/분기/반기/연)에 자동 적용됩니다. 라벨은 실제 수집값(SPY·ONEQ 프록시) 기준
**"S&P 500 / 나스닥 종합 평균"** — 가지고 있지도 않은 VOO/QQQ 종가로 계산한 것처럼 적지
않습니다(§0-1). 두 벤치마크가 모두 계산됐을 때만 표시하고, 아니면 평균 줄 자체를 생략합니다
("평균: 데이터 없음" 같은 애매한 줄 없음).

- **🏦 "시중은행 평균 예금금리" 비교는 오너 결정으로 제외** — 실제 출처(한국은행 ECOS Open API
  연동)를 제안하자 *"수익 창출이 안 되면 못 쓴다"* 며 빼기로 결정. 출처 없이 그럴듯한 금리를
  넣지 않았고(§0-1, 테스트가 미구현 여부까지 확인), **되살릴 때는 ECOS 연동이 먼저입니다.**
- 자세한 서술·검증(신규 21체크)은 `TASK_HISTORY.md` #116 참고.

### 10-12. 📅 '일간' — 주말·공휴일에는 가장 최근 기록일 자료를 대신 표시 (2026-08-16, TASK_HISTORY #117)

오너 요청("굳이 이렇게 비워둘 필요는 없을거 같은데")으로, '일간'에서 기록이 없는 날을 고르면
`utils/report_db.resolve_display_date()`(순수 함수, 스키마·쿼리 변경 0)가 **기준일 이전 가장 최근
기록일**을 골라 평일과 완전히 동일한 렌더링 경로로 보여줍니다. 값은 하나도 만들지 않고 저장된
날짜만 고르며(§0-1), 대체 시 반드시 "어느 날 기록을 대신 보여주는지" 경고 상자를 먼저 띄우고,
대체할 과거 기록이 없으면 기존 "데이터 부족" 그대로입니다(뒤 날짜를 끌어오는 경로 없음).
한국·미국 블록은 각자의 마지막 기록일을 따로 판정합니다.

- 🔍 **주간~연간에는 일부러 적용하지 않았습니다** — 창이 여러 날이라 같은 문제가 없고, 거기까지
  대체하면 "9월 리포트에 8월 숫자"가 실리는 전혀 다른 이야기가 됩니다. 오너가 원하면 별도
  작업으로 가능하지만 **확인 없이 넣지 않았습니다.**
- 자세한 원인 분석·검증(신규 24체크, 리포트 스위트 547체크)은 `TASK_HISTORY.md` #117 참고.

---

## 11. 💰 "투자 감사합니다!" 배당금 모듈 (6번째 모듈) — 진행 상황 (2026-08-23 신설)

> 지시서 원본은 `DIVIDEND_MODULE_WORK_ORDER.md`(단일 진실 소스). 이 섹션은 요약입니다.
> 모듈 번호는 2026-08-24 오너 확정 — "내 성적표" 3번째(변경 없음), "리포트" **4번째**(기존
> 5번째), "결투다!" **5번째**(기존 4번째), 이 모듈("투자 감사합니다!")이 **6번째**. 관련 12개
> 파일(리포트·결투다! 실 코드 주석/독스트링 포함, `TASK_HISTORY.md` 과거 기록 포함)을 새
> 번호로 맞춰 고쳤습니다(`4444dd3`).

### 11-1. 지금 상태 = 🟠 **데이터 수집 완주 · 화면 구현 완료 · 비공개 대기**

> **⚡ 한 줄 요약 (2026-08-24)** — 2026 사업연도 배당 데이터 첫 전수 수집이 GitHub Actions
> 에서 **완주**했고(4.8시간, 요청 5,484건), 배당 캘린더 화면(`/dividend`)도 구현을
> 마쳤습니다. 다만 `DIVIDEND_ENABLED` 기본값이 꺼져 있어 **오너가 실 데이터로 확인하고
> 켜기 전까지는 아무에게도 안 보입니다**(§0-3-6 스테이징). 유니버스 문제(§11-4 첫 항목)는
> 화면에 정직하게 노출만 해뒀을 뿐 아직 해결되지 않았습니다 — 지금 데이터로는 "전
> 상장사"가 아니라 2023~2025년 배당 이력이 있던 2,734종목만 다룹니다(§0-1).

### 11-2. 만든 것

| 파일 | 역할 |
|---|---|
| `collector_dividend_kr.py` | DART `alotMatter.json` 기반 수집기 본체. 보고서 우선순위 탐색·`se` 라벨 키워드 파싱·체크포인트·예산 종료·raw 분리 저장 |
| `corp_code_mapper.py` | 종목코드 ↔ DART `corp_code` 매핑 (+ `corpCode.xml` ZIP 캐시) |
| `tests/test_dividend_collector.py` | 오프라인 검증 **188개**(69 → §2-4 단위검증 +12 → 교차검증·체크포인트 +42 → 델타 병합 +25 → 덮어쓰기 방지 가드 +8 → 종목코드 형식확장·회사명 매칭 +32, 2026-08-24). 네트워크는 `monkeypatch` 로 갈아끼워 실제 소켓을 열지 않음. 픽스처는 삼성전자 실제 응답 원문 |
| `.github/workflows/collect_dividend_kr.yml` | 수동 실행 워크플로우 (수집 → `data/` 커밋·푸시). 워크플로우 입력값은 `env:`로 받아 셸 인젝션 방어(2026-08-24 외부 검토 반영) |
| `data/dividend_kr_2026_latest.json` | 가공본 + `summary` 리포트 (실측 6.8MB) |
| `data/dividend_kr_2026_raw.jsonl` | DART 원본 응답 그대로 (§0-3-3, append-only, 실측 9.4MB) |
| `data/dividend_history_kr_2023_2025.json` | 2023~2025 배당 기준선(KIND 연간 집계, 오너가 직접 확보, 8,202건) — 2026년 미확정 종목의 "작년 배당율" 폴백에 사용 |
| `web/pages/dividend_page.py` | (2026-08-24 신설) 배당 캘린더 화면. §11-5 참고 |
| `data/dividend_kr_2026_universe_delta.json` | (2026-08-24 신설) 유니버스 확장용 델타 파일 — 기존 2,734종목 유니버스에 없는 29종목(§11-4 1번). 아직 실제 수집에는 안 씀 |
| `data/dividend_kr_2026_universe_delta_excluded_preferred.json` | (2026-08-24 신설) 위 델타에서 제외된 우선주 추정 110종목의 감사 로그(부모종목·판단 근거 포함) |

### 11-3. 첫 전수 실행 결과 (2026-08-24, 오너가 수동 실행)

| 항목 | 값 |
|---|---|
| 유니버스 | 2,734종목 (`data/dividend_history_kr_2023_2025.json`) |
| 완주 여부 | ✅ `completed: true` / `stopped_reason: null` |
| 사용 요청 | 5,484건 (종목당 평균 2.0건) |
| 소요 시간 | 17,294초 ≈ **4.8시간** (job timeout 340분, 여유 약 1시간) |
| 상태별 | OK **2,572**(그중 실제 보통주 현금배당 확정 **155**) / NO_DATA **107** / UNMAPPED **55** |

- **NO_DATA 107건은 "무배당"이 아닙니다.** DART 는 '조회 결과 없음'과 '존재하지 않는
  corp_code' 를 **똑같이 status 013** 으로 돌려줍니다(개발 세션 실측). 그래서 이 107건에는
  진짜 무배당 회사와 매핑이 틀린 회사가 섞여 있고 **API 응답만으로는 구분할 수 없습니다.**
  이 한계는 각 레코드의 `status_reason` 과 리포트 `known_limitations` 에 그대로 적혀
  있고, 화면(`/dividend`)에도 원문 그대로 노출됩니다(§0-1).
- **UNMAPPED 55건**은 상장폐지·비상장 전환 등으로 DART 고유번호 표에 없는 종목입니다.
  조용히 빠지지 않고 같은 스키마의 레코드 + `summary.unmapped_detail` 로 남아 있습니다.
- **OK 2,572건 중 실제 배당액이 있는 건 155건뿐**입니다(`ok_with_common_dps`) — 나머지
  2,417건은 정기보고서는 찾았지만 배당 표 자체가 없습니다. 화면은 이 155건만 달력에
  올리고, 나머지는 "작년 배당율" 폴백 목록으로 따로 보여줍니다(§11-5).

### 11-4. 지금 열려있는 일 (이 모듈) — §0-5 항목 7과 같은 목록

1. 🟢 **유니버스를 "전 상장사"로 교체할지 — 79종목 문제는 해소, 실제 실행만 남음
   (2026-08-24).** 지금 대상은 과거 배당 이력 종목뿐이라 2026년 신규 상장·신규 배당 회사가
   **조회조차 안 됩니다.** `data/kr_ticker_master.json`(4,034건, `type` 필드로 ETF 1,161건을
   정확히 걸러낼 수 있음)으로 바꾸면 2,873종목인데, 그중 79종목의 코드 형식 문제를 직접
   까본 결과 **79개가 아니라 성격이 다른 3그룹**이었습니다: 23개는 우선주(본주가 이미
   유니버스에 있어 데이터가 이미 수집돼 있었음 — §11-4 참고), 30개는 스팩(구조상 배당
   없음, 원래 빠지는 게 맞음), 26개만 진짜 신규상장 회사(삼성에피스홀딩스·대신밸류리츠
   등). ✅ **코드 형식 문제 자체는 오늘 해결**했습니다 — `normalize_stock_code()`를
   실제 KRX 6자리 영숫자 형식 하나만 정확히 추가로 받아들이도록 좁게 확장(기존 숫자
   경로는 완전히 그대로)했고, 그래도 DART 표에 직접 안 잡히는 경우를 위해 회사명 2차
   매칭(`build_corp_name_index()`, 정확히 일치할 때만)을 추가해 특정 79개가 아니라
   앞으로도 계속 통하는 구조적 해법으로 만들었습니다. **실제 확인된 사실**: 현재
   2,734종목 유니버스에서 UNMAPPED였던 55종목 전부가 "형식 오류로 DART에 아예 물어보지도
   못한" 경우였음 — 이번 수정만으로 다음 실행부터 55종목 전부 최소한 DART에 물어는 보게
   됩니다. 이미 끝낸 2,734종목을 다시 안 돌리는 **델타 실행 결과를 기존 파일에 병합하는
   도구도 완성**했습니다 — `merge_delta_output()` + CLI `--merge-delta`/
   `--force-merge`(오푸스 높음, 테스트 25개). 종목코드가 하나라도 겹치면 무조건 거부,
   같은 델타를 실수로 두 번 합치면 매니페스트(`dividend_kr_{year}_merge_log.json`)로
   걸러내며 `force=True`로도 겹침 자체는 못 넘어감(§0-1 — 어느 쪽 값이 맞는지 임의로 안
   고름), 실패 시 원본 파일은 절대 부분쓰기 안 됨까지 테스트로 확인. 유니버스 파일 자체는
   아직 `data/dividend_history_kr_2023_2025.json` 그대로이며, 이번 작업은 손대지 않았습니다.
   ✅ **델타 유니버스 파일 자체는 완성(2026-08-24)** — `data/kr_ticker_master.json`(전
   상장 2,873종목)과 기존 2,734종목 유니버스를 직접 대조해 `data/dividend_kr_2026_
   universe_delta.json`(신규 29종목)과 감사용 제외 목록(우선주 추정 110종목,
   `..._excluded_preferred.json`)을 만들었고, `load_universe()`/`load_universe_name_map()`
   실함수로 직접 파싱해 29종목·29개 이름 전부 정상 인식되는 것까지 확인했습니다.
   **중요한 발견**: 이 29종목 중 **23개(79%)가 리츠(REITs)**입니다(맥쿼리인프라·SK리츠·
   롯데리츠 등) — 기존 유니버스에는 리츠가 **단 하나도 없었습니다.** 배당이 사업모델의
   핵심인 자산군 전체가 통째로 빠져 있었다는 뜻이라, 단순 "79종목 문제" 이상의 무게가
   있는 발견입니다. 남은 건 **실제 델타 수집 실행(오너가 GitHub Actions로 트리거)** 뿐이며,
   지금 워크플로우(`collect_dividend_kr.yml`)는 전체 2,734종목 유니버스·`data/` out-dir을
   그대로 고정해 두고 있어, 델타만 따로 돌리려면 워크플로우 입력값 확장이나 별도
   워크플로우가 필요합니다(다음에 오너가 요청하면 진행).
2. ✅ **체크포인트 `run_key` 날짜 버그 — 완료(2026-08-24, 오푸스 높음).**
   `run_key`에서 실행 날짜를 빼고(`build_run_key()`로 분리) 대신 `load_checkpoint()`에
   `max_age_days=14`(기본값) 신선도 검사를 추가했습니다 — 하루 넘겨 재실행해도 정상
   이어받고, 2주 넘게 방치된 체크포인트만 "다른 실행"으로 간주해 새로 시작합니다.
2-1. ✅ **기존 산출물 덮어쓰기 방지 가드 — 완료(2026-08-24, 오푸스 높음).** 반대 방향
   구멍 — `latest.json`이 이미 있는데 체크포인트가 없거나 다른 실행조건이면 그냥
   덮어쓰던 것을 `--allow-overwrite` 없이는 거부하도록 막음(진짜 이어하기는 무관).
   부작용으로 이 모듈 자체 워크플로우(완주 때마다 체크포인트를 지움)가 다음 분기 갱신
   때 막힐 뻔한 것도 함께 발견해 `.github/workflows/collect_dividend_kr.yml`에
   `--allow-overwrite`를 반영(안전 — 이 워크플로우는 항상 같은 out-dir·universe만 씀).
3. ✅ **§0-3-3 — 완료(2026-08-24).** raw/가공 분리 ✅, 사용자 다운로드 ✅(`4b8f000`),
   **2개 출처 교차검증도 신설**(`build_kind_baseline_index()`/`check_cross_source()`,
   `--history-baseline` 옵트인 플래그, 기존 워크플로우는 안 건드림). DART 응답에 공짜로
   오는 전기·전전기 값을 2023~2025 KIND 기준선과 대조해 `cross_source_notes` 필드에만
   기록(값은 절대 안 고침, §0-1). **실 데이터 실행 결과**: 비교 가능한 971건 중
   522건(53.8%)에서 불일치 발견 — DPS 자체 불일치는 70건(3.9%)뿐이지만 그중 067900
   와이엔텍은 "주당배당금(원)" 칸에 총배당금액이 들어간 것으로 보이는 **진짜 데이터
   결함으로 의심됨**(단위검증(§2-4)은 라벨 표기만 보므로 못 잡는 종류 — 교차검증이 아니면
   몰랐을 것). ⚠️ **오너가 판단해야 할 것 2가지**: ① DPS 불일치 70건 중 36건은 "전기=
   year-1" 대신 "전기=year"로 놓고 비교하면 정확히 맞아떨어짐 — DART 전기/전전기와 KIND
   `fiscal_year`가 가리키는 회계기간이 일부 종목에서 다르게 해석될 가능성이 있어, 이
   522건 중 상당수가 "진짜 오류"가 아니라 "연도 매핑 방식 차이"일 수 있습니다. ② 배당성향
   불일치 943건(56.7%, p90 16.3%p)은 반올림 오차라기엔 너무 커서, DART(연결 우선)와
   KIND(자체 계산)가 애초에 서로 다른 값을 계산하고 있을 가능성이 있습니다. 지금은 "발견만
   하고 안 고친다"는 원칙대로 노트로만 남겼습니다 — 신뢰할 신호인지는 오너 확인 필요.
   ✅ **067900 와이엔텍 건, 오너가 DART 원문에서 직접 확인(2026-08-24).** DART
   정기공시 항목별 검색(`dart.fss.or.kr`) "6. 배당에 관한 사항 → 나. 주요배당지표" 표를
   직접 열어보니, 전기(제36기, 2025년 결산) 행의 "주당 현금배당금(원)" 칸에
   `1,809,965,900`이 그대로 들어 있고, 이는 같은 행 "현금배당금총액(백만원) 1,809"와
   거의 같은 숫자입니다(주당 배당금이 아니라 총액이 "주당" 칸에 잘못 들어간 것). 즉 이
   수집기의 버그가 아니라 **DART 공시 원본 자체의 표기 오류**로 확인됐습니다 — 교차검증이
   실제 데이터 결함을 정확히 짚어낸 사례.
4. ✅ **§2-4 단위 확인 — 완료(2026-08-24, `f25223d`)**. `se` 라벨의 단위 키워드(백만원/원/주)를
   필드가 기대하는 단위와 대조하는 `extract_unit_token()`/`check_unit_token()`을 신설, 불일치는
   새 필드 `unit_mismatch_notes`에만 기록(값 변환 없음). 실 raw 5,484줄 전수 재실행 결과
   불일치 **0건** — 지금 데이터는 이미 정상, 이 기능은 향후 회귀 방지용. 테스트 69 → 81개
   전부 통과. (요약 리포트에도 `records_with_unit_mismatch`/`records_with_cross_source_mismatch`
   집계를 추가해 앞으로는 전수 스캔 없이 한눈에 모니터링 가능합니다.)
5. 🟡 **`data/` 커밋 용량 약 19MB** — 실측: `dividend_kr_2026_latest.json` 6,796,583B /
   `dividend_kr_2026_raw.jsonl` 9,365,716B / `dart_corpcode_raw.zip` 3,598,077B.
   raw jsonl 은 **append-only** 라 재실행마다 커지고, git 은 매 커밋마다 전체 스냅샷을
   보관하므로 저장소가 계속 무거워집니다(§0-3-2 의 "GitHub 도 남의 서버" 관점). 분기당
   4회 갱신이라 당장 위험한 규모는 아니지만, **연 단위로 파일을 나누거나 raw 를 압축하는
   방안**을 정해두는 편이 좋습니다.
6. 🟡 **오너 실기기 확인 → 화면 재구성 완료, 재확인 대기 (2026-08-24).**
   `DIVIDEND_ENABLED=true`로 Render에 실제 배포하고 `/dividend`를 열어본 오너 피드백:
   "정보량이 너무 많은데... 열자마자 한숨부터 나온다 — 달력으로 보는 가장 간단한 구조를
   생각했는데, 더 단순하고 깔끔하게 표현하는 게 목표." 원인은 달력이 나오기 전에 색깔
   경고 배너 2개(`UNIVERSE_NOTICE`·`CALENDAR_DATE_NOTICE`)와 0.85rem 회색 문단 3개가
   먼저 쌓여 있던 구조였습니다. **오푸스 높음으로 재구성 완료** — 문구는 **한 글자도
   지우지 않고**(§0-1), "지금 수치를 오해하지 않으려면 먼저 봐야 하는 것"(데이터 기준
   시각·요약 카드 5장·정합성 경고·`CALENDAR_DATE_NOTICE`)만 달력 위에 남기고, 배경
   설명(수집 범위·수익률 산정 방식·우선주 표기 규칙·알려진 한계·원본 다운로드)은 달력
   **아래** 기본 접힘 패널(`_render_notice_panel()`) 하나로 모았습니다. 남긴 문단들의
   글자 크기도 새 CSS 클래스 `.vh-notice-text`(0.95rem·#cbd5e1·line-height 1.8)로 키움
   — 기존 `.vh-muted`는 다른 화면과 공유라 그대로 두고 배당 페이지 전용 클래스만 추가.
   검증: `py_compile` 클린, 기존 188개 테스트 전부 통과(무관), 실 데이터(확정 155건)로
   렌더 트리를 직접 돌려 정직성 문구 6개가 전부 화면에 남아있음을 확인,
   `CALENDAR_DATE_NOTICE`는 여전히 접이식 밖 상시 배너로 확인. **남은 건 오너가 재배포 후
   직접 다시 열어보고 마음에 드는지 확인하는 것** — 괜찮으면 `web/layout.py`의
   `DIVIDEND_MENU_ADMIN_ONLY`를 `False`로 바꿔 전체 공개(결투·성적표와 같은 3단계 절차).
7. ✅ **우선주(優先株) 현금배당 화면 노출 — 완료(2026-08-24, 오푸스 높음).** 이미 수집돼
   있었지만 한 번도 화면에 안 보이던 `dps_cash_preferred`(실제 20건)를 확정 종목
   테이블에 추가 — 보통주와 절대 안 섞이게 별도 줄(보라색)로 표시, 값이 없는 ~99%
   종목에는 아무 것도 안 붙어 기존 표시와 완전히 동일. 우선주 종류가 2개 이상이라
   대표값을 하나로 못 고른 경우(002790 아모레퍼시픽홀딩스, `[405.0, 667.0]`)는 하나로
   뭉개지 않고 전부 나열 + "대표값 미정" 배지. 확정/미확정 판정 기준(`status == "OK"
   and dps_cash_common is not None`)은 이번 작업으로 전혀 안 바뀜 — 우선주만 있고
   보통주가 없는 종목은 여전히 미확정(폴백) 목록에 남습니다.

### 11-5. 화면(`/dividend`) 설계 요약 (2026-08-24, 오푸스 높음으로 구현)

- **달력 날짜의 의미** — 각 레코드의 `stlm_dt`(그 보고서가 다루는 회계기간의 결산기준일)를
  씁니다. DART 정기보고서에는 실제 지급일·배당락일 정보가 아예 없어, "이 날짜 = 배당금
  받는 날"이 아니라는 사실을 달력 바로 위 상시 배너로 밝힙니다(툴팁 하나로 숨기지 않음).
- **"이번 해 배당 확정"의 판정 기준은 단 하나** — `status == "OK" and dps_cash_common is
  not None`. 이 기준을 만족하는 155건만 달력에 찍힙니다.
- **"작년 배당율입니다" 폴백** — 나머지 2,579종목은 `data/dividend_history_kr_2023_2025.json`
  에서 같은 종목의 **배당액이 실제로 있는 가장 최근 연도**(2025→2024→2023)를 찾아 보여주고,
  실제로 쓴 연도를 라벨에 명시합니다. 폴백 종목은 2026년 날짜가 없으므로 **달력 칸에는
  절대 넣지 않고** 별도 목록에 둡니다. 세 해 모두 없으면 "데이터 없음"(0원으로 지어내지
  않음).
- **유니버스 정직성** — "코스피·코스닥·코넥스 전 상장사"라고 말하지 않고, 이번 수집이
  2023~2025년 배당 이력 종목(2,734개)만 대상이었다는 사실과 상태별 건수(확정 155 / 미확정
  2,417 / 데이터없음 107 / 매핑실패 55)를 화면 상단에 그대로 노출합니다.
- **배당수익률** — DART 원문값(`cash_yield_common`) 그대로. 현재가 데이터가 없어 (DPS÷현재가)
  로 재계산할 수단 자체가 없고, 분기·반기보고서 수익률이 누적과 안 맞을 수 있다는
  `yield_reliability_note` 를 배지+툴팁으로 노출.
- **XSS·세션 격리** — DART/KIND가 준 문자열(회사명·사유·파싱메모 등)은 전부 `esc()`(23곳).
  로그인 불필요한 공개 화면이라 `web/auth.py` 접근 자체가 없고, 모듈 최상위 mutable 전역이
  0개라 `tests/test_web_session_isolation.py` 의 화이트리스트 등록도 불필요.
- **스테이징 게이트** — `web/layout.py` 의 `DIVIDEND_ENABLED`(기본 꺼짐)/
  `DIVIDEND_MENU_ADMIN_ONLY`(기본 관리자 전용). 결투/성적표와 같은 3단계 절차·같은 판정
  관례(정확히 "true"일 때만 켜짐).
- **검증**: `py_compile` 클린, 실 데이터로 순수 데이터가공 함수 직접 실행(달력 집계 155건
  == 수집기 요약 `ok_with_common_dps` 155 일치 확인), 악성 문자열(`<img onerror=...>` 등)로
  XSS 이스케이프 직접 실행 확인. ⚠️ nicegui가 없는 샌드박스에서 만들어서 실제 서버 화면
  모양(달력 격자 반응형 등)은 미확인 — 오너 실기기 확인 필요(§11-4 6번).

