# 프로젝트 현황판 (PROJECT_STATUS.md)

> **다음에 이 프로젝트를 다시 열 때는 (나 자신이든, 제미나이든, 다른 클로드 세션이든) 전체 파일을 처음부터 다 읽지 말고 이 문서부터 읽으세요.**
> 이 문서 하나로 "지금까지 뭘 했고, 지금 뭐가 문제고, 다음에 뭘 해야 하는지"를 알 수 있게 유지하는 게 목적입니다.
> 작업할 때마다 이 문서의 **최근 작업 로그**와 **지금 열려있는 일** 섹션을 업데이트해주세요.
> 완료된 작업의 상세 이력(번호별 전체 목록)은 `TASK_HISTORY.md`를 참고하세요. 이 문서는 요약만 남깁니다.

> 🛑 **오너 지시 (2026-08-10): 매크로(§4, 방공망 지표) 관련 작업은 여기 지시서(§4)에만 기록으로 남겨두고 지금부터 중단합니다.**
> §4에 남아있는 미완료 항목(FX_Swap_Point/Put_OTM_OI 실측 전환 등)을 **오너가 먼저 명시적으로 지시하지 않는 한** 먼저 나서서 조사·코딩·제안하지 마세요. "다음엔 뭘 해야 하나" 같은 질문에도 매크로를 기본 추천으로 꺼내지 마세요.
> 중단 직전 확인: `market_history.csv` 자동 누적 정상 작동 확인함(2026-08-10) — VKOSPI·선물베이시스 실측값 정상 기록 중, #72(공매도 2종 제외) 코드는 08-11부터 자동 수집에 반영됨. 그러니 중단해도 데이터 파이프라인 자체는 계속 건강하게 쌓입니다.

마지막 업데이트: 2026-08-12 — **✅ 리포트 모듈 배치 파이프라인 실검증 완료(#97).** #96 수정분 재실행에서 `PGRST125` 오류(원인: `SUPABASE_URL` 시크릿에 REST 경로가 포함된 "API URL"을 잘못 넣음 — 베이스 프로젝트 URL로 정정)를 한 번 더 넘긴 뒤, 벤치마크 수집·**보유 종목 8행/사용자 1명 조회**·**스냅샷 2행(KR+US) 저장**·파일 커밋까지 전 구간 실제 운영 확인 완료. §10-4 체크리스트 1~3번 끝, 남은 건 사이드바 배선(선택)과 스냅샷 누적 대기뿐. (직전 업데이트: **📈 리포트 모듈 첫 자동 실행 실패 버그 2건 수정(#96, 오푸스 높은 추론 강도).** 오너가 Secrets 등록 후 `Daily Report Snapshots` 를 dry_run 으로 돌렸더니 벤치마크가 두 지수 모두 0행으로 실패하고 커밋 단계가 exit 128 로 잡을 통째로 죽였습니다. ① 소스는 멀쩡했고 **우리 파싱이 응답 최상위 값만 훑은 게 원인** — 실제 행 목록은 한 겹 안쪽(`data.data`)에 있었습니다. 깊이 우선 재귀 탐색으로 바꾸고, 글자가 겹치는 quote 스키마에 속지 않도록 **날짜(`t`)가 있는 스키마만** 시세 행으로 인정하게 했습니다(공급자 이름은 조건으로 쓰지 않음 — SPY=tiingo, **ONEQ=spg** 로 서로 다름). 종목 확인도 실제로는 없는 ETF 이름 대신 응답 `symbol` 로 교체. ② 파일이 없을 때 `git add`가 죽던 워크플로우는 **있으면만 add** 로 수정(커밋 대상은 여전히 `data/us_index_history.json` 하나뿐). 실응답 캡처 픽스처 2종을 신설해 재검증 — **SPY·ONEQ 각 125행(2026-02-11~2026-08-11) 정상 추출**, 오프라인 255→270체크, 기존 6개 스위트 회귀 없음. ⚠️ **오너의 워크플로우 재실행 확인 필요**(§10-1). (직전 업데이트: **📈 리포트 모듈(5번째 모듈) v1 신규 구축 — 코드 완성 + 오프라인 검증까지(#95, 오푸스 높은 추론 강도). 자세한 진행 상황은 아래 §10 신설 섹션 참고.** "내 성적표" 밑에 붙는 완전히 별도 모듈로, 매일 자동으로 사용자별 평가금액 스냅샷을 쌓고(GitHub Actions + service_role, 앱에는 절대 안 들어감) 그 표 하나에서 일간/주간/월간/분기/반기/연간 리포트를 전부 계산합니다. 벤치마크 소스는 **실응답으로 확인**: 한국은 `market_history.csv`의 코스피 종가를 읽기 전용 재사용, 미국은 stockanalysis.com **ETF 프록시**(SPY/ONEQ)의 과거주가 엔드포인트(요청 1회로 약 6개월치 일별 종가, #92 devalue 디코더 재사용) — **FRED 는 지수 실값을 주지만 S&P DJI 재배포 제한 때문에 KRX API와 같은 기준으로 일부러 미채택**. 신규 파일 6개(SQL/수집기/데이터계층/화면/워크플로우/테스트), **기존 파일 한 줄도 수정 안 함**, 신규 오프라인 255체크 + 기존 6개 스위트 회귀 없음. ⚠️ 실제 Supabase 연결·실크롤링은 오너가 SQL 실행 + GitHub Actions Secrets 등록 후 진행(§10-4). (직전 업데이트: **✅ #92·#93 실검증 완료 + KORU 현재가 표시 확인.** 오너가 force 재크롤링 후 원격 데이터 직접 확인: `us_all_market_prices.json`(주식 5,607종목)·`us_all_etf_prices.json`(ETF 5,567종목) 둘 다 같은 회차(2026-08-12 16:58 KST)에 정상 수집(pages_fetched 1, failed 0, blocked false). 화면에서도 보유 ETF **KORU 현재가 $17.64로 정상 표시**(기존 "현재가 없음"에서 교체) — §9-4는 이제 모바일 최종 확인(최후 공정)만 남음. (직전 업데이트: **📊 "내 성적표" 미국 ETF 현재가 수집기 신설(#93, 오너 지시로 "ETF 제외" 원안을 뒤집음).** 오너 지시: "그냥 ETF까지도 넓히자 … ETF로 투자하는 비율도 소수라고 할 수 없을 만큼 많기 때문에 무시할 수는 없어"(실제 계기: 보유 ETF **KORU**가 계속 "현재가 없음"). 같은 사이트의 **ETF 스크리너** 데이터 엔드포인트를 실제로 받아 확인한 결과 주식 스크리너와 **포맷도 행 키(s/n/price)도 동일**해서 devalue 디코더를 그대로 재사용했고, 역시 **요청 1회로 5,480 ETF**가 옵니다. 파일은 소스가 둘이라 **분리**(`data/us_all_etf_prices.json`) 후 화면에서 합칩니다. ⚠️ 뒤집은 건 **현재가 범위뿐** — 밸류에이션(PEGY/퀀트)은 ETF에 EPS·ROE가 없어 §0-1대로 여전히 상위 550 보통주 전용. 오프라인 462→496체크.) (그 전 업데이트: **미국 상장 전 종목 현재가 수집기 신설(#92, §9-4 1번의 "같이 생각해볼 것" 해결).** 상위 550 밖 미국 종목이 "현재가 없음"으로만 뜨던 문제를 한국(#84)과 같은 원리로 해결 — stockanalysis.com 스크리너의 SvelteKit 데이터 엔드포인트를 **요청 1회**로 불러 미국 상장 5,607종목의 티커·회사명·현재가를 받습니다(응답이 devalue 직렬화 포맷이라 전용 디코더를 구현, 실응답 원문으로 검증). 구글 파이낸스는 전 종목 목록 구조가 사라져 폐기. `data/us_all_market_prices.json` 신설, 오프라인 422→462체크. ⚠️ 실제 자동 수집 로그 확인은 다음 미국주식 워크플로우 실행 후 필요.) (그 전 업데이트: **#84(전 종목 종가 수집기) 실검증 완료(§9-4 1번).** GitHub Actions 로그 대신 로컬로 pull된 `data/kr_all_market_prices.json`(코스피 2,477+코스닥 1,819=4,296건, 가격 결측 0건)을 직접 열어, 오너가 실제 화면에서 입력·확인했던 종목 가격과 정확히 일치함을 대조 확인. §9-4는 이제 모바일 최종 확인(최후 공정)만 남음.) (그 전 업데이트: 순서 재조정(오너 지시, 코드 변경 없음, §9-4·9-5).) 모바일 실기기 확인을 최후 공정(최종 검토)으로 이동, Confirm email은 이미 켜져있음을 확인해 완료 처리, #84(전 종목 종가) 확인과 함께 미국 550종목 밖 가격 확장 방안을 같이 고민하기로 함. v2(OCR)·v3(리포트) 순서를 바꿔 리포트를 먼저 착수하기로 했고, 리포트는 "내 성적표"와 별도 모듈로 신설 예정. (같은 날 손실 종목 안내 캡션 정리(#89), 가독성 3건(#88)도 처리됨.) "VS" 배너가 이상하게 보이던 건 글꼴 문제가 아니라 "$"가 마크다운에서 LaTeX 수식으로 오인되던 렌더링 버그였음(원화는 "$"가 없어 이 문제가 없었던 게 "한국보다 미국이 떨어진다"던 신고의 실제 원인). 미국 종목은 이제 공개 미국주식 화면과 같은 한글 표기를 씀. (같은 날 미국 종목 빠른 검색 티커 매칭 버그(#87), 원형차트 범례 종목코드 제거(#86)도 처리됨. 종목 추가 버튼 무반응 버그(#85)는 오너가 배포 후 실제 화면에서 여러 종목 연속 추가로 정상 동작 확인 완료(2026-08-12) — ✅ 해결됨. 코스피+코스닥 전 종목 종가 수집기(#84)는 아직 실제 네이버 응답으로는 미검증 — 다음 GitHub Actions 자동 수집 후 로그 확인 필요, §9-4. #83 전체 상장종목 마스터 목록은 실검증 완료.))))

> ⚠️ **미해결 — 오너가 배포된 실제 화면에서 #79(정렬 필터)·#80(빨강/파랑 통일)이 반영 안 된 걸
> 확인함(2026-08-11).** 코드는 로컬에 있고 `git diff`가 깨끗해서(커밋됨) 코드 자체 문제는
> 아닌 것으로 보이나, 실제 서비스에 배포됐는지 확인이 안 됐습니다. **다음에 반드시**: ①
> `git log`로 이 커밋들이 진짜 push까지 됐는지 확인, ② Streamlit Cloud에서 완전
> Reboot(과거 `KeyError: 'utils.scoring'` 캐시 버그와 같은 원인일 가능성 — TASK_HISTORY 참고),
> ③ 그래도 안 되면 원인 재조사. §9-4에도 기록.

같은 날 앞서 #79에서 수익률 표만 국내 관례(빨강=오름/파랑=내림)로 바꿔놨는데 "평균매입가 vs
현재가" 배너·상단 평가손익 합계는 여전히 해외 관례(초록/빨강)라 화면 안에 두 관례가 섞여 있던
걸 오너에게 알렸고, "해외관례보다는 국내관례로 가자"는 확정 지시로 배너·요약 지표까지 전부
빨강/파랑으로 맞췄습니다(#80). 그 전엔 수익률 색상+표 정렬 신설(#79), CSS로 안 고쳐지던 버튼
정렬을 Material 아이콘으로 근본 교체(#78), 종목 입력 통합 단일 칸 재설계(#76), 종목 삭제
인라인 버튼화(#75). 테스트 187→211→214→228→238→240→**243체크**, 기존 5개 스위트 회귀 없음.
**여전히 스테이징(기본 숨김)** — 공개 전환은 아직 승인 전. 매크로(§4)는 2026-08-10 오너 지시로
중단 상태 유지(위 배너 참고, 공매도 2종 재분류 #72까지 완료된 상태에서 멈춰있음).

> 더 오래된 일자별 업데이트 로그는 `TASK_HISTORY.md`에 번호 순으로 전부 남아있습니다(#1~#84).
> 이 문서는 "지금 상태"만 요약해서 유지합니다 — 과거 항목을 여기 계속 쌓지 않습니다(2026-08-09 정리).

---

## 1. 이 프로젝트는 무엇인가

"잘 보면 보이는 손 (The Visible Hand)" — 코스피 시장 위험도 + 코스피 시가총액 상위 200개 종목 PEGY(가치) 점수를 매일 자동으로 계산해서 보여주는 대시보드.

> ⚠️ **명칭 주의**: 여기서 "200개 종목"은 **KRX 공식 "코스피 200 지수" 편입종목이 아니라, 순수 시가총액 순위 상위 200개**입니다(2026-08-06 오너 확인 — 공식 지수 전환은 하지 않기로 함). 공식 지수는 유동주식 시총·업종 안배·유동성 심사를 거쳐 연 2회만 리밸런싱되지만, 이 프로젝트는 매일 시가총액만으로 순위를 매깁니다. 화면·다운로드 파일의 사용자 노출 문구는 모두 "시가총액 상위 200"으로 표기하도록 통일함(파일명 `collector_kospi200.py`/`kospi200_pegy_latest.json` 등 내부 이름은 하위 호환을 위해 유지).

- 실제 서비스 주소: `visiblehand.co.kr` (커스텀 도메인, `index.html`이 아래 Streamlit 앱을 iframe으로 감싸는 구조)
- 실제 앱이 돌아가는 곳: **Streamlit Community Cloud** (`https://visible-hand-dashboard-2vmzz6tk63wsac7n345ord.streamlit.app/`)
- 코드 저장소: `github.com/moonbear135/visible-hand-dashboard` (public repo)
- 데이터 자동 수집: **깃허브 액션(GitHub Actions)** — PC를 꺼놔도 매일 자동으로 돈다
- 공개 화면은 **"사실 이 가격이에요"(코스피 PEGY 밸류에이션, 기본 화면)** 와 **"🇺🇸 미국 주식은 이가격"(2026-08-08 공개 전환, 사이드바 상단 메뉴)** 두 개. "잘 보면 보이는 손"(매크로 방공망)은 관리자 전용으로 전환됨 (2026-08-05, 아래 참고)

## 2. 파일 구조 한눈에 보기 (다시 다 안 읽어도 되게)

| 파일/폴더 | 역할 |
|---|---|
| `scrape_daily.py` | 코스피 지수/환율/수급 데이터 수집 → `market_history.csv`에 누적. 백필 모드 지원 |
| `collector_kospi200.py` | 코스피 시가총액 상위 200개 종목 PEGY 데이터 수집 → `data/*.json`. 상장주식수(FDR 우선), Trailing EPS 계산값 예외, 그레이엄 넘버 등이 여기 있음 |
| `.github/workflows/scrape.yml` | **코스피 자동화 워크플로우.** 매일 평일 KST 16:05 실행 |
| `.github/workflows/scrape_us.yml` | **🇺🇸 미국주식 자동화 워크플로우 (2026-08-07 신설).** 서머타임 대응으로 cron 2개(20:35/21:35 UTC)를 걸고, 수집기의 `--skip-if-not-ready` 사전 점검이 하루 한 번만 실제 수집되게 걸러냄 |
| `.github/workflows/keep_awake.yml` | (2026-08-09 신설) Streamlit 무료 호스팅이 **12시간 무방문 시 앱을 재우는 정책** 대응 — 8시간마다 실제 Streamlit 앱 URL(커스텀 도메인 아님)을 방문해 타이머 리셋. 데이터 수집과 무관, 실패해도 영향 없음 |
| `market_history.csv` | 날짜별 종합 위험 점수 이력 |
| `data/kospi200_pegy_latest.json`, `data/pegy_summary_history.json` | 종목별 최신 데이터 + **시장 전체** 요약 이력 (⚠️ `*summary_history.json` 은 중앙값 요약이지 종목별 이력이 아닙니다 — 종목별 이력은 2026-08-09 신설된 `*_stock_history.csv`) |
| `views/pegy_view.py` | **공개** PEGY 밸류에이션 화면. Forward 데이터 없으면 해당 섹션만 마스킹 처리 |
| `views/macro_view.py` | **관리자 전용** 매크로 방공망 화면 (2026-08-05부로 비공개 전환) |
| `views/admin_view.py` | 관리자 인증 + 관리자 전용 메뉴(매크로 화면 진입 체크박스 등) |
| `utils/guardrail.py` | 종목 차단/마스킹 판정 (필수 조건은 이제 `price`뿐, Forward 결측은 차단 아님) |
| `utils/scoring.py` | 퀀트 스코어링. 만점(score_max)은 종목마다 실제 산출 가능한 항목만 합산(고정 100 아님, 배지엔 %도 함께 표시). 역성장/적자·PER이상·극단고평가 하드컷오프는 오늘 수집분 전체 분포 대비 z-score로 심각도 스케일링 |
| `utils/db.py` / `utils/gdrive_helper.py` | DB 저장, 구글드라이브 백업(백업 폴더ID는 `GDRIVE_TARGET_FOLDER_ID` 환경변수로 오버라이드 가능) |
| `collector_us_stocks.py` | (2026-08-06 신설) 🇺🇸 미국주식 수집기 **기초틀**. 유니버스 CSV 수집·필터(550개), 상품유형 분류, 히스테리시스(550/600), stockanalysis.com 통계 페이지 파서, `_now_et()`/ET 세션 계산, 샘플 프로토타입 CLI. **아직 전수 수집·스코어링·화면은 없음** |
| `utils/scoring_us.py` | (2026-08-07 신설) 🇺🇸 미국주식 전용 퀀트 스코어링 + 파생 밸류에이션(PEGY/목표가/그레이엄) + 미국판 가드레일. 배점: PEGY 35 + ROE·ROIC 30 + 주주환원 20 + F-Score 10 + 베타 5 |
| `views/us_stocks_view.py` | (2026-08-07 신설) 🇺🇸 미국주식 화면. **공개 화면**(2026-08-08 오너 승인으로 §0-3-6 스테이징 종료, 베타 배너 제거). 상단 지수 3종 + 코스피와 동일 카드 구조 + USD 단독 표기 + 30개 페이지네이션 |
| `utils/company_names_kr.py` | (2026-08-07 신설) 미국 종목 한글 표기. 정식 한글명 사전(티커 키, 오너가 직접 수정) 우선 + 규칙 기반 음역 폴백(완벽하지 않음을 코드에 명시) |
| `tests/test_us_scoring.py` | (2026-08-07 신설) 미국주식 스코어링·전수수집 배선 오프라인 검증 78건 (실데이터 12종목 + 합성 극단케이스 + end-to-end + 화면 렌더링) |
| `utils/constants_us.py` | (2026-08-06 신설) 미국주식 전용 상수. 밸류에이션 임계값은 전부 🚧잠정값+TODO🚧 (코스피 값을 그대로 쓰지 않음) |
| `tests/test_us_stocks.py` | (2026-08-06 신설) 미국주식 기초틀 오프라인 검증(네트워크 불필요). 실제 CSV 종목명·실제 페이지 원문 라벨로 74개 체크 |
| `tests/test_stock_history.py` | (2026-08-09 신설) 종목별 시계열 이력·다운로드 오프라인 검증 98건. 필드 스펙(내부 필드 유출 0/한국어 라벨), 상태 가드, **실제 수집기 함수를 직접 호출한 실패·차단 시나리오**, 같은 날 중복 방지, 실데이터 end-to-end(BOM·pandas 재파싱·종목코드 0 보존) |
| `utils/constants.py` | (2026-08-06 신설) 프로젝트 전역 임계값·가중치 단일 출처. PER 오염 판정, 목표가 캡, ROE 기준선, 매크로 `RISK_WEIGHTS`/`INVESTOR_WEIGHTS` 등 |
| `utils/macro_scoring.py` | (2026-08-06 신설) 매크로 종합점수 계산(z-score 정규화+시그모이드 변환+동시충격 증폭기) 단일 모듈. `scrape_daily.py`(저장)와 `views/macro_view.py`(표시)가 공용으로 호출. 2026-08-10(#68) **실측 지표 정규화 함수**(`measured_downside_risk` 등) 추가, (#70) 값이 클수록 위험인 지표용 `measured_upside_risk` + 이력 컬럼 분포 생성기 `history_column_population` 추가 |
| `utils/krx_openapi.py` | (2026-08-10 신설, TASK_HISTORY #70) **KRX OPEN API 최소 클라이언트.** VKOSPI 지수값 / KOSPI200 지수 종가 / KOSPI200 선물 근월물 종가만 가져와 매크로 2지표에 씁니다. 인증키는 환경변수 `KRX_OPENAPI_KEY`에서만 읽고 **HTTP 헤더 `AUTH_KEY`로만** 전달(URL에 실으면 서버 로그에 남음). 외부 래퍼 라이브러리 **미채택**(근거는 파일 상단 주석·#70). 실패는 전부 `None` → 그 지표만 배점 제외. **⚠️ 실서버 응답으로 검증된 적 없음** — 지수명·선물 근월물 식별이 미확인이라, 못 찾으면 응답에 있던 이름을 전부 로그로 출력합니다 |
| `app.py` / `visiblehand.py` | 앱 진입점. 공개 메뉴는 코스피 PEGY 화면(기본) + 🇺🇸 미국주식(사이드바 상단 체크박스, 2026-08-08 공개 전환) 두 개. 매크로는 관리자 인증 후에만 |
| `utils/scheduler.py` | 앱 내장 스케줄러(기본 비활성화, `ENABLE_INAPP_SCHEDULER=1`일 때만). 정식 경로는 GitHub Actions. (구버전 레거시 `scheduler.py`/`run_scheduler.bat`는 2026-08-06 삭제됨) |
| `utils/stock_history.py` | (2026-08-09 신설) **종목별 시계열 이력의 단일 출처.** 다운로드에 내보낼 필드 목록 + 한국어 라벨(`KOSPI_HISTORY_FIELDS` 26개 / `US_HISTORY_FIELDS` 40개 — 카드에 실제로 보이는 재무 지표만, 내부 진단·색상 필드 전부 제외) + 이력 append/조회. `record_daily_history()` 가 수집 상태를 보고 **SUCCESS/DEGRADED 일 때만** 기록하고 같은 날짜는 중복 없이 교체. streamlit 미의존 |
| `data/kospi200_stock_history.csv`, `data/us_stocks_history.csv` | (2026-08-09부터 생성) **종목별 날짜 이력**(하루 한 종목 = 한 줄). 컬럼명은 영문 키(라벨 문구가 바뀌어도 과거 행이 안 깨지게), 인코딩 `utf-8-sig`. ⚠️ **과거 소급 없음 — 도입 후 첫 수집분부터 쌓입니다.** 워크플로우의 `git add data/` 범위에 자동 포함 |
| `utils/stock_export.py` | (2026-08-08 신설 → 2026-08-09 재작성) "종목별 데이터 다운로드" 내보내기 모듈. 이력 행 목록 → **날짜=행 / 지표=열** CSV(한국어 헤더 · **UTF-8 BOM**, 엑셀 한글 깨짐 방지) / JSON(`ensure_ascii=False`, 숫자 컬럼만 숫자로 복원해 종목코드 `005930` 이 안 깨짐) + 파일명 안전화(`BRK/B`→`BRK_B`). streamlit 미의존 순수 함수라 코스피·미국 양쪽이 같은 형식을 공유 |
| `ENGINEERING_SPEC.md` | 코딩 원칙 문서. **하드코딩·더미 데이터 금지가 최우선 규칙.** §0-3(절대 준수 메뉴얼: 후행지표 전용, 크롤링 매너 등), 예시2-보충(계산값 허용 예외), 예시2-보충2(섹션별 마스킹 원칙)까지 포함 |
| `AUDIT_REPORT.md` | 2026-08-05 오푸스 코드베이스 감사 보고서 (13개 파일, 50여 건) |
| `MACRO_REDESIGN_PROPOSAL.md` | (2026-08-09 신설, TASK_HISTORY #66) 매크로 14개 위험 지표의 실측 소스 조사 결과와 재설계 제안. 지표별 A/B/C 판정 · 소스/필드/지연(T+n)/난이도 · 가중치 재분배안 · 구현 순서. **§5 구현순서 1번(#68, 실측 2지표)·2번(#69, C 6개 제외+가중치 재분배)·3·4번(#70, VKOSPI·선물 베이시스, 실서버 검증 완료)·6번(#72, 공매도 2종을 실측 불가로 재분류 — 오너 결정 옵션②)까지 반영됨. 남은 건 5번(SMBS 스왑포인트)과 `Put_OTM_OI`. §4-3 가중치 재설계는 미적용(활성 6개 전부 실측 전환 후)** |
| `collector_us_indices.py` | (2026-08-12 신설, #95 / 파싱 버그 수정 #96) 📈 리포트용 **미국 벤치마크 일별 종가** 수집기. stockanalysis.com 의 ETF 프록시(SPY=S&P500, ONEQ=나스닥종합) 과거주가 엔드포인트를 요청 1회씩 불러 `data/us_index_history.json` 에 누적(요청 1회당 125행 확인). ⚠️ 지수 포인트가 아니라 **ETF 종가**(키 이름에 PROXY 명시). devalue 디코더·HTTP 헬퍼는 `collector_us_stocks.py` 에서 import 재사용. 행 탐색은 **깊이 우선 재귀 + 날짜(`t`) 필수** — 최상위만 훑던 #95 버전은 실응답에서 0행이었음 |
| `utils/report_db.py` | (2026-08-12 신설, #95) 📈 리포트 데이터 계층. 기간(일/주/월/분기/반기/연) 경계 계산 · 스냅샷 행 생성 · 기간 집계와 데이터 부족 판정 · 벤치마크 기간 수익률 · Supabase 접근. **저장소에서 유일하게 `service_role` 키를 쓰는 파일**(환경변수에서만 읽고 streamlit 미import) |
| `views/report_view.py` | (2026-08-12 신설, #95) 📈 리포트 화면(스테이징, 기본 숨김 `REPORT_ENABLED`). 아직 `visiblehand.py` 에 배선하지 않음(오너 승인 후) |
| `sql/report_schema.sql` | (2026-08-12 신설, #95) `portfolio_daily_snapshots` 테이블 + **RLS select 정책 1개**(사용자는 읽기만, 쓰기는 배치 전용). 오너가 Supabase SQL Editor 에서 1회 실행 |
| `.github/workflows/scrape_report_snapshots.yml` | (2026-08-12 신설, #95) 평일 23:20 UTC — 미국 벤치마크 수집 → 사용자별 스냅샷 적재(Supabase) → `data/us_index_history.json` 커밋 |
| `data/us_index_history.json` | (#95, **아직 실제 수집 미실행**) 미국 벤치마크(SPY/ONEQ) 일별 종가 누적 파일. 공개 데이터라 기존 `data/` 관례대로 GitHub 에 커밋 |
| `tests/test_report.py` | (2026-08-12 신설, #95 / #96 에서 실응답 픽스처로 교체) 리포트 모듈 오프라인 검증 **270체크**(기간 경계·데이터 부족 판정·스냅샷 생성·벤치마크·**실제 응답 원문**으로 수집기 실행·가짜 Supabase 배선·범위 확인) |
| `TASK_HISTORY.md` | Cowork 작업 목록 위젯에서 정리(삭제)한 완료 항목 전체 이력 |
| `credentials.json` 등 | 구글 API 비밀키. `.gitignore` 등록되어 git에는 안 올라감 (확인 완료) |

## 3. 최근 작업 로그 (요약, 최신순 — 2026-08-09 정리, 상세는 전부 TASK_HISTORY.md에 있음)

- **2026-08-10** — 매크로 재설계 1단계(#68, 실측 2지표를 과거 분포 대비 정규화) → 2단계(#69, 실측 불가 6개를 점수에서 제외하고 4개는 "공부용 참고" 섹션으로 분리·2개는 개념 중복이라 완전 삭제, 남은 8개 가중치 비례 재분배로 합계 100.00) → 3·4단계(#70, `VKOSPI_Skew`·`Synthetic_Futures`를 KRX OPEN API 실측으로 교체 — `utils/krx_openapi.py` 신설, 오너 Actions 수동 실행으로 **실서버 검증 완료**) → #71(실서버 검증 정리 + ECOS 스왑레이트 조사, 코드 미수정) → **6단계(#72, 공매도 2종을 "실측 불가"로 재분류 — 점수 제외 + 공부용 참고 섹션 이동 + 가중치 재분배로 지표 8개 → 6개)**. Streamlit 잠들기 방지 워크플로우 실전 검증(#67).
- **2026-08-09** — 매크로 14개 지표 실데이터 연결 조사 + 재설계 제안서 작성(#66, 코드 미수정 · `MACRO_REDESIGN_PROPOSAL.md` 신설). 종목별 시계열 이력 신설(#64) + UI 폴리시 6건(#65: 국기 이모지 깨짐, 툴팁 오버플로우, 안내박스 흐림 2건, 코스피 지수 가독성, 신선도 경고 관리자 전용화, 미국 화면 학습용 안내 박스).
- **2026-08-08** — 미국주식 페이지 공개 전환(#61), 사이드바 대분류/중분류 라디오 개편(#62), 종목별 데이터 다운로드 도구 신설(#63, 오너 피드백으로 #64에서 시계열로 재설계).
- **2026-08-07** — 미국주식 550종목 전수 수집 성공, 임계값 재캘리브레이션(#55), 한글 음역 품질 개선(#56), GitHub Actions 자동화 연결(#60), 프리셋 필터 버그 수정(#57), BPS 목표가 바닥값 도입(#58).
- **2026-08-06** — 미국주식 모듈 기초틀 구축, 타임존(UTC/KST) 버그 전면 수정, `AUDIT_REPORT_V2.md` 27건 처리 완료, 퀀트 스코어 심각도 세분화(z-score 기반), KOSPI 200 히스테리시스 버퍼(200/230) 도입.
- **2026-08-05 이전** — PEGY 화면 초기 구축, 매크로 방공망 관리자 전용 전환, 배포 크래시 2건 대응, 상장주식수 파싱 오류 수정. (TASK_HISTORY #1~18)

> 각 항목이 왜 필요했는지, 어떻게 검증했는지, 어느 파일을 고쳤는지의 전체 서술은 `TASK_HISTORY.md` #1~#65에 번호 순으로 전부 남아있습니다. 이 섹션은 날짜 감각만 잡는 용도입니다 — 여기 계속 새 문단을 쌓지 말고, 새 작업은 한 줄 요약만 맨 위에 추가하세요.

모든 항목은 `git log origin/main..HEAD`가 비어있음을 기준으로 **전부 깃허브에 푸시 완료된 상태**입니다.

## 4. 지금 열려있는 일 (다음에 반드시 확인/처리할 것)

- **🔴 오너 액션 1건 — "내 성적표"·"리포트" 공개 전환 마지막 단계 (2026-08-13, TASK_HISTORY #106).**
  두 모듈 공개는 오너가 2026-08-12 최종 승인했고, 공개 전 보안 최종 점검·사이드바 레이아웃 정리까지
  끝났습니다. **코드 쪽에 남은 일 없음.** 남은 건 **Streamlit Cloud → 앱 → Settings → Secrets**
  (⚠️ GitHub Actions Secrets 아님)에 아래 2줄을 **추가**하고 재배포하는 것뿐입니다.
  ```toml
  SCORECARD_ENABLED = "1"
  REPORT_ENABLED = "1"
  ```
  기존 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 는 그대로 두세요. 되돌리려면 두 줄을 지우거나 `"0"` 으로
  바꾸면 즉시 다시 숨겨집니다. **Supabase 쪽에 추가로 실행할 SQL/ALTER 문은 없습니다**(RLS 재점검
  결과 고칠 것이 없었음). 자세한 점검 결과는 §9-6.
- **✅ 종목별 시계열 이력 검증 완료 (2026-08-09, TASK_HISTORY #64).** 코스피 200종목·미국 548종목 둘 다 첫 기록 확인.
- **🟡 진행 중 — 매크로 14개 위험 지표 재설계 (TASK_HISTORY #66 제안서 기반). 조사 결과 A 3개 / B 5개 / C(제거 권고) 6개** (`MACRO_REDESIGN_PROPOSAL.md` §5에 구현 순서).
    - **✅ 1단계 완료 (2026-08-10, TASK_HISTORY #68)** — `KOSPI_5D_Return`·`Stock_Net_Sell` 2개를 임의 선형식(`0.5 - 2.5×수익률`, `0.5 ± 0.3`)에서 **실측 원값의 과거 분포 대비 z-score + ±3σ 윈저라이즈**로 교체(`utils/macro_scoring.py`에 공용 함수 신설, `tests/test_macro_scoring.py` 46체크 통과). 이제 순매도 **금액**이 점수에 반영됩니다. `RISK_WEIGHTS`·나머지 12개 지표는 미변경.
      **⚠️ 다음 `scrape.yml` 자동 크롤링(평일 KST 16:05)이 돌아야 새 값이 CSV에 쌓입니다** — 과거 행은 옛 계산값 그대로(기록 개변 금지). 순매수 정규화는 이력 20행이 모일 때까지 중립 0.5로 나옵니다(표본 부족 시 지어내지 않음).
    - **✅ 2단계 완료 (2026-08-10, TASK_HISTORY #69)** — C 판정 6개(가중치 합 40.0)를 점수 계산에서 제외해 **지표가 14개 → 8개**가 됐습니다. `constants.py` 키 삭제뿐 아니라 `scrape_daily.py`/`macro_view.py`의 **계산식과 `market_scores` 항목까지 함께 제거**(믿지 않는 값을 계속 계산해 CSV에 남기지 않기 위함). `market_history.csv`의 기존 컬럼·과거 값은 **보존**하고 앞으로만 안 씁니다.
        - 그중 4개(`ELS_KnockIn`·`NDF_Night_Rate`·`Futures_Net_Sell`·`Non_Arbitrage_Ratio`)는 실제 시장에서 쓰이는 정식 개념이라 화면의 **"📚 공부용 참고 — 지금은 다루지 않는 지표"** 섹션으로 옮겨, 필요한 데이터·공부 방법(구체 검색어·출처)·데이터가 생겼을 때의 **가중치 참고 범위(확정 아님)** 를 설명 텍스트로만 남겼습니다. 값은 하나도 계산하지 않습니다.
        - 나머지 2개는 **완전 삭제**: `Foreign_Broker_Dump`(실측인 `Stock_Net_Sell`의 외국인 항목과 같은 숫자를 이름만 바꿔 재사용), `Put_Buy_Simple`(활성 지표 `Put_OTM_OI`와 같은 '풋옵션 수요' 개념). 삭제 이유는 화면에 표시합니다.
        - 가중치는 **§4-2 안 (가) 기계적 비례 재분배**: 남은 8개 합 62.0 × 100/62 → 합계 **100.00**(반올림 잔여 0.01은 유일하게 실측 검증된 `KOSPI_5D_Return`에 배정). **지표 간 상대 비중은 개정 전과 동일**합니다. §4-3(안 나)은 나머지 지표가 아직 프록시라 **적용하지 않았고**, 3~6단계 실측 연결이 끝난 뒤 한 번에 적용할 예정입니다.
        - **⚠️ 이 변경도 다음 `scrape.yml` 자동 크롤링(평일 KST 16:05)부터 CSV에 반영됩니다.** 화면 표는 즉시 8개로 줄어듭니다.
    - **✅ 3·4단계 완료 + 실서버 검증 완료 (2026-08-10, TASK_HISTORY #70).** `VKOSPI_Skew`·`Synthetic_Futures` 2개를 KRX OPEN API 실측으로 교체. 실측 지표가 **2개 → 4개**(8개 중). `utils/krx_openapi.py` 신설(외부 라이브러리 미채택 — 근거는 #70), `RISK_WEIGHTS`는 **또 미변경**, 테스트 241체크 전부 통과.
        - **실서버 검증 완료(오너, 2026-08-10 Actions 수동 실행)**: 1회차는 `git push`가 rejected 상태였던 걸 모르고 실행해서 구코드로 돌았음(로그에 KRX 관련 줄 0개로 원인 특정) → `git pull`/`push` 재정리 후 2회차 성공. 로그: `KRX 기준일=2026-08-07`, `KOSPI200 종가=974.73`(지수명 정확일치), **`VKOSPI=75.59`**(지수명 정확일치 '코스피 200 변동성지수'), **선물 근월물 자동 선택**(`코스피200F 202609`, 거래량 최대 규칙으로 후보 7종목 중 선택), **선물 베이시스=+4.02**(교차검증 통과). VKOSPI 지수명·선물 근월물 식별 로직 **둘 다 실제로 작동 확인됨.**
        - **VKOSPI=75.59 값 타당성도 뉴스로 교차검증**: 2026-07-30 레버리지 규제 이슈로 VKOSPI 86.18까지 급등 → 08-03 69.53 → 08-07 75.59 흐름과 일치, 이상치 아님(실측 신뢰도 상승).
        - `Put_OTM_OI`는 이번에도 손대지 않았습니다(옵션 API에 행사가격 필드가 없어 OTM 구분 불가 — 옵션 API 호출조차 안 함).
        - **부트스트랩 정상 진행 중**: 이력 20행이 쌓일 때까지 두 지표 모두 중립(0.5)로 나옵니다(현재 6행) — 약 2주 뒤부터 실제 정규화 값이 나오기 시작합니다. 버그 아님.
    - **⏸️ 5단계 — `FX_Swap_Point`: 오너가 보류 결정(2026-08-10, 급하지 않음/작업 규모 대비 실익 낮음).** ECOS 경로는 조사 완료(#71) — 외환스왑레이트/포인트 전용 통계표를 찾지 못해 대안 아님으로 결론. SMBS 경로도 조사(URL 확인: `smbs.biz/Exchange/FxSwap.jsp` 등 통화별 페이지, 문의처 `appdev@smbs.biz` 발견)했지만, 조회 결과 표가 자바스크립트로 채워지는 구조라 정적 페이지 가져오기로는 실제 데이터 엔드포인트를 찾지 못했고, 이걸 확인하려면 브라우저 개발자도구(Network 탭)가 필요합니다. **오너가 "일이 너무 커진다"며 지금은 넘어가기로 결정** — 나중에 여유 있을 때 ① 오너가 직접 브라우저에서 Network 탭 확인 후 공유, ② `appdev@smbs.biz`에 정식 데이터 제공 문의, ③ 이 지표도 6단계처럼 실측 불가로 재분류 중 하나를 선택하면 재개. 코드는 손대지 않음(여전히 옛 환율 레벨 프록시 그대로).
    - **✅ 6단계 완료 (2026-08-10, TASK_HISTORY #72) — `Short_Ratio`/`Stock_Short_Balance`(공매도 2종)를 "실측 불가"로 재분류.** 오너가 옵션②(pykrx 예외 불허)를 확정해 실행했습니다. 활성 지표 **8개 → 6개**.
        - **⚠️ #69의 6개와 사유가 다릅니다(뭉뚱그리지 말 것).** #69는 "무료로 구할 데이터 자체가 없음"이었지만, 공매도 2종은 **데이터가 존재합니다** — pykrx의 `get_shorting_volume_by_date()`/`get_shorting_balance_by_date()`로 실측값을 받아올 수 있습니다. 다만 그 경로가 `data.krx.co.kr`의 로그인 요구(2025-12-27 회원제 전환)를 Referer 헤더 스푸핑으로 **우회**하는 방식이라 §0-3-2와 충돌하고, KRX 공식 OPEN API에는 공매도 서비스가 아예 없습니다(#66 전수 확인). 즉 **"없어서"가 아니라 "안 쓰기로 해서"** 뺀 것이며, 화면 `missing_data`·코드 주석·테스트에 이 차이를 그대로 적었습니다.
        - **삭제가 아니라 이동**: 두 지표는 여전히 학습 가치가 있어 화면 "📚 공부용 참고" 섹션(4개 → **6개**)에 설명·공부법(KRX 정보데이터시스템에서 직접 보는 법, pykrx 학습 — 단 이 프로젝트에선 원칙상 미사용, 공매도 잔고비율·대차거래·공매도 금지/재개 검색어)과 함께 남습니다. 개념 중복이라 공부 목록에서도 뺀 `Foreign_Broker_Dump`·`Put_Buy_Simple`과는 성격이 다릅니다.
        - **가중치**: 남은 6개 합 85.48 × 100/85.48 → 합계 **100.00**. `FX_Swap_Point` 22.64 / `Synthetic_Futures` 22.64 / `KOSPI_5D_Return` 22.65 / `Put_OTM_OI` 15.09 / `VKOSPI_Skew` 11.32 / `Stock_Net_Sell` 5.66. **잔여 조정 없음**(단순 반올림 합이 그대로 100.00 — #69 때는 99.99라 0.01을 붙였음). 상대 비중은 개정 전과 동일.
        - 두 지표의 입력이던 `volatility`/`dist_from_high` 계산과, 그 값이 없으면 **수집 전체를 중단하던 하드 실패 게이트도 함께 제거**했습니다(참조하는 코드가 하나도 남지 않아 불필요한 실패 경로만 됐기 때문). KOSPI·환율 종가 결측 방어는 그대로 유지.
        - `market_history.csv`의 기존 두 컬럼·과거 값은 **보존**(COL_MAP 매핑도 유지), 앞으로 새 행에만 안 씁니다. 테스트 **364체크 전부 통과**.
    - **📌 현재 매크로 상태 (2026-08-10 최종)** — 활성 **6개** 중 **4개가 실측**: `KOSPI_5D_Return`·`Stock_Net_Sell`(#68) + `VKOSPI_Skew`(=VKOSPI 레벨)·`Synthetic_Futures`(=선물 베이시스, #70, KRX OPEN API 실서버 검증 완료). **아직 프록시는 2개**: `FX_Swap_Point`(환율 레벨 프록시) · `Put_OTM_OI`(코스피 등락률 프록시). 점수에서 뺀 **8개** = 공부용 참고 섹션 **6개**(`ELS_KnockIn`·`NDF_Night_Rate`·`Futures_Net_Sell`·`Non_Arbitrage_Ratio` + `Short_Ratio`·`Stock_Short_Balance`) + 개념 중복 완전제외 **2개**(`Foreign_Broker_Dump`·`Put_Buy_Simple`).
    - **▶️ 그 다음 = 남은 프록시 2개, 둘 다 지금은 보류.** ⑤ `FX_Swap_Point` — 5단계 참고, 오너가 2026-08-10 보류 결정(급하지 않음). ⑥ `Put_OTM_OI` — #70 실행 로그로 `ISU_NM` 형식을 확인해 행사가 파싱을 시도하거나, 풋/콜 미결제약정 비율로 재라벨링(**지표 의미가 바뀌므로 오너 승인 필요**), 아직 착수 안 함. 둘 다 실측이 되면 그때 `MACRO_REDESIGN_PROPOSAL.md` §4-3(실측 직접성 기준 가중치 재설계)을 한 번에 적용하는 것이 다음 과제입니다. **현재는 매크로 재설계 관련 활성 작업 없음** — 다음에 이 프로젝트를 열면 여기부터 볼 필요 없이 §1~2 화면 확인이나 다른 작업으로 바로 넘어가도 됩니다.
    - **✅ 선행 조건 전부 완료 (2026-08-10)** — KRX OPEN API 인증키 발급(유효기간 2026/08/10~2027/08/09), **4개 서비스 이용신청 전부 승인 완료**(파생상품지수/KOSPI 시리즈 지수/선물 일별매매/옵션 일별매매), GitHub Secret `KRX_OPENAPI_KEY` 등록 완료, `scrape.yml`이 그 시크릿을 `scrape_daily.py` 스텝에 전달하도록 배선 완료. **인증키 값 자체는 어떤 문서·코드·로그에도 남기지 않습니다**(코드는 환경변수에서만 읽고 HTTP 헤더로만 전달 — URL에 실으면 서버 로그에 남기 때문).
    - **⚠️ 약관 주의 — KRX OPEN API 제6조②: "비상업적 목적으로만 이용 가능, 결과에 대한 대가를 제3자에게 청구 불가."** 오너가 사이트에 **광고를 붙일 계획**이 있음(2026-08-10 확인) — 매크로 화면이 계속 비공개 상태면 지금은 충돌 없음. 나중에 이 화면을 공개 전환하거나 광고 붙은 페이지에 이 데이터를 섞으면 ① 그 페이지엔 광고 제외 ② KRX 유료 상업 라이선스 전환 중 하나를 선택해야 함. **일단 "개발까지만 하고 공개 여부는 나중에 판단"으로 진행 확정** — 공개 전환 전에 반드시 이 조항 재검토할 것.
- **⚪ 참고만** — 미국주식 페이지 상단 지수가 아직 단일 출처(§0-3-3 미충족, 화면에 "근사치입니다" 고지 있어 공개 블로커 아님). `GEMINI_API_KEY` 미등록이어도 자동화는 정상 작동(AI 코멘트만 생략).

> 완료된 항목(AUDIT_REPORT_V2 27건, git filter-repo 히스토리 정리, 해외주식 페이지 구축 전체, bcrypt 지원 등)은 전부 `TASK_HISTORY.md`에 남아있습니다.
> **처리 순서(오너 확정): 0(AUDIT_REPORT_V2) → 4(git filter-repo) → 1(해외주식 페이지) 전부 완료. 다음은 2번(매크로 재설계)입니다.**

## 5. 자동화 동작 방식 요약

**🇰🇷 코스피 — `.github/workflows/scrape.yml`**

- 트리거: 매일 평일 KST 16:05 (cron), 또는 Actions 탭에서 수동(`Run workflow`)
- 순서: `collector_kospi200.py`(종목 데이터) → `scrape_daily.py`(매크로 데이터) → 변경사항 `git commit & push`
- 수동 실행 시 `target_date` 입력칸에 `YYYY-MM-DD`를 넣으면 그 날짜만 콕 집어 보정(백필) 가능. 이때는 실시간 시세 조회를 건너뛰고 그 날짜의 실제 종가만 사용함 (오늘 시세로 과거가 오염되는 걸 방지)

**🇺🇸 미국주식 — `.github/workflows/scrape_us.yml`** (2026-08-07 신설, TASK_HISTORY #60)

- 트리거: 매일 평일 **20:35 UTC / 21:35 UTC 두 번** (cron 2줄), 또는 Actions 탭에서 수동(`Run workflow`)
- **왜 두 번인가**: 우리가 수집해야 할 시각은 언제나 "미국 정규장 마감 16:00 ET + 30분 = 16:30 ET"인데,
  GitHub Actions의 cron은 UTC 고정이라 서머타임에 따라 대응 UTC 시각이 1시간 움직입니다.
  EDT(3~11월) → 20:30 UTC, EST(11~3월) → 21:30 UTC (둘 다 `describe_collection_schedule()`을
  실제 호출해 확인한 값. 정각 혼잡을 피해 5분씩 뒤로 밀어 20:35/21:35로 설정).
- **그래도 실제 수집은 하루 한 번**: 수집기가 `--skip-if-not-ready` 사전 점검을 합니다. 판정 기준은
  "이번에 담기게 될 거래일(session_date)을 기존 스냅샷이 이미 담고 있는가" —
  이미 있으면 아무것도 하지 않고 정상 종료(exit 0)합니다. 서머타임 전환일에도 자동으로 맞습니다.
- **덤**: 앞쪽 실행이 HTTP 429로 끊기면 스냅샷이 갱신되지 않으므로 뒤쪽 실행이 같은 날 자동 재시도합니다
  (체크포인트 `data/us_collect_checkpoint.json`에서 이어받아 처음부터 다시 하지 않음).
- 순서: `collector_us_stocks.py collect --skip-if-not-ready` → `data/us_*.json` 변경분 `git commit & push`
  (푸시 거부 시 fetch+rebase 최대 3회 재시도 — 코스피 워크플로우와 동일 패턴)
- 수동 실행 시 `force` 체크박스를 켜면 사전 점검을 무시하고 같은 거래일도 다시 수집합니다.
- 시크릿 불필요(`GEMINI_API_KEY` 등 안 씀 — 미국주식 수집기에는 AI 코멘트 기능이 없습니다).

## 6. 다음에 코드를 또 고칠 때 지켜야 할 원칙

`ENGINEERING_SPEC.md`의 최우선 규칙: **데이터를 못 가져오면 하드코딩된 값이나 그럴듯한 가짜 숫자로 조용히 채우지 말고, 명확한 오류를 내거나 화면에 "데이터 없음"으로 표시할 것.** 단, 아래 두 가지 예외는 오너 승인으로 허용됨(둘 다 §0-1에 조건 명시):

- **계산값 예외**: 실측 불가 + 순수 사칙연산 + 입력값도 실측일 때만, 대수적 역산 허용(반드시 "계산값" 마크).
- **섹션별 마스킹**: 일부 데이터(주로 Forward/미래 추정)만 없으면 종목 전체를 막지 말고, 그 섹션만 마스킹하고 나머지는 정상 노출.

이 프로젝트는 **후행지표 전용**입니다(§0-3-1) — 실시간 데이터를 다루지 않고, 장마감 후 확정된 데이터만 사용합니다.

## 7. 로컬-GitHub 데이터 동기화 원칙 (2026-08-06 추가)

자동 크롤링(GitHub Actions)은 GitHub 원격 저장소에만 데이터를 쌓고, 로컬 작업 폴더는 오너가 수동으로
`git pull` 해야만 최신 상태가 됩니다. 시간 기반 자동 동기화는 오너 컴퓨터가 항상 켜져 있지 않아서
적용하지 않기로 함(2026-08-06 논의). 대신: **세션 시작 시, 또는 크롤링 결과 확인·데이터 검증 등 데이터
관련 작업을 요청받으면, 먼저 로컬이 GitHub 최신 상태인지 확인하고 뒤처져 있으면 pull부터 안내할 것.**
오늘 로컬/원격(raw.githubusercontent CDN 캐시)/실제 서비스(Streamlit) 세 곳이 서로 다른 시점을 보여줘서
혼선을 겪었던 것이 이 원칙을 세운 계기.

## 8. 해외주식(미국주식) 페이지 — 설계 원칙 요약 (완료된 기능, 참고용만 유지 — 2026-08-09 정리)

미국주식 페이지는 2026-08-08 공개 전환으로 완료됐습니다. 조사 과정·확정 이력·수치 전체 서술은
`TASK_HISTORY.md` #52~#65에 남아있습니다. 여기는 **비슷한 신규 시장을 또 추가할 때 재사용할 설계
원칙만** 요약해 남깁니다.

- **범위**: 나스닥+뉴욕 시가총액 상위 약 550개. 채권/노트류, 미국식 우선주(Preferred Stock 명시) 제외.
  동일 회사 복수 보통주 클래스(BRK/A·BRK/B, GOOGL·GOOG)는 전부 포함(대표 1개로 합치지 않음). MLP
  보통유닛·ADR은 일반 주식으로 취급해 포함. 통화는 USD 단독 표기(원화 환산 없음).
- **데이터 소스**: 유니버스는 `Ate329/top-us-stock-tickers` CSV(무료·자동 갱신), 펀더멘털은
  stockanalysis.com 종목별 statistics 페이지(소형주까지 커버리지 확인됨).
- **코스피와 다르게 설계한 부분과 이유**: 재무 건전성은 Trailing ROE 대신 F-Score 사용(ROE 중복 계상
  방지) · 변동성은 실측 시계열이 없어 베타를 배수가 아닌 점수 항목(5점)으로만 반영 · 주주환원율에
  자사주매입 포함(희석 시 음수 허용, 0으로 자르지 않음) · Forward EPS는 계산값(종가÷Forward PER,
  "🧮 계산값" 배지로 실측과 구분) · 애널리스트 컨센서스 목표가는 모델 목표가와 합치지 않고 나란히 노출.
- **타임존**: America/New_York 전용 헬퍼(`_now_et()`)로 서머타임까지 처리 — KST 크론 스케줄링 시
  날짜 경계 버그(장마감이 서머타임 여부로 KST 오전 5~6시 사이에서 날짜가 바뀜) 재발 방지용.
- **임계값 설계 원칙**: 처음부터 flat 하드코딩 대신 z-score/절대거리 비례 스케일링으로 설계(코스피
  쪽에서 나중에 감사로 고친 패턴을 처음부터 반영) — 실측 548종목 분포로 확정 완료(TASK_HISTORY #55).
- **남은 참고사항**: 상단 지수가 아직 단일 출처(§0-3-3 미충족, 화면에 "근사치" 고지로 공개 블로커
  아님으로 처리) — §4 참고.

## 9. 📊 "내 성적표" (3번째 모듈) — 진행 상황 (2026-08-11 신설, TASK_HISTORY #73)

> 이 섹션은 §4(매크로)와 **완전히 별개**입니다. 매크로는 오너 지시로 중단 상태이고, 지금 진행 중인
> 신규 작업은 이 §9입니다. 지시서 원본은 `SCORECARD_WORK_ORDER.md`(단일 진실 소스),
> 최초 검토 메모는 `MY_SCORECARD_MODULE_NOTES.md`.

### 9-1. 지금 상태 = 🔓 **공개 승인 완료 + 공개 전환 전 최종 점검 완료** (2026-08-13, #106)

> **⚡ 현재 상태 한 줄 요약 (2026-08-13 갱신)** — 오너가 2026-08-12 "둘 다 공개로 전환해줘"로
> §9·§10 두 모듈의 공개를 최종 승인했고, 공개 전 보안/견고성 최종 점검과 사이드바 레이아웃
> 정리까지 끝났습니다(#106). **코드 쪽에 남은 일은 없습니다** — 오너가 Streamlit Cloud Secrets 에
> `SCORECARD_ENABLED = "1"` / `REPORT_ENABLED = "1"` 2줄을 추가하면 그 즉시 공개됩니다.
> 자세한 점검 결과와 정확한 절차는 **아래 §9-6** 참고. (아래 9-1 본문은 2026-08-11 시점 기록입니다.)

#### (2026-08-11 시점 기록) 🚧 스테이징, 실서버 연결 검증 완료 + 오너 실사용 피드백 3라운드 반영

v1 골격(지시서 §6의 1~5단계)을 만들고 오프라인 검증까지 마친 뒤, **오너가 실제 Supabase 프로젝트를
만들어 SQL 스크립트 실행 → Email 인증 활성화 → Streamlit Cloud Secrets 등록까지 전부 마치고 직접
회원가입/로그인/종목 입력을 실제로 테스트했습니다.** RLS 8개 정책 정상 적용 확인(`rls_enabled` 쿼리
결과 true), 로그인 세션·holdings 저장·PEGY 연동까지 실제로 동작함을 확인했습니다.

그 과정에서 나온 **실사용 피드백 4건을 같은 날 바로 수정**했습니다(코딩 담당: 소넷, Opus 아님 — 범위가
작고 이미 있는 코드의 UX 개선이라 새로 아키텍처를 짤 필요는 없었습니다):
1. **Enter 키로 실수 제출되는 문제** — 입력 폼이 `st.form`이었던 탓에 다음 종목을 입력하려고 습관적으로
   Enter를 치면 그 시점 값 그대로 추가돼버렸습니다. `st.form`을 없애고 **명시적 버튼 클릭으로만 제출**
   되도록 변경.
2. **수량/매입가 입력 UX** — `st.number_input(format="%.6f")`라 항상 "0.000000"이 먼저 채워져 있어
   타이핑이 뒤에 이어 붙는 문제, 콤마 입력 불가 문제를 함께 해결 — `st.text_input`으로 바꾸고 콤마 섞인
   입력("1,664,333")을 그대로 파싱하도록 `_parse_positive_number()` 신설.
3. **종목코드 칸에 종목명을 잘못 넣는 사고** — 한국 종목코드 칸에 "하이닉스" 같은 한글을 넣으면 어떤
   검증도 없이 그대로 저장돼(계속 "현재가 없음") 문제 원인을 알기 어려웠습니다. 코드 형식 검증을
   추가해 한글이 섞이면 명확한 안내와 함께 막고, 종목명으로 자동 찾기는 처음엔 **미국 주식 전용**으로
   한정했었습니다 — ⚠️ 이 제한은 같은 날 바로 다음 §(#76)에서 오너 지시로 철회됩니다.
4. **삭제 기능을 못 찾음** — 기능 자체는 처음부터 있었지만 화면 맨 아래(밸류에이션 카드 다음)에 접혀
   있어 발견하기 어려웠습니다. **표 바로 아래로 이동 + 기본 펼침**으로 변경(→ 이후 #75에서 인라인
   버튼 방식으로 다시 개편).
5. **입력 필드가 안 비워짐** — 추가 성공 후에도 종목명 등이 이전 값 그대로 남아있어, 다음 종목을 넣을
   때 실수로 재사용되는 사고(`하이닉스`라는 이름이 다른 종목코드에도 계속 붙던 버그)가 있었습니다.
   `_reset_input_fields()`로 추가 성공 시 입력칸을 비우도록 수정(시장 선택은 유지).

테스트는 187 → **211체크**로 확장, 기존 5개 스위트 전부 회귀 없음(#74 시점 기준 — 이후 #75/#76에서
228체크까지 더 늘어남, 아래 참고).

**#75 — 종목 삭제를 드롭다운 방식에서 종목별 인라인 ✏️(수정)/🗑️(삭제) 버튼으로 개편.** 오너가
"드롭다운으로 고를 게 아니라 종목 옆에 바로 수정·삭제 버튼을 두자"고 요청. `st.dataframe()` 정적 표
대신 종목마다 직접 `st.columns()`를 그려 버튼을 바로 붙였고, ✏️를 누르면 수량·매입가를 그 자리에서
고쳐 저장할 수 있게(`update_holding()` 최초 연동 — 가중평균 병합이 아니라 **값 직접 덮어쓰기**, 화면
문구로 명시) 만들었습니다. 테스트 211 → 214체크.

**#76 — 종목 입력을 "코드/티커/이름" 통합 단일 칸으로 재설계.** 오너가 한국 주식(삼성전자우)을
종목명으로 입력하다 막히는 걸 실제로 겪고 "종목코드/티거/종목명 이게 전부 다 한곳에서 기능할 수
있게 해야지"라고 명확히 지시 — #74에서 만든 "미국 전용" 제한을 완전히 없앴습니다. 종목코드/종목명
2칸이었던 입력을 **1칸**으로 합치고, `utils/scorecard_db.py`에 `resolve_stock_query(market, query,
indexes)`를 신설해 입력값 하나로 코드/이름을 모두 판별합니다: ① 코드로 해석해 유니버스 안에 있으면
즉시 확정 → ② 실패하면 이름으로 찾기(§0-1: 정확일치→유일한 부분일치→그 외 포기) → ③ 그것도
실패했지만 "코드처럼 생긴 형식"이면 유니버스 밖 종목의 코드로 보고 그대로 채택(이름 없이, "현재가
없음"으로 정직 표시) → ④ 그마저 안 되면 이유와 함께 실패. **한국/미국 두 시장 모두 동일 로직.**
테스트 214 → **228체크**.

**#77 — 화면 UI 다듬기 3건 (오너 스크린샷 기반).** 실제 화면을 캡처해 지적한 문제들: ①
✏️/🗑️ 버튼이 옆 셀보다 아래로 처져 보이는 문제(원인: "비중" 열이 좁아 "100.0%"가 두 줄로 줄바꿈되며
버튼이 밀림 → `st.container(key=...)`로 표를 스코프하고 그 안에서만 `white-space: nowrap` +
`align-items: center` CSS 적용해 근본 수정, 다른 화면엔 영향 없음), ② "Trailing 적정가 · Forward
목표가"가 잿빛 한 줄 텍스트로 너무 작던 것 → 위 지표들과 같은 `st.metric` 카드로 교체해 크게,
③ "내 평균매입가 vs 현재가"(오너가 아이디어는 마음에 들어했지만 눈에 안 띈다고 지적) → 손익 방향에
따라 초록/빨강으로 바뀌는 큰 배너(📈/📉 + 등락률 %)로 강화. 구조 변경 없는 CSS/텍스트 다듬기라 테스트
체크 수는 그대로(228).

**#78 — 수정/삭제 버튼을 이모지에서 Material 아이콘으로 교체.** #77에서 CSS로 버튼 내부를
중앙정렬했는데, 오너가 재배포·리부팅 후에도 스크린샷으로 "안 고쳐지고 있다"고 재확인 — 원인을
다시 보니 CSS가 아니라 **이모지 글리프 자체의 비대칭 여백** 때문이었습니다(flex 정렬은 박스 안
위치만 잡을 뿐, 글자 모양 자체의 치우침은 못 고침). `st.button("✏️")`/`st.button("🗑️")`를
`st.button("", icon=":material/edit:")`/`icon=":material/delete:"`(Streamlit 지원 Material
Symbols 벡터 아이콘)로 바꿔 근본적으로 해결 — 오너가 배포 후 스크린샷으로 정중앙 고정 확인.

**#79 — 수익률 색상(국내 증시 관례) + 표 정렬 기능.** 오너 요청 두 가지: ① 수익률 양수는
빨강·음수는 파랑으로(국내 증시 관례 — 앞서 만든 손익 배너의 초록/빨강과는 다른 관례라 화면 안에
두 색 체계가 혼재하게 됨, 아래 참고), ② 표에 오름차순/내림차순 정렬. `_colored_pct()`(Streamlit
`:red[..]`/`:blue[..]` 마크다운 문법) + `sort_holding_rows()`/`SORT_FIELD_OPTIONS`(종목명/수량/
평균매입가/현재가/평가손익/수익률/비중 정렬, 통화 블록마다 selectbox+radio, 기본값은 "기본순서")
신설. **현재가를 모르는 종목은 정렬 방향과 무관하게 항상 맨 뒤**(§0-1 — 없는 값에 순위를 매기지
않음). 테스트 228 → **238체크**.

**#80 — 손익 색상을 국내 증시 관례로 화면 전체 통일.** #79 직후 "화면에 색 관례가 두 가지
섞여 있다"고 오너에게 알렸더니 "해외관례보다는 국내관례로 가자"고 확정. "내 평균매입가 vs
현재가" 배너를 `st.success`(초록)/`st.error`(빨강) → `st.error`(빨강)/`st.info`(파랑)로,
상단 "평가손익" 합계는 `st.metric` 내장 delta 색(파랑 옵션이 없음)을 `delta_color="off"`로
끄고 `_colored_pct()`로 직접 색칠하도록 교체 — Streamlit에 파랑 계열 박스가 `st.info` 하나뿐이라
이름(에러/정보)과 무관하게 색상만 빌려 쓰는 것임을 코드 주석으로 명시했습니다. 이제 요약 3칸·표·
상세 배너가 전부 빨강=오름/파랑=내림 한 관례로 통일됨. 테스트 238 → **240체크**.

> ✅ **해결됨** — #79(정렬 필터)·#80(빨강/파랑)이 처음엔 배포 화면에 반영 안 됐었는데(위 미해결
> 기록), 이후 오너 스크린샷에서 "정렬 기준/정렬 방향" selectbox와 색상이 정상적으로 뜨는 게
> 확인됨(2026-08-11). Streamlit Cloud 재배포/Reboot 지연이었던 것으로 보임 — 코드 문제는 아니었음.

**#81 — 종목 빠른 검색(이름 일부만 쳐도 후보 목록).** "롯데까지만 쳐도 롯데 관련 종목이
좌르르 나오면 좋겠다"는 요청으로, 통합 입력칸 위에 `st.selectbox`를 추가했습니다. Streamlit
selectbox는 클릭 후 타이핑하면 옵션을 그 자리에서 자동 필터링해주는 내장 기능이 있어 별도
자바스크립트 없이 "이름 일부만 쳐도 후보가 나오는" 동작이 됩니다. 후보는 **유니버스(상위
200/550) 안 종목명만**(§0-1 — 없는 종목을 지어내서 보여줄 수 없음), 밖의 종목은 기존처럼
통합 칸에 코드를 직접 입력하는 경로가 그대로 남아있습니다. 고르면 통합 입력칸에 자동으로
채워지고, `_reset_input_fields()`도 이 selectbox(시장별로 키가 다름)까지 함께 초기화하도록
확장. 테스트 240 → **243체크**.

**#82 — 모바일 대응 2건 + 종목명 줄바꿈.** 오너 지시("모바일이 기본인 세상이니 미리미리
고려") — 표가 좁은 화면(≤640px)에서 세로로 안 쌓이고 가로 스크롤되게, "종목" 칸이 이름+코드로
겹쳐 보이던 걸 `<br>`로 명시적 두 줄 강제. §9-3에 "모바일 우선 고려" 원칙으로 등록. 테스트 243 →
**245체크**.

**#83 — 코스피+코스닥+ETF 전체 상장종목 마스터 목록 신설.** 오너 지시: "코스피 시가총액
200종목 외 유니버스의 입력이 가능하도록" / 범위 확인: "코스피 코스닥 전체 ETF까지 포함해야하니까".
지금까지 이름 검색은 밸류에이션이 있는 상위 200(코스피)/550(미국) 유니버스 안에서만 됐고, 그 밖
종목은 코드를 정확히 알아야만 입력 가능했습니다. `collector_kospi200.py`에 신규 수집기
`run_kr_ticker_master_collector()`를 추가해 FinanceDataReader `StockListing('KRX')`(코스피+
코스닥 전체)와 `StockListing('ETF/KR')`(국내 ETF)로 종목코드↔이름(+시장/유형 구분)만 담은
`data/kr_ticker_master.json`을 새로 만듭니다 — **가격·밸류에이션은 절대 담지 않음**(§0-1, 이름
검색 보조용일 뿐). FDR 컬럼 구조가 바뀌거나 호출이 실패해도 예외를 밖으로 던지지 않고 조용히
건너뛰며(핵심 코스피 200 수집에 영향 없음), `__main__`에서 핵심 수집 뒤에 try/except로 감싸
실행. `utils/scorecard_db.py`에 로더 `load_kr_ticker_master()`, `resolve_stock_query()`를
3단계 폴백(①상위 유니버스 →②전체 마스터 목록 →③코드형식 그대로 인정)으로 확장(`broad_index`
파라미터, 기본값 None으로 하위호환 100% 유지). `views/scorecard_view.py`의 "종목 빠른 검색"과
통합 입력칸 모두 이 넓어진 목록을 사용하도록 배선. 테스트 245 → **369체크**(scorecard 260 +
stock_history 109 — 수집기 자체를 가짜 FDR 응답으로 직접 호출하는 기능 테스트 신설).
⚠️ 샌드박스는 외부 네트워크 접근이 안 돼(`pip install finance-datareader` 자체가 실패) 실제
FDR 응답 컬럼 구조는 검증하지 못함 — 다음 GitHub Actions 자동 수집 로그에서 `[전체 상장종목
목록]` 줄 확인 필요(§9-4).

**#84 — 코스피+코스닥 전 종목 종가 수집기 신설.** #83으로 이름 검색은 전체 상장종목까지
넓어졌지만 가격은 여전히 상위 200 밖은 "없음"이었습니다. 오너 요청: "종가만 가지고 오면
우리는 해결되는거잖아". 먼저 KRX 공식 API·공공데이터포털·증권사 API를 조사했는데, KRX
OPEN API 이용약관 제6조②에 "비상업적 목적으로만 이용 가능, 대가를 제3자에게 청구 불가"가
명시돼 있어(원문 확인 완료) 오너의 광고 수익화 계획과 충돌 — 보류. 공공데이터포털의 같은
계열 금융위 API도 "공공누리 2유형(상업적 이용 금지)"으로 확인돼 마찬가지. 대신 **이미 이
앱 전체(가격·PER·EPS·DPS)가 쓰고 있는 네이버 금융 시가총액 순위 페이지**를 그대로
확장하기로 함(상업적 이용 문제가 새로 생기는 게 아님, 다만 네이버 자체 약관 원문까지는
확인 못해 오너가 감수하는 리스크로 문서화). 이 페이지는 종목 하나하나가 아니라 **페이지당
50종목씩** 통째로 내려주는 목록형이라(오너가 "3,500개면 10시간 아니냐" 우려했던 것과
달리) 코스피+코스닥 전체를 55~80페이지, 몇 분 안에 커버 가능함을 확인. `run_kr_all_
market_prices_collector()`가 `data/kr_all_market_prices.json`(코드↔이름↔종가만, 밸류에이션
없음)을 생성 — ETF 필터링 없음(ETF도 조회 대상), 페이지 하나가 실패해도 그 50종목만
건너뛰고 계속 진행(순위 무결성 불필요). `make_price_lookup()`에 `broad_kr_prices` 폴백
신설(기본값 None, 하위호환 유지) — 상위 200에 없는 한국 종목은 이 파일에서 2차로 가격을
찾습니다. `valuation_summary()`는 그대로라 "밸류에이션 정보 없음" 문구는 계속 정확함
(가격은 나오지만 PEGY는 여전히 없음). 테스트 369 → **391체크**(scorecard 271 +
stock_history 120 — 가짜 HTML로 페이지네이션·중간 실패·마지막 페이지 판정까지 실제
함수 호출로 검증). ⚠️ 실제 네이버 응답 구조는 다음 자동 수집 로그로 확인 필요(§9-4).

**#85 — "종목 추가" 버튼이 조용히 씹히는 실사용 버그 수정 (2026-08-12, ✅ 오너 실기기 재확인 완료).**
오너가 실제 화면에서 종목을 입력하고 버튼을 눌러도 로딩창도 에러도 없이 아무 반응이 없는 증상을
보고("칸을 채우고 버튼을 눌렀는데 반응이 없어") — 스크린샷·브라우저 콘솔/네트워크/WebSocket
점검을 함께 여러 라운드 진행한 끝에, 임시 디버그 로그로 "추가" 버튼의 처리 코드 자체가 전혀
실행되지 않는다는 걸 확인하고 원인을 코드에서 추적했습니다. **근본 원인**: "종목 빠른 검색"
selectbox가 선택을 소비한 뒤 `st.session_state.pop(picker_key, None)`으로 자기 자신을
"초기화"하고 있었는데, `pop()`은 서버 쪽 상태만 지울 뿐 **브라우저에는 알려주지 않아**
브라우저가 들고 있던 이전 선택값이 다음 상호작용 때 그대로 다시 제출되면서 매 스크립트
실행 맨 앞에서 `st.rerun()`을 계속 유발 — "추가" 버튼의 처리 코드까지 스크립트가 도달하지
못했습니다(무한 루프가 아니라, 클릭 1번당 재실행 1번씩 그 앞에서 매번 멈추는 현상). **수정**:
정적 키 대신 매 소비 시 값을 1씩 올리는 **논스(nonce) 기반 동적 키**(`scorecard_picker_
{market}_{nonce}`)로 바꿔, 매번 완전히 새 위젯 인스턴스로 취급되게 함.

오너 요청으로 **오푸스에게 이 수정을 별도 검토**시켰고("오푸스로 검토 한번해볼래?"), 실제
Streamlit 1.50.0 소스코드 대조까지 거쳐 진단·수정이 맞다는 걸 확인하면서 같은 원리의 잠재
버그 2건을 추가로 찾아 함께 고쳤습니다: ① `_reset_input_fields()`가 종목/수량/매입가 입력칸도
같은 `pop()` 방식이라 브라우저에 남은 값이 다음 제출 때 되살아나 **종목이 조용히 중복
합산될 위험**이 있었음 — `scorecard_pending_reset` 플래그 + 위젯 생성 *전에* 값을 직접
대입하는 `_consume_pending_reset()`으로 교체. ② `st.success()` 직후 바로 `st.rerun()`을
호출해 성공 메시지가 화면에 그려지기도 전에 사라지고 있었음 — `scorecard_flash`
session_state에 메시지를 담아두고 다음 렌더에서 표시하도록 수정. 테스트도 오푸스가 지적한
약점(문자열만 보고 통과하던 허술한 체크, `module.st.session_state`를 저장/복원 없이
덮어써 실제 streamlit 모듈을 오염시킬 뻔한 위험)까지 보강. 테스트 391 → **399체크**
(scorecard 279 + stock_history 120, stock_history는 이번 라운드 변경 없음). **✅ 오너가
배포 후 실제 화면에서 여러 종목(SK하이닉스·현대차2우B·삼성전자우·SOL AI반도체TOP2플러스)을
연속으로 추가해 정상 반영됨을 직접 확인**(2026-08-12) — 이 버그는 완전히 해결된 것으로 간주.

**#86 — 원형차트(보유 비중/수익 비중) 범례에서 종목코드 제거 (2026-08-12).** 오너 요청: "종목코드만
없어져도 더 깔끔하고 글자가 더 커질 수 있을 것 같은데." 차트 라벨 전용 함수 `_row_chart_label()`을
신설해 두 원형차트 범례에는 종목명만 표시하도록 함(표·다른 텍스트는 코드 병기 그대로 유지 — 차트
범례에만 한정된 변경). scorecard 테스트를 실행해 실측한 결과 **280체크**(+stock_history 120 =
**400체크**), 실패 0건 — 이번 참에 과거 기록된 "399"라는 숫자가 실제 실행 결과와 소폭 어긋나 있던
것도 바로잡았습니다(이후로도 이 문서의 체크 수는 스크립트 실행 결과를 직접 세어 기록).

**#87 — 미국주식 종목 빠른 검색이 티커로는 엉뚱한 결과를 보여주던 문제 수정 (2026-08-12).** 오너가
"XOM"을 쳤더니 관계없는 회사 이름이 잔뜩 나오는 걸 지적("티커가 먼저 검색이 되야 하는게 아냐?").
원인은 #81 후보 라벨에 종목명만 있고 티커가 없어, Streamlit selectbox의 퍼지(순서만 맞으면 매치)
필터가 이름 속에 우연히 X→O→M 순서로 글자가 나오는 걸 전부 잡아낸 것 — 라벨을 "티커 · 종목명"으로
바꿔 티커를 검색 텍스트 맨 앞에 포함시키고, 선택 시 입력칸엔 라벨 문자열이 아니라 실제 티커/코드
값을 채우도록 수정. 한국 시장도 동일 형식으로 통일.

**#88 — 가독성 3건: 미국 종목 한글 표기 + "VS" 배너 렌더링 버그 + ETF 긴 이름 줄바꿈
(2026-08-12).** 오너 스크린샷 지적 3+1건을 한 번에 처리. "VS" 배너가 이상한 글꼴로 겹쳐
보이던 건 글꼴 문제가 아니라, `format_amount()`가 만드는 "$147.80"을 마크다운에 두 번 넣으면
Streamlit이 그 사이를 LaTeX 수식으로 오인하는 **렌더링 버그**였음(원화는 "$"가 없어 이 문제가
원천적으로 없었음 — "한국보다 미국이 떨어진다"던 신고와 정확히 일치) — `_md_amount()`로 "$"를
이스케이프해 해결. 미국 종목은 표·차트·캡션·선택 드롭다운 전부 영문 풀네임 대신 공개
미국주식 화면과 완전히 같은 한글 표기(`utils/company_names_kr.py` 재사용, 새 로직 없음)로
전환. 표의 종목 칸은 인라인 스타일로 줄바꿈을 다시 허용해 ETF 등 긴 이름이 옆 칸을 밀어내지
않도록 방어.

**#89 — 손실 종목 안내 캡션 정리 (2026-08-12, #88 당일 후속).** "미국주식쪽은 예쁘게 잘 된거
같고 한국주식쪽은 수익비중에 있는 저 텍스트 때문에 이뻐보일 수가 없는건가?" — 한국은 손실
종목이 많아 코드까지 붙은 문장이 쉼표로 줄줄이 이어져 지저분해 보였음. 차트 범례(#86)처럼
코드를 빼고, 한 문장 대신 종목당 한 줄 목록으로 재구성.

**#92 — 미국 상장 전 종목 현재가 수집기 신설 (2026-08-12).** #84로 한국은 상위 200 밖 종목도
현재가가 나오게 됐지만 미국은 상위 550 밖이면 전부 "현재가 없음"이었습니다(§9-4 1번의 "같이
생각해볼 것"). 같은 원리 — **한 번에 여러 종목을 주는 묶음 소스** — 로 해결했습니다.
구글 파이낸스는 지금 전 종목 목록 구조 자체가 없어져 폐기하고, 이미 이 프로젝트가 쓰고 있는
**stockanalysis.com 의 자체 종목 스크리너**를 씁니다(같은 사이트라 약관 리스크가 새로 생기지
않음). 함정은 페이지 넘김이 서버가 아니라 자바스크립트에 있어서 `?p=2` 로는 HTML이 안 바뀐다는
점이었고, SvelteKit 데이터 엔드포인트(`__data.json`)를 직접 부르면 데이터가 오는데 그게 평범한
JSON 이 아니라 **"devalue" 직렬화 포맷**(값을 평평한 배열에 담고 인덱스로 서로 참조, 중복 값은
한 번만 저장)이라 **전용 디코더를 직접 구현**했습니다. 실제 응답을 받아 확인한 결정적 사실:
`?p=N` 과 무관하게 응답에 **전 종목이 통째로**(`count=5607`, 행 인덱스도 정확히 5,607개) 들어
있어 **요청 1회로 미국 상장 5,607종목의 티커·회사명·현재가**를 받습니다. 결과는 가격 전용 파일
`data/us_all_market_prices.json`(§0-1 — 밸류에이션은 일부러 미포함), 화면은 `make_price_lookup()`의
`broad_us_prices` 2차 폴백으로 연결(기존 호출부 하위호환 유지). 자동화는 기존 550종목 수집이
끝난 뒤 독립 실행(try/except)이고, **사전 점검을 통과한 실행에서만** 돌게 해 장중 가격을 담지
않도록 했습니다(§0-3-1).

**#93 — 미국 ETF 현재가 수집기 신설 (2026-08-12, #92 당일 후속, 오너 지시).** *"아니 그냥
ETF까지도 넓히자 … 미국주식은 개별주식을 가지고 있는 사람도 많겠지만 ETF로 투자하는 비율도
소수라고 할 수 없을 만큼 많기 때문에 무시할 수는 없어"* — 그동안 오너 **원안대로 ETF를 일부러
제외**해 왔는데(수집기 §"③ ETF/펀드류"), 오너가 그 정책을 명시적으로 뒤집었습니다. 계기는 보유
ETF **KORU(코러)**가 성적표에서 계속 "현재가 없음"으로 뜬 것. ⚠️ 뒤집은 건 **"현재가를 보여주는
범위"뿐**이고, 밸류에이션(PER/PEGY/퀀트점수)은 ETF에 EPS·ROE가 아예 없어 §0-1대로 **여전히 상위
550 보통주 전용**입니다. 소스는 같은 사이트의 **ETF 스크리너**(`/etf/screener/__data.json`)이고,
실제로 받아 확인해 보니 주식 스크리너와 **devalue 포맷도, 행의 키 이름(`s`/`n`/`price`)도 글자
그대로 같아서** 디코더·정규화 코드를 한 줄도 안 고치고 재사용했습니다(ETF 전용 컬럼 매핑 불필요).
역시 **요청 1회에 전 ETF**(응답 `count=5480`)가 옵니다. 파일은 **분리**했습니다 —
`data/us_all_etf_prices.json`. 한국(#84)이 한 파일인 건 코스피·코스닥·ETF가 **한 소스·한 루프**라
그런 것이고, 미국은 요청이 둘이라 합치면 한쪽만 성공한 회차에 다른 쪽이 통째로 사라지고 metadata
(수집시각·소스 URL·건수)도 뒤섞이기 때문입니다. 대신 **읽는 쪽(화면)에서 하나로 합쳐**
기존 `broad_us_prices` 폴백에 그대로 넘기므로 `make_price_lookup()` 시그니처는 그대로입니다.
#92 수집 본체는 라벨·파일명·소스 URL만 파라미터로 뽑아 공용 함수로 승격했고(로직 변경 없음),
진입점은 주식/ETF 두 개가 대칭입니다. CLI `python collector_us_stocks.py etf-prices` 추가.

테스트 누적: 187 → 211(#74) → 214(#75) → 228(#76~#77) → 238(#78~#79) → 240(#80) →
243(#81) → 245(#82) → 369(#83) → 391(#84) → 399(#85) → 400(#86) → 401(#87) → 421(#88) →
422(#89) → 462(#92) → **496(#93, 현재, 실측)**, 매 단계 기존 4개 스위트(macro/quant/us_scoring/us_stocks)
전부 회귀 없음. `git diff -w --stat` 범위는
매번 `utils/scorecard_db.py`·`views/scorecard_view.py`·`tests/test_scorecard.py`(#83부터는
`collector_kospi200.py`·`tests/test_stock_history.py`도 포함, #85~#89는
`views/scorecard_view.py`·`tests/test_scorecard.py`·`TASK_HISTORY.md`·`PROJECT_STATUS.md`,
#92·#93은 `collector_us_stocks.py`·`utils/constants_us.py`·`utils/scorecard_db.py`·
`views/scorecard_view.py`·테스트 2개 + 신규 `tests/fixtures/us_screener_data_json_head.json`
(#93은 `tests/fixtures/us_etf_screener_data_json_head.json`))로
한정됨 — 기존 두 공개 모듈·매크로 관련 파일은 전혀 건드리지 않았습니다.
(⚠️ #92·#93은 `collector_us_stocks.py`·`utils/constants_us.py`를 건드리는데, 이 둘은 공개
미국주식 모듈 소속입니다 — 다만 **기존 수집·스코어링 경로는 한 줄도 바꾸지 않고** 새 함수와
새 상수만 덧붙였고(#93이 손댄 `fetch_us_screener_page()`/#92 수집 본체도 **기본값·동작이 동일**),
`tests/test_us_stocks.py`·`tests/test_us_scoring.py` 회귀 없음을 확인했습니다.)

**공개 화면에는 여전히 아무 변화가 없습니다.** 사이드바 진입 체크박스는 ① 관리자 인증 상태이거나
② `SCORECARD_ENABLED` 플래그를 명시적으로 켠 경우에만 만들어지며, 기본값은 꺼짐입니다(§0-3-6) —
지금은 오너 본인 계정으로만 테스트 중이고 아직 공개 승인 전입니다.

### 9-2. 만든 것

| 파일 | 역할 |
|---|---|
| `sql/scorecard_schema.sql` | **오너가 Supabase SQL Editor에서 1회 수동 실행**할 설정 스크립트. `profiles`/`holdings` 테이블 + **RLS 정책 8개**(본인 행만 select/insert/update/delete) + `anon` 권한 회수 + 트리거. 여러 번 실행해도 안전, `DROP TABLE` 없음 |
| `utils/scorecard_db.py` | 데이터 계층 단일 출처. 순수 계산(가중평균·비중·수익비중)은 Supabase 없이 단독 호출 가능하게 분리. 기존 스냅샷(`data/*.json`)은 **읽기 전용** 재사용. `load_kr_ticker_master()`(전체 상장종목 로더)·`resolve_stock_query(..., broad_index=)`(3단계 폴백)·`load_kr_all_market_prices()`/`load_us_all_market_prices()`/`load_us_all_etf_prices()`(#93, 가격 전용 보조 목록 로더, `make_price_lookup(..., broad_kr_prices=, broad_us_prices=)`의 2차 폴백 — 미국은 화면에서 주식+ETF를 합쳐 넘김) 포함. `supabase`/`streamlit` 임포트는 `try/except ImportError` |
| `views/scorecard_view.py` | 로그인/회원가입 → 보유종목 **통합 단일 칸**(코드/티커/이름, 전체 상장종목 대상 이름검색) 입력 → 통화별 표·요약(정렬·색상) → **원형차트 2종**(비중/이익 기여) → 선택 종목 PEGY 요약 카드 → 종목별 인라인 ✏️수정/🗑️삭제, 모바일 대응 |
| `collector_kospi200.py` | (기존 공개 수집기, #83부터 보조 함수 추가) `run_kr_ticker_master_collector()` — 코스피+코스닥+국내ETF 전체 상장종목 코드↔이름을 `data/kr_ticker_master.json`으로 저장(가격 없음, 이름 검색 보조 전용) |
| `data/kr_ticker_master.json` | (#83 신설, 아직 실제 수집 미실행) 코스피+코스닥+국내ETF 전체 상장종목 코드↔이름 목록. GitHub Actions가 기존 `git add data/` 글롭으로 자동 커밋 |
| `data/kr_all_market_prices.json` | (#84 신설, 2026-08-12 실검증 완료 — 4,296건) 코스피+코스닥 전 종목(ETF 포함) 코드↔이름↔종가만. 마찬가지로 `git add data/` 글롭으로 자동 커밋 |
| `collector_us_stocks.py` | (기존 공개 수집기, #92부터 보조 함수 추가) `run_us_all_market_prices_collector()`(주식) / `run_us_all_market_etf_prices_collector()`(#93, ETF) — stockanalysis.com 스크리너의 SvelteKit 데이터 엔드포인트를 **요청 1회** 불러 티커↔이름↔현재가만 저장. 두 진입점은 공용 본체 `_run_us_screener_prices_collector()`에 소스 URL·파일명·라벨만 바꿔 끼우는 구조. 응답이 devalue 직렬화 포맷이라 전용 디코더(`decode_sveltekit_data_json()`)도 같은 파일 §3-2에 있음. 기존 550종목 수집 경로는 한 줄도 안 바뀜 |
| `data/us_all_market_prices.json` | (#92 신설, 아직 실제 수집 미실행) 미국 상장 전 종목(실측 5,607) 티커↔이름↔현재가만. 밸류에이션 없음(§0-1). `git add -A data/` 글롭으로 자동 커밋 |
| `data/us_all_etf_prices.json` | (#93, 2026-08-12 실검증 완료) 미국 상장 **ETF**(실수집 5,567) 티커↔펀드명↔현재가만. 주식 파일과 **일부러 분리**(소스가 다른 두 요청 — 한쪽 실패가 다른 쪽을 지우지 않도록). 밸류에이션 없음(§0-1) |
| `tests/test_scorecard.py` | 오프라인 **315체크** (네트워크·Supabase 접속 불필요, 가짜 클라이언트+합성 데이터+실제 스냅샷) |
| `tests/test_stock_history.py` | 오프라인 **173체크** (기존 시계열/다운로드 검증 + `run_kr_ticker_master_collector()`(#83)·`run_kr_all_market_prices_collector()`(#84)·`run_us_all_market_prices_collector()`(#92) 가짜 응답 기능 테스트 + devalue 디코더 실응답 픽스처 검증 포함) |
| `tests/fixtures/us_screener_data_json_head.json` | (#92 신설) 2026-08-12 stockanalysis.com 스크리너 데이터 엔드포인트에서 **실제로 받은 응답의 앞부분을 그대로 잘라낸** devalue 픽스처(값 무수정, 뒤쪽 5,600여 행·화면 메타데이터만 제거). 네트워크 없이 디코더 회귀 검증용 |
| `tests/fixtures/us_etf_screener_data_json_head.json` | (#93 신설) 같은 방식으로 잘라낸 **ETF 스크리너** 실응답 픽스처(`count=5480`, VOO~VUG 7행, 값 무수정). 주식 스크리너와 행 키가 동일함을 회귀로 못 박아 둠 |
| `requirements.txt` | `supabase` 추가 (없어도 기존 두 모듈은 정상 작동) |
| `visiblehand.py` | 라우팅 추가. 임포트를 `try/except`로 감싸 이 모듈이 깨져도 기존 두 공개 화면은 살아남음 |

기존 파일(`views/pegy_view.py`, `views/us_stocks_view.py`, `utils/scoring.py`, `utils/scoring_us.py`,
`utils/constants.py`, 매크로 관련 전부)은 **한 줄도 수정하지 않았습니다**(테스트로 확인).
수집기 2개(`collector_kospi200.py` #83·#84 / `collector_us_stocks.py` #92·#93)와
`utils/constants_us.py`(#92·#93)에는 **새 함수·새 상수를 덧붙이기만** 했고 기존 수집·스코어링
경로는 건드리지 않았습니다(기존 스위트 회귀 없음으로 확인).

### 9-3. 이 모듈에서 특별히 지킨 원칙

- **개인 금융정보는 git에 안 올린다** — 기존 데이터는 전부 공개 저장소에 커밋되지만 이 모듈의 데이터는
  Supabase에만 저장됩니다. 접속 정보는 **Streamlit Cloud Secrets**(GitHub Actions Secrets가 아님)에서
  `st.secrets`로만 읽습니다. 소스·문서·로그 어디에도 실제 값이 없습니다.
- **RLS가 진짜 방어선** — anon key는 설계상 브라우저에 노출되는 게 정상인 키라 KRX 인증키와 성격이
  다릅니다. 그래서 "사용자는 자기 행만 본다"를 앱 코드가 아니라 **DB 정책**으로 강제합니다.
  `service_role` 키를 읽는 코드는 없습니다(RLS를 통째로 우회하므로 앱에 절대 넣으면 안 됨).
- **로그인 세션 공유 사고 방지** — Supabase 클라이언트를 `@st.cache_resource`로 캐시하면 한 사람의
  로그인이 모든 방문자에게 공유됩니다. 캐시하지 않고 방문자별 `st.session_state`에만 둡니다.
- **환율 변환 없음** — 원화/달러를 끝까지 분리해 계산하고, 둘을 합친 "총 자산" 숫자는 만들지 않습니다
  (DB의 CHECK 제약으로도 혼용을 차단). 화면 상단에 고지 상시 노출.
- **지어내지 않기(§0-1)** — 현재가는 기존 수집 스냅샷의 실측값만 사용. 유니버스(코스피 상위200/미국
  상위550) 밖 종목은 현재가·평가금액·수익률을 전부 비우고 "현재가 없음"으로 표시하며 비중 계산에서도
  제외합니다(0%로 속이지 않음). Supabase 호출 실패는 빈 값으로 삼키지 않고 `st.error`까지 도달시킵니다.
- **가중평균 표기** — 오너 마인드맵 예시(10주 100만원 + 3주 21만원 → 13주 평균 93,076원)와 화면 숫자가
  어긋나지 않도록, 저장·계산은 전체 정밀도(93,076.923…)로 하고 **원화 표기만 절사**합니다.
- **시세 데이터 상업적 이용 리스크 (2026-08-11, #84 조사로 확정된 원칙)** — KRX 공식
  OPEN API(`openapi.krx.co.kr`, 지금 매크로 화면의 VKOSPI·선물베이시스가 쓰는 바로 그
  API)는 이용약관 제6조②에 "비상업적 목적으로만 이용 가능, 대가를 제3자에게 청구 불가"가
  명시돼 있어 **광고 수익화와 양립 불가**임을 원문으로 확인했습니다. 공공데이터포털의
  금융위 계열 API도 같은 이유(공공누리 2유형)로 보류. 그래서 광고를 붙일 계획이 있는
  기능에는 KRX 공식 API를 새로 쓰지 않고, 이미 쓰고 있는 네이버 금융 스크래핑 방식을
  유지·확장하는 쪽으로 갑니다 — 이건 "완전히 안전하다고 확인된" 것은 아니고 오너가 감수
  하기로 한 리스크입니다(네이버 자체 약관 원문은 조사에서 확인 못함). 나중에 매크로 화면을
  다시 켜고 광고까지 붙이게 되면, VKOSPI·선물베이시스는 이 원칙과 충돌하니 그때 다시
  점검이 필요합니다.
- **모바일 우선 고려 (2026-08-11 오너 지시로 신설된 원칙)** — "모바일이 기본인 세상이니 미리미리
  고려하면서 디자인하자." 앞으로 이 화면(§9)에 새 UI를 만들 때는 데스크톱에서만 확인하고 끝내지
  않고, 좁은 화면(≤640px)에서 깨지지 않는지도 함께 고려합니다. 표처럼 칸이 많은 요소는 억지로
  욱여넣거나 글자를 줄이지 말고 **가로 스크롤**로 유지하는 쪽을 기본 원칙으로 삼습니다(#82 사례).
  다만 이 샌드박스는 실제 모바일 화면을 렌더링해 볼 수 없어, 최종 확인은 항상 오너의 실제 기기로
  해야 합니다.

### 9-4. ▶️ 오너가 다음에 할 일

**1~5번은 완료했습니다**(Supabase 프로젝트 생성 → SQL 스크립트 실행·RLS 확인 → Email 인증 활성화
→ Streamlit Cloud Secrets 등록 → 실제 회원가입/로그인/종목 입력 테스트, 2026-08-11).

> **2026-08-12 오너 지시로 순서 재조정**: 모바일 실기기 확인은 "내 성적표" v1의 **최후
> 공정(최종 검토)** 로 미룹니다 — 아래 다른 항목들이 다 끝나고 배포가 안정된 뒤 마지막에
> 한 번에 확인하는 게 효율적이라는 판단. `Confirm email`은 스크린샷으로 이미 켜져있음을
> 확인해 **완료 처리**합니다.

1. ✅ **전 종목 종가 실제 수집 확인 — 2026-08-12 실검증 완료 (#84).** GitHub Actions 로그를
   직접 볼 순 없었지만(비공개, 로그인 필요), 로컬로 pull된 `data/kr_all_market_prices.json`을
   직접 열어 확인: 2026-08-11 22:09 수집분, **코스피 2,477 + 코스닥 1,819 = 4,296건**(ETF
   포함이라 순수 주식 예상치 2,000~2,700보다 많은 게 정상), **가격이 None인 종목 0건**.
   무엇보다 오너가 그날 스코어카드 화면에서 직접 입력·확인했던 종목들(SK하이닉스
   1,425,000원, 현대차2우B 199,400원, NH투자증권 28,350원, SOL AI반도체TOP2플러스
   15,715원)과 이 파일의 값이 **정확히 일치** — 실제 화면에 반영된 값 자체가 이 수집기의
   산출물이었음을 직접 확인했습니다. "수집 실패" 관련 경고 필드도 metadata에 없음.

   ✅ **#83(전체 상장종목 마스터 목록)은 2026-08-11 실제 워크플로우 실행으로 검증
   완료됐습니다** — `data/kr_ticker_master.json`이 실제로 생성돼 4,030건(주식 2,870 +
   ETF 1,160, 코스피/코스닥/코스닥글로벌/코넥스 전부 포함, 삼성전자·SK하이닉스 등 정상
   확인)이 커밋됐고, "컬럼 구조가 예상과 다릅니다" 경고도 없었습니다.

   > **2026-08-12 오너 지시 — 같이 생각해볼 것 → ✅ #92로 해결(코드 반영 완료, 실검증 대기)**:
   > "미국 주식 550종목 밖은 가격을 어떻게 할지"를 함께 조사한 결과, **한국(#84)과 정확히 같은
   > 원리**(종목 하나하나가 아니라 한 번에 여러 종목을 주는 묶음 소스)로 풀렸습니다. 구글
   > 파이낸스는 전 종목 목록 구조가 사라져 폐기했고, 이미 이 프로젝트가 쓰고 있는
   > **stockanalysis.com 자체 스크리너**의 데이터 엔드포인트를 **요청 1회** 불러 미국 상장
   > 5,607종목의 가격을 받는 방식으로 구현했습니다(유료 API 불필요). 자세한 내용은
   > TASK_HISTORY #92. 아래 5번에 실검증 항목을 새로 추가했습니다.

2. ✅ **`Confirm email`은 이미 켜져있음을 확인 완료(2026-08-12, 오너 스크린샷)** — Supabase
   Authentication → Sign In/Providers에서 토글이 켜진 상태(초록불) 확인. 실사용 중 리다이렉트
   문제로 잠깐 껐던 적이 있었는데(Site URL이 기본값 `localhost:3000`이던 것), 그 원인은 이미
   고쳐뒀고(Site URL을 실제 도메인으로 교체) 지금 상태로 문제 없음.
3. 위 1·5·6번까지 실검증이 모두 끝났으니, 남은 건 아래 4번(모바일, 최후 공정)뿐 — 그것까지
   끝나면 공개 전환 여부·UI 배치를 결정 (§0-3-6 승인). 승인 전까지는 계속 숨김 유지.
4. **모바일 실기기 확인 — 최후 공정(최종 검토)으로 이동(2026-08-12).** #82에서 표 가로
   스크롤·종목명 두 줄 강제를 추가했는데, 이 샌드박스는 실제 폰 화면을 렌더링해 볼 수 없어
   코드만 보고 고친 것입니다. 위 1~3번이 다 정리된 뒤, 그 사이 쌓인 모든 변경사항(통합
   입력창, 빠른 검색 selectbox, 인라인 ✏️수정/🗑️삭제, 정렬, 색상, Enter/콤마 입력, 미국 종목
   한글 표기 등)을 오너의 실제 휴대폰으로 **한 번에 몰아서** 최종 확인: ① 표가 세로로 안
   쌓이고 옆으로 스크롤되는지, ② 종목명이 코드와 겹치지 않는지, ③ 버튼(✏️🗑️)이 눌리는
   크기인지.
5. ✅ **미국 전 종목 현재가 실제 수집 확인 완료 (#92, 2026-08-12 force 재크롤링).**
   `data/us_all_market_prices.json` 원격 파일 직접 확인: `count=5607`, `source_reported_count=5607`,
   `pages_fetched=1`, `failed_page_count=0`, `source_blocked=false`, `collected_at_kst=2026-08-12T16:58:53`.
6. ✅ **미국 ETF 현재가 실제 수집 확인 완료 (#93, 2026-08-12 — 5번과 같은 회차).**
   `data/us_all_etf_prices.json` 원격 파일 직접 확인: `count=5567`, `source_reported_count=5567`,
   `pages_fetched=1`, `failed_page_count=0`, `source_blocked=false`, `collected_at_kst=2026-08-12T16:58:54`.
   화면에서도 **KORU(코러) 현재가 $17.64로 정상 표시** 확인(오너 스크린샷, 기존 "현재가 없음"에서
   교체됨) — 이번 작업의 출발점이 된 바로 그 사례가 해결됨.

### 9-5. v1 이후 로드맵 (2026-08-12 오너 지시로 순서 변경)

> **2026-08-12 오너 지시**: v2(OCR)·v3(리포트) 순서를 바꿉니다 — §9-4가 끝나는 대로 **리포트
> 쪽을 먼저** 착수합니다. 리포트는 "내 성적표" 안에 다 표기하기엔 정보가 너무 많아서, "내
> 성적표" **아래에 별도 모듈**(기존 코스피/미국주식/내 성적표처럼 독립된 화면)로 만드는 게
> 낫다고 확정. 착수 시점에 이 모듈 전용 지시서(`SCORECARD_WORK_ORDER.md`처럼)를 새로 만들
> 예정 — 아직 코드는 없습니다.
>
> ⚠️ **오너 지시 — 리포트 모듈은 코딩할 때 오푸스(Opus)를 높은 추론 강도로 써서 작업할
> 것.** (지금까지 "내 성적표" 버그 수정·가독성 다듬기는 대부분 소넷으로 충분했지만, 리포트는
> 새 모듈을 처음부터 설계하는 작업이라 난이도가 다름 — 착수 시점에 이 지시를 반드시
> 다시 확인할 것.)

- **다음 순서 — 리포트 모듈** (이전 계획의 v3, "내 성적표"와 별도 화면/메뉴로 신설 예정): 매일
  백그라운드 수익률 누적, 일간·주간 결산 리포트, 벤치마크 비교. §9-4가 끝나면 착수. **코딩은
  오푸스(높은 추론 강도)로 진행.**
- **그다음 — 스크린샷 업로드 + AI OCR** (이전 계획의 v2, Gemini Developer API 별도 결제
  프로젝트 필요, ZDR 신청 권장. `MY_SCORECARD_MODULE_NOTES.md` 참고): 리포트 모듈 이후로 순연.
- 코스피 상위200 / 미국 상위550 **밖 종목의 실시간 시세 조회** (v1은 "밸류에이션 정보 없음"으로만
  표시 — #83으로 이름 검색 범위는 코스피+코스닥+국내ETF 전체로, #84로 한국 종목은 현재가까지
  코스피+코스닥 전체로, #92로 미국 종목도 현재가가 미국 상장 전 종목(약 5,600)으로,
  #93으로 **미국 ETF(약 5,480)까지** 넓어졌습니다.
  다만 ① 여전히 "실시간"이 아니라 수집 시점 종가일 뿐이고, ② PEGY/퀀트 밸류에이션은 상위
  200/550 밖은 여전히 없으며(ETF는 EPS·ROE 자체가 없어 앞으로도 없음), ③ **미국은 이름 검색
  범위가 아직 상위 550 안**입니다 — 티커를 직접 입력하면 가격이 나오지만 회사명으로 찾는 건
  #83의 미국판이 아직 없어서 안 됩니다
  (`us_all_market_prices.json`/`us_all_etf_prices.json`에 이름도 들어있으니 나중에
  `resolve_stock_query()`의 `broad_index`에 연결하면 되는, 작은 후속 작업))
- "1년 미접속 시 데이터 삭제" 자동 배치 (v1은 회원가입 화면 안내 문구만)

### 9-6. 🔓 공개 전환 — 최종 점검 완료 (2026-08-13, TASK_HISTORY #106)

> 오너가 2026-08-12 **"둘 다 공개로 전환해줘"** 로 §9(내 성적표)·§10(리포트) 두 모듈의 공개를
> 최종 승인했습니다(§0-3-6 승인 완료). 아래는 실제로 불특정 다수에게 열기 전에 마친 마지막 점검입니다.

**✅ 점검 결과 요약 — 발견 2건 · 수정 2건 · "문제 없음" 확인 4건**

| # | 항목 | 결과 |
|---|---|---|
| 1 | XSS / 인젝션 (`unsafe_allow_html` 에 사용자 소유 값) | **🔧 수정** — `_row_label_html()` 에서 `stock_name`·`ticker` 를 `html.escape()` |
| 2 | RLS 정책 재확인 (두 SQL 파일) | ✅ 문제 없음 — **SQL 변경 없음 = 오너가 실행할 ALTER 문 없음** |
| 3 | 입력 검증 (음수·0·초대형·NaN) | **🔧 수정** — `_parse_positive_number()` 에 유한성 + 상한 검사 추가 |
| 4 | 가입 남용 / 이메일 인증 우회 | ✅ 우회 경로 없음 (신규 구현 없음 — Supabase Auth 영역) |
| 5 | `service_role` 키 격리 | ✅ `utils/report_db.py` 한 곳뿐, 새어든 파일 없음 |
| 6 | 에러 메시지의 내부 정보 노출 | ✅ 스택트레이스·크레덴셜 노출 경로 없음 (가장 흔하던 DB 원문 오류 1종은 3번으로 차단) |

- **1번(XSS) 상세** — `holdings.stock_name` 은 사용자 소유 컬럼이고 Supabase 는 설계상 anon key +
  로그인 JWT 로 REST 를 직접 호출할 수 있어, 로그인한 사람이 화면을 거치지 않고 자기 행에 HTML 을
  써넣을 수 있었습니다. **RLS 덕분에 그 값은 본인 화면에만 그려지고 남의 세션으로는 절대 넘어가지
  않지만**(= 남을 공격하는 XSS 는 아님), 본인 세션 스크립트가 그 사람의 로그인 토큰을 훔칠 수 있어
  ("이 문자열을 종목명에 붙여넣어 보세요" 식 사회공학) 이스케이프했습니다. 기존 두 줄 표기·긴 ETF
  이름 줄바꿈(#88) 동작은 100% 그대로입니다.
- **3번(입력 검증) 상세** — 파이썬 `float()` 가 `"nan"`/`"inf"`/`"1e400"` 을 파싱하고 `nan <= 0`·
  `inf <= 0` 이 둘 다 거짓이라 양수 검사를 통과했습니다. 특히 **수정(✏️) 경로는 `make_lot()` 을
  거치지 않아 검증이 아예 없어서** 그 값이 그대로 Supabase 로 나갔고, `1e300` 같은 값은 DB 까지
  갔다가 `numeric field overflow` **원문**이 사용자 화면에 노출됐습니다. 두 입력 폼이 공유하는
  함수에서 한 번에 막도록 보강(상한 `MAX_INPUT_VALUE` 는 DB 의 `numeric(20,6)` 정의에서 유도).
- **2번(RLS) 상세** — `sql/scorecard_schema.sql` 8개 정책이 전부 `to authenticated` +
  `auth.uid() = user_id/id`, update 는 `using`·`with check` 를 **둘 다** 걸어 소유권 이전까지 차단.
  `sql/report_schema.sql` 은 select **1개뿐**이라 사용자가 과거 수익률 기록을 고치거나 지울 수 없음.
  둘 다 `revoke all ... from anon` 이중 방어. **고칠 것이 없었습니다.**

**▶️ 남은 절차 = 오너가 Streamlit Cloud Secrets 에 2줄 추가 (이게 전부입니다)**

Streamlit Cloud → 앱 → **Settings → Secrets** (⚠️ GitHub Actions Secrets 가 **아닙니다**) 에
기존 `SUPABASE_URL` / `SUPABASE_ANON_KEY` 는 그대로 두고 아래 2줄만 **추가**한 뒤 재배포:

```toml
SCORECARD_ENABLED = "1"
REPORT_ENABLED = "1"
```

- 코드 변경은 필요 없습니다 — `is_scorecard_enabled()` / `is_report_enabled()` 가 이 값을 읽어
  그 즉시 일반 방문자에게도 사이드바 대분류 "📊 내 성적표"가 나타납니다.
- **되돌리려면** 두 줄을 지우거나 값을 `"0"` 으로 바꾸면 즉시 다시 숨겨집니다(관리자 미리보기는
  플래그와 무관하게 계속 동작 — `is_*_visible(admin_mode)` 가 OR 조건).
- ⚠️ 화면 안에 남아 있는 "🚧 개발 중인 화면입니다(스테이징)" 경고 배너 2개는 **일부러 그대로
  두었습니다** — v1이고 실제 사용 이력이 짧다는 사실을 방문자에게 정직하게 알리는 문구라
  §0-1 취지에 맞습니다. 문구를 바꾸거나 뺄지는 오너 판단(코드 한 줄 수정이면 됩니다:
  `views/scorecard_view.py.render_scorecard_page()` · `views/report_view.py.render_report_page()`).

## 10. 📈 "리포트" (5번째 모듈) — 진행 상황 (2026-08-12 신설, TASK_HISTORY #95)

> 이 섹션은 §4(매크로, 중단 상태)와 완전히 별개이고, §9("내 성적표")와도 **별도 모듈**입니다.
> 지시서 원본은 `REPORT_WORK_ORDER.md`(단일 진실 소스). 코딩은 오너 지시대로 오푸스(높은 추론 강도).

### 10-1. 지금 상태 = 🔓 **공개 승인 완료 + 공개 전환 전 최종 점검 완료** (2026-08-13, #106)

> **⚡ 현재 상태 한 줄 요약 (2026-08-13 갱신)** — 오너가 2026-08-12 "둘 다 공개로 전환해줘"로
> 이 모듈의 공개를 최종 승인했고, 공개 전 보안/견고성 최종 점검이 끝났습니다(#106). 이 모듈에서
> 특히 중요한 **`service_role` 키 격리**는 저장소 전체 grep 으로 재확인했고
> (`utils/report_db.py` 한 곳뿐, 새어든 파일 없음), **RLS 도 select 정책 1개로 정확히 좁혀져
> 있어 SQL 파일을 고칠 것이 없었습니다** — 따라서 **오너가 추가로 실행해야 할 ALTER 문은 없습니다.**
> 사이드바에서 "📊 내 성적표 → 📈 리포트" 하위 메뉴가 관리자 메뉴 아래로 내려가 있던 배치 문제도
> 함께 정리했습니다(§10-4 4번). **코드 쪽에 남은 일은 없고**, 오너가 Streamlit Cloud Secrets 에
> `SCORECARD_ENABLED = "1"` / `REPORT_ENABLED = "1"` 2줄을 추가하면 공개됩니다 — 정확한 절차와
> 전체 점검 결과는 **§9-6** 에 한 곳으로 모아 적었습니다(두 모듈이 같은 절차라 중복하지 않았습니다).
>
> ⚠️ 리포트는 스냅샷이 **며칠 쌓여야** 의미 있는 숫자가 나옵니다. 공개 직후 방문한 신규 가입자에게는
> 정상적으로 "📭 아직 저장된 스냅샷이 없습니다"가 보입니다(과거를 역산해 만들어내지 않음 — §0-1).
> 이건 버그가 아니라 설계이며, 화면이 그 사실을 그대로 설명합니다.

#### (2026-08-12 시점 기록) 🚧 첫 자동 실행 실패 버그 2건 수정 완료 → 워크플로우 재실행 확인 대기

작업지시서 §9 의 착수 순서(①벤치마크 소스 조사 → ②SQL → ③미국 지수 수집기 → ④데이터 계층 →
⑤워크플로우 → ⑥화면)를 그대로 따라 6단계까지 만들고, 지시대로 **여기서 멈췄습니다.**
실제 Supabase 연결·실크롤링은 오너가 아래 §10-4 를 진행한 뒤에 이어집니다(#92/#93 과 같은 패턴).

**⚠️ 2026-08-12 첫 수동 실행(dry_run) 실패 → 원인 2건 확정·수정 완료 (TASK_HISTORY #96)**

| # | 증상(CI 로그) | 진짜 원인 | 조치 |
|---|---|---|---|
| ① | `⚠️ … 수집 실패 – 응답에서 일별 시세 행을 찾지 못했습니다` (두 지수 모두 0행) | 소스는 정상. **우리 파싱이 응답의 최상위 값만 훑었음** — 실제 행 목록은 한 겹 안쪽 `{"data":{"symbol":"SPY","source":"tiingo","data":[행…]}}` 에 있었음 | 깊이 우선 재귀 탐색으로 교체 + **날짜(`t`) 있는 스키마만** 시세 행으로 인정(글자가 겹치는 quote 스키마 배제) + 블록 메타데이터 가점. 종목 확인은 없는 ETF 이름 대신 응답 `symbol` 로 |
| ② | `fatal: pathspec 'data/us_index_history.json' did not match any files` → `exit 128` | 수집기가 "둘 다 실패하면 파일을 안 만드는" **정상 동작**인데, 워크플로우가 파일명을 그대로 `git add` 해서 잡 전체가 죽음 | `if [ -f … ]` 로 **있으면만 add**(커밋 대상은 여전히 그 한 파일뿐) |

⚠️ **공급자 이름을 조건으로 쓰지 않습니다** — 실응답 확인 결과 SPY 는 `"source":"tiingo"` 인데
**ONEQ 는 `"spg"`(S&P Global)** 입니다. "tiingo 노드만" 같은 조건을 걸면 나스닥이 통째로 빠집니다.

**재검증(실응답 전문)**: 수정 후 SPY/ONEQ 응답 원문으로 수집기를 직접 실행 → **각각 125행 정상
추출, 2026-02-11 ~ 2026-08-11**(SPY 최신 종가 770.56 / ONEQ 104.16), 티커 검증 통과, 경고 0건.
실응답을 그대로 잘라낸 픽스처 2종(`tests/fixtures/us_index_history_{spy,oneq}_data_json_head.json`)
을 신설해 오프라인 테스트도 실응답 기준으로 바꿨습니다(v1 합성 페이로드가 실제와 구조가 달라
테스트는 통과하고 실전만 실패했던 게 이번 사고의 근본 원인).

⏳ **남은 확인(오너)**: 워크플로우 재실행 → 두 지수 모두 `✅ … 응답 125행` 로그 확인 →
`data/us_index_history.json` 커밋 확인 → dry_run 없이 실제 스냅샷 적재까지 확인(§10-4 3번).

### 10-2. 만든 것 (신규 파일 6개, 기존 파일 **한 줄도 수정 안 함**)

| 파일 | 역할 |
|---|---|
| `sql/report_schema.sql` | `portfolio_daily_snapshots`(사용자 × 시장 × 거래일 = 1행) + RLS. **정책이 select 하나뿐**인 게 `holdings` 와 다른 점 — 쓰기는 배치(service_role)만 하고 사용자는 자기 과거 기록을 못 고칩니다. `priced_count > 0` CHECK 로 "그날 가격을 하나도 모르면 행 자체가 안 생기게" DB 레벨에서도 강제 |
| `collector_us_indices.py` | 미국 벤치마크(S&P500·나스닥) 일별 종가 수집기 — 아래 §10-3 소스 조사 결과 참고 |
| `utils/report_db.py` | 기간 경계 계산 · 스냅샷 행 생성 · 기간 집계/데이터 부족 판정 · 벤치마크 수익률 · Supabase 접근. **저장소에서 유일하게 service_role 을 쓰는 파일** |
| `views/report_view.py` | 리포트 화면(기간 선택·이전/다음 기간·시장별 블록·벤치마크 비교·데이터 부족 안내). 기본 숨김 |
| `.github/workflows/scrape_report_snapshots.yml` | 평일 23:20 UTC 실행(코스피·미국 수집이 끝난 뒤) |
| `tests/test_report.py` | 오프라인 **270체크** (네트워크·Supabase 불필요). #96 부터 파싱 검증은 **실제 응답 원문 픽스처**(`tests/fixtures/us_index_history_{spy,oneq}_data_json_head.json`, 2026-08-12 캡처)로 합니다 |

### 10-3. 🔍 벤치마크 소스 — 실응답으로 확인한 결과와 채택 근거 (§0-1)

- **한국(KR)**: `market_history.csv` 에 "코스피 종가"가 이미 매일 쌓이고 있어 **읽기 전용으로 재사용**
  합니다(새 크롤링 없음). ⚠️ 매크로 파이프라인 산출물이라 **쓰기 금지** — 테스트가 이 파일의 바이트가
  변하지 않는지까지 확인합니다. 코스닥 지수는 이 파일에 없어 **v1 은 코스피만**(후속 과제).
- **미국(US)**: 이 프로젝트에 미국 지수 데이터가 아예 없어 새로 조사했습니다.
  - stockanalysis.com 에는 **지수 자체 페이지가 없음**을 확인(`/quote/index/SPX/__data.json` 이
    데이터가 아니라 symbol-lookup 리다이렉트를 돌려줌).
  - 대신 이미 화면 상단 지수에 쓰고 있는 **추종 ETF 프록시**의 과거주가 엔드포인트
    `https://stockanalysis.com/etf/{symbol}/history/__data.json` 가 살아 있고, **요청 1회로 약
    6개월치 일별 종가**를 돌려줍니다(2026-08-12 실응답 재확인 — SPY·ONEQ 모두 125행,
    2026-02-11 ~ 2026-08-11. 행 `{"a":104.16,"c":104.16,...,"t":"2026-08-11",...}`). 응답이 #92 와
    같은 devalue 포맷이라 **디코더를 그대로 재사용**했습니다.
    - 응답 구조(#96 에서 실측 확정): 행 목록은 최상위가 아니라 한 겹 안쪽
      `{"data":{"symbol":"SPY","source":"tiingo","updated":"…","data":[행…]}, "news":[…], …}`.
      **ETF 이름은 이 엔드포인트에 없습니다**(v1 주석의 `"name":"Fidelity …"` 는 다른 페이지 값
      이었음) → 종목 확인은 응답이 스스로 말하는 `symbol` 로 합니다. 시세 공급자는 종목마다 달라
      (SPY=`tiingo`, ONEQ=`spg`) **조건으로 쓰지 않습니다.**
  - **FRED 는 채택하지 않았습니다(중요)**: `fredgraph.csv?id=SP500` 로 **지수 실값** 일별 종가를
    무료로 받을 수 있음을 실응답으로 확인했지만, 같은 응답에 S&P Dow Jones Indices 의 재배포 제한
    ("Reproduction of S&P 500 in any form is prohibited except with the prior written permission of
    S&P Dow Jones Indices LLC")이 명시돼 있습니다. **#84 에서 KRX OPEN API 를 배제했던 것과 같은
    기준**(오너의 광고 수익화 계획과 충돌)으로 보류했고, 근거를 `collector_us_indices.py` 상단에
    남겼습니다. 나중에 "지수 실값이 꼭 필요하다"고 판단되면 그 주석을 근거로 재검토하면 됩니다.
  - Stooq / Yahoo chart API / Nasdaq 공식 API 는 이 개발 환경에서 응답 자체가 오지 않아,
    살아있는지도 확인 못 한 소스라 코드에 넣지 않았습니다(§0-1).
- ⚠️ **그래서 미국 벤치마크 값은 "지수 포인트"가 아니라 "ETF 종가"입니다**(S&P500 약 6,600pt vs
  SPY 약 750달러). 이 사실이 숨겨지지 않도록 키 이름부터 `SP500_PROXY_SPY` / `NASDAQ_PROXY_ONEQ`
  로 두고 DB 코멘트·화면 문구에도 그대로 적었습니다. 리포트가 쓰는 건 절대값이 아니라 **기간
  수익률**이라 목적은 달성되지만, 운용보수·추적오차·배당락 때문에 지수와 완전히 같지는 않습니다.
  배당 조정가가 아니라 **미조정 종가**를 저장합니다(조정가는 과거 값이 소급해 바뀌어 기록이 흔들림).

### 10-4. ▶️ 오너가 다음에 할 일 (이걸 해야 실검증이 시작됩니다)

1. **Supabase SQL 실행** — "내 성적표"와 **같은 프로젝트**에서 SQL Editor → `sql/report_schema.sql`
   전체 붙여넣고 Run → Table Editor 에서 `portfolio_daily_snapshots` 와 "RLS enabled" 확인
   (파일 맨 아래 §7 자가 점검 쿼리 실행 권장 — 정책이 **select 1개**여야 정상).
2. **GitHub Actions Secrets 등록** (⚠️ Streamlit Cloud Secrets 가 **아닙니다**):
   저장소 → Settings → Secrets and variables → Actions → New repository secret
   - `SUPABASE_URL` : 앱에 쓰는 것과 같은 프로젝트 URL
   - `SUPABASE_SERVICE_ROLE_KEY` : Supabase → Settings → API → **service_role** 키
     🔴 이 키는 RLS 를 통째로 우회합니다. **앱(Streamlit Cloud)에는 절대 넣지 마세요.**
3. ✅ **워크플로우 실검증 완료 (2026-08-12).** 1차 실행은 실패(§10-1 버그 2건) → 수정 후 재실행도
   `PGRST125: Invalid path specified in request URL` 로 한 번 더 실패 — 원인은 코드가 아니라
   **`SUPABASE_URL` 시크릿에 Supabase "Data API → API URL"(`https://xxx.supabase.co/rest/v1/`,
   REST 경로 포함)을 그대로 넣은 것**이었습니다. `create_client()`가 자체적으로 `/rest/v1`을 또
   붙이는 구조라 경로가 중복됨. **베이스 프로젝트 URL**(`https://xxx.supabase.co`, 경로·끝 슬래시
   없음)로 정정 후 재실행 → **완전히 성공**: 벤치마크(KOSPI 6579.04 / SPY 770.56) 수집, **보유
   종목 8행 / 사용자 1명** 정상 조회, **스냅샷 2행(KR+US) 저장**, 벤치마크 파일 커밋까지 전부 정상.
   ⚠️ 다른 사람이 이 시크릿을 다시 등록할 일이 있으면 "API URL"이 아니라 **"Project URL"**(또는
   REST 경로 없는 베이스 주소)을 넣어야 한다는 걸 기억해두세요.
4. ✅ **화면 배선 완료 + 최종 형태로 반복 조정 (2026-08-12, #98→#105).** 처음엔 "내 성적표"와
   형제 체크박스였다가(#98) → 클릭 한 번 전환을 위해 시장 선택 라디오 안 셋째 선택지로
   통합(#101) → 오너가 그린 2단 트리(대분류 "💡 사실 이 가격이에요"/"📊 내 성적표" + 각각의
   중분류)로 재구성(#102) → 그 과정에서 "로그인 성공은 뜨는데 메뉴가 안 보이는" 실사용 버그
   발견·수정(#103, admin_mode 스테일 문제) → 관리자 메뉴는 사이드바 맨 아래 유지가 오너
   방침이라 위치는 원복하고 `st.rerun()`으로 정확성만 지키는 방식으로 마무리(#105). 현재 최종
   구조: 맨 위 대분류 라디오(사실 이 가격이에요/내 성적표, 관리자에게만 후자 노출) → 각 대분류
   중분류 → 맨 아래 관리자 로그인. 일반 방문자 화면은 이전과 100% 동일 — 관리자 인증 후에만
   "내 성적표"/"리포트"가 보입니다.
   ✅ **해결됨 (2026-08-13, TASK_HISTORY #106) — 대분류 전환 시 관리자 메뉴·매크로 체크박스
   위치가 흔들리던 문제.** ("공개 전환 시점에 레이아웃 최종 정리"로 미뤄뒀던 항목입니다.)
   원인이 두 가지였습니다 — ① 두 대분류의 콘텐츠 구성 자체가 달랐고(가격 쪽은 헤더+시장
   라디오, 성적표 쪽은 아무것도 없음), ② 내 성적표의 하위 메뉴 라디오가 `render_admin_sidebar()`
   **뒤**에서 그려져 관리자 메뉴 아래로 내려가 있었습니다(#102의 2단 트리 그림·#105 방침과 어긋난
   상태). **두 대분류가 완전히 같은 요소 순서 — [대분류 헤더 1개] + [선택지 2개짜리 라디오 1개] —
   를 그리도록 구조를 맞추고** 하위 메뉴를 관리자 메뉴 위로 올려 해결했습니다.
   ⚠️ **CSS 주입(`position: sticky/fixed`)은 쓰지 않았습니다** — Streamlit 내부 DOM에 의존해
   버전이 바뀌면 조용히 깨지기 때문입니다. `st.container(height=)`도 픽셀 상수를 테마·화면폭마다
   다시 맞춰야 하고 어긋나면 스크롤바 뒤로 메뉴가 잘려서 채택하지 않았습니다. 지금 방식은
   **매직넘버 0개 · 공개 API만** 씁니다. `admin_mode_hint` + `st.rerun()` (#103/#105) 로직은 그대로.
5. 스냅샷이 며칠 쌓인 뒤에야 일간/주간 리포트가 의미를 갖습니다. 그 전에는 화면이 정상적으로
   "데이터 부족"만 보여줍니다(과거를 역산해 만들어내지 않습니다). 매일 23:20 UTC 자동 실행되므로
   그대로 두면 자연히 쌓입니다.

### 10-5. 이 모듈에서 특별히 지킨 원칙

- **service_role 격리** — `utils/report_db.py` 한 파일만 이 키를 쓰고, **환경변수에서만** 읽으며
  streamlit 을 아예 import 하지 않습니다(앱 설정에서 읽히는 경로 자체를 만들지 않음). 화면 코드에는
  `create_service_client` 가 등장하지 않습니다(테스트로 강제).
- **읽기는 전체 사용자, 쓰기는 스냅샷 한 곳** — 배치는 `holdings` 를 select 만 합니다.
- **데이터 부족은 주 컨텐츠로** — 작은 경고 한 줄 붙인 정상 리포트로 위장하지 않고, 부족하다는 사실과
  **실제로 비교한 날짜**를 화면 한가운데에 보여줍니다. 몇 %가 아니라 날짜·일수 같은 사실로 말합니다.
- **소급 계산 금지** — 기능을 켠 날부터 쌓입니다. 벤치마크가 없는 날은 NULL 이고 전날 값을 복사하지
  않습니다. 비교 기준일의 벤치마크 종가가 없으면 가까운 날로 대체하지 않고 "비교 불가"로 둡니다.
- **환율 변환 없음** — 원화/달러는 시장별 행으로 끝까지 분리(DB CHECK 제약으로도 차단).
- **v1 은 단순·정직한 지표** — 구간 시작/종료 스냅샷의 단순 비교이고, 구성이 바뀐 구간에는 "매매
  영향이 섞여 있다"는 안내를 반드시 붙입니다. TWR 같은 정교한 계산은 v1 범위 밖(지어내지 않기).
- **화면 숫자와 스냅샷이 같은 계산** — 평가금액은 "내 성적표"의 `make_price_lookup()`/
  `evaluate_holding()` 을 그대로 재사용합니다(두 번 구현하면 언젠가 반드시 어긋납니다).

### 10-6. v1 에서 제외한 것(다음 순서)

- 시간가중수익률(TWR) 등 정교한 수익률 계산 · 코스닥 벤치마크 · 리포트 PDF/이메일 발송·알림
- 미국 지수 **실값**(FRED 등 재배포 제한 소스)의 사용 여부 판단 — §10-3 참고, 오너 결정 사항
- 사이드바 배선/공개 전환 — §0-3-6 오너 승인 후

