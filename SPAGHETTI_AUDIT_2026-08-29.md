# visible_hand 스파게티 코드 감사 리포트

**감사일**: 2026-08-29 · **범위**: 레포 전체 프로덕션 Python 코드 (91,528줄, 106개 파일) + 테스트 스위트(29개 파일, 33,280줄)
**방법**: 8개 모듈로 분할해 Opus 서브에이전트가 각각 전 파일을 정독·교차검증(재현 실행, grep 전수확인, AST 측정 포함)
**직접 반영**: 위험도 낮고 격리된 항목 8건은 이미 커밋 `ba2b62b`로 반영됨 (아래 0장)
**나머지**: 구조적 리팩터링이 필요한 항목은 코드를 건드리지 않고 이 문서에만 기록 (오너 판단 필요)

---

## 0. 지금 바로 반영한 것 (커밋 `ba2b62b`, 아직 push 안 됨)

| 파일 | 내용 |
|---|---|
| `scrape_daily.py` | 수급 크롤링 요청에 `timeout=5` 누락 → 무기한 hang 위험 제거 |
| `probe_indicator_universe_timing.py` | 죽은 코드 제거 (미사용 `fail_positions`) |
| `web/pages/dividend_page.py` | 다운로드 파일명 `date.today()` → `today_kst()` (UTC 배포 서버에서 한국 새벽에 어제 날짜 파일이 내려가던 문제) |
| `web/pages/duel_page.py` | `ui.label`/`error_banner`에 박힌 마크다운 `**` 리터럴 7곳 제거 — 이 위젯들은 마크다운을 렌더링하지 않아 별표가 그대로 화면에 보였음 |
| `web/pages/duel_page.py` | 규칙 4 안내 문구 정정 — "코스피 상위 200종목뿐" → 실제 유니버스인 "코스피+코스닥 통합 상위 500종목" (화면 다른 곳은 이미 500으로 맞게 나옴) |
| `utils/data_validator.py` | 죽은 함수 `normalize_currency` 제거 (미사용 + 실패 시 0.0을 돌려주는, 원칙에 어긋나는 형태로 방치돼 있었음) |
| `utils/db.py` | 미참조 `RETIRED_METRIC_COLUMNS`, `backfill_missing_metrics` 별칭 제거 |
| `utils/gdrive_helper.py` | 개발자 로컬 Windows 경로가 박힌 `__main__` 테스트 블록 제거 |

**검증**: 7개 파일 전부 `py_compile` 통과. `test_duel_page_usd.py`(149 passed), `test_dividend_us_page.py`, `test_duel.py`(176 passed) 등 관련 테스트 회귀 없음 확인. 삭제한 식별자를 참조하는 테스트 0건 확인. cmd로 push하기 전 `git show ba2b62b`로 diff를 한 번 확인하셔도 됩니다.

---

## 1. 최우선으로 봐주셨으면 하는 것 (전체 8개 모듈 통틀어 Top 5)

이번 감사에서 나온 것 중 **실제 사용자에게 잘못된 결과가 조용히 나가고 있을 가능성이 가장 큰** 5가지만 추리면 이렇습니다. 전부 "리포트로만 남긴" 항목이라 코드는 아직 그대로입니다.

1. **BNK금융지주가 유니버스에서 매일 자동 제외됨** — `collector_kospi200.py:892-912`의 ETF 필터가 `"BNK"` 같은 브랜드 키워드로 부분일치를 하는데, "BNK금융지주"라는 실제 코스피 상장사 이름이 여기 걸립니다. 화면·이력·순위표 어디에도 안 나타나고, 제외된 자리만큼 뒷순위 종목들의 순위가 전부 하나씩 밀립니다.
2. **미국주식 BPS 바닥값 로직이 목표가·상승여력을 왜곡** — `utils/scoring_us.py`의 `floor_price` 보정이 2.5배 폭주 방지 캡을 우회하고, "목표가 초과" 교차검증을 `PBR-1`의 동어반복으로 만듭니다. 실행으로 재현 확인함(예: 저PBR 우량주가 화면에 "+233% 상승 여력" 초록 바로 표시).
3. **결투 모듈 달러 계좌에 "원" 단위가 그대로 노출** — `utils/duel_rules.py`의 `calculate_fill()` 실패 사유 문구가 통화 분리 규율에서 딱 하나 빠져, 달러 계좌 사용자가 "가용 예수금 1,051원"처럼 원화 단위로 표시된 메시지를 봅니다.
4. **배당 KR 달력이 연도에 갇혀 있음** — `web/pages/dividend_page.py`의 달력이 `today.year`를 못 벗어나, 12월 결산 배당의 지급예정일(보통 다음 해 3~4월)이 파일에는 있는데 화면 어디서도 볼 방법이 없습니다.
5. **인증 화면 4곳이 취소된 요청을 "성공"으로 표시** — `web/auth_ui.py`(회원가입/비밀번호 재설정/변경)가 프로젝트의 표준 블로킹 처리 함수(`run_blocking`)를 안 쓰고 있어서, 요청이 취소되면 사용자는 "비밀번호를 변경했다"는 성공 토스트를 보지만 실제로는 안 바뀌었을 수 있습니다.

이 5개는 전부 "격리 수정 가능"으로 분류됐지만, 실제 서비스 데이터·화면에 영향을 주는 항목이라 제가 판단만으로 손대지 않았습니다. 원하시면 이 5개만 추려서 다음 라운드에 바로 고치는 것도 가능합니다.

---

## 2. 모듈별 요약

각 모듈 리포트는 이 세션의 대화 기록에 전문이 남아 있습니다. 여기서는 등급별 건수와 대표 항목만 정리합니다.

### 2-1. 데이터 수집기 (9개 파일, 11,297줄) — 높음 12 · 중간 14 · 낮음 15
- **총평**: `collector_indicator_kr.py`·`corp_code_mapper.py`는 이 저장소의 모범 사례. 문제는 `collector_kospi200.py`(591줄 함수 1개 포함)와 `scrape_daily.py`(510줄 함수 1개)에 집중.
- 대표 항목: 위 Top 5의 #1(ETF 필터), 무배당 오판정(`collector_kospi200.py:1279`, 배당수익률 행 존재만으로 "무배당 확정" 처리), 시총 순위표 열 번호 파싱(`:918-931`, 프로젝트 자체 금지 규칙 위반), 재무제표 파싱 예외가 이미 성공한 값까지 폐기(`:532-797`), 미국주식 종가 계산 시 단위 오류 방어 없음(`collector_us_stocks.py:919-933`).

### 2-2. 결투(듀얼) 모듈 (8개 파일, 12,135줄) — 높음 6 · 중간 9 · 낮음 7
- **총평**: `utils/duel_rules.py`는 외부 의존성 0개로 실제로 순수함. 문제는 KR/USD 미러링이 만든 divergence와 3,561줄짜리 `duel_page.py`.
- 대표 항목: 위 Top 5의 #3, 야간 배치 오케스트레이터 KRW/USD 244줄 복붙(이미 세부 분기가 갈라지기 시작함), 지수 종가에 "가장 최근 값" 폴백을 써서 전체 사용자 주문이 거짓 사유로 취소될 수 있음(`run_duel_daily_batch.py:97-135`), `duel_page.py`가 3,561줄 god-file(KR/USD 미러 19쌍).

### 2-3. 스코어카드 모듈 (9개 파일, 8,687줄) — 높음 4 · 중간 6 · 낮음 7
- **총평**: `utils/scorecard_*_db.py`·`scorecard_ocr.py`·`scorecard_consent_page.py`는 모범적. 문제가 `scorecard_page.py`의 694줄짜리 `_render_input_form` 한 함수에 몰려 있음.
- 대표 항목: OCR 대량 등록 버튼에 중복클릭 방어가 없어 **실제 보유수량이 이중 합산될 수 있음**(`:869-991`), OCR 업로드 경로 2곳이 표준 블로킹 처리를 우회해 취소 시 조용히 실패(`:1100,1105`), DB 예외 원문(Postgres 에러코드·테이블명)이 화면에 그대로 노출(`utils/scorecard_db.py:1403`).

### 2-4. 배당 모듈 KR/US (3개 파일, 3,090줄) — 높음 3 · 중간 6 · 낮음 10
- **총평**: `dividend_us_logic.py`(로직/UI 분리)가 이 모듈 최고 지점. XSS 처리·"0으로 때우지 않기" 원칙이 잘 지켜짐.
- 대표 항목: 위 Top 5의 #4, 지급일정 파일 로드 실패를 완전히 삼켜 달력 배지가 통째로 사라짐(`:1194-1208`), `status="ERROR"` 레코드가 요약에서 사라지고 작년 배당율로 대체되면서 화면 문구의 등식이 거짓이 됨(`:843-857,1123-1157`), 검색창에 debounce가 없어 타건마다 전량 재스캔 3회.

### 2-5. 매크로/PEGY/보조지표/리포트 (13개 파일, 11,356줄) — 높음 4 · 중간 4 · 낮음 2
- **총평**: `utils/indicators.py`·`indicator_universe.py`·`report_db.py`는 교과서적. 문제는 Streamlit(`views/`)/NiceGUI(`web/pages/`) 듀얼런이 만든 7,751줄 중복 — **이 유예기간이 2026-08-31로 이미 도래**.
- 대표 항목: 복붙 드리프트로 Trailing 적정가 상한 배지가 화면에 안 붙는 실제 버그(`views/pegy_view.py:1021`, 변수를 만들어놓고 화면에 안 붙임), PEGY 화면 1,301줄 함수 안에 717줄짜리 for 루프, 표본 부족 시 0.5 중립값이 실측값과 화면에서 구분 불가.

### 2-6. 미국주식 (4개 파일) — 높음 4 · 중간 9 · 낮음 12
- **총평**: §0-1 원칙이 실제 구조로 관철됨. 문제는 US 전용 신규 로직(BPS 바닥값)이 기존 계약을 깬 것.
- 대표 항목: 위 Top 5의 #2 (재현 실행으로 확인), "데이터 정합성 모순" 판정된 종목의 Forward 밸류에이션이 경고 없이 정상 렌더링, 착시 저평가 ROIC 기준이 상수(6.5)·카드색(8.0)·안내문(8.0) 세 곳에서 불일치.

### 2-7. 공유 인프라 — 진입점/인증/레이아웃/공용 컴포넌트/utils (29개 파일, 7,538줄) — 높음 4 · 중간 13 · 낮음 10 · 죽은코드 10
- **총평**: `web/blocking.py`·`web/auth.py`의 저장소 접근 단일화는 이 규모에서 드물게 잘 설계됨. 문제는 그 좋은 규칙을 일부 화면이 우회하는 것.
- 대표 항목: 위 Top 5의 #5, `utils/stock_history.py`가 이력 파일 읽기 실패 시 **전체 이력을 오늘치로 덮어씀**(`:263-311`, 같은 저장소의 `utils/db.py`가 명시적으로 금지한 바로 그 실수), 관리자 로그인에 시도 횟수 제한 없음 + 3주째 방치된 무솔트 SHA-256 하위호환 경로.
- **컷오버 유예 만료 임박**: `app.py`/`visiblehand.py`/`views/`가 2026-08-31까지만 유지하기로 돼 있었는데 오늘이 08-29입니다. `views/admin_view.py`에는 관리자 인증 로직의 **두 번째 사본**(평문 비밀번호가 세션에 상주하는 문제 포함)이 살아 있어, 이 삭제가 보안 개선도 겸합니다.

### 2-8. 테스트 스위트 (29개 파일, 33,280줄) — 높음 4 · 중간 9 · 낮음 3
- **총평**: 개별 테스트의 문서화 품질은 이 저장소 전체에서 가장 뛰어남(왜 이 동작을 지켜야 하는지 이력까지 남김). 문제는 `conftest.py`가 0개라 그 공백을 테스트끼리 서로 import하는 방식으로 메우고 있는 것.
- 대표 항목: `test_quant.py`가 함수명이 `run_golden_tests`라 **pytest가 아예 수집하지 않음**(8개 핵심 검증이 실행된 적 없음), `check()`/`FAILURES` 자체 하네스가 7개 파일에 완전히 동일하게 복제, 프로덕션 소스 문자열의 정확한 등장 횟수를 단언하는 브리틀 테스트 다수(무해한 리팩터링에도 깨짐).

---

## 3. 잘 되어 있는 부분 (그대로 유지하시면 됩니다)

여러 서브에이전트가 공통적으로 짚은, 이 프로젝트의 자산입니다.

- `utils/indicators.py`, `utils/indicator_universe.py`, `collector_indicator_kr.py`, `corp_code_mapper.py`, `utils/expiry_alarms.py`, `utils/krx_openapi.py`의 "못 찾으면 고르지 않는다" 설계
- `web/blocking.py`의 취소/None 구분 설계, `web/auth.py`의 저장소 접근 단일 감사 지점(AST 테스트로 강제됨)
- `utils/data_source.py`의 4단계 실패 등급(원격 성공 → 캐시 → 로컬 사본 → 완전 실패는 숫자를 안 그림)
- `dividend_us_logic.py`, `web/pages/report_page.py`, `web/pages/indicator_page.py`의 로직/표현 계층 분리
- 테스트의 시간 주입식 처리(날짜를 하드코딩하지 않고 파라미터로 받음), 실제 네트워크 호출 0건

---

## 4. 권장 우선순위

1. **이번 주** — 컷오버 유예 만료(2026-08-31) 처리: `app.py`/`visiblehand.py`/`views/`를 `archive/`로 이동. 관리자 인증 사본과 매크로/PEGY 7,751줄 중복이 동시에 해소됩니다.
2. **다음 라운드 후보** — 위 1장의 Top 5. 전부 격리 수정 가능하지만 실제 데이터/화면에 영향을 주므로 검토 후 결정.
3. **계획이 필요한 것** — `duel_page.py`(3,561줄)·`scorecard_page.py`의 `_render_input_form`(694줄)·PEGY 화면(1,301줄) 분해, KR/USD 배치 오케스트레이터 통합, `tests/conftest.py` 신설.

이 문서는 커밋 `ba2b62b` 시점 기준입니다. 각 모듈의 전체 상세 내용(파일별 줄 번호, 재현 코드 포함)은 이 대화 세션에 전문이 남아 있으니, 특정 항목을 더 깊이 파고 싶으시면 말씀해 주세요.
