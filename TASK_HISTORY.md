# 작업 이력 (TASK_HISTORY.md)

> Cowork 진행 상황(작업 목록) 위젯에 쌓인 완료 항목들을 정리해둔 기록입니다.
> 위젯 자체는 깔끔하게 유지하기 위해 완료된 항목을 지우고, 여기에 전체 이력을 남깁니다.
> 지금 진행 중/예정 상태는 `PROJECT_STATUS.md`의 "지금 열려있는 일"을 참고하세요.

마지막 정리: 2026-08-06 (밤, 4차)

---

## 완료된 작업 (시간순)

1. 진단: 자동화 미작동 원인 파악
2. 수정: 워크플로우 파일 통합 및 버그 수정
3. 검증: 수정된 워크플로우 문법 확인
4. 8월 4일 데이터 백필 기능 추가
5. 오푸스 심층 검토: 하드코딩/더미 데이터 전수조사
6. 지침 문서에 하드코딩 금지 원칙 추가
7. 오푸스 전면 수정: 감사 보고서 항목 전체 반영
8. 타임존 버그 수정 및 프로젝트 현황판(PROJECT_STATUS.md) 작성
9. PEGY 상장주식수 파싱 오류 수정 (FinanceDataReader 구조화 데이터 도입)
10. Streamlit GZip 크래시 수정 (streamlit==1.50.0 버전 고정)
11. altair/Python 3.14 크래시 수정 (위험지수 차트를 Plotly로 교체)
12. 위험 기여도 상세표 iframe 고정높이로 인한 잘림 버그 수정
13. 오너 작성 "절대 준수 메뉴얼"을 `ENGINEERING_SPEC.md` §0-3 섹션으로 반영
14. "잘 보면 보이는 손"(매크로 방공망) 화면을 공개 메뉴에서 제거하고 관리자 전용으로 전환
15. PEGY 200종목 중 53종목 "데이터 없음" 원인 규명(버그 아닌 무료 데이터 한계) + DPS "-" 셀 파싱 가짜 에러 로그 버그 수정
16. 대수적 역산("계산값") 허용 예외를 `ENGINEERING_SPEC.md`에 신설 → Trailing EPS 16종목을 가격÷PER로 복구, "🧮 계산값" 배지 추가
17. Forward(미래 추정) 데이터 결측 시 종목 전체 차단 대신 Forward 섹션만 마스킹하도록 `guardrail.py`/`scoring.py`/`pegy_view.py` 개편
18. 그레이엄 넘버(Graham Number, Trailing 전용 참고 목표가) 추가 — 적자 기업은 산출 불가로 정직 표기, 금융주는 강한 경고 배지
19. "동기화/업데이트" 문구 통일(`market_history.csv`에 실제 수집완료시각 `Collected_At` 컬럼 신설, 매크로 화면의 파일 mtime 기반 표시를 이 값으로 교체) + "실시간" 문구 코드베이스 전수 제거(`.py`·`index.html` 11곳: 주석·로그·툴팁·SEO 메타 태그 포함)
20. "코스피 200" 명칭 정직화 — 실제로는 KRX 공식 지수가 아니라 시가총액 상위 200위 스크래핑임을 확인, 화면·다운로드 파일 노출 문구를 전부 "시가총액 상위 200"으로 교체 (파일명은 하위호환 위해 유지)
21. 코스피 시가총액 상위 200 히스테리시스 버퍼 도입 — 진입 200위/이탈 230위, `apply_hysteresis_buffer()`/`_load_previously_tracked_codes()` 신설, 화면은 항상 정확히 200개만(`is_visible` 필터). 합성 데이터 유닛테스트 통과, 실제 운영 데이터 검증은 다음 자동 수집 이후 필요
22. PEGY 카드 UI 개선 2건 — 그레이엄 넘버 박스를 Forward 마스크 안에서 Trailing 섹션 바로 아래로 이동 / 배당필수업종 DPS=0 전체차단을 완화해 Forward 카드 자리에만 노란 확인-필요 배지로 대체(Trailing·퀀트 점수는 정상 노출)
23. 긴 문장 가독성 개선 — 상단 투자주의 배너, 퀀트스코어 가이드, 그레이엄 넘버 설명, 각종 마스크 카드 설명문에 줄바꿈(`<br>`) 추가
24. PEGY 카드 전체 차단 범위 대폭 축소 — 진짜 못 그리는 2가지(price 없음/상장주식수 오류)만 하드블록, 역성장·Forward PER극단치·g_eff산출불가·일반 하네스실패는 Forward 카드 자리만 마스킹(Trailing은 정상 노출). 합성 데이터 10개 시나리오 검증 완료
25. 버그 수정: `forward_available` 판정에 f_pegy까지 넣던 것 수정 — Forward 데이터(f_per/growth)는 있는데 성장률이 마이너스라 PEGY만 못 구하는 경우 "Forward 데이터 없음" 배지가 잘못 붙던 문제(CJ대한통운 사례) 해결, 역성장 컷오프가 대신 정확히 처리하도록 함
26. 퀀트 스코어 만점 계산 통일 + 하드컷오프 심각도 세분화 — 역성장/적자·PER 이상치 하드컷오프가 flat 점수(15점/10점)+고정 만점(100/None)을 리턴하던 걸, 실제 산출 가능한 항목 기준 만점으로 통일하고, 점수 상한을 오늘 수집된 종목 전체 분포 대비 z-score(횡단면 정규화+윈저라이즈, 퀀트 팩터 모델 표준 기법)로 심각도에 비례하게 스케일링. `collector_kospi200.py` 스코어링을 2차 패스로 분리(population 통계 계산 후 일괄 적용). 합성 데이터 10개 시나리오 검증 완료
27. 퀀트 스코어 % 표시 강화 — 만점이 종목마다 다른(35~100점) 문제로 원점수만 봐선 비교가 안 되던 걸, 배지에 달성률(%)을 색상과 함께 크게 표시해 보완. 초기 버전은 툴팁 문구가 길어져 카드 밖으로 튀어나가는 렌더링 버그가 있었으나 즉시 수정
28. Forward ROE 수집 가능성 진단 로그 추가 (task #4, 조사 진행 중) — 네이버 "주요재무제표" 표(이미 fetch 중)에 ROE 컨센서스가 있는지 확인하기 위해 처음 5종목만 로그를 찍는 진단 코드 추가. 실제 결론은 다음 수집 실행 로그 확인 후 판단 예정
29. AUDIT_REPORT.md 보류 항목 정리 — 죽은 파일 삭제(scheduler.py/run_scheduler.bat/.python-version/runtime.txt, 나머지는 이미 없었음), TARGET_FOLDER_ID 환경변수화, macro_view.py의 미사용 PYKRX_AVAILABLE 제거, 관리자 비밀번호 해시 SHA-256→bcrypt 전환 지원(하위호환 자동판별, 유닛테스트 통과). git 히스토리 옛 비밀번호 제거(git filter-repo)는 크롤링과 겹치지 않는 안전한 시점으로 별도 보류(task #10)

30. **Forward ROE 실제 구현** (진단 로그로 확인된 결과 반영, task #4 완료) — 네이버 "주요재무제표" 표에 `ROE(지배주주)` 행 + 연간추정(예: "2026.12(E)") 컬럼이 실제로 존재함을 실제 로그로 확인. `DataValidator.classify_header_timeframe()`을 재사용해 `ANNUAL_EST` 컬럼만 정확히 골라내고(분기 추정 컬럼과 혼동 안 함) `f_roe` 추출, 범위 이상치(|값|>300%)는 제외. 진단 전용이었던 `diag_roe_check` 파라미터 제거. `views/pegy_view.py`에 Trailing 대비 Forward ROE가 비정상적으로 큰 폭(2.5배 이상 또는 +25%p 이상)으로 벌어지면 노란 경고 배지+툴팁("추정치 변동 큼")을 표시 — 값 자체는 조작·평활화하지 않고 그대로 노출(SPEC 원칙 준수). 실제 로그 데이터와 동일한 합성 테스트로 컬럼 선택 로직 검증 완료.
31. **VOL_PENALTY 하드코딩 세분화** (오너 지적: "1.18x가 전 종목 고정값인데 왜 고정했나, 이건 하드컷오프가 아니라 단순 하드코딩") — 변동성 벌점이 기준선(2.0%) 초과 시 무조건 flat 1.18배였던 걸, 기준선 초과폭에 비례해 1.05배~1.40배 사이로 선형 스케일링하는 `compute_vol_penalty()`로 교체(+10%p 이상 초과분은 1.40배로 윈저라이즈 상한). PEGY/목표가 계산에 인라인으로 바로 쓰이는 값이라 스코어링처럼 population z-score 2차 패스로 미루기엔 구조 변경 범위가 커서, 절대거리 기반 스케일링을 대안으로 선택(코드 주석에 트레이드오프 명시). `views/pegy_view.py`의 안내 문구도 "1.18x 벌점"→"정도에 비례한 벌점, 1.05~1.40x"로 수정. 입력값 9종(None~50%)에 대한 출력 스케일링 순서 검증 완료.
32. **새 크롤링 결과 검증 정리** — 오너가 실행해둔 강제 크롤링 로그(GitHub Actions run #11, 성공)를 직접 확인해 히스테리시스 버퍼(`tracked_count:201`, `hidden_buffer_count:1` 정상), `market_history.csv` 누적 저장 성공(`Collected_At` 포함 로직 정상 실행 확인), Forward ROE 컨센서스 존재(`[진단/ROE컨센서스확인]` 로그)까지 3가지를 확정. `forward_available` 배지 재라벨(시프트업/CJ대한통운류)과 퀀트 스코어 심각도 세분화의 실제 종목 데이터 확인은 원격 JSON 파일이 너무 커서(샌드박스 fetch 도구 용량 제한) 이번 세션에서는 못 봤음 — 다음 세션에서 재확인 필요(task #7, #8로 계속 추적).

## 진행 예정 (백로그)

(현재 없음 — 다음 백로그 항목은 `PROJECT_STATUS.md`의 "지금 열려있는 일" 참고)

---

> 이후 완료되는 작업은 이 문서 "완료된 작업" 목록 맨 아래에 번호를 이어서 추가해주세요.
