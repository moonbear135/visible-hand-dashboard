# 작업 이력 (TASK_HISTORY.md)

> Cowork 진행 상황(작업 목록) 위젯에 쌓인 완료 항목들을 정리해둔 기록입니다.
> 위젯 자체는 깔끔하게 유지하기 위해 완료된 항목을 지우고, 여기에 전체 이력을 남깁니다.
> 지금 진행 중/예정 상태는 `PROJECT_STATUS.md`의 "지금 열려있는 일"을 참고하세요.

마지막 정리: 2026-08-12 (📊 **내 성적표 진입점을 시장 선택 라디오로 통합**(체크박스+라디오 2단계 조작 → 라디오 하나로 클릭 한 번 전환, #100의 임시방편 제거) #101 / 📊 **내 성적표 켜졌을 때 시장 선택 라디오 비활성화 UX 수정**(#101에서 대체·제거됨) #100 / 📈 **리포트를 내 성적표 하위 탭으로 재배치**(형제 체크박스 2개 → 체크박스 1개 + 하위 라디오) #99 / 📈 **리포트 화면 사이드바 배선**(관리자 미리보기) #98 / 📈 **리포트 모듈 배치 파이프라인 실검증 완료** #97 — 벤치마크 수집·holdings 조회(8행/1명)·스냅샷 적재(2행, KR+US)·파일 커밋 전 구간 실제 운영 확인. 중간에 만난 `PGRST125` 오류는 코드 버그가 아니라 `SUPABASE_URL` 시크릿에 Data API의 "API URL"(`/rest/v1/` 포함)을 잘못 넣은 것 — 베이스 프로젝트 URL로 정정 후 해결. 남은 건 `visiblehand.py` 메뉴 배선(선택)과 스냅샷 며칠 누적 대기뿐 / 📈 **리포트 모듈 첫 자동 실행 실패 버그 2건 수정** #96 — ① 벤치마크 수집기가 응답의 **최상위 값만** 훑어 한 겹 안쪽(`data.data`)의 일별 시세 행을 못 찾던 파싱 버그(깊이 우선 재귀 탐색 + 날짜 `t` 필수 조건으로 quote 스키마 배제 + 블록 메타데이터 가점, 공급자 이름은 조건으로 쓰지 않음 — SPY=tiingo/ONEQ=spg 로 서로 다름), 종목 확인을 없는 ETF 이름 대신 응답 `symbol` 로 교체, 실응답 캡처 픽스처 2종 신설, 실응답 전문 재검증 결과 **SPY·ONEQ 각 125행(2026-02-11~2026-08-11)** 정상 추출 / ② 파일이 없으면 `git add`가 exit 128 로 잡 전체를 죽이던 워크플로우 버그(있으면만 add) — 오프라인 255→270체크, ⚠️ 오너의 워크플로우 재실행 확인 필요 / 📈 **리포트 모듈(5번째 모듈) v1 신규 구축 — 코드 완성 + 오프라인 검증까지** #95 — 벤치마크 소스 조사 실응답 확인(미국은 stockanalysis.com ETF 프록시 SPY/ONEQ 과거주가 엔드포인트, FRED 는 S&P DJI 재배포 제한으로 일부러 미채택 / 한국은 `market_history.csv` 코스피 종가 읽기 전용 재사용), `sql/report_schema.sql`·`collector_us_indices.py`·`utils/report_db.py`·`views/report_view.py`·`.github/workflows/scrape_report_snapshots.yml`·`tests/test_report.py` 신설, 기존 파일 무수정, 신규 오프라인 255체크 + 기존 6개 스위트 회귀 없음, ⚠️ 실 Supabase 연결·실크롤링은 오너가 Secrets 등록 후 진행 / 📊 #93 ETF 현재가 수집기 실검증 완료(force 재크롤링 — count 5,567, pages_fetched 1, 화면에서 KORU $17.64 정상 표시) #94 / "내 성적표" 미국 ETF 현재가 수집기 신설(오너 지시로 "ETF 제외" 원안을 뒤집음 — 현재가에 한해서만, 밸류에이션은 그대로 상위 550 보통주 전용. ETF 스크리너도 요청 1회로 5,480종목, 별도 파일 `data/us_all_etf_prices.json`) #93 — 오프라인 462→496체크 / 미국 상장 전 종목 현재가 수집기 신설(stockanalysis.com 스크리너 devalue 디코더, 요청 1회로 5,607종목) #92 — 오프라인 422→462체크, ⚠️ 실제 자동 수집 로그 확인 필요 / #84 전 종목 종가 수집기 실검증 완료(로컬 데이터 대조) #91 / v1 마무리 순서 재조정(모바일 확인 최후 공정으로, Confirm email 완료, v2/v3 순서 교체) #90 / 손실 종목 안내 캡션 정리(코드 제거+목록화) #89 / 가독성 3건 — 미국 종목 한글 표기·"VS" 배너 $ 렌더링 버그·ETF 긴 이름 줄바꿈 #88 / 미국 종목 빠른 검색 티커 매칭 버그 수정 #87 / 원형차트 범례 종목코드 제거 #86 / 종목 추가 버튼이 조용히 씹히는 실사용 버그 수정(빠른 검색 selectbox nonce 키 + 오푸스 리뷰로 텍스트입력 pop 위험·성공메시지 미노출 2건 추가 수정) #85 — 오프라인 391→399체크 / 코스피+코스닥 전 종목 종가 수집기(유니버스 밖 종목 현재가 표시) #84 — 오프라인 369→391체크, ⚠️ 실제 네이버 응답 검증은 다음 자동 수집 후 필요 / 코스피+코스닥+ETF 전체 상장종목 마스터 목록(이름 검색 확장) #83 — 오프라인 245→369체크, ⚠️ 실제 FDR 응답 검증은 다음 자동 수집 후 필요 / 모바일 대응(표 가로 스크롤·종목명 두 줄 강제) #82 / 종목 빠른 검색(이름 일부만 쳐도 후보 목록) #81 / 손익 색상을 국내 증시 관례로 화면 전체 통일 #80 / 수익률 색상(국내 증시 관례)·표 정렬 기능 #79 / 수정·삭제 버튼 Material 아이콘 교체(근본 정렬 수정) #78 / 화면 UI 다듬기(버튼 정렬·적정가 글자크기·매입가 vs 현재가 가시성) #77 / 종목 입력을 코드/티커/이름 통합 단일 칸으로 재설계 #76 — 오프라인 214→228체크 / 종목 삭제를 인라인 수정·삭제 버튼으로 개편 #75 / 실서버 연결 검증 완료 + 실사용 UX 버그 5건 수정 #74 — 오프라인 187→211체크, 여전히 **스테이징(기본 숨김)** / 내 성적표 v1 골격 신규 구축 #73 / 공매도 2종 실측불가 재분류·가중치 재분배 #72 / KRX OPEN API 실서버 검증 완료 + §5 5~6단계 조사 #71 / VKOSPI·선물베이시스 실측 연결 #70 / 매크로 실측불가 6개 제외·가중치 재분배 #69 / 매크로 실측 2지표 정규화 교체 #68 / Streamlit 잠들기 방지 워크플로우 #67 / 매크로 14개 지표 실데이터 조사·제안서 #66)

---

## 완료된 작업 (시간순)

1. 진단: 자동화 미작동 원인 파악
2. 수정: 워크플로우 파일 통합 및 버그 수정
3. 검증: 수정된 워크플로우 문법 확인
4. 8월 4일 데이터 백필 기능 추가
5. 오푸스 심층 검토: 하드코딩/더미 데이터 전수조사
6. 지침 문서에 하드코딩 금지 원칙 추가
7. 오푸스 전면 수정: 감사 보고서 항목 전체 반영
8. 타임존 버그 수정 및 프로젝트 현황판(PROJECT_STATUS.md) 작성
9. PEGY 상장주식수 파싱 오류 수정 (FinanceDataReader 구조화 데이터 도입)
10. Streamlit GZip 크래시 수정 (streamlit==1.50.0 버전 고정)
11. altair/Python 3.14 크래시 수정 (위험지수 차트를 Plotly로 교체)
12. 위험 기여도 상세표 iframe 고정높이로 인한 잘림 버그 수정
13. 오너 작성 "절대 준수 메뉴얼"을 `ENGINEERING_SPEC.md` §0-3 섹션으로 반영
14. "잘 보면 보이는 손"(매크로 방공망) 화면을 공개 메뉴에서 제거하고 관리자 전용으로 전환
15. PEGY 200종목 중 53종목 "데이터 없음" 원인 규명(버그 아닌 무료 데이터 한계) + DPS "-" 셀 파싱 가짜 에러 로그 버그 수정
16. 대수적 역산("계산값") 허용 예외를 `ENGINEERING_SPEC.md`에 신설 → Trailing EPS 16종목을 가격÷PER로 복구, "🧮 계산값" 배지 추가
17. Forward(미래 추정) 데이터 결측 시 종목 전체 차단 대신 Forward 섹션만 마스킹하도록 `guardrail.py`/`scoring.py`/`pegy_view.py` 개편
18. 그레이엄 넘버(Graham Number, Trailing 전용 참고 목표가) 추가 — 적자 기업은 산출 불가로 정직 표기, 금융주는 강한 경고 배지
19. "동기화/업데이트" 문구 통일(`market_history.csv`에 실제 수집완료시각 `Collected_At` 컬럼 신설, 매크로 화면의 파일 mtime 기반 표시를 이 값으로 교체) + "실시간" 문구 코드베이스 전수 제거(`.py`·`index.html` 11곳: 주석·로그·툴팁·SEO 메타 태그 포함)
20. "코스피 200" 명칭 정직화 — 실제로는 KRX 공식 지수가 아니라 시가총액 상위 200위 스크래핑임을 확인, 화면·다운로드 파일 노출 문구를 전부 "시가총액 상위 200"으로 교체 (파일명은 하위호환 위해 유지)
21. 코스피 시가총액 상위 200 히스테리시스 버퍼 도입 — 진입 200위/이탈 230위, `apply_hysteresis_buffer()`/`_load_previously_tracked_codes()` 신설, 화면은 항상 정확히 200개만(`is_visible` 필터). 합성 데이터 유닛테스트 통과, 실제 운영 데이터 검증은 다음 자동 수집 이후 필요
22. PEGY 카드 UI 개선 2건 — 그레이엄 넘버 박스를 Forward 마스크 안에서 Trailing 섹션 바로 아래로 이동 / 배당필수업종 DPS=0 전체차단을 완화해 Forward 카드 자리에만 노란 확인-필요 배지로 대체(Trailing·퀀트 점수는 정상 노출)
23. 긴 문장 가독성 개선 — 상단 투자주의 배너, 퀀트스코어 가이드, 그레이엄 넘버 설명, 각종 마스크 카드 설명문에 줄바꿈(`<br>`) 추가
24. PEGY 카드 전체 차단 범위 대폭 축소 — 진짜 못 그리는 2가지(price 없음/상장주식수 오류)만 하드블록, 역성장·Forward PER극단치·g_eff산출불가·일반 하네스실패는 Forward 카드 자리만 마스킹(Trailing은 정상 노출). 합성 데이터 10개 시나리오 검증 완료
25. 버그 수정: `forward_available` 판정에 f_pegy까지 넣던 것 수정 — Forward 데이터(f_per/growth)는 있는데 성장률이 마이너스라 PEGY만 못 구하는 경우 "Forward 데이터 없음" 배지가 잘못 붙던 문제(CJ대한통운 사례) 해결, 역성장 컷오프가 대신 정확히 처리하도록 함
26. 퀀트 스코어 만점 계산 통일 + 하드컷오프 심각도 세분화 — 역성장/적자·PER 이상치 하드컷오프가 flat 점수(15점/10점)+고정 만점(100/None)을 리턴하던 걸, 실제 산출 가능한 항목 기준 만점으로 통일하고, 점수 상한을 오늘 수집된 종목 전체 분포 대비 z-score(횡단면 정규화+윈저라이즈, 퀀트 팩터 모델 표준 기법)로 심각도에 비례하게 스케일링. `collector_kospi200.py` 스코어링을 2차 패스로 분리(population 통계 계산 후 일괄 적용). 합성 데이터 10개 시나리오 검증 완료
27. 퀀트 스코어 % 표시 강화 — 만점이 종목마다 다른(35~100점) 문제로 원점수만 봐선 비교가 안 되던 걸, 배지에 달성률(%)을 색상과 함께 크게 표시해 보완. 초기 버전은 툴팁 문구가 길어져 카드 밖으로 튀어나가는 렌더링 버그가 있었으나 즉시 수정
28. Forward ROE 수집 가능성 진단 로그 추가 (task #4, 조사 진행 중) — 네이버 "주요재무제표" 표(이미 fetch 중)에 ROE 컨센서스가 있는지 확인하기 위해 처음 5종목만 로그를 찍는 진단 코드 추가. 실제 결론은 다음 수집 실행 로그 확인 후 판단 예정
29. AUDIT_REPORT.md 보류 항목 정리 — 죽은 파일 삭제(scheduler.py/run_scheduler.bat/.python-version/runtime.txt, 나머지는 이미 없었음), TARGET_FOLDER_ID 환경변수화, macro_view.py의 미사용 PYKRX_AVAILABLE 제거, 관리자 비밀번호 해시 SHA-256→bcrypt 전환 지원(하위호환 자동판별, 유닛테스트 통과). git 히스토리 옛 비밀번호 제거(git filter-repo)는 크롤링과 겹치지 않는 안전한 시점으로 별도 보류(task #10)

30. **Forward ROE 실제 구현** (진단 로그로 확인된 결과 반영, task #4 완료) — 네이버 "주요재무제표" 표에 `ROE(지배주주)` 행 + 연간추정(예: "2026.12(E)") 컬럼이 실제로 존재함을 실제 로그로 확인. `DataValidator.classify_header_timeframe()`을 재사용해 `ANNUAL_EST` 컬럼만 정확히 골라내고(분기 추정 컬럼과 혼동 안 함) `f_roe` 추출, 범위 이상치(|값|>300%)는 제외. 진단 전용이었던 `diag_roe_check` 파라미터 제거. `views/pegy_view.py`에 Trailing 대비 Forward ROE가 비정상적으로 큰 폭(2.5배 이상 또는 +25%p 이상)으로 벌어지면 노란 경고 배지+툴팁("추정치 변동 큼")을 표시 — 값 자체는 조작·평활화하지 않고 그대로 노출(SPEC 원칙 준수). 실제 로그 데이터와 동일한 합성 테스트로 컬럼 선택 로직 검증 완료.
31. **VOL_PENALTY 하드코딩 세분화** (오너 지적: "1.18x가 전 종목 고정값인데 왜 고정했나, 이건 하드컷오프가 아니라 단순 하드코딩") — 변동성 벌점이 기준선(2.0%) 초과 시 무조건 flat 1.18배였던 걸, 기준선 초과폭에 비례해 1.05배~1.40배 사이로 선형 스케일링하는 `compute_vol_penalty()`로 교체(+10%p 이상 초과분은 1.40배로 윈저라이즈 상한). PEGY/목표가 계산에 인라인으로 바로 쓰이는 값이라 스코어링처럼 population z-score 2차 패스로 미루기엔 구조 변경 범위가 커서, 절대거리 기반 스케일링을 대안으로 선택(코드 주석에 트레이드오프 명시). `views/pegy_view.py`의 안내 문구도 "1.18x 벌점"→"정도에 비례한 벌점, 1.05~1.40x"로 수정. 입력값 9종(None~50%)에 대한 출력 스케일링 순서 검증 완료.
32. **새 크롤링 결과 검증 정리** — 오너가 실행해둔 강제 크롤링 로그(GitHub Actions run #11, 성공)를 직접 확인해 히스테리시스 버퍼(`tracked_count:201`, `hidden_buffer_count:1` 정상), `market_history.csv` 누적 저장 성공(`Collected_At` 포함 로직 정상 실행 확인), Forward ROE 컨센서스 존재(`[진단/ROE컨센서스확인]` 로그)까지 3가지를 확정. `forward_available` 배지 재라벨(시프트업/CJ대한통운류)과 퀀트 스코어 심각도 세분화의 실제 종목 데이터 확인은 원격 JSON 파일이 너무 커서(샌드박스 fetch 도구 용량 제한) 이번 세션에서는 못 봤음 — 다음 세션에서 재확인 필요(task #7, #8로 계속 추적).

33. **"고성장률 PEGY 보정" 하드코딩 제거 (오너가 실데이터 검증 중 발견: 삼성전자우/SK하이닉스가 "+150% 상승여력"(초록, 저평가 신호)인데 배지는 "🔴 고평가 관망"으로 서로 모순)** — `utils/scoring.py`에 있던 "성장률 100% 이상이면 f_pegy를 max(f_pegy*2, 1.5)로 강제 덮어쓴다"는 로직이 원인이었음(기저효과 왜곡 방어용이었으나 플랫 하드코딩). 이 f_pegy 덮어쓰기가 배지 판정에만 쓰이고 화면의 목표가·적정가 갭은 원래(안 덮어써진) f_pegy로 계산되다 보니 배지와 화면 숫자가 반대 방향을 가리키는 자기모순 발생. `_growth_pegy_score_ratio()` 신설 — f_pegy는 절대 건드리지 않고(배지·목표가 항상 일관), 대신 PEGY 카테고리 "점수"만 성장률이 100%를 얼마나 초과했는지(절대거리 기준, per_extreme·VOL_PENALTY와 동일한 이유로 population z-score 대신 절대 기준 사용)에 비례해 100%(무보정)~20%(최대보정) 사이로 윈저라이즈. `views/pegy_view.py`에 "⚠️ 고성장 추정 보수반영" 배지+툴팁 추가해 왜 저평가 배지인데 점수는 낮은지 설명. 삼성전자우/SK하이닉스 실제 데이터로 재현 테스트 — 수정 전 "🔴 고평가 관망"이었던 게 수정 후 "🟢 강력 저평가"로 화면 숫자와 일관되게 바뀌고, 점수는 각각 38/85·45/85로 여전히 보수적으로 반영됨(예전처럼 무조건 5/35로 뭉개지지 않음) 확인. growth<100% 정상 회귀 케이스는 영향 없음(89/100) 확인.

34. **Opus 2차 재감사 (`AUDIT_REPORT_V2.md`) — 오너 요청: "감사를 이미 했는데도 하드코딩이 계속 나온다, 규모 커지기 전에 한번 더 훑자"** — 페이블5는 사용 크레딧 문제로 호출 불가(에이전트 실행 실패), Opus로 대체 진행. 27건 신규 발견(상 10/중 12/하 5). 최우선: `collector_kospi200.py`의 `abs(t_per)`가 적자 기업 24종목의 마이너스 PER 부호를 지워 흑자처럼 보이게 만드는 버그(적자 기업에 그레이엄 목표가까지 표시되는 부작용 포함). 그 외 목표가 `price×2.5` 캡이 40종목에서 그대로 목표가가 되는데 화면엔 흔적이 없는 문제, ROE 프리미엄 절벽식 하드코딩(오늘 고친 것과 동일 계열), 배당 수집실패=0점 처리, PEGY 점수/배지 경계값 불일치, 매크로 화면 가중치가 실제 계산과 다른 문제 등. 상세 착수 순서·전체 목록은 `AUDIT_REPORT_V2.md` 참고. 아직 수정 작업은 미착수(다음 세션 진행 예정).
35. **로컬-GitHub 동기화 원칙 수립** — 시간 기반 자동 pull은 오너 컴퓨터가 상시 켜져있지 않아 부적합 판단 → 세션 시작/데이터 작업 요청 시마다 로컬이 뒤처져 있는지 확인 후 pull 안내하는 방식으로 `PROJECT_STATUS.md` §7에 원칙 명문화.

36. **`AUDIT_REPORT_V2.md` 27건 전체 처리 완료.** 오너 지시("검사한김에 오푸스로 싹 고치자")로 Opus 에이전트가 collector_kospi200.py/utils/scoring.py/utils/guardrail.py/views/pegy_view.py 19건 + `utils/constants.py`(임계값 단일 출처) 신설까지 먼저 처리(에이전트 실행이 15분 뒤 오너가 중단시켰으나, 확인 결과 컴파일·import·실데이터 재현 테스트 전부 통과하는 완결된 상태였음). 나머지 scrape_daily.py/macro_view.py 8건(4-1~4-5, 5-1~5-3)은 이어서 직접 처리: ① 동시 충격 증폭기 flat 1.3/1.15 → 극단신호 비율 비례 스케일링, ② KOSPI 5일 모멘텀/전일대비 변화율 실패시 0.0 대입 제거(배점 제외로 전환), ③ 개인 순매도 규모(stock_net_base) 방향성만 반영하던 것을 3주체 수급 비중 반영으로 보완, ④ 네이버 시세로 FDR 종가 덮어쓸 때 괴리 발생 시 변화율 재계산(값은 그대로 유지, 시점 불일치만 해소), ⑤ `utils/macro_scoring.py` 신설로 scrape_daily.py/macro_view.py의 가중치·시그모이드 정규화·증폭기 로직을 단일 모듈로 통합(가중치는 `utils/constants.py`의 `RISK_WEIGHTS`), ⑥ 매크로 화면이 과거 날짜를 보여줄 때 재계산 대신 그날 실제 저장된 `SubScore_*`/`Multiplier` 컬럼을 읽도록 변경(구버전 행은 근사치 폴백 + "구버전 데이터" 표시), ⑦ 감사이력 탭에 v1.5.0 항목 추가. 전체 컴파일·import·합성테스트 통과 확인.

37. **타임존(UTC/KST) 버그 재발견 및 전면 수정** — run #13 실데이터 검증 중 오너가 JSON `last_updated_at`이 "08:09"(실제 KST 완료 17:09)로 9시간 어긋난 걸 발견. `datetime.now()`가 GitHub Actions/Streamlit 서버(UTC 기본)에서 naive UTC를 반환하는 게 원인 — `collector_kospi200.py`는 애초에 KST 처리가 없었고, `scrape_daily.py`/`utils/db.py`의 `Collected_At`은 KST 변수가 이미 있는데도 안 쓰고 있었음(부분 회귀). 전부 `datetime.now(KST)`로 통일: ① `collector_kospi200.py`(`_now_kst()` 신설, metadata `last_updated_at` + 로그), ② `scrape_daily.py`/`utils/db.py`의 `Collected_At`, ③ `views/pegy_view.py`의 신선도(staleness) 비교 기준, ④ `views/macro_view.py`의 "오늘 날짜" 판정(`_today_kst()` 신설, 기본 조회일/데이터 신선도/다운로드 파일명), ⑤ `utils/scheduler.py`의 "16시 이후 자동실행" 트리거(기본 비활성 상태지만 재발 방지 차원에서 함께 수정). 전체 컴파일 통과.

38. **run #14 실크롤링으로 AUDIT_REPORT_V2.md + 타임존 수정 최종 확인 완료.** `last_updated_at`이 "2026-08-06 17:59"(실제 크롤링 완료 시각과 일치, KST 정상)로 확인 — 타임존 버그 완전 해결. 목표가 캡(`f_target_capped`) 36종목 정상 플래그(삼성전자 목표가 576250 = 현재가×2.5 배수 캡 정상 적용), 고성장 PEGY 보정(`growth_score_capped`) 28종목 정상. `market_history.csv`에 `Multiplier`/`ExtremeSignalCount`/`SubScore_*` 14개 컬럼 전부 실제값으로 채워짐(매크로 재설계 정상 작동). AUDIT_REPORT_V2.md 27건 + 타임존 버그, 데이터 레벨 검증 전부 종료.

39. **사이드바 빈 공간 정리(1차) → 오너 피드백으로 캡션 되돌림** — 사이드바 상단 빈 공간이 8/5 매크로 관리자 전용 전환 시 없앤 메뉴 선택 라디오의 죽은 CSS 흔적임을 확인, 죽은 CSS는 제거. 처음엔 "현재 공개 서비스: 사실 이 가격이에요" 캡션을 채워넣었으나, 오너 지적("곧 해외종목 페이지 다룰 건데 이렇게 하면 안 되지 않나") 반영해 캡션은 되돌림 — 해외주식 페이지 착수 시 정적 문구가 아니라 실제 페이지 네비게이션 메뉴로 채워야 이중작업이 안 생기기 때문. 죽은 CSS 제거만 유지.
40. **목표가 상한(캡) 배지 문구 개선** — 오너 지적("밑에 상한 도달값 표시는 직관성이 너무 떨어져"): 캡 배수(현재가×2.5)가 고정 상수라 캡에 걸린 모든 종목이 항상 정확히 "＞+150%"로 동일했는데, 개별 계산값처럼 숫자를 앞세워 보여줘서 오해 소지가 있었음(오늘 계속 나온 "flat 하드코딩을 계산값처럼 보여주는" 패턴과 동일 계열). `views/pegy_view.py`의 "상한 도달 (＞+150%, 추정 신뢰구간 밖)" 문구를 숫자 없이 "상승여력 산출 안 함 (상한 캡 적용)"으로 교체 — 상단의 "🧮 상한 적용값" 툴팁 배지가 이유를 이미 설명하므로 하단 갭 표시는 상태만 명확히 전달.

41. **목표가 툴팁에 용어·계산공식 명시** — 오너 지적("이거 포워드 PER로 계산된 포워드 PEGY잖아, 용어하고 계산 공식을 설명내용에도 넣어주라"): 기존 툴팁은 "성장률·주주환원·이익창출력을 고려해 계산"이라고만 뭉뚱그려 설명해 실제 산식이 안 보였음. `views/pegy_view.py`의 목표가 툴팁을 "Forward PEGY 역산" 방식으로 명시하고 ①목표 PEGY=1.0+ROE/ROIC 프리미엄 ②목표 PER=목표 PEGY×Forward 실효성장률(g_eff) ③목표주가=Forward EPS×목표 PER 3단계 산식을 그대로 노출(collector_kospi200.py 실제 코드 로직과 1:1 일치).

42. **Forward 섹션 "가치 지표" 헤더 라벨 중복 제거** — 오너 지적: Forward 박스 상단 헤더가 "Forward PER / EPS"인데 바로 아래 값 줄에도 "Forward PER"/"Forward EPS"가 또 나와 같은 이름이 두 번 반복. Trailing 섹션은 헤더가 구체 지표명이 아니라 상위 카테고리명("가치 및 회수 지표")이라 아래 개별 지표명(PER/EPS/PBR)과 안 겹치는 걸 참고해, Forward 헤더도 "가치 지표"라는 카테고리명으로 교체(개별 값 줄의 "Forward PER"/"Forward EPS" 라벨은 그대로 유지).

43. **해외주식 페이지 데이터 소스 조사 완료 (착수 전 작업지시서 업데이트)** — 오너 지시("마인드맵 기준으로 하되, 그동안 바뀐 내용 손보자")로 실제 웹 조사 진행. 종목 유니버스는 `github.com/Ate329/top-us-stock-tickers`의 무료 CSV(나스닥+뉴욕 통합, 시가총액순 정렬, 매일 자동갱신)로 확정, 펀더멘털은 `stockanalysis.com` 개별 종목 페이지 스크래핑(네이버 방식과 동일 패턴)으로 잠정 확정. 오너가 제안한 "정보 적은 소형주부터 테스트" 방식대로 시가총액 500위권 소형주(RBC Bearings)로 실측한 결과 Trailing+Forward 펀더멘털이 전부 확인됨 — Finnhub 등 회원가입이 필요한 유료/제한 API는 당장 불필요해짐. `PROJECT_STATUS.md` §8에 전체 작업지시서(범위/소스/이번 세션 원칙 적용/미확정 사항) 신설. 이후 오너가 통화 표시를 "달러(USD)로만, 원화 환산 없음"으로 확정(§8-5). 이어서 종목 유니버스 필터링 규칙도 확정 — 채권/노트 제외, 미국식 우선주(한국 우선주와 성격 다름, 고정배당 채권형) 제외. 복수 보통주 클래스(GOOGL/GOOG, BRK/A/BRK/B) 처리는 처음엔 "대표 1개만" 제안했다가 오너 정정("가격이 너무 다를 수 있고 의결권은 해외투자자에게 안 중요하다")으로 **전부 포함, 중복제거 안 함**으로 최종 확정.

44. **토큰 한계 임박 — 세션 마무리, 오푸스 인계용 실행 지시서 신설** — 오너 지시("토큰 보충되면 소넷 말고 바로 오푸스로 기초틀 만들 수 있게 지시서를 만들어두자")로 `US_STOCKS_WORK_ORDER.md` 신설. 오늘 세션 조사·확정된 내용(범위, 데이터 소스 2곳 실측 검증, 종목 필터링 3단계, USD 단독 표시, 이번 세션에 확립된 아키텍처 원칙 7가지, 착수 우선순위, 오너 확인 필요 항목)을 이 대화 맥락 없이도 오푸스가 바로 착수할 수 있도록 독립 문서로 정리. `PROJECT_STATUS.md` §8과 내용 동기화됨.

45. **"내 성적표"(3번째 모듈) 마인드맵 1차 검토** — 오너 공유 마인드맵을 `MY_SCORECARD_MODULE_NOTES.md`로 전사+리뷰. 핵심 지적: 지금 git 공개저장소 방식은 개인 금융정보(로그인/보유종목/매입가) 저장에 안 맞음(별도 비공개 DB 필요), 스코프가 커서 v1(수동입력+차트+밸류에이션 툴팁)/v2(OCR)/v3(리포트) 단계적 출시 권장. 이어서 제미나이 OCR 관련 비용/개인정보 문답 — ZDR(제로 데이터 보관) 필요, 개인 구독(제미나이 어드밴스드)은 API 접근권 미포함이라 별도 결제 프로젝트 필요, 모델은 Gemini 2.5 Flash-Lite가 GPT-4o mini보다 저렴, 스크린샷당 비용은 어느 쪽이든 무시 가능한 수준. **오너 확정: 미국주식 모듈 완성 후 착수.**

46. **🇺🇸 미국주식 모듈 기초틀 구축 (US_STOCKS_WORK_ORDER.md §6의 1~4번 완료)** — 신규 파일 3개만 추가하고 기존 코스피 파일(`collector_kospi200.py`/`views/pegy_view.py`/`utils/scoring.py`/`utils/constants.py`)은 **한 줄도 건드리지 않음**(§0-3-6 모듈 분리).
    - **`collector_us_stocks.py` 신설** — ① 유니버스 CSV 수집·파싱(컬럼 구조가 바뀌면 즉시 RuntimeError, 행 수가 700 미만이면 "소스 갱신 중단 의심"으로 중단), ② 상품유형 분류기 `classify_instrument()`(채권/노트·미국식 우선주·ETF + 오너 규칙 밖 워런트/라이츠/하이브리드유닛/구조화신탁), ③ `filter_universe()` 550개 컷(복수 보통주 클래스는 중복제거 없이 전부 포함, 시총 내림차순 아니면 직접 재정렬), ④ 히스테리시스 버퍼(진입 550/이탈 600, 코스피 200/230 로직 이식), ⑤ stockanalysis.com 통계 페이지 파서(표의 **라벨 텍스트 키워드**로만 값 매칭 — 열 번호 고정 금지 SPEC §2-1 준수, 41개 필드), ⑥ **장마감 종가 전용 파서**(프리마켓/애프터마켓 블록을 절대 쓰지 않음, §0-3-1) + 소스가 알려주는 ET 타임스탬프에서 실제 세션 날짜 확보, ⑦ 무배당 확정("does not appear to pay any dividends" 문장 탐지) vs 수집 실패 구분, ⑧ 두 출처(유니버스 CSV ↔ 통계 페이지) 시가총액 교차검증(§0-3-3), ⑨ 샘플 수집 프로토타입(종목당 소요시간 실측 + 550종목 재추정 + raw/가공 분리 저장), ⑩ CLI 3종(`schedule`/`universe`/`sample`).
    - **`_now_et()` + ET 기준 수집 세션 계산** — `zoneinfo.ZoneInfo("America/New_York")` 기반. "장마감(16:00 ET)+30분"을 ET로 계산하고 KST/UTC 환산과 cron 힌트를 함께 출력. 서머타임(EDT)에는 KST 익일 05:30, 표준시(EST)에는 06:30으로 **날짜·시각이 이동**하는 것까지 테스트로 고정. 미국 휴장일 캘린더는 지어내지 않고 미적용 사실을 `holiday_calendar_applied=False`로 명시하며, **실제 세션 날짜의 출처는 항상 수집한 페이지의 "At close: …" 타임스탬프**로 설계.
    - **`utils/constants_us.py` 신설** — 코스피 상수를 베끼지 않고 미국 시장용으로 새로 작성. 밸류에이션 임계값은 전부 🚧잠정값🚧 + 근거 주석 + "550종목 전수 수집 후 실제 분포로 재캘리브레이션" TODO 명시(TARGET_PER_CAP 35배, ROE 기준선 15%, ROIC 12%, FPER 극단치 100배 등). 시장과 무관한 개념(PEGY 밴드, 목표가 2.5배 캡)만 코스피와 동일값 유지.
    - **`tests/test_us_stocks.py` 신설 (오프라인, 네트워크 불필요) — 74개 체크 전부 통과.** 실제 CSV 종목명 22건으로 분류기 검증(채권 5·우선주 4·워런트/유닛/신탁 4 정확히 분류, "Preferred Bank" 같은 보통주 오판 없음), 550컷·복수클래스 포함·재정렬·소스 가드 검증, 실제 8종목(ROK/BRK.B/CAVA/IBRX/MAC/JPM/ET/PLXS) 페이지 **원문 라벨·값**으로 필드 매핑 검증(적자기업 "Loss Per Share" 음수 수집, 리츠 P/FFO, 은행 ROIC n/a→None, 무배당 n/a→None, 유료(Upgrade) 항목 숫자 오인 방지), 숫자 파서 14케이스(T/B/M 스케일·$·%·음수·쉼표), 프리마켓 함정 HTML로 종가 파서 검증, ET 세션 계산 10케이스.
    - **검증된 실데이터 사실**: 유니버스 CSV 상위 929행 기준 제외 대상은 우선주 26·채권 15·하이브리드유닛 2·워런트 1·구조화신탁 1 = 45건(4.8%)이며, **550위 컷오프는 시가총액 약 $15.2B 부근**(그 구간 안에서 제외되는 건 우선주 19·채권 6·유닛 2 = 27건). stockanalysis.com은 소형주(시총 $7.3B PLXS)까지 PER/Forward PER/PEG/ROE/ROIC/EPS/BPS/배당/목표주가/3년 성장전망을 모두 제공 — 대형주와 커버리지 차이 없음 확인.
    - **못 한 것(정직 기록)**: 이 세션의 샌드박스는 외부 네트워크 직결이 막혀(프록시 403) `requests` 기반 실행을 못 했습니다. 그래서 ① HTML→(라벨,값) 추출 계층과 ② 종목당 실제 소요시간은 **실측하지 못했고**, 대신 페이지 원문으로 검증 가능한 매핑/파싱 계층만 테스트했습니다. 550종목 소요시간은 슬립 상수 기반 추정(약 27~41분)이며, 오너가 로컬에서 `python collector_us_stocks.py sample --count 12` 한 번만 돌리면 실측·재추정이 자동 출력됩니다.

47. **미국주식 로컬 샘플 수집 실행 결과 (오너 로컬 PowerShell, `python collector_us_stocks.py sample --count 12`)** — 유니버스 필터: 원본 4233행(파싱불가/시총결측 1134행 제외 후) → 채권/노트 99·미국식우선주 89·워런트 125·라이츠 1·하이브리드유닛 4·구조화신탁 3 제외 → **최종 550종목, 컷오프 시총 $15.03B**. 샘플 12종목 수집 **12/12 성공**, 종목당 평균 0.432초(중앙값 0.461초, 최대 0.76초) — 정중한 슬립 포함 550종목 재추정 **약 26.9분**(이전 27~41분 추정과 같은 범위, 실측으로 확정). **다만 발견된 이슈**: 테스트가 미국 장중(현지 오전 11시12분, 시장 개장 중)에 실행돼 12종목 전부 "장마감 종가(At close)" 블록을 못 찾고 시총÷주식수 계산값으로 폴백함 — 장마감 전용 파서 자체의 결함인지, 단순히 장중이라 그 블록이 아직 없는 게 정상인지 **미확인**. 장마감(미국 동부 오후4시반) 이후 재실행해서 "At close" 파싱이 정상 작동하는지 반드시 재확인 필요(§0-3-1 후행지표 전용 원칙과 직결 — 장중 계산값을 실수로 쓰면 원칙 위반).

48. **장마감 종가 파서 실제 버그 발견 및 수정 (오너 로컬 2차 재검증, 장마감 후에도 11/12 실패)** — 1차 로컬 검증(장중)에 이어 오너가 지시대로 장마감 후(현지 21:08 ET) `python collector_us_stocks.py sample --count 12`를 재실행했는데도 12개 중 11개가 여전히 "종가 직접 파싱 실패 → 시총÷주식수 계산값 사용"으로 나옴 — 장중 타이밍 문제가 아니라 진짜 파서 버그였음(THC 하나만 우연히 성공). 소넷이 실시간으로 `stockanalysis.com/stocks/nvda/` 실제 페이지를 fetch해 원인 특정: "At close:"(라벨)와 "Aug 6, 2026, 4:00 PM EDT"(값)가 서로 다른 텍스트 노드로 분리되어 있고, 애프터마켓 거래가 있었던 종목(대부분의 활발히 거래되는 대형주)은 그 뒤에 "After-hours: …" 블록이 이어지는 게 실제 레이아웃 — `BeautifulSoup.get_text("\n")`를 거치면 "At close:"만 있는 줄과 날짜만 있는 줄로 쪼개져 기존 한 줄 정규식이 매칭 실패. 애프터마켓 거래가 없었던 THC만 우연히 한 줄로 붙어있어 통과했던 것. `collector_us_stocks.py`의 `extract_close_price()`를 수정 — 마커 줄 자체에 타임스탬프가 없으면 뒤 최대 3줄 안에서 ET 타임스탬프를 재조합해 찾도록 개선(애프터마켓 가격이 아니라 반드시 "At close" 앞의 가격만 사용, §0-3-1 후행지표 원칙 유지). 기존 오프라인 테스트가 이 버그를 못 잡은 이유도 확인 — 테스트 픽스처(`HTML_WITH_PREMARKET`)가 "At close: 날짜"를 한 줄로 합쳐 만들어 실제 페이지 구조(분리된 노드)를 재현 못 했음. `tests/test_us_stocks.py`에 실제 NVDA 구조를 재현한 회귀 테스트(`HTML_AFTERHOURS_SPLIT_LABEL`) 추가, 합성 테스트로 수정 검증 완료(4가지 레이아웃 케이스 전부 통과). **실측 검증 완료** — 오너가 재실행한 결과 12/12 전부 "종가 직접 파싱 실패" 경고 없이 정상 수집 확인(장마감 종가 파서 버그 완전 해결). 550종목 재추정 약 27.1분으로 이전 실측(26.9분)과 거의 동일 — 안정적. 이제 미국주식 모듈 기초틀의 핵심 수집 경로가 실데이터로 검증됨.

49. **미국 종목 유형(워런트/유닛/신탁/MLP/ADR) 오너 확정** — 오너가 용어를 완전히 구분 못 하겠다고 해 하나씩 설명: 워런트·라이츠(옵션형 파생상품, 실적·배당 개념 없음)와 하이브리드유닛(워런트가 섞인 번들상품)과 신탁(세금·법률상 껍데기만 신탁인 사실상의 채권형 우선주)은 전부 "우선주 느낌" 기준에 부합해 제외 확정. MLP(예: ET/MPLX/PAA)는 이름은 "유닛"이지만 실제 사업체의 정상 소유권(법적 형태만 파트너십)이라 "우선주 느낌"이 아니라서 포함 확정 — "유닛"이라는 같은 단어가 SPAC 번들상품과 MLP 소유권 두 가지 완전히 다른 걸 가리키는 용어 함정이었음을 확인. 코드 기본값과 전부 일치해 별도 수정 불필요, `PROJECT_STATUS.md` §8-7 항목 1·2를 ✅ 확정으로 마감.

50. **§8-7 마지막 UI 3항목 오너 확정 — 해외주식 착수 전 확인사항 전부 완료.** ① 상단 지수: S&P500/나스닥종합/다우존스 3개(러셀2000 제외), 2x2 그리드 강제 안 하고 가독성 우선 자유배치. ② 한글 종목명: 널리 쓰이는 정식 한글명 있으면 그대로, 없으면 발음대로 음역(번역이 아닌 기계적 음역이라 §0-1 지어내기 위반 아님, 개별 수정 가능하게 설계). ③ 카드 레이아웃: 코스피와 동일. `PROJECT_STATUS.md` §8-7 5개 항목 전부 ✅ 확정 — 다음은 전수 수집 → 스코어링(`utils/scoring_us.py`) → 화면(`views/us_stocks_view.py`) 착수 단계.

51. **🇺🇸 미국주식 모듈 2단계 — 전수 수집 CLI + 스코어링 엔진 + 화면 + 한글 종목명 (오푸스)** — §8-7 오너 확정 5개 항목을 전부 반영해 기초틀 위에 실제 파이프라인을 올렸습니다. 기존 코스피 파일(`collector_kospi200.py`/`utils/scoring.py`/`utils/constants.py`/`views/pegy_view.py`)은 **한 줄도 수정하지 않았습니다**(§0-3-6).
    - **`utils/scoring_us.py` 신설** — 코스피 `utils/scoring.py`의 구조(2차 패스 횡단면 z-score + 윈저라이즈, 앵커점 선형보간, "수집 못 한 항목은 배점에서 제외")만 물려받고 **상수는 전부 `utils/constants_us.py`에서만** 가져옵니다(코스피 constants.py를 import 하지 않음). 배점 구성도 미국 데이터에 맞게 재설계: PEGY 35 + 자본효율성(ROE 15 + ROIC 15) + 주주환원 20 + 재무건전성(피오트로스키 F-Score) 10 + 변동성(베타) 5. ⚠️ 코스피의 "Trailing 안정성 10점(=Trailing ROE)"을 그대로 베끼면 자본효율성 카테고리와 **같은 지표를 두 번 계상**하게 되어 F-Score로 교체했고, 코스피의 20일 실측 변동성 벌점(g_eff 분모에 곱하기)은 미국 쪽에 시계열이 없어 **베타를 점수 항목으로만** 반영했습니다(근거를 댈 수 없는 배수를 PEGY·목표가에 곱하지 않음). Forward EPS는 소스가 직접 주지 않아 `장마감 종가 ÷ Forward PER`로 역산하되 §0-1 예시2-보충 4개 조건을 명시하고 `f_eps_calculated`/`f_eps_source` 마킹 + 화면 "🧮 계산값" 배지를 붙였습니다. 업종 특수 케이스(적자기업 PER n/a·리츠 Price/FFO·은행 ROIC n/a)는 수집기가 이미 구분해 둔 필드를 그대로 활용해 "수집 실패"가 아니라 "소스가 원래 안 주는 값"으로 정직하게 표기합니다. 미국판 가드레일(`apply_us_guardrail`)도 함께 신설 — 카드를 못 그리는 2가지(종가 없음/발행주식수 오염)만 하드 블록하고 나머지는 Forward 섹션만 마스킹.
    - **`collector_us_stocks.py`에 `collect` 전수 수집 CLI 추가** — 코스피 패턴 그대로 ① 직전 스냅샷에서 추적 티커 로드 → ② 유니버스 필터 + 히스테리시스(550/600) → ③ 상단 지수 3종 → ④ 1차 패스 종목 수집·파생계산·가드레일 → ⑤ **2차 패스**(population 통계 계산 후 일괄 스코어링) → ⑥ raw/가공 스냅샷 분리 저장(§0-3-3) + 요약 이력. 실패 종목은 조용히 건너뛰지 않고 `metadata.failed_tickers`에 사유와 함께 남기며, 소스 차단(403/429) 시 즉시 중단하고 **기존 스냅샷을 덮어쓰지 않습니다.** `--limit N`(동작 확인용)·`--skip-indices` 옵션과 지수 소스 점검용 `indices` 서브커맨드도 추가.
    - **`views/us_stocks_view.py` 신설** — 코스피 카드 레이아웃과 동일 구조(헤더 배지 줄 / 자본효율성 바 / Trailing 섹션 / 그레이엄 넘버 / Forward 섹션 + 마스킹 분기 / 페이지네이션). 오너 확정 반영: 상단 지수 3개를 2x2 강제 없이 flex 자유배치, 한글명+영문명+티커 동시 표기, **USD 단독 표기(₩·"원" 문구 0건 자동 검증)**, 페이지네이션 30개. 미국 전용 추가 요소: 애널리스트 컨센서스 목표주가(소스 실측)를 우리 모델 목표가와 **별도 줄로 나란히** 노출(둘이 다른 값임을 툴팁에 명시), 리츠/베타/F-Score 배지.
    - **`utils/company_names_kr.py` 신설 (하이브리드 한글 표기)** — 시가총액 상위권 위주 정식 한글명 사전 약 190개(티커 키) + 규칙 기반 음역 함수. 오너가 개별 종목을 고칠 때는 **`KR_NAME_OVERRIDES`에 티커 한 줄만 추가**하면 되고(사전이 항상 음역보다 우선), 음역 결과에는 화면에 "음역" 배지가 붙어 정식 표기와 구분됩니다. 음역기는 영어 철자↔발음이 1:1이 아니라 **완벽할 수 없다는 한계를 모듈 상단에 명시**했습니다(예: Sunoco→수노코 ✓, Plexus→플레크서스(실제 플렉서스) ✗).
    - **`visiblehand.py` 사이드바 연동** — §0-3-6에 따라 공개 메뉴에 올리지 않고 **관리자 인증 후에만** 보이는 "🇺🇸 미국주식 (베타, 검토중)" 체크박스로 진입. 비로그인 공개 경로(`render_pegy_page()`)는 그대로입니다.
    - **검증(정직 기록)**: 이 세션 샌드박스도 외부 네트워크가 프록시 차단(403)이라 **실제 550종목 수집은 실행하지 못했습니다.** 대신 `tests/test_us_scoring.py` 신설로 ① 오너가 로컬에서 받아온 **실데이터 12종목**(`data/us_sample/`)을 파생계산→가드레일→2차 패스 스코어링 전 구간에 통과시켜 확인(SUN 역성장 -0.26%p → PEGY 미산출+역성장 배지+만점 65, INCY 주주환원율 -3.09% 음수 그대로 반영 등), ② 합성 데이터로 극단 케이스 9종(적자·리츠·은행 ROIC n/a·성장률 150% 캡·목표가 2.5배 캡·PER 500배 오염·종가 결측·Forward 결측·population 표본 부족), ③ **`_http_get`만 가짜로 바꾼 end-to-end 배선 테스트**로 `run_us_collector()` 전 구간(유니버스 필터→수집→스코어링→raw/가공 분리 저장→히스테리시스 재로드)을 실제 코드 경로 그대로 1회 완주, ④ streamlit 스텁으로 화면 렌더링 무크래시 + HTML 태그 균형(div/span) + 원화 표기 0건 확인, ⑤ 전체 `py_compile`. **총 78개 체크 전부 통과.** 기존 `tests/test_us_stocks.py`(74건)·`tests/test_quant.py`도 회귀 없음 재확인.
    - **못 한 것(정직 기록)**: ① 실제 550종목 전수 수집은 **오너가 로컬에서 `python collector_us_stocks.py collect`를 직접 실행**해야 합니다. ② 상단 지수 3종의 데이터 소스(Stooq EOD CSV)는 **응답을 한 번도 받아보지 못해 파서 로직만 검증**된 상태입니다 — `python collector_us_stocks.py indices`로 먼저 확인 필요. ③ 지수는 현재 **단일 출처**라 §0-3-3(2개 이상 출처)을 아직 못 지켰습니다(2차 출처를 지어내지 않고 비워둠). ④ `utils/constants_us.py`의 밸류에이션 임계값은 여전히 잠정값이라, 첫 전수 수집 분포를 보고 재캘리브레이션해야 합니다.

52. **🇺🇸 상단 지수 3종 소스 교체 (Stooq HTTP 404 → stockanalysis.com ETF 프록시) + 전수수집 429 차단 대응.** 오너 로컬 실행에서 두 가지 실측 문제가 나와 둘 다 수정했습니다.
    - **지수 소스 교체**: `python collector_us_stocks.py indices`/`collect` 양쪽에서 Stooq CSV(`^spx`/`^ndq`/`^dji`) 3종 전부 HTTP 404 확인 — 무료·무인증이라 골랐던 엔드포인트가 실제로는 죽어있었음. 대안으로 stockanalysis.com의 ETF 페이지(`/etf/spy/`, `/etf/oneq/`, `/etf/dia/`)를 실측 확인 — 개별 종목 통계 페이지와 완전히 동일한 "At close: …" 구조라 **이미 검증된 `extract_close_price()`/`extract_label_value_pairs()`를 그대로 재사용**해 `fetch_one_index_quote()`로 교체. 오너 확정 2가지 반영: ① ETF 주가(예: SPY $768)는 실제 지수 포인트값(S&P500 ≈6,598)과 다른 별개 숫자라 **화면에 절대가를 아예 노출하지 않고 당일 등락률(%)만** 표시(`views/us_stocks_view.py` `render_index_header()` 재작성, "ETF(심볼) 등락률 기준 — 실제 지수 포인트값이 아닌 근사치" 안내 문구 추가). ② 나스닥종합은 QQQ(나스닥100 추종, 다른 지수)가 아니라 ONEQ(Fidelity Nasdaq Composite Index ETF, "Index Tracked: NASDAQ Composite Index" 페이지에서 직접 확인)를 사용. 소스가 나중에 바뀌어도 조용히 틀린 지수를 보여주지 않도록 `tracked_index_verified`(페이지의 "Index Tracked" 라벨이 기대 문구를 포함하는지)를 매번 재확인해 메타데이터에 남김. `tests/test_us_stocks.py`에 `test_index_proxy()` 신설(정상 케이스·엉뚱한 지수로 소스가 바뀐 경우·요청 자체 실패 케이스 3종, 실제 ONEQ 실측값 104.78/전일종가 102.07/등락률 2.66%로 검증), `tests/test_us_scoring.py`의 end-to-end 배선 테스트도 ETF 프록시 페이지를 가짜로 제공하도록 갱신 — 전체 재실행 결과 회귀 없음 확인(offline 84+81건 전부 통과), 화면 렌더링도 절대가 미노출·₩/원 미노출 스모크 테스트로 확인.
    - **전수수집 HTTP 429 차단 대응**: 오너가 `python collector_us_stocks.py collect`(550종목 전수)를 처음 실행했을 때 187종목째(약 11분 경과)에 stockanalysis.com이 HTTP 429로 차단 — §0-3-2 원칙대로 재시도 없이 즉시 중단하고 기존 스냅샷은 보존됨(설계대로 정상 동작, 데이터 손상 없음). 다만 종목당 슬립(2~3초)만으로는 부족했던 것으로 보여, `utils/constants_us.py`에 `US_CRAWL_BATCH_SIZE`(60)·`US_CRAWL_BATCH_COOLDOWN_SEC`(90) 신설 — 60종목마다 90초 추가 휴지기를 두는 `_polite_sleep_with_batch_cooldown()`으로 교체해 총 요청 밀도를 낮췄습니다. ⚠️ stockanalysis.com의 정확한 rate limit 정책(임계값·시간창)은 비공개라 이 값은 "추정 완화책"이지 재발 방지를 보장하지 않는다는 점을 코드 주석에 명시 — 다시 차단되면 배치 크기를 더 줄이거나 휴지기를 늘려야 함. 550종목 소요시간 추정치를 27분→약 40분으로 갱신.
    - **부수 관찰(수정 불필요)**: 같은 로그에서 CZZ(#53) 1종목이 HTTP 404로 개별 실패(다른 549종목과 무관, 유니버스 CSV상 이미 상장폐지/티커변경된 종목으로 추정) — 기존 `failed_tickers` 로직이 설계대로 조용히 건너뛰지 않고 사유와 함께 기록해 정상 동작 확인, 별도 조치 불필요.
    - **다음 확인 필요**: 오너가 `python collector_us_stocks.py indices`(3종 단독)와 `collect`(550종목 전수, 배치 휴지기 포함 약 40분)를 로컬에서 재실행해 실제로 429 없이 완주되는지, 지수 3종 등락률이 정상 표시되는지 확인 필요.

53. **오너 로컬 재실행에서 드러난 2가지 잔여 문제 수정 — "Index Tracked" 라벨 미검출 + HTTP 429 재차단(290/550).** #52 수정을 오너가 실제로 재실행하며 바로 발견한 2건.
    - **"Index Tracked" 검증 경고가 매번 뜸(등락률 자체는 정상)**: `indices` 실행 로그에서 3종 전부 등락률은 정확히 나왔지만("S&P 500 등락률=-0.16%") "'Index Tracked' 라벨이 기대 문구를 포함하지 않음(실제: 'None')" 경고가 붙어 나옴. 원인 확인: "Index Tracked"는 Previous Close 등이 들어있는 `<table>` 통계 박스가 아니라 그 아래 "About {ETF명}" 서술 섹션에 있어(표 밖 레이아웃), 표 전용인 `extract_label_value_pairs()`로는 애초에 찾을 수 없는 위치였음. 표 밖 "라벨(+같은 줄 또는 다음 줄 값)"을 찾는 `_scan_line_label()`을 신설해 표 검색이 실패하면 폴백으로 재시도하도록 수정. **테스트 자체의 결함도 함께 확인**: 최초 작성한 오프라인 테스트 픽스처가 실수로 "Index Tracked"를 `<table>` 안에 넣어 만들어서 이 버그를 못 잡았던 것 — `tests/test_us_stocks.py`의 픽스처를 표 밖(같은 줄 결합형 + 다른 줄 분리형 2가지) 레이아웃으로 다시 만들어 재현·검증.
    - **550종목 전수수집이 이번엔 290종목째에서 다시 HTTP 429**(60종목마다 90초 휴지기 적용 후에도 재발, 다만 이전 187종목보다는 늦게 걸림). 정확한 rate limit 규칙을 모르는 채로 휴지기 값만 계속 키우는 건 도박이라, **체크포인트 기반 이어하기**를 주 대응책으로 신설: 차단되면 그때까지 완료된 종목(raw/가공 결과 + 실패 목록)을 `data/us_collect_checkpoint.json`에 저장하고, **다음 실행이 처음부터 다시 하지 않고 이어서** 남은 종목만 이어받습니다(같은 세션 날짜에서만 재사용 — 날짜가 다르면 어제 종가와 오늘 종가가 섞일 수 있어 재사용하지 않고 새로 시작, §0-1). 완주하면 체크포인트를 정리합니다. 배치 휴지기 값도 보조로 한 번 더 조정(60종목/90초 → 40종목/120초). `tests/test_us_scoring.py`에 3번째 종목에서 강제 차단시키는 end-to-end 시나리오 추가 — ① 차단 시 체크포인트 저장, ② 재실행 시 이미 완료된 종목은 재요청 없이 남은 것만 이어서 완주, ③ 완주 후 체크포인트 정리, ④ 다른 세션 날짜 체크포인트는 재사용 안 함 — 4가지 전부 실제 `run_us_collector()` 경로로 검증 완료.
    - 전체 오프라인 테스트(`test_us_stocks.py`/`test_us_scoring.py`/`test_quant.py`) + 전체 `py_compile` 재확인, 회귀 없음.
    - **다음 확인 필요**: 오너가 `python collector_us_stocks.py indices`(경고 없이 3종 다 나오는지)와 `python collector_us_stocks.py collect`(429로 끊기면 그대로 다시 같은 명령을 실행 — 체크포인트에서 이어지는지, 최종적으로 550종목 완주되는지)를 로컬에서 재확인.

54. **🇺🇸 미국주식 550종목 전수 수집 실측 성공 (#53 수정 반영 후 오너 로컬 재실행).** `python collector_us_stocks.py collect` 완주 확인 — 548종목 추적, 검증 통과 547/548(99.8%), 상태 SUCCESS, 소요 55.5분. 이번엔 HTTP 429 차단이 아예 발생하지 않음(배치 휴지기 보강 40종목/120초 효과로 추정, 체크포인트 이어하기는 이번엔 쓸 일이 없었음 — 다음에 또 차단되면 그때 검증됨). 지수 3종("Index Tracked" 라벨 폴백 수정 반영)도 경고 없이 등락률 정상 표시(S&P500 -0.16%, 나스닥종합 -0.04%, 다우존스 -0.85%) — #53의 두 수정 모두 실측으로 확인됨. 개별 실패 2종목(CCZ, BTSGU, HTTP 404)은 상장폐지/티커변경 추정으로 실패 목록에 정상 기록, 나머지 진행에 영향 없음. `data/us_stocks_latest.json`/`us_stocks_raw_latest.json`/`us_summary_history.json` 생성 확인. **다음은 화면 육안 확인 + 이번 실제 분포로 `utils/constants_us.py` 임계값 재캘리브레이션 + 음역 품질 검토 + 공개 전환 시점 결정.**

55. **🇺🇸 미국주식 임계값 실측 재캘리브레이션 (`utils/constants_us.py` §5, 2026-08-07 밤).** #54의 실제 548종목 스냅샷(`data/us_stocks_latest.json`) 분포를 근거로 잠정값(TODO)들을 확정값으로 전환. 대부분(PER 오염선 300배, Forward PER 하드컷 100배, 목표가 PER 캡 35배, ROE/ROIC value-trap 기준선 9%/6.5%, 베타 severity cap 2.0, 고성장 보수반영 100%p, valid_ratio 정책값)은 실측 분포와 대조한 결과 이미 합리적으로 걸려있어 조정 없이(또는 소폭만) 확정. **가장 중요한 발견**: PEGY 하드컷오프(피터 린치 원전 해석 그대로 2.0을 코스피와 동일하게 써왔음)가 미국 시장에서는 실제 p75(2.02)와 거의 같아, 전체 548종목 중 115개(21%)가 전부 "🔴 극단적 고평가(위험)" 배지를 받는 문제를 실측으로 발견 — "극단적" 배지치고 비율이 너무 높음을 오너에게 보고, **미국만** 3.0으로 상향하기로 확정(코스피 2.0은 시장 규모·분포가 다르다는 오너 판단으로 그대로 유지). 3.0 기준으로는 47개(8.6%)만 걸려 "상위 1할 안팎의 진짜 극단치"라는 배지 취지에 맞음을 확인. ROE 기준선도 실측 중앙값(16.0%)에 맞춰 15%→16%로 소폭 조정. 전체 재컴파일·오프라인 테스트(`test_us_scoring.py`/`test_us_stocks.py`/`test_quant.py`) 회귀 없음 확인. ⚠️ **주의**: 이 조정은 다음 `collect` 재실행부터 반영됩니다 — 지금 디스크에 있는 `us_stocks_latest.json`은 여전히 옛 2.0 기준으로 계산된 배지를 담고 있어, 화면에서 실제로 확인하려면 재수집이 필요합니다.

56. **🇺🇸 한글 음역 품질 검토 + 엔진 버그 수정 + 정식 한글명 사전 확충 (`utils/company_names_kr.py`, 2026-08-07 밤).** 실제 548종목의 음역 결과(360개) 전수를 육안 검토. **근본 원인 발견**: "er"(예: Western→웨스턴)이 음역기의 모음 규칙에 아예 없어서 단독 'e'(ㅔ) 규칙이 먼저 걸려버리고, 그 결과 뒤따르는 자음(주로 n)이 받침으로 못 붙어 "웨스테느"처럼 부자연스러운 결과가 반복적으로 나왔던 것 — 개별 종목 문제가 아니라 엔진 자체의 구멍이었음. `_VOWELS` 규칙 테이블에 "er"/"ir" → "ㅓ" 항목을 추가하는 것만으로 Western Digital(웨스테느디지털→웨스턴디지털), Kroger(크로게→크로거), Dover(도베→도버), Zimmer Biomet(지메비메트→지머비메트), Charter Communications(차테커뮤니케이션스→차터커뮤니케이션스) 등 68개 종목의 음역이 한 번에 개선됨(오프라인 테스트 회귀 없음 확인). "ar"/"or"/"ur"는 이미 기존 단모음+r생략 규칙으로 정상 동작해 손대지 않음. 엔진 수정 후에도 남아있는, **세계적으로 잘 알려진 브랜드인데 여전히 부정확한 경우**(예: Cloudflare→클라드플라, SpaceX→스파스에크스플로라션테크놀로지스, Nasdaq→나스다, eBay→에베, Warner Bros. Discovery→와네브로스디스코베리 등) 48개는 `KR_NAME_OVERRIDES`에 정식 표기로 직접 등재(스페이스X/클라우드플레어/로빈후드/워너브라더스디스커버리/나스닥/이베이/몽고디비/로쿠/로지텍/할리버튼/톰슨로이터스/큐리그닥터페퍼/짐머바이오멧/듀폰/트랜스유니온 등). 결과: 정식 한글명 사전 등재 비율 34.3%(188/548)→43.1%(236/548)로 상승, 나머지 312종목은 자동 음역이지만 엔진 개선으로 전반적 품질 상향. 더 깊은 잔여 부정확 사례(th 이중자음 근사치 한계로 인한 Southern→사선/서던 등)는 개별 단어 조합의 영어 철자 불규칙성 문제라 근본적으로 완벽할 수 없음을 재확인 — 오너가 `KR_NAME_OVERRIDES`에 원하는 대로 한 줄씩 추가하는 기존 설계를 계속 활용하면 됨. 전체 오프라인 테스트(`test_us_scoring.py`/`test_us_stocks.py`/`test_quant.py`) + 전체 `py_compile` 회귀 없음 확인. ⚠️ 이 변경도 다음 `collect` 재실행부터 화면에 반영됩니다(기존 스냅샷은 옛 음역 그대로).

57. **🇺🇸 "고평가/주의 종목 그룹" 빠른 필터가 345/548(63%)로 과다 표시되던 버그 수정 (`views/us_stocks_view.py`).** 오너가 실제 배포 화면에서 "극단적 고평가" 배지는 30개뿐인데 프리셋은 345개를 보여준다고 지적 — 배지별 실측 분포로 산술 검증 후(235+30+33+12+1+33+1=345, 정확히 일치) 원인 확정: 프리셋의 키워드 OR매칭(`"고평가"|"역성장"|"위험"|"검증"`)에서 "검증"이 "PER 검증 실패"(경고, 정상 매칭) 뿐 아니라 "🔵 Trailing만 검증됨(Forward 데이터 없음)"(단순 데이터 가용성 안내, 33개)과 "⚠️ 데이터 검증 필요"(fallback 안내, 1개)까지 잘못 끌어옴 — 이 둘은 밸류에이션 경고가 아니라 데이터 상태 안내라 34개가 오분류. 해당 두 뱃지를 명시적으로 제외하도록 수정, 올바른 카운트는 311개. 커밋·푸시 완료.

58. **🇺🇸 "모델 목표가"가 저성장 자본집약형 우량주(예: 버크셔해서웨이 B)에서 구조적으로 비정상 저평가 나오는 문제 — BPS 참고 바닥값 도입 (`utils/scoring_us.py`, `views/us_stocks_view.py`).** 오너가 버크셔 카드에서 "모델 목표가 $68.87"(현재가 $524.61 대비 87% 프리미엄, 극단적 고평가 배지)를 보고 이상함을 지적. 원인: 목표주가 공식(목표PER = (1+ROE/ROIC프리미엄) × 실효성장률g_eff, 목표가 = Forward EPS × 목표PER)이 성장률로 적정 배수를 매기는 Peter Lynch식 PEG=1 모델이라, 버크셔처럼 g_eff가 거의 0(3.31%p)인 보험/지주/유틸리티류 우량주에서는 ROIC가 우량해도(17.96%) 목표PER이 3.35배까지 눌려버림 — 공식 자체의 구조적 한계(임계값 문제가 아님). 오너가 화면에 이미 표시되던 "PBR 기준 바닥가"(=BPS, $338.46)를 실제 하한으로 써야 하지 않냐고 제안 → 전 종목 일괄 적용 시 기존 "착시 저평가(value trap)" 배지 로직과 정면 모순(부실자산 종목의 부풀려진 장부가에 잘못된 바닥을 깔아줄 위험)임을 짚어 오너와 합의 후, **ROE·ROIC 둘 다 실측되고 value trap 기준선을 통과한 종목에 한해서만**(`floor_price_eligible` 게이트) f_target/t_fair에 BPS를 참고 하한으로 적용하도록 구현(PANW 등 value trap 종목은 실측으로 미적용 확인). "청산가치"라는 확정적 표현은 쓰지 않고 "장부가 기준 참고 바닥값(실사 아님)"으로 톤 조정. 화면에 적용 시 파란색 배너 + 목표가 옆 배지로 크게 표시(오너 요청). ⚠️ US 전용, 코스피 미적용(§0-1 시장별 분리). 실데이터(버크셔) 재계산으로 68.87→338.46 확인, 오프라인 테스트 회귀 없음. 극단적 고평가 배지는 별도 PEGY 하드컷오프 로직이라 이 수정과 무관하게 그대로 유지(버크셔 PEGY 7.72는 여전히 US 기준 3.0 초과). 다음 `collect` 재실행부터 화면 반영.

59. **git 히스토리 옛 관리자 비밀번호 제거 (`git filter-repo`, task #10, 오래 미뤄온 항목 착수·완료).** `AUDIT_REPORT.md`가 지적한 대로 커밋 `80bb132`(`views/admin_view.py`)에 평문 비밀번호(`MOONvisiblehand9118`)와 그 SHA-256 해시가 주석으로 남아있던 문제. 공개 저장소라 진짜 안전장치는 비밀번호 자체를 바꾸는 것이지 히스토리 정리는 사후 청소일 뿐임을 오너에게 먼저 확인 — 오너가 이미 다른 값으로 로테이션해둔 것 확인 후 진행. 절차: ① 전체 폴더 robocopy 백업(안전장치), ② `pip install git-filter-repo`(설치 스크립트가 PATH에 안 잡혀 `set PATH`로 임시 추가해 해결), ③ `git filter-repo --replace-text`로 옛 비밀번호·해시 문자열을 전체 히스토리(214개 커밋)에서 치환·제거(파일 자체는 유지, 문제의 그 줄만 치환), ④ 재작성 후 히스토리 전수 검색으로 잔존 0건 확인, ⑤ `origin` 재연결 후 `git push --force`로 GitHub 반영. 브랜치가 `main` 하나뿐이고 다른 클론이 없는 프로젝트라 강제 푸시 부작용 없음. 오너가 명령어를 직접 실행(어시스턴트는 명령만 제공, 기존 관례 유지).

60. **🇺🇸 미국주식 수집 GitHub Actions 자동화 연결 (`.github/workflows/scrape_us.yml` 신설 + 수집기 `--skip-if-not-ready` 방어 로직, 2026-08-07 밤).** 지금까지 미국주식 550종목은 오너가 매번 로컬 PC에서 손으로 `python collector_us_stocks.py collect`를 돌려야 했습니다(코스피는 이미 `scrape.yml`로 무인 자동화 중). 오너가 "자동화부터 먼저, 그 다음 공개 전환"으로 확정해 이번엔 자동화만 붙였습니다.
    - **핵심 난제 — 서머타임**: 우리가 수집해야 하는 시각은 언제나 "미국 정규장 마감 16:00 ET + 30분 = 16:30 ET"인데, GitHub Actions의 cron은 UTC 고정이라 서머타임 전환 때마다 1시간씩 어긋납니다. 크론 시각을 추측으로 넣지 않고 `describe_collection_schedule()`을 2026-07-15(EDT)/2026-01-15(EST) 시각으로 **실제 호출해 `collect_ready_at_utc`를 확인**했습니다 — EDT(UTC-4) → **20:30 UTC**, EST(UTC-5) → **21:30 UTC**(§0-1). 기존 `scrape.yml` 관례(정각 혼잡 회피 +5분)를 따라 cron을 `35 20 * * 1-5` / `35 21 * * 1-5` 두 줄로 걸었습니다.
    - **가장 중요한 발견 — 기존 `is_ready_now` 플래그는 방어 로직으로 쓸 수 없음**: 코드 주석이 권장하던 방식은 "크론 2개 + 스크립트가 ET 기준으로 아직 마감+30분 전이면 종료"였고, `resolve_collection_session_et()`가 이미 `is_ready_now`를 제공하고 있어 그걸 재사용하면 되는 줄 알았습니다. 그런데 코드를 실제로 읽고 여러 시각으로 호출해보니 그 함수는 **"마감+30분이 지난 가장 최근 평일"을 찾을 때까지 하루씩 거슬러 올라가는** 구조라, 루프 탈출 조건 자체가 `now >= ready_at`입니다 — 즉 `is_ready_now`는 **어떤 시각을 넣어도 항상 True**인 사실상 무의미한 필드였습니다(장중 09:35 ET로 호출해도 False가 되지 않고 그냥 '어제 세션'을 가리킴). 없는 기능을 있다고 가정하고 그 위에 워크플로우를 세웠으면 EDT 기간에 하루 두 번 550종목을 중복 크롤링할 뻔했습니다(§0-1 지어내지 않기 / §0-3-2 크롤링 매너 양쪽 위반).
    - **실제 채택한 방어 로직**: `collector_us_stocks.py`에 `snapshot_covered_session_dates()`(기존 스냅샷이 이미 담고 있는 거래일 집합을 `metadata.session_dates_from_source` + `metadata.session_hint.session_date`에서 읽음)와 `evaluate_collection_readiness()`를 신설하고, `collect` 서브커맨드에 `--skip-if-not-ready` 옵션을 추가했습니다. 판정 기준은 **"이번에 담기게 될 거래일(session_date)을 기존 스냅샷이 이미 담고 있는가"** — 이미 있으면 아무것도 하지 않고 정상 종료(exit 0)합니다. 이 기준 하나로 서머타임 양쪽이 자동으로 맞습니다: [EDT] 20:35 UTC=16:35 EDT → 대상=당일, 스냅샷엔 전일까지 → **수집** / 21:35 UTC=17:35 EDT → 방금 넣은 당일이라 → 건너뜀. [EST] 20:35 UTC=15:35 EST(아직 장중) → 대상=전 거래일, 이미 있음 → 건너뜀 / 21:35 UTC=16:35 EST → 대상=당일, 아직 없음 → **수집**. 옵션을 주지 않으면 종전과 100% 동일하게 동작하므로 오너가 로컬에서 손으로 돌릴 때(같은 날 재수집 포함)는 아무 영향이 없습니다.
    - **부수 효과 2가지(설계 의도에 포함)**: ① 앞쪽 크론이 HTTP 429로 중간에 끊기면 스냅샷이 갱신되지 않으므로 **뒤쪽 크론이 같은 날 자동으로 재시도**합니다 — #53에서 만든 체크포인트(`data/us_collect_checkpoint.json`)에서 이어받아 처음부터 다시 하지 않습니다. ② 오너가 로컬에서 이미 수집해 푸시해둔 날은 액션이 같은 데이터를 다시 크롤링하지 않습니다(§0-3-2).
    - **안전 원칙 — "모르면 건너뛰기"가 아니라 "모르면 수집한다"**: 스냅샷 파일이 없거나 / JSON이 깨졌거나 / `metadata.status`가 `FAILED`면 '이미 수집됨'으로 치지 않고 수집을 진행합니다. 건너뛰면 데이터가 하루 통째로 비지만, 수집하면 최악이라도 중복 크롤링 1회이기 때문입니다.
    - **워크플로우 파일(`.github/workflows/scrape_us.yml`, 신규)**: 기존 `scrape.yml`은 한 글자도 건드리지 않고 **별도 파일로 분리**(§0-3-6 모듈 분리 — 이미 잘 도는 코스피 자동화를 다치지 않게). 담은 것 — cron 2줄 / `workflow_dispatch` 수동 실행 버튼(+`force` 체크박스: 사전 점검을 무시하고 같은 거래일도 다시 수집) / `timeout-minutes: 90`(오너 로컬 실측 55.5분 + 429 재시도 여유, GitHub 기본 6시간은 사고 시 점유가 큼) / `concurrency: {group: us-stocks-collect, cancel-in-progress: false}`(수집이 60분을 넘겨 두 크론이 겹치는 경우 뒤 실행을 취소하지 않고 대기시켜 중복 크롤링 방지) / 커밋은 `git add -A data/`(완주 시 체크포인트 파일이 **삭제**되는데 그 삭제까지 커밋해야 다음 실행이 유령 체크포인트를 물려받지 않음) + 봇 이름 `github-actions[bot]` + `scrape.yml`과 동일한 "푸시 거부되면 fetch+rebase 후 최대 3회 재시도" 블록 그대로 재사용. **시크릿은 하나도 넣지 않았습니다** — 미국주식 수집기 코드를 전수 확인한 결과 AI 코멘트 기능이 없어 `GEMINI_API_KEY`가 불필요하고, 외부 패키지도 `requests`/`beautifulsoup4` 둘뿐입니다(그래도 코스피와 같은 관례를 위해 `-r requirements.txt`를 그대로 씀).
    - **검증**: ① 전체 `py_compile`(수집기·테스트·utils·views 전부) 통과. ② `tests/test_us_stocks.py`에 `[6] 무인 자동화 사전 점검` 12건 신설 — 스냅샷 없는 첫 실행/EDT 두 크론/EST 두 크론을 **실제 워크플로우 크론 시각(20:35·21:35 UTC)을 UTC→ET 변환해** 시뮬레이션, 앞 크론 실패 시 재시도, `status=FAILED` 재수집, 깨진 JSON 안전 처리, 주말 실행, CLI 옵션 배선까지 전부 확인. ③ 기존 오프라인 테스트 3종(`test_us_stocks.py`/`test_us_scoring.py`/`test_quant.py`) 전부 재실행해 회귀 없음(체크포인트 이어하기 end-to-end 포함). ④ 실제 CLI를 진짜 데이터 폴더에 대고 `collect --skip-if-not-ready`로 돌려 **exit 0으로 조용히 종료하고 `data/` 아래 어떤 파일도 건드리지 않음**을 확인(현재 ET 04:45 기준 대상 세션 2026-08-06이 이미 스냅샷에 있어 정상적으로 건너뜀). ⑤ 두 워크플로우 YAML을 `yaml.safe_load`로 파싱해 문법·크론·타임아웃·스텝 구조 확인, 시크릿 참조 0건 확인. ⚠️ **참고**: 이 프로젝트의 테스트 3종은 `unittest.TestCase`가 아니라 스크립트형(`python tests/xxx.py`)이라 `python -m unittest discover`로는 0건이 수집됩니다 — 반드시 스크립트로 직접 실행해야 합니다.
    - **아직 검증 못 한 것(정직 기록, §0-1)**: 이 워크플로우가 **GitHub 서버에서 실제로 돈 적은 아직 없습니다.** 오너가 푸시한 뒤 Actions 탭에서 `Run workflow`로 한 번 수동 실행해 실측 확인이 필요합니다. GitHub Actions 러너에서의 실제 소요시간도 미측정이며(오너 로컬 40~56분 기준으로 90분 타임아웃), 러너 IP에서 stockanalysis.com이 HTTP 429를 더 자주 낼지도 아직 모릅니다 — 그래서 체크포인트/재시도 구조를 그대로 살려뒀습니다.
    - **남은 것**: 미국주식 페이지는 이제 **공개 메뉴 전환(§0-3-6) 하나만** 남았습니다(이번 작업 범위 아님, 오너 승인 후 `visiblehand.py` 사이드바 수정).

61. **🇺🇸 미국주식 화면 공개 메뉴 전환 — ENGINEERING_SPEC §0-3-6 스테이징 절차 종료 (2026-08-08, 오너 승인).** 2026-08-06 착수 이후 관리자 인증 후에만 볼 수 있던 "🇺🇸 미국 주식은 이가격" 화면을, 오너의 명시적 승인으로 일반 방문자에게도 열었습니다. §0-3-6이 요구하는 "오너의 승인/허가"가 이번 승인입니다.
    - **전제조건 재검증(추측 없이 코드/데이터로 확인, §0-1)**: ① **임계값 재캘리브레이션 확정** — `utils/constants_us.py` §5의 항목이 전부 "✅ 확정(2026-08-07 실측)" 표기와 실측 근거 수치를 달고 있음을 직접 읽어 확인(#55). ② **GitHub Actions 자동화 연결** — `.github/workflows/scrape_us.yml` 존재 확인에 더해, **실제로 GitHub 서버에서 돌아간 증거**를 확인했습니다: 봇 커밋 `0907c19`/`55ba92e` "US Auto Update: 2026-08-07 [무인 자동화 로봇]" 2건, 그리고 그 결과물인 `data/us_stocks_latest.json` 메타데이터(`last_updated_at_et 2026-08-07 18:05`, `status=SUCCESS`, 548종목, `valid_ratio 0.998`). #60 기록 시점에는 "GitHub 서버에서 실제로 돈 적 없음"이 미검증 항목으로 남아 있었는데, 그 사이 실전 실행으로 해소된 것을 확인했습니다. → 두 전제조건 모두 충족, 공개 전환 진행.
    - **`visiblehand.py` — 메뉴를 관리자 게이트 밖으로 이동**: 기존에는 `admin_mode = render_admin_sidebar()` **뒤에** `if admin_mode:` 로 감싸 체크박스를 만들고, 라우팅도 `if admin_mode and view_us_stocks:` 였습니다. 이제 체크박스를 사이드바 **상단 공개 메뉴 자리**(2026-08-06에 "해외주식 페이지 착수 시 진짜 메뉴로 채울 예정"이라고 코드 주석에 예고해둔 바로 그 빈 자리)로 옮기고, 인증 여부와 무관하게 항상 렌더링합니다. 라우팅은 `if view_us_stocks:` 로 단순화. 위젯 종류(체크박스)와 배치 관례(메뉴 → `st.sidebar.markdown("---")` → 관리자 로그인)는 이 프로젝트가 예전에 쓰던 공개 메뉴 패턴을 그대로 따랐습니다(새 패턴을 만들지 않음). session_state 키도 의미에 맞게 `admin_view_us_stocks` → `view_us_stocks` 로 변경. 라벨은 "🇺🇸 미국주식 (베타, 검토중)" → **"🇺🇸 미국 주식은 이가격 (미국 밸류에이션 리포트)"** 로 코스피 공개 메뉴 라벨 톤에 맞춰 정리하고, 도움말도 "오너 검토 후 공개 전환 예정" 문구를 빼고 "체크 해제하면 코스피 화면으로 돌아옵니다" 안내로 교체.
    - **⚠️ 매크로 방공망은 그대로 비공개**: `elif admin_mode and st.session_state.get("admin_view_macro")` 분기와 `views/admin_view.py` 는 한 글자도 건드리지 않았습니다. 비로그인 세션에 `admin_view_macro` 플래그가 남아 있어도 매크로 화면이 열리지 않는 것을 스텁 테스트로 확인했습니다(§0-3-1 유지).
    - **`views/us_stocks_view.py` — "🧪 [베타 · 검토중] 아직 공개되지 않은 화면입니다" 배너 제거**: 톤을 낮춰 남기는 대신 **완전히 제거**했습니다. ① 배너가 알리던 내용("밸류에이션 임계값이 아직 잠정값")이 #55로 이미 사실이 아니게 됐고(잘못된 고지를 남겨두는 건 §0-1 위반), ② 이 프로젝트의 공개 화면(`views/pegy_view.py`, 코스피)에는 이런 "공개 상태 안내" 배너가 애초에 없고 빨간 투자주의 경고 배너만 있어서, 같은 공개 화면끼리 구조를 맞추는 게 자연스럽기 때문입니다. 배너를 걷어내며 아래 투자주의 경고 박스의 위쪽 여백을 코스피 화면과 동일한 `margin: 10px auto 14px auto` 로 맞췄습니다. 투자주의 경고 배너 자체는 그대로 유지.
    - **잔여 "관리자 전용" 전제 문구 정리**: `views/us_stocks_view.py` 모듈 docstring("이 화면은 아직 공개 메뉴가 아닙니다")과 `render_us_stocks_page()` docstring("관리자 전용 베타"), `utils/constants_us.py` docstring("모든 임계값은 잠정값 / 🧪 배너는 유지합니다"), `utils/scoring_us.py` §1 주석("🚧 전부 잠정값입니다")을 현재 사실에 맞게 갱신했습니다. 종목 카드 아래 `⚙️ [관리자] 수집 이슈` expander는 `is_admin` 게이트가 걸린 디버그용이라 **그대로 유지**(공개 방문자에게는 보이지 않음).
    - **건드리지 않은 것**: 코스피 관련 파일(`views/pegy_view.py`, `collector_kospi200.py`, `utils/scoring.py`, `utils/guardrail.py`, `.github/workflows/scrape.yml`) 전부 무수정. 상단 지수 단일 출처 문제(§0-3-3 미충족, stockanalysis.com ETF 프록시)는 오너 판단으로 공개 블로커가 아니며 화면에 이미 "근사치입니다" 고지가 붙어 있어 **새 경고를 덧붙이지 않고 현행 유지**.
    - **검증**: ① 전체 `python3 -m py_compile`(진입점·수집기·utils·views·tests) 통과. ② 오프라인 테스트 3종 전부 스크립트 직접 실행해 회귀 없음 — `tests/test_us_stocks.py` 전체 통과 / `tests/test_us_scoring.py` 전체 통과 / `tests/test_quant.py` 전체 통과. ③ **streamlit 스텁 기반 렌더링·라우팅 스모크 테스트를 새로 작성해 실행**(저장소에는 커밋하지 않는 임시 스크립트): `admin_mode=False` 상태로 실제 스냅샷(548종목)을 넣고 `render_us_stocks_page()`가 예외 없이 완주하는 것, 렌더링된 전체 출력에 "검토중"/"공개되지 않은"/"잠정값"/"관리자 전용" 문구가 하나도 남지 않고 투자주의 배너와 타이틀은 그대로 있는 것을 확인. `visiblehand.main()`도 3가지 시나리오로 호출해 (a) 비로그인+체크해제 → 코스피, (b) **비로그인+체크ON → 미국주식(=공개 전환의 핵심)**, (c) 비로그인+`admin_view_macro` 잔존 플래그 → 코스피(매크로 안 열림)를 실측 확인. ④ `git diff -w` 로 의도한 파일 외 혼입이 없음을 확인(공백/CRLF 노이즈 제외 시 `visiblehand.py`·`views/us_stocks_view.py`·`utils/constants_us.py`·`utils/scoring_us.py` 4개 + 문서 2개만 변경).
    - **의미**: 이로써 처리 순서(0→4→1→2) 중 **1번 해외주식 페이지가 사실상 마무리**됐습니다. 다음은 **2번 매크로 14개 위험 지표 재설계**입니다.

62. **사이드바 공개 메뉴를 대분류/중분류 구조로 개편 — 체크박스 → 라디오(택1) (`visiblehand.py`, 2026-08-08, 오너 요청).** #61 공개 전환 때는 미국주식을 체크박스 하나로 켜는 구조였는데, 그러면 "코스피가 본체이고 미국은 옵트인 부가기능"처럼 읽힌다는 오너 지적. 한국·미국을 **대등한 두 리포트**로 보이게 메뉴 구조만 바꿨습니다(계산 로직·화면 내용은 무수정).
    - **구조**: 대분류 `💡 사실 이 가격이에요` → 중분류 `🇰🇷 한국 주식은 이가격이에요`(기본값, `render_pegy_page()`) / `🇺🇸 미국 주식은 이가격이에요`(`render_us_stocks_page()`). 중분류 문구는 오너가 요청한 그대로 쓰고 국기 이모지만 붙였습니다.
    - **위젯 선택**: `st.sidebar.radio`(택1). 새 패턴을 만들지 않고 이 프로젝트가 2026-08-05 이전에 실제로 쓰던 사이드바 서비스 선택 방식과 동일하며, 화면 안의 다른 택1 UI(`views/*_view.py`의 페이지 이동 라디오, 빠른 필터 selectbox)와도 결이 맞습니다. 체크박스와 달리 "둘 중 하나"라는 관계가 위젯 자체로 드러납니다.
    - **대분류 헤더**: 라디오 라벨에 넣지 않고 바로 위에 별도 markdown 블록(좌측 teal 액센트바 + 17px)으로 뒀습니다 — 사이드바 최상단 브랜드 타이틀 "🏢 잘 보면 보이는 손"(24px teal, **매크로 방공망**이라는 완전히 다른 화면의 이름)과 혼동되지 않게 크기·색·모양으로 구분하기 위해서입니다. 라디오 자체 라벨은 "시장 선택"으로 두어 도움말(툴팁) 앵커를 유지했고, help 문구도 새 구조에 맞게(두 리포트를 나란히 설명) 다시 썼습니다.
    - **세션 키**: `view_us_stocks`(bool) → `selected_market`(선택된 라벨 문자열). 옛 키를 참조하던 다른 코드는 없음을 grep으로 확인했고, `PROJECT_STATUS.md`의 언급만 같이 갱신했습니다.
    - **CSS**: 라디오가 다시 생겨서 항목 글자 가시성 규칙을 되살렸는데, 2026-08-05에 지운 옛 규칙과 달리 `[data-testid="stRadio"]` 안쪽으로 범위를 좁혔습니다 — 옛 규칙은 사이드바의 모든 위젯 라벨(관리자 비밀번호 입력창 등)까지 같이 바꿔버렸기 때문입니다. 셀렉터가 안 맞아도 기본 스타일로 렌더링될 뿐 깨지지 않습니다.
    - **건드리지 않은 것**: `elif admin_mode and st.session_state.get("admin_view_macro")` 매크로 분기, `views/admin_view.py`, `views/pegy_view.py`, `views/us_stocks_view.py`, 코스피/미국 계산 로직 전부 무수정. 변경 파일은 `visiblehand.py` 1개 + 문서 2개.
    - **검증**: `python3 -m py_compile visiblehand.py` 통과. streamlit이 이 샌드박스에 없어 #61과 같은 방식의 임시 스텁(저장소 미커밋)으로 `main()`을 6가지 시나리오로 호출해 11개 항목 전수 확인 — 첫 진입 기본값=코스피 / 한국 선택=코스피 / 미국 선택=미국주식 / 비로그인+`admin_view_macro` 잔존 플래그=코스피(매크로 안 열림) / 관리자 로그인+매크로 체크=매크로 / 미국 선택이 매크로보다 우선(기존 우선순위 그대로), 그리고 옵션 문구 2개·`index=0`·대분류 헤더·브랜드 타이틀 유지·옛 체크박스 소멸까지 확인. `git diff -w --stat`으로 CRLF 노이즈 제외 시 위 3개 파일만 변경됨을 확인.

63. **⬇️ "종목별 데이터 다운로드" 도구 신설 — 코스피·미국 양쪽 화면에 종목 1건 전체 데이터 CSV/JSON 내보내기 (`utils/stock_export.py` 신설 + `views/pegy_view.py` / `views/us_stocks_view.py`, 2026-08-08, 오너 승인 디자인).** 지금까지 다운로드는 "200종목 전체 스냅샷" / "550종목 전체 스냅샷" 같은 통짜 파일뿐이라, 관심 종목 하나만 엑셀로 뜯어보려면 수백 종목짜리 JSON을 직접 열어야 했습니다. 화면 상단 검색·필터 UI 바로 아래에 **종목 하나를 검색해 고르면 그 종목의 전체 레코드를 CSV·JSON으로 받는** 별도 섹션을 추가했습니다.
    - **내보내는 범위 — 고르지 않고 전부(§0-1)**: 카드에 보이는 지표만 추리지 않고 스냅샷 레코드(`s` 딕셔너리)의 **모든 키를 저장된 순서 그대로** 내보냅니다(코스피 67~69개 / 미국 115개 필드). `data_issues`, `collect_errors`, `missing_fields`, `*_source`, `f_target_cap_reason` 같은 내부 진단 필드도 포함 — "왜 이 값이 비어 있는지"가 데이터 자체에 남아야 받는 사람이 판단할 수 있기 때문입니다.
    - **CSV 구조는 "세로형"(항목 1열 / 값 1열)**: 이 저장소의 기존 CSV(`market_history.csv`, 관리자용 전체 스냅샷 CSV)는 전부 **가로형**이지만 그건 "행이 여러 개"(날짜별·종목별)라서 자연스러운 형태입니다. 이번 파일은 **한 종목 1건**이라 가로형이면 컬럼이 100개 넘게 옆으로 늘어서 엑셀에서 가로 스크롤을 계속 해야 읽힙니다. → `항목,값` 2컬럼 N행으로 정하고 **코스피·미국에 동일 구조**를 적용했습니다(공통 모듈이라 구조가 갈라질 수 없음).
    - **⚠️ 실제로 터졌을 문제 1 — CSV 한글 깨짐**: 일반 사용자는 받은 CSV를 엑셀로 바로 엽니다. 그냥 `utf-8`로 인코딩하면 한국어 윈도우 엑셀이 CP949로 읽어 한글이 전부 깨집니다. → **`utf-8-sig`(UTF-8 BOM)** 로 인코딩합니다(이 프로젝트의 기존 관리자용 스냅샷 CSV 다운로드도 같은 이유로 `utf-8-sig`를 쓰고 있어 관례가 이미 있었음). 검증에서 생성 바이트의 앞 3바이트가 BOM(`EF BB BF`)인지 직접 확인합니다.
    - **⚠️ 실제로 터졌을 문제 2 — 파일명에 못 쓰는 문자**: 미국 티커 `BRK/B`, `BRK/A`의 `/`가 파일명에 그대로 들어가면 브라우저가 경로로 해석해 저장이 깨집니다. → `sanitize_filename_part()`로 `\w`·`-` 외 문자를 `_`로 치환(한글은 파일명에 그대로 쓸 수 있어 유지). 결과: `버크셔해서웨이_B_BRK_B_20260808.csv`, `삼성전자_005930_20260808.csv`, `엔비디아_NVDA_20260808.csv`. 같은 치환 규칙을 `st.download_button`의 `key`에도 적용했습니다(`us_dl_csv_BRK_B`) — key에 종목 코드/티커를 넣어야 종목을 바꿨을 때 버튼이 새 데이터로 갱신됩니다.
    - **JSON**: `ensure_ascii=False`(한글이 `가` 이스케이프가 아니라 그대로) + `indent=2`. 스냅샷 레코드를 **그대로** 내보내며 요약·래핑 메타데이터를 덧붙이지 않습니다(원본 구조 그대로가 재사용에 유리).
    - **결측 처리**: `None`은 빈 칸으로 둡니다. `데이터 없음` 같은 한국어 문구를 넣으면 숫자 컬럼이 문자열로 오염되고, 0이나 평균값으로 채우는 건 §0-1 정면 위반입니다. 검증에서 "CSV의 빈 칸 집합 == 원본 레코드의 `None` 필드 집합"을 종목별로 대조합니다. 리스트·딕트 값은 구조를 잃지 않게 `json.dumps(..., ensure_ascii=False)` 문자열로 넣습니다.
    - **검색창은 기존 것을 공유하지 않고 전용으로 신설**: 오너 디자인의 "기존 검색창 UI 패턴 재사용"은 **위젯 패턴(라벨·placeholder 문구·매칭 규칙)을 그대로 따른다**는 뜻으로 해석했고, **입력값 자체는 공유하지 않습니다**. 공유하면 "파일 하나 받으려고 친 검색어" 때문에 아래 200/550종목 카드 목록이 통째로 필터링돼 기존 화면 동작이 바뀝니다(회귀 위험). → 전용 키 `pegy_download_search` / `us_download_search`를 따로 뒀고, 매칭 규칙만 기존 검색과 100% 동일하게 맞췄습니다(코스피=종목명 대소문자 무시 + 종목코드 부분일치, 미국=영문명·한글명·티커 부분일치).
    - **UI**: `st.expander`로 접어둔 별도 섹션(기본 접힘) — 카드 목록 위 세로 공간을 잡아먹지 않게. 검색어가 비면 안내(`st.info`), 매칭이 없으면 조용히 넘어가지 않고 `st.warning`("검색 결과가 없습니다 …"), 매칭이 2개 이상이면 `st.selectbox` 후보 목록(`삼성전자 (005930)` / `엔비디아 (NVDA)`), 1개면 바로 확정. 고른 종목은 작은 요약 카드(이름·코드/티커·뱃지·현재가·퀀트 스코어·내보낼 항목 수)로 확인시킨 뒤 **⬇ CSV로 다운로드 / ⬇ JSON으로 다운로드** 버튼 2개를 `st.columns(2)`로 나란히 둡니다. `use_container_width`는 **일부러 쓰지 않았습니다** — streamlit 1.50에서 버튼류의 이 인자는 deprecated 경로이고, 이 저장소의 기존 `st.download_button` 호출들도 쓰지 않습니다.
    - **공통 모듈 `utils/stock_export.py` 신설(streamlit 미의존)**: `build_stock_csv_bytes` / `build_stock_json_bytes` / `build_export_filename` / `sanitize_filename_part` / `to_export_text`. 화면 코드 없이 **함수 단위로 실데이터에 직접 돌려볼 수 있게** 하려고 순수 함수로 분리했고, 덕분에 코스피·미국 파일 형식이 구조적으로 갈라질 수 없습니다.
    - **건드리지 않은 것**: 기존 카드 렌더링·페이지네이션·검색/필터 로직, 코스피/미국 계산 로직(`utils/scoring*.py`, `utils/guardrail.py`, 수집기), 매크로 관련 파일 전부 무수정. 두 화면 모두 **추가만 있고 기존 줄 삭제·변경이 0**입니다(`git diff -w --stat` = `views/pegy_view.py` +112 / `views/us_stocks_view.py` +130, 전부 insertion).
    - **검증**: ① `python3 -m py_compile views/pegy_view.py views/us_stocks_view.py utils/stock_export.py` 통과. ② 오프라인 테스트 3종 스크립트 직접 실행해 회귀 없음(`tests/test_us_stocks.py` / `tests/test_us_scoring.py` / `tests/test_quant.py` 전부 통과 — ⚠️ 이 3종은 `unittest discover`로는 0건 수집되므로 반드시 스크립트로 실행). ③ **실제 스냅샷 전 종목(코스피 201 + 미국 548)에 CSV/JSON 생성 함수를 전수 실행**해 예외 0건. 대표 종목(삼성전자 / 결측 최다인 맥쿼리인프라 32필드 / NVDA / BRK.B / BRK.A)은 생성된 바이트를 **`utf-8-sig`로 디코드해 한글이 정상인지**, **`pandas.read_csv`로 다시 파싱해 `항목/값` 2컬럼·행 수=필드 수·필드 순서 일치**인지, **JSON을 `json.loads`로 되읽어 원본 dict와 완전 일치**하는지, 빈 칸 집합이 원본 `None` 집합과 같은지까지 대조. ④ streamlit 스텁(저장소 미커밋 임시 스크립트)으로 두 도구 함수를 9가지 시나리오로 호출 — 빈 검색어/무매칭 경고/코드 단일 매칭/한글명 매칭/다중 매칭 selectbox(삼성 14개, BRK 3개)/슬래시 티커 파일명·key 치환/요약 카드 문구까지 실측 확인.

64. **📈 종목별 데이터 다운로드 전면 재설계 — "내부 디버깅 필드 통짜 스냅샷" → "카드에 보이는 재무 지표만, 한국어 라벨, 날짜별 시계열" (`utils/stock_history.py` 신설 + `utils/stock_export.py` 재작성 + 수집기 2개 + 화면 2개, 2026-08-09, 오너 지적 반영).** #63으로 만든 다운로드를 오너가 실제로 받아 열어보고 두 가지를 지적했습니다. ① 내보낸 게 분석용 재무 데이터가 아니라 **개발자용 내부 필드**였다(`is_visible`, `badge_bg`/`badge_fg`(색상 hex), `f_target_cap_reason`, `value_trap_severity`, `consistency_warnings`, `data_issues`, `score_excluded_items` …). ② **하루짜리 스냅샷**이라 추세를 볼 수 없다 — 날짜별로 쌓이는 형식이어야 한다.
    - **⚠️ 과거는 소급 생성하지 않습니다(§0-1) — 이력은 "다음 수집분부터" 쌓입니다.** 이 프로젝트가 지금까지 이력화해 온 건 `data/pegy_summary_history.json` / `data/us_summary_history.json` 뿐인데, 그건 **시장 전체 요약(중앙값)** 이지 종목별이 아닙니다. 종목별 과거 스냅샷을 보관한 적이 없으므로 과거 날짜를 만들어 채울 근거 자체가 없습니다. 그래서 이력이 0건인 종목에는 파일을 만들어 주는 대신 **"아직 이력이 없습니다 / 다음 수집부터 쌓입니다"** 를 화면에 그대로 안내합니다. 도입 초기에 행이 1줄인 것도 정상입니다.
    - **필드 선정 기준 — "카드에 실제로 렌더링되는 것"만, 코드로 확인.** `views/pegy_view.py` / `views/us_stocks_view.py` 의 카드 HTML을 한 줄씩 훑어 실제 표시되는 지표만 추렸습니다(코스피 26개 + 날짜 / 미국 40개 + 날짜). 색상 hex(`badge_bg`·`badge_fg`), 진단·사유 필드(`*_source`, `*_capped`, `*_uncapped`, `*_reason`, `data_issues`, `collect_errors`, `consistency_warnings`, `missing_fields`, `value_trap_basis`/`severity`, `is_visible`/`is_valid`/`is_unverified`, `url` 등)는 **전부 제외**. 라벨은 영문 키가 아니라 한국어(`t_per` → `PER(Trailing, 배)`)이며, 코스피·미국 공통 지표는 문구를 통일했습니다.
    - **일부러 통일하지 않은 라벨 2개(같은 키인데 실제로 다른 지표라서)**: `growth` — 코스피는 네이버 '추정EPS vs TTM EPS' 증감률(`예상 EPS 성장률(%)`), 미국은 소스가 주는 3년 컨센서스(`3년 EPS 성장 전망(%)`). `sh_return` — 코스피는 배당수익률만(자사주 매입 공시 미수집, `배당수익률(%)`), 미국은 배당+자사주 합산(`주주환원율(%)`). 억지로 같은 이름을 붙이면 서로 다른 값을 같은 지표로 읽게 됩니다. 테스트가 이 2개는 **달라야** 통과하도록 고정해 뒀습니다.
    - **일부러 넣지 않은 것**: 코스피 `PBR 바닥가`·`상승여력 %` 는 카드에 보이지만 스냅샷에 저장된 값이 아니라 화면이 그 자리에서 계산하는 표시값입니다(미국은 `floor_price` 가 스냅샷에 있어 컬럼으로 포함). 저장된 값이 아닌 걸 이력에 굳혀두면 나중에 계산식이 바뀔 때 과거 행이 거짓이 되므로, 원재료(현재가·PBR·목표주가)를 그대로 주고 계산은 받는 쪽에 맡깁니다. 코스피 `f_pegy` 도 **카드에 렌더링되지 않아** 넣지 않았습니다(미국 카드에는 있어서 미국에만 있음).
    - **저장 구조 — 시장별 CSV 파일 1개(`data/kospi200_stock_history.csv`, `data/us_stocks_history.csv`)에 전 종목 이력을 담고, 다운로드 시점에 해당 종목 행만 필터링.** 근거 3가지: ① **종목별 파일 750개**로 나누면 매일 750개 파일이 바뀌어 커밋 diff를 사람이 볼 수 없고, 상장폐지·티커 변경 때 고아 파일이 남습니다. ② **JSON이 아니라 CSV**인 이유 — 이 저장소에서 "날짜별로 append 되는 시계열"의 기존 관례가 `market_history.csv` 이고, JSON(indent=2)으로 쌓으면 종목 하나가 30줄이라 하루 750종목 × 수백 일이면 수백 MB로 폭주하며 diff도 못 읽습니다. CSV는 하루치가 정확히 **종목 수만큼 추가된 줄**로 보입니다(코스피 약 47KB/일, 미국 약 148KB/일 — 이미 매일 통째로 커밋되는 `us_stocks_latest.json` 2.2MB / `us_stocks_raw_latest.json` 4.3MB에 비하면 미미). ③ 파일 안의 **컬럼명은 영문 키**(스냅샷 원본 키와 동일)로 두고 한국어 라벨은 다운로드 파일에서만 씁니다 — 화면 문구는 앞으로도 다듬을 수 있는데 그때마다 과거 이력의 헤더가 어긋나면 안 되기 때문입니다. 인코딩은 저장 파일·다운로드 파일 모두 `utf-8-sig`(오너가 이력 파일을 엑셀로 직접 열어도 한글이 안 깨지게).
    - **"성공적으로 완주했을 때만 기록" 이중 방어**: ① 기록 호출 위치가 **각 수집기의 맨 끝(스냅샷 저장 완료 후)** 이라, 중간에 목록 스크래핑 실패·히스테리시스 0건·소스 차단(HTTP 429 `USSourceBlockedError`)·수집 0건이 나면 그 전에 예외로 중단되어 **이 줄에 도달하지 못합니다**(= 차단당한 날은 이력에 아무것도 안 쌓임). ② 그 위에 `record_daily_history()` 가 `status` 를 한 번 더 봅니다 — `SUCCESS`/`DEGRADED` 가 아니면(=`FAILED` 등) 기록을 거부하고, 종목이 0건이어도 빈 날짜 행을 만들지 않습니다. 오염된 하루가 영구 이력에 한 번 박히면 되돌릴 수 없기 때문입니다(§0-1). ③ 이력 파일 I/O가 실패해도 `try/except` 로 경고만 남기고 **이미 성공한 수집을 실패로 만들지 않습니다**.
    - **같은 날 재실행 방어**: 같은 날짜 기록이 이미 있으면 중복으로 쌓지 않고 **그 날짜 행만 교체**합니다. (미국 워크플로우는 서머타임 때문에 cron이 하루 두 줄이고 `--skip-if-not-ready` 가 실제 수집을 하루 한 번으로 걸러주지만, `force=true` 수동 재실행 같은 경우를 대비한 방어 로직입니다.)
    - **날짜 기준**: 코스피는 수집 시각의 KST 날짜. 미국은 "언제 돌렸나"가 아니라 **그 데이터가 속한 거래일** — 페이지의 `At close: …` 타임스탬프에서 뽑은 `session_date_from_source` 의 최빈값을 쓰고(이미 metadata 계산에 쓰던 집계값 재사용), 실측 세션 날짜가 하나도 없을 때만 계산값으로 폴백합니다.
    - **버퍼 구간 종목도 기록**: 히스테리시스 버퍼(코스피 201~230위 / 미국 동일 구조)의 `is_visible=False` 종목도 함께 씁니다. 순위가 잠깐 밖으로 밀렸다고 그 종목의 시계열에 구멍이 나면 안 되기 때문입니다(몇 위였는지는 `시가총액 순위` 컬럼에 그대로 남습니다).
    - **다운로드 파일 형식**: **날짜=행 / 지표=열**의 시계열 표(엑셀에서 바로 꺾은선 그래프를 그릴 수 있는 형태). 헤더는 한국어 라벨, CSV는 `utf-8-sig`(BOM), JSON은 `ensure_ascii=False`+`indent=2`에 **숫자 컬럼만 숫자로 복원**(필드별 종류가 선언돼 있어 종목코드 `005930` 이 5930으로 망가지지 않음), 수집 못 한 값은 CSV 빈 칸 / JSON `null`(0·문구로 안 채움). 파일명은 예전 하루치 파일과 섞이지 않게 `삼성전자_005930_이력_20260809.csv` 로 `이력` 을 넣었고, `BRK/B` 의 `/` 치환 규칙은 그대로 유지했습니다.
    - **화면 UI는 오너가 이미 승인한 구조 그대로**(expander → 전용 검색창 → 다중 매칭 시 selectbox → 요약 카드 → CSV/JSON 버튼 2개). 요약 카드 문구만 "내보낼 항목 N개" → **"이력 기간 / 기록일수 / 지표 수"** 로 바꿨고, 내부에서 부르는 함수만 이력 기반으로 교체했습니다. 카드 목록·페이지네이션·검색 필터는 무수정.
    - **워크플로우**: `.github/workflows/scrape.yml` 은 `git add data/`, `scrape_us.yml` 은 `git add -A data/` 라서 **새 이력 파일이 자동으로 커밋 범위에 포함**됩니다 — 워크플로우 파일은 한 줄도 고칠 필요가 없었고, 실제로 고치지 않았습니다(테스트가 이 조건을 검사합니다).
    - **건드리지 않은 것**: 유니버스 필터링·히스테리시스·스코어링·가드레일·체크포인트/재시도·`--skip-if-not-ready` 사전 점검 등 기존 수집 로직 전부 무수정(순서 변경·삭제 0). 매크로 관련 파일 전부 무수정. 두 수집기는 **import 블록 + 맨 끝 호출**만 추가됐습니다(`git diff -w --stat`: collector_kospi200 +36 / collector_us_stocks +40, 전부 insertion).
    - **검증**: ① `python3 -m py_compile` — 수집기 2개 / `utils/stock_export.py` / `utils/stock_history.py` / 화면 2개 / `visiblehand.py` 전부 통과. ② 기존 오프라인 테스트 3종(`tests/test_us_stocks.py` / `tests/test_us_scoring.py` / `tests/test_quant.py`) 스크립트 직접 실행 — 전부 회귀 없이 통과(⚠️ 이 3종은 `unittest discover` 로는 0건 수집되므로 반드시 스크립트로 실행). ③ **신설 `tests/test_stock_history.py` 98개 체크 전부 통과** — 필드 스펙(내부 필드 유출 0, 한국어 라벨, 공통 라벨 일치/의도적 예외), 실제 스냅샷→행 변환, 상태 가드(FAILED·0건이면 파일조차 안 생김), **실제 수집기 함수를 직접 호출한 실패/차단 시나리오**(코스피 목록 실패 → RuntimeError, 미국 `USSourceBlockedError` → 둘 다 이력 기록 함수를 부르지도 않음), 소스 순서 배선, 누적·같은 날 중복 방지·1일치 정상 동작, 실데이터 end-to-end(BOM 확인, `pandas.read_csv` 재파싱, 종목코드 앞자리 0 보존, JSON null 대조, 슬래시 티커 파일명). ④ streamlit 스텁 + 임시 이력 파일(저장소 미커밋, `stock_history_path` 를 임시 경로로 바꿔 **실제 이력 파일은 건드리지 않음**)로 두 화면의 다운로드 도구를 실제 호출 — 이력 0건일 때 버튼 없이 안내만 뜨는지, 삼성전자/NVDA/BRK.B 파일명·key·요약 카드까지 확인. ⑤ `git diff -w --stat` 으로 의도한 5개 파일 + 신규 2개만 변경됨을 확인(이 저장소 고질병인 CRLF 노이즈 제외).

65. **UI 폴리시 묶음 — 오너가 실제 배포 화면을 육안으로 훑으며 발견한 자잘한 문제 6건 (2026-08-08~09).** 카드 리뷰 중 나온 사소하지만 실제 사용성에 걸리는 것들을 그때그때 바로 수정.
    - 사이드바 국기 이모지(🇰🇷/🇺🇸)가 Windows에서 폰트 미지원으로 "KR"/"US" 텍스트로 깨져 보여 이모지 자체를 제거(`visiblehand.py`).
    - 퀀트 스코어 툴팁(`.q-tooltiptext`)이 `width: max-content`라 긴 설명 텍스트가 박스 밖으로 튀어나감 — 고정폭(300px) + `overflow-wrap: break-word` + `white-space: normal`로 교체, 코스피·미국 양쪽 다 수정.
    - "착시 저평가/퀀트 스코어 가이드" 박스(코스피)와 "미국주식 퀀트 스코어" 안내 박스가 반투명 배경(`rgba(15,23,42,0.75)`)이라 뒤 배경이 비쳐 흐릿하게(뿌옇게) 보임 — 불투명(`#0f172a`)으로 교체.
    - 코스피 지수/원달러 환율 숫자가 흰색·30px라 눈에 잘 안 띔 — 34px·초록색(`#4ade80`)으로 가독성 개선(오너 요청).
    - "마지막 수집이 N일 전입니다, 자동 수집이 멈춰 있는지 확인해 주세요" 데이터 신선도 경고문이 일반 방문자에겐 의미 없는 운영자용 지시문이라 관리자 인증 시에만 노출되도록 전환(`views/pegy_view.py`) — 실제 데이터 시점 자체는 바로 아래 "마지막 동기화" 배너에 이미 표기되어 있어 일반 사용자에게 숨기는 정보는 없음(§0-1).
    - 코스피 화면에만 있던 "📘 학습용 보조 도구 안내" 박스가 미국주식 화면엔 없었던 것을 오너가 지적 — 동일 문구로 미국 화면에도 추가.
    - 전부 `py_compile` + 기존 오프라인 테스트 3종 회귀 없음 확인, 커밋 6건으로 나눠 반영.

66. **매크로 14개 위험 지표 실데이터 연결 조사 + 재설계 제안서 작성 (2026-08-09, 코드 미수정 · 연구 단계).** 오너 지적("14개 전부가 실은 5개 실측값의 선형 변형인 추정 프록시 — §0-3-1 위반")을 받아, 지표별로 무료 실측 소스가 실제로 존재하는지 전수 조사하고 결과를 `MACRO_REDESIGN_PROPOSAL.md`(신규)에 정리. **`.py` 파일은 한 줄도 수정하지 않았고**(다음 세션이 깨진 상태로 시작하지 않도록 의도적으로 연구만 수행), 산출물은 문서 1개.
    - **판정 결과 A 3개 / B 5개 / C 6개.** A(실측 연결 가능, 가중치 합 27.0): `KOSPI_5D_Return`·`Stock_Net_Sell`(둘 다 **이미 수집 중인 실측값**인데 `clip(0~1)` 프록시 스케일에 묶여 있는 케이스), `FX_Swap_Point`(서울외국환중개 F/X Swap POINT 페이지가 로그인 없이 공개인 것을 직접 확인, 단 조회 엔드포인트 미검증). B(재라벨링 필요, 35.0): `VKOSPI_Skew`→VKOSPI 레벨, `Synthetic_Futures`→선물 베이시스(합성선물 괴리는 이자율 가정이 들어가 §0-1 계산값 예외 2번 조건 위반), `Put_OTM_OI`, `Short_Ratio`, `Stock_Short_Balance`. C(실측 불가, 40.0 = 전체의 39.2%): `ELS_KnockIn`·`NDF_Night_Rate`·`Futures_Net_Sell`·`Non_Arbitrage_Ratio`·`Foreign_Broker_Dump`·`Put_Buy_Simple`.
    - **조사 중 드러난 큰 사실 3가지**: ① KRX 정보데이터시스템(data.krx.co.kr)이 **2025-12-27부터 회원제(로그인 필수)** 로 전환 — 기존 직접 호출 방식은 더 이상 정공법이 아님. ② 정공법은 **KRX OPEN API**(openapi.krx.co.kr, 무료, 인증키+**서비스별 개별 승인** 2단계, **전일 데이터는 익일 08:00 갱신**, 1일 10,000회). ③ **공매도·투자자별 선물/옵션·프로그램매매(차익/비차익) 통계는 OPEN API에 아예 없음**(서비스 목록 7개 카테고리 전수 확인) → pykrx(로그인 차단을 Referer 헤더로 우회 중) 외 무료 경로가 없어 §0-3-2와 충돌, 오너 결정 필요 항목으로 분리.
    - **가중치**: 현재 `RISK_WEIGHTS` 합계가 100이 아닌 **102.0**임을 실제로 더해서 확인. 다만 `compute_final_score()`가 매 실행 `total_weight`로 재정규화하므로 지표를 빼도 점수가 깨지지는 않음 — 대신 상대 비중이 1.645배씩 자동 상승하므로 의도적 재설계가 필요. 제안서에 (가) 기계적 비례 재분배안과 (나) 실측 직접성 기준 재설계안(합계 100) 둘 다 제시.
    - **미확인 항목을 낙관적으로 A로 올리지 않고 그대로 남김**: 선물/옵션 API의 미결제약정 필드 존재 여부, 파생상품지수 API의 VKOSPI 포함 여부, ECOS 스왑레이트 통계표 존재 여부, SMBS 조회 엔드포인트·약관 — 4건 전부 "착수 첫 단계에서 1회 호출로 확인, 부정적이면 즉시 C로 강등"으로 기록.
    - **다음 착수 지표**: `KOSPI_5D_Return` → `Stock_Net_Sell` (둘 다 신규 크롤링 0건, 같은 파일 같은 블록, 승인 대기 없음). KRX OPEN API 인증키 신청은 승인에 하루 이상 걸리므로 코드 작업과 병렬로 먼저 걸어둘 것.
67. **Streamlit Community Cloud "12시간 무방문 시 잠들기" 방지 워크플로우 신설 (2026-08-09~10).** 오너가 `visiblehand.co.kr` 접속 시 "Zzzz... 잠들었어요" 화면을 보고 문의 → 원인 확인 결과 Streamlit 무료 호스팅이 2025-03부터 24시간→**12시간** 무방문 시 앱을 재우는 정책(공식 문서 확인). 도메인 구매와는 무관하게(도메인은 iframe 껍데기일 뿐, 실제 서버는 그대로 무료 티어) 적용되는 정책임을 설명.
    - **1차 버전(curl)은 실패로 판명, 원인 규명 후 교체.** 처음엔 8시간마다 curl로 실제 앱 URL을 GET하는 방식으로 만들었으나, 오너가 공유한 Actions 로그에서 `curl: (47) Maximum (50) redirects followed`로 3회 시도 전부 실패 확인. 원인: Streamlit의 "Yes, get this app back up!" 깨우기는 브라우저 JS로 버튼을 눌러야 실행되는 클라이언트 사이드 동작이라, 순수 HTTP 요청은 핸드셰이크를 못 끝내고 리다이렉트만 계속 받음 — curl로는 구조적으로 불가능한 접근이었음(정직 기록).
    - **2차(현재) 버전: 헤드리스 브라우저(Selenium+Chrome)로 교체.** `keep_awake_ping.py`(신규) — 실제 Streamlit 앱 URL(iframe 안쪽 진짜 주소, 커스텀 도메인 아님)을 헤드리스 크롬으로 열고, "깨우기" 버튼이 보이면 클릭·사라짐까지 확인, 없으면 이미 깨어있는 것으로 판단. `.github/workflows/keep_awake.yml`이 8시간마다 이 스크립트를 실행. `selenium`/`webdriver-manager`는 앱용 `requirements.txt`에 넣지 않고 워크플로우 안에서만 별도 설치(앱 배포와 완전 분리). 데이터 수집·git 커밋과 무관한 독립 워크플로우라 실패해도 기존 자동화엔 영향 없음. **공식 기능이 아닌 커뮤니티 우회법**이며 Streamlit이 정책을 더 조이면 무력화될 수 있다는 점을 코드 주석에 명시.
    - **✅ 실전 검증 완료 (2026-08-10)**: 오너가 Actions 탭에서 수동 실행(Run workflow) → 초록 체크 확인. 8시간 주기 스케줄로 계속 돌아갈 예정.

68. **매크로 재설계 1단계 — 실측 2지표(`KOSPI_5D_Return`·`Stock_Net_Sell`)를 "임의 선형식"에서 "과거 분포 대비 정규화"로 교체 (2026-08-10, `MACRO_REDESIGN_PROPOSAL.md` §5 구현순서 1번 / §6 1·2순위).** #66 조사에서 A 판정(이미 실측인데 프록시처럼 취급됨)을 받은 2개만 손봤습니다. 나머지 12개 지표·`RISK_WEIGHTS` 값은 **한 줄도 건드리지 않았습니다**(가중치 재설계는 C 6개 제거와 함께 다음 단계).
    - **고친 문제**: ① `Stock_Net_Sell` = `0.5 ± 0.3`(부호만) × 개인비중 → **외국인 -2.8조 순매도와 -3,716억 순매도가 완전히 동점**이었습니다(실제 CSV에 2026-08-03·08-04 둘 다 `0.715`로 저장돼 있는 것을 확인). ② `KOSPI_5D_Return` = `0.5 - 2.5 × 수익률` → 계수 2.5의 근거가 없고 ±20% 밖은 clip으로 잘려 정보가 사라짐.
    - **선택한 방법(새로 발명하지 않고 기존 패턴 재사용)**: 실측 원값을 **그 지표 자신의 과거 분포 대비 z-score**(`utils/scoring.py`의 `_population_zscore`)로 표준화하고, **±3σ를 0~1로 선형 매핑 + 그 바깥은 윈저라이즈**(같은 파일의 `_winsorized_scale`)합니다. ±3σ 경계는 이미 밸류에이션 z-score 윈저라이즈에 쓰던 기준과 동일. 시그모이드 대신 선형 윈저라이즈를 고른 이유는 이 값이 뒤이어 `compute_sub_scores()`에서 **한 번 더 z-score+시그모이드**를 거치기 때문(이중 시그모이드는 중간 구간 해상도를 뭉갬).
    - **정규화 기준선(모집단)을 어디서 가져오는가**: `KOSPI_5D_Return`은 **FDR 코스피 시계열의 최근 약 1년(252영업일) 5일 수익률 분포**(최소 표본 60개) — CSV 이력이 아니라 원 시계열이라 **첫 실행부터 정상 정규화**됩니다. `Stock_Net_Sell`은 주체별로 **`market_history.csv`의 과거 순매수 금액**(최소 표본 20행 — 이 프로젝트가 이미 쓰던 "누적 이력 20행 미만 = 표본 부족" 경고와 같은 기준). 지금 이력이 6행이라 **순매수 3주체는 20행이 쌓일 때까지 중립 0.5**로 나오고, 그때마다 로그에 표본 부족을 남깁니다(§0-1 — 없는 분포를 지어내지 않음).
    - **방향**: 두 지표 모두 "값이 작을수록(더 마이너스일수록) 위험"이라 같은 함수(`measured_downside_risk`)를 씁니다. 3주체는 각각 자기 분포로 따로 정규화한 뒤 기존 `INVESTOR_WEIGHTS`(0.55/0.37/0.08)로 합산 — 이제 외국인·기관도 **부호가 아니라 금액**이 반영됩니다.
    - **신설/변경**: `utils/macro_scoring.py`에 `measured_downside_risk` / `compute_population_stats` / `rolling_return_population` / `net_flow_population` 추가(수집·화면이 같은 함수를 쓰도록 단일 출처 유지). `scrape_daily.py`는 해당 2블록만 교체하고 **실측 원값 컬럼 `KOSPI_5D_Return_Pct`(5일 수익률 %)를 CSV에 추가**(기존 컬럼은 이름 변경·삭제 0, `utils/db.py`의 `COL_MAP`에도 같은 매핑 추가). `views/macro_view.py`는 관리자 미리보기 분기를 같은 함수로 교체하고 "산출 공식"·지표명·상단 경고문을 실측 사실에 맞게 정정(14개 중 **12개**가 아직 프록시).
    - **검증**: `py_compile` 5개 파일 통과. 기존 오프라인 테스트 4종(`test_quant` / `test_stock_history` / `test_us_scoring` / `test_us_stocks`) 회귀 없음. **신설 `tests/test_macro_scoring.py` 46개 체크 전부 통과** — 방향성(폭락>하락>보합>급등), **금액이 다르면 점수도 달라야 함(이번 버그의 핵심을 못박는 테스트)**, ±3σ 윈저라이즈·단조성·항상 [0,1], 표본 부족 시 크래시 없이 중립 0.5, 실제 `market_history.csv` 스팟체크(외국인 순매도 최대일 2026-08-06이 최고 위험 0.652 / 순매수 최대일 07-31이 최저 0.196), 가중치 미변경 고정.
    - **⚠️ 화면에는 아직 반영되지 않습니다**: 이 코드는 다음 `scrape.yml` 자동 크롤링(평일 KST 16:05)이 돌아야 새 로직 값이 `market_history.csv`에 쌓이기 시작합니다. 과거 행은 그대로 두므로(기록 개변 금지) 당분간 표에는 옛 값과 새 값이 섞여 있게 됩니다.
    - **다음 단계**: 제안서 §5의 2번 — **C 판정 6개 지표 제거 + `RISK_WEIGHTS` 재설계(§4-3 안)**.

69. **매크로 재설계 2단계 — 실측 불가 6개 지표를 점수에서 제외하고 "공부용 참고" 섹션으로 분리 + `RISK_WEIGHTS` 비례 재분배 (2026-08-10, `MACRO_REDESIGN_PROPOSAL.md` §5 구현순서 2번 / §4-2 안 (가)).** 지표 **14개 → 8개**. 오너 지시: "실측 불가 6개는 삭제하지 말고 별도로 구분해두고, 공부하고 싶은 사람이 볼 수 있게 안내할 것. 단 내용이 필요없거나 허무맹랑하면 과감히 삭제할 것."
    - **6개 지표 각각의 판정(기계적으로 다 남기지 않고 하나씩 검토했습니다)**
        - `ELS_KnockIn`(7.0) → **공부 섹션 유지.** ELS 낙인은 실제로 반복해서 시장을 흔든 정식 개념(홍콩H지수 ELS 대규모 손실, 2020년 3월 ELS 헤지 마진콜에 따른 증권사 달러 조달난)이라 학습 가치가 큼. 다만 낙인 배리어·기초자산 여유·조건별 미상환 잔액이 한 세트로 매일 공개되지 않아 산출 불가(SEIBro는 월 단위 집계).
        - `NDF_Night_Rate`(12.0) → **공부 섹션 유지.** 역외 NDF는 언론에도 상시 등장하는 정식 개념. 다만 일별 NDF 종가를 무료 공표하는 곳을 찾지 못했고(SMBS NDF 페이지에 시세표 자체가 없음을 #66에서 직접 확인), 유료 단말 영역.
        - `Futures_Net_Sell`(6.0) → **공부 섹션 유지.** 투자자별 선물 순매도는 실제로 매일 기사화되는 지표. 다만 KRX 정보데이터시스템 로그인 전환 + OPEN API에 투자자별 파생 통계 미제공으로 무료 경로 없음.
        - `Non_Arbitrage_Ratio`(6.0) → **공부 섹션 유지.** 프로그램매매 차익/비차익 구분은 정식 통계이나 KRX 화면 전용(로그인 필수), OPEN API 미제공.
        - `Foreign_Broker_Dump`(6.0) → **공부 섹션에도 넣지 않고 완전 삭제.** 계산식이 `0.5 ± (외국인 순매수 부호)`뿐이라, 이미 **실측으로 살아있는 `Stock_Net_Sell`의 외국인 항목과 같은 입력을 이름만 바꿔 다시 센 것**이었습니다. '외국계 창구 매도 동향'이라는 개념 자체도 투자자 유형별 수급(외국인)과 대부분 겹치고 창구 주인과 실제 투자자가 일치하지도 않아, 별도 지표로 세울 근거가 약하다고 판단했습니다.
        - `Put_Buy_Simple`(3.0) → **공부 섹션에도 넣지 않고 완전 삭제.** '풋옵션 수요'는 지금도 활성인 `Put_OTM_OI`(풋옵션 미결제약정)가 이미 다루는 개념이라 두 번 세는 것이고, 투자자별 옵션 매매 데이터도 무료 공개가 없습니다.
        - → **삭제한 2개도 조용히 사라지게 두지 않고**, 화면에 "왜 뺐는지"를 이유와 함께 표시합니다(§4-4 권고).
    - **최종 `RISK_WEIGHTS` (전/후 비교, 합계 102.0 → 100.00)**

      | 지표 | 이전 | 이후 | 산정 근거 |
      | --- | --- | --- | --- |
      | `FX_Swap_Point` | 12.0 | **19.35** | 12.0 × 100/62 = 19.3548… 반올림 |
      | `Synthetic_Futures` | 12.0 | **19.35** | 동일 |
      | `KOSPI_5D_Return` | 12.0 | **19.36** | 19.3548… + 반올림 잔여 0.01 (아래 설명) |
      | `Put_OTM_OI` | 8.0 | **12.90** | 8.0 × 100/62 = 12.9032… |
      | `Short_Ratio` | 6.0 | **9.68** | 6.0 × 100/62 = 9.6774… |
      | `VKOSPI_Skew` | 6.0 | **9.68** | 동일 |
      | `Stock_Short_Balance` | 3.0 | **4.84** | 3.0 × 100/62 = 4.8387… |
      | `Stock_Net_Sell` | 3.0 | **4.84** | 동일 |
      | ~~`ELS_KnockIn`~~ | 7.0 | — | 제외(공부 섹션) |
      | ~~`NDF_Night_Rate`~~ | 12.0 | — | 제외(공부 섹션) |
      | ~~`Futures_Net_Sell`~~ | 6.0 | — | 제외(공부 섹션) |
      | ~~`Non_Arbitrage_Ratio`~~ | 6.0 | — | 제외(공부 섹션) |
      | ~~`Foreign_Broker_Dump`~~ | 6.0 | — | **완전 삭제**(Stock_Net_Sell과 중복) |
      | ~~`Put_Buy_Simple`~~ | 3.0 | — | **완전 삭제**(Put_OTM_OI와 중복) |
      | **합계** | **102.0** | **100.00** | |

    - **가중치 산정 방법 — 왜 (가) 비례 재분배를 썼나(암산 아님, 실제로 더해서 검증)**: 14개 합 102.0 → 제외 6개 합 40.0 → 남은 8개 합 **62.0**. 여기에 `100/62 = 1.6129…`를 곱해 소수 둘째 자리로 반올림했고, 단순 반올림 합이 **99.99**라 잔여 0.01을 한 곳에 붙여 정확히 100.00을 맞췄습니다. 잘림값이 가장 큰 항목이 12.0짜리 3개로 동률이었는데, 그중 **유일하게 실측이 검증된 `KOSPI_5D_Return`**(#68)에 붙였습니다 — "아직 프록시인 지표를 반올림 잔여분만큼이라도 부풀리지 않는다"는 기준입니다. **결과적으로 지표 간 상대 비중은 개정 전과 정확히 동일**합니다(테스트로 못박음).
    - **제안서 §4-3(안 나, 실측 직접성 기준 재설계)을 지금 적용하지 않은 이유**: §4-3 표는 `FX_Swap_Point`/`VKOSPI`/`Futures_Basis`/`Put_OTM_OI`/`Short_Ratio`/`Stock_Short_Balance`가 **이미 실측으로 연결된 상태**를 전제로 짜여 있는데, 실제로는 이 6개가 여전히 옛 임의 선형 프록시입니다(#68에서 실측으로 바뀐 건 2개뿐). 아직 프록시인 지표에 "실측이니까"라며 가중치를 올리면 근거 없이 특정 지표를 부풀리는 것이라 §0-1 정신에 어긋납니다. **`KOSPI_5D_Return`·`Stock_Net_Sell`만 §4-3의 20점으로 올리는 절충안도 검토했지만 채택하지 않았습니다** — 8개 중 2개만 다른 기준으로 정하면 어느 쪽 기준도 아닌 중간 상태가 되고, 이번 변경(6개 제외)과 비중 변경이 뒤섞여 나중에 점수 변화의 원인을 분리할 수 없게 됩니다. **§5의 3~6단계(KRX OPEN API 실측 연결)가 끝난 뒤 §4-3을 근거가 갖춰진 상태에서 한 번에 적용하는 것**을 다음 과제로 남깁니다.
    - **공부 섹션에 넣은 4개 지표 (화면: `views/macro_view.py` "📚 공부용 참고 — 지금은 다루지 않는 지표")**: 지표마다 ① 쉬운 한 줄 설명 ② 왜 시장에서 중요한가 ③ 지금 왜 못 다루는가(구체적인 데이터 부족 사유) ④ 어떻게 공부하면 좋은가(구체 검색어·출처명: SEIBro, 한국은행 금융안정보고서/ECOS, KRX 정보데이터시스템 화면, 서울외국환중개, '낙인 배리어'·'베이시스'·'차익/비차익' 등) ⑤ **데이터가 생기면 가중치가 대략 어느 범위일지(확정 아님, 참고 범위)** 를 적었습니다. 값은 하나도 계산하지 않고 **순수 설명 텍스트만** 렌더링합니다(§0-1 — 없는 데이터로 숫자를 만들지 않음).
        - `ELS_KnockIn` 대략 5~10 — 성격이 비슷한 '잔고성' 지표(공매도 잔고 4.84)와 시장 전체 파생 지표(9.68) 사이. 데이터가 월 단위라면 매일 점수보다 경보 플래그가 정직함을 함께 명시.
        - `NDF_Night_Rate` 대략 8~12 — 살아있는 `FX_Swap_Point`(19.35)와 **같은 환율·달러자금 채널**이라 따로 세면 이중계산. 들어온다면 그 채널 안에서 비중을 나눠 갖는 형태.
        - `Futures_Net_Sell` 대략 6~10 — 성격이 비슷한 공매도 비중·공포지수가 9.68. 단 선물 베이시스(19.35 자리)와 정보가 겹침.
        - `Non_Arbitrage_Ratio` 대략 3~6 — 외국인 현물 순매도가 '실행되는 통로'에 가까워 이미 실측인 `Stock_Net_Sell`(4.84)과 같은 사건을 가리키는 폭이 큼 → 활성 지표 중 최저 구간.
    - **고친 파일**: `utils/constants.py`(가중치 8개 + 산정 과정 주석 + `RETIRED_RISK_INDICATORS` 신설), `scrape_daily.py`(`els_base`/`ndf_base`/`fut_base`/`non_base`/`dump_base`/`put_buy_base` 계산과 `market_scores` 6항목 제거 — **믿지 않는 값을 계속 계산해 CSV에 남기지 않기 위해 계산 자체를 삭제**), `views/macro_view.py`(관리자 미리보기 분기 동일 정리 + `FRIENDLY_NAMES`/`formulas` 정리 + 공부 섹션 신설 + 지표 개수 하드코딩 제거 + 감사추적 탭 v1.6.0 추가), `utils/db.py`(`METRIC_COLUMNS`를 `RISK_WEIGHTS`에서 파생하도록 변경), `utils/macro_ai.py`(코멘트 재생성 판정이 `len == 14` 하드코딩이던 것을 "요청할 지표가 전부 있는가"로 교체), `utils/macro_scoring.py`(증폭기 비율 상수 주석 보강).
    - **`market_history.csv`는 손대지 않았습니다** — 기존 6개 컬럼과 과거 값은 그대로 보존(기록 개변 금지). `COL_MAP`에도 6개의 한글 매핑을 남겨 과거 행을 계속 읽을 수 있게 했고, 다만 **새 행에는 더 이상 기록되지 않습니다**.
    - **부수효과(정직 기록)**: 동시 충격 증폭기는 개수가 아니라 **비율**(극단 신호 ÷ 그날 산출 가능 지표 수)로 동작하므로 코드는 그대로 돌지만, 같은 "2개 극단"이 예전엔 2/14≈14.3%(증폭 없음), 지금은 2/8=25%(약한 증폭 시작)로 판정됩니다. 지표 수에 맞춰 상수를 다시 손대면 그 자체가 근거 없는 재튜닝이라, 원래의 비율 설계를 유지하고 변화 사실만 `utils/macro_scoring.py` 주석에 남겼습니다.
    - **검증**: `py_compile` 6개 파일 통과. `tests/test_macro_scoring.py`를 46 → **140개 체크로 확장** 전부 통과 — 새 테스트는 ① 가중치 합이 실제로 100.00인가(부동소수 오차 1e-9), ② 102.0/40.0/62.0 산수 재검산, ③ 각 지표가 정확히 `이전값 × 100/62`인가, ④ 지표 간 **상대 비중 보존**, ⑤ 제외한 6개가 `RISK_WEIGHTS`·양쪽 파일의 점수 사전·계산 변수 어디에도 없는가, ⑥ 반대로 과거 CSV 읽기용 `COL_MAP` 매핑은 보존됐는가, ⑦ **화면 공부 섹션 목록(4) + 완전 삭제 목록(2) = 실제로 코드에서 뺀 목록(6)** 정확 일치(문서-코드 불일치 방지), ⑧ 가중치 범위가 확정치가 아닌 "참고 범위"로 표기됐는가. 기존 회귀 테스트 4종(`test_quant`/`test_stock_history`/`test_us_scoring`/`test_us_stocks`) 전부 통과. 실제 `market_history.csv`로 화면 산출 경로를 돌려 8개 지표·가중치 합 100.0이 나오는 것도 확인.
    - **⚠️ 화면 반영 시점**: 이번 변경도 다음 `scrape.yml` 자동 크롤링(평일 KST 16:05)부터 새 값이 쌓입니다. 화면 표는 즉시 8개로 줄어들지만, 과거 행의 옛 지표 값은 CSV에 그대로 남아 있습니다(표시하지 않을 뿐).
    - **다음 단계**: 제안서 §5의 3~6번 — B 판정 5개(+`FX_Swap_Point`)의 **실제 실측 연결**. KRX OPEN API 인증키·서비스별 이용신청 승인이 선행 조건입니다.

70. **매크로 재설계 3·4단계 — `VKOSPI_Skew`·`Synthetic_Futures` 2개를 KRX OPEN API 실측으로 교체 (2026-08-10, `MACRO_REDESIGN_PROPOSAL.md` §5 구현순서 3·4번).** 8개 활성 지표 중 실측이 2개(#68) → **4개**가 됐습니다. 선행 조건이던 인증키 발급·4개 서비스 이용신청 승인·GitHub Secret 등록을 오너가 모두 완료해 착수했습니다.
    - **🚨 가장 먼저 읽을 것 — 실서버 검증이 아직 안 됐습니다.** 이 개발 샌드박스는 외부 네트워크가 막혀 있어(`openapi.krx.co.kr` 포함) **실제 KRX API를 한 번도 호출해보지 못했습니다.** 인증키 값도 GitHub Secret 안에만 있고 세션에는 없습니다(의도적 설계). 그래서 이번 작업은 **"코드 작성 + 모킹으로 배선 검증"까지**이며, 규격이 실제로 맞는지는 **오너가 Actions에서 수동 실행한 로그로만 확인 가능**합니다. 아래 "오너 확인 절차" 참고. 문서 어디에도 "실측 확인됨"이라고 쓰지 않았습니다.
    - **무엇이 바뀌었나 (옛 식 → 새 식)**
        - `VKOSPI_Skew`: `0.4 + 0.4×(변동성/5) − 0.2×코스피등락률` (이름만 '공포지수', 실제 입력은 우리가 계산한 10일 변동성) → **KRX가 산출·공표하는 VKOSPI(코스피200 변동성지수) 지수값 그 자체**를 누적 이력 대비 z-score → ±3σ를 0~1로 윈저라이즈.
        - `Synthetic_Futures`: `0.5 + 0.3×(환율−1300)/200` (이름만 '합성선물', 실제 입력은 **환율 레벨**) → **선물 베이시스 = KOSPI200 선물 근월물 종가 − KOSPI200 지수 종가**. 실측 2개의 순수 뺄셈이라 §0-1 계산값 예외 조건(무위험이자율·잔존만기 같은 가정 없음)을 충족합니다. 진짜 '합성선물 괴리'(콜-풋 패리티)는 가정이 필요해 그대로는 불가하다고 #66에서 이미 판정했습니다.
        - **방향이 서로 반대라 따로 함수를 만들었습니다.** VKOSPI는 **높을수록** 위험(`measured_upside_risk` 신설), 베이시스는 **낮을수록**(백워데이션) 위험(`measured_downside_risk` 재사용). 정규화 방법론 자체는 #68과 **완전히 동일**하며 새로 발명한 것이 없습니다. 부호 실수를 못박으려고 "upside + downside = 1.0"을 테스트로 검증합니다.
        - 주체별(외국인/기관/개인) 오프셋(`+0.05`, `−0.2`, `+0.15 if 순매도` 등)은 **전부 제거하고 3주체 동일값**으로 바꿨습니다. VKOSPI와 베이시스는 시장 전체가 하나로 매기는 가격이라 주체별로 다를 수 없고, 옛 오프셋 숫자에는 아무 근거가 없었습니다(`KOSPI_5D_Return`과 같은 취급).
    - **📌 라이브러리를 쓰지 않고 직접 구현했습니다 — 근거 (오너 지시로 직접 판단)**
      후보 `pykrx-openapi`(raccoonyy, PyPI, 스타 12개)의 소스를 GitHub raw로 전부 읽고 비교했습니다.
        1. **인증키 전달 위치가 공식 규격과 다릅니다(결정적).** KRX 공식 서비스 상세 페이지 본문에 **"(Request 헤더에 인증키 값을 AUTH_KEY 필드에 추가하여 전달)"** 이라고 명시돼 있는데, `pykrx-openapi`의 `client.py::_make_request`는 `params={"AUTH_KEY": ..., "basDd": ...}`로 **쿼리스트링에** 넣습니다. 동작 여부를 떠나 인증키가 URL에 실리면 서버 접근로그·프록시·예외 메시지(요청 URL이 그대로 찍힘)에 키가 남습니다. "인증키를 코드·문서·로그 어디에도 남기지 않는다"는 이번 지침과 정면으로 충돌합니다.
        2. **필요한 건 31개 엔드포인트 중 3개**뿐이고, 각각 `requests.get` 한 번입니다. 래퍼 전체를 들이는 비용 대비 이득이 없습니다.
        3. **의존성 최소 유지 관례** — 이 저장소는 requests/beautifulsoup4/pandas/FinanceDataReader 정도만 쓰고, `requirements.txt`에 "선택 패키지는 일부러 넣지 않는다"고 이미 적어두었습니다. 새 의존성은 Streamlit Cloud 빌드와 Actions 파이프라인 전체에 영향을 줍니다.
        4. **검증 불가** — 네트워크가 막혀 이 라이브러리가 실제로 도는지 확인할 수 없습니다. 검증 못 한 코드를 통째로 신뢰하기보다 우리가 읽고 이해한 60줄을 직접 들고 있는 편이 장애 추적에 유리합니다.
      → `requirements.txt`는 **변경하지 않았고**, 왜 안 넣었는지를 그 파일 주석에 남겼습니다. 다만 **엔드포인트 경로·파라미터명은 pykrx-openapi에서, 응답 필드명은 `seobaeksol/krx-rs`(Rust, serde rename에 실제 JSON 키가 박혀 있음)에서** 확인했고, 서로 독립적인 두 구현이 같은 값을 쓰고 있어 신뢰도가 올라갑니다.
    - **📌 확인한 것 / 확인 못 한 것 (섞지 않고 구분)**
        - ✅ **공식 페이지로 확인**: 인증키는 **HTTP 헤더 `AUTH_KEY`**, 요청 파라미터는 `basDd`(YYYYMMDD). 서비스 목록·2010-01-04부터 제공·서비스별 개별 이용신청 필요.
        - ✅ **제3자 구현 2건 일치로 확인**: Base URL `https://data-dbg.krx.co.kr/svc/apis/{category}/{endpoint}`, 응답 최상위 키 `OutBlock_1`. 필드명 — 지수: `IDX_CLSS`/`IDX_NM`/`CLSPRC_IDX`, 선물: `ISU_CD`/`ISU_NM`/`PROD_NM`/`TDD_CLSPRC`/`SPOT_PRC`/`ACC_TRDVOL`/`ACC_OPNINT_QTY`.
        - ❌ **VKOSPI의 정확한 지수명 — 확인 못 했습니다.** 파생상품지수 API는 숫자 '지수 코드'가 아니라 **계열구분(`IDX_CLSS`) + 지수명(`IDX_NM`) 문자열**로 구분됩니다(그래서 "지수 코드"라는 것 자체가 존재하지 않습니다). 공식 페이지의 출력값 표가 로그인해야 렌더링돼 목록을 못 봤습니다. → 이름을 **지어내지 않고**, 후보 목록(`VKOSPI_NAME_CANDIDATES`) 정확일치 → 실패 시 "이름에 '변동성' 포함이 **유일할 때만**" 부분일치 → 그래도 실패하면 **응답에 실제로 들어있던 지수명 전부를 로그로 출력**하고 값은 `None`으로 둡니다. 후보가 2개 이상이면 **아무거나 고르지 않습니다**(그게 지어내기라서).
        - ❌ **KOSPI200 선물 근월물 식별 — 확인 못 했습니다.** 선물은 만기가 있어 종목코드가 매달 바뀝니다. `ISU_NM`의 만기 표기 형식을 확인 못 해 파싱하지 않고, **"그 상품군에서 누적 거래량(`ACC_TRDVOL`)이 가장 많은 종목"을 근월물로** 봅니다(근월물이 원월물보다 거래량이 압도적으로 많은 것은 선물시장의 구조적 성질). 이건 **관측값을 고르는 규칙**이지 값을 만드는 게 아니며, 선택 결과(종목명·코드·거래량)를 매번 로그에 남겨 첫 실행에서 검증할 수 있게 했습니다. `미니`/`위클리`/`야간`/`섹터` 상품은 이름 규칙으로 제외합니다(미니는 계약 크기가 1/5라 섞이면 안 됨).
        - ❌ **KOSPI200 지수명(`IDX_NM`)도 미확인** — 다만 여기서는 부분일치를 **일부러 쓰지 않습니다**. "코스피 200 TR", "코스피 200 중소형주" 같은 이름이 겹치는 지수가 많아 잘못 집을 위험이 크기 때문입니다. 정확일치만 허용하고, 실패하면 응답의 지수명 전부를 로그로 남깁니다.
    - **📌 `Put_OTM_OI`는 이번에 손대지 않았습니다 (지시대로).** 미결제약정 필드 `ACC_OPNINT_QTY`는 옵션 응답에 **있는 것으로 보입니다**(krx-rs의 `OptionsDailyRecord`에 '미결제약정수량'으로 매핑). 그런데 **같은 레코드에 행사가격 필드가 없습니다** — 필드는 `RGHT_TP_NM`(권리구분)/`TDD_*`(시고저종)/`ACC_TRDVOL`/`ACC_TRDVAL`/`ACC_OPNINT_QTY`/`IMP_VOLT`/`NXTDD_BAS_PRC` 뿐입니다. 즉 **"OTM(외가격)"을 가르려면 `ISU_NM` 문자열에서 행사가를 파싱해야 하는데 그 형식을 확인하지 못했습니다.** 확인 못 한 형식을 가정해 파싱하면 그 자체가 지어내기라 **옛 프록시 그대로 두었습니다.** 옵션 API는 이번 단계에서 **호출조차 하지 않습니다**(엔드포인트 정의만 남김). 다음 세션 후보 2가지를 `utils/krx_openapi.py` 맨 아래에 기록해뒀습니다 — (가) 실행 로그로 `ISU_NM` 형식 확인 후 행사가 파싱, (나) 행사가 없이도 계산 가능한 **풋/콜 미결제약정 비율**로 재라벨링(지표 이름·의미가 바뀌므로 오너 승인 필요).
    - **📌 시점(as-of) 문제를 숨기지 않고 기록합니다.** KRX OPEN API는 **전일(T-1) 확정치를 익일 08:00에** 공개하므로, 우리 수집 시각(KST 16:05)에 '오늘'로 조회하면 빈 응답이 옵니다. 그래서 조회일부터 하루씩 거슬러 올라가 데이터가 있는 가장 최근 거래일을 찾고(최대 8일, 주말+명절 연휴 커버), **그 기준일을 값과 함께 CSV에 저장**합니다(신설 `VKOSPI_Level_AsOf`/`Futures_Basis_AsOf` 컬럼). 서로 다른 지연을 가진 값을 한 행에 섞어놓고 지연을 안 적으면 그것 자체가 §0-1 위반이기 때문입니다(제안서 §3-2·§3-3 권고 반영). 8일을 넘으면 "오늘의 위험"을 말하기엔 너무 낡았다고 보고 `None` 처리합니다.
    - **📌 부트스트랩 — 당분간 중립 0.5가 나옵니다(정상 동작).** 정규화 기준선(과거 분포)은 **우리가 CSV에 쌓아온 원값 컬럼**(`VKOSPI_Level_Raw`/`Futures_Basis_Raw`)으로만 만듭니다. 과거치를 빨리 채우겠다고 KRX에 하루씩 수십~수백 번 조회하는 건 크롤링 매너(§0-3-2) 위반이라 하지 않았습니다. **이력 20행이 쌓일 때까지는 두 지표 모두 중립 0.5**로 나옵니다(#68 순매수와 완전히 같은 부트스트랩). 원값은 첫날부터 쌓이므로 약 한 달 뒤 정상 정규화됩니다.
    - **📌 실패 처리 — 어떤 오류에도 "그 지표만" 빠집니다.** 인증키 미설정 / 미승인(401) / 한도 초과(429) / 네트워크 오류 / `OutBlock_1` 없음 / 필드명 변경 / 지수명 불일치 — 전부 **프록시로 조용히 폴백하지 않고** `None`을 돌려주고, 기존 `unavailable_items` 로직이 배점에서 통째로 뺍니다. VKOSPI 실패와 베이시스 실패는 **서로 독립**입니다(하나가 죽어도 다른 하나는 살아남음). 두 지표가 다 빠져도 남은 6개(가중치 합 70.97)로 재정규화되어 점수가 정상 산출되는 것을 확인했습니다.
    - **📌 크롤링 매너(§0-3-2)**: 1회 실행당 호출 **10회 이하**(거래일 탐색 최대 8 + 파생지수 1 + 선물 1). 호출 간 0.5초 대기, **4xx는 재시도하지 않고**(§0-3-2 "차단 신호에 매달리지 않기") 네트워크/5xx만 **1회만** 재시도. KRX 한도는 인증키당 1일 10,000회입니다.
    - **바뀐 파일**
        - `utils/krx_openapi.py` **(신설)** — 인증키를 환경변수 `KRX_OPENAPI_KEY`에서만 읽고 **헤더로만** 전달하는 최소 클라이언트 + 행 선택기 + 상위 수집 함수 `collect_krx_risk_inputs()`. 파일 상단에 위 "확인한 것/못 한 것"과 라이브러리 미채택 근거를 전부 적어뒀습니다.
        - `utils/macro_scoring.py` — `measured_upside_risk()` 신설(방향만 반대, 방법론 동일), `net_flow_population()` → `history_column_population()`으로 일반화(기존 이름은 얇은 별칭으로 유지해 호출부·테스트 무손상).
        - `scrape_daily.py` — `skew_base`/`synth_base` 계산식 삭제, KRX 수집·정규화 블록 추가, 신설 원값/as-of 컬럼 4개 저장, `COL_MAP` 추가.
        - `views/macro_view.py` — 같은 두 식을 **동시에** 삭제(두 파일 산식이 갈라지면 미리보기와 실제 저장값이 또 어긋남). 미리보기 분기에서는 두 지표를 `None`(산출 불가)으로 둡니다 — **Streamlit 렌더링 중에 KRX API를 호출하지 않습니다**(화면 새로고침마다 외부 API를 두드리는 건 매너에도, 키 관리에도 맞지 않음). 화면 표기명(`FRIENDLY_NAMES`)과 산식 설명(`formulas`)을 실제 내용대로 교체.
        - `utils/db.py` — 신설 4개 컬럼의 한글 매핑 추가(저장 쪽과 **글자 하나까지 동일**해야 CSV 복원됨. 테스트로 못박음).
        - `.github/workflows/scrape.yml` — `scrape_daily.py` 스텝에 `KRX_OPENAPI_KEY: ${{ secrets.KRX_OPENAPI_KEY }}` 추가(기존 `GEMINI_API_KEY`와 동일 패턴).
        - `requirements.txt` — **패키지 변경 없음.** 왜 새 의존성을 안 넣었는지만 주석으로 기록.
    - **⚠️ `RISK_WEIGHTS`는 이번에도 한 숫자도 바꾸지 않았습니다.** `VKOSPI_Skew` 9.68 / `Synthetic_Futures` 19.35 그대로입니다. 프록시→실측 전환과 가중치 재설계는 별개 결정이고, #68·#69에서 일관되게 지켜온 원칙입니다. 제안서 §4-3(실측 직접성 기준 재설계)은 **8개 전부가 실측이 된 뒤 오너가 명시적으로 요청할 때** 한 번에 적용합니다. 테스트가 이 두 값을 직접 못박고 있어 무심코 바뀌면 실패합니다.
    - **⚠️ `market_history.csv` 기존 컬럼은 하나도 지우지 않았습니다.** 특히 `VKOSPI_Skew`/`Synthetic_Futures`의 **한글 헤더 문자열은 옛 이름 그대로 유지**했습니다("공포지수 비대칭도…", "합성선물 가격 차이…"). 저장된 과거 행의 한글 헤더를 영문 컬럼으로 되돌리는 게 이 매핑이라, 값을 바꾸면 과거 기록을 못 읽게 됩니다(기록 개변 금지). 대신 **화면에 보이는 이름**만 실제 내용대로 바꿔 라벨-내용 불일치를 없앴습니다. 이 둘이 서로 반대 방향이라는 점도 테스트로 못박았습니다.
    - **검증 (실서버 없이 할 수 있는 최대치)**: `py_compile` 6개 파일 + `scrape.yml` YAML 파싱 통과. `tests/test_macro_scoring.py`를 140 → **241개 체크로 확장**, 전부 통과. 새 테스트 5블록 — ⑪ VKOSPI 방향(단조 증가·중립 0.5·윈저라이즈·**upside+downside=1.0** 거울상), ⑫ 파서·선택기('코스피 200 TR' 함정 회피, '변동성' 중복 시 **고르지 않음**, 미니 선물에 안 낚임, 거래량/종가 결측 시 None), ⑬ **HTTP 호출부만 가짜로 바꾼 end-to-end**(값이 끝까지 흘러오는가 + **인증키가 헤더로만 가고 URL에는 절대 없는가** + `basDd` 형식 + 호출 3회), ⑭ 실패 모드 9종(키 없음/401/429/네트워크/규격 변경/휴장일 되감기/탐색 상한/지표별 독립 실패/필드명 변경), ⑮ 배선(두 파일 옛 식 소멸, 미리보기 프록시 부활 방지, 두 `COL_MAP` 일치, 워크플로우 시크릿 전달, **`RISK_WEIGHTS` 미변경**). 기존 회귀 4종(`test_quant`/`test_stock_history`/`test_us_scoring`/`test_us_stocks`) 전부 통과.
    - **🔜 오너 확인 절차 (이게 남은 유일한 검증입니다)**
        1. GitHub → Actions → **Daily Market Scraper** → **Run workflow** (target_date는 비워둠) 로 수동 실행.
        2. 로그에서 **`[KRX]`** 를 검색합니다. 정상이면 다음이 순서대로 찍힙니다:
           `KRX 기준일(as-of) = ...` → `KOSPI200 종가 = ...` → `VKOSPI = ... (지수명 정확일치: '...')` → `KOSPI200 선물 근월물 종가 = ... (근월물=거래량 최대: '...')` → `선물 베이시스 = ... − ... = ...`
        3. **`VKOSPI` 로 검색해 값이 실제로 찍히는지**가 핵심 확인 포인트입니다. 대략 12~40 사이면 정상 스케일입니다.
        4. 실패했다면 로그가 **원인과 다음 행동**을 그대로 알려줍니다:
           - `인증 실패(HTTP 401)` → 서비스별 이용신청 승인 상태 확인(키 발급과 서비스 승인은 별개).
           - `지수를 특정하지 못했습니다 — ... 응답에 있던 지수명: [...]` → **그 목록을 그대로 알려주시면** `VKOSPI_NAME_CANDIDATES`/`KOSPI200_NAME_CANDIDATES`를 실제 문자열로 못 박겠습니다(이게 가장 가능성 높은 시나리오입니다).
           - `KOSPI200 선물 근월물을 특정하지 못했습니다 — ... 응답에 있던 상품명: [...]` → 같은 방식으로 상품명 후보를 확정.
           - `'OutBlock_1' 가 없습니다 (받은 최상위 키: [...])` → 응답 규격이 조사 결과와 다름. 그 키 목록이 필요합니다.
        5. 성공하면 `market_history.csv`에 `VKOSPI 지수값 (실측 원값, KRX OPEN API)` / `선물 베이시스 (...)` / 각 `기준 거래일` 컬럼이 새로 생깁니다. **점수 자체는 이력 20행이 쌓일 때까지 중립 0.5**로 나오는 게 정상입니다(버그 아님).
    - **다음 단계**: 제안서 §5의 5~6번 — ⑤ SMBS F/X Swap POINT 엔드포인트·약관 확인 후 `FX_Swap_Point` 연결(역공학 필요), ⑥ 공매도 2종(`Short_Ratio`·`Stock_Short_Balance`)은 pykrx의 로그인 우회 사용 여부가 §0-3-2 원칙 문제라 **오너 결정 선행**. 그리고 위 로그 확인 후 `Put_OTM_OI` 처리 방향 결정.
71. **#70 실서버 검증 완료 + §5 5~6단계 착수 전 조사 (2026-08-10, 코드 미수정).** 오너가 GitHub Actions "Daily Market Scraper"를 수동 실행해 `utils/krx_openapi.py`를 실제로 검증. 1회차는 직전 `git push`가 rejected된 걸 모른 채 실행해 구코드로 돌았고(로그에 `[KRX]` 줄 0개로 원인 특정 — env 블록에 `KRX_OPENAPI_KEY` 자체가 없었음), `git pull`(원격의 자동 크롤링 봇 커밋과 병합) 후 재푸시·2회차 실행에서 성공. 로그로 VKOSPI 지수명 정확 일치, KOSPI200 선물 근월물 자동 선택(거래량 최대 규칙), 베이시스 교차검증까지 전부 확인됨. VKOSPI=75.59 값이 이상해 보여 WebSearch로 뉴스 교차검증한 결과 2026-07-30 레버리지 규제발 VKOSPI 급등(86.18)→08-03(69.53)→08-07(75.59) 실제 흐름과 일치함을 확인, 이상치 아님. 이어서(토큰 소진 임박으로 코딩 없이 조사만) §5 5단계용으로 한국은행 ECOS Open API에 외환스왑레이트 통계표가 있는지 짧게 조사 — 환율 통계표 코드(`731Y`계열)는 찾았지만 스왑레이트 전용 코드는 **못 찾음**(다음 세션에 ecos.bok.or.kr 사이트에서 직접 검색 필요). 6단계(공매도 2종)는 기술 문제가 아니라 pykrx 로그인 우회가 §0-3-2와 충돌하는 **오너 결정 필요** 항목으로 재확인, PROJECT_STATUS.md §4에 결정 선택지 3가지 정리해둠.

72. **매크로 재설계 6단계 — 공매도 2종(`Short_Ratio`·`Stock_Short_Balance`)을 "실측 불가"로 재분류해 점수에서 제외하고 "공부용 참고" 섹션으로 이동 + `RISK_WEIGHTS` 비례 재분배 (2026-08-10, `MACRO_REDESIGN_PROPOSAL.md` §5 구현순서 6번).** 활성 지표 **8개 → 6개**. 오너 결정 사항이던 항목(#66·#71에서 "기술 문제가 아니라 원칙 문제라 오너가 정해야 함"으로 분리해둔 것)을 오너가 **옵션②(pykrx 예외 불허 + 실측 불가 재분류)** 로 확정해 착수했습니다.
    - **🚨 이번 2개는 #69의 6개와 뺀 이유가 다릅니다 (뭉뚱그리지 않았습니다).** #69의 6개는 "무료로 구할 수 있는 데이터 자체가 없다"였습니다. 공매도 2종은 반대로 **데이터가 존재합니다** — KRX가 매일 집계하고 `pykrx`의 `get_shorting_volume_by_date()` / `get_shorting_balance_by_date()` 로 실제로 받아올 수 있습니다. 문제는 **경로**입니다: `data.krx.co.kr` 이 2025-12-27부터 회원제(로그인 필수)로 바뀐 뒤에도 pykrx는 그 로그인 요구를 **Referer 헤더 스푸핑으로 우회**해서 동작하고, 이는 `ENGINEERING_SPEC.md` §0-3-2(로그인 우회 크롤링 금지)와 정면 충돌합니다. KRX 공식 OPEN API에는 공매도 관련 서비스가 **아예 없습니다**(7개 카테고리 전수 확인, #66). → 오너가 예외를 허용하지 않고 두 지표를 "실측 불가"로 재분류하기로 결정. **삭제가 아니라 이동입니다** — 두 지표는 여전히 학습 가치가 큰 정식 개념이라 화면 "📚 공부용 참고" 섹션에 설명·공부법과 함께 남습니다(개념 중복이라 공부 목록에서도 뺀 `Foreign_Broker_Dump`·`Put_Buy_Simple`과는 성격이 완전히 다릅니다).
    - **빼기 전 두 지표의 실제 정체(정직 기록)**: `Short_Ratio` = `0.4 + 0.4 × (코스피 10일 변동성 / 5)`, `Stock_Short_Balance` = `0.5 + 0.3 × (52주 고점 대비 낙폭)`. 즉 이름만 '공매도'였고 입력은 코스피 변동성·낙폭이었습니다. 계산식을 남겨두면 믿지 않는 값이 계속 CSV에 쌓이므로 **계산 자체를 삭제**했습니다(#69와 같은 원칙, §0-1).
    - **최종 `RISK_WEIGHTS` (전/후 비교, 합계 100.00 → 100.00, 8개 → 6개)**

      | 지표 | 이전(#69 기준) | 이후 | 산정 근거 |
      | --- | --- | --- | --- |
      | `FX_Swap_Point` | 19.35 | **22.64** | 19.35 × 100/85.48 = 22.636874… |
      | `Synthetic_Futures` | 19.35 | **22.64** | 동일 (✅ 실측 — 선물 베이시스, #70) |
      | `KOSPI_5D_Return` | 19.36 | **22.65** | 19.36 × 100/85.48 = 22.648573… (✅ 실측, #68) |
      | `Put_OTM_OI` | 12.90 | **15.09** | 12.90 × 100/85.48 = 15.091249… |
      | `VKOSPI_Skew` | 9.68 | **11.32** | 9.68 × 100/85.48 = 11.324286… (✅ 실측 — VKOSPI, #70) |
      | `Stock_Net_Sell` | 4.84 | **5.66** | 4.84 × 100/85.48 = 5.662143… (✅ 실측, #68) |
      | ~~`Short_Ratio`~~ | 9.68 | — | 제외(공부 섹션으로 이동) |
      | ~~`Stock_Short_Balance`~~ | 4.84 | — | 제외(공부 섹션으로 이동) |
      | **합계** | **100.00** | **100.00** | |

    - **가중치 산정 방법 (암산 아님, 실제로 더해서 검증)**: 8개 합 100.00 → 제외 2개 합 **14.52**(9.68 + 4.84) → 남은 6개 합 **85.48**. 여기에 `100/85.48 = 1.1698642957…`을 곱해 소수 둘째 자리로 반올림했습니다. #69와 완전히 같은 **(가) 기계적 비례 재분배**이고, 지표 간 상대 비중은 개정 전과 **정확히 동일**합니다(테스트로 못박음). ⚠️ **이번에는 반올림 잔여 조정이 필요 없었습니다** — 단순 반올림 합계가 그대로 정확히 **100.00**이었습니다(#69 때는 99.99라 잔여 0.01을 `KOSPI_5D_Return`에 붙였습니다). 필요했다면 같은 원칙(가장 확실히 실측인 지표에 붙임)을 적용했을 것이고 잘림값이 가장 큰 항목은 `VKOSPI_Skew`(+0.004286, 실측 #70)였지만, **붙일 잔여가 0이라 어느 지표에도 손대지 않았습니다.**
    - **§4-3(안 나, 실측 직접성 기준 재설계)은 이번에도 적용하지 않았습니다.** 활성 6개 중 아직 2개(`FX_Swap_Point`·`Put_OTM_OI`)가 프록시라 전제가 갖춰지지 않았고, "지표 제거"와 "비중 재설계"를 섞으면 나중에 점수 변화의 원인을 분리할 수 없기 때문입니다(#69·#70과 동일한 판단).
    - **`volatility` / `dist_from_high` 처리 (판단 근거를 남깁니다)**: 이 두 값을 쓰던 곳은 위 공매도 2개 지표뿐이었습니다(`grep`으로 `scrape_daily.py`·`views/macro_view.py` 전수 확인). 지표가 사라지면서 **참조하는 코드가 하나도 남지 않아** 선언·계산을 함께 제거했고, 동시에 `scrape_daily.py`에 있던 **"변동성/고점대비낙폭 산출 실패 시 당일 수집 전체 중단(raise)" 하드 실패 게이트도 제거**했습니다. 이 게이트의 원래 목적은 "이 값이 없으면 공매도 2개가 시드 상수(1.2 / 0.08)로 계산돼 정상처럼 보이는 가짜 점수가 만들어지는 것"을 막는 것이었는데, 그 2개가 사라진 지금은 **아무 데도 쓰이지 않는 값 때문에 수집 전체가 죽는** 불필요한 실패 경로만 남기 때문입니다. KOSPI·환율 종가 결측 방어(§0-1의 핵심)는 **그대로 살아 있습니다**(테스트로 확인).
    - **고친 파일**: `utils/constants.py`(`RISK_WEIGHTS` 6개로 축소 + #72 개정 이력 주석 + `RETIRED_RISK_INDICATORS`에 2개 추가 — **"공부 섹션에 남기지만 사유가 다른 2개"라는 별도 그룹 주석**으로 기존 4개·중복제외 2개와 명확히 구분), `scrape_daily.py`(`short_base`/`bal_base` 계산 및 `market_scores` 2항목 제거, `volatility`/`dist_from_high` 제거, 하드 실패 게이트 제거, 변경 이력 주석), `views/macro_view.py`(`FRIENDLY_NAMES`·`formulas`·미리보기 `market_scores_raw`에서 2개 제거, `STUDY_ONLY_INDICATORS`에 2개 추가(4개 → **6개**), 공부 섹션 안내문에 "빼낸 이유가 두 종류"임을 명시, 화면 경고문·안내문의 지표 개수·프록시 개수 정정(8중6 프록시 → **6중 2개만 프록시, 4개 실측**), 감사추적 탭 **v1.7.0** 신설), `tests/test_macro_scoring.py`(아래).
    - **`market_history.csv`는 손대지 않았습니다** — 두 지표의 기존 컬럼·과거 값 그대로 보존(기록 개변 금지). `scrape_daily.py`/`utils/db.py`의 `COL_MAP` 한글 매핑도 그대로 남겨 과거 행을 계속 읽을 수 있게 했고, **새 행에만 값이 기록되지 않습니다.**
    - **부수효과(정직 기록)**: 동시 충격 증폭기는 개수가 아니라 **비율**(극단 신호 ÷ 그날 산출 가능 지표 수)로 동작하므로 코드는 그대로 돌지만, 같은 "2개 극단"이 #69 이후 2/8 = 25%였던 것이 이제 2/6 ≈ 33.3%로 판정됩니다. 지표 수에 맞춰 상수를 다시 손대면 그 자체가 근거 없는 재튜닝이라(#69와 동일한 판단) 원래 비율 설계를 유지합니다.
    - **검증**: `py_compile` 4개 파일 통과. `tests/test_macro_scoring.py`를 241 → **364개 체크로 확장**, 전부 통과 — 새/수정 테스트는 ① `RISK_WEIGHTS` 합계 100.00·개수 6개·목록 일치, ② #69 산수(102.0/40.0/62.0)와 #72 산수(100.00/14.52/85.48) **양쪽 재검산**, ③ 각 지표가 정확히 `#69 값 × 100/85.48`인가, ④ **단순 반올림 합이 그대로 100.00이라 잔여 조정을 하지 않았음**(주석과 코드가 어긋나면 실패), ⑤ 상대 비중 보존, ⑥ 두 지표의 계산식·점수 사전 항목이 두 파일 모두에서 사라졌는가(+ `volatility`/`dist_from_high`/하드 실패 게이트까지), ⑦ 과거 CSV 복원용 `COL_MAP` 매핑은 보존됐는가, ⑧ 공부 섹션(6) + 완전 제외(2) = `RETIRED_RISK_INDICATORS`(8) 정확 일치, ⑨ **두 지표의 `missing_data`가 '데이터 없음'이 아니라 'pykrx / data.krx.co.kr 2025-12-27 로그인 전환 / §0-3-2'를 구체적으로 밝히는가**(#69의 4개와 사유가 다르다는 것을 코드 레벨에서 못박음), ⑩ 공부법에 "이 프로젝트에서는 쓰지 않습니다" 단서가 붙었는가, ⑪ 가중치가 '참고 범위'로 표기됐는가. 기존 회귀 테스트 4종(`test_quant`/`test_stock_history`/`test_us_scoring`/`test_us_stocks`) 전부 통과.
    - **⚠️ 화면 반영 시점**: 다음 `scrape.yml` 자동 크롤링(평일 KST 16:05)부터 새 구성으로 값이 쌓입니다. 화면 표는 즉시 6개로 줄어들지만, 과거 행의 공매도 2개 값은 CSV에 그대로 남아 있습니다(표시하지 않을 뿐).
    - **현재 상태 요약**: 활성 **6개** 중 **4개가 실측**(`KOSPI_5D_Return`·`Stock_Net_Sell`·`VKOSPI_Skew`(VKOSPI 레벨)·`Synthetic_Futures`(선물 베이시스)), 나머지 2개(`FX_Swap_Point`·`Put_OTM_OI`)만 아직 프록시. 점수에서 뺀 8개는 공부 섹션 6개 + 개념중복 완전제외 2개.
    - **다음 단계**: ⑤ SMBS F/X Swap POINT 엔드포인트·약관 확인 후 `FX_Swap_Point` 실측 연결(역공학 필요, ECOS 대안은 #71에서 스왑레이트 전용 통계표를 못 찾아 보류), 그리고 `Put_OTM_OI` 처리 방향 결정(#70 로그로 `ISU_NM` 형식 확인 후 행사가 파싱 vs 풋/콜 미결제약정 비율로 재라벨링 — 후자는 지표 의미가 바뀌므로 오너 승인 필요).

73. **📊 "내 성적표"(3번째 모듈) v1 골격 신규 구축 — Supabase 스키마·RLS + 데이터 계층 + 화면 + 오프라인 테스트 (2026-08-11, `SCORECARD_WORK_ORDER.md` §6 착수 1~5단계).** 기존 두 모듈(코스피/미국주식)은 **한 줄도 건드리지 않고** 완전히 별도 모듈로 만들었습니다(§0-3-6). 아직 **스테이징 상태**로, 오너 승인 전까지 공개 메뉴에 노출되지 않습니다.
    - **왜 저장 아키텍처가 다른가**: 기존 데이터는 전부 공개 GitHub 저장소에 커밋되지만, 내 성적표는 로그인·보유종목·매입가 같은 **개인 금융정보**라 그 방식을 재사용할 수 없습니다. 오너 확정(2026-08-11)대로 **Supabase(Auth + Postgres + RLS)** 를 쓰고, 접속 정보는 git이 아니라 **Streamlit Community Cloud의 Secrets**에 넣습니다(GitHub Actions Secrets와 다른 곳 — 이 모듈은 스케줄 크롤링이 아니라 방문 시점에 앱이 직접 호출하는 구조라서).
    - **신규 파일 4개**
      - `sql/scorecard_schema.sql` — 오너가 Supabase SQL Editor에 붙여넣어 **1회 수동 실행**할 설정 스크립트. `profiles`(auth.users 확장) + `holdings`(user_id/market/ticker/stock_name/quantity/avg_purchase_price/currency/updated_at) 테이블, `updated_at` 자동 갱신 트리거, 가입 시 profiles 자동 생성 트리거, 그리고 **RLS 정책 8개**(테이블별 select/insert/update/delete를 `auth.uid() = user_id`로 제한) + `anon` 롤 권한 회수까지. 여러 번 실행해도 안전하게(idempotent) 작성했고 `DROP TABLE`은 넣지 않았습니다. **원/달러 혼용은 DB에서도 막습니다** — `market='KR' ↔ currency='KRW'` CHECK 제약과 `(user_id, market, ticker)` 유니크 제약.
      - `utils/scorecard_db.py` — 데이터 계층 단일 출처. ① 정규화(시장→통화 자동 결정, 한국 6자리 코드 앞자리 0 복원, 미국 티커 대문자화) ② **순수 계산 로직**(수량 가중평균, 보유 평가, 비중/수익 비중) — Supabase 클라이언트 없이 단독 호출·테스트 가능 ③ 기존 스냅샷 조회(`data/kospi200_pegy_latest.json`의 `code` / `data/us_stocks_latest.json`의 `symbol`, **읽기 전용**) ④ Supabase 래퍼(클라이언트 생성, 회원가입/로그인/로그아웃, holdings CRUD). `supabase`·`streamlit` 임포트를 전부 `try/except ImportError`로 감싸, 패키지가 없어도 이 모듈 임포트만으로 앱이 죽지 않습니다.
      - `views/scorecard_view.py` — 로그인/회원가입 폼 → 보유종목 수동 입력 폼 → 통화별 요약·표 → **원형차트 2종**(평가금액 비중 / 이익 기여 비중) → 선택 종목의 PEGY 요약 카드 → 삭제. 상단에 "환율 변환 없음" 고지 상시 노출.
      - `tests/test_scorecard.py` — 오프라인 검증 **187개 체크**(네트워크·Supabase 접속 불필요).
    - **수정 파일 2개**: `requirements.txt`(`supabase` 추가 — 없어도 기존 두 모듈은 정상 동작한다는 주석 포함), `visiblehand.py`(라우팅 추가. 임포트를 `try/except`로 감싸 모듈이 깨져도 기존 두 공개 화면은 살아남고, 실패 사유는 관리자 사이드바에만 표시).
    - **🚧 기본 숨김(스테이징, §0-3-6)**: 사이드바 진입 체크박스는 ① 관리자 인증 상태이거나 ② `SCORECARD_ENABLED` 플래그를 명시적으로 켠 경우에만 생깁니다. 둘 다 아니면 위젯 자체가 만들어지지 않아 **일반 방문자 화면은 이전과 100% 동일**합니다(라우팅 분기도 기존 `MARKET_US`/매크로/기본 pegy 순서를 그대로 두고 앞에 한 갈래만 추가).
    - **가중평균 (오너 마인드맵 예시 그대로)**: A증권 10주 @100,000원 + B증권 3주 @70,000원 → 13주 / 1,210,000원 / 평균 **93,076.923…원**. 저장·계산은 전체 정밀도(DB도 `numeric(20,6)`)로 하고 **화면 표기만 원 단위 내림**해 오너 예시의 "93,076원"과 일치시켰습니다(반올림하면 93,077이 되어 예시와 어긋납니다 — 이 판단 근거를 코드 주석에 남겼습니다).
    - **지어내지 않기(§0-1) 적용 지점**: 현재가는 오직 기존 수집 스냅샷의 실측 `price`만 사용하고, 유니버스(코스피 상위200/미국 상위550) 밖 종목은 현재가·평가금액·수익률을 **전부 `None`으로 두고 "현재가 없음"으로 표시**합니다(사용자 입력 매입가를 현재가로 쓰는 경로 없음, 비중도 0%가 아니라 계산 제외 + 별도 안내). Supabase 호출 실패는 빈 목록으로 삼키지 않고 `ScorecardError`로 올려 `st.error`까지 도달시킵니다. 손실 종목은 원형차트에 음수 조각으로 넣을 수 없어 수익 비중 차트에서 빼고 **뺐다는 사실과 금액을 화면에 명시**합니다.
    - **보안 설계 메모**: anon key는 설계상 클라이언트에 노출되는 게 정상인 키라 KRX 인증키와 성격이 다르고(그 차이를 코드 주석에 명시), **실제 방어선은 RLS**입니다. `service_role` 키를 읽는 코드는 어디에도 없습니다. Supabase 클라이언트를 `@st.cache_resource`로 캐시하면 **한 사람의 로그인 세션이 모든 방문자에게 공유**되므로 캐시하지 않고 방문자별 `st.session_state`에만 보관합니다(주석·테스트로 못박음). 앱에서도 `.eq("user_id", …)` 필터를 항상 걸어 RLS와 이중 방어.
    - **검증 (전부 오프라인, 실제 Supabase 미접속 — 작업지시서 §4-4)**: `py_compile` 4개 파일 통과. `tests/test_scorecard.py` **187체크 전부 통과** — ① 가중평균(오너 예시 값·순서 무관·3건 이상·잘못된 입력 거부) ② 원/달러 분리(통화 혼합 합산 시 예외, 통화를 합친 '총 자산' 그룹이 존재하지 않음, 소스에 환율 토큰 부재) ③ 스냅샷 조회(합성 JSON + **저장소의 실제 스냅샷** — 우선주 영숫자 코드 `00680K`·`0126Z0`까지 조회되는지 실측 확인) ④ 비중/수익 비중(현재가 미상 종목을 0으로 속이지 않는지) ⑤ **Supabase 없을 때 폴백**(패키지 없음/Secrets 없음/둘 다 없음 3가지 경우의 사유 문구, 클라이언트 `None` 반환이 에러가 아님, 그 상태의 CRUD는 조용히 실패하지 않고 예외) ⑥ **가짜 Supabase 클라이언트**로 CRUD 배선(재입력 시 insert가 아니라 update로 가중평균 갱신, 모든 쿼리에 user_id 필터, 네트워크 실패 시 예외 전파) ⑦ SQL 스크립트의 RLS 정책 8개·CHECK 제약·크레덴셜 미포함 ⑧ 화면/라우팅(기본 숨김, 기존 라우팅 무손상, streamlit 스텁을 주입해 **supabase 패키지 없이 실제 import 성공** 확인). 기존 5개 스위트(`test_macro_scoring`/`test_quant`/`test_stock_history`/`test_us_scoring`/`test_us_stocks`) **전부 통과(회귀 없음)**.
    - **오너가 다음에 할 일 (순서대로)**: ① Supabase 프로젝트 생성 → ② SQL Editor에서 `sql/scorecard_schema.sql` 실행 후 RLS 표시 확인 → ③ Authentication → Providers → Email 활성화 → ④ **Streamlit Cloud**(GitHub Actions 아님) Settings → Secrets에 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 등록(`service_role` 절대 금지) → ⑤ 재배포 후 관리자 로그인 상태에서 미리보기로 실제 연결 테스트 → ⑥ 승인되면 그때 공개 노출 방식 결정. **v1 범위 밖(다음 버전)**: 스크린샷 OCR(v2), 매일 수익률 누적·리포트·벤치마크(v3), 유니버스 밖 종목 실시간 시세, 1년 미접속 자동 삭제 배치.

74. **📊 "내 성적표" 실서버 연결 검증 완료 + 실사용 UX 버그 5건 즉시 수정 (2026-08-11, #73 당일 후속).** 오너가 #73에서 만든 골격으로 Supabase 프로젝트 생성부터 실제 회원가입/로그인/종목 입력까지 전부 직접 해보면서 발견한 문제를 그 자리에서 고쳤습니다. 이번엔 소넷이 직접 코딩(오푸스 아님) — 이미 있는 코드의 UX 개선 범위라 새 아키텍처 결정이 필요 없었습니다.
    - **실서버 검증 경과**: SQL 스크립트 실행 → `pg_class.relrowsecurity` 쿼리로 RLS 켜짐 직접 확인(엄지 아이콘 대신 "N RLS policies" 배지가 그 표시라는 것도 확인) → Email Provider 활성화 → Streamlit Cloud Secrets 등록(Supabase가 최근 키 체계를 "anon/service_role"에서 "Publishable/Secret"으로 개편해, `Publishable key`를 `SUPABASE_ANON_KEY`에 매핑해야 한다는 점 안내) → 첫 이메일 인증 링크가 `localhost:3000`(Supabase 기본값)으로 리다이렉트되어 접속 거부 화면이 뜬 문제 발생(단, 실제로는 인증 자체는 서버에서 이미 성공 처리된 상태였음 — URL의 `access_token`이 그 증거) → 근본 원인(Site URL 미설정)을 알려주되, 당장 테스트를 진행할 수 있도록 `Confirm email`을 임시로 꺼서 우회 → 로그인·종목 입력까지 성공.
    - **발견된 버그 5건과 수정**
        1. **Enter 키 오submit**: `st.form`을 쓰고 있어서 입력 필드에서 Enter를 치면(다음 종목 입력하려던 습관적 동작) 그 시점 값 그대로 즉시 추가돼버림. `st.form` 제거 → 명시적 `st.button` 클릭으로만 제출되도록 변경(폼이 없으면 Enter는 그 필드 값만 확정할 뿐 아무것도 제출하지 않음).
        2. **수량/매입가 입력 UX**: `st.number_input(format="%.6f")`가 "0.000000"을 먼저 채워둬서 타이핑이 그 뒤에 이어붙는 문제 + 콤마 입력 불가 문제. `st.text_input` + 신설 `_parse_positive_number()`(콤마 제거 후 float 파싱, 빈 값·0·음수·비숫자는 전부 명시적 예외)로 교체.
        3. **종목코드 칸에 종목명을 잘못 입력하는 사고**: "하이닉스" 같은 한글을 한국 종목코드 칸에 넣어도 검증 없이 그대로 저장돼 영구히 "현재가 없음" 상태가 됨(원인을 알기 어려움). `_KR_TICKER_LIKE` 정규식(숫자로 시작하는 영숫자 1~6자)으로 형식을 검증해 한글이 섞이면 명확한 안내와 함께 막습니다. **오너 지시**로 "종목명만으로 자동 찾기"(`find_ticker_by_name()`, §0-1 원칙대로 정확일치→유일한 부분일치→그 외엔 포기)는 **미국 주식 전용**(`NAME_LOOKUP_MARKETS = {MARKET_US}`)으로 한정 — 한국은 6자리 코드가 이미 짧고 익숙해 직접 입력이 더 명확하다는 판단.
        4. **삭제 기능을 못 찾음**: 기능 자체는 #73부터 있었지만 화면 맨 아래(밸류에이션 카드 다음, 접힌 expander)에 있어 발견이 어려웠음. 표 바로 아래로 위치를 옮기고 기본 펼침(`expanded=True`)으로 변경.
        5. **입력 필드가 안 비워짐**: 추가 성공 후에도 종목명 등 이전 값이 세션에 남아있어, 다음 종목 추가 시 실수로 재사용됨(예: "하이닉스"라는 이름이 다른 종목코드 행에도 계속 붙어 표시되던 버그). `_reset_input_fields()`를 만들어 추가 성공 시 `st.rerun()` 전에 입력 4칸(코드/이름/수량/매입가) 관련 `session_state` 키를 지움(시장 선택 라디오는 유지).
    - **검증**: `tests/test_scorecard.py` 187 → **211체크**로 확장(신규 함수 `find_ticker_by_name` 단위 테스트 6건 + UI 헬퍼 `_parse_positive_number`/`_KR_TICKER_LIKE`/`_reset_input_fields` 회귀 방지 테스트 다수). 기존 5개 스위트 전부 통과(회귀 없음). `git diff -w --stat` 범위 = `utils/scorecard_db.py`/`views/scorecard_view.py`/`tests/test_scorecard.py` 3개 파일뿐(기존 두 모듈·매크로 관련 파일 미수정).
    - **오너가 다음에 할 일**: 재배포 후 오늘 고친 5건이 실제로 잘 되는지 재확인, `Confirm email`을 다시 켤지 결정(Site URL은 이미 실제 도메인으로 고쳐둔 상태), 문제 없으면 공개 전환 여부 결정(§0-3-6).

75. **📊 "내 성적표" 종목 삭제 UX를 드롭다운 방식에서 종목별 인라인 버튼으로 전면 개편 (2026-08-11, #74 당일 후속).** #74에서 삭제 기능 위치만 옮겼는데, 오너가 "드롭다운으로 고를 게 아니라 종목 옆에 바로 수정/삭제 버튼을 두자"고 요청해 표 자체를 다시 짰습니다.
    - **무엇이 바뀌었나**: `st.dataframe()`으로 그리던 정적 표 + 화면과 분리된 "삭제할 종목 선택" 드롭다운을, **종목별로 직접 `st.columns()`를 그리는 방식**으로 교체했습니다. 각 종목 줄 맨 끝에 ✏️(수정)·🗑️(삭제) 버튼이 바로 붙어 있어 고르고 누르는 두 단계가 아니라 한 번에 처리됩니다.
    - **수정(✏️) 기능 신설**: 지금까지는 삭제만 가능했는데, 오너 요청으로 **값을 바로 고치는 기능**을 추가했습니다. ✏️를 누르면 그 줄 아래 수량·매입가 입력창이 펼쳐지고, 저장을 누르면 이미 있던 `update_holding()`(#73에서 데이터 계층에 만들어뒀지만 화면에서 한 번도 안 쓰던 함수)을 호출해 **값을 그대로 덮어씁니다** — 입력 폼의 "가중평균으로 합치기"와는 다른 동작이라는 걸 화면 문구로 명시했습니다(잘못 입력한 걸 고치는 용도 vs 새 계좌분을 추가하는 용도는 다른 시나리오).
    - **검증**: 테스트 211 → **214체크**(드롭다운 방식 제거·`update_holding` 연동·버튼 존재를 소스 레벨로 확인). 기존 5개 스위트 전부 통과. `git diff -w --stat` 범위 = `views/scorecard_view.py`·`tests/test_scorecard.py` 2개 파일뿐.

76. **📊 "내 성적표" 종목 입력을 "코드/티커/이름" 통합 단일 입력창으로 재설계 (2026-08-11, #74~#75 당일 후속, 오너의 명확한 지시로 이전 결정 번복).** #74에서 "종목명 자동조회는 미국 전용"으로 만들었는데, 오너가 실제로 한국 주식(삼성전자우)을 종목명으로 입력하다가 막히는 걸 겪고 "종목코드/티거/종목명 이게 전부 다 한곳에서 기능할 수 있게 해야지"라고 명확히 지시 — 시장 제한을 없애고 아예 필드 자체를 하나로 합쳤습니다.
    - **무엇이 바뀌었나**: "종목코드/티커" 칸과 "종목명" 칸으로 나뉘어 있던 입력을 **칸 하나**("종목 (종목코드 / 티커 / 종목명 — 아무거나 입력하세요)")로 합쳤습니다. `utils/scorecard_db.py`에 새 함수 `resolve_stock_query(market, query, indexes)`를 만들어 입력값 하나로 코드/이름을 모두 판별합니다: ① 코드로 바로 해석해 유니버스(스냅샷) 안에 있으면 즉시 확정 → ② 실패하면 이름으로 찾기(`find_ticker_by_name`, §0-1 그대로: 정확일치→유일한 부분일치→그 외 포기) → ③ 그것도 실패했지만 입력이 "코드처럼 생긴 형식"(신설 `KR_TICKER_LIKE`/`US_TICKER_LIKE` 정규식)이면 유니버스 밖 종목의 코드로 보고 그대로 채택(이름 없이, 나중에 "현재가 없음"으로 정직 표시) → ④ 그마저도 안 되면 이유와 함께 실패. **한국/미국 두 시장 모두 동일하게 적용** — #74에서 만든 `NAME_LOOKUP_MARKETS = {MARKET_US}` 제한은 완전히 제거했습니다.
    - **화면 코드 정리**: `views/scorecard_view.py`의 `_KR_TICKER_LIKE`/`NAME_LOOKUP_MARKETS`는 삭제(형식 판정 로직은 데이터 계층인 `scorecard_db.py`로 이동해 재사용·단위테스트가 쉽도록 함). `_reset_input_fields()`가 지우는 session_state 키도 `scorecard_ticker`+`scorecard_name` 2개에서 `scorecard_query` 1개로 단순화.
    - **검증**: 테스트 214 → **228체크**(`resolve_stock_query` 단위 테스트 10건 신설 — 코드 직접 입력/앞자리 0 복원/이름 입력(한국 포함)/유니버스 밖 코드 그대로 채택/이름 중복 시 추측 안 함 등). 기존 5개 스위트 전부 통과. `git diff -w --stat` 범위 = `utils/scorecard_db.py`·`views/scorecard_view.py`·`tests/test_scorecard.py` 3개 파일뿐.

77. **📊 "내 성적표" 화면 UI 다듬기 3건 (2026-08-11, #76 당일 후속) — 오너 스크린샷 기반 피드백.** 실제 화면 스크린샷 2장을 보고 지적한 문제들을 수정했습니다.
    - **✏️/🗑️ 버튼이 옆 셀보다 아래로 처져 보임**: 원인은 "비중" 열이 좁아 "100.0%"가 두 줄로 줄바꿈되면서 그 옆 버튼이 밀려 내려간 것. `st.container(key=...)`로 표 영역을 스코프한 뒤 그 안에서만 적용되는 CSS(`white-space: nowrap` + `align-items: center`)를 걸어, 값이 줄바꿈되지 않고 모든 셀이 항상 세로 중앙 정렬되도록 근본적으로 고쳤습니다(다른 화면·다른 표에는 영향 없음). 비중/수익률 열 너비도 약간 넓힘.
    - **"Trailing 적정가 · Forward 목표가" 글자가 너무 작음**: 잿빛 한 줄 텍스트였던 걸, 위에 있는 현재가/PEGY/퀀트점수와 같은 `st.metric` 카드 2개로 바꿔 같은 크기로 눈에 띄게 키웠습니다.
    - **"내 평균매입가 vs 현재가" 가시성 강화**: 오너가 아이디어 자체는 좋다고 했지만 눈에 잘 안 띈다고 지적 — 잿빛 캡션을 손익 방향에 따라 초록(`st.success`)/빨강(`st.error`)으로 바뀌는 큰 배너로 교체, 📈/📉 아이콘과 등락률(%)을 볼드로 함께 표시.
    - **검증**: 테스트는 UI 시각 요소라 소스 존재 여부까지만 확인 가능한 범위는 이미 228체크에 포함(구조 변경 없음, 텍스트/CSS만 바뀜) — 5개 기존 스위트 + scorecard 스위트 228체크 전부 회귀 없이 통과. `git diff -w --stat` 범위 = `views/scorecard_view.py` 1개 파일뿐.

78. **📊 "내 성적표" 수정/삭제 버튼을 이모지에서 Material 아이콘으로 교체 (2026-08-11, #77 CSS 정렬 수정이 안 먹혀서 근본 원인 재진단).** #77에서 CSS로 버튼 내부를 중앙정렬했는데도 오너가 재배포·리부팅까지 하고 스크린샷을 다시 보내며 "안 고쳐지고 있다"고 확인 — 원인을 다시 보니 CSS 문제가 아니라 **이모지 글리프 자체가 글꼴마다 여백이 삐뚤어서** 벌어지는 문제였습니다(flex 중앙정렬은 박스 안 콘텐츠 위치를 정할 뿐, 글자 자체의 비대칭 여백까지는 못 고침).
    - **무엇이 바뀌었나**: `st.button("✏️", ...)`/`st.button("🗑️", ...)`(이모지를 버튼 글자로 사용)를 `st.button("", icon=":material/edit:")`/`st.button("", icon=":material/delete:")`(Streamlit이 지원하는 Material Symbols 벡터 아이콘)로 교체. 폰트 글리프가 아니라 항상 같은 비율로 그려지는 아이콘이라 박스 정중앙에 고정됩니다. `help="수정"`/`help="삭제"` 툴팁은 그대로 유지.
    - **검증**: 228체크 전부 통과(문자열 그대로 남아있던 `help=` 체크는 안 깨짐, 새로 `icon=":material/edit:"`/`icon=":material/delete:"` 존재 확인 체크 추가). `git diff -w --stat` 범위 = `views/scorecard_view.py` 1개 파일뿐. ⚠️ 이 아이콘 렌더링은 샌드박스에서 직접 화면으로 확인할 수 없어 오너의 배포 후 스크린샷으로만 검증 가능 — 이번엔 실제로 정중앙에 고정된 것을 오너가 스크린샷으로 확인함.

79. **📊 "내 성적표" 수익률 색상(국내 증시 관례) + 표 정렬 기능 신설 (2026-08-11, #78 당일 후속).** 오너 요청 두 가지: ① 수익률을 오르면 빨강/내리면 파랑으로(국내 증시 관례 — 해외 관례인 초록/빨강과 다름, 한눈에 보기 쉽게), ② 표에 오름차순/내림차순 정렬 필터.
    - **수익률 색상**: `_colored_pct()` 신설 — Streamlit 마크다운 `:red[..]`/`:blue[..]` 문법으로 양수는 빨강, 음수는 파랑, 0%는 무색. `cols[5].write()` → `cols[5].markdown()`으로 교체.
    - **정렬**: `utils/scorecard_db.py`에 `sort_holding_rows(rows, field, ascending)` + `SORT_FIELD_OPTIONS`(종목명/수량/평균매입가/현재가/평가손익/수익률/비중) 신설 — **현재가를 모르는 종목(유니버스 밖)은 정렬 방향과 무관하게 항상 맨 뒤**로 고정합니다(§0-1: 없는 값에 순위를 매기지 않음). 화면에는 통화 블록(KR/US)마다 "정렬 기준"·"정렬 방향" selectbox/radio를 추가, 기본값은 "기본순서"(추가한 순서 그대로 — 사용자가 고를 때만 재정렬).
    - **검증**: `sort_holding_rows` 단위 테스트 7건 신설(오름차순/내림차순/값없는 종목 항상 맨 뒤/원본 리스트 불변/종목명 정렬 등) — 테스트 228 → **238체크**. 기존 5개 스위트 전부 통과. `git diff -w --stat` 범위 = `utils/scorecard_db.py`·`views/scorecard_view.py`·`tests/test_scorecard.py` 3개 파일뿐.

80. **📊 "내 성적표" 손익 색상을 국내 증시 관례로 화면 전체 통일 (2026-08-11, #79 당일 후속).** #79에서 수익률 표만 국내 관례(빨강=오름/파랑=내림)로 바꿔놨는데, "내 평균매입가 vs 현재가" 배너·상단 "평가손익" 합계는 여전히 #77 때 만든 해외 관례(초록/빨강)였습니다. 이 불일치를 오너에게 알렸더니 "해외관례보다는 국내관례로 가자"고 확정 지시.
    - **"내 평균매입가 vs 현재가" 배너**: `st.success`(초록)/`st.error`(빨강) → `st.error`(빨강)/`st.info`(파랑)로 교체. Streamlit에 파랑 계열 강조 박스가 `st.info` 하나뿐이라, 이름(에러/정보)과 무관하게 **색상만 빌려 쓰는 것**임을 코드 주석으로 명시(실제 에러 상태 아님에 주의).
    - **상단 "평가손익" 합계**: `st.metric`의 내장 delta 색(초록/빨강, 파랑 옵션 없음)을 `delta_color="off"`로 끄고, 바로 아래에 수익률 표와 같은 `_colored_pct()`로 직접 빨강/파랑 텍스트를 붙였습니다.
    - **결과**: 이제 "내 성적표" 화면 전체(요약 3칸·표·상세 배너)가 빨강=오름/파랑=내림 한 가지 관례로 통일됨.
    - **검증**: 회귀 방지 체크 2건 추가(배너 st.error/st.info 조합, delta_color="off") — 테스트 238 → **240체크**. 기존 5개 스위트 전부 통과. `git diff -w --stat` 범위 = `views/scorecard_view.py`·`tests/test_scorecard.py` 2개 파일뿐.

81. **📊 "내 성적표" 종목 빠른 검색(이름 일부만 쳐도 후보가 나오는 selectbox) 신설 (2026-08-11, #80 당일 후속).** 오너가 "롯데까지만 쳐도 롯데 관련 종목이 좌르르 나오면 좋겠다"고 요청 — 통합 입력칸(#76) 위에 유니버스(상위 200/550) 안에서 이름으로 빠르게 찾는 보조 검색창을 추가했습니다.
    - **무엇을 만들었나**: `st.selectbox`를 씁니다. Streamlit selectbox는 클릭 후 타이핑하면 옵션을 그 자리에서 자동으로 필터링해주는 내장 기능이 있어(별도 자바스크립트·API 호출 없이) "이름 일부만 쳐도 후보가 나오는" 동작이 그대로 됩니다. 옵션은 현재 선택된 시장의 유니버스 종목명 전체(정렬됨). 하나를 고르면 `st.session_state["scorecard_query"]`에 자동으로 채워지고 재실행됩니다.
    - **§0-1 지킴**: 이 검색창은 유니버스 안 종목만 후보로 보여줍니다(밖의 종목을 지어내서 보여줄 수 없으므로). 유니버스 밖 종목은 기존처럼 아래 통합 입력칸에 코드를 직접 입력하는 경로가 그대로 남아있습니다 — 이 selectbox는 "빠른 선택" 보조 수단일 뿐, 유일한 입력 경로가 아닙니다.
    - **`_reset_input_fields()` 확장**: 추가 성공 시 이 selectbox(시장별로 키가 다른 `scorecard_picker_KR`/`scorecard_picker_US`)도 함께 초기화하도록 반영.
    - **검증**: 테스트 240 → **243체크**. 기존 5개 스위트 전부 통과. `git diff -w --stat` 범위 = `views/scorecard_view.py`·`tests/test_scorecard.py` 2개 파일뿐.

82. **📊 "내 성적표" 모바일 대응 2건 (2026-08-11, #81 당일 후속) — 오너가 "모바일이 기본인 세상이니 미리 고려하자"고 지시.** 데스크톱에서는 안 보이던 문제를 화면 폭이 좁을 때를 가정해 미리 손봤습니다.
    - **표가 좁은 화면에서 세로로 깨지는 문제**: Streamlit은 화면이 640px보다 좁아지면 `st.columns()`로 만든 칸들을 자동으로 세로로 쌓아버리는 내장 반응형 규칙이 있습니다. 이 표(종목/수량/평균매입가/현재가/평가손익/수익률/비중/수정/삭제, 9칸)가 그대로 세로로 쌓이면 글자가 겹쳐 보이고 표 모양이 완전히 깨집니다. 표 영역만 스코프한 CSS로 `@media (max-width: 640px)`에서 세로 쌓임을 막고(`flex-direction: row !important`, `min-width: 700px`), 바깥을 가로 스크롤(`overflow-x: auto`)되게 감쌌습니다 — 좁은 화면에서는 좌우로 스와이프해서 표 전체를 봅니다(글자를 억지로 줄이는 대신 읽기 쉬운 크기를 유지하는 쪽을 택함). 입력 폼처럼 원래 세로로 쌓이는 게 자연스러운 곳은 그대로 둠(표에만 적용).
    - **"종목" 칸에서 이름+코드가 옆 칸(수량)과 겹쳐 보이는 문제**: 오너가 실제 화면 스크린샷에 빨간 줄로 표시해 지적. 원인은 이름+코드를 한 줄에("삼성전자우 (005935)") 억지로 넣는데 표 전체에 걸어둔 `white-space: nowrap`(#77, 버튼 처짐 방지용) 때문에 줄바꿈이 아예 안 되던 것. `_row_label_html()` 신설 — `<br>`로 "종목명 / (코드)" 두 줄을 **명시적으로** 강제해, nowrap과 충돌 없이 항상 두 줄로 표시되도록 했습니다.
    - **검증**: 테스트 243 → **245체크**. 기존 5개 스위트 전부 통과. `git diff -w --stat` 범위 = `views/scorecard_view.py`·`tests/test_scorecard.py` 2개 파일뿐. ⚠️ 이 샌드박스는 실제 모바일 화면을 렌더링해 볼 수 없어 브라우저 반응형 규칙 지식에 기반한 수정입니다 — 오너의 실제 휴대폰 확인이 필요합니다.

83. **📊 "내 성적표" 코스피+코스닥+ETF 전체 상장종목 마스터 목록 신설 (2026-08-11, #82 당일 후속) — 오너 지시: "코스피 시가총액 200종목 외 유니버스의 입력이 가능하도록" / 범위 확인: "코스피 코스닥 전체 ETF까지 포함해야하니까".** 지금까지 종목 입력은 밸류에이션이 있는 상위 200(코스피)/550(미국) 유니버스 안에서만 이름 검색이 됐고, 그 밖 종목은 코드를 정확히 알아야만 입력할 수 있었습니다. 이번 작업으로 코스피+코스닥+국내ETF 전체 상장종목을 대상으로 이름 검색이 가능해졌습니다(단, 가격/밸류에이션은 여전히 상위 200 안에 있는 종목만 나옵니다 — 그 밖은 종목명+코드만 확인용으로 제공).
    - **`collector_kospi200.py`에 신규 수집기 `run_kr_ticker_master_collector()` 추가**: 이미 프로젝트에서 쓰고 있던 FinanceDataReader로 `StockListing('KRX')`(코스피+코스닥 전 종목)와 `StockListing('ETF/KR')`(국내 ETF)를 호출해 종목코드↔이름(+시장 구분·주식/ETF 구분)만 담은 `data/kr_ticker_master.json`을 새로 만듭니다. **가격·밸류에이션은 절대 담지 않습니다**(이름 검색 보조용일 뿐, §0-1 위반 방지 — 상위 200 밖 종목에 "찾았지만 가격은 없음"이 아니라 애초에 가격 필드 자체가 없는 구조로 설계). FDR 버전이 바뀌어 컬럼명이 달라지거나 응답이 아예 실패해도 예외를 밖으로 던지지 않고 조용히 건너뛰도록(기존 핵심 수집에 영향 없이) 방어적으로 작성. `__main__`에서 핵심 수집(`run_kospi200_collector`) **뒤에**, try/except로 감싸 실행 — 이 보조 기능이 실패해도 핵심 코스피 200 수집 결과는 그대로 유지됩니다. `.github/workflows/scrape.yml`은 이미 `data/` 전체를 커밋하므로 워크플로우 파일 수정 없이 자동으로 커밋됩니다.
    - **`utils/scorecard_db.py`에 로더 `load_kr_ticker_master()` 추가**: 이 JSON을 읽어 기존 `build_universe_index()`로 코드→정보 딕셔너리로 변환. 파일이 아직 없으면(수집이 아직 한 번도 안 돌았으면) 빈 딕셔너리를 돌려줄 뿐 에러를 내지 않음.
    - **`resolve_stock_query()`에 3단계 폴백으로 확장(`broad_index` 파라미터 신설, 기본값 None으로 하위호환 100% 유지)**: ① 기존 상위 200/550 유니버스에서 코드/이름 일치 → ② (신규) 그래도 못 찾으면 전체 상장종목 마스터 목록에서 코드/이름 일치(찾으면 이름은 확인되지만 가격은 없음) → ③ 그래도 못 찾으면 "코드처럼 생겼으면" 그대로 받아들임(기존 동작) → ④ 최종 실패 시 "코스피·코스닥·국내ETF 전체 상장종목에서도 찾지 못했다"는 더 정확한 안내 문구. 여러 후보가 겹치는 이름(모호한 경우)은 기존 `find_ticker_by_name()`의 §0-1 원칙(추측 대신 실패 처리)을 그대로 재사용.
    - **`views/scorecard_view.py` 배선**: 화면 로딩 시 마스터 목록도 함께 불러오고, "종목 빠른 검색" selectbox의 후보 이름 목록에 상위 200 종목명뿐 아니라 전체 상장종목명까지 합쳐서 보여주도록(라벨도 "코스피·코스닥·국내ETF 전체"로 안내 문구 변경) 확장. 통합 입력칸에 이름을 직접 쳐도 `resolve_stock_query`가 이 넓은 목록까지 확인합니다.
    - **검증**: `tests/test_scorecard.py`에 신규 로더 단위 테스트(`test_kr_ticker_master`)와 `resolve_stock_query`의 `broad_index` 3단계 폴백 테스트(코드 직접 일치/이름 일치/ETF 이름 일치/모호한 이름 실패/전체에서도 못 찾음/하위호환 확인) 추가, `tests/test_stock_history.py`에는 수집기 자체를 가짜 FDR 응답으로 직접 호출하는 기능 테스트(`test_kr_ticker_master_collector`, 정상 수집·컬럼 구조 이상·FDR 예외·FDR 미설치 4가지 시나리오 + `__main__` 배선 순서/예외처리 확인) 신설. 두 스위트 합계 245 → **369체크**(scorecard 260 + stock_history 109). 기존 5개 스위트(macro/quant/us_scoring/us_stocks/stock_history) 전부 회귀 없이 통과. `git diff -w --stat` 범위 = `collector_kospi200.py`·`utils/scorecard_db.py`·`views/scorecard_view.py`·`tests/test_scorecard.py`·`tests/test_stock_history.py` 5개 파일 + 문서 2개뿐.
    - ⚠️ **미검증 항목(샌드박스는 외부 네트워크 접근 불가 — `pip install finance-datareader` 자체가 실패함)**: `run_kr_ticker_master_collector()`가 실제 FinanceDataReader 응답(`StockListing('KRX')`/`StockListing('ETF/KR')`)에서 어떤 컬럼명을 실제로 반환하는지는 합성 데이터로만 확인했습니다. **다음 GitHub Actions 자동 수집(또는 오너가 수동으로 워크플로우를 한 번 실행) 후, Action 로그에서 `[전체 상장종목 목록]`으로 시작하는 줄들을 확인**해 실제 수집 건수(코스피+코스닥 통상 2,500~2,700개, ETF 통상 800개 안팎)가 나왔는지, "컬럼 구조가 예상과 다릅니다" 경고가 없는지 확인이 필요합니다. 문제가 있으면 컬럼 후보 목록(`code_col`/`name_col`/`market_col`)만 로그 확인 후 조정하면 됩니다.

84. **📊 "내 성적표" 코스피+코스닥 전 종목 종가 수집기 신설 (2026-08-11, #83 당일 후속) — 오너 지시: "종가만 가지고 오면 우리는 해결되는거잖아 한번 찾아줘봐".** #83으로 상위 200 밖 종목도 이름 검색은 되게 됐지만, 여전히 현재가는 "없음"으로만 표시됐습니다. 이번엔 그 현재가 자체를 채웠습니다.

    - **왜 KRX 공식 API·증권사 API가 아니라 네이버 금융인가(오너와 함께 조사)**: KRX Data Marketplace OPEN API 이용약관 원문을 직접 확인한 결과, 제6조②에 "API 서비스를 비상업적인 목적으로만 이용할 수 있으며... 대가를 제3자에게 청구해서는 아니된다"는 조항이 명시돼 있어 — 오너가 계획 중인 광고 수익화와 정면으로 충돌합니다(§9-3 참고, 이미 매크로 화면이 쓰고 있는 VKOSPI·선물베이시스도 같은 리스크가 있음을 함께 확인). 대안으로 검토한 공공데이터포털(data.go.kr) 금융위원회 API도 같은 계열 데이터(주식배당정보)가 "공공누리 2유형(상업적 이용 금지)"으로 확인돼 마찬가지로 보류. 반면 **지금 코스피200 수집기가 이미 쓰고 있는 네이버 금융 시가총액 순위 페이지(`finance.naver.com/sise/sise_market_sum.naver`)는 이미 이 앱 전체(가격·PER·EPS·DPS)의 기존 데이터 출처라 상업적 이용 문제가 새로 생기는 게 아니라는 결론으로, 이 방식을 그대로 확장하기로 함**. (⚠️ 네이버 자체 약관 원문까지는 이번 조사에서 확인하지 못했고, 이는 확립된 결론이 아니라 오너가 감수하기로 한 리스크임을 문서로 남겨둡니다.)
    - **속도 문제 해소**: 오너가 "200종목에 50분이면 3,500개는 10시간 아니냐"고 우려 — 확인해보니 기존 코스피200 수집기가 느린 이유는 종목 하나하나 개별 페이지를 도는 방식이기 때문이고, 시가총액 순위 페이지 자체는 **페이지당 50종목씩 한 번에** 내려주는 목록형 페이지라 페이지 수만 늘리면(코스피+코스닥 전체 약 55~80페이지) 몇 분 안에 전체 시장을 커버할 수 있음을 확인.
    - **`collector_kospi200.py`에 신규 수집기 `run_kr_all_market_prices_collector()` 추가**: 코스피(`sosok=0`)·코스닥(`sosok=1`) 시가총액 순위 페이지를 끝까지(정상 응답인데 종목이 0개인 페이지를 만나면 그 시장의 마지막 페이지로 판단) 순회하며 종목코드·이름·종가만 `data/kr_all_market_prices.json`으로 저장합니다. 기존 코스피200 랭킹용 수집(`fetch_kospi200_real_market_data`)과 달리 **ETF/펀드 필터링을 하지 않고**(ETF도 조회 대상이므로) **순위 무결성도 요구하지 않습니다** — 페이지 하나가 재시도(3회) 끝에 실패해도 그 페이지(약 50종목)만 건너뛰고 계속 진행합니다(어차피 다음 실행 때 다시 시도되고, 잘못된 값이 저장되는 게 아니라 "이번엔 갱신 안 됨"일 뿐이므로 §0-1 위반 아님). 매너 있는 크롤링(요청 사이 2~3초 딜레이, 페이지별 최대 3회 재시도)은 기존 코스피200 수집기와 동일하게 유지. `__main__`에서 핵심 수집 → 전체 상장종목 목록 수집(#83) 뒤, 완전히 독립적으로(try/except) 실행 — 실패해도 앞 두 단계는 그대로 유지됩니다.
    - **`utils/scorecard_db.py`에 로더 `load_kr_all_market_prices()` 추가**: `load_kr_ticker_master()`와 마찬가지로 밸류에이션 스냅샷(`indexes`)과는 **의도적으로 분리**된 별도 구조 — 가격만 있고 PEGY/퀀트 점수는 없습니다.
    - **`make_price_lookup()`에 `broad_kr_prices` 파라미터 신설(기본값 None, 하위호환 100% 유지)**: 한국 종목이 상위 200 유니버스 안에 있으면 그 값을 그대로 우선 사용하고, 없을 때만 이 전 종목 종가 목록을 2차로 확인합니다. `valuation_summary()`는 이 폴백을 타지 않으므로 "밸류에이션 정보 없음" 문구는 여전히 정확합니다(가격은 나오지만 PEGY 카드는 계속 "정보 없음").
    - **`views/scorecard_view.py` 배선**: 화면 로딩 시 이 파일도 함께 불러와 `make_price_lookup(indexes, broad_kr_prices=kr_all_prices)`로 포트폴리오를 계산 — 상위 200 밖 보유 종목도 이제 평가금액·평가손익·수익률·비중이 실제로 계산되어 표시됩니다("현재가 없음" 대신).
    - **검증**: `tests/test_scorecard.py`에 `make_price_lookup`의 `broad_kr_prices` 폴백 단위 테스트(우선순위·2차 폴백·미국 시장 미적용·하위호환)와 `load_kr_all_market_prices()` 로더 테스트, `views` 배선 회귀 체크 추가. `tests/test_stock_history.py`에는 수집기 자체를 가짜 HTML 응답(`requests.get` 몽키패치)으로 직접 호출하는 기능 테스트 신설 — 정상 페이지네이션, 중간 페이지 실패 후 재시도·건너뛰기, 마지막 페이지 판정(빈 표), 완전 실패 시 파일 미생성, `__main__` 배선 순서/예외처리까지 확인. 두 스위트 합계 369 → **391체크**(scorecard 271 + stock_history 120). 기존 4개 스위트(macro/quant/us_scoring/us_stocks) 전부 회귀 없이 통과. `git diff -w --stat` 범위 = `collector_kospi200.py`·`utils/scorecard_db.py`·`views/scorecard_view.py`·`tests/test_scorecard.py`·`tests/test_stock_history.py` 5개 파일 + 문서뿐.
    - ⚠️ **미검증 항목(샌드박스 네트워크 제한)**: 실제 네이버 금융 페이지의 현재 HTML 구조(표 클래스명·칸 순서)에 대해서는 합성 HTML로만 검증했습니다. 기존 코스피200 수집기가 같은 페이지·같은 파싱 로직을 이미 실사용 중이라 구조 자체가 다를 가능성은 낮지만, **다음 GitHub Actions 실행 로그에서 `[전 종목 종가]` 줄들을 확인**해 코스피+코스닥 합쳐 통상 2,000~2,700건 안팎이 반영됐는지, 실패 페이지가 과도하게 많지 않은지 확인이 필요합니다. 또한 §9-3에 새 원칙으로 기록한 대로, 네이버 페이지가 향후 대규모 개편되면 이 기능뿐 아니라 앱 전체의 가격·밸류에이션 수집이 동시에 영향받을 수 있음을 오너도 인지하고 있습니다(기존에도 있던 리스크, 확장으로 새로 커진 것은 아님).

85. **📊 "내 성적표" 종목 추가 버튼이 조용히 씹히는 실사용 버그 수정 (2026-08-11, #84 당일 후속) — 오너가 실제로 종목을 추가하다가 버튼을 눌러도 아무 반응이 없는 걸 발견, 여러 라운드에 걸쳐 재현·진단.**
    - **증상**: "종목 빠른 검색" selectbox로 종목을 고른 뒤 "➕ 추가" 버튼을 누르면 에러도 성공 메시지도 없이 화면이 살짝 깜빡이기만 하고 아무 일도 일어나지 않음. 리붓·새로고침·탭 재접속·다른 브라우저 주소 직접 접속으로도 재현됨(연결/캐시 문제가 아니었음). 이상하게도 재시도하면 가끔 되기도 함(비결정적으로 보임).
    - **진단 과정**: 브라우저 개발자도구(Console·Network·WebSocket 메시지)로 연결 상태를 오너와 함께 단계별로 확인했으나 결정적 단서를 못 찾음 → 임시로 화면에 처리 단계별 디버그 메시지(버튼 클릭 감지/종목 인식 결과/저장 결과)를 노출하는 코드를 잠깐 배포했으나, 그 디버그 메시지조차 하나도 뜨지 않는 것으로 확인됨 — 이는 "추가" 버튼의 처리 코드 자체에 도달하지 못하고 있다는 강력한 단서였음.
    - **원인**: "종목 빠른 검색" selectbox(#81 신설)가 선택을 소비한 뒤 자기 자신의 위젯 상태를 `st.session_state.pop(picker_key, None)`으로 지우고 `st.rerun()`을 호출하는 방식이었음. 원인을 정확히 확정하기 위해 **오푸스로 별도 리뷰**를 받아 Streamlit 1.50 소스(`session_state.py`)까지 대조 확인함: **`pop()`은 서버 쪽 상태만 지울 뿐 브라우저에는 값이 바뀌었다고 알리지 않습니다**(Streamlit은 위젯 키에 값을 "대입"할 때만 `set_value` 플래그를 프런트엔드로 내려보냄 — 삭제는 이 신호를 만들지 않음). 그래서 브라우저가 들고 있던 "선택된 값"이 다음 상호작용(버튼 클릭 포함) 때 그대로 서버로 되돌아왔고, 이 값이 되돌아오면 selectbox 블록이 또 `st.rerun()`을 걸어버려 **클릭할 때마다 재실행이 딱 1번씩 일어나면서도 그 아래 "추가" 버튼 처리 코드에는 매번 도달하지 못하고 멈추는 현상**이었음(오푸스 리뷰로 "무한 루프"라는 초기 표현을 "클릭이 조용히 씹히는 현상"으로 정정 — CPU가 계속 도는 게 아니라 클릭 1회당 재실행 1회일 뿐). "가끔 되는" 것처럼 보인 이유도 확인됨: selectbox 후보 목록(유니버스 스냅샷)이 갱신되면 위젯의 element id 자체가 바뀌어 자동으로 리셋되기 때문.
    - **수정 1(원인)**: selectbox의 키를 고정 문자열(`scorecard_picker_{market}`) 대신 **매번 소비할 때마다 값이 올라가는 nonce를 포함한 키**(`scorecard_picker_{market}_{nonce}`)로 바꿔, 선택을 소비하면 다음 렌더에서 완전히 새로운 위젯 인스턴스가 그려지도록 함 — 이전 위젯의 잔존 상태가 되살아날 여지 자체를 없앰.
    - **오푸스 리뷰로 추가 발견된 문제 2건(같은 파일, 같은 원리)도 함께 수정**:
      1. `_reset_input_fields()`가 종목/수량/매입가 입력창도 똑같이 `pop()`으로 지우고 있었음 — 같은 원리로 브라우저에 남은 직전 값이 다음 클릭 때 되돌아올 수 있어, **같은 종목이 의도치 않게 중복 합산될 위험**(§0-1)이 있었음(이번엔 무반응 버그로는 안 나타났지만 잠재 위험). "위젯 생성 전 대입만 프런트엔드에 전달된다"는 원리를 이용해, `_reset_input_fields()`는 "다음 렌더에서 지워달라"는 표시(`scorecard_pending_reset`)만 남기고, 실제 대입은 새 함수 `_consume_pending_reset()`이 `_render_input_form()` 맨 위(위젯을 하나도 만들기 전)에서 수행하도록 재구성.
      2. `st.success(...)` 호출 직후 곧바로 `st.rerun()`이 걸려, 성공 메시지가 화면에 그려지기도 전에 다음 렌더로 교체돼 사실상 안 보이고 있었음(오너가 "에러도 성공 메시지도 없다"고 신고한 것과 일치). `st.session_state["scorecard_flash"]`에 메시지를 남겨두고, 재실행 후 다음 렌더 맨 위에서 그려주는 방식으로 교체 — 이제 실제로 화면에 성공 메시지가 남아 보임.
    - **검증**: 오푸스가 테스트 코드의 약점(문자열만 보고 통과하는 취약한 체크, 실제 리셋 동작 미검증, `module.st.session_state`를 직접 덮어써 진짜 streamlit 설치 환경을 오염시킬 수 있는 위험)까지 지적해줘서 함께 보강함 — `_consume_pending_reset()` 자체를 직접 호출해 검증하는 테스트 신설, 정적 키 pop 방식으로 되돌아가지 않았는지 소스 확인, session_state 저장/복원으로 테스트 위생 개선. 테스트 391 → **399체크**. 기존 5개 스위트 전부 회귀 없이 통과. 진단용으로 잠깐 배포했던 디버그 코드는 전부 제거함(테스트에도 "DEBUG 잔존 없음" 회귀 가드 추가). `git diff -w --stat` 범위 = `views/scorecard_view.py`·`tests/test_scorecard.py`·`TASK_HISTORY.md`뿐.
    - ⚠️ 이 버그는 실사용 중 재현된 것이고 원인도 소스 코드 대조로 확정됐지만, 오너가 실제 화면에서 여러 번 반복 테스트(특히 연속으로 여러 종목 추가)해서 최종 확인이 필요합니다.
    - **✅ 오너가 배포 후 실제 화면에서 SK하이닉스·현대차2우B·삼성전자우·SOL AI반도체TOP2플러스 등 여러 종목을 연속으로 추가해 정상 반영됨을 직접 확인함(2026-08-12).** 이 버그는 완전히 해결된 것으로 간주.

86. **📊 "내 성적표" 원형차트(보유 비중/수익 비중) 범례에서 종목코드 제거 (2026-08-12).** 오너 요청:
    "밑에 있는 보유비중이랑 수익비중 그래프있는 쪽에 종목코드는 안뜨게 할 수 없어? 종목코드만 없어져도
    더 깔끔하고 글자가 더 커질 수 있을 것 같은데." 차트 라벨 전용 함수 `_row_chart_label()`을 신설해
    종목명만 반환하도록 하고(이름이 없는 종목은 지어내지 않고 코드 그대로 노출, §0-1), 두 원형차트의
    `px.pie(names=...)`와 plotly 미설치 시 폴백 표 모두 이 함수로 교체. 표(`_row_label_html`)나 그 외
    텍스트(`_row_label`)는 종목코드 병기를 그대로 유지 — 이번 변경은 차트 범례에만 한정됨(손실 종목
    안내 캡션의 코드 병기도 그대로 유지, 요청 범위 밖). 두 차트 모두 새 라벨 함수를 쓰는지 확인하는
    소스 검사 2건 추가, 실행 시 출력을 직접 세어 확인(scorecard **280체크** + stock_history 120체크
    = **400체크**, 실패 0건 — 과거 기록된 "399"는 조건부 체크 등으로 실행 시점마다 개수가 소폭 달라질
    수 있어 이번엔 실측값으로 정정). `git diff -w --stat` 범위 = `views/scorecard_view.py`·
    `tests/test_scorecard.py`뿐.

87. **📊 "내 성적표" 미국주식 종목 빠른 검색이 티커로는 엉뚱한 결과를 보여주던 문제 수정 (2026-08-12).**
    오너가 미국 시장에서 "XOM"(엑슨모빌 티커)을 쳤더니 관련 없는 회사 이름들(American Express,
    Chipotle 등)이 잔뜩 나오는 걸 발견하고 "티커가 먼저 검색이 되야 하는게 아냐?"라고 지적. **원인**:
    #81에서 만든 빠른 검색 후보 라벨에 **종목명만** 들어있고 티커는 아예 없었음 — Streamlit
    selectbox의 내장 필터는 입력한 글자들이 그 순서대로 어딘가에 등장하기만 하면 맞는 걸로 보는
    퍼지(fuzzy subsequence) 매칭이라, "X-O-M"이 "eXpress cOMpany"처럼 이름 속에 우연히 순서대로
    박혀 있는 경우까지 다 걸려버렸음 — 진짜 티커 일치가 하나도 없었던 것. **수정**: 후보 라벨을
    "종목명"에서 **"티커 · 종목명"**으로 바꿔 티커 자체를 검색 대상 텍스트 맨 앞에 포함시킴 —
    이제 티커를 그대로 치면 라벨 시작 지점부터 정확히 일치해 최상단에 뜨고, 이름 검색도 그대로 됨.
    선택했을 때 입력칸에 채우는 값도 라벨 전체 문자열 대신 `candidate_map`으로 매핑한 **실제
    티커/코드**로 바꿔, 뒷단 `resolve_stock_query()`가 가장 확실한 "코드로 바로 확정" 경로(1단계)를
    타도록 함(§0-1). 한국 시장도 같은 방식으로 통일(코드가 라벨 앞에 붙음). 테스트 실행 결과
    scorecard **281체크**(+stock_history 120 = **401체크**), 실패 0건. `git diff -w --stat` 범위 =
    `views/scorecard_view.py`·`tests/test_scorecard.py`뿐.

88. **📊 "내 성적표" 가독성 3건 — 미국 종목 한글 표기 + "VS" 배너 렌더링 버그 + ETF 긴 이름 줄바꿈
    (2026-08-12).** 오너가 스크린샷으로 세 가지를 한 번에 지적: ① "VS" 배너 글자가 이상한 글꼴로
    겹쳐 보임, ② 한국 쪽보다 미국 쪽 가독성이 많이 떨어짐, ③ 미국 종목명이 너무 길고 티커+한글
    표기가 낫겠다, ④ (겸사겸사) 한국 ETF도 이름이 길어지면 나중에 끊어질까 걱정.
    - **① "VS" 렌더링 버그(진짜 원인 발견)**: 겉보기엔 "글꼴/간격 문제"처럼 보였지만 실제로는
      **마크다운 렌더링 버그**였음. `format_amount()`가 만드는 "$147.80" 표기를 그대로
      `st.error`/`st.info` 마크다운 문자열에 두 번(매입가·현재가) 넣으면, Streamlit 마크다운
      (KaTeX)이 그 "$"와 "$" 사이를 LaTeX 수식으로 오인해서 렌더링이 깨짐 — 스크린샷의 이탤릭
      수식체로 겹쳐 보이던 "VS"가 바로 그 증거. 원화는 "원" 접미사라 "$"가 아예 없어 이 문제가
      원천적으로 없었고, 이게 정확히 "한국보다 미국 쪽이 떨어진다"던 신고와 일치. **수정**:
      `_md_amount()` 신설 — `format_amount()` 결과의 "$"를 "\\$"로 이스케이프해 화면엔 그대로
      "$147.80"으로 보이면서 수식으로는 해석되지 않게 함. "VS" 배너뿐 아니라 손실 종목 안내
      캡션(손실 종목 2개 이상이면 같은 방식으로 깨질 수 있었던 잠재 버그)에도 적용. `st.metric`은
      애초에 마크다운을 안 거쳐 이 버그가 없어 그대로 `format_amount()`를 유지(과잉수정 방지).
    - **② 미국 종목 티커+한글 표기**: "티커로만 표시하고 종목명을 한국어로 표기할 수 없을까?"
      요청대로, 표·차트·손실 종목 캡션·밸류에이션 선택 드롭다운 전부 영문 풀네임 대신
      "한글명 (티커)" 형식으로 바꿈. **새 로직을 만들지 않고**, 공개 미국주식 화면
      (`views/us_stocks_view.py`)이 이미 쓰고 있는 것과 완전히 같은 값을 재사용 — 상위 550
      유니버스 안 종목은 수집 시점에 미리 계산해 스냅샷에 넣어둔 `name_kr`(정식 한글명 사전
      우선 + 규칙 기반 자동 음역 폴백, `utils/company_names_kr.py`)을 그대로 읽고, 그 유니버스
      밖이라 스냅샷에 없는 종목만 같은 모듈을 즉석에서 호출해 보조로 만듦(§0-1 — 한글명을
      전혀 못 만드는 극히 드문 경우는 지어내지 않고 영문명/티커로 되돌아감). 새 함수
      `_us_korean_name()`/`_display_name()` 신설, 기존 `_row_label()`/`_row_chart_label()`/
      `_row_label_html()`이 모두 `indexes`를 받아 이를 거치도록 시그니처 확장.
    - **③ ETF 등 긴 종목명 줄바꿈 대비**: 표의 "종목" 칸에 부모 표 전체에 걸려있던
      `white-space: nowrap`(#77, 버튼 처짐 방지용)을 이 칸에만 인라인 스타일로 다시 풀어(부모
      클래스 CSS보다 인라인 스타일이 우선순위가 높아 확실히 적용됨) 이름이 아무리 길어도 옆
      칸을 밀어내거나 잘리지 않고 자연스럽게 줄바꿈되도록 함 — 숫자 칸들의 nowrap(버튼 정렬
      유지용)은 그대로 유지되어 영향 없음.
    - **검증**: 소스 문자열 검사뿐 아니라 실제 함수를 합성 데이터로 직접 호출해 검증(유니버스
      안/밖 미국 종목, 한국 종목 비교군, `_md_amount()`의 KRW/USD 결과값 등). scorecard 테스트
      실행 결과 **301체크**(+stock_history 120 = **421체크**), 실패 0건. `git diff -w --stat`
      범위 = `views/scorecard_view.py`·`tests/test_scorecard.py`뿐.
    - ⚠️ 음역 결과는 근사치입니다(`utils/company_names_kr.py` 자체 한계, §0-1에 이미 문서화됨) —
      정식 한글명 사전(`KR_NAME_OVERRIDES`)에 없는 종목은 자동 음역이 어색할 수 있고, 고치고
      싶으면 그 사전에 티커 한 줄만 추가하면 공개 화면·성적표 양쪽에 동시에 반영됩니다.

89. **📊 "내 성적표" 손실 종목 안내 캡션 정리 (2026-08-12, #88 당일 후속).** 배포 확인 스크린샷에서
    오너가 "미국주식쪽은 예쁘게 잘 된거 같고 한국주식쪽은 수익비중에 있는 저 텍스트 때문에
    이뻐보일 수가 없는건가?"라고 지적 — 한국은 손실 종목이 5개(미국은 1개)라 종목코드까지 붙은
    문장이 쉼표로 줄줄이 이어져 "수익 비중" 차트 바로 아래가 유독 지저분해 보였음. ① 차트
    범례(#86)와 똑같이 이 캡션도 종목코드를 빼고(`_row_label` → `_row_chart_label`로 교체, 코드는
    바로 위 표에서 이미 확인 가능), ② 한 문장으로 몰아 쓰던 걸 종목당 한 줄씩 목록으로 나눠
    손실 종목이 몇 개든 스캔하기 쉽게 함. 테스트 실행 결과 scorecard **302체크**(+stock_history
    120 = **422체크**), 실패 0건. `git diff -w --stat` 범위 = `views/scorecard_view.py`·
    `tests/test_scorecard.py`뿐.

90. **📊 "내 성적표" v1 마무리 순서 재조정 (2026-08-12, 오너 지시 — 코드 변경 없음, 문서만).**
    "그러면 기초공사는 일단 된 것 같아, 여기까지가 지금 작업 공정 중 어디쯤이지?"라는 질문에
    §9-4/§9-5 체크리스트 현황을 보고한 뒤, 오너가 순서를 다음처럼 재조정:
    - **모바일 실기기 확인을 최후 공정(최종 검토)으로 이동** — 나머지 항목이 다 정리된 뒤, 그
      사이 쌓인 모든 변경을 한 번에 몰아서 확인하는 쪽이 효율적이라는 판단.
    - **`Confirm email`은 완료 처리** — 오너가 Supabase Authentication 화면 스크린샷으로 토글이
      이미 켜져있음을 직접 확인.
    - **#84(전 종목 종가) 확인과 함께, 미국 상위 550 밖 종목 가격을 어떻게 할지도 같이 고민하기로
      함** — 지금은 미국 쪽에 한국(#83·#84)과 같은 "전 종목" 확장이 없어 상위 550 밖은 계속
      "현재가 없음"으로 남습니다. 같은 방식(네이버류 공개 스크래핑)이 미국에도 있을지, 다른
      방법이 나을지는 **아직 정해지지 않았고, 오너가 #84 결과를 확인하고 올 때 함께 조사·논의
      예정**입니다. 이번엔 조사도 코드 변경도 하지 않았습니다.
    - **v2(OCR)·v3(리포트) 우선순위를 뒤바꿈** — §9-4가 끝나는 대로 **리포트 쪽을 먼저** 착수.
      "리포트는 내 성적표 밑의 파트로 별도 모듈로 만드는게 나을 것 같아, 내성적표 섹터에서 다
      표기하기에는 정보가 너무 많다"는 오너 판단에 따라, 리포트는 "내 성적표"와 **별도의
      독립 화면/모듈**로 만들기로 확정(기존 코스피/미국주식/내 성적표처럼). 착수 시점에
      `SCORECARD_WORK_ORDER.md`처럼 이 모듈 전용 지시서를 새로 작성할 예정 — 아직 착수 전.
    - **리포트 모듈 코딩은 오푸스(Opus)를 높은 추론 강도로 쓸 것 — 오너 지시.** 지금까지 "내
      성적표" 버그 수정·가독성 다듬기는 대부분 소넷으로 처리했지만, 리포트는 새 모듈을
      처음부터 설계하는 작업이라 착수 시점에 이 지시를 반드시 다시 확인.
    `PROJECT_STATUS.md` §9-4·§9-5만 갱신, 코드·테스트 변경 없음. `git diff -w --stat` 범위 =
    `PROJECT_STATUS.md`·`TASK_HISTORY.md`뿐.

91. **📊 "내 성적표" #84(전 종목 종가 수집기) 실검증 완료 (2026-08-12).** 오너가 GitHub Actions
    스케줄 실행 이력을 확인하는 과정에서, 세션 날짜를 2026-08-13으로 착각해 "어제(8/12) 스케줄이
    빠졌다"고 잘못 진단한 걸 오너가 실제 휴대폰 시각(8/12 12:08)으로 정정 — `TASK_HISTORY.md`·
    `PROJECT_STATUS.md`에 잘못 찍힌 "2026-08-13" 날짜 6~11군데를 전부 "2026-08-12"로 정정.
    (자동화 자체는 문제 없었음 — 8/11 스케줄 실행은 정상, 8/12 스케줄은 아직 시각이 안 돼서
    안 보였을 뿐.) 이 과정에서 "GitHub Actions 로그 대신 로컬 pull본으로 확인할 수 없나"는
    오너 질문에 착안해, 로컬 저장소의 `data/kr_all_market_prices.json`을 직접 열어 §9-4 1번을
    실검증: 2026-08-11 22:09 수집분, **코스피 2,477 + 코스닥 1,819 = 4,296건**(ETF 포함), 가격
    None인 종목 **0건**. 결정적으로, 오너가 그날 스코어카드 화면에서 실제로 입력·확인했던
    SK하이닉스(1,425,000원)·현대차2우B(199,400원)·NH투자증권(28,350원)·SOL AI반도체TOP2플러스
    (15,715원) 가격이 이 파일 값과 **정확히 일치** — 화면에 보였던 값이 실제로 이 수집기의
    산출물이었음을 직접 대조 확인. §9-4는 이제 모바일 최종 확인(최후 공정)만 남음. 코드
    변경 없음, `PROJECT_STATUS.md`·`TASK_HISTORY.md`만 갱신.

92. **📊 "내 성적표" 미국 상장 전 종목 현재가 수집기 신설 (2026-08-12, #84의 미국판) — 오너 지시:
    "코스피에서 한 것과 같은 원리로 미국도 풀어달라".** 한국은 #84로 상위 200 밖 종목도 실제 종가가
    나오게 됐지만, 미국은 여전히 상위 550 밖이면 전부 "현재가 없음"이었습니다. 기존
    `collector_us_stocks.py`가 **종목 하나당 페이지 하나**를 도는 방식이라 550종목에 40~56분이
    걸리는 게 한계의 이유였고(전체 미국 상장은 약 5,600종목), 이번엔 코스피 때와 똑같이
    **"한 번에 여러 종목을 주는 묶음 소스"**를 찾아 가격만 따로 모으는 방식으로 풀었습니다.

    - **막다른 길 하나 — 구글 파이낸스**: 예전에는 전체 종목을 페이지네이션으로 훑을 수 있었지만,
      2026-08 현재 베타 화면은 "많이 오른 종목 / 많이 내린 종목 / 거래량 상위"를 카테고리당 4~6개씩만
      보여주는 큐레이션 페이지로 바뀌어 전 종목 목록 구조 자체가 없습니다. 이 경로는 폐기.
    - **선택한 소스 — stockanalysis.com 자체 종목 스크리너**: 이미 이 프로젝트가 종목별 통계
      페이지로 쓰고 있는 **같은 사이트**라 라이선스/약관 리스크가 새로 생기지 않고(robots.txt 는
      `Disallow:` 없이 일반 크롤러 전부 허용), 표에 Symbol / Company Name / Stock Price 가 직접
      들어있어 종목별 페이지를 또 들어갈 필요가 없습니다.
    - **기술적 함정 — 페이지 넘김이 서버가 아니라 자바스크립트에 있었음**: 스크리너 1페이지는
      서버사이드렌더링이라 평범한 HTML 파싱이 되는데, `?p=2` 같은 쿼리로는 2페이지 HTML이 나오지
      않습니다(SvelteKit 클라이언트가 처리). 대신 SvelteKit 데이터 엔드포인트
      (`/stocks/screener/__data.json`)를 직접 부르면 데이터가 오는데, 이게 **평범한 JSON 이 아니라
      "devalue" 직렬화 포맷**입니다 — 모든 값을 평평한 배열 하나에 담고 컨테이너 안에는 값 대신
      그 배열의 **인덱스**를 넣으며, 같은 값(예: 여러 종목이 공유하는 업종명, GOOGL/GOOG 의 동일
      회사명)은 한 번만 저장해 여러 곳에서 같은 인덱스를 가리킵니다. `json.loads()` 만으로는 원래
      구조가 안 나와서 **전용 디코더를 직접 구현**했습니다(`collector_us_stocks.py` §3-2
      `_devalue_deref()` / `decode_sveltekit_data_json()`).
    - **실응답으로 확인한 결정적 사실 — 페이지네이션이 아예 필요 없었음**: 이 세션에서 실제
      `__data.json` 응답을 받아 뜯어본 결과, `?p=3` 으로 요청해도 응답에는 **시가총액 1위(NVDA)부터
      전 종목이 통째로** 들어 있었고, 응답의 `count` 필드와 행 인덱스 배열 길이가 **정확히 5,607개**로
      일치했습니다(행 배열 원소 수를 정규식으로 직접 세어 확인). 즉 **요청 1회로 미국 상장 5,607종목의
      티커·회사명·현재가를 전부** 받습니다 — 550종목에 40~56분 걸리던 것이 사실상 몇 초가 됩니다.
      다만 사이트가 나중에 진짜 페이지네이션으로 바뀔 수 있으므로 수집기는 페이지 루프 구조를
      유지하고, "새로 추가된 티커가 없으면 멈춤 / 소스가 알려준 전체 수에 도달하면 멈춤"으로
      **오늘은 1회 요청에서 자연스럽게 끝나도록** 했습니다.
    - **`collector_us_stocks.py`에 신규 수집기 `run_us_all_market_prices_collector()` 추가**:
      결과는 `data/us_all_market_prices.json`(`{"metadata": {...}, "stocks": [{"symbol","name","price"}]}`).
      §0-1 — **밸류에이션은 일부러 담지 않습니다**(가격만 있고 PER/PEGY/퀀트점수가 전부 None 인
      종목을 "찾음"으로 표시해 오해를 주지 않으려고 파일 자체를 분리, `kr_all_market_prices.json`과
      같은 설계). 가격을 숫자로 못 읽거나 0 이하인 행은 0 으로 메우지 않고 버립니다. 페이지 하나가
      재시도(3회) 끝에 실패해도 그 페이지만 건너뛰고 계속 진행하고, 차단(403/429)은 §0-3-2대로
      재시도 없이 즉시 중단하되 **그때까지 받은 분량은 저장하고 "차단당했다"는 사실을 metadata 에
      남깁니다**. 한 종목도 못 받으면 파일을 아예 만들지 않아 기존 파일이 보존됩니다.
    - **자동화 배선**: `cmd_collect()`에서 기존 550종목 수집이 **끝난 뒤** try/except 로 감싸 독립
      실행 — 실패해도 550종목 스냅샷은 그대로입니다. ⚠️ 의도적으로 `--skip-if-not-ready` 사전 점검을
      **통과한 실행에서만** 돌게 뒀습니다: 서머타임 때문에 cron 이 두 번 걸려 있는데 그중 한 번은
      아직 **장중**(EST 기간 20:35 UTC = 15:35 ET)이라, 점검 앞에 두면 장중 가격을 담게 되어
      §0-3-1(후행지표 전용, 장마감 종가만)을 어기게 됩니다. `.github/workflows/scrape_us.yml`은
      이미 `git add -A data/` 라 **수정 없이** 새 파일이 자동 커밋됩니다. 오너가 로컬에서 이 부분만
      돌려볼 수 있게 `python collector_us_stocks.py prices` 진입점도 추가.
    - **`utils/scorecard_db.py` / `views/scorecard_view.py` 배선**: `load_us_all_market_prices()`
      로더 신설(한국판과 완전히 같은 패턴, 파일 없으면 에러 아니라 빈 dict) + `make_price_lookup()`에
      `broad_us_prices` 파라미터 신설(기본값 None, 기존 호출부 하위호환 100%). 미국 종목이 상위 550
      유니버스 안에 있으면 그 값을 우선 쓰고, 없을 때만 이 목록을 2차로 확인합니다. 두 폴백(한국·미국)은
      시장별로 엄격히 분리돼 교차 조회 경로가 없습니다.
    - **검증**: devalue 디코더는 **이 세션에서 실제로 받은 응답 원문의 앞부분을 그대로 잘라낸 픽스처**
      (`tests/fixtures/us_screener_data_json_head.json` — 값은 한 글자도 손대지 않고 뒤쪽 5,600여 행과
      화면 메타데이터만 잘라냄)로 검증했습니다: `count=5607` 판독, 값 공유 복원(GOOG 가 GOOGL 과 같은
      회사명 인덱스를 가리키는 것, AVGO 가 NVDA 의 업종 인덱스를 가리키는 것), 잘려나간 뒤쪽을 가리키는
      인덱스는 크래시 대신 None. 수집기 자체는 가짜 `requests` 응답으로 실제 함수를 직접 호출하는 기능
      테스트(중간 페이지 실패 후 건너뛰기, 소스가 페이지 파라미터를 무시할 때 즉시 멈춤, 차단 시 재시도
      없이 중단+부분 저장, 완전 실패 시 파일 미생성, `cmd_collect` 배선 순서·try/except·사전 점검 뒤에
      오는지)로 검증. 두 스위트 합계 422 → **462체크**(scorecard 315 + stock_history 147). 기존 4개
      스위트(macro/quant/us_scoring/us_stocks) 전부 회귀 없이 통과. `git diff -w --stat` 범위 =
      `collector_us_stocks.py`·`utils/constants_us.py`·`utils/scorecard_db.py`·`views/scorecard_view.py`·
      `tests/test_scorecard.py`·`tests/test_stock_history.py` 6개 파일 + 신규
      `tests/fixtures/us_screener_data_json_head.json` + 문서 2개.
    - ⚠️ **미검증 항목**: 실제 GitHub Actions 환경에서의 최초 실행(응답 크기 약 1.1MB 다운로드,
      5,600여 종목 저장)은 아직 안 돌아봤습니다. 다음 미국주식 자동 수집 로그에서
      `[미국 전 종목 현재가]` 줄을 확인해 5,000건 이상 반영됐는지, 실패/차단이 없었는지 확인이
      필요합니다(#84 때와 같은 실검증 절차). 또한 devalue 는 사이트 프레임워크 내부 포맷이라
      **언젠가 형식이 바뀔 수 있습니다** — 그때는 이 수집기만 0건으로 조용히 실패하고(파일 미생성)
      기존 550종목 밸류에이션 수집과 화면은 그대로 동작합니다.

93. **📊 "내 성적표" 미국 ETF 현재가 수집기 신설 — 오너 지시로 "ETF 제외" 원안을 뒤집음
    (2026-08-12, #92 당일 후속).** 오너 지시 원문: *"아니 그냥 ETF까지도 넓히자 긁어오는데
    문제가될까? 미국주식은 개별주식을 가지고 있는 사람도 많겠지만 ETF로 투자하는 비율도 소수라고
    할 수 없을 만큼 많기 때문에 무시할 수는 없어"*. 실제 계기는 오너가 보유한 **KORU(코러,
    Direxion Daily MSCI South Korea Bull 3X ETF)**가 성적표 화면에서 계속 "현재가 없음"으로
    뜨던 것이었습니다. 기존 미국 수집기 2종(550종목 밸류에이션 `run_us_collector`, #92 전 종목
    현재가 `run_us_all_market_prices_collector`)은 둘 다 오너 원안(`collector_us_stocks.py`
    §"③ ETF/펀드류 (오너 원안: ETF 제외)")에 따라 ETF를 **의도적으로** 빼 왔는데, 이번에 그
    제외 정책이 **현재가 표시에 한해** 명시적으로 뒤집혔습니다.

    - **뒤집힌 것과 뒤집지 않은 것**: 넓힌 건 **"현재가를 보여주는 범위"**뿐입니다.
      **밸류에이션(PER/PEGY/퀀트점수) 유니버스는 여전히 상위 550 보통주 전용**으로 그대로 뒀습니다
      — ETF에는 EPS·ROE 같은 기업 재무제표가 아예 없어서 PEGY를 계산하는 시늉만 해도 §0-1(지어내지
      않기) 위반이기 때문입니다. 화면에서 ETF는 "현재가는 있고 밸류에이션 정보는 없음"으로 정직하게
      표시됩니다.
    - **소스 조사 (실응답으로 확인, 추측 없음)**: `https://stockanalysis.com/etf/screener/` —
      #92와 **같은 사이트**라 약관/라이선스 리스크가 새로 생기지 않습니다. 화면 컬럼은 Symbol /
      Fund Name / Asset Class / Assets / Stock Price / % Change / Volume / Holdings 로 주식
      스크리너와 다르고, 페이지네이션 UI도 "Page 1 of 279"로 표시되지만 —
      **`/etf/screener/__data.json` 을 실제로 받아 확인한 결과**:
        · 주식 스크리너와 **완전히 같은 SvelteKit devalue 직렬화 포맷**이고,
        · `count = 5480`(응답이 알려준 전체 ETF 수), 평평한 배열의 인덱스가 32,000번대까지 이어져
          **한 번의 요청에 전 ETF가 통째로** 옵니다(주식 5,607개 때와 같은 구조),
        · 한 행의 실제 모양은
          `{"s":"VOO","n":"Vanguard S&P 500 ETF","assetClass":"Equity","aum":1033499074065,`
          `"price":691.59,"change":0.13,"volume":1180435,"holdings":519}` —
          즉 **티커(`s`)·이름(`n`)·현재가(`price`) 세 키가 주식 스크리너와 글자 그대로 동일**합니다.
          컬럼 순서(위치 인덱스)에는 애초에 의존하지 않으므로(§2-1 정신 — 키 이름으로 집음)
          **ETF 전용 컬럼 매핑이 필요 없었습니다**. ETF 전용 키(assetClass/aum/volume/holdings)는
          "가격 전용 파일" 목적에 맞게 **일부러 저장하지 않습니다**(주식 파일과 구조 동일 유지).
        · 사이트 헤더에 적힌 "5561 ETFs"와 응답의 `count=5480`이 다른데, 지어내지 않고 응답이 알려준
          값을 그대로 `metadata.source_reported_count`에 기록합니다.
    - **구현 (재사용 우선, 스파게티 금지)**: devalue 디코더(`_devalue_deref()`,
      `decode_sveltekit_data_json()`)·행 추출(`extract_screener_rows()`)·정규화
      (`normalize_screener_rows()`)는 **한 줄도 안 바꾸고 그대로 재사용**했습니다. 바뀐 것은
      ① `fetch_us_screener_page(page, base_url=...)` 에 소스 주소 파라미터 추가,
      ② #92의 수집 본체를 공용 내부 함수 `_run_us_screener_prices_collector(label, base_url,
      filename, description, ...)`로 승격(로직 변경 없음, 하드코딩돼 있던 라벨·파일명·소스 URL·
      설명문만 파라미터화), ③ 그 위에 얇은 진입점 2개 —
      `run_us_all_market_prices_collector()`(주식, 동작 100% 동일)와
      **`run_us_all_market_etf_prices_collector()`(ETF, 신설)** — 를 대칭으로 뒀습니다.
    - **병합 vs 분리 판단 → 분리(`data/us_all_etf_prices.json` 신설)**: 한국(#83/#84)은 코스피+
      코스닥+ETF를 한 파일(`kr_all_market_prices.json`)에 넣었지만, 그건 **하나의 소스·하나의 수집
      루프**에서 나오기 때문입니다. 미국은 주식 스크리너와 ETF 스크리너가 **서로 다른 두 요청**이라
      한 파일에 합치면 ① ETF만 성공한 회차에 주식 5,600건이 통째로 날아가거나 그 반대가 되고,
      ② 읽어서 합쳐 다시 쓰는 방식은 "이 값이 언제 수집된 건지"가 뒤섞여 §0-1에 어긋나며,
      ③ 이미 있는 `us_all_market_prices.json`의 `metadata`(단일 `source` URL·`count`·`pages_fetched`·
      `source_blocked`)가 두 소스를 담는 순간 의미가 흐려집니다. 그래서 **파일은 나누고(각 파일의
      metadata가 그 내용과 1:1로 정직하게 대응), 합치는 일은 읽는 쪽(화면)에서** 합니다 — 미국 티커
      공간에서 주식과 ETF는 겹치지 않아 그냥 합쳐도 안전합니다(만에 하나 겹치면 보통주 우선).
    - **자동화 배선**: `cmd_collect()`에서 #92 주식 현재가 수집 **뒤에**, **자기만의 try/except**로
      감싸 실행합니다(하나의 try 에 묶지 않은 이유: 주식이 실패해도 ETF는 시도되고 그 반대도
      마찬가지). §0-3-1대로 `--skip-if-not-ready` 사전 점검을 통과한(=항상 장마감 이후) 실행에서만
      돕니다. 요청은 스크리너 1~2회뿐이라 서버 부하는 사실상 그대로(§0-3-2, 딜레이·재시도·차단
      즉시중단 규칙 모두 #92와 동일 코드). `.github/workflows/scrape_us.yml`은 `git add -A data/`
      라 **수정 없이** 새 파일이 자동 커밋됩니다. 단독 점검용 CLI `python collector_us_stocks.py
      etf-prices` 도 `prices`와 대칭으로 추가.
    - **`utils/scorecard_db.py` / `views/scorecard_view.py` 배선**: `load_us_all_etf_prices()`
      로더 신설(기존 3개 로더와 같은 패턴, 파일 없으면 에러 아니라 빈 dict). 화면에서
      `us_all_prices = {**us_etf_prices, **us_all_prices}` 로 합쳐 기존 `broad_us_prices` 폴백에
      그대로 넘기므로 `make_price_lookup()`은 **시그니처 변경 없음**(하위호환 100%). 이걸로 KORU
      같은 ETF도 "현재가 없음" 대신 실제 종가가 뜹니다.
    - **검증**: ETF 디코더는 **이 세션에서 실제로 받은 ETF 스크리너 응답에서 잘라낸 픽스처**
      (`tests/fixtures/us_etf_screener_data_json_head.json` — 값 무수정, 뒤쪽 5,470여 행·화면
      메타데이터만 제거)로 검증: `count=5480` 판독, 7개 행이 공유하는 자산군 값(devalue 인덱스 공유)
      복원, 소수점 셋째 자리 가격(VUG 86.885) 무손실, 잘려나간 인덱스는 크래시 대신 None, 정규화
      결과가 `{symbol,name,price}` 3필드뿐인지. 수집기는 가짜 `requests` 로 실제 함수를 호출해
      **ETF 엔드포인트로만 요청하는지(주식 주소는 한 번도 안 부름)**, 저장 파일명이
      `us_all_etf_prices.json`인지, **같은 폴더의 주식 파일을 건드리지 않는지**, metadata의 소스/건수/
      차단여부, 전부 실패 시 파일 미생성, `cmd_collect` 배선(순서·try 두 개·사전 점검 뒤)까지 확인.
      오프라인 462 → **496체크**(scorecard 315→323 + stock_history 147→173). 기존 4개 스위트
      (macro/quant/us_scoring/us_stocks) 전부 회귀 없이 통과. `git diff -w --stat` 범위 =
      `collector_us_stocks.py`·`utils/constants_us.py`·`utils/scorecard_db.py`·`views/scorecard_view.py`·
      `tests/test_scorecard.py`·`tests/test_stock_history.py` 6개 파일 + 신규
      `tests/fixtures/us_etf_screener_data_json_head.json` + 문서 2개.
    - ✅ **실검증 완료 (2026-08-12, #94)**: 아래 참고.

94. **📊 #93 ETF 현재가 수집기 실검증 완료 (2026-08-12, force 재크롤링).** 오너가 코드 푸시 후
    "Daily US Stocks Scraper" 워크플로우를 `force=true`로 수동 재실행. 원격 `data/us_all_etf_prices.json`
    직접 확인 결과 `metadata`: `count=5567`, `source_reported_count=5567`, `pages_fetched=1`,
    `failed_page_count=0`, `source_blocked=false`, `collected_at_kst=2026-08-12T16:58:54` — 예측대로
    요청 1회에 전 ETF가 실제로 담겼습니다. 화면(성적표)에서도 **KORU 현재가 $17.64로 정상 표시**
    확인(오너 스크린샷, 기존 "현재가 없음" → 실제 가격으로 교체됨). §9-4 "내 성적표" 관련 항목 전부
    완료.

95. **📈 리포트 모듈(5번째 모듈) v1 신규 구축 — 코드 완성 + 오프라인 검증까지 (2026-08-12, 오푸스
    높은 추론 강도).** 지시서는 `REPORT_WORK_ORDER.md`(단일 진실 소스). "내 성적표" 밑에 붙는
    **완전히 별도 모듈**로, 매일 자동으로 사용자별 평가금액 스냅샷을 쌓고 그 하나의 표에서
    일간/주간/월간/분기/반기/연간 리포트를 전부 계산해 보여줍니다. 기존 파일은 **한 줄도 고치지
    않았습니다**(읽기 전용 import 로만 재사용).
    - **① 벤치마크 소스 조사(§9-①) — 실응답으로 확인, 추측 없음**:
        - **한국**: 이미 `market_history.csv`에 "코스피 종가"가 매일 쌓이고 있어 **읽기 전용으로
          재사용**(매크로 파일이라 절대 쓰지 않음 — 테스트로 바이트 동일성까지 확인). 코스닥은
          이 파일에 없어 v1 제외(후속 과제).
        - **미국**: 이 프로젝트에 미국 지수 데이터가 아예 없어 새로 조사. **stockanalysis.com 에는
          지수(S&P 500 자체) 페이지가 없음**을 실제로 확인(`/quote/index/SPX/__data.json` → 데이터가
          아니라 `{"type":"redirect","location":"/symbol-lookup/?q=INDEX-SPX"}`). 대신 이 프로젝트가
          이미 화면 상단 지수에 쓰는 **추종 ETF 프록시**의 과거주가 엔드포인트
          `https://stockanalysis.com/etf/{symbol}/history/__data.json` 가 살아 있고, **요청 1회로
          약 6개월치 일별 종가**가 옵니다(2026-08-12 ONEQ 실응답: `"name":"Fidelity Nasdaq Composite
          Index ETF"`, 행 `{"a":104.16,"c":104.16,"h":105.28,"l":103.91,"o":105.28,"t":"2026-08-11",
          "v":194370,"ch":-0.6}`). 응답이 #92 와 같은 devalue 포맷이라 **디코더를 그대로 재사용**.
        - **FRED 는 조사 완료 후 일부러 채택하지 않음**: `fredgraph.csv?id=SP500` 로 **지수 실값**
          일별 종가(10년치)를 무료로 받을 수 있음을 실응답으로 확인했지만, 같은 응답 Notes 에
          S&P DJI 재배포 제한("Reproduction of S&P 500 in any form is prohibited except with the
          prior written permission of S&P Dow Jones Indices LLC")이 명시돼 있어, **#84 에서 KRX
          OPEN API 를 배제했던 것과 같은 기준**(오너의 광고 수익화 계획과 충돌)으로 보류.
          Stooq·Yahoo·Nasdaq 공식 API 는 이 환경에서 응답 자체가 오지 않아 "살아있는지도 확인 못 한
          소스"라 코드에 넣지 않았습니다(§0-1).
        - ⚠️ 그래서 저장되는 미국 벤치마크 값은 **지수 포인트가 아니라 ETF 종가**입니다(S&P500 약
          6,600pt vs SPY 약 750달러). 이 사실이 숨겨지지 않도록 **키 이름부터**
          `SP500_PROXY_SPY`/`NASDAQ_PROXY_ONEQ` 로 두고 화면 문구·DB 코멘트에도 그대로 적었습니다.
          배당 조정가(`a`)가 아니라 **미조정 종가(`c`)** 를 저장합니다(조정가는 과거 값이 소급해서
          바뀌어 이미 저장한 기록이 흔들림).
    - **② `sql/report_schema.sql`**: `portfolio_daily_snapshots`(사용자 × 시장 × 거래일 = 1행) +
      **RLS**. 기존 `holdings` 와 달리 정책이 **select 하나뿐**입니다 — 쓰기는 GitHub Actions 배치
      (service_role)만 하고, 사용자가 자기 과거 수익률 기록을 고칠 수 없어야 하기 때문입니다.
      `priced_count>0` CHECK 로 "가격을 하나도 모르는 날은 행 자체가 안 생기게" DB에서도 강제.
    - **③ `collector_us_indices.py`(신규)**: 위 엔드포인트에서 SPY/ONEQ 일별 종가를 받아
      `data/us_index_history.json` 에 **누적**. 기존 수집기 관례 그대로(요청 간 랜덤 딜레이, 재시도,
      차단 시 즉시 중단·있는 것만 저장), ETF 이름으로 "엉뚱한 종목 아님"을 매번 재확인, **이미
      저장된 날짜 값은 덮어쓰지 않고 충돌 사실만 기록**(기록 개변 금지).
    - **④ `utils/report_db.py`(신규)**: 기간 경계 계산(일/주/월/분기/반기/연) · 스냅샷 행 생성 ·
      기간 집계와 **데이터 부족 판정**(COMPLETE/IN_PROGRESS/INSUFFICIENT/NO_DATA) · 벤치마크 기간
      수익률 · Supabase 접근. 기준점은 "기간 시작 **직전** 스냅샷"이라 월간 리포트가 지난달 마지막
      값부터 제대로 재고, 그런 스냅샷이 없으면 INSUFFICIENT 로 내려 "완성 리포트"인 척하지 않습니다.
      평가금액은 "내 성적표"와 **같은 가격 조회 함수**(`make_price_lookup`)를 재사용해 화면 숫자와
      어긋나지 않게 했습니다. **이 파일만 `service_role` 키를 쓰며, 환경변수에서만 읽고 streamlit 을
      아예 import 하지 않습니다**(앱 설정에서 읽히는 경로 자체를 만들지 않음).
    - **⑤ `.github/workflows/scrape_report_snapshots.yml`(신규)**: 평일 **23:20 UTC**(코스피 07:05,
      미국 20:35/21:35 수집이 모두 끝난 뒤) 실행 → 미국 벤치마크 수집 → 스냅샷 적재 →
      `data/us_index_history.json` 만 커밋. `snapshot_date` 는 실행 시각이 아니라 **가격 스냅샷이
      말하는 거래일**이라, 앞선 수집이 실패해 어제 파일이 남아 있어도 '오늘 값'으로 둔갑하지 않습니다.
    - **⑥ `views/report_view.py`(신규)**: 기간 선택 + 이전/다음 기간 이동, 시장별 블록, 벤치마크
      비교(포트폴리오와 **정확히 같은 두 날짜**로만 계산 — 날짜가 없으면 가까운 날로 대체하지 않고
      "비교 불가"), 데이터 부족 구간은 **주 컨텐츠로** 표시. 기본 숨김(`REPORT_ENABLED` 또는 관리자).
      ⚠️ `visiblehand.py` 배선은 지시서 §7 파일 목록에 없어 **일부러 하지 않았습니다**(기존 파일
      무수정) — 미리보기를 원하면 그 파일 상단 주석의 3줄만 넣으면 됩니다.
    - **검증**: `py_compile` 전 파일 통과, 신규 `tests/test_report.py` **255체크**(가짜 응답으로
      수집기 실행 경로까지, 실제 Supabase·네트워크 없이), 기존 6개 스위트 전부 회귀 없음
      (macro 365 / quant 8 / scorecard 323 / stock_history 173 / us_scoring 94 / us_stocks 139).
    - **다음(오너)**: `sql/report_schema.sql` 실행 → GitHub Actions Secrets 에 `SUPABASE_URL`·
      `SUPABASE_SERVICE_ROLE_KEY` 등록(⚠️ Streamlit Cloud 아님) → 워크플로우 수동 실행(dry_run 먼저)
      으로 실검증. 그 전까지 스냅샷이 없으니 화면은 정상적으로 "데이터 부족"만 보여줍니다.

96. **📈 리포트 모듈 첫 자동 실행 실패 버그 2건 수정 (2026-08-12, 오푸스 고추론 — #95 당일 후속).**
    오너가 Secrets(`SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`)를 등록하고 `Daily Report Snapshots`
    를 수동 실행(dry_run)했더니 **벤치마크 수집이 0행**으로 실패하고, 이어서 커밋 단계가 **exit 128**
    로 잡 전체를 죽였습니다. 실제 응답을 다시 받아 코드와 대조해 원인을 확정하고 둘 다 고쳤습니다.
    - **버그 ①(핵심) — 행 목록을 한 겹 얕은 곳에서만 찾고 있었음.** 로그:
      `⚠️ … 수집 실패 – 응답에서 일별 시세 행을 찾지 못했습니다(소스 구조 변경 가능성)`.
      `extract_history_rows()` 가 노드의 **최상위 값들만**(`node_data.values()`) 훑었는데, 실제
      응답은 행 목록이 한 겹 더 안쪽에 있었습니다 —
      `{"data": {"symbol":"SPY","source":"tiingo","updated":"…","data":[행…]}, "news":[…], …}`.
      즉 최상위 `data` 는 **dict**(이력 블록)라 `looks_like_history_rows()` 가 곧바로 False 를
      내고, 진짜 행 배열(`data.data`)에는 닿지도 못했습니다. 소스는 멀쩡했고 우리 파싱이 틀렸던 것.
      → `iter_history_row_candidates()`(깊이 우선 재귀, 순환/중복 방지) + `extract_history_block()`
      으로 교체해 **깊이에 의존하지 않고** 찾습니다. 후보가 여럿이면 ⓐ 블록 메타데이터
      (`symbol`/`source`/`updated`/`timestamp`) 점수 → ⓑ 길이 순으로 고릅니다.
      "행 판정" 조건은 **날짜(`t`, YYYY-MM-DD) 필수**로 못박았습니다 — 같은 응답에 c/h/l/o/v 로
      글자가 겹치는 '지금 시세(quote)' 스키마가 함께 오는데, 그쪽은 날짜가 없어 이 조건 하나로
      확실히 걸러집니다.
      ⚠️ **공급자 이름(`"source":"tiingo"`)을 조건으로 걸지 않았습니다** — 같은 날 실응답에서
      SPY 는 `tiingo` 인데 **ONEQ 는 `spg`(S&P Global)** 였습니다. 조건으로 걸었으면 나스닥 쪽이
      통째로 빠졌을 겁니다(§0-1 — 추측 금지, 실응답으로 확인).
      곁들여 **종목 확인 방식도 교체**: 과거주가 엔드포인트에는 ETF **이름이 아예 없어서**(v1 주석의
      `"name":"Fidelity …"` 는 다른 페이지 값이었음) 이름 검증이 정상 응답에도 매번 거짓 경고를
      냈을 상황이었습니다. 이제 응답이 스스로 말하는 **티커**(`symbol`)로 확인하고, 이름은 있을
      때만 부가 확인합니다(없으면 경고 대신 '확인 불가' 로 기록).
    - **버그 ② — 파일이 없으면 커밋 단계가 잡을 통째로 죽임.** 로그:
      `fatal: pathspec 'data/us_index_history.json' did not match any files` → `exit code 128`.
      수집기는 "둘 다 실패하면 기존 값 보존을 위해 파일을 아예 안 만드는" **정상 동작**이 있어서
      파일 없음은 사고가 아니라 예상 가능한 상태입니다. → `if [ -f … ]` 로 **있으면만 add**,
      없으면 안내 로그만 남기고 통과. 커밋 대상은 여전히 공개 데이터 `data/us_index_history.json`
      **한 파일뿐**입니다(`-A` 로 data 폴더를 통째로 담지 않음 — 다른 워크플로우 산출물·민감 파일
      침범 방지).
    - **왜 오프라인 테스트가 못 잡았나 → 픽스처 교체.** v1 의 합성 페이로드가 행 목록을 최상위
      바로 아래에 두는 바람에(실응답과 구조가 다름) 테스트는 통과하고 실전만 실패했습니다.
      2026-08-12 실제 응답 원문을 다시 받아 앞부분을 그대로 잘라낸 픽스처 2개를 신설했고
      (`tests/fixtures/us_index_history_{spy,oneq}_data_json_head.json`, 기존 스크리너 픽스처와
      같은 관례), 파싱·수집 실행 경로 검증을 전부 이 실응답으로 바꿨습니다.
    - **재검증(실응답 전문)**: 수정 후 SPY/ONEQ 전체 응답으로 수집기를 직접 실행 —
      **각각 125행 정상 추출, 2026-02-11 ~ 2026-08-11**(SPY 최신 종가 770.56 / ONEQ 104.16),
      티커 검증 통과, 경고 0건. `tests/test_report.py` 255→**270체크** 전부 통과, 기존 6개 스위트
      회귀 없음(macro 365 / quant 8 / scorecard 323 / stock_history 173 / us_scoring 94 /
      us_stocks 139).
    - **다음(오너)**: `Daily Report Snapshots` 워크플로우 재실행(dry_run=true 먼저) → 로그에서
      두 지수 모두 `✅ … 응답 125행` 이 찍히는지, `data/us_index_history.json` 커밋이 올라오는지
      확인 → 문제 없으면 dry_run 없이 한 번 더 돌려 실제 스냅샷 적재까지 검증.

97. **📈 리포트 모듈 배치 파이프라인 실검증 완료 (2026-08-12, #95/#96 당일 후속).**
    #96 수정분 푸시 후 오너가 `Daily Report Snapshots` 워크플로우를 재실행하며 실검증했습니다.
    - **첫 재실행(dry_run=true)**: 벤치마크 수집은 경고 없이 성공(KOSPI 2026-08-12=6579.04,
      SP500_PROXY_SPY 2026-08-11=770.56)했지만, "사용자별 평가금액 스냅샷 적재" 단계에서 새 오류
      `PGRST125: Invalid path specified in request URL`. 원인 확정: 오너가 `SUPABASE_SERVICE_ROLE_KEY`
      등록 전 상태로 먼저 테스트했을 때는 "보유 종목 0행/사용자 0명"(비어있는 결과, 에러 아님)이었던
      것과 달리, 이번엔 진짜 요청 URL 오류. GitHub Secrets 의 `SUPABASE_URL` 값이 Supabase 대시보드의
      "Data API → API URL" 필드(`https://xxx.supabase.co/rest/v1/` — REST 엔드포인트 전체 경로,
      `/rest/v1/` 포함)를 그대로 붙여넣은 것으로 확인됨. `utils/report_db.py`(및 `scorecard_db.py`)의
      `create_client(url, key)` 는 **베이스 프로젝트 URL**(`https://xxx.supabase.co`, 경로 없음)을
      기대하므로 `/rest/v1/` 이 중복돼 PostgREST 가 요청 경로를 못 읽은 것. 코드 버그 아님 — 오너가
      `SUPABASE_URL` 시크릿 값을 베이스 URL로 정정.
    - **재실행(dry_run=false, 실제 적재)**: 전부 성공. 로그: `KR 거래일 = 2026-08-12
      (kospi200_pegy_latest.json) / US 거래일 = 2026-08-11 (us_stocks_latest.json)` ·
      `KR 벤치마크 KOSPI 2026-08-12 = 6579.04` · `US 벤치마크 SP500_PROXY_SPY 2026-08-11 = 770.56` ·
      **보유 종목 8행 / 사용자 1명** · **스냅샷 2행 저장(KR+US)**. 벤치마크 이력 파일 커밋도 정상.
    - **§10(리포트 모듈) 배치 파이프라인 실검증 완료** — 벤치마크 수집·holdings 조회·스냅샷 적재·
      파일 커밋 전 구간이 실제 운영 환경에서 정상 동작 확인. 남은 건 ⓐ `views/report_view.py`를
      `visiblehand.py`에 배선(현재 미배선, 오너 원할 때 3줄 추가)과 ⓑ 스냅샷이 며칠 쌓여 리포트가
      의미를 가질 때까지 대기(소급 계산 없음, §0-1).

98. **📈 리포트 화면 사이드바 배선 (2026-08-12, #97 당일 후속).** 오너 요청("연결이 어려우면
    오푸스로 작업해줘" — 이번엔 기존 패턴을 그대로 복사하는 정형화된 작업이라 소넷으로 직접
    처리). `visiblehand.py`에 "내 성적표"와 완전히 동일한 패턴으로 연결: import try/except
    가드(`views.report_view` 로드 실패해도 기존 화면은 그대로), 관리자 인증 또는 `REPORT_ENABLED`
    플래그일 때만 사이드바에 "📈 리포트 (준비중·미리보기)" 체크박스 노출, 체크 시
    `render_report_page()` 라우팅(최우선 순위). 일반 방문자 사이드바·화면은 이전과 100% 동일.
    py_compile 통과, `git diff -w --stat`= `visiblehand.py` 한 파일(29줄 추가)만, 기존 6개
    테스트 스위트 전부 회귀 없이 통과.

99. **📈 리포트를 "내 성적표" 하위 탭으로 재배치 (2026-08-12, #98 당일 후속).** 오너 피드백:
    "리포트/내 성적표 둘 다 형제 체크박스라 안 보는 쪽을 매번 꺼야 해서 쓰기 불편하다." →
    체크박스 2개를 없애고 **"📊 내 성적표" 체크박스 하나**만 남긴 뒤, 그걸 켰을 때만 그 밑에
    라디오(하위 탭, "📊 내 보유종목" / "📈 리포트")로 고르게 바꿨습니다. 코드/데이터는 여전히
    완전히 독립된 별도 모듈(구조 변경 없음) — 메뉴 배치만 중첩으로 바뀐 것. 같은 로그인 세션을
    공유하므로 탭을 오가도 재로그인이 필요 없다는 안내 문구도 라디오에 붙였습니다.
    py_compile 통과, `git diff -w --stat` = `visiblehand.py` 한 파일뿐, 기존 6개 스위트 회귀 없음.

100. **📊 "내 성적표" 켜졌을 때 시장 선택 라디오 비활성화 (2026-08-12, #99 당일 후속).** 오너가
     "내 성적표가 체크된 상태에서는 '사실 이 가격이에요'가 선택이 안 되는 것 같다"고 신고.
     원인 확인: **버그가 아니라 기존부터 있던 라우팅 우선순위**(내 성적표 체크박스가 켜져
     있으면 라우팅에서 시장 라디오 선택이 항상 무시되고 성적표/리포트 화면이 우선 표시됨)인데,
     라디오 자체는 계속 클릭 가능한 상태로 보여서 "눌러도 반응 없음"처럼 느껴진 것. 클릭해도
     안 먹힌다는 혼란을 없애려고 **내 성적표가 켜져 있으면 라디오를 `disabled=True`로 회색
     비활성화**하고, 옆에 "내 성적표를 먼저 끄세요" 안내 캡션을 붙였습니다. `view_scorecard`
     세션 상태는 그 체크박스 위젯보다 코드상 먼저 등장하는 라디오 시점에도 이전 실행 값을
     안전하게 읽을 수 있어(Streamlit 세션 상태 특성) 별도 재구조화 없이 해결. py_compile 통과,
     `git diff -w --stat` = `visiblehand.py` 한 파일뿐, 기존 6개 스위트 회귀 없음.

101. **📊 "내 성적표" 진입점을 시장 선택 라디오 안으로 통합 (2026-08-12, #100 당일 후속).**
     오너 요청: "좀 더 빠르게 전환될 수 있게 할 수는 없을까?" — #100까지는 "시장 선택 라디오"와
     "내 성적표 체크박스"가 별개 위젯이라, 성적표를 보려면 체크하고 시장 리포트로 돌아가려면
     다시 체크 해제해야 하는 2단계 조작이 필요했습니다. **하나의 라디오로 통합**해서 셋(한국/
     미국/내 성적표) 중 하나를 고르는 단일 선택으로 바꿨습니다 — 전환이 클릭 한 번으로 끝나고,
     #100에서 넣었던 "비활성화+안내 캡션" 임시방편도 필요 없어져 제거했습니다. `admin_mode` 는
     이 라디오보다 코드상 나중에 확정되지만, `st.session_state["admin_mode"]` 를 미리 읽어도
     안전함(Streamlit 세션 상태가 이전 실행 값을 이미 들고 있음)을 확인하고 그대로 활용했습니다.
     내 성적표를 고르면 그 아래 하위 라디오(내 보유종목/리포트)가 그대로 이어집니다(#99 구조
     유지). 회귀 테스트 2건이 옛 구현의 리터럴 문자열(`"show_scorecard = False"` 등)을 검사하고
     있어 새 구현에 맞게 갱신(`tests/test_scorecard.py`, 의미는 동일 — 기본 숨김 + 관리자 게이팅).
     py_compile 통과, `git diff -w --stat` = `visiblehand.py` + `tests/test_scorecard.py` 두 파일뿐,
     기존 6개 스위트 전부 통과(회귀 없음).

102. **📊 사이드바를 오너가 그린 2단 트리 구조로 재구성 (2026-08-12, #101 당일 후속).**
     #101의 "라디오 하나에 3개 선택지(한국/미국/내 성적표)"는 클릭 한 번 전환은 됐지만, 오너가
     원한 그림(대분류 2개 "💡 사실 이 가격이에요" / "📊 내 성적표"가 나란히 있고, 각각 그 밑에
     중분류가 달린 트리)과는 계층이 달랐습니다. **최상위 라디오**(대분류 택1: 사실 이
     가격이에요/내 성적표)를 새로 두고, 고른 대분류에 따라 그 밑에서만 기존 중분류 라디오(한국/
     미국, 또는 내 보유종목/리포트)가 나타나도록 재구성했습니다. 대분류 전환은 여전히 클릭
     한 번, 일반 방문자(내 성적표 비공개 상태)에게는 대분류 라디오 자체가 안 뜨고 예전과 100%
     동일하게 동작합니다(선택지가 1개뿐이면 라디오를 그리지 않고 바로 그 값으로 고정).
     #101에서 새로 쓴 회귀 테스트 2건도 새 변수명(`top_choice`/`TOP_SCORECARD`/`top_options`)에
     맞게 다시 갱신. py_compile 통과, `git diff -w --stat` = `visiblehand.py` +
     `tests/test_scorecard.py` 두 파일뿐, 기존 6개 스위트 전부 회귀 없음.

## 진행 예정 (백로그)

(현재 없음 — 다음 백로그 항목은 `PROJECT_STATUS.md`의 "지금 열려있는 일" 참고)

---

> 이후 완료되는 작업은 이 문서 "완료된 작업" 목록 맨 아래에 번호를 이어서 추가해주세요.
