# ⚔️ 결투다! (모의투자 대결) — 신규 모듈 작업지시서 (초안)

> 이 문서는 **다른 세션·다른 모델이 이 대화의 맥락을 전혀 모르는 상태에서 처음 읽어도 바로 착수할 수 있도록** 만든 독립 실행 지시서입니다. 작성일 **2026-08-19**.
>
> 근거는 세 가지입니다. ① 오너가 직접 그린 **draw.io 큰 그림 다이어그램**을 놓고 진행한 설계 검토 대화, ② 그 다이어그램을 대상으로 한 **오푸스 모델의 기술 실현 가능성 심층 검토**(배당 파이프라인 부재, 실시간 체결 불가, 공개표 격리 등), ③ 그 검토 결과에 대해 오너가 내린 **일련의 명시적 설계 결정**. 이 문서의 "확정" 표기는 전부 ③에서 나온 것이고, 오너가 아직 답하지 않은 것은 하나도 확정으로 적지 않았습니다.
>
> 함께 읽어야 하는 문서: `ENGINEERING_SPEC.md`(전체 원칙 — 특히 §0-1 · §0-3-1 · §0-3-2 · §0-3-8 · §0-3-10), `PROJECT_STATUS.md`(현재 아키텍처·파일 지도 — §0-2 작업 시작 절차 필수), `sql/scorecard_schema.sql`·`sql/report_schema.sql`·`utils/report_db.py`(이 모듈이 그대로 본뜰 기존 패턴).
>
> ⚠️ **2026-08-19 현재 이 모듈의 코드는 단 한 줄도 존재하지 않습니다.** 0단계(설계 검토)만 끝났고 1단계부터는 전부 미착수입니다. 이 문서에 "완료"로 적힌 건 0단계뿐입니다.
>
> 🔴 표시는 **오너의 명시적 결정 없이는 그 부분의 코딩을 시작하면 안 되는 지점**입니다. 🔴가 붙은 항목을 임의의 그럴듯한 값으로 채우고 진행하는 것은 §0-1 위반입니다.

---

## 한 줄 요약

> 📌 **유니버스 표기 안내 (2026-09-03 추가)**: 이 문서 곳곳의 "코스피 상위 200(종목)"은 설계 당시(2026-08-19~20)의
> 사실이고, **2026-08-26 에 코스피 수집기의 유니버스가 코스피+코스닥 통합 시가총액 상위 500 으로 확대**됐습니다
> (§3-3 "확장성 메모"가 실제로 실행된 것 — TASK_HISTORY #150, `collector_kospi200.py` 진입 500위/이탈 575위).
> 결투 코드(`utils/duel_batch.py` · `web/pages/duel_page.py`)는 이미 "통합 상위 500"으로 갱신돼 있으니, 아래 본문의
> "상위 200"은 전부 "통합 상위 500"으로 읽으세요. 날짜가 박힌 확정 기록이라 본문 문장 자체는 고치지 않았습니다.

"사실 이 가격이에요"(코스피200 / 미국주식 밸류에이션)와 "내 성적표"(실제 보유 자산)에 이어, **가상의 돈으로 하는 모의투자 대결 모듈 "결투다!"** 를 완전히 별도 모듈로 신규 구축합니다(§0-3-6). 두 갈래로 이루어집니다.

- **1갈래 — "덤벼라 나 자신"**: 사용자 1명당 1개월/3개월/6개월 주기의 **가상계좌 3개**. 시드 1천만원, 매월 10일 80만원 추가 입금, 코스피 상위 200종목만, 원화만. **매수는 예수금이 있으면 언제든, 매도는 계좌마다 그 주기(30/90/180일)당 딱 1회 리밸런싱**(✅ 2026-08-21 오너 확정 — §2-3-1 참고, 이전엔 매수 전용·매도 절대 불가였습니다). 주문은 저장 즉시 체결되지 않고 **그날 밤 배치가 확정 종가로 체결**합니다.
- **2갈래 — "내 밑으로 눈 깔어"**: 완전 옵트인 **공개 순위표**. 실제 자산이 아니라 가상계좌 성적만 공개하며, 공개 데이터는 원본 테이블이 아니라 **별도의 발행 전용 표**에서만 읽습니다.

> 📌 **모듈 번호 표기(2026-08-24 재번호 확정)**: "내 성적표"는 3번째 모듈(`MY_SCORECARD_MODULE_NOTES.md` 첫 줄, `PROJECT_STATUS.md` §9), "리포트"는 4번째(`PROJECT_STATUS.md` §10), **"결투다!"는 5번째**, "투자 감사합니다!"(배당금)는 6번째 모듈입니다. 원래 계획은 리포트=5·결투다!=4였는데, 오너가 2026-08-24에 순서를 맞바꿔 확정했습니다 — 그 이전 대화·커밋 메시지·과거 문서에 "결투다! = 4번째"라고 남아있는 곳이 있다면 그건 당시 기준입니다.

---

## 왜 이 순서인가

이 모듈은 기존 두 모듈과 성격이 다릅니다. 코스피·미국주식 화면은 "수집한 공개 데이터를 보여주는" 읽기 전용 화면이고, "내 성적표"는 "사용자가 넣은 것을 사용자 본인에게만 보여주는" 화면입니다. **"결투다!"는 이 프로젝트에서 처음으로 ① 앱이 사용자 대신 상태를 바꾸고(주문→체결→잔고), ② 그 결과를 다른 사용자에게 보여주는(순위표) 모듈**입니다. 그래서 순서를 이렇게 짰습니다.

1. **스키마부터 확정(1단계).** 이 모듈에서 되돌리기 가장 어려운 실수는 화면 버그가 아니라 **데이터 모양을 잘못 잡는 것**입니다. 특히 수익률을 시간가중수익률(TWR)로 내려면 **현금흐름이 언제 얼마 들어왔는지**가 스냅샷에 처음부터 남아 있어야 합니다. 나중에 붙이면 과거 구간 수익률을 영원히 재계산할 수 없습니다. 이건 코딩 전에 확정해야 하는 문제라 맨 앞에 뒀습니다.
2. **1갈래를 먼저, 통째로 끝낸다(2~4단계).** "덤벼라 나 자신"은 **혼자서도 완결된 제품**입니다. 순위표가 없어도 "내 가상계좌 3개가 어떻게 굴러가는지"만으로 쓸 만하고, 공개 기능이 하나도 없으니 §0-3-8 사고 위험도 0입니다. **1갈래만 만들고 배포해도 됩니다** — 실제로 그렇게 하기를 권장합니다.
3. **공개는 맨 뒤(5단계).** 공개 인프라는 §0-3-8(최상위 금지사항)의 정면에 있습니다. 1갈래 데이터가 실제로 며칠 쌓여서 "무엇을 공개하게 되는지"가 눈에 보이는 상태가 된 뒤에 손대는 게 훨씬 안전합니다. 게다가 순위표는 참가자가 최소 몇 명은 있어야 화면 자체가 성립합니다.
4. **오프라인 테스트를 실검증 앞에 별도 단계로 둔다(4단계).** `tests/test_report.py`(547체크)·`tests/test_scorecard_ocr.py`가 이미 증명한 방식입니다. 체결 계산·현금 가용액·TWR·동의 게이팅은 전부 **네트워크와 Supabase 없이** 순수 함수로 검증 가능하게 짜야 합니다.

**이미 확보돼 있어 다시 안 만들어도 되는 것** (그대로 재사용 — §0-3-10):

- **로그인·사용자 식별 인프라** — `sql/scorecard_schema.sql`의 `public.profiles`(auth.users 확장, 이메일만 보관하는 최소 PII)와 RLS 8정책 패턴(본인 행만 select/insert/update/delete, `anon` 권한 회수). 새 테이블도 이 스크립트와 **똑같은 모양**으로 쓰면 됩니다. 새 로그인 체계 만들지 마세요.
- **"사용자 × 거래일 = 1행" 일별 스냅샷 패턴** — `sql/report_schema.sql`의 `portfolio_daily_snapshots`(합계)와 `portfolio_holding_snapshots`(종목별 상세), 그리고 이를 채우는 `utils/report_db.py`. **RLS 정책이 select 하나뿐**(쓰기는 배치의 service_role만)이라는 구조, `priced_count > 0` CHECK로 "그날 가격을 하나도 모르면 행 자체를 안 만든다"는 DB 레벨 강제, `price_as_of_kst` 컬럼까지 — 이 모듈이 필요로 하는 히스토리 요구사항과 사실상 같은 문제입니다. **새 패턴을 발명하지 말고 이걸 확장하세요.**
- **기간 경계 계산·데이터 부족 판정·표시일 대체** — `utils/report_db.py`의 기간 경계 함수와 `resolve_display_date()`(기준일 이전 가장 최근 기록일을 고르는 순수 함수, 값은 하나도 만들지 않음). "주말·공휴일엔 무엇을 보여줄 것인가" 문제는 이미 §10-12에서 풀려 있습니다.
- **코스피 상위 200종목 일별 확정 종가** — `collector_kospi200.py` → `data/kospi200_pegy_latest.json`, 그리고 종목별 날짜 이력 `data/kospi200_stock_history.csv`(`utils/stock_history.py`의 `record_daily_history()`가 수집 상태가 **SUCCESS/DEGRADED일 때만** 기록). 이 모듈은 **가격을 새로 수집하지 않습니다.**
- **유니버스 밖 종목 현재가** — `data/kr_all_market_prices.json`(코스피+코스닥 전체). 상위 200에서 밀려난 보유 종목의 평가에 쓸 수 있습니다(아래 3단계 참고).
- **일 1회 배치 자동화 골격** — `.github/workflows/scrape.yml`(평일 KST 16:05, 코스피 수집), `.github/workflows/scrape_report_snapshots.yml`(평일 23:20 UTC, 스냅샷 적재). 새 배치는 이 두 개의 뒤에 붙습니다.
- **service_role 격리 규칙** — 지금은 `utils/report_db.py` 한 파일만 service_role 키를 쓰고, 환경변수에서만 읽으며, 사용자 접속 앱 프로세스에는 절대 넣지 않습니다(§0-3-8). 이 모듈의 배치도 **똑같이** 합니다.
- **동시 접속 격리 회귀 테스트** — `tests/test_web_session_isolation.py`. 새 화면도 이 스위트에 반드시 추가합니다.

**반대로, 재사용하면 안 되는 것 — `public.holdings`.** 이 표는 "사용자 1명당 실제 포트폴리오 1개"만 모델링합니다. 계좌 개념도, 가상 현금도, 주문/원장 표도, 매수 전용 제약도 없습니다. 여기에 가상 자산을 섞으면 ① 의미가 무너지고(실제/가상, 팔 수 있는 것/없는 것), ② 훗날 공개 순위표의 읽기 경로가 **실제 자산 테이블을 스칠 수 있는 구조**가 됩니다. §0-3-8은 "버그가 나도 새어나갈 수 없는 구조"를 요구하므로, 가상계좌는 **물리적으로 분리된 새 테이블**이어야 합니다. 컬럼 타입·RLS 정책 같은 **관례만** 베끼고, 표는 새로 만듭니다.

---

## 0단계 — ✅ 완료 (2026-08-19) — 설계 검토 라운드

이번 라운드에서 실제로 결론이 난 것들입니다. **다시 논의하지 말고 아래를 전제로 진행하세요.**

1. **✅ 큰 그림 확인** — 오너가 draw.io로 그린 다이어그램을 기준으로, 모듈을 "덤벼라 나 자신"(개인 모의투자)과 "내 밑으로 눈 깔어"(공개 순위표) 두 갈래로 분리하는 데 합의했습니다. 두 갈래는 **의존 방향이 한쪽뿐**입니다(공개표가 개인 데이터를 읽지, 그 반대는 없음).

2. **✅ 배당 자동 반영은 v1 범위 밖으로 확정.** 오푸스 검토 결과, **양쪽 시장 어느 수집기에도 배당 "지급 캘린더"(사건별 날짜 + 정확한 주당 금액)가 없습니다.**
   - `collector_kospi200.py`: `dps`·`div_yield`가 있고 3상태 `dps_status`(`collected` / `no_dividend_confirmed` / `not_collected`)로 "확인된 무배당"과 "수집 실패"를 제대로 구분하고 있습니다(§0-1 모범 사례). **그러나 `dps`는 후행 연간 스냅샷 한 개**이고, 그중 일부는 재무제표 실측이 아니라 `price × div_yield / 100`으로 **역산한 추정치**(코드 주석에 "실측 아님"으로 명시), 우선주는 모주(母株) 값을 물려받은 것(역시 "실측 아님, 과소평가 가능"으로 명시). **배당락일도, 지급일도, 사건별 이력도 없습니다.**
   - `collector_us_stocks.py`: `dps`·`div_yield`·`payout_ratio`·`shareholder_yield`·`ex_dividend_date`와 3상태 `dividend_status`, `detect_dividend_statement()`가 있습니다. 그러나 `ex_dividend_date`는 **수집 시점에 화면에 떠 있던 값 하나**일 뿐 시계열이 아니고, **지급일 필드는 아예 없습니다**(배당락일 ≠ 지급일, 보통 몇 주 차이).
   - 지급 캘린더를 만들려면 DART/KIND 공시, KRX, 또는 네이버 배당 페이지 같은 **완전히 새로운 데이터 소스와 파이프라인**이 필요합니다. 게다가 한국 기업 상당수는 **배당락 시점(지급 2~3개월 전)에 주당 금액이 아직 확정되지 않습니다** — 즉 "가져와서 저장"이 아니라 **"선언됨, 금액 미정"이라는 상태를 모델링**해야 하는 별개의 프로젝트입니다.
   - **오너 결정: v1에서는 배당을 반영하지 않습니다. 그리고 화면에 그 사실을 그대로 씁니다 — "배당금은 반영되지 않습니다".** 조용히 빼는 것과 눈에 보이게 빼는 것은 다릅니다. 정직하게 드러낸 누락은 §0-1을 만족하지만, 추정치로 대체한 배당은 만족하지 않습니다.

3. **✅ 즉시 체결은 불가능함을 확인하고, 그것을 기능으로 채택.** 이 프로젝트에는 실시간·장중 시세가 **어디에도 없고, 앞으로도 두지 않습니다**(§0-3-1 — 현재의 한계가 아니라 원칙입니다). 사용자가 낮에 주문을 저장하는 시점에 "오늘 종가"는 아직 세상에 존재하지 않습니다. 따라서 **주문 저장 = 체결이 아니라 예약**이고, 그날 밤 배치가 확정 종가로 체결합니다. 아래 2단계에서 상세히 다룹니다.

4. **✅ 공개는 "권한 완화"가 아니라 "별도 발행표"로 한다는 방향 확정.** 원본 테이블의 RLS를 손대서 옵트인 행만 남에게 보이게 하는 방식은 **채택하지 않습니다.** 대신 배치가 동의 범위 안의 필드만 뽑아 **물리적으로 분리된 발행 전용 표**에 쓰고, 순위표 화면은 그 표만 읽습니다.

5. **✅ 비활동 뱃지 기능은 채택 안 함 — 보류가 아니라 폐기.** 매수 전용 + 가변 길이 창 설계에서는 "비활동"의 정의 자체가 성립하지 않습니다. 6개월 계좌는 **몇 달간 조용한 게 정상**이고, 현금을 다 쓴 계좌는 다음 입금 전까지 **물리적으로 거래가 불가능**합니다. 다시 꺼내지 마세요.

6. **✅ 데이터 보존 정책은 지금 만들지 않음.** 일별 스냅샷 이력은 당분간 **무기한 보관, 자동 정리 없음**. 사용자 수가 실제로 문제가 될 만큼 늘었을 때 설계합니다(미리 만들지 않음 — §0-3-10 YAGNI).

7. **참고 — §0-3-11(외부 AI 교체 가능성)은 이 모듈의 중심 원칙이 아닙니다.** 현재 설계에 외부 AI 모델이나 외부 API 호출이 **하나도 없습니다**(가격은 기존 수집기 산출물 재사용, 순위 계산은 우리 DB 안에서 끝남). 나중에 "AI 대결 코멘트" 같은 걸 붙이게 되면 그때 §0-3-11을 적용하세요. 억지로 인용하지 않았습니다.

---

## 1단계 — ✅ 오너 승인 완료 — 스키마 설계 확정 (2026-08-19)

**✅ 오너 승인 완료.** 실제로 작성된 `sql/duel_schema.sql`은 아래 초안과 세부사항(특히 RLS 세분화)이 다를 수 있습니다 — 실행하며 발견한 안전상의 이유로 코딩 에이전트가 손을 본 부분은 이 문서의 2단계 섹션과 "다음에 이 문서를 다시 열 때"에 각각의 이유와 함께 적어뒀습니다. 아래 초안은 **설계 의도를 보여주는 참고 자료**로 남겨둡니다.

파일 위치: `sql/duel_schema.sql` (기존 두 스크립트처럼 **오너가 Supabase SQL Editor에 1회 붙여넣어 실행**할 완결 스크립트. 여러 번 실행해도 안전하게, `DROP TABLE` 없이).

### 1-1. 가상계좌 (`duel_accounts`)

```sql
create table if not exists public.duel_accounts (
    id            uuid primary key default gen_random_uuid(),
    user_id       uuid not null references auth.users (id) on delete cascade,
    window_type   text not null check (window_type in ('M1', 'M3', 'M6')),  -- 1/3/6개월 주기
    seed_amount   numeric(20, 6) not null default 10000000,                  -- 1천만원 고정
    currency      text not null default 'KRW' check (currency = 'KRW'),      -- v1은 원화만
    anchor_date   date not null,      -- 매수 창 주기의 기준일 (계좌 생성일)
    status        text not null default 'active' check (status in ('active', 'closed')),
    created_at    timestamptz not null default now(),
    constraint duel_accounts_user_window_unique unique (user_id, window_type)
);
```

- `unique (user_id, window_type)` — 사용자당 창 유형별 정확히 1개, 즉 **최대 3개**. `holdings`의 `unique (user_id, market, ticker)`와 같은 발상입니다.
- `currency`를 별도 제약 컬럼으로 두는 이유는 `holdings`와 동일합니다 — **원화와 달러가 섞이는 경로를 애초에 만들지 않기 위해서**(§0-1). v1에 USD가 없더라도 컬럼은 둡니다.
- ⚠️ **v1에서 USD/미국주식 가상거래는 시도하지 마세요.** 이 앱 어디에도 **환율 시계열이 없습니다**. 환율 없이 달러 계좌를 원화 순위표에 올리려면 어딘가에서 환율을 지어내야 하고, 그건 §0-1 정면 위반입니다.

### 1-2. 가상 보유 포지션 (`duel_positions`)

```sql
create table if not exists public.duel_positions (
    id             uuid primary key default gen_random_uuid(),
    account_id     uuid not null references public.duel_accounts (id) on delete cascade,
    ticker         text not null,
    stock_name     text not null,
    quantity       numeric(20, 6) not null check (quantity >= 0),
    avg_cost       numeric(20, 6) not null check (avg_cost >= 0),  -- 가중평균 매입단가
    status         text not null default 'active'
                   check (status in ('active', 'delisted')),
    delisted_date  date,
    updated_at     timestamptz not null default now(),
    constraint duel_positions_account_ticker_unique unique (account_id, ticker)
);
```

- **`numeric(20, 6)` 고정** — `holdings.avg_purchase_price`가 float이 아니라 `numeric(20,6)`인 이유(가중평균 재계산 시 반올림 오차 누적 방지)를 그대로 물려받습니다(§0-3-10). 여기서 float를 쓰면 매수를 수십 번 반복한 계좌의 평단가가 조용히 틀어집니다.
- 주식 수량은 정수지만 컬럼은 `numeric`으로 둡니다(무상증자·액면분할 대응 여지 — 3단계 참고). **다만 v1의 매수 체결은 항상 정수 주식만 만듭니다.**
- **매도 금지의 DB 레벨 강제**: `quantity`가 정상 주문 경로로 **감소할 수 없어야** 합니다. 트리거로 `new.quantity < old.quantity`일 때 예외를 던지되, 상장폐지 상각·기업행위 조정 같은 **관리 경로만** 예외로 두는 방식을 권장합니다(예: 세션 변수나 전용 함수 경유). 화면 로직에 실수가 있어도 DB가 막아주는 이중 방어는 §0-3-9가 이미 요구하는 방식입니다. (⚠️ 2026-08-21 정정 — §2-3-1: 이 트리거는 그대로 두되, 창당 1회 리밸런싱 매도 정산을 위한 **두 번째 예외 경로**가 추가됐습니다. 상장폐지 상각과는 별개 세션 변수입니다.)

### 1-3. 예약 주문 (`duel_orders`) — ⚠️ 2026-08-19 2차 라운드에서 수정됨 (금액 기준 → 수량 기준)

```sql
create table if not exists public.duel_orders (
    id                  uuid primary key default gen_random_uuid(),
    account_id          uuid not null references public.duel_accounts (id) on delete cascade,
    ticker              text not null,
    stock_name          text not null,
    requested_quantity  integer not null check (requested_quantity > 0),  -- 사용자가 입력한 "몇 주"
    side                text not null default 'buy' check (side = 'buy'),  -- 매수 전용, 확장 금지
    status              text not null
                        check (status in
                            ('pending', 'filled', 'partially_filled', 'cancelled', 'expired')),
    saved_at            timestamptz not null default now(),   -- 최초 저장 시각 (KST 기준 기록)
    last_edited_at      timestamptz,                          -- 수정 이력 (마지막 수정 시각만, 단순 이력)
    target_date         date,        -- 이 주문이 귀속되는 거래일 (배치가 확정)
    filled_date         date,
    filled_price        numeric(20, 6),
    filled_quantity     integer,     -- 실제 체결 수량. requested_quantity보다 작을 수 있음(부분체결)
    filled_amount       numeric(20, 6),
    fail_reason         text         -- partially_filled/expired/cancelled 사유를 사람이 읽을 문장으로
);
```

- **2026-08-19 오너 확정 — 주문은 "얼마어치"가 아니라 "몇 주"로 입력합니다.** 최초 설계안(`order_amount_krw`, 원화 금액 입력)은 폐기합니다. 사용자는 종목과 `requested_quantity`(주식 수)만 입력합니다.
- ~~`side`에 `check (side = 'buy')`를 거는 건 장식이 아닙니다. "나중에 매도 기능 넣을 때 이 컬럼만 바꾸면 되지"라는 생각을 물리적으로 막기 위한 표시입니다. 이 모듈에 매도는 없습니다.~~ → **⚠️ 2026-08-21 정정(§2-3-1)**: 그 "나중"이 실제로 왔습니다. `side`는 이제 `'buy'`/`'sell'`을 허용하고, `rebalance_window_index` 컬럼과 부분 유니크 인덱스가 "창당 1회"를 대신 강제합니다.
- `fail_reason`은 반드시 채웁니다. 주문이 조용히 사라지는 경로가 하나라도 있으면 §0-1 위반입니다.
- **부분체결(`partially_filled`) — 2026-08-19 오너 확정.** 저장 시점엔 종가를 모르므로, 체결 시점에 `requested_quantity × 그날 종가`가 남은 예수금을 넘으면 **주문 전체를 취소하지 않고, 예수금으로 살 수 있는 최대 정수 수량만큼만 자동으로 체결**합니다(`floor(가용현금 / 종가)`). `filled_quantity < requested_quantity`가 되며, `fail_reason`에 "요청 10주 중 7주만 예수금 부족으로 체결"처럼 사람이 읽을 문장을 반드시 남깁니다. 화면에도 요청 수량과 실제 체결 수량이 다르면 **눈에 띄게** 알려야 합니다(§0-1 — 조용히 다른 결과를 주지 않기). `filled_quantity`가 0이면(1주 가격에도 못 미침) `status='expired'`.
- **주문 수정·취소 — 2026-08-19 오너 확정, 둘 다 허용.** `pending` 상태인 주문은 **그날 밤 배치가 처리를 시작하기 전까지** 사용자가 자유롭게 `requested_quantity`를 바꾸거나 아예 취소할 수 있습니다. 배치가 이미 그 주문을 집어간 뒤에는(체결 처리 진입 이후) 수정·취소를 막습니다 — 그렇지 않으면 배치와 사용자 조작이 동시에 같은 행을 건드리는 경합이 생깁니다. `last_edited_at`은 감사용 이력이며, 여러 번 고쳐도 마지막 시각만 남기는 단순 필드입니다(전체 수정 이력이 꼭 필요하면 나중에 별도 이력 테이블로 분리 — 지금은 YAGNI, §0-3-10).

### 1-4. 현금 원장 (`duel_cash_ledger`) — **append-only**

```sql
create table if not exists public.duel_cash_ledger (
    id          bigserial primary key,
    account_id  uuid not null references public.duel_accounts (id) on delete cascade,
    event_type  text not null
                check (event_type in ('seed', 'monthly_deposit', 'buy', 'reversal')),
    amount      numeric(20, 6) not null,   -- 입금은 +, 매수는 -
    event_date  date not null,             -- KST 기준 날짜
    order_id    uuid references public.duel_orders (id),  -- buy일 때만
    memo        text,
    created_at  timestamptz not null default now()
);
```

- **잔고 컬럼 하나를 두고 UPDATE하는 방식을 쓰지 마세요.** 현금 잔고는 항상 `sum(amount)`로 계산합니다.
  - 이유는 `avg_purchase_price`를 float 대신 `numeric(20,6)`으로 잡은 것과 **같은 규율**입니다: 숫자·상태가 **되돌릴 수 없게 망가지는 것**을 구조적으로 막는 것. 잔고 컬럼 방식에서는 체결 로직에 버그가 한 번 나면 잔고가 조용히 어긋나고, 나중에 발견해도 "원래 얼마였어야 하는지"를 복원할 방법이 없습니다. 원장 방식이면 **반대 부호의 `reversal` 행을 한 줄 덧붙여 정정**할 수 있고, 무슨 일이 있었는지도 남습니다.
  - 성능 걱정은 하지 마세요. 계좌당 연간 수십~수백 행 수준입니다(`REPORT_WORK_ORDER.md`의 스냅샷 용량 판단과 같은 계산).
- ⚠️ **`rollover`(이월)를 이벤트로 만들지 마세요.** 초안 논의에서 `event_type`에 `rollover`가 거론됐지만, **이월은 돈의 이동이 아닙니다** — 안 쓴 현금이 계좌에 그냥 남아 있는 상태일 뿐입니다. 이걸 금액 있는 원장 행으로 남기면 **잔고가 이중 계상되고 TWR의 현금흐름 판정까지 오염됩니다.** 화면에서 "이월된 금액"을 보여주고 싶다면 그건 원장 행이 아니라 **계산해서 표시하는 값**입니다. 위 스키마에서 `rollover`를 뺀 건 의도적입니다.
- ~~`event_type`에 매도가 없다는 점도 확인하세요.~~ → **⚠️ 2026-08-21 정정(§2-3-1)**: 이제 있습니다 — `'sell'`은 입금과 같은 방향(+)이고, 주문 연결이 매수와 동일하게 필수입니다.

### 1-5. 일별 스냅샷 (`duel_daily_snapshots` / `duel_holding_snapshots`)

`sql/report_schema.sql`의 두 표를 그대로 본뜨되, **TWR을 위해 현금흐름 정보를 반드시 함께 기록**합니다.

```sql
create table if not exists public.duel_daily_snapshots (
    id                uuid primary key default gen_random_uuid(),
    account_id        uuid not null references public.duel_accounts (id) on delete cascade,
    snapshot_date     date not null,
    position_value    numeric(20, 6) not null,   -- 그날 종가 기준 보유 평가금액
    cash_balance      numeric(20, 6) not null,   -- 그날 종료 시점 현금 (원장 합계)
    total_value       numeric(20, 6) not null,   -- position_value + cash_balance
    total_cost        numeric(20, 6) not null,   -- 매입원가 합계
    cash_flow_amount  numeric(20, 6) not null default 0,  -- ★ 그날 발생한 외부 입금액
    cash_flow_kind    text,                                -- 'seed' / 'monthly_deposit' / null
    priced_count      integer not null,
    unpriced_count    integer not null,
    price_as_of_kst   text,
    created_at        timestamptz not null default now(),
    constraint duel_snapshots_account_date_unique unique (account_id, snapshot_date),
    constraint duel_snapshots_priced check (priced_count > 0 or position_value = 0)
);
```

- **`cash_flow_amount` / `cash_flow_kind`가 이 표의 핵심입니다.** 이게 없으면 TWR을 절대 사후에 계산할 수 없습니다. 아래 2-6 참고.
- **"외부 현금흐름"의 정의를 코드 주석에 못 박으세요**: 시드 지급과 매월 10일 입금**만** 외부 현금흐름입니다. **주식 매수는 현금흐름이 아닙니다** — 계좌 안에서 현금이 주식으로 바뀐 것뿐이라 총자산이 변하지 않습니다. 여기를 헷갈리면 수익률이 통째로 틀립니다.
- `priced_count > 0` CHECK는 `report_schema.sql`이 이미 쓰는 방어입니다("그날 가격을 하나도 모르면 행 자체를 안 만든다"). 다만 이 모듈에는 **보유 종목이 0개이고 현금만 있는 계좌**가 정상적으로 존재하므로(신규 계좌), 위처럼 예외를 열어야 합니다.
- 종목별 상세 `duel_holding_snapshots`(계좌 × 종목 × 거래일 = 1행: 수량, 평단가, 종가, 평가금액, 상태)도 `portfolio_holding_snapshots`와 같은 모양으로 함께 만듭니다. 순위표에서 "보유종목·수량·매입금액"을 공개하려면 결국 이 표가 원천이 됩니다.
- RLS: **select 정책 하나만**, `auth.uid()`가 계좌 소유자일 때만. 쓰기는 배치(service_role) 전용. `report_schema.sql`과 동일합니다.

### 1-6. 무작위 닉네임 (`duel_nicknames`)

```sql
create table if not exists public.duel_nicknames (
    account_id  uuid primary key references public.duel_accounts (id) on delete cascade,
    nickname    text not null unique,
    created_at  timestamptz not null default now()
);
```

- **닉네임은 완전 무작위 생성이며, `user_id`나 이메일 등 어떤 식별자에서도 유도하지 않습니다.** 해시든 시드든 마찬가지 — 결정적으로 계산되는 순간 역추적 가능성이 생깁니다(§0-3-8, §0-3-9). 생성은 난수 → `unique` 충돌 시 재시도.
- 이 표는 **비공개**입니다. 공개표에는 닉네임 문자열만 실리고 `account_id`는 절대 실리지 않습니다(아래 1-8).

### 1-7. 공개 동의 상태 (`duel_public_consent`) — **하나의 boolean으로 뭉치지 말 것**

```sql
create table if not exists public.duel_public_consent (
    account_id              uuid primary key references public.duel_accounts (id) on delete cascade,
    -- 2갈래 공개 항목별 개별 동의 (5개, 각각 독립)
    consent_rank            boolean not null default false,  -- 순위
    consent_return          boolean not null default false,  -- 수익률
    consent_holdings        boolean not null default false,  -- 보유종목 (개별 열람 포함)
    consent_quantity        boolean not null default false,  -- 수량
    consent_buy_amount      boolean not null default false,  -- 매입금액
    -- 위 5개를 모두 체크한 뒤 밟는 별도의 최종 확인
    final_confirmed         boolean not null default false,
    final_confirmed_at      timestamptz,
    -- ★ 완전히 독립된 별개 동의: 실제 '내 성적표' 매입총합을 구간 산정에 사용
    consent_real_principal_bracket boolean not null default false,
    -- 철회 이력 (재동의 3개월 차단 판정용)
    revoked_at              timestamptz,
    updated_at              timestamptz not null default now()
);
```

- **왜 boolean 7개인가**: 오너의 명시적 요구입니다. "공개" 토글 하나로 뭉치면 사용자가 **무엇에 동의했는지 시스템이 알 수 없게** 되고, 그 상태에서 발행 배치가 필드를 하나 더 실어버리면 그건 곧 §0-3-8 사고입니다. 동의는 **항목별로 저장돼야 발행 배치가 항목별로 게이팅**할 수 있습니다.
- **`consent_holdings`의 문구는 "보유종목을 공개합니다"가 아니라 "내 보유종목이 순위표에서 다른 사람에게 개별 열람 가능하게 공개됩니다"** 여야 합니다. 개별 열람은 "공개"의 함축이 아니라 **따로 명시해야 하는 사실**입니다(오너 지시).
- **`consent_real_principal_bracket`은 위 5개와 절대 묶지 마세요.** 이건 이 모듈의 데이터가 아니라 **"내 성적표"의 실제 자산 데이터**(매입총합)를 끌어다 쓰는 동의입니다. AI가 이 위험을 지적했고 오너가 **분리하기로 확정**했습니다 — 가상 대결 성적은 자랑하고 싶지만 실제 자산 규모는 어떤 형태로도 알리고 싶지 않은 사용자가 당연히 존재하기 때문입니다.
- **철회 후에도 `revoked_at` 한 줄은 남깁니다.** 3개월 재동의 차단을 판정하려면 이 시각이 필요합니다. 이건 **비공개 동의 테이블에 남는 것**이므로 §0-3-8과 충돌하지 않습니다 — 삭제 대상은 **발행된 공개 기록**이지 동의 상태 관리 기록이 아닙니다. 이 구분을 코드 주석에 적어두세요.

### 1-8. 발행 전용 공개표 (`duel_public_leaderboard` 외)

```sql
create table if not exists public.duel_public_leaderboard (
    id              bigserial primary key,
    published_date  date not null,
    window_type     text not null check (window_type in ('M1', 'M3', 'M6')),
    bracket_key     text not null,     -- 원금 구간 식별자 (또는 '구간 미적용')
    rank            integer not null,
    nickname        text not null,     -- duel_nicknames에서 온 무작위 문자열
    twr_pct         numeric(20, 6),    -- consent_return이 false면 null
    created_at      timestamptz not null default now(),
    constraint duel_public_rank_unique unique (published_date, window_type, bracket_key, rank)
);
```

- **이 표에는 `user_id`도 `account_id`도 넣지 마세요.** 넣는 순간 "이 표만 읽으면 안전하다"는 보장이 사라집니다. 철회 시 삭제는 **비공개 `duel_nicknames`에서 닉네임을 찾아 그 닉네임의 공개 행을 지우는** 방식으로 합니다. 즉 연결고리는 배치(service_role) 쪽에만 존재합니다.
- 보유종목·수량·매입금액은 별도 발행표(`duel_public_holdings`: `published_date`, `nickname`, `ticker`, `stock_name`, 그리고 **동의한 항목만 채워지는** `quantity` / `buy_amount`)로 분리합니다. 동의하지 않은 컬럼은 **null**이며, 화면은 null을 "비공개"로 렌더링합니다(§0-1 — 0이나 빈 문자열로 채우지 마세요. "0주 보유"와 "수량 비공개"는 다른 말입니다).
- RLS: 이 표만 **로그인 사용자 전체에게 select 허용**. insert/update/delete 정책은 **아무에게도 주지 않습니다**(배치의 service_role만 RLS를 우회해 씁니다).

### 1-9. 🔴 이 단계에서 오너가 답해야 하는 것

- 위 8개 표의 **개수·분리 방식**이 이대로 좋은지 (특히 현금 원장을 append-only로 가는 것, 공개표에 `account_id`를 아예 안 넣는 것).
- 표/컬럼 **이름**(위는 전부 예시입니다).
- 아래 "🔴 미결정 항목 모아보기"에 있는 것들 중 스키마에 영향을 주는 것 — 특히 **매수 창의 길이**(2-3)와 **주문 수정/취소 허용 여부**(2-4).

---

## 2단계 — Branch 1 "덤벼라 나 자신" 핵심 구현 (✅ 사실상 완료 — SQL/규칙/DB계층·야간배치·화면까지 전부 작성·검증 완료, `main.py` 배선 한 줄만 남음)

신규 파일 계획 (기존 파일은 **한 줄도 수정하지 않는 것**을 목표로. `report` 모듈이 신규 파일 6개로 기존 파일 무수정을 달성한 전례가 있습니다):

| 파일 | 역할 |
|---|---|
| `sql/duel_schema.sql` | 1단계의 표 전부 + RLS. 오너가 Supabase SQL Editor에서 1회 실행 |
| `utils/duel_rules.py` | **순수 함수만.** 체결 수량·잔여 현금 계산, 가용 현금 계산, 매수 창 개폐 판정, TWR 계산, 동의 게이팅 판정. Supabase·네트워크·NiceGUI 미의존 → 4단계에서 통째로 오프라인 테스트 |
| `utils/duel_db.py` | Supabase 접근 계층. **`utils/report_db.py`에 이어 service_role을 쓰는 두 번째 파일** — ⚠️ `PROJECT_STATUS.md`의 "저장소에서 유일하게 service_role을 쓰는 파일" 서술을 **반드시 갱신**하세요(§0-1: 문서도 사실이어야 합니다) |
| `web/pages/duel_page.py` | NiceGUI 화면. 기본 숨김 `DUEL_ENABLED`(§0-3-6) |
| `.github/workflows/duel_daily.yml` | 야간 배치 (아래 2-5) |
| `tests/test_duel.py` | 오프라인 검증 (4단계) |

### 2-1. 계좌 개설과 시드

1. **모듈에 참여(옵트인)하면 3개 계좌가 한 번에 생깁니다** — `M1`/`M3`/`M6`. 사용자가 개수를 고르거나 하나만 만들 수는 없습니다(v1).
2. 각 계좌에 `duel_cash_ledger`에 `event_type='seed'`, `amount=10000000`, `event_date=개설일` 행을 **계좌당 1행씩** 넣습니다. 즉 참여 시점에 사용자는 **총 3천만원의 가상 현금**을 갖게 됩니다.
3. `seed_amount`는 v1에서 **사용자가 바꿀 수 없습니다.** 화면에 입력칸을 만들지 마세요.

### 2-2. 매월 10일 정기 입금

1. **매월 10일, 3개 계좌 전부에 각각 80만원**을 입금합니다. 창 길이에 비례해 깎지 않습니다 — `M6` 계좌도 `M1` 계좌와 똑같이 매월 80만원입니다(오너 확정).
2. 즉 사용자 1명당 **월 240만원**이 유입됩니다. 화면 안내 문구에 이 총액을 적어두는 편이 오해가 적습니다.
3. **✅ 2026-08-19 3차 확정 — 입금 시각은 매월 10일 00시 00분 01초.** 그날 하루가 시작하자마자 입금됩니다(더 이상 "10일 밤이냐 자정 직후냐"가 미결이 아닙니다). 이러면 같은 날 저녁 주문 접수 시간대(2-4의 18:00:01~22:00:00)에 이 돈을 바로 주문에 쓸 수 있습니다 — "10일에 입금"이라는 표현과 실제 사용 가능 시점이 어긋나지 않습니다.
4. 원장에 `event_type='monthly_deposit'`, `event_date=해당 월 10일` 행을 넣습니다. **10일이 주말·공휴일이어도 그대로 10일자로 넣습니다** — 이건 시장 이벤트가 아니라 현금 이벤트입니다.
5. **집합 연산으로 한 번에 넣으세요** — `insert ... select`로 전체 활성 계좌에 대해 단일 쿼리. 사용자별 루프 금지(§0-3-2, 아래 2-7).
6. **멱등성 필수**: 배치가 두 번 돌아도 같은 달 입금이 두 번 들어가면 안 됩니다. `unique (account_id, event_type, event_date)` 부분 유니크 인덱스로 DB가 막게 하세요.

### 2-3. 매수 창(주기) — ✅ 2026-08-19 확정 (2차 라운드)

**확정된 것**: `M1`/`M3`/`M6`은 "얼마나 자주 새 매수 기회 창이 열리는가"를 뜻합니다. 창이 열려 있는 동안에는 **가용 현금이 있는 한 여러 번 나눠서 계속 매수**할 수 있습니다. "하루짜리 매매 규칙"이 아닙니다. 안 쓴 현금은 **소멸하지 않고 다음 기간으로 이월**되며, **강제 투자되지 않습니다**.

**✅ 창의 길이 확정 — 오너 결정: 창 길이 = 사실상 무제한.** "예수금이 있으면 언제든 매수 가능, 예수금이 0이 되면 다음 입금 전까지 매수 불가"가 이 모듈의 유일한 규칙입니다. 이전 초안에서 별도로 "창이 며칠 열려 있는가"를 묻던 항목은 이걸로 해소됐습니다 — `duel_rules.py`의 `is_buy_window_open(account, on_date)`은 사실상 `"가용 현금 > 0"` 하나로 구현하면 됩니다.

**✅ M1/M3/M6 계좌의 차별화 방식 확정(2026-08-19).** 세 계좌는 매매 **규칙**(시드·입금액·매수 가능 조건)이 완전히 동일합니다. 이건 의도된 설계입니다 — **차별화는 규칙이 아니라 사용자가 각 계좌에서 실제로 어떤 종목을 사는지(투자 선택)에서 나옵니다.** 예를 들어 "1개월 계좌는 이 종목, 6개월 계좌는 저 종목에 걸어본다"처럼, 오너 본인이 계좌별로 다른 투자 아이디어를 시험해보는 용도입니다. 그래서 **규칙 레벨의 인위적 차별화(주문 빈도 제한 등)는 추가하지 않습니다.** 이름(1/3/6개월)은 입금·정산 리듬을 가리키는 라벨일 뿐이고, 세 계좌를 나란히 비교하는 화면(2-8)이 이 실험을 보여주는 핵심 UI가 됩니다.

> 📝 **네이밍 주의 — 오너에게 확인 요청할 것.** 매도가 없으므로 이 구조는 금융 용어의 "리밸런싱"이 아니라 **적립식 매수(periodic accumulation)** 입니다. 내부/작업용 모듈명은 지시받은 대로 유지하되, **사용자에게 보이는 화면 문구에 "리밸런싱"이라는 단어를 쓰면 기대가 어긋납니다**(사용자는 비중 조정=매도 포함을 떠올립니다). UI 문구는 7-2의 문구 검토 항목에 포함시켰습니다.
>
> **⚠️ 2026-08-21 철회 — 아래 2-3-1 정정으로 이 경고의 전제 자체가 사라졌습니다.** 이제 실제로 매도(리밸런싱)가 있으므로, 화면에 "리밸런싱"이라는 단어를 쓰는 것은 기대와 어긋나지 않고 오히려 정확한 표현입니다. 이 문단은 "왜 예전엔 이 단어를 피했는지"의 기록으로만 남깁니다.

### 2-3-1. 🔴 2026-08-21 오너 확정 — 정정: 창당 1회 리밸런싱 매도를 추가합니다

**이 절은 바로 위 2-3의 "규칙 레벨의 인위적 차별화(주문 빈도 제한 등)는 추가하지 않습니다"와 아래 "참고 — 범위 밖"의 "매도 기능 — 이 모듈에 매도는 영원히 없습니다"를 정정합니다.**

이번 리뷰 중 오너가 확인해준 원래 의도가 위 두 결정과 어긋난다는 것이 드러났습니다 — 이전 라운드의 **대화 착오**였습니다. 실제 원래 설계는 M1/M3/M6 계좌가 각각 자기 주기로 **딱 1회씩** 리밸런싱 매도를 할 수 있어야 한다는 것이었고, 그래야 "내 성적표"(실제 계좌)와 비교하는 시스템이 성립합니다(매도 없이 한 번 고른 종목에 영원히 묶이면 실제 투자 행태와 너무 달라져 비교 자체가 무의미해짐). 오너 확정 발언: *"한도가 정해져있는 매도는 해야하고 그 리밸런싱은 해야하는거고 안그러면 내 계좌하고 비교하는 시스템이 성립되지 않아."*

**✅ 확정된 스펙**

1. **창 길이 = M1 30일 / M3 90일 / M6 180일.** 기준일은 계좌 개설일(`anchor_date`)이 아니라 **그 계좌에 처음 주식이 들어온 날**(신규 컬럼 `first_holding_date`) — 아직 아무것도 안 산 계좌는 창이 시작되지 않아, 첫 매수 전에 기회가 소멸하는 사고를 막습니다.
2. **창을 그냥 흘려보내도 기회는 누적되지 않습니다** — 매 창마다 정확히 1회.
3. **매도 주문 1건 = 종목 1개, 수량은 1주~전량 자유.** 여러 종목을 한 번에 처리하는 바스켓이 아닙니다.
4. **체결 타이밍은 매수와 완전히 동일** — 접수 시간대(D일)에 저장 → D+1 확정 종가로 체결. **같은 밤에 매도 먼저 처리하고, 그 대금이 그날 밤 매수 재원으로 즉시 사용됩니다.**
5. **서버(DB) 레벨로 강제합니다** — 화면 로직 실수와 무관하게 `duel_orders`의 부분 유니크 인덱스(`account_id, rebalance_window_index`, `side='sell' and status <> 'cancelled'`)가 창당 1건만 허용합니다.
6. **접수 시간대 안에서 스스로 취소하면 그 창의 자리가 다시 열립니다** — 매수 주문의 "배치 처리 전까지 자유롭게 수정·취소"(2-4-7)와 같은 원칙입니다. **가격을 못 구해 시스템이 취소한 매도도 마찬가지로 자리를 되살립니다** — 크롤링 실패는 사용자 잘못이 아니므로, 그것 때문에 하나뿐인 기회를 영구히 뺏지 않기로 구현 중 최종 결정했습니다.
7. 화면에 이번 창의 사용 여부·남은 일수를 표시합니다.

구현 상세(스키마 변경, `utils/duel_rules.py`의 `resolve_rebalance_window()`/`calculate_sell_fill()`/`apply_sell_fill_to_position()`, 매도 정산 전용 RPC `duel_settle_sell_positions()`/`_usd`, 화면의 매도 주문 폼·창 뱃지·주문내역 매수/매도 구분)는 2026-08-21 구현 라운드에서 완료·검증되어 반영됐습니다. TWR(2-6) 계산은 **변경이 필요 없었습니다** — 매도는 계좌 안에서 주식↔현금 전환일 뿐 총자산을 바꾸지 않으므로 외부 현금흐름(`cash_flow_amount`)에 잡히지 않습니다.

**이 정정으로 아래 두 곳도 함께 정정됩니다** — 각 자리에 정정 표시를 남겨 두었습니다.
- 위 2-3-1 이전 문단의 "리밸런싱" 단어 회피 경고 (→ 철회)
- 아래 "참고 — 이번엔 명시적으로 범위 밖"의 "매도 기능" 항목 (→ 정정 표시)

### 2-4. 주문 저장 → 익일 체결 (이 모듈의 심장) — ⚠️ 2026-08-19 3차 라운드에서 시간 모델 전면 확정

**설계 원칙: 주문은 하루 중 정해진 시간대에만 접수되고, 체결은 그 다음 거래일의 확정 종가로 이루어집니다.**

#### 왜 "그날 종가"가 아니라 "다음날 종가"인가 — 이번 라운드에서 가장 중요한 확정 사항

이전 초안은 "주문 저장 당일 밤, 그날 종가로 체결"이었습니다. 그런데 오너가 정한 **주문 접수 시간대(18:00:01~22:00:00)는 그날 크롤링이 이미 끝난 뒤**입니다. 즉 이 시간대엔 **그날 종가가 이미 확정되어 사용자도 이미 알고 있는 값**입니다. 이미 아는 가격으로 체결한다면 §0-3-1이 막으려는 "이미 알려진 정보로 매매"가 되어 대결의 공정성이 무너집니다. 그래서 **체결가는 반드시 그날(D) 종가가 아니라 다음 거래일(D+1) 종가**를 씁니다 — 접수 시간대에도, 체결 시점에도 사용자는 항상 아직 모르는 가격을 놓고 주문하게 됩니다.

1. **주문 접수 시간대 — ✅ 확정: D일 18:00:01 ~ 22:00:00.** 코스피 크롤링(`scrape.yml`, 평일 KST 16:05)이 끝나고 데이터가 확실히 안정된 뒤인 18:00:01부터 22:00:00까지만 새 주문·수정·취소를 받습니다. 이 시간대 밖에 접속해도 화면 자체는 볼 수 있지만, 주문 액션은 **다음 접수 시간대(다음 영업일 18:00:01)로 넘어갑니다.** 화면에 "지금은 주문 접수 시간이 아닙니다 — 오늘 18:00부터 22:00까지 접수합니다"처럼 명확히 안내하세요.
2. **주문 입력**: 사용자는 **종목 + 사고 싶은 주식 수(수량)**를 넣습니다. **금액이 아니라 수량입니다**(2026-08-19 확정 — 최초 설계안이던 금액 입력은 폐기).
3. **입력 시점 검증**:
   - 종목이 **코스피 상위 200 유니버스 안**인가 (`data/kospi200_pegy_latest.json` 기준). 밖이면 거절.
   - 지금이 **매수 창이 열린 기간**인가 (2-3, 가용 현금 > 0) **그리고** 지금이 **주문 접수 시간대**인가 (위 1번).
   - ⚠️ **정확한 예수금 초과 여부는 저장 시점엔 검증할 수 없습니다.** 체결가(D+1 종가)를 아직 모르기 때문입니다. 참고용으로 가장 최근 종가로 "대략 이 정도 필요할 것" 안내는 가능하지만 차단 조건은 아닙니다. **최종 판정은 D+1 체결 시점에만** 이루어집니다.
4. **저장**: `status='pending'`, `saved_at`(KST), `target_date`는 D+1(다음 거래일)로 채웁니다. **이 시점에 현금은 차감하지 않습니다.**
5. **크롤링 실패 시 처리 — ✅ 2026-08-19 확정.** 그 주문이 귀속된 거래일에 확정 종가 수집 자체가 실패하면(2-9의 판정 알고리즘 참고), **그 주문은 자동으로 취소·실패 처리됩니다.** 다음 성공일로 "이월"하지 않습니다 — 실패 이유(`fail_reason`)를 명확히 남기고, 화면에서도 그 사실이 눈에 띄게 보여야 합니다. **그리고 이건 사후 통보로 끝내지 않습니다** — 주문을 넣기 **전에**, "크롤링이 실패하면 이 주문은 체결되지 않고 취소됩니다"라는 안내를 **크고 명확하게, 체크 확인 영역에 포함해서** 미리 고지합니다(§0-1 — 실패 가능성 자체를 숨기지 않기).
6. **익일 체결**: D+1의 코스피 확정 종가 수집이 성공한 뒤, 배치가 그 거래일에 귀속된 `pending` 주문을 처리합니다.
   - 체결가 = **D+1의 KRX 정규장 종가**(기존 수집기가 확정한 값을 그대로 사용, §0-3-10 — 두 번째 "오늘 종가" 정의를 만들지 않기).
   - **가용 현금 계산**: 같은 계좌에 여러 `pending` 주문이 있으면 **`saved_at`이 빠른 순서대로** 하나씩 예수금을 배정해 뒤 주문이 앞 주문 몫까지 넘보지 않게 합니다.
   - `requested_quantity × 종가` ≤ 그 시점 가용 현금이면 **전량 체결**(`status='filled'`). 초과하면 **`floor(가용현금 / 종가)`만큼만 부분체결**(`status='partially_filled'`, `fail_reason`에 "요청 10주 중 7주만 예수금 부족으로 체결"). **주문 전체를 취소하지 않습니다.** `filled_quantity`가 0이면 `status='expired'`.
   - 체결금액 = `filled_quantity × 종가`. 잔돈은 현금으로 남습니다.
   - `duel_positions` 가중평균 갱신: `new_avg = (기존수량×기존평단 + filled_quantity×체결가) / (기존수량 + filled_quantity)` (`holdings`와 동일 규칙).
   - 원장에 `event_type='buy'`, `amount = -체결금액`, `order_id` 채워서 추가.
   - **화면 표시(§0-1)**: 부분체결은 "요청 10주 → 7주 체결"처럼 요청값·실제값을 **둘 다** 보여줍니다.
7. **주문 수정·취소 — ✅ 확정, 둘 다 허용.** `pending` 주문은 **접수 시간대(18:00:01~22:00:00) 안에서라면** 자유롭게 `requested_quantity`를 바꾸거나 취소할 수 있습니다. 접수 시간대가 끝나면(22:00:00 이후) 그 주문은 D+1 체결 대상으로 확정되어 더 이상 손댈 수 없습니다.

🔴 **이 절에서 여전히 남은 것**: 크롤링이 여러 날 연속 실패했을 때(2-9에서 "실패"로 판정된 날이 계속될 때) 사용자에게 어떻게 알릴지의 UI 세부(문구 자체는 5번에서 요구사항만 확정, 실제 화면 카피는 7-2에서 마무리).

### 2-5. 야간 배치의 순서와 위치

기존 자동화 뒤에 붙습니다. 코스피 수집(`.github/workflows/scrape.yml`, 평일 KST 16:05)이 끝난 **이후**에만 돌아야 하므로, 새 워크플로우는 별도 cron보다 **`scrape.yml` 완료를 조건으로 실행**(`workflow_run`)하는 편이 안전합니다. 별도 cron으로 간다면 수집 완료 여부를 **반드시 사전 점검**하세요(`scrape_us.yml`의 `--skip-if-not-ready` 패턴과 동일).

배치 내부 순서(이 순서가 중요합니다):

1. **그날 확정 종가가 실제로 확보됐는지 판정** → 안 됐으면 **체결 단계 전체를 건너뜁니다**(부분 체결 금지). 실패를 로그와 상태로 남기고 종료(§0-1: 조용히 넘어가지 않기).
2. **정기 입금** (해당일이 10일이면).
3. **`pending` 주문 체결** → 포지션 갱신 → 원장 기록.
4. **일별 스냅샷 적재** (`duel_daily_snapshots`, `duel_holding_snapshots`) — 이 시점의 현금·포지션·그날 현금흐름을 함께 기록.
5. **(5단계 이후)** 공개표 재생성.

> 🔴 **입금 시각에 대한 미결(이 문서를 쓰며 발견)**: 위 순서대로면 10일자 입금은 **10일 밤**에 반영되므로, 사용자는 그 돈을 **11일부터** 주문에 쓸 수 있습니다. "10일에 입금"이라는 표현과 어긋날 수 있습니다. 대안은 10일 **자정 직후** 입금 전용 스텝을 따로 돌리는 것입니다. 오너 확인 필요.

**§0-3-12 관련** — 이 모듈은 **코스피 수집기 산출물의 신선도에 새롭게 의존**합니다. 지금까지는 수집기가 하루 실패해도 화면 숫자가 하루 낡을 뿐이었지만, 앞으로는 **사용자의 주문이 체결되지 않는** 결과로 이어집니다. 수집 실패가 며칠 연속되면 **화면에 그 사실이 보여야** 합니다(대기 중인 주문 개수와 사유). 방치 금지.

### 2-6. 수익률 — 시간가중수익률(TWR)로 계산

**단순 수익률 `(현재가치 / 시드머니) − 1`을 쓰면 안 됩니다.** 매월 80만원이 계속 들어오기 때문에, 단순 수익률은 **입금액을 투자 성과로 착각**합니다. 시드 1천만원에 6개월간 480만원이 입금되면, 아무것도 안 사고 현금으로만 들고 있어도 "48% 수익"으로 보입니다. 이건 §0-1이 금지하는 "사실과 다른 숫자"입니다.

**TWR 계산 방식**(구현은 `utils/duel_rules.py`의 순수 함수로):

- 일별 구간 수익률: `r_t = (V_t − F_t) / V_{t−1} − 1`
  - `V_t` = t일 종료 시점 총자산(`total_value` = 포지션 평가액 + 현금)
  - `F_t` = t일에 발생한 **외부 현금흐름**(`cash_flow_amount` — 시드·정기입금만. **매수는 여기 포함되지 않습니다**)
- 기간 TWR = `Π(1 + r_t) − 1`
- 시드는 계좌 첫날의 `V_0`으로 잡거나 0일차 유입으로 잡습니다. 둘 중 하나로 정하고 **주석에 명시**하세요.

**그래서 `cash_flow_amount`가 스냅샷에 처음부터 있어야 합니다.** 나중에 붙이면, 그 이전 구간은 "이 날 총자산이 늘어난 게 수익인지 입금인지"를 판별할 방법이 없어 **과거 수익률을 영원히 재계산할 수 없습니다.** 1단계 스키마 승인 전에 이 컬럼이 빠지지 않았는지 반드시 확인하세요.

**✅ 2026-08-19 확정 — "내 성적표"와 "결투" 두 모듈의 수익률 계산은 서로 다르고, 섞으면 안 됩니다.** "내 성적표"(실제 자산, 결투 순위표의 원금 구간 산정에도 쓰이는 그 모듈)는 **지금 방식 그대로**(매입총합 대비 현재가 기준, 현금·예수금 개념 없음) 둡니다 — 이 모듈에서 손대는 게 아닙니다. 반면 **"덤벼라 나 자신"(결투 계좌 1/2/3)은 시드머니와 매수 기록을 저희가 직접 추적**하고 있으므로, **예수금까지 포함한 누적 수익률(TWR)** 로 계산합니다. 두 수익률의 계산 방식이 다르다는 걸 화면 문구에도 명확히 구분해서 적으세요 — 같은 "수익률"이라는 단어를 쓰면 사용자가 두 값을 비교 가능한 걸로 착각할 수 있습니다.

**✅ 2026-08-19 4차 확정 — 표시는 "누적 TWR"만. 연환산은 v1에 없습니다.** 계좌 개설일부터 오늘까지의 전체 구간을 하나로 묶은 누적 수익률(`Π(1+r_t) − 1`)을 퍼센트로 보여줍니다. 오너가 "일단 기본 모델을 만들어보고, 보면서 고치자"고 하셔서, 아래는 AI가 잡은 **v1 기본 계산 규약**입니다 — 실제로 화면에 띄워보고 이상하면 언제든 조정 가능한 초안입니다.

- **계좌 첫날 처리**: 계좌 개설일을 0일차로 두고, 그날의 `total_value`는 시드머니(1천만원)와 같습니다(아직 아무 매수도 없으므로). TWR 누적 곱은 **1일차(개설 다음 날)부터** 시작합니다 — 0일차 자체는 "수익률이 발생하기 전 시작점"이라 구간 수익률 계산에 넣지 않습니다.
- **상장폐지 상각일**: 3-1에서 이미 정한 대로, 상장폐지 상각은 **현금흐름이 아니라 그날의 평가손실**입니다. 즉 `cash_flow_amount`에 넣지 않고 `position_value`에 반영되게만 하면, 위 TWR 공식이 **별도 특수 처리 없이 자동으로 그날 손실을 반영**합니다. 계산식을 새로 만들 필요가 없습니다.
- **표시 반올림**: 소수점 둘째 자리(%) 권장 — `holdings`·`report` 화면들이 이미 쓰는 표시 규약을 그대로 따르면 됩니다(§0-3-10).
- **연환산·기간별(월간/분기별) 수익률은 v1 범위 밖**입니다. 필요해지면 나중에 별도로 추가하되, 이미 `duel_daily_snapshots`에 일별 `cash_flow_amount`가 쌓이고 있으므로 **과거 데이터를 다시 모을 필요 없이 계산만 추가**하면 됩니다 — 그래서 이 컬럼을 처음부터 넣어둔 겁니다.

### 2-7. ⚠️ 배치는 반드시 집합 연산 (§0-3-2 — 우리 자신의 인프라에도 적용)

§0-3-2는 2026-08-19 오너 재지시로 범위가 넓어져서 **"우리가 결제하는 Supabase·Render를 포함해 네트워크로 접속하는 모든 대상"** 에 적용됩니다. 이 모듈의 배치는 그 원칙이 가장 깨지기 쉬운 곳입니다.

**금지 — 이런 모양이 나오면 리뷰에서 반려하세요:**

```python
for user in users:                       # ❌ 사용자 수 × 쿼리 수
    orders = supabase.table("duel_orders").select("*").eq("user_id", user.id).execute()
    for o in orders.data:
        supabase.table("duel_positions").update(...).execute()
```

**요구 — 전체 사용자를 한 번에 처리하는 집합 연산:**

- 체결: 그날 종가 표를 조인해 **`update ... from` 한 방**으로 `duel_orders`의 pending 행 전체를 갱신.
- 포지션 갱신: `insert ... on conflict (account_id, ticker) do update` 로 일괄 upsert.
- 원장: `insert ... select` 로 일괄 삽입.
- 스냅샷: `insert ... select ... group by account_id` 한 방.
- 순위(5단계): `rank() over (partition by window_type, bracket_key order by twr_pct desc)` **윈도우 함수**로 배치에서 미리 계산해 발행표에 저장. **화면 로드 시 순위를 계산하는 코드는 만들지 마세요** — 방문자 수만큼 전체 스캔이 돌게 됩니다.

사용자가 10명일 때는 루프도 돌아갑니다. 그래서 위험합니다 — **문제가 드러나는 시점이 사용자가 늘어난 뒤**이고, 그때는 이미 실사용 중입니다. AI 보조 1인 개발에서 가장 현실적인 실패 모드라 명시적 구현 요구사항으로 못 박습니다.

### 2-8. 화면 (`web/pages/duel_page.py`)

- 계좌 3개 나란히 비교(총자산·현금·평가액·TWR·보유종목). 스냅샷 히스토리로 추이 표시.
- **현금을 반드시 포함한 수익률**을 보여줍니다. 포지션 평가액만 보여주면 "현금으로 들고 있는 계좌"의 성적이 사실과 달라집니다.
- 상시 노출 고지 문구 3종(§0-1 — 숨기지 않고 화면에 씁니다):
  - **"배당금은 반영되지 않습니다."**
  - **"주문은 저장 즉시 체결되지 않습니다. 저장한 주문은 예약일 뿐이고, 다음 거래일(D+1)의 장이 끝난 뒤 확정된 종가로 그날 밤 배치가 체결합니다."** (✅ 2026-08-20 정정 — 이 문구의 예전 초안 "그날 장 마감 후 확정된 종가로 그날 밤에 체결"은 2-4에서 나중에 확정된 D+1 체결 원칙과 어긋났습니다. 화면 작업 중 발견돼 여기서 바로잡았습니다 — §0-1, 문서 안에서도 나중에 확정된 내용이 우선합니다.)
  - ~~"이 모듈은 매수만 가능합니다. 매도는 지원하지 않습니다."~~ → **⚠️ 2026-08-21 정정(§2-3-1)**: "매수는 언제든, 매도는 창마다(30/90/180일) 딱 1회"로 교체됐습니다(정확한 문구는 `web/pages/duel_page.py`의 `NOTICE_BUY_ONLY`).
- 기본 숨김 `DUEL_ENABLED`(기본 false) + `web/layout.py`의 `_MENU` 배선은 7단계 승인 후에.
- `tests/test_web_session_isolation.py`에 이 화면을 **반드시 추가**합니다(§0-3-8: 사람 눈 확인에 의존하지 않기).
- **✅ 2026-08-19 6차 확정 — 공개 순서는 "전체 숨김 → 관리자 전용 공개 → 전체 공개"의 3단계입니다.** 기존에 있는 "관리자 전용" 화면 패턴(`web/layout.py`의 `_MENU_GROUPS`, 예: 매크로 방공망 페이지가 관리자 계정에게만 메뉴에 보이는 방식)을 **그대로 재사용**합니다(§0-3-10). 즉 `DUEL_ENABLED`를 켠 뒤에도 곧바로 전체 사용자에게 보이는 게 아니라, 먼저 **관리자(오너) 계정에게만 메뉴·화면이 보이는 상태**로 얼마간 두고 오너가 직접 써보며 확인한 뒤, 그다음에 `_MENU`에서 관리자 전용 표시를 떼어 일반 사용자에게도 공개합니다. 깃허브 푸시 자체는 이 순서와 무관하게 평소대로 계속 진행합니다 — 코드가 저장소에 있는 것과 사이트에서 누구 눈에 보이는지는 별개입니다.

### 2-9. ✅ 크롤링 성공/실패 판정 알고리즘 — 2026-08-19 3차 확정

이 모듈이 생기기 전까지는 "오늘 수집이 잘 됐는가"가 그렇게 중요하지 않았습니다(화면 숫자가 하루 낡을 뿐이었으니까요). 이제는 **이 판정 결과가 주문 체결 여부를 직접 좌우**하므로, 별도의 가벼운 점검 로직을 새로 둡니다. **일별 이력 누적은 필요 없고, 그날 값이 전일 대비 변했는지만 순수하게 검사**합니다.

**점검 대상 — 총 52개**: 코스피 지수, 코스닥 지수, **코스피 시가총액 상위 50종목**의 종가.

**판정 규칙**(오너 설계):

1. **52개 전부가 전일과 동일(무변동)** → 크롤링 실패 또는 휴장일 둘 중 하나입니다. 이 알고리즘은 **둘을 구분하지 않습니다** — 어느 쪽이든 결과적으로 "이 날짜엔 신뢰할 수 있는 새 종가가 없다"는 같은 결론이므로, **그날 귀속된 pending 주문은 전부 실패 처리**하면 됩니다(2-4의 5번). 실제로 어느 쪽이었는지는 관리자가 로그로 나중에 들여다볼 수 있으면 충분하고, 자동 판정에서 굳이 나눌 필요는 없습니다.
2. **지수(코스피·코스닥) 중 하나 이상은 변했는데, 상위 50종목 일부가 무변동** → 크롤링 부분 실패로 간주.
3. **상위 50종목은 전부 변했는데 코스피·코스닥 지수가 무변동** → 이것도 크롤링 실패로 간주(지수는 종목 움직임을 반영해야 하므로 앞뒤가 안 맞는 상태).
4. **허용 오차: 상위 50종목 중 최대 10개까지는 무변동이어도 정상으로 봅니다.** 유동성이 낮은 날엔 실제로 종가가 안 움직이는 종목이 있을 수 있기 때문입니다. **11개 이상 무변동이면** 자동으로 "실패"를 확정하지 않고, **관리자 확인을 거쳐** 실패 여부를 최종 판정합니다(대량 무변동은 이례적이라 사람 눈이 한 번 보는 게 안전합니다 — §0-1).

**구현 위치**: `utils/duel_rules.py`의 순수 함수(`check_crawl_freshness(today_prices, yesterday_prices) -> "ok" | "failed" | "needs_review"`)로 짜서 4단계에서 오프라인 테스트하기 쉽게 합니다. 이 함수는 이 모듈 전용이 아니라 나중에 다른 모듈에서도 "오늘 데이터가 신선한가"를 물을 때 재사용할 수 있게 일반적으로 짜두면 좋습니다(§0-3-10).

---

## 3단계 — 상장폐지·기업행위 예외처리 (미착수)

### 3-1. 상장폐지 = 100% 손실 상각 (오너 확정)

- 보유 종목이 상장폐지되면 **그 포지션의 평가액을 0으로 확정**합니다. `duel_positions.status='delisted'`, `delisted_date` 기록. `avg_cost`와 `quantity`는 **지우지 않습니다** — 손실이 손실로 보여야 하기 때문입니다.
- **"값을 모르는 상태"로 두지 않는 것은 오너가 의도적으로 고른 단순화입니다.** §0-1은 "모르는 것을 아는 척하지 말라"는 원칙인데, 여기서는 "확인된 상장폐지"라는 **사실**에 근거해 0으로 확정하는 것이므로 위반이 아닙니다. **다만 그 사실 확인이 진짜여야 합니다** — 아래 3-2.
- 상장폐지는 **현금흐름이 아니라 평가손실**입니다. `cash_flow_amount`에 넣지 마세요. TWR에서 손실로 정확히 반영됩니다.

### 3-2. ✅ 상장폐지 탐지 — 2026-08-19 확정: v1은 수동 처리 (자동 탐지는 안 만듦)

**이 코드베이스 어디에도 상장폐지 탐지 기능이 없고, v1에서는 만들지 않기로 확정했습니다.** 이유: 코스피 상위 200종목은 대형주 중심이라 상장폐지가 자주 일어나는 일이 아니고, DART/KIND 공시를 정기적으로 교차 확인하는 파이프라인은 그 자체로 별도 프로젝트급 작업이라 지금 들이는 비용 대비 얻는 게 적습니다(§0-3-10 YAGNI). 가상자금이라 시급성도 낮습니다.

**v1 운영 방식**:
- 오너(또는 관리자 계정)가 실제 상장폐지 사실을 **직접 확인**했을 때, 관리 화면(또는 최소한 관리자 전용 스크립트)에서 **수동으로** 해당 포지션을 `status='delisted'` + `delisted_date` + 평가액 0으로 확정 상각합니다.
- 그 전까지는 가격 데이터가 없으면(3-3의 유니버스 이탈과 구분해서) **"가격 확인 중"** 정도로만 표시하고, **자동으로 0원 처리하지 않습니다** — §0-1이 요구하는 대로 "수집 실패"와 "상장폐지 확정"을 절대 자동으로 같다고 판단하지 않습니다.
- 나중에 사용자가 늘어 수동 처리가 감당 안 될 만큼 자주 발생하면, 그때 자동 탐지 파이프라인을 별도 과제로 검토합니다. **지금은 착수하지 않습니다.**

### 3-3. ✅ 유니버스 이탈 ≠ 상장폐지 — 2026-08-19 3차 확정

코스피 **상위 200종목**은 시가총액 순위라 **매일 바뀝니다.** 보유 중인 종목이 201위로 밀려나는 건 **상장폐지가 아닙니다.**

**확정된 처리**:

- **주문 가능 유니버스** = 주문 시점의 코스피 상위 200 (그대로 유지).
- **보유 중 이탈 종목의 평가는 계속합니다.** 201~500위 사이에 있는 동안은 **`data/kr_all_market_prices.json`(코스피+코스닥 전종목 현재가)**로 폴백해 가격을 계속 조회합니다 — 이 파일은 이미 "내 성적표" v2에서 검증된 채로 존재합니다. **최장 3개월간** 이렇게 추적합니다.
- **500위 밖으로 더 밀려나면 강제 청산합니다.** 그 시점의 마지막 확정 종가로 포지션을 정리하고(매도 금지 원칙의 **의도된 예외** — 데이터 커버리지 한계에 의한 관리자 처리이지 사용자의 매도 행위가 아닙니다), 정리된 금액은 그대로 계좌 현금으로 돌아가 이후 다른 종목 매수에 쓸 수 있습니다. 이건 손실 처리(3-1의 상장폐지 상각)와는 다릅니다 — **정상 가격으로 정리되는 것**이므로 원장에는 매수와 마찬가지로 순수한 현금흐름 아닌 평가전환 이벤트로 남기면 됩니다(구체 원장 표현은 2단계 구현 시 확정).
- **⚠️ 확장성 메모(오너 지시)**: 만약 실제 운영 중 상위 200위에서 밀려나는 보유 종목이 많아지면, **코스피 수집기의 수집 범위 자체(200 → 300, 500 등)를 늘리는 선택지를 열어둡니다.** 지금 200으로 고정 코딩하지 말고, 이 숫자를 설정값(환경변수 또는 상수 하나)으로 빼서 나중에 조정하기 쉽게 짜두세요(§0-3-10 — 나중에 바꿀 곳이 한 군데이게).

### 3-4. ✅ 상장폐지 외의 기업행위 — 2026-08-19 3차 확정 (범위 축소)

**오너 결정: 이 모듈은 순수하게 주가·시가총액만 보고 움직이므로, 주주가치 훼손/제고 같은 정성적 판단은 애초에 검토 대상이 아닙니다.** 실제로 조정이 필요한 건 **"주당 가격과 보유 수량 자체가 바뀌는" 기업행위**뿐입니다 — 액면분할·병합, 무상증자, 감자가 여기 해당합니다(무상증자·감자도 분할·병합과 같은 범주로, 주식 수가 바뀌고 그만큼 주가가 조정되는 사건이라 데이터가 없으면 여전히 §0-1: 화면이 사실과 달라짐 → "주가 폭락"으로 잘못 보일 수 있습니다).

**AI가 추가로 짚어두는 것** (오너가 "문제될 것 같으면 얘기해달라"고 하셔서 — 지금 결정할 필요는 없고, 실제로 겪을 때를 대비한 참고입니다):
- **종목코드 변경** — 회사가 그대로인데 코드만 바뀌는 경우(예: 합병 후 신주 코드 부여). 가격이 바뀌는 사건은 아니지만, 우리 수집기가 옛 코드를 추적하다 "이 종목이 사라졌다"고 오판할 수 있어 3-3의 유니버스 이탈 로직이 잘못 발동될 위험이 있습니다.
- **거래정지** — 상장폐지도, 유니버스 이탈도 아니지만 그 기간엔 확정 종가 자체가 없습니다. 2-9의 무변동 판정(허용치 10개)에 우연히 걸리거나 안 걸리거나 할 수 있는데, 어느 쪽이든 "그 종목 값은 모른다"로 처리되면 자연히 안전합니다.

**이 두 가지는 지금 별도 로직을 만들 필요는 없습니다** — 위 3-2·3-3의 기존 규칙(가격 미확인 → 상각하지 않고 대기, 500위 밖 → 정리)이 사실상 이 경우들도 커버합니다. 실제로 문제가 생기면 그때 세부 규칙을 다듬으면 됩니다.

---

## 4단계 — 오프라인 테스트 (`tests/test_duel.py` 신규, 미착수)

`tests/test_report.py`(547체크)·`tests/test_scorecard_ocr.py`(76체크)와 같은 방식 — **네트워크·Supabase 없이** 가짜 클라이언트와 순수 함수로 검증합니다. `utils/duel_rules.py`를 순수 함수로 짜야 하는 이유가 이것입니다.

최소 커버리지:

- **체결 계산**: 요청 수량 × 종가가 가용 현금 이내면 전량 체결, 초과하면 `floor(가용현금/종가)`만큼 **부분체결**되고 `fail_reason`에 요청/실제 수량이 남는지, 잔돈이 현금으로 남는지, 1주도 못 사면 `expired` + 사유 문구가 남는지.
- **가용 현금**: 같은 계좌에 여러 `pending` 주문이 있을 때 **`saved_at` 빠른 순서대로** 예수금이 배정되는지 — **뒷 주문이 앞 주문 몫까지 넘보지 않는지 회귀 테스트로 고정**.
- **주문 수정·취소**: 배치 처리 전까지는 `requested_quantity` 수정·취소가 되고, 배치가 이미 처리를 시작한 뒤에는 거부되는지.
- ~~**매도 불가**: 애플리케이션 경로로 수량 감소 시도가 전부 거부되는지. DB 트리거는 별도로 SQL 레벨 확인.~~ → **⚠️ 2026-08-21 정정(§2-3-1)**: 이제 창당 1회 매도는 **허용**돼야 하고, 창당 2회째 시도·창 밖 시도·보유 초과 시도만 거부되는지로 바뀝니다.
- **정기 입금**: 3계좌 전부에 80만원씩, 배치 2회 실행 시 중복 입금 없음(멱등성).
- **이월**: 안 쓴 현금이 다음 기간에 그대로 남고, **입금으로 이중 계상되지 않는지**.
- **TWR**: 입금이 있는 기간에 단순 수익률과 **다른 값**이 나오는지(둘이 같으면 TWR이 안 걸린 것), 현금만 들고 있는 계좌의 TWR이 **0%로 나오는지**(단순 수익률이라면 +48%로 나올 시나리오).
- **후행지표 원칙 회귀 테스트**: 그날 확정 종가가 없는 상태에서 체결 함수를 호출했을 때 **절대 체결되지 않고** 명확한 실패를 내는지. `SCORECARD_V2_OCR_WORK_ORDER.md`가 "OCR 값이 현재가 필드를 채우지 않는지"를 회귀 테스트로 고정한 것과 같은 목적 — **나중에 누가 실수로 넣으면 바로 잡히게.**
- **동의 게이팅(5단계 선행 가능)**: 동의하지 않은 필드가 발행표 행에 **절대 채워지지 않는지**(null인지), 동의 boolean 7개의 모든 조합에 대해.
- **철회**: 철회 시 해당 닉네임의 공개 행이 **전부** 사라지는지, 3개월 안에 재동의가 **거부**되는지.
- **최소 인원 미달**: 임계값 미만인 구간이 **발행되지 않고**, 이전에 발행됐던 행도 제거되는지.
- **세션 격리**: `tests/test_web_session_isolation.py`에 결투 화면 추가 — 서로 다른 두 사용자 컨텍스트에서 계좌·주문·현금이 섞이지 않는지.
- 기존 스위트(scorecard, report, us, stock_history) **전부 회귀 없음**.

---

## 5단계 — Branch 2 "내 밑으로 눈 깔어" 공개 인프라 (✅ 완료 — 백엔드·화면 2종·발행 워크플로우·`main.py` 배선까지 전부 작성·검증·반영 완료 → 🔴 2026-08-23, 이 단계 전체가 은퇴됨. 아래 §5-20 참고)

🔴 **2026-08-23 정정 — 이 단계(Branch 2) 전체가 은퇴되었습니다.** 공개 대상이 결투 가상계좌 성적에서 "내 성적표"(실제 보유 자산)로 바뀌면서, 아래에 적힌 화면·표·플래그는 전부 삭제/DROP 대상이 되었고 새 모듈(`scorecard_public_*`)로 교체됐습니다. **이 절의 서술은 정정하지 않고 그대로 남겨 둡니다** — 처음 이 설계를 왜 이렇게 잡았는지의 기록이자, 실제로는 무엇이 잘못 구현됐었는지의 증거이기 때문입니다. 아래 §5-20에 무엇이, 왜, 어떻게 바뀌었는지 전부 적었습니다. 새 설계의 상세는 별도 문서 `SCORECARD_PUBLIC_LEADERBOARD_WORK_ORDER.md`를 보세요. **`/duel`(Branch 1, "덤벼라 나 자신")은 이 은퇴와 무관하며 그대로 운영됩니다.**

✅ **2026-08-20 오너 정정 — 이 경고문의 의미를 명확히 함.** "착수하지 말라"는 **코딩을 미루라는 뜻이 아니라, 실제로 순위표가 화면에 발행/노출되는 시점을 사람이 충분히 쌓이기 전까지 미루라는 뜻**입니다("착수하지 말라고 보다는 일정 사람이 쌓이기 전까지 공개를 하지 않는 방향으로 가자, 만들 수 있을 때는 만들어만 두는 건 좋을 거 같아"). 코드는 지금 만들어도 됩니다 — 어차피 5-1(기본 비공개)·5-6(최소 인원 미달 시 미발행)·2-8/7단계의 3단계 공개 전환 패턴이 이미 "실제로 아무도 안 쌓였는데 순위표가 뜨는 사고"를 구조적으로 막고 있습니다. 그리고 §0-3-8은 이 프로젝트의 **최상위 무예외 원칙**입니다 — 이 단계의 모든 코드는 "버그가 나도 새어나갈 수 없는가"를 기준으로 리뷰해야 합니다.

### 5-1. 기본값은 비공개

옵트인하지 않은 사용자는 **어떤 발행표에도 존재하지 않습니다.** "편의상 기본 공개"는 선택지가 아닙니다(§0-3-8 명문).

### 5-2. 2단계 이상의 granular 동의 UX (오너 명시 요구, 2026-08-19 3차 라운드에서 세부 확정)

1. **1층 — 항목별 개별 체크박스 5개, 문장 하나씩.** 하나의 "공개하기" 토글로 뭉치면 안 됩니다.
   - ☐ **순위** — 내 순위가 공개 순위표에 표시됩니다.
   - ☐ **수익률** — 내 계좌의 수익률이 공개됩니다.
   - ☐ **보유종목** — **내 보유종목이 순위표에서 다른 사람에게 개별 열람 가능하게 공개됩니다.** (이 문장에서 "개별 열람"을 빼지 마세요. 오너가 명시적으로 요구한 문구 요소입니다.)
   - ☐ **수량** — 종목별 보유 수량이 공개됩니다.
   - ☐ **매입금액** — 종목별 매입금액이 공개됩니다.
2. **✅ "일부 공개" 조합 없음 — 2026-08-19 확정.** "수익률은 공개, 순위는 비공개" 같은 부분 조합은 허용하지 않습니다. **위 5개 항목은 전부 체크하거나(전체 공개), 전부 체크하지 않거나(공개 안 함) 둘 중 하나뿐입니다.** (개별 문장을 하나씩 보여주는 건 "무엇에 동의하는지 사용자가 명확히 인지하게" 하려는 목적이지, 항목별로 선택적으로 고를 수 있다는 뜻이 아닙니다 — UI에서 5개 체크박스는 여전히 각각 존재하지만, 최종 확인(아래 3번)은 5개가 **전부** 체크됐을 때만 눌립니다.) 이 규칙은 **아래 4번의 독립 동의(실제 매입총합 사용)에는 적용되지 않습니다** — 그건 별개입니다.
3. **2층 — 별도의 최종 확인 단계.** 5개를 다 체크했다고 바로 발행되지 않습니다. **한 번 더, 분리된 확인**을 거쳐야 `final_confirmed=true`가 되고, 그때부터 발행 대상이 됩니다.
4. **완전히 별개인 독립 동의**: ☐ **"내 '내 성적표' 실제 매입총합을 순위표 원금 구간 산정에 사용합니다."**
   - **위 5개와 절대 같은 묶음에 넣지 마세요.** 다른 모듈의 **실제 금융 데이터**를 끌어오는 동의이고, AI가 이 위험을 제기했을 때 오너가 **분리를 확정**했습니다. 위 "전부 아니면 전무" 규칙과 무관하게 독립적으로 켜고 끌 수 있습니다.
   - 이 동의가 없는 사용자는 **원금 구간 산정에서 제외**되며, 그럼에도 나머지 동의(1~5번 전체)가 있다면 순위표에는 참여할 수 있어야 합니다(구간 없는 전체 순위표 또는 "구간 미적용" 그룹).
5. **✅ 책임 고지 문구 — 2026-08-19 오너 확정.** "읽지 않고 동의한 것도 본인 책임이다"라는 취지의 문장을 **개별 체크박스 영역과 최종 확인 영역, 최소 두 곳 이상**에 명확히 노출합니다. 정확한 문안은 7-2에서 확정하되, 요구사항 자체는 지금 못박습니다 — 한 곳에만 작게 적어두는 걸로는 부족합니다.

### 5-3. 원금 구간(브래킷) — ✅ 경계값 2026-08-19 3차 확정

- 구간 기준은 **실제 "내 성적표" 매입원가합계(매입총합)**입니다. 결투 시드머니는 **모두 1천만원으로 동일**하므로 구간 기준이 될 수 없습니다(구분이 안 됨).
- 이 값은 **5-2의 4번(독립 동의)이 있는 사용자에 대해서만** 조회합니다. 동의 없는 사용자의 `holdings`를 읽는 코드 경로가 하나라도 있으면 §0-3-8 위반입니다.
- **구간 경계(오너 확정, 총 8구간)**:

  | 구간 | 매입원가합계 범위 |
  |---|---|
  | 1 | 1억원 이상 |
  | 2 | 6천만원 이상 ~ 1억원 미만 |
  | 3 | 3천만원 이상 ~ 6천만원 미만 |
  | 4 | 1천만원 이상 ~ 3천만원 미만 |
  | 5 | 500만원 이상 ~ 1천만원 미만 |
  | 6 | 300만원 이상 ~ 500만원 미만 |
  | 7 | 100만원 이상 ~ 300만원 미만 |
  | 8 | 100만원 미만 |

  경계값은 오너가 준 원본 그대로이며(이상/미만 방향을 이렇게 해석했습니다 — 겹치거나 빈 구간이 없도록), `bracket_key`는 이 8개 중 하나의 식별자(예: `"1억+"`, `"6천~1억"` 등)로 저장하면 됩니다.
- **✅ 2026-08-19 4차 확정 — "체급" 해석 확인 완료.** 매입원가합계는 **체급(구간)을 나누는 용도**이고, 구간 안에서 1등·2등을 가르는 실제 순위는 **TWR 수익률**입니다. 오너가 정확히 이 의도라고 확인했습니다("최초의 체급의 균형을 맞추기 위해서").
- **✅ 체급 고정 규칙(신설, 2026-08-19 4차 확정)**: **한 번 정해진 체급(구간)은 다음 정기 시즌이 오기 전까지 바뀌지 않습니다.** 시즌 도중에 "내 성적표"의 매입원가합계가 실제로 늘거나 줄어도, 그 시즌 안에서는 **처음 배정된 체급 그대로** 유지되고, 순위는 그 고정된 체급 안에서 TWR로만 갈립니다. 체급이 시즌 중에 계속 흔들리면 "체급을 맞춰서 공정하게 겨룬다"는 원래 취지가 무너지기 때문입니다(체급이 실시간으로 바뀌면 사실상 원금이 큰 사람이 계속 유리한 체급으로 옮겨다니는 것과 같아짐).
  - **✅ 시즌 주기 확정(2026-08-19 5차) — 1년.** 체급은 **전체 자산 규모**를 반영하는 것이라, 분기·반기처럼 짧은 주기로 다시 매기면 단기 변동에 흔들려 "체급"이라는 개념 자체가 무의미해집니다(오너 판단). **매 1년마다 그 시점의 "내 성적표" 매입원가합계로 체급을 재산정**하고, 그 사이 1년 동안은 매입원가가 얼마나 변하든 체급이 고정됩니다. 코드에는 `duel_season_length_months = 12`처럼 설정값 하나로 빼두면 나중에 바꾸기 쉽습니다(§0-3-10). 시즌 시작 기준일(예: 매년 1월 1일 vs 사용자별 가입일 기준)은 2단계 구현 시 정하면 됩니다 — 전자가 전체 사용자를 한 번에 재산정하는 집합 연산(§0-3-2)으로 처리하기 쉬워 추천합니다.
- **📌 순위표 최상단 고정 문구 — 2026-08-19 오너 확정, 반드시 크게 노출**. 아래 문구를 랭킹 페이지 어디서도 스크롤 없이 바로 보이는 위치(최상단)에 고정합니다. 문구는 그대로 씁니다 — 요약·축약하지 마세요:

  > 종목의 추천, 매수, 매도 권유가 아니라 지금의 데이터는 어디까지나 개인의 공부를 목적으로 진행되고 있는 것이며 투자의 책임은 개인에게 있습니다.
  >
  > 실제로 공개되어있는 '내 성적표'의 데이터는 개인이 등록한 것입니다. 운영자는 '내 성적표'의 데이터를 확인, 검증하고 있지 않으며 확인, 검증을 요청하고 있지 않습니다. 주의바랍니다.

  이 문구는 §0-1과도 연결됩니다 — "내 성적표"는 사용자가 스스로 입력(또는 OCR로 넣은 뒤 직접 확인)한 값이라 운영자가 사실 여부를 검증하지 않는다는 점을, 그 데이터가 남에게 공개되는 화면에서는 특히 명확히 알려야 합니다.

### 5-4. 발행 배치 — 원본 RLS는 손대지 않습니다

**확정된 방식:**

1. 야간 배치(service_role)가 `final_confirmed=true`이고 `revoked_at`이 없는 계좌만 골라냅니다.
2. **각 계좌의 동의 boolean에 맞는 필드만** 뽑습니다. 동의 없는 필드는 **null**로 둡니다(0이나 빈 문자열 금지 — §0-1).
3. 구간별·창유형별로 `rank() over (...)`로 순위를 계산합니다.
4. 최소 인원 임계값을 만족하는 구간만 `duel_public_leaderboard` / `duel_public_holdings`에 **전량 재작성**(그날 발행분을 통째로 갈아끼우는 방식이 부분 갱신보다 안전합니다).
5. 화면은 **이 두 표만** 읽습니다. `duel_positions`·`holdings`·`profiles`·`duel_cash_ledger`를 순위표 코드 경로에서 **import조차 하지 않게** 하세요.

**이렇게 하는 이유**: 원본 테이블의 RLS를 "옵트인한 행은 남도 볼 수 있게" 완화하는 방식은, **정책 조건 한 줄만 잘못 써도 전원의 데이터가 열립니다.** 발행표 방식은 **순위표 읽기 경로에 버그가 나더라도 이미 의도적으로 발행된 것 이상은 물리적으로 존재하지 않습니다.** §0-3-8이 요구하는 건 "조심하기"가 아니라 이런 **구조적 방어**입니다.

### 5-5. 무작위 닉네임

- 생성 시점: **옵트인 시가 아니라 5단계(공개) 동의 시**(5-9에서 정정 — 1갈래만 참여하고 공개엔 동의 안 한 사용자에게 괜히 공개용 닉네임을 만들어주지 않기 위함). 계좌별로 저장(`duel_nicknames`).
- **`user_id`·이메일·가입시각 등 어떤 값에서도 유도하지 않습니다.** 해시도 안 됩니다 — 알고리즘이 알려지면 역조회가 가능해집니다(§0-3-9: 이미 알려진 기법에는 예외 없이 방어).
- 재계산·재사용 금지. 한 번 만든 닉네임은 그 계좌에 고정이고, 다른 계좌가 물려받지 않습니다.
- **✅ 2026-08-20 확정 — 형식 = 형용사 2개(서로 다름) + 명사 1개, 숫자 없음.** 처음엔 (형용사+명사+숫자4자리)였는데, 오너가 "숫자가 붙으니 닉네임이 기계적으로 느껴진다"고 지적해 숫자를 없앴습니다. 그 대신 숫자가 채우던 안전 여유분을 **형용사 목록을 48개 → 130개로, 명사 목록을 48개 → 128개로 크게 늘리는 것**으로 옮겼습니다. 전체 후보 공간은 `130 × 129 × 128 ≈ 214만 가지`(형용사 2개를 순서 있게, 서로 다르게 고르므로 `130×129`)로, 원래 4자리 숫자 방식(약 2,300만 가지)보다는 좁아졌지만, 아래 재검산 결과 실제 운영에는 문제가 없는 수준입니다.
  - **재검산(직접 계산해 확인함)**: 진짜 중요한 숫자는 "참가자 N명이 있을 때, 방금 만든 새 닉네임 후보 하나가 그중 하나와 겹칠 확률"(재시도를 유발하는 확률)입니다 — 이건 `N ÷ 214만`으로 **선형**입니다. 참가자 1만 명일 때 약 0.47%, 5만 명일 때도 약 2.3%뿐이고, 겹쳐도 `duel_db.ensure_nickname()`이 최대 8번까지 자동 재시도합니다. 8번 다 실패해서 발급 자체가 막힐 확률은 5만 명 기준으로도 10^-13 수준(사실상 0)입니다.
  - **"생일 문제" 확률과 헷갈리지 않기**: "참가자 N명 전체 안에 어딘가 겹치는 쌍이 하나라도 있을 확률"은 이보다 훨씬 빠르게 커져서(N=5,000명 근처부터 거의 100%) 언뜻 위험해 보이지만, 이건 실제로 두 사람이 같은 닉네임을 갖게 된다는 뜻이 아닙니다 — `duel_nicknames.nickname`의 DB `unique` 제약이 그 자체를 애초에 불가능하게 막고, 그저 "재시도가 어딘가에서 한 번쯤 일어난다"는 뜻일 뿐입니다.
  - 단어 목록의 실제 구성(어떤 형용사·명사를 썼는지)은 이제 확정됐고, 어감 자체에 이견이 있으면 언제든 단어만 바꿀 수 있습니다(구조는 그대로 두고 `utils/duel_rules.py`의 `NICKNAME_ADJECTIVES`/`NICKNAME_NOUNS` 두 목록만 편집하면 됩니다).

### 5-6. 최소 인원 임계값

- 참여자가 적을 때 순위표를 공개하면 **소수 N 역추적**으로 익명성이 무너집니다(3명짜리 구간에서 "1위 닉네임"은 사실상 실명입니다).
- 임계값 미만인 구간은 **아예 발행하지 않습니다.** 이미 발행돼 있던 행도 제거합니다.
- ✅ **임계값 = 500명으로 확정**(0단계 2차 라운드에서 이미 확정됨 — 위 "🔴 미결정 항목 모아보기"에도 기록돼 있습니다. 이 절의 🔴 표시는 그 확정을 반영 못 한 옛 문구였습니다, 2026-08-20 정정).

### 5-7. 순위표 표시

- **상위 500 + 하위 500**, 페이지네이션. 미국주식 화면(30개 페이지네이션)의 기존 패턴을 참고하세요(§0-3-10).
- 순위는 **배치에서 미리 계산돼 발행표에 저장된 값**을 읽기만 합니다. **페이지 로드 시 계산 금지**(§0-3-2).

### 5-8. 동의 철회

1. 철회 즉시 **그 계좌의 발행된 공개 기록을 전부 영구 삭제**합니다 — 과거 순위, 과거 발행 수익률, 발행된 보유종목 행까지 **숨김이 아니라 삭제**입니다.
2. 철회 후 **3개월간 재동의 차단.** `revoked_at + interval '3 months'` 이전에는 동의 저장 경로가 거부해야 합니다. **애플리케이션과 배치 양쪽에서** 확인하세요(화면만 막으면 배치가 되살릴 수 있습니다).
3. 철회 사실 자체(`revoked_at`)는 비공개 동의 테이블에 남습니다 — 3개월 판정에 필요하며 공개 데이터가 아닙니다.

### 5-9. 백엔드 구현 완료 (2026-08-20, 오푸스 높음 — 6번째 코딩 작업, 화면 2종·발행 워크플로우는 명시적으로 이번 범위 밖)

**무엇을 만들었는지:**

- `sql/duel_schema.sql`에 **예외적으로** 2군데 손을 댔습니다(사전에 "진짜 버그를 발견했을 때만"이라고 못박아 둔 조건에 해당한다고 판단한 경우입니다):
  1. `duel_public_leaderboard`의 유니크 제약을 `(published_date, window_type, bracket_key, rank)`에서 **`(published_date, window_type, bracket_key, nickname)`**으로 바꿨습니다(제약 이름: `duel_public_leaderboard_participant_unique`). 원래대로였다면 **동순위가 하나라도 생기는 순간 발행 배치가 그 자리에서 죽습니다** — 그런데 현금만 들고 아무것도 안 산 계좌는 TWR이 정확히 0.0%로 전부 동일해서, 500명 규모에서는 동순위가 사실상 반드시 생깁니다. 첫 실발행 때 배치가 터졌을 사안이라 예외 조치로 지금 고쳤습니다. 조회 성능용 비유니크 인덱스(`duel_public_leaderboard_group_rank_idx`)도 같이 추가했고, 마이그레이션은 `drop constraint if exists` → `add constraint` 3줄로 멱등하게 짰습니다.
  2. 새 표 `public.duel_bracket_assignments`(§8-3, `account_id`+`season_key` 복합 기본키, `bracket_key`, `assigned_at`) 신설. **왜 필요했는지**: "체급은 시즌 동안 고정"(5-3) 규칙을 지킬 유일한 저장소가 없었습니다 — 발행표(`duel_public_leaderboard`)는 매일 통째로 지웠다 다시 쓰는 표라 여기서 체급을 읽으면 그날그날 달라질 수 있어서 규칙을 코드로 강제할 수가 없었습니다. 이 표는 RLS로 본인 것만 조회 가능하고, **`service_role`에도 update/delete 권한을 안 줬습니다**(insert만 가능) — "한 번 배정되면 그 시즌엔 절대 안 바뀐다"를 앱 로직이 아니라 **DB 권한 자체**로 강제한 것입니다. 표 개수 10 → 11로 갱신.
- `utils/duel_rules.py` (+444줄, 신설 §10): `BRACKET_TIERS`(8구간 경계값, 유일한 출처), `assign_bracket()`, `BRACKET_NONE_KEY`(원화·달러 혼합 보유 또는 무보유 동의자용 — "구간 미적용", 절대 환율을 지어내지 않음), `DUEL_SEASON_LENGTH_MONTHS=12`, `season_key_for_date()`/`resolve_bracket_for_season()`(시즌 시작일=**매년 3월 1일, 2026-08-20 오너 확정** — 처음엔 1월 1일을 추천했었으나 오너가 3월 1일로 확정, 아래 참고), `generate_nickname()`(인자 없음, `random`이 아니라 `secrets` 사용, 형용사+명사+숫자4자리 조합으로 약 2,300만 개, 어떤 신원 값에서도 유도하지 않음), `rank_participants()`(동순위는 같은 순위를 공유, TWR이 `None`인 계좌는 순위·최소인원 집계 모두에서 제외), `MIN_PARTICIPANTS_FOR_PUBLICATION=500`(5-6의 옛 🔴 표시가 이미 고쳐졌던 값과 동일 — 재확인), `RECONSENT_BLOCK_MONTHS=3`, `resolve_reconsent_block()`.
- `utils/duel_db.py` (+662줄): (A) 사용자 세션용 — `fetch_my_consent()`, `revoke_consent()`(`revoked_at` 설정 + 동의 boolean 7개 전부 초기화, **행 자체는 삭제 안 함**, 중복 클릭에도 안전), `ensure_nickname()`(**옵트인 시점이 아니라 5단계 동의 시점에 생성** — 1갈래만 참여하고 공개엔 동의 안 한 사용자에게 괜히 공개용 닉네임을 만들어주지 않기 위함), `save_consent()`에 3개월 재동의 차단 검사 추가. (B) 배치 전용(§B-7 신설) — `fetch_publishable_consents`, `fetch_revoked_consent_accounts`, `fetch_nicknames_for_accounts`, `fetch_bracket_assignments`, **`fetch_real_principal_holdings(service_client, user_ids)`**(`user_ids`를 필수 인자로 만들어 청크 단위 `in` 필터만 허용 — 동의 안 한 사용자의 보유종목을 실수로라도 전체 조회하는 코드 경로 자체를 차단, §0-3-8), `fetch_published_group_index`, `leaderboard_has_any_rows`, `insert_bracket_assignments`, `delete_published_rows_for_date`, `delete_published_rows_for_nicknames`, `delete_published_group`, `write_public_leaderboard`, `write_public_holdings`, `_assert_no_identity_fields()`(발행 직전 마지막 방어선 — 신원 유추 가능 필드가 섞여 들어가면 여기서 막힘).
- 신규 `utils/duel_publish.py`(718줄) — 발행 배치 오케스트레이션. `duel_batch.py`(야간 체결 배치)와 **의도적으로 분리**했습니다(서로 다른 배치 주기·권한 범위이기 때문). 순서: 철회 정리 → 발행 대상 동의자 조회 → 체급 배정(고정 규칙 적용) → TWR 계산 → 순위 산정 → 최소 인원 게이팅 → 그날 발행분 **전량 삭제 후 재작성**(5-4의 확정 방식 그대로). `dry_run=True` 지원.
- 신규 `tests/test_duel_publish.py`(1,327줄, 120개), `tests/test_duel_db.py` 확장(가짜 쿼리 객체에 `.filter()` 추가, 기존 단언 2곳 수정, "발행표를 안 건드린다"는 약한 검증 하나를 더 강한 불변식 검증으로 교체).
- **검증**: `python3 -m py_compile utils/duel_rules.py utils/duel_db.py utils/duel_publish.py tests/test_duel_publish.py tests/test_duel_db.py` 클린. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_batch.py tests/test_duel_publish.py` → **360개 전부 통과**(55+111+74+120, 회귀 0) — **제가 직접 재실행해서 확인한 결과입니다**(에이전트 보고 수치와 일치). 실제 로컬 PostgreSQL 검증(동의 전체-아니면-전무 강제, 철회 후 재동의 차단, 동순위 처리, 발행 두 표 RLS, `duel_bracket_assignments` 사용자 간 격리, 철회 시 과거 기록 완전 삭제, 최소 인원 미달 시 삭제 등 23개 시나리오)은 에이전트 보고 기준이며 제가 SQL 자체를 재실행해 재검증하지는 않았습니다 — 다음에 실제 Supabase 스테이징에 반영할 때 6단계 실검증에서 한 번 더 확인하는 걸 권합니다.
- **✅ 2026-08-20 오너 확인 완료 — 4건 전부 확정**:
  1. **동의 철회 시 발행표 삭제 시점 → "다음 야간 발행 배치 때 지우는 지금 방식 그대로" 확정.** 즉시 삭제 RPC는 만들지 않습니다. 코드 변경 없음(이미 그렇게 짜여 있었습니다).
  2. **시즌 시작일 → 매년 3월 1일로 확정(1월 1일에서 변경).** `utils/duel_rules.py`의 `DUEL_SEASON_ANCHOR_MONTH`를 1 → 3으로 바꿨고, 관련 주석·테스트(`tests/test_duel_publish.py`의 시즌 경계 테스트 5개)도 3월 1일 기준으로 다시 계산해 갱신했습니다.
  3. **닉네임 형식 → 숫자 접미사 완전 제거, 형용사 2개(서로 다름)+명사 1개로 재설계.** "어느 정도를 말하는거야"라는 되물음에 실제 목록 일부를 보여드렸더니, "숫자가 붙으면 너무 기계적으로 느껴진다"는 답을 주셔서 숫자를 아예 없앴습니다. 그 대신 단어 목록을 형용사 48→130개, 명사 48→128개로 크게 늘려서 안전 여유분을 확보했습니다(상세 계산은 5-5절 참고 — "재시도를 유발하는 실제 확률"과 "생일 문제 확률"을 구분해서 다시 계산했고, 후자를 전자로 착각하면 안 된다는 점도 정리했습니다). `generate_nickname()`·`nickname_space_size()` 재작성, 관련 테스트도 새 형식에 맞게 다시 씀(디코딩 헬퍼 `_decompose_nickname()` 추가, 두 형용사가 실제로 다른지 검증하는 테스트 신설).
  4. `bracket_key`는 코드에서 쓰는 ASCII 식별자이고, 화면에 보여줄 한글 라벨(`bracket_label()`)은 별도 함수로 분리해뒀습니다 — 이름 자체에 이견 없으면 그대로 갑니다.
  - **재검증**: `python3 -m py_compile utils/duel_rules.py utils/duel_db.py tests/test_duel_publish.py` 클린. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_batch.py tests/test_duel_publish.py` → **361개 전부 통과**(닉네임 테스트 1개 신설로 360→361, 회귀 0). 5만 개 무작위 생성 스트레스 테스트(직접 실행)로 공간 크기(2,146,560)와 실제 충돌 개수가 계산과 일치하는 것도 확인했습니다.
- **미착수였던 것 → 이번 라운드(§5-10)에서 완료.**

### 5-10. 화면 2종 + 발행 워크플로우 완료 (2026-08-20, 오푸스 — 7번째 코딩 작업)

**무엇을 만들었는지:**

- 신규 `web/pages/duel_consent_page.py`(740줄, `/duel/consent`) — 동의 관리 화면. 5-2의 구조 그대로: 1층(체크박스 5개, 문장 하나씩) → 2층(별도 최종 확인) → 완전히 별개인 독립 동의(실제 매입총합 사용) → 책임 고지 문구(체크박스 영역·최종확인 영역 **두 곳**). 이미 동의한 계좌는 현재 상태(닉네임·항목별 on/off·동의 시각)를 보여주고 철회 버튼 제공, 철회 전 확인 단계(되돌릴 수 없고 3개월 재동의 차단이 걸린다는 안내)를 거치게 했습니다. 닉네임은 최초 동의 저장 시점에 `ensure_nickname()`으로 발급(5-5 확정 순서: 옵트인 시가 아니라 5단계 동의 시).
- 신규 `web/pages/duel_leaderboard_page.py`(492줄, `/duel/leaderboard`) — 공개 순위표 화면. 5-3 고정 문구 2문단을 **글자 그대로**(맞춤법·띄어쓰기까지) 화면 최상단에 고정 — 테스트가 이 문서 원문과 직접 대조합니다(수정하면 테스트가 실패하도록 걸어뒀습니다). 창유형×체급 선택 → 순위표(상위 500+하위 500, 5-7의 배치 계산값만 읽음, 화면에서 재계산 안 함) → 행 클릭 시 그 닉네임의 보유종목 개별 열람(5-2). 참가자 미달로 그 그룹이 아직 발행 안 됐으면 "아직 공개할 만큼 사람이 안 모였습니다"로 정상 상태 안내(에러 아님).
- `utils/duel_db.py`에 **읽기 전용** 신규 함수 4개: `fetch_public_leaderboard_latest_date()`, `fetch_public_leaderboard(client, *, window_type, bracket_key, published_date=None, limit=, offset=, order_desc=)`, `fetch_public_holdings_for_nickname()`, `fetch_my_nickname()`(만들지 않고 읽기만 — 화면을 그리는 것만으로 닉네임이 발급되면 안 됨). 전부 `select("*")` 대신 컬럼을 하나하나 나열(`PUBLIC_LEADERBOARD_COLUMNS`/`PUBLIC_HOLDINGS_COLUMNS`) — 나중에 발행표에 컬럼이 늘어도 화면으로 새어나가지 않게 하는 구조적 방어(§0-3-8). `order_desc`로 정렬만 뒤집어서 인원수를 몰라도 "하위 500"을 정확히 읽습니다(방문마다 count 질의 도는 것을 피함, §0-3-2).
- `utils/duel_rules.py`에 §10-6(순위표 표시 상수·순수함수) 신설.
- `web/layout.py`에 신규 플래그 4개 — `DUEL_CONSENT_ENABLED`/`DUEL_CONSENT_MENU_ADMIN_ONLY`(동의 화면용), `DUEL_LEADERBOARD_ENABLED`/`DUEL_LEADERBOARD_MENU_ADMIN_ONLY`(순위표 화면용). 1갈래 스위치(`DUEL_ENABLED`)를 재사용하지 않고 화면마다 따로 둔 이유: 7-3이 "1갈래 먼저 공개 → 모집 → 2갈래 공개"를 요구하는데 스위치가 하나면 이 순서 자체가 불가능하기 때문. 두 2갈래 화면도 `DUEL_ENABLED`가 켜져 있을 때만 존재하도록 중첩 확인합니다(1갈래 없이 2갈래만 켜지는 경우를 막음).
- 신규 `run_duel_publish_batch.py`(130줄, 저장소 루트) — `run_duel_daily_batch.py`와 같은 분업(I/O·환경변수만 담당, 판단은 전부 `utils/duel_publish.py`).
- 신규 `.github/workflows/duel_publish_daily.yml`(138줄) — 발행 배치 워크플로우. **cron을 체결 배치(`duel_daily.yml`)보다 반드시 뒤에 오도록** 잡았습니다(UTC 08:40 = KST 17:40, 체결 배치의 타임아웃 상한 UTC 08:30보다 10분 뒤) — 순서가 뒤집히면 그날 체결이 반영 안 된 하루 낡은 성적으로 순위표가 나갑니다. 늦게 갱신되는 것과 틀린 값이 공개되는 것 중 후자가 훨씬 위험하다는 판단(§0-3-8)으로 여유를 둔 쪽을 택했습니다. **이 파일은 `.github/workflows/`라 이번 세션에서 저장소에 바로 못 넣습니다 — 아래 안내대로 device_bash로 반영해야 합니다.**
- 신규 `tests/test_duel_public_ui.py`(956줄, 44개) — 새 화면 2종의 핵심 규칙(전부-아니면-전무, 최종확인 분리, 철회 확인, 5-3 문구 원문 대조, 신원 미노출) + 렌더 스모크(가짜 nicegui/web.auth로 감싸 실제 실행까지 확인) + 신규 DB 함수 4개(빈 결과·페이지네이션·필터).
- `tests/test_duel_db.py` 기존 테스트 1건 재조정("A절에 발행표 이름이 아예 안 나온다" → "A절은 발행표에 **쓰지는** 않는다"로 — 스키마가 이미 `authenticated`에게 발행표 select를 허용하므로 조회 자체는 정상이고, 쓰기만 막으면 되는 게 맞는 검증입니다. 약화가 아니라 잘못된 검증을 바로잡은 것).
- **검증**: `python3 -m py_compile`(신규·수정 파일 전부) 클린 — **제가 직접 재실행해 확인**. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_batch.py tests/test_duel_publish.py tests/test_duel_public_ui.py` → **405개 전부 통과**(361 + 신규 44, 회귀 0) — **제가 직접 재실행해 확인, 에이전트 보고 수치와 일치**. `.github/workflows/duel_publish_daily.yml` YAML 파싱도 직접 재확인 완료. `select("*")` 미사용도 직접 grep으로 재확인.
- **✅ main.py 배선 완료(2026-08-20)** — 오너 컴퓨터에서 device_bash로 직접 편집(`duel_page`·`duel_consent_page`·`duel_leaderboard_page` 3줄 전부 추가 — 기존에 `duel_page` 한 줄도 실은 아직 안 들어가 있었던 걸 이번에 확인해서 같이 넣었습니다). `python3 -m py_compile main.py` 클린. `/duel`·`/duel/consent`·`/duel/leaderboard` 세 주소 모두 이제 `main.py`가 import합니다.
- **✅ 오너 확인 완료 — 2건**:
  1. **새 플래그 이름 4개(`DUEL_CONSENT_ENABLED` 등)** — 확인 과정에서 오해가 있었습니다: 이 4개는 **Python 코드 안의 상수/환경변수 이름일 뿐**, 화면에는 절대 안 뜹니다(Render 서버 설정과 소스 코드에만 존재). 사용자가 실제로 보는 문구는 이미 전부 한글로 따로 있습니다(메뉴: "⚔️ 결투다!" → "⚔️ 참전하기" / "🔓 공개 동의 관리" / "🏆 공개 순위표 (내 밑으로 눈 깔어)") — 오너가 지금까지 써온 톤 그대로입니다. 그래서 내부 이름 4개는 그대로 두고, 실제로 사용자에게 보이는 한글 라벨들을 다음에 오너가 화면으로 직접 보면서 검토하는 쪽으로 정리했습니다.
  2. **발행 배치 실행 시각 → 30분 여유로 확정(10분에서 늘림).** cron을 UTC 08:40(KST 17:40) → UTC 09:00(KST 18:00)으로 늦췄습니다. 이 결정의 트레이드오프: 예전엔 "저녁 주문 접수 시작(18:00:01) 전에 순위표가 반드시 갱신돼 있다"가 보장됐는데, 30분 버퍼를 넉넉히 두면서 그 보장이 없어집니다(최악의 경우 KST 18:20에 끝날 수 있음). 순위표 화면과 주문 접수는 서로 다른 기능이라 순서가 안 맞아도 실질적 문제는 없다고 판단해, "두 배치가 서로 부딪힐 여지를 줄이는 것"을 우선했습니다. `tests/test_duel_public_ui.py::test_publish_workflow_runs_after_the_fill_batch`가 정확한 숫자가 아니라 "발행 배치가 체결 배치의 타임아웃 상한 이후에 시작하는가"만 검증하므로 재검증 없이도 통과 확인(직접 재실행해서 405개 그대로 통과 확인).
- **범위 밖(다음에)**: 없음 — 이번 라운드의 급한 확인 사항은 전부 처리됐습니다.

### 5-11. 🆕 USD 트랙("달러 결투") — 신규 제안, 설계 확정 (2026-08-20, 코딩 착수 전 · 아직 코드 없음)

**배경**: 기존 v1은 원화·코스피 상위 200 한정이었고(§0-3, "범위 밖" 참고), 그 이유는 "환율 시계열이 없다"였습니다. 오너가 "내 성적표"("scorecard") 모듈이 이미 **환율 변환 없이 통화별로 완전히 분리해서** 수익률을 계산하고 있다는 전례(`utils/scorecard_db.py`의 `NO_FX_CONVERSION_NOTICE`, "통화가 다른 보유분은 합칠 수 없습니다" 하드 예외)를 근거로, "KRW·USD를 절대 합치지 않는" 별도 USD 트랙이라면 환율 없이도 가능하다고 지적 — 확인 결과 맞습니다. **아래는 그 새 트랙의 설계이고, 전부 오너 확정 사항이지만 코딩은 아직 한 줄도 시작하지 않았습니다.**

**✅ 확정된 것들:**

1. **아키텍처 = 완전 분리, 구분 컬럼 아님.** 처음엔 AI가 "기존 표에 `market`/`currency` 컬럼 하나만 추가"를 권했으나, 오너가 "차라리 아예 나누는 편이 오류 났을 때 막기 쉽지 않냐"고 반론 → 타당해서 뒤집었습니다. 이 저장소가 이미 같은 상황에서 같은 선택을 한 전례가 있습니다 — `scrape_us.yml` 머리말: "코스피용 `scrape.yml`과 완전히 분리된 별도 워크플로우입니다... 이미 잘 돌고 있는 코스피 자동화를 건드리지 않기 위해 파일 자체를 나눴습니다." 구분 컬럼 방식은 배치 코드가 `WHERE market = ...` 조건 하나만 빠뜨려도 두 트랙이 서로의 데이터를 지우거나 섞을 통로가 생기지만, 물리적으로 다른 테이블·다른 배치 스크립트·다른 워크플로우 파일로 나누면 그 통로 자체가 존재하지 않습니다. **그래서**: KRW용 표(계좌·주문·포지션·현금원장·스냅샷·발행표·발행보유종목)와 물리적으로 **다른** USD 전용 표 한 벌, 배치 스크립트도 `run_duel_daily_batch.py`/`run_duel_publish_batch.py`와 완전히 별도인 USD 전용 스크립트 한 벌, 워크플로우도 별도 `.yml` 파일 한 벌을 새로 만듭니다(정확한 파일명·테이블명은 실제 구현 착수 시 정함). 단, **순수 계산 규칙**(TWR 공식, 체급 배정 로직 등 통화와 무관하게 규칙 자체는 동일한 부분)은 공통 함수로 뽑아 양쪽 배치가 같이 부르게 해서, "규칙이 바뀌었는데 한쪽만 고쳤다"는 실수를 막습니다. 즉 **데이터는 분리, 순수 규칙은 공유**.
2. **계좌 구조 = A안 확정.** 창유형(M1/M3/M6)마다 KRW 계좌 + USD 계좌를 **각각 별도 계좌**로 두어 총 6개 계좌 — 듀얼 통화(계좌 하나가 두 통화를 동시에 갖는 방식)는 **하지 않습니다.** 화면에서는 "내 성적표"가 이미 하는 방식 그대로, 같은 창유형 카드 안에 **위쪽엔 KRW 계좌 블록, 아래쪽엔 USD 계좌 블록**을 나란히 보여주되, 두 블록을 합산한 숫자(예: "총 자산")는 **절대 화면에 만들지 않습니다** — 환율과 시드머니를 섞지 않는다는 원칙을 화면 레이어에서도 그대로 지킵니다.
3. **시드머니 = $7,500** (KRW 트랙 1천만원의 비례값, 오너 확정).
4. **월 정기입금 = $500, 같은 날짜(매월 10일).** KRW 트랙과 마찬가지로 **창 길이에 비례해 깎지 않고** M6도 M1과 똑같이 매달 $500이 들어가며, 이 입금액이 계좌 현금 잔고에 **누적**됩니다 — 이미 KRW 트랙에 있는 로직(2-2, `duel_rules.py`의 "매월 정기 입금액... 창 길이에 비례해 깎지 않습니다" 주석 및 TWR 계산에서 입금액을 투자 성과로 착각하지 않도록 하는 `r_t = (V_t − F_t) / V_(t−1) − 1` 보정식)을 **그대로 재사용**하고, 시드·입금 상수만 USD용으로 새로 둡니다(예: `SEED_AMOUNT_USD = 7500`, `DEPOSIT_AMOUNT_USD = 500`). 새로 발명하는 로직이 아니라 이미 검증된 로직에 숫자만 바꿔 넣는 것이라 리스크가 낮습니다.
5. **유니버스 = 뉴욕+나스닥 합쳐 약 500개.** 이미 있는 `collector_us_stocks.py`(2026-08-08부터 공개 중인 "미국주식" 화면이 쓰는 수집기, 시가총액 상위 약 550개, 채권/노트류·미국식 우선주 제외)의 유니버스가 이 목표치와 거의 일치합니다 — 실제 구현 시 정확한 개수·필터 조건이 이 트랙의 요구와 완전히 맞는지 재확인이 필요하지만, 새 수집 파이프라인을 처음부터 만들 필요는 없어 보입니다(`data/us_stocks_history.csv`가 결투 배치가 필요로 하는 것과 같은 형태의 종목별 일별 종가 이력 파일이라 그대로 활용 가능해 보입니다).
6. **주문 접수 시간대 = D일 KST 16:00~21:00, 체결 = D+1 미국 정규장 마감가.** 실제로 ET로 환산해 검증했습니다 — 이 시간대는 ET 기준 03:00~08:00(서머타임)/02:00~07:00(표준시)로, 그날 미국 정규장 개장(09:30 ET)보다 한참 전입니다. 즉 국내주식 모델("오늘 장이 이미 끝난 뒤 받아서 다음날 마감가로 체결")과는 메커니즘이 다르지만("오늘 장이 열리기도 전에 받아서 그 열릴 장의 마감가로 체결"), 결과는 똑같이 안전합니다 — 주문 시점에 그 마감가가 존재하지 않으므로 선행매매가 구조적으로 불가능합니다. "D+1 마감가"라는 표현도 그대로 맞아떨어집니다: KST D일 저녁에 주문을 받으면, 그 직후 열리는 미국 정규장의 마감 시각이 KST로는 **D+1일 새벽 05:00(서머타임)~06:00(표준시)**이기 때문입니다.
   - **배치 시각(제안, 아직 정식 확정 아님)**: 미국 종가 수집(`scrape_us.yml`)이 KST 05:35(서머타임)/06:35(표준시)에 끝나므로, 그 뒤 여유를 두고 "결투 미국 체결 배치"는 KST 07:00~07:30, "결투 미국 발행 배치"는 그 뒤 KST 07:40~08:00 근처로 잡는 안을 제안합니다. 국내 트랙(저녁에 도는 배치)과 반대로 **새벽에 도는 배치**가 되는 구조입니다. 정확한 cron 값은 실제 구현 착수 시 확정합니다.
   - 미국 증시 자체의 휴장일(추수감사절 등)은 `collector_us_stocks.py`가 휴장일 달력을 하드코딩하지 않고 "이미 그 거래일을 수집했는가"만으로 자연 판별하는 방어 로직(`evaluate_collection_readiness()`)을 이미 갖고 있는데, 결투용 체결 배치에도 같은 방어가 필요한지는 실제 구현 시 재확인이 필요합니다.
7. **체급 구간(8단계, USD) = 확정.** KRW 8단계 경계값(§5-3)이 전부 시드머니(1천만원)의 정확한 배수(0 / 0.1 / 0.3 / 0.5 / 1.0 / 3.0 / 6.0 / 10.0배)로 끊여 있다는 걸 확인하고, 같은 비율을 시드머니 $7,500에 그대로 적용해 아래로 확정했습니다(지어낸 값이 아니라 실제 KRW 경계값의 기계적 비율 환산, 오너 확인 완료):
   - `$750 미만`
   - `$750 이상 ~ $2,250 미만`
   - `$2,250 이상 ~ $3,750 미만`
   - `$3,750 이상 ~ $7,500 미만` (시드머니 원금 지점 포함)
   - `$7,500 이상 ~ $22,500 미만`
   - `$22,500 이상 ~ $45,000 미만`
   - `$45,000 이상 ~ $75,000 미만`
   - `$75,000 이상`
8. **시즌 = KRW 트랙과 동일 공유.** 3월 1일 기준, 12개월. USD 트랙만의 별도 시즌 로직은 만들지 않습니다.
9. **순위표 = 한국장/미국장 완전 별개 표, 절대 병합·비교 안 함.** 이유가 두 가지입니다. (a) 통화 혼합 금지 원칙과의 일관성 — 매수/매도/TWR에서 환율을 안 쓰기로 했으면 그 위의 순위·체급도 통화별로 완전히 분리돼야 앞뒤가 맞습니다. (b) **배치 시각·휴장일 캘린더가 애초에 다릅니다** — 국내 배치는 KST 저녁, 미국 배치는 KST 새벽에 돌고, 한국 휴장일(설·추석)과 미국 휴장일(추수감사절 등)이 서로 안 겹치므로, 표를 하나로 합쳐두면 "한쪽만 갱신되고 한쪽은 어제 그대로"인 상태가 상시로 생겨 사용자에게 혼란을 줍니다. 표 자체를 나누면 "이 표는 이 표대로의 주기로 갱신된다"가 당연해져서 문제가 사라집니다.

**✅ 2026-08-20 추가 확정 — 닉네임/동의 정체성:**

10. **닉네임 = 사용자 단위로 공유(계좌·트랙 단위 아님).** 같은 사용자가 KR 트랙과 US 트랙에 둘 다 공개 동의하면 **같은 닉네임 문자열**을 씁니다. 순위표·발행표는 위 9번대로 완전히 분리하지만(트랙 간 데이터가 물리적으로 안 섞임), 닉네임 자체는 "완전 분리" 원칙의 예외입니다 — 6번의 "순수 계산 규칙은 공유"와 같은 맥락으로, **닉네임 배정은 공통 자원**이라 두 트랙이 각자 중복으로 새로 만들지 않고 하나만 씁니다. 구현 함의: 기존 `duel_nicknames` 표(현재는 계좌 단위로 붙어 있을 가능성)를 **사용자(user_id) 단위**로 두고, `ensure_nickname()`이 호출될 때 "이 사용자가 어느 트랙에서든 이미 닉네임을 받은 적 있는가"부터 확인해서, 있으면 그걸 재사용하고 없으면 그때 새로 발급합니다. USD 트랙용으로 별도 `duel_nicknames_usd` 표를 새로 만들지 않습니다 — 이 표만큼은 KRW/USD 공통입니다.
    - **✅ 2026-08-20 오너 확인 완료 — 동의는 트랙별 완전 독립.** 오너가 원래 "둘 다 공개 동의"라고 하신 건 "나누는 걸 구현하기 어려울 것 같아서"였는데, 실제로는 어렵지 않습니다(위 1번의 "완전 분리" 아키텍처를 이미 그렇게 하기로 한 이상, 동의 표도 KR용·US용이 물리적으로 다른 표가 되는 게 오히려 자연스러운 기본 형태이지, 따로 합치는 작업이 추가로 필요한 게 아닙니다). 그래서 최종 확정: **두 트랙 각각 지금처럼 독립적인 5개 체크박스 동의(§5-2)를 완전히 따로 갖습니다** — KR만 동의하고 US는 동의 안 해도 되고, 그 반대도 가능하며, 한쪽을 철회해도 다른 쪽엔 영향이 없습니다. 공유되는 건 오직 10번의 "동의했을 때 붙는 닉네임 문자열"뿐이고, 동의 여부·항목별 공개 범위·철회는 트랙마다 완전히 별개입니다.

**🆕 아직 안 정해진 것 (다음에 정할 것, 코딩을 막지는 않지만 착수 전 확인 권장):**

- 정확한 테이블명·파일명 체계, 정확한 cron 시각 — 위에 제안값은 있으나 실제 구현 착수 시 확정.
- `collector_us_stocks.py` 유니버스가 이 트랙이 요구하는 "뉴욕+나스닥 500개" 조건과 정확히 일치하는지 재확인.

**⚠️ 위 결정들은 5-12부터 실제 코드로 옮기기 시작했습니다** — 아래 5-12 는 그 1차 라운드(스키마)이고, 배치·화면·테스트는 아직 남아 있습니다.

### 5-12. 🆕 USD 트랙 1차 코딩 — 스키마(`sql/duel_schema.sql`) 완료 (2026-08-20, 8번째 코딩 작업)

**오너 지시**: "코딩부터 확실하게 끝내는걸로." 원화 트랙이 1단계(스키마 승인)→2단계(코딩)를 7차례 라운드로 나눠 진행했던 것과 같은 방식으로, USD 트랙도 스키마부터 시작해 여러 라운드로 나눠 진행합니다. **이번 라운드는 스키마만입니다.**

**무엇을 만들었는지 (`sql/duel_schema.sql`, 1533줄 → 2339줄):**

- **§6 `duel_nicknames` 재구조화(머리말 (m)) — 기본키를 `account_id`(계좌 단위) → `(user_id, window_type)`(사용자×창유형 단위)로 직접 수정.** 5-11-10 확정("같은 사용자면 같은 닉네임")을 구현하려면 원화 계좌(`duel_accounts`)와 달러 계좌(`duel_accounts_usd`)가 물리적으로 다른 표의 서로 다른 id 라 계좌 단위 기본키로는 공유를 표현할 수 없었습니다. §9-6 RLS 정책도 `duel_account_is_mine()` 간접 호출 대신 `auth.uid() = user_id` 직접 비교로 함께 바꿨습니다. **창유형을 넘어선 공유(M1과 M3가 같은 닉네임)는 여전히 안 합니다** — 순위표 자체가 창유형별로 나뉘어 있어 그 경계를 존중합니다.
- **§12 마이그레이션 브리지** — 위 재구조화는 원화 표의 기존 정의 자체를 바꾼 것이라, "이 파일의 예전 버전을 이미 실행해 계좌 단위 `duel_nicknames`가 실제로 만들어져 있는 프로젝트"를 위한 안전장치가 따로 필요했습니다. `information_schema`로 `account_id` 컬럼 존재 여부를 스스로 판단해서, **새로 설치하는 프로젝트에서는 이 블록이 통째로 아무 일도 하지 않습니다**(§6이 처음부터 최종 구조를 만들기 때문). 예전 구조가 있으면 정책을 먼저 지우고(컬럼 삭제보다 반드시 먼저 — 안 그러면 "다른 객체가 의존한다"는 오류) `duel_accounts`에서 역참조해 `user_id`/`window_type`을 백필한 뒤 컬럼을 지웁니다.
- **§13 USD 트랙 전용 표 10개** — `duel_accounts_usd`(시드 $7,500, `currency='USD'` 고정) / `duel_positions_usd` / `duel_orders_usd`(D일 KST 16:00~21:00 접수 → D+1 미국 정규장 마감가) / `duel_cash_ledger_usd`(월 $500 정기입금, append-only) / `duel_daily_snapshots_usd` / `duel_holding_snapshots_usd` / `duel_public_consent_usd`(원화 표와 **완전 독립** — 5-11-10 확정) / `duel_bracket_assignments_usd`(체급 8단계, 경계값은 앱 상수가 단일 출처) / `duel_public_leaderboard_usd` / `duel_public_holdings_usd`(원화 발행표와 물리적으로 완전히 분리 — 5-11-9). 전부 원화 표(§1~8-3)와 컬럼·제약이 동일하고 표 이름에 `_usd`만 붙습니다.
- **트리거 함수는 5개 전부 원화와 공유(복제 안 함)** — `duel_set_updated_at` · `duel_positions_buy_only`(매도 금지) · `duel_orders_guard`(상태 전이 가드) · `duel_cash_ledger_append_only` · `duel_consent_guard`(철회 되돌리기 금지)는 전부 NEW/OLD로만 동작하고 표 이름을 하드코딩하지 않아서, 새 트리거로 그대로 연결했습니다(5-11-1의 "순수 규칙은 공유" — 규칙을 고쳤는데 한쪽만 고치는 사고를 원천 차단). 표 이름이 본문에 박혀 있어 복제가 불가피했던 건 소유자 판정 함수(`duel_account_is_mine_usd`) 하나뿐입니다.
- **§14 RLS + 권한** — §9와 정확히 같은 원칙을 10개 새 표에 그대로 적용(발행표 2개만 로그인 사용자 전체 select + service_role만 CRUD 포함 delete, 나머지는 표별로 §9와 동일한 select/insert/update 조합). anon 권한도 전부 회수.
- **§14-10 옵트인 RPC `public.duel_opt_in_usd()`** — §9-10과 완전히 같은 안전성 근거(인자 없음, 대상은 `auth.uid()`뿐, 금액은 `duel_seed_amount_usd()` 상수 함수, 멱등). **닉네임은 여기서 만들지 않습니다** — 5-5/5-11-10 확정 순서 그대로 5단계 동의 시점에 생성됩니다.

**검증(실제 로컬 PostgreSQL 16.13에 직접 실행 — 문서 검토만이 아니라 진짜 DB로):**

1. 신규 설치 시나리오 — 전체 파일을 처음부터 실행 → 클린. **같은 파일을 다시 한번 그대로 재실행**(오너가 SQL Editor에서 실수로 두 번 누르는 상황을 그대로 재현) → 역시 클린(멱등성 확인). `duel_nicknames`가 새로 설치 시 처음부터 `(user_id, window_type)` 구조로 만들어지는지 컬럼 목록으로 확인.
2. **예전 버전(계좌 단위 `duel_nicknames`)이 이미 설치된 프로젝트 시나리오** — 실제로 계좌 단위 구조를 만들고 실 데이터(계좌 1개 + 닉네임 1개)를 넣은 뒤 §12 브리지를 적용 → 데이터 유실 없이 `(user_id, window_type)` 구조로 정확히 마이그레이션되는 것을 눈으로 확인(기존 닉네임 문자열이 그대로 살아 있고, `user_id`/`window_type`이 실제 계좌 정보로 정확히 채워짐).
3. 표 21개(원화 11 + USD 10) 전부 RLS 켜짐 확인. 발행표 2개(USD)에 `user_id`/`account_id`/`email` 컬럼이 새어 들어가지 않았는지 확인(0행).
4. **역할별 실동작 시나리오** — `anon`으로 `duel_opt_in_usd()` 호출 → 권한 거부. 사용자 A로 호출 → USD 계좌 3개 + 시드 원장 3행 생성, **3번 연달아 호출해도 계좌·시드 각 3개로 고정**(멱등). 사용자 B가 옵트인해도 A의 USD 계좌가 안 보임(교차 격리). 같은 사용자 A가 원화 옵트인도 실행한 뒤 `duel_nicknames`에 `(user_id=A, window_type='M1')`로 닉네임을 넣어보고, 이 한 행이 원화·달러 양쪽 M1 계좌 어디서 조회해도 같은 닉네임으로 해석될 수 있는 구조인지 확인. `duel_cash_ledger_usd` update 시도 → 권한 거부(append-only). `duel_positions_usd` 수량 감소 시도 → 트리거가 거부, `set local duel.allow_quantity_decrease='on'` 관리 경로는 통과. `duel_bracket_assignments_usd` update 시도 → 권한 거부(시즌 고정 강제). `duel_public_leaderboard_usd`는 service_role이 insert 후 delete까지 가능(전량 재작성·철회 삭제에 필요).

**아직 안 된 것(다음 라운드)**: `utils/duel_rules.py`(SEED_AMOUNT_USD=7500·DEPOSIT_AMOUNT_USD=500·BRACKET_TIERS_USD 8단계·주문 접수 시간대 16:00~21:00 등 상수), `utils/duel_db.py`(닉네임 함수를 새 (user_id, window_type) 구조에 맞게 수정 — 이건 원화 트랙의 기존 코드도 함께 고쳐야 하는 부분입니다) + 신규 `utils/duel_db_usd.py`, 배치 오케스트레이션(`utils/duel_batch_usd.py`·`run_duel_daily_batch_us.py`·워크플로우), 발행 배치, 화면, 테스트.

**✅ 2026-08-20, 오너가 위 스키마를 실제 프로덕션 Supabase 프로젝트의 SQL Editor에 붙여넣고 직접 실행 완료.** "Success. No rows returned" 확인 + 검증 질의 2건 모두 정상(`select count(*) from pg_class where relname like 'duel\_%'` → 21, `select public.duel_seed_amount_usd()` → 7500) + 쿼리를 "duel_schema"라는 이름으로 저장까지 마쳤습니다. 이 스키마는 이제 문서상의 계획이 아니라 **실제로 돌아가는 프로덕션 구조**입니다.

---

### 5-13. 🆕 USD 트랙 2차 코딩 — `utils/duel_rules.py` USD 상수 + 닉네임 스키마 변경의 파급 수정 (2026-08-20, 9번째 코딩 작업)

**배경**: §5-12에서 `duel_nicknames`의 기본키를 `account_id` → `(user_id, window_type)`로 바꾼 것은 스키마만의 변경이 아니었습니다 — **원화 트랙의 기존 파이썬 코드가 이미 옛 구조(`account_id` 컬럼)를 전제로 짜여 있었기 때문에, 스키마가 바뀐 순간 그 코드들이 프로덕션에서 실제로 깨지는 상태**가 됐습니다. 이번 라운드는 그 파급을 원인부터 고치는 라운드입니다 — 스키마를 되돌리지 않고(§5-11-10 확정을 유지) 코드 쪽을 새 구조에 맞춥니다.

**무엇을 고쳤는지 (`utils/duel_db.py`):**

- **`ensure_nickname(client, account_id)` → `ensure_nickname(client, user_id, window_type)`.** 닉네임이 이제 계좌가 아니라 (사용자, 창유형) 단위로 존재하므로, "이 계좌의 닉네임을 만들어줘"가 아니라 "이 사용자의 이 창유형 닉네임을 만들어줘"로 질문 자체가 바뀌었습니다. 멱등성(있으면 그대로 반환)·재시도(unique 충돌 시 새 후보)·경합 처리(다른 탭이 먼저 만들면 그 값을 그대로 씀) 로직은 전부 그대로입니다 — 바뀐 건 "무엇을 키로 조회하는가"뿐입니다.
- **`fetch_my_nickname(client, account_id)` → `fetch_my_nickname(client, user_id, window_type)`.** 같은 이유.
- **`_fetch_nickname_row(client, account_id)` → `_fetch_nickname_row(client, user_id, window_type)`.** 위 두 함수가 공유하는 내부 조회 헬퍼.
- **`fetch_nicknames_for_accounts(service_client, account_ids)` → `fetch_nicknames_for_accounts(service_client, accounts)`.** 배치 전용 일괄 조회 함수라 가장 손을 많이 댄 곳입니다. 예전에는 "계좌 id 목록"만 있으면 됐는데, 이제는 각 계좌의 `user_id`/`window_type`을 알아야 어떤 닉네임과 짝인지 알 수 있습니다. 그래서 인자를 **계좌 id 문자열 목록이 아니라 계좌 행(딕셔너리) 목록**으로 바꿨습니다 — 호출부가 이미 `fetch_all_active_accounts()` 등으로 읽어 둔 계좌 행을 그대로 넘기면 됩니다. 내부적으로는 `user_id`로 청크 단위 조회한 뒤 `(user_id, window_type) → nickname` 대응표를 만들고, 그걸로 각 계좌 행의 `id`에 닉네임을 매핑해 돌려줍니다(반환 모양 `{account_id: nickname}` 자체는 호출부 호환을 위해 그대로 유지).

**무엇을 고쳤는지 (`web/pages/duel_consent_page.py`):**

동의 관리 화면이 저장 성공 뒤 `ensure_nickname()`을 부르는 경로 전체(`_render_consent_form` → `_render_real_principal_form` → `_save`)가 그동안 계좌 id만 들고 다녔는데, 이제 `user_id`/`window_type`도 함께 필요해져서 **계좌 id 문자열 대신 계좌 행 전체(dict)를 들고 다니도록** 함수 시그니처를 바꿨습니다. `save_consent()` 호출(여전히 계좌 id 하나만 필요) 자리에서는 `account.get("id")`로 꺼내 쓰고, `ensure_nickname()` 호출 자리에서는 `account.get("user_id")`/`account.get("window_type")`을 함께 넘깁니다.

**무엇을 고쳤는지 (`utils/duel_publish.py`):**

`fetch_nicknames_for_accounts()`의 새 시그니처에 맞춰 발행 배치의 두 호출 지점(철회 계좌 정리, 발행 대상 조립)을 계좌 행을 넘기도록 고쳤습니다. 이 과정에서 **호출 순서도 함께 바꿨습니다** — 원래는 "철회 청소 → 계좌 전체 조회" 순서였는데, 철회 청소 단계에서도 이제 계좌 행(user_id 포함)이 있어야 닉네임을 찾을 수 있으므로 **계좌 전체 조회를 배치의 가장 앞으로 옮겼습니다.** ("철회 청소가 다른 무엇보다 먼저"라는 §5-8-1 원칙은 그대로입니다 — 계좌 조회는 철회 대상을 고르는 것과 무관한, 순수하게 "닉네임을 찾기 위한 참조표 준비"일 뿐입니다.)

**무엇을 새로 만들었는지 (`utils/duel_rules.py` §11 "USD 트랙"):**

- `SEED_AMOUNT_USD = 7500`, `MONTHLY_DEPOSIT_USD = 500` — 스키마의 `duel_seed_amount_usd()`·`duel_cash_ledger_usd` 코멘트와 값이 반드시 같아야 하는 단일 출처(§0-3-10). 정기 입금일(`MONTHLY_DEPOSIT_DAY`, 매월 10일)은 통화와 무관한 달력 개념이라 원화와 **공유**하고 USD 전용 상수를 새로 만들지 않았습니다.
- **✅ `ORDER_WINDOW_OPEN_TIME_USD = time(16, 0, 1)` / `ORDER_WINDOW_CLOSE_TIME_USD = time(21, 0, 0)` — 2026-08-20 오너 최종 확정.** 원화 트랙(정각에서 1초 뒤, 18:00:01)과 **같은 관례**로 16:00:01에 여는 것으로 확정됐습니다. (중간에 오너가 "정각(16:00:00)이 낫겠다"고 한 번 바꿨다가, 바로 이어서 "01초로 가자"고 다시 :01로 확정 — 코드·테스트·문서 전부 최종값 :01 로 맞춰져 있습니다.) 관련 테스트(`tests/test_duel.py`)를 KRW 와 같은 경계 검증 방식으로 되돌리고, 두 트랙이 같은 1초 오프셋 관례를 쓰는지 회귀 고정하는 테스트(`test_order_window_usd_and_krw_share_the_same_one_second_offset_convention`)를 추가해 두었습니다.
- `resolve_order_window_usd(now_kst)` — `resolve_order_window()`와 판정 로직이 완전히 동일하고 상수만 다른 함수입니다. 함수를 공유(상수를 인자로 받게 시그니처 변경)하지 않고 **의도적으로 복제**했습니다 — 원화 쪽 기존 호출부·테스트를 전부 건드리지 않기 위해서입니다. 대신 "둘 중 하나를 고치면 반드시 다른 쪽도 고치라"는 메모를 양쪽 docstring에 남겨 뒀습니다.
- `BRACKET_TIERS_USD` 8단계 — 오너가 이전 대화에서 확정한 경계값 $750 / $2,250 / $3,750 / $7,500 / $22,500 / $45,000 / $75,000. 우연이 아니라 **원화 8구간과 정확히 같은 배율**(기준 단위의 100/60/30/10/5/3/1배)입니다 — 이 사실을 `test_bracket_tiers_krw_and_usd_use_the_same_ratio` 테스트로 회귀 고정해 뒀습니다.
- `BRACKET_KEYS_USD`, `BRACKET_LABELS_USD`, `assign_bracket_usd()`, `bracket_label_usd()` — 원화 쪽과 완전히 같은 모양의 미러 함수. **"구간 미적용"(`BRACKET_NONE_KEY`/`BRACKET_NONE_LABEL`)은 새로 만들지 않고 원화와 공유**합니다 — "구간을 못 정하는 이유"(동의 안 함/보유 없음/통화 혼재)는 통화와 무관한 개념이기 때문입니다.

**검증**: `python3 -m py_compile`로 수정된 5개 파일(`utils/duel_db.py`, `utils/duel_publish.py`, `utils/duel_rules.py`, `web/pages/duel_consent_page.py` + 테스트 4개 파일) 전부 문법 확인 완료. 기존 회귀 테스트(`ensure_nickname` 5개, 소스 검사 2개, `fetch_my_nickname` 스텁 1개, 발행 배치 픽스처)를 새 시그니처에 맞게 고치고, USD 전용 신규 테스트(체급 8단계 경계값·구멍/겹침 없음·미지의 값 거절·"구간 미적용" 공유·KRW와 같은 배율·주문 접수 시간대 경계 5종·SQL 시드 상수 대조)를 추가해 `tests/test_duel.py`·`tests/test_duel_db.py`·`tests/test_duel_publish.py`·`tests/test_duel_public_ui.py` 전체를 실행 → **432개 전부 통과**(회귀 0건). (주문 접수 시간대 초 단위를 16:00:01→16:00:00으로 오너가 확정한 뒤 테스트를 새 경계에 맞게 갱신하고 회귀 테스트 1개를 추가해 재검증 완료.)

**아직 안 된 것(다음 라운드)**: 신규 `utils/duel_db_usd.py`(USD 표를 바라보는 DB 접근 계층 — `_usd` 표 CRUD·`fetch_nicknames_for_accounts`가 KRW/USD 계좌를 함께 받을 수 있는지 재검토), 배치 오케스트레이션(`utils/duel_batch_usd.py`·`run_duel_daily_batch_us.py`·워크플로우), 발행 배치의 USD 지원, 화면("내 성적표" 스타일의 KRW/USD 계좌 병기 또는 USD 전용 화면), 위 각각의 테스트.

---

### 5-14. 🆕 USD 트랙 3차 코딩 — 신규 `utils/duel_db_usd.py`(USD 표 DB 접근 계층) + 전용 테스트 (2026-08-20, 10번째 코딩 작업)

**배경**: §5-13에서 예고한 "다음 코딩 라운드"입니다. `utils/duel_db.py`(원화)의 전체 2,654줄을 처음부터 끝까지 다시 읽고, `sql/duel_schema.sql` §13(USD 표 10개)·§14-10(`duel_opt_in_usd()` RPC)을 대조하며 **1:1 미러 파일**을 새로 작성했습니다.

**설계 원칙 — "완전 분리, 그러나 순수 로직은 공유"**: 스키마(§13-14)가 이미 세운 원칙을 코드에도 그대로 옮겼습니다. **표 이름이 함수 본문에 박혀 있는 함수만 복제**하고, 표 이름을 몰라도 되는 순수 로직·순수 인프라는 **재사용**합니다.

- **`utils.duel_db`에서 그대로 import 해서 씁니다(재구현 안 함)**: 공통 검증·변환 헬퍼(`_execute`·`_require_client`·`_iso_date`·`_now_kst`·`_require_text`·`_require_positive_int`·`_require_offset`·`_require_amount`·`_first_row`·`_is_duplicate_key_error`·`_assert_unique_keys`·`_filter_is_null`·`_filter_not_null`·`_assert_no_identity_fields`·`_validate_fill_payload`·`_validate_daily_snapshot`·`_validate_holding_snapshot`), 순수 집계 함수(`group_rows_by_account`·`sum_cash_balance`·`cash_balances_by_account`), 서비스 클라이언트 생성(`create_service_client`·`service_config_present`) — **원화·달러가 같은 Supabase 프로젝트, 같은 service_role 키를 씁니다**(두 번째 클라이언트 생성 경로를 만들지 않았습니다). 그리고 무엇보다 **닉네임 3종**(`ensure_nickname`·`fetch_my_nickname`·`fetch_nicknames_for_accounts`) — 5-11-10 확정대로 `duel_nicknames`는 처음부터 원화·달러가 공유하는 표이고, USD 전용 닉네임 함수를 만드는 것 자체가 "닉네임이 계좌별로 갈라진다"는 잘못된 모델을 코드에 새기는 일이라 **만들지 않았습니다**. `fetch_real_principal_holdings`("내 성적표" 실보유 조회)도 통화 컬럼을 이미 갖고 있는 트랙 무관 함수라 그대로 재사용합니다.
- **새로 정의합니다(표 이름 또는 통화별 규칙이 본문에 박힌 것들)**: §0의 `_usd` 표 이름 상수 10개 + RPC 이름(`duel_opt_in_usd`), A 절 전체(`opt_in_usd`·`save_order_usd`·`edit_order_usd`·`cancel_order_usd`·조회 5종·`save_consent_usd`·`fetch_my_consent_usd`·`revoke_consent_usd`·순위표 읽기 3종), B 절 전체(배치 전용 CRUD — 활성계좌·원장·포지션·스냅샷 일괄조회, 옵트인 백필, 정기입금, 체결 기록, 발행표 쓰기/지우기 등 약 30개 함수).
- **주문 접수 시간대**는 `duel_rules.resolve_order_window_usd()`(16:00:01~21:00:00, §5-13에서 오너가 최종 확정한 값)를 씁니다 — `resolve_order_window()`(원화, 18:00:01~22:00:00)를 실수로 쓰면 시간대가 두 시간 어긋납니다.
- **`_translate_order_guard_error_usd()`** — 이 파일에서 "재사용하지 않기로" 한 유일한 예외 케이스입니다. 원화용 `_translate_order_guard_error()`는 트리거 자체는 원화·달러가 공유하지만, **오류 문장 안에 "18:00~22:00"이 하드코딩**돼 있어 그대로 재사용하면 USD 사용자에게 원화 시간대를 보여주는 사고가 납니다. 그래서 이 함수만 새로 정의해 "16:00~21:00"을 담습니다(트리거는 공유, 사람이 읽는 번역 문구만 트랙별로 분리).

**검증**:
1. `python3 -m py_compile utils/duel_db_usd.py`(및 관련 파일 전부) — 클린.
2. import 및 정체성 점검 — `duel_db_usd.fetch_nicknames_for_accounts is duel_db.fetch_nicknames_for_accounts` 등 공유 함수 9종이 실제로 **같은 객체**인지(재정의가 아닌지) 직접 대조, `user_write_signature_violations_usd()`(빈 목록 = 통과), 트리거 오류 번역문에 "16:00~21:00"만 있고 "18:00~22:00"이 새어 들어오지 않는지 — 전부 통과.
3. **신규 `tests/test_duel_db_usd.py`(133개, `tests/test_duel_db.py`의 `FakeClient`/`sequence`를 재사용 — 가짜 클라이언트도 단일 출처로, `tests/test_duel_publish.py`·`tests/test_duel_batch.py`와 같은 관례) 작성**. 이 파일이 특히 확인하는 것: ① 머리말이 선언한 "재사용 vs 신규 정의" 경계가 실제 코드와 일치하는지(공유 함수 `is` 동일성 + AST 로 재정의 여부 검사), ② 접수 시간대가 정확히 16:00:01~21:00:00 경계로 판정되는지, ③ 트리거 거절 번역문이 KRW 문구를 새지 않는지, ④ 표/RPC 이름이 KRW 것과 겹치지 않고 `_usd` 접미사가 실제로 붙어 있는지, ⑤ 시드($7,500)·정기입금($500) 금액과 §0-3-2(집합 연산) 회귀가 USD 배치 함수에도 적용되는지, ⑥ 사용자 쓰기 함수 자가 점검, ⑦ KRW 표에는 요청이 전혀 가지 않는지(트랙 격리), ⑧ None 클라이언트 에러 처리, ⑨ 공개 함수 전부 docstring 보유.
4. **전체 결투 스위트 재실행**: `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_db_usd.py tests/test_duel_batch.py tests/test_duel_publish.py tests/test_duel_public_ui.py` → **565개 전부 통과**(기존 432 + 신규 133, 회귀 0). 저장소 전체 `py_compile`도 클린.
   - ⚠️ 이 세션에는 저장소 전체가 다 올라와 있지 않아(`web/auth.py`·`web/state.py` 미존재), `python3 -m pytest`(전체 스위트)를 돌리면 그 두 모듈이 없어서 나는 실패 18건이 함께 뜹니다 — 전부 이번 작업과 무관한 **기존 파일 부재**이고(`duel_db_usd.py`를 전혀 참조하지 않는 실패들), 결투 스위트만 따로 돌리면 문제 없이 전부 통과합니다. 저장소를 다시 열면 그 두 파일이 있으니 재발하지 않을 실패입니다.

**아직 안 된 것(다음 라운드)**: 배치 오케스트레이션(`utils/duel_batch_usd.py`·`run_duel_daily_batch_us.py`·워크플로우 파일), 발행 배치의 USD 지원(`utils/duel_publish.py` 확장 또는 신규 `utils/duel_publish_usd.py`), 화면(`duel_page.py` 확장 또는 USD 전용 화면), 위 각각의 테스트. KRW v1의 6단계(실검증)·7단계(공개 전환)도 여전히 미착수입니다.

---

### 5-15. 🆕 USD 트랙 4차 코딩 — 야간 배치 오케스트레이션(`utils/duel_batch_usd.py`·`run_duel_daily_batch_us.py`·워크플로우) + 전용 테스트 (2026-08-20, 11번째 코딩 작업)

**배경**: §5-14에서 예고한 "다음 코딩 라운드"입니다. `utils/duel_batch.py`(원화, 1,217줄)·`run_duel_daily_batch.py`(222줄)·`.github/workflows/duel_daily.yml`(147줄)을 전부 처음부터 끝까지 다시 읽고, 이번엔 **이 세션의 저장소 미러에 없는 파일들**(`data/` 전체, `collector_us_stocks.py`, `.github/workflows/*.yml` 여러 개)을 오너 컴퓨터에서 `device_bash`로 직접 열어서(§0-1 "지어내지 않는다" 규율 — 실제 파일과 대조 없이 타이밍을 추정하지 않았습니다) 확인하며 작성했습니다.

**재사용 vs 신규 정의 — 이번에도 같은 원칙**: `select_probe_stocks`·`build_freshness_probe`·`load_probe_state`·`save_probe_state`·`judge_crawl_freshness`·`resolve_action`·`plan_order_fills`·`last_snapshot_dates`·`collect_external_cash_flows`·`build_snapshot_rows`·`compute_twr_by_account`·`is_monthly_deposit_date`·`default_state_dir`·`_to_date`·`_positive_price`·`_round6` — 이 14개는 표 이름도 통화 리터럴도 안 담고 있어서 그대로 import 해서 씁니다. `utils/stock_history.py`의 `KOSPI_HISTORY_FIELDS`·`US_HISTORY_FIELDS`가 둘 다 순위 필드 이름으로 `"rank"`("시가총액 순위")를 쓴다는 것까지 직접 확인해서, `select_probe_stocks()`가 시장 무관하게 안전히 재사용되는 걸 근거로 확정했습니다.

- **새로 정의한 것 1 — `run_nightly_batch_usd()`(오케스트레이터 본체)**: `duel_db_usd.*` 호출로 교체한 것 외에, 원화에는 없는 **날짜 파라미터 2개**(`target_date`, `today_kst`)로 분리한 것이 이번 라운드의 핵심 설계 변경입니다 — 아래 🔴 항목에 상세 설명.
- **새로 정의한 것 2 — `format_summary_lines_usd()`**: 코드 리뷰(원화 `duel_batch.py`에서 "원" 리터럴 grep) 중 원화 함수의 실행 라인 하나(`"...체결에 쓴 현금 합계: {...}원"`)에 원화 기호가 그대로 박혀 있는 걸 발견 — 그대로 재사용했다면 달러 금액에 "원"이 찍히는 사고가 났을 것입니다. 그래서 이 함수는 통째로 새로 정의해 `"${...:,.2f}"`로 고쳤습니다(`_translate_order_guard_error_usd()`에 이은, 이 트랙의 두 번째 "공유 로직이지만 통화 리터럴 때문에 신규 정의" 사례).

**🔴 이번 라운드에서 제가 직접 도출한 설계 결정 — 오너 확인 필요**:

USD 트랙의 야간 배치가 처리할 "확정하려는 거래일"의 기본값을 **"어제"**로 정했습니다(원화 배치는 항상 "오늘"입니다). 이유: 미국 정규장은 한국 시각으로 다음 날 새벽에 마감하고, 그 확정 종가를 모으는 실제 수집 워크플로우(`.github/workflows/scrape_us.yml` — 2026-08-20 `device_bash`로 오너 컴퓨터의 실제 파일을 직접 읽어 확인)는 서머타임엔 완료가 06:30~07:05 KST, 표준시엔 07:30~08:05 KST(둘 다 **다음 날** 새벽)입니다. 즉 "미국 거래일 X"의 확정 종가는 한국 날짜로 X+1일 아침에야 저장소에 들어오므로, 이 배치가 한국 날짜 Y에 돌 때 처리할 수 있는 것은 언제나 Y의 하루 전(X = Y-1) 거래일뿐입니다. 이 사실을 근거로 `.github/workflows/duel_daily_us.yml`의 cron을 **매일 03:00 UTC(= KST 정오 12:00)**로 잡았습니다 — 최악의 수집 완료 시각(08:05 KST)에서 거의 4시간 여유, 다음 주문 접수 시작(16:00:01 KST, 같은 한국 날짜)보다 4시간 앞서 끝납니다.

이와 별개로, **정기 입금($500, 매월 10일)만은 "확정하려는 거래일"이 아니라 배치가 실제로 도는 한국 날짜**(`today_kst`, 신규 파라미터, 생략 시 `datetime.now(KST).date()`)로 판정합니다 — 시장 이벤트가 아니라 현금 이벤트라는 기존 2-2-4 원칙을 그대로 지키려면, "어제로 하루 밀린" 거래일 기준으로 입금일을 판정하면 안 되기 때문입니다. 이 두 날짜가 실제로 다르게 동작하는지(`test_deposit_uses_today_kst_not_target_date` 등)와 `today_kst`를 생략하면 진짜 오늘 날짜로 떨어지는지까지 테스트로 고정했습니다.

**⚠️ 이 두 결정(target_date=어제 기본값 / today_kst 분리) 모두 오너가 명시적으로 확정한 것이 아니라, 이번 라운드에 실제 수집 스케줄(scrape_us.yml)로부터 제가 도출한 값입니다.** 코딩을 막는 항목은 아니라 이대로 커밋하지만, 저장소를 다시 열면 꼭 한 번 확인해주세요 — 아래 "🔴 미결정 항목 모아보기"에도 항목을 추가했습니다.

**새 파일 4개**:
- `utils/duel_batch_usd.py`(약 340줄) — `PROBE_STATE_FILENAME_USD`·`PROBE_INDEX_KEYS_SPEC_USD`(`SP500_PROXY_SPY`·`NASDAQ_PROXY_ONEQ`)·`default_state_path_usd()`·`run_nightly_batch_usd()`·`format_summary_lines_usd()`.
- `run_duel_daily_batch_us.py`(약 205줄) — 실행 스크립트. `--target-date`(생략 시 어제)·`--today-date`(신규, 생략 시 오늘)·`--dry-run`·`--override`·`--data-dir`·`--state-path`. `report_db.load_us_index_closes()`로 지수 2종 최신 종가, `scorecard_db.load_universe_index(MARKET_US)`로 상위 종목, `report_db.build_price_lookup()`으로 종가 조회 — 전부 "내 성적표"·"리포트"와 같은 로더 재사용(§0-3-10).
- `.github/workflows/duel_daily_us.yml`(148줄) — cron `"0 3 * * *"`(매일, 원화는 평일만 — 이유는 위 🔴 참고), `workflow_dispatch` 입력에 `today_date` 추가, 기준값 파일(`data/duel_freshness_probe_previous_usd.json`) 커밋에 원화와 같은 fetch+rebase 재시도(최대 3회) 패턴.
- `tests/test_duel_batch_usd.py`(약 340줄, 37개) — §0 재사용-정체성 검사(14개 함수 `is` 동일성), `format_summary_lines_usd`가 원화 함수와 다른 객체인지·달러 기호를 쓰는지, 기준값 파일명이 원화와 안 겹치는지, `PROBE_INDEX_KEYS_SPEC_USD`가 `report_db`의 벤치마크 키와 실제로 일치하는지, 위 날짜 분리 동작, 표 배선 격리, §0-3-2 쿼리 수 회귀([3,50,900]), dry-run, None 클라이언트 에러 처리, docstring 완비.

**검증**:
1. `python3 -m py_compile` 4개 파일 전부 — 클린.
2. `tests/test_duel_batch_usd.py` 37개 신규 작성 — 처음엔 3개 실패(테스트 계좌에 시드 원장이 없어 매수 주문이 "체결"이 아니라 예수금 부족으로 "만료" 처리됨) → `_seed_ledger()` 헬퍼(시드 $7,500) 추가해 수정 → 재실행 37개 전부 통과.
3. **전체 결투 스위트 재실행**: `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_db_usd.py tests/test_duel_batch.py tests/test_duel_batch_usd.py tests/test_duel_publish.py tests/test_duel_public_ui.py` → **602개 전부 통과**(기존 565 + 신규 37, 회귀 0).
4. `.github/workflows/duel_daily_us.yml`은 `device_commit_files`가 `.github/workflows/` 경로를 막고 있어 `device_bash` 헤어독으로 오너 컴퓨터에 직접 작성 후 `md5sum`으로 클라우드 사본과 대조 — 일치 확인.

**아직 안 된 것(다음 라운드)**: 발행 배치의 USD 지원(`utils/duel_publish.py` 확장 또는 신규 `utils/duel_publish_usd.py`), 화면(`duel_page.py` 확장 또는 USD 전용 화면), 위 각각의 테스트. KRW v1의 6단계(실검증)·7단계(공개 전환)도 여전히 미착수입니다.

---

### 5-16. 🆕 USD 트랙 5차 코딩 — **체결 거래일이 하루 늦게 잡히던 버그 수정**(오너가 직접 발견) + 신규 `resolve_fill_trading_day_usd()` (2026-08-21, 12번째 코딩 작업)

**배경 — 오너가 그림을 그려가며 되물어서 발견됐습니다.** §5-15까지 마친 뒤, 오너가 "달러 결투"의 시간축을 다시 짚어보다가 **미국장의 실제 개장(09:30 ET)·마감(16:00 ET) 시각과 USD 주문 접수 시간대(16:00:01~21:00:00 KST)를 한 그림 위에 겹쳐 그려놓고** "그래서 왜 당일 종가를 쓰면 안 된다는 거냐"고 되물으셨습니다. 그 질문이 핵심이었습니다 — **원화 트랙에서 "당일 종가를 쓰면 안 되는 이유"가 USD 트랙에는 아예 성립하지 않기 때문**입니다.

- **원화**: 접수 시간대 18:00:01~22:00:00 KST는 그날 KRX 정규장이 끝나고 코스피 크롤링(16:05~16:40 KST)까지 마친 **뒤**입니다. 접수 시점에 **그날(D) 종가는 이미 확정돼 있고 사용자도 이미 아는 값**이라, 그 값으로 체결하면 "이미 알려진 정보로 매매"가 됩니다(§0-3-1). 그래서 D 자신을 후보에서 반드시 빼야 공정합니다. `utils/duel_rules.py::resolve_fill_trading_day()`의 docstring에 이 이유가 원래부터 적혀 있었습니다.
- **USD**: 접수 시간대 16:00:01~21:00:00 KST는 미국 동부시각으로 **03:00~08:00 ET**(§5-13에서 오너가 최종 확정)이고, 그날 미국 정규장 개장(09:30 ET)보다 **한참 전**입니다. 즉 접수 시점에 **그날 마감가는 아직 존재조차 하지 않는 완전히 미확정된 값**입니다 — 뺄 이유가 하나도 없습니다.

그런데 `utils/duel_db_usd.py::save_order_usd()`가 체결 거래일(`target_date`)을 정할 때 **원화용 `duel_rules.resolve_fill_trading_day()`를 그대로 재사용**하고 있었습니다. 그 함수는 "저장한 그날 자신은 무조건 후보에서 제외"하므로, USD 주문은 **원래 제외할 필요가 없는 날짜를 하나 더 건너뛰어 체결이 불필요하게 하루 늦어지는** 상태였습니다. 화면에도 로그에도 오류로 나타나지 않고 조용히 하루가 밀리는 종류의 버그라, 오너가 시간축을 직접 되짚지 않았다면 실검증 단계에서도 못 잡았을 가능성이 큽니다.

**실측 증거(수정 전)**: `tests/test_duel_db_usd.py::test_save_order_usd_inside_window_writes_pending_order_to_the_usd_table()`은 `INSIDE_WINDOW_USD = 2026-08-19 18:30 KST`에 주문을 저장하면서 확정 거래일 목록으로 `[8/19, 8/20, 8/21]`(8/19 자신이 이미 후보에 있음)을 넘기는데, 저장된 `target_date`가 `"2026-08-20"`이었습니다 — 8/19가 후보에 있는데도 그냥 건너뛴 것입니다. 수정 후에는 `"2026-08-19"`가 됩니다.

**어떻게 고쳤는지 — 이 트랙의 확립된 패턴("순수 규칙은 공유하되, 통화별 전제가 본문에 박힌 것만 새로 정의")의 세 번째 사례**로 처리했습니다(첫째 `_translate_order_guard_error_usd()` — 오류 문구에 원화 시간대 "18:00~22:00"이 박혀 있던 경우(§5-14), 둘째 `format_summary_lines_usd()` — 로그 문구에 "원"이 박혀 있던 경우(§5-15)). **앞의 두 사례가 "사람이 읽는 문구" 때문에 갈라진 것과 달리, 이번 사례는 규칙의 전제 자체(주문 접수 시간대가 그날 장의 앞이냐 뒤냐)가 통화마다 다르기 때문에 갈라졌다는 점이 다릅니다** — 이 트랙에서 처음으로 "판정 로직 자체"가 갈라진 경우입니다.

**무엇이 새로 생겼는지 / 무엇을 고쳤는지:**

- **신규 `utils/duel_rules.py::resolve_fill_trading_day_usd(saved_at_kst, trading_days)`** (§11 USD 트랙 절, `resolve_order_window_usd()` 바로 뒤). 원화 함수와 **실행 코드의 차이는 딱 한 곳**입니다 — `later = [day for day in days if day > saved_date]` → `eligible = [day for day in days if day >= saved_date]`(**당일 자신을 후보에 포함**). 반환값 규약·예외 처리(거래일 목록이 `None`이거나 비었으면 `DuelRuleError`, 근거 없는 날짜를 만들지 않는다는 §0-1 원칙)는 원화와 완전히 동일하게 유지했습니다. docstring에 ① 원화와 판정 방향이 정반대라는 것, ② 그 이유(USD 접수 시간대가 그날 미국장 개장 전이라 당일 마감가가 주문 시점에 미확정), ③ "원화 함수를 실수로 그대로 쓰면 체결이 불필요하게 하루 늦어진다"는 경고를 다른 두 예외 사례와 같은 톤으로 남겼습니다.
- **`utils/duel_rules.py` §11 절 머리말** — "이 절에는 오직 '숫자 자체가 통화마다 다른' 것들만 옵니다"라는 기존 문장이 더 이상 정확하지 않아, 이 함수가 **상수가 아니라 규칙의 전제 때문에 갈라진 예외**라는 단서를 덧붙였습니다.
- **⚠️ 기존 `resolve_fill_trading_day()`(원화)는 한 글자도 건드리지 않았습니다** — 원화 트랙의 동작은 그대로입니다(아래 검증 3번에서 실행 코드를 직접 대조 확인).
- **`utils/duel_db_usd.py::save_order_usd()`** — 호출을 `resolve_fill_trading_day_usd()`로 교체하고, docstring의 "원화와 다른 점" 목록을 3가지 → 4가지로 늘려 이 항목을 명시했습니다(잘못 쓰면 하루 밀린다는 경고 포함).
- **`utils/duel_db_usd.py` 머리말의 "재사용 vs 신규 정의" 목록** — `resolve_fill_trading_day`를 "재사용" 목록에서 빼고, "새로 정의합니다" 목록에 **세 번째 예외 사례**로 옮겨 적었습니다(`_translate_order_guard_error_usd()` 설명 바로 뒤). `apply_buy_fill_to_position`·`compute_twr`는 이번 버그와 무관하므로 재사용 목록에 그대로 남습니다.
- **`utils/duel_batch_usd.py` 머리말의 날짜 설명** — "X일에 접수된 주문의 체결 거래일은 X 이후 첫 확정 거래일, 즉 보통 X+1일이 되고…"라고 **버그가 있던 옛 동작을 설명하던 문단**을 실제 동작에 맞게 고쳤습니다: 한국 날짜 X일 저녁에 접수된 주문은 **X일 자신**(X가 확정 거래일인 한)의 미국 정규장 마감가로 체결되고, 그 마감가는 크롤링 시차 때문에 **한국 날짜 X+1일 이른 아침**에야 저장소에 들어오므로 배치가 처리하는 것은 여전히 "실행일의 하루 전 거래일"이라는 설명입니다. **이 파일의 실행 코드(`target_date`/`today_kst` 두 인자 분리, cron 관련 로직)는 한 줄도 건드리지 않았습니다** — 설명 문구만 정확하게 고쳤습니다.
- **테스트 수정 1건** — `tests/test_duel_db_usd.py`의 위 실측 증거 테스트에서 `assert payload["target_date"] == "2026-08-20"` → `"2026-08-19"`(이유를 주석으로 남김). 같은 파일의 다른 `target_date` 하드코딩 2곳(`fetch_pending_orders_for_fill_usd` 테스트 §9, `expire_or_cancel_all_pending_for_date_usd` 테스트 §10)은 **코드를 하나하나 읽어 확인한 결과 `save_order_usd()`를 전혀 거치지 않고 배치 조회 함수에 날짜를 직접 인자로 넘기는 테스트**여서 이번 버그의 영향을 받지 않습니다 — 고치지 않았습니다.
- **테스트 신규 5개** — `tests/test_duel.py`에 새 섹션 `⑥-USD 체결 거래일 확정 — USD 트랙`을 기존 ⑥절 바로 뒤에 추가했습니다: (a) 당일 자신이 후보에 있으면 당일 자신을 반환(날짜만 넘길 때·시각까지 붙을 때·접수 시간대 양끝 16:00:01/21:00:00 전부), (b) 당일이 후보에 없으면(주말·추수감사절 같은 휴장일) 그 이후 첫 후보를 반환, (c) 목록이 `None`/빈 집합/빈 리스트이거나 이후 거래일이 하나도 없으면 예외(§0-1 — "당일 포함"이 "목록 없어도 오늘을 쓴다"로 잘못 구현되면 잡힘), (d) **원화 함수와의 직접 대조 회귀 테스트** — 정확히 같은 `saved_date`·`trading_days`를 두 함수에 넣어 `krw == 8/20` / `usd == 8/19` / `usd < krw` / 두 함수가 `is not` 다른 객체임을 고정하고, 덧붙여 "차이는 오직 당일 포함 여부 하나뿐"임을 보이기 위해 **저장일이 거래일이 아닌 경우엔 두 함수의 답이 같아진다**는 것도 함께 고정(이 트랙에 이미 있는 `test_order_window_usd_and_krw_share_the_same_one_second_offset_convention`이 "같음"을 고정하는 것과 정반대 성격의 "다름을 고정하는" 테스트), (e) **호출부 회귀** — 함수만 새로 만들고 호출부를 안 바꾸면 버그가 그대로 남으므로, `utils/duel_db_usd.py`가 `_usd` 함수를 부르고 `utils/duel_db.py`는 여전히 원화 함수를 부르는지 소스로 직접 대조.

**검증(전부 직접 실행):**

1. `python3 -m py_compile utils/duel_rules.py utils/duel_db_usd.py utils/duel_batch_usd.py tests/test_duel.py tests/test_duel_db_usd.py` — 전부 클린.
2. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_db_usd.py tests/test_duel_batch.py tests/test_duel_batch_usd.py tests/test_duel_publish.py tests/test_duel_public_ui.py -q` → **607개 전부 통과**(수정 전 기준선 602개를 먼저 그대로 재실행해 602 통과를 확인한 뒤, 신규 5개를 더해 607 — **회귀 0건**).
3. **되돌리기 검증(revert-and-confirm, 이 저장소의 기존 관례) 2회.** ① `resolve_fill_trading_day_usd()`의 `>=`를 원화처럼 `>`로 되돌림 → 신규 테스트 2개 + 수정한 테스트 1개, 총 **3개가 정확히 실패**(`assert '2026-08-20' == '2026-08-19'`) → 다시 `>=`로 고치니 전부 통과. ② 이번엔 함수는 그대로 두고 `save_order_usd()`의 호출만 원화 함수로 되돌림 → 호출부 회귀 테스트 + 저장 테스트, **2개가 정확히 실패** → 되돌리니 전부 통과. 즉 새 테스트들이 실제로 이 버그를 잡습니다.
4. **원화 함수 무결성 대조** — `resolve_fill_trading_day is not resolve_fill_trading_day_usd`(다른 객체, `__code__`도 다름) 확인 + 두 함수의 **실행 코드를 줄 단위로 직접 대조**해, 차이가 ① 함수 이름 ② `day > saved_date` → `day >= saved_date` ③ 그에 딸린 지역변수명(`later`→`eligible`) 3줄뿐이고 **원화 함수 본문은 전혀 바뀌지 않았음**을 확인.
5. `grep -rn "resolve_fill_trading_day\b" utils/ tests/ web/` — 원화 함수를 **실제로 호출하는 자리는 `utils/duel_db.py:615`(원화 `save_order()`) 하나뿐**이고 그대로입니다. 나머지 매치는 전부 주석·docstring·테스트 참조입니다(`web/pages/duel_page.py:935`도 주석 한 줄).

**⚠️ 이 수정은 §5-15의 cron·`target_date`/`today_kst` 분리 로직 자체를 바꾸지 않습니다(오너와 이미 별도로 확인 완료).** 배치가 "실행되는 한국 날짜의 하루 전 거래일"을 처리한다는 구조도, `.github/workflows/duel_daily_us.yml`의 cron `0 3 * * *`(= KST 정오)도, 정기입금($500)만 `today_kst`로 판정한다는 분리도 전부 그대로입니다 — 미국 마감가가 한국 날짜 X+1일 아침에야 들어온다는 수집 시차는 이번 수정과 무관하게 변하지 않기 때문입니다. **바뀐 것은 오직 주문에 붙는 `target_date`의 절대값이 하루 앞당겨진 것뿐**이고(한국 날짜 X일 저녁 주문 → `target_date = X`, 그것을 **X+1일 낮 배치가 처리**), 배치의 스케줄링 구조는 손대지 않았습니다. `utils/duel_batch_usd.py`는 설명 문구만 고쳤고 실행 코드는 한 줄도 안 건드렸습니다.

**아직 안 된 것(다음 라운드)**: 발행 배치의 USD 지원(`utils/duel_publish.py` 확장 또는 신규 `utils/duel_publish_usd.py`), 화면(`duel_page.py` 확장 또는 USD 전용 화면), 위 각각의 테스트. KRW v1의 6단계(실검증)·7단계(공개 전환)도 여전히 미착수입니다. → **아래 §5-17에서 발행 배치·실행 스크립트·워크플로우·테스트까지 완료. 남은 것은 화면뿐입니다.**

---

### 5-17. 🆕 USD 트랙 6차 코딩 — **발행 배치 오케스트레이션**(신규 `utils/duel_publish_usd.py`) + 실행 스크립트 + 워크플로우 + 전용 테스트 (2026-08-21, 13번째 코딩 작업)

**배경**: §5-14~§5-16이 예고한 "다음 코딩 라운드"입니다. `utils/duel_db_usd.py`에 발행 배치가 필요로 하는 B절 함수(활성계좌·포지션·스냅샷 일괄조회, 발행 대상 동의·철회 조회, 체급 배정 조회/기록, 발행표 삭제/쓰기, 그룹 이력 조회 등 14종)는 §5-14에서 **이미 전부 만들어져 있었고**, 없던 것은 그 함수들을 순서대로 부르는 **오케스트레이션 계층**이었습니다. 이번 라운드에서 `utils/duel_publish.py`(원화, 726줄)를 처음부터 끝까지 다시 읽고 그 통화 미러를 새로 작성했습니다. **화면은 이번 범위 밖입니다(다음 라운드).**

**무엇을 만들었는지 (새 파일 4개 + 기존 1개에 함수 1개 추가):**

- **신규 `utils/duel_publish_usd.py`(약 450줄)** — USD 발행 배치 오케스트레이션. 순서는 원화와 글자 그대로 같습니다: 계좌 전체 조회 → 철회 청소 → 발행 대상 동의 조회 → 체급 배정(시즌 고정) → TWR → 순위 → 최소 인원 게이팅 → 그날 발행분 전량 삭제 후 재작성. `dry_run=True` 지원. 머리말에 `duel_db_usd.py`·`duel_batch_usd.py`와 **같은 문체로** §0-3-8 5원칙(USD 기준으로 다시 서술)·재사용 목록·신규 정의 목록·파일 분리 근거를 적었습니다.
- **신규 `run_duel_publish_batch_us.py`(약 150줄, 저장소 루트)** — `run_duel_publish_batch.py`(원화)의 짝. 인자 체계(`--published-date`·`--dry-run`)와 서비스 클라이언트 생성 방식(`duel_db_usd.create_service_client()` = 원화와 **같은 함수 객체**, 같은 프로젝트·같은 키)을 그대로 따르고 `run_publish_batch_usd()`를 부릅니다. 요약 줄 자체는 원화와 공유하는 함수가 만들기 때문에, 로그만 보고 트랙을 헷갈리지 않도록 **실행 머리말에 대상 표 이름 두 개를 찍습니다**.
- **신규 `.github/workflows/duel_publish_daily_us.yml`(약 160줄)** — cron `"50 3 * * *"`. 계산 근거는 아래 별도 항목.
- **신규 `tests/test_duel_publish_usd.py`(약 1,200줄, 108개)** — 아래 별도 항목.
- **`utils/duel_rules.py`에 신규 `resolve_bracket_for_season_usd()` 1개 추가**(§11-2, `bracket_label_usd()` 바로 뒤) + §11 절 머리말 갱신. 이유는 아래 "재사용 vs 신규 정의"의 🔴 항목. **원화 함수 `resolve_bracket_for_season()`은 한 글자도 건드리지 않았습니다**(검증 4번에서 실행 코드를 줄 단위로 대조 확인).
- **⚠️ `utils/duel_publish.py`(원화)는 한 줄도 수정하지 않았습니다.** 파일 타임스탬프까지 그대로이고, `tests/test_duel_publish_usd.py::test_krw_publish_module_was_not_modified_to_know_about_usd`가 그 파일 실행 코드에 `_usd`·`USD`·`duel_publish_usd`·`duel_db_usd`가 나타나면 실패하도록 회귀로 고정해 뒀습니다(원화 파일이 "혼자 열어서 처음부터 끝까지 읽을 수 있는 §0-3-8 검토 단위"라는 성질을 지키기 위해).

**재사용 vs 신규 정의 — 판단 근거(코드를 한 줄씩 직접 읽고 확인):**

- **그대로 재사용(같은 객체, `is` 동일성 테스트로 고정) 17개** — `DuelPublishError`(예외 타입 공유 → 호출부가 한 종류만 잡으면 됨) · 상태/사유 상수 7개(`PRINCIPAL_OK`/`PRINCIPAL_NO_HOLDINGS`/`PRINCIPAL_FX_MIXED`/`SKIP_*` 4개 — "보유가 없음"·"통화가 섞임"은 통화 무관 개념이라 값이 다를 이유가 없음) · `_to_date` · `_as_float` · `assert_full_consent`(동의 항목 5개는 두 트랙이 **같은 컬럼 이름**을 쓰고 본문에 표 이름도 통화도 없음) · `consented_user_ids_for_real_principal` · `build_publish_rows` · `leaderboard_payload` · `holdings_payload` · `split_groups_by_threshold` · `format_summary_lines`. 여기에 `duel_batch_usd.compute_twr_by_account`(= `duel_batch`의 것과 같은 객체, §5-15 재사용 목록)와 `duel_db_usd.fetch_real_principal_holdings`·`fetch_nicknames_for_accounts`·`group_rows_by_account`(= `duel_db`의 것과 같은 객체, §5-14 재사용 목록)까지 포함하면 총 21개입니다. 모듈이 선언한 재사용 목록(`REUSED_FROM_DUEL_PUBLISH`)이 실제 코드와 일치하는지도 테스트가 검사합니다.
  - **`format_summary_lines()` 재사용은 특히 조심해서 확인했습니다.** §5-15에서 `duel_batch.format_summary_lines()`의 실행되는 f-string 안에 `"…현금 합계: {…:,.0f}원"`이 박혀 있는 걸 발견해 USD 전용 함수를 갈라야 했던 전례가 있어서, 발행 요약 함수에도 같은 함정이 있는지 AST로 직접 뒤졌습니다 — **없습니다**(발행 요약은 금액을 한 번도 출력하지 않고 계좌 수·인원 수·행 수만 출력합니다). 그 판단 근거 자체를 테스트로 고정했고(`test_shared_summary_lines_carry_no_currency_literal`), 그 테스트는 **"원화 배치 함수는 실제로 이 검사에 걸리는지"까지 함께 확인**해서 검사가 무력해지지 않았음을 증명합니다. (검사어를 "원" 한 글자로 잡으면 "참가 **인원**"·"전**원**" 같은 정상 문구가 걸려서, 실제 사고 패턴인 **서식 자리 바로 뒤에 붙는 통화 단위**(`{…}원`)와 명시적 기호(₩·$)만 보도록 AST 수준에서 판정합니다.)
- **새로 정의 5개** —
  1. `summarize_real_principal_usd()` — 원화 함수는 `scorecard_db.CURRENCY_KRW`가 아닌 통화가 하나라도 있으면 FX_MIXED이고 `krw_cost_basis`를 반환합니다. USD는 **정확히 반대**(비-USD가 섞이면 FX_MIXED, `usd_cost_basis` 반환). 원화 함수를 그대로 썼다면 **달러 보유자가 전원 "구간 미적용"으로 떨어져 USD 체급이 통째로 사라졌을** 자리입니다. 반환 키 이름도 일부러 다르게(`usd_cost_basis`) 뒀습니다 — 같으면 두 요약 dict가 섞여도 아무도 눈치채지 못합니다.
  2. `summarize_real_principal_by_user_usd()` — 원화 함수가 본문에서 `summarize_real_principal()`을 직접 부르기 때문에 함께 갈라졌습니다(묶기 로직 자체는 동일).
  3. `resolve_bracket_for_account_usd()` — `duel_rules.assign_bracket_usd()`(§0-3-2의 $750/$2,250/$3,750/$7,500/$22,500/$45,000/$75,000 8구간)를 씁니다.
  4. `all_possible_groups_usd()` — `duel_rules.BRACKET_KEYS_USD`를 씁니다(`ACCOUNT_WINDOW_TYPES`는 통화 무관이라 공유). 개수(27)를 파일에 숫자로 적지 않았고, 테스트도 하드코딩 대신 실제 상수 길이로 계산합니다.
  5. `run_publish_batch_usd()` — `duel_db.*` 호출을 전부 `duel_db_usd.*`로 교체 + 위 함수들 사용.
- **🔴 작업 지시와 달랐던 지점 1건 — `duel_rules.resolve_bracket_for_season()`은 통화 중립이 아니었습니다.** 이번 라운드 지시서에는 "시즌 고정 규칙은 통화 무관 순수 함수이니 그대로 재사용하라"고 적혀 있었는데, **실제로 코드를 읽어 보니 그 함수 본문이 유효한 체급 목록으로 `BRACKET_KEYS`(원화 9개)를 하드코딩**하고 있었습니다. 직접 실행해서 확인했습니다: `resolve_bracket_for_season(None, "usd_75000_plus", …)` → `DuelRuleError: 새로 계산한 체급 식별자를 알 수 없습니다`. 즉 그대로 재사용했다면 시즌 고정이 조용히 사라지는 게 아니라 **USD 발행 배치가 매일 밤 그 자리에서 죽었을** 것입니다(첫 실발행 때 터졌을 사안). 그래서 이 트랙의 확립된 패턴("순수 규칙은 공유하되, 통화별 전제가 본문에 박힌 것만 새로 정의")의 **네 번째 사례**로 처리해 `resolve_bracket_for_season_usd()`를 새로 만들었습니다. 앞선 세 사례(① `_translate_order_guard_error_usd()` — 오류 문구의 원화 시간대, ② `format_summary_lines_usd()` — 로그 문구의 "원", ③ `resolve_fill_trading_day_usd()` — 판정의 전제)와 달리, **이번은 "검증 대상 집합"이 통화마다 다른 경우**입니다. 실행 코드의 차이는 `BRACKET_KEYS` → `BRACKET_KEYS_USD` **두 곳뿐**이고(그 밖에 오류 문구에 "(USD)"를 덧붙임), 원화 함수 본문은 그대로입니다. ⚠️ **시즌 경계 자체(`season_key_for_date()`, 3월 1일·12개월)는 5-11-8 확정대로 여전히 공유합니다** — 갈라진 것은 체급 식별자 검증뿐입니다. `duel_rules.py` §11 절 머리말도 "이 절에는 숫자만 온다"는 문장이 더 이상 정확하지 않아 예외 2건을 명시하도록 갱신했습니다.

**🔴 cron 계산 — 직접 계산했습니다(오너 확인 필요 항목):**

1. **선행 배치의 최악 종료 시각**을 저장소의 실제 파일에서 읽었습니다 — `.github/workflows/duel_daily_us.yml`: cron `"0 3 * * *"`(03:00 UTC = KST 정오) + `timeout-minutes: 20` → **최악의 경우 03:20 UTC 종료**.
2. **원화 트랙이 실제로 쓴 여유 폭**을 그대로 가져왔습니다 — `duel_daily.yml`(cron `'10 8 * * 1-5'` + timeout 20분 → 08:30 UTC 종료) → `duel_publish_daily.yml`(cron `'0 9 * * 1-5'` = 09:00 UTC) = **여유 30분**. 이 30분은 2026-08-20 오너가 직접 확정한 값입니다(§5-10 — 처음 10분에서 늘림).
3. **같은 30분을 USD에 적용**: 03:20 UTC + 30분 = **03:50 UTC = KST 12:50** → cron **`"50 3 * * *"`**.
4. **뒤쪽 여유도 확인**: 이 배치의 `timeout-minutes: 20`을 다 써도 04:10 UTC(= KST 13:10)에 끝나므로, 다음 USD 주문 접수 시작(**16:00:01 KST**, §5-13 오너 확정)보다 **약 2시간 50분 앞서** 끝납니다. 원화 트랙이 30분 버퍼를 택하면서 포기해야 했던 "접수 시작 전 갱신 보장"이, USD 트랙에서는 여유 있게 지켜집니다.
5. **매일 돌립니다(`* * *`, 평일 한정 아님)** — 선행 `duel_daily_us.yml`이 매일 도는 것과 맞췄습니다. 발행만 평일이면 미국 금요일 마감 성적이 월요일까지 순위표에 안 실립니다. (원화 발행이 `1-5`인 것은 KRX가 주말에 확실히 쉬기 때문입니다.)
6. `concurrency` 그룹은 원화(`duel-publish-batch`)와 **다른 이름**(`duel-publish-batch-usd`)을 썼습니다 — 같은 이름이면 서로 다른 표를 만지는 두 배치가 서로를 기다립니다. 결과를 커밋하지 않으므로 `permissions: contents: read`(최소 권한)입니다.
   - ⚠️ 🔴 **이 cron 값은 오너 확정이 아니라 위 1~3을 제가 계산해 도출한 값입니다.** 게다가 선행 `duel_daily_us.yml`의 cron `0 3 * * *` 자체도 §5-15에서 도출된 미확정 값입니다("🔴 미결정 항목" 6번). **선행 배치 시각이 바뀌면 이 파일의 cron도 함께 다시 계산해야 합니다** — 두 값은 항상 "선행 종료 + 30분"으로 묶여 있고, 테스트(`test_usd_publish_workflow_keeps_the_same_buffer_the_owner_confirmed_for_krw`)가 그 관계를 정확한 숫자가 아니라 **관계로** 고정합니다.
   - ⚠️ **이 워크플로우 파일은 이번 세션 샌드박스에만 완성해 뒀습니다.** 실제 사용자 저장소의 `.github/workflows/` 경로 반영은 오너가 별도 도구(device_bash 등)로 처리할 예정입니다 — §5-15에서 `duel_daily_us.yml`을 반영한 것과 같은 방식입니다.

**신규 테스트 `tests/test_duel_publish_usd.py`(108개) — 무엇을 보는가:**

- **§0 재사용-정체성 검사** — 재사용 17개가 원화 모듈과 **같은 객체**인지(`is`), USD 전용 5개가 원화 함수와 **다른 객체**인지(`is not` + `__code__` 비교), 모듈이 선언한 재사용 목록이 실제와 일치하는지.
- **🔴 KRW·USD 대조 회귀** — 같은 보유 목록을 두 요약 함수에 넣어 답이 **정확히 뒤집히는지**(국내주식만 → 원화 OK/USD FX_MIXED, 미국주식만 → 원화 FX_MIXED/USD OK, 섞임 → 둘 다 FX_MIXED, 없음 → 둘 다 NO_HOLDINGS). §5-16이 추가한 `resolve_fill_trading_day` vs `_usd` 대조 테스트와 **같은 성격**("다름을 고정")입니다. 반환 키 이름이 서로 다른 것까지 고정합니다.
- **시즌 함수가 갈라진 이유 자체를 고정** — 원화 시즌 함수에 USD 체급을 넘기면 예외, USD 시즌 함수는 정상, USD 시즌 함수에 원화 체급을 넘기면 예외(양방향), 시즌 경계는 공유.
- **체급 경계 15개** — `BRACKET_TIERS_USD` 경계값 **그 자체**가 어느 쪽으로 가는지 한 줄씩. 시즌 고정(시즌 중 유지 / 새 시즌 재산정 / 손상된 값 거절 / 원화 체급이 USD 배정 기록에 섞이면 거절), 두 트랙의 체급 키가 겹치지 않는지(`BRACKET_NONE_KEY`만 의도적 공유).
- **그룹 조합** — 3 × `len(BRACKET_KEYS_USD)`, 개수 하드코딩 없음, 원화 체급이 섞이지 않는지, 실행 코드에 "27"이라는 숫자가 없는지.
- **🔴 §0-3-8 5원칙 증명(배치를 통째로 돌려서)** — ① 동의자 0명이면 `holdings` 질의가 **아예 안 나감** + 동의자/비동의자가 섞였을 때 `in` 필터에 동의자 id만 들어감 + `report_db.fetch_all_holdings`를 부르지 않음, ② 조회 필터(`final_confirmed=true` + `revoked_at is null`)가 실제로 나가는지 **그리고** 필터가 깨져 동의 안 한 행이 섞여 들어오면 행 단위 확인이 **잡아 멈추는지**(발행 insert가 0건), ③ TWR 없는 계좌가 0%로 둔갑하지 않고 빠지는지 + payload에서 `None`이 `0`으로 안 바뀌는지 + 발행표 쓰기가 식별자 필드를 마지막 방어선에서 거절하는지, ④ 499 vs 500 경계 + 미달 그룹 과거 행 삭제(날짜 필터 없이) + **참가자가 전부 사라진 그룹까지** 점검 + 발행표가 비어 있으면 그 점검을 건너뛰는지, ⑤ 철회 청소가 **다른 무엇보다 먼저** 도는지 + 철회 삭제에 `published_date` 필터가 **없는지**.
- **트랙 격리** — 배치를 통째로 돌려 KRW 결투 표 9개에 질의가 하나도 안 가는지(`duel_nicknames`만 의도적 공유), 소스에 원화 표 이름 문자열이 없는지, `duel_db`에서 쓰는 것이 통화 무관 상수 하나(`CONSENT_REAL_PRINCIPAL_FLAG`)뿐인지.
- **§0-3-2 쿼리 수 회귀** — 계좌 [3, 50, 900]에서 고정 왕복 7 + 청크 × 상수 + 2. 원화 테스트와 같은 방식.
- **그 밖** — 전량 재작성(삭제가 삽입보다 먼저) · 같은 날 두 번 돌려도 중복 없음 · 체급 배정은 insert만(upsert/update 없음, AST 확인) · 닉네임 없는 계좌는 빠지고 요약에 남음(배치가 대신 만들지 않음) · 발행되는 매입금액이 달러 단위인지 · `dry_run`이 정말 아무것도 안 쓰는지 · 발행일 필수 · None 클라이언트 · docstring 완비 · 실행 스크립트가 USD 함수를 부르고 인자 체계가 원화와 같은지 · 워크플로우 cron 관계 4종.

**검증(전부 직접 실행):**

1. `python3 -m py_compile utils/duel_publish_usd.py run_duel_publish_batch_us.py tests/test_duel_publish_usd.py utils/duel_rules.py` — 전부 클린. 저장소 전체 `python3 -m compileall`도 클린. `python3 -m pyflakes`(신규 3개 파일) 클린.
2. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_db_usd.py tests/test_duel_batch.py tests/test_duel_batch_usd.py tests/test_duel_publish.py tests/test_duel_publish_usd.py tests/test_duel_public_ui.py -q` → **715개 전부 통과**(§5-16 기준선 607개를 먼저 그대로 재실행해 607 통과를 확인한 뒤, 신규 108개를 더해 715 — **회귀 0건**).
3. **되돌리기 검증(revert-and-confirm) 6회** — 전부 "되돌리면 정확히 실패 → 고치면 전부 통과"를 확인했습니다.
   ① `assign_bracket_usd` → `assign_bracket`(원화 체급 경계) → **33개 실패**.
   ② `resolve_bracket_for_season_usd` → `resolve_bracket_for_season`(원화 시즌 검증) → **36개 실패**(배치가 `DuelRuleError`로 죽는 것을 그대로 재현 — 이 함수를 가른 판단이 옳았음을 실증).
   ③ `scorecard_db.CURRENCY_USD` → `CURRENCY_KRW`(통화 판정 뒤집기) → **8개 실패**(대조 회귀 테스트가 정확히 잡음).
   ④ `BRACKET_KEYS_USD` → `BRACKET_KEYS`(그룹 목록) → **2개 실패**.
   ⑤ 워크플로우 cron `50 3` → `10 3`(체결 배치 종료 전으로) → **2개 실패**("USD 여유 폭(-10분)이 원화(30분)와 다릅니다").
   ⑥ 실행 스크립트가 원화 함수를 부르도록 되돌림 → **1개 실패**(함수만 만들고 호출부를 안 바꾸는 §5-16식 사고를 잡는 회귀).
4. **원화 함수 무결성 대조** — `resolve_bracket_for_season`과 `_usd`의 실행 코드를 AST로 언팩해 줄 단위로 대조: 차이는 `BRACKET_KEYS` → `BRACKET_KEYS_USD` 2곳과 오류 문구의 "(USD)" 뿐이고 **원화 함수 본문은 전혀 바뀌지 않았음**을 확인. `utils/duel_publish.py`는 파일 타임스탬프까지 그대로.
5. `grep` 확인 — 새 파일 3개(`utils/duel_publish_usd.py`·`run_duel_publish_batch_us.py`·워크플로우)에서 `_usd` 접미사 없는 결투 표 이름을 찾으면, **머리말 docstring의 트랙 격리 설명 한 줄**만 나옵니다(실행 코드에는 없음 — 표 이름 문자열 자체가 하나도 없고 전부 `duel_db_usd` 상수를 통해 갑니다. 테스트가 AST로 이 사실을 고정합니다).
6. `.github/workflows/duel_publish_daily_us.yml` YAML 파싱 직접 확인 — `name`/`cron`/`jobs`/`timeout-minutes` 전부 정상.
7. 실행 스크립트를 실제로 돌려봤습니다(`python3 run_duel_publish_batch_us.py --dry-run`) — Supabase 접속 정보가 없는 환경이라 **의도대로** "조용히 성공한 척하지 않고" 한국어 사유와 함께 종료 코드 1로 죽습니다.

**아직 안 된 것(다음 라운드)**: **화면뿐입니다** — `web/pages/duel_page.py` 확장(같은 창유형 카드 안에 위쪽 KRW 블록 / 아래쪽 USD 블록, **합산 숫자는 절대 만들지 않음** — 5-11-2)·USD 동의 관리 화면·USD 순위표 화면(원화 순위표와 **완전히 별개 표**를 읽는 것 — 5-11-9), `web/layout.py` 플래그·메뉴 배선, `main.py` 배선, 그리고 그 테스트. KRW v1의 6단계(실검증)·7단계(공개 전환)도 여전히 미착수입니다. → **아래 §5-18에서 `/duel` 화면(KRW/USD 병기)까지 완료. 남은 것은 USD 동의 관리 화면과 USD 순위표 화면 둘뿐입니다.**


---

### 5-18. 🆕 USD 트랙 7차 코딩 — **화면**(`web/pages/duel_page.py` 확장: 같은 창유형 카드에 KRW/USD 블록 병기) + 신규 `tests/test_duel_page_usd.py` (2026-08-21, 14번째 코딩 작업)

**배경**: §5-12~§5-17이 예고한 "마지막 남은 것 = 화면"의 첫 조각입니다. 이번 라운드가 지금까지의 USD 라운드와 **성격이 다른 이유**를 먼저 적어 둡니다 — 스키마·규칙·DB계층·배치·발행은 전부 **아직 아무도 호출하지 않는 새 코드**였지만, `web/pages/duel_page.py`는 **이미 배포돼 `/duel` 로 서비스 중인 화면 파일**입니다. 그래서 이번 라운드의 최우선 제약은 "USD를 잘 만드는 것"이 아니라 **"원화 트랙이 지금 하는 동작을 한 글자도 바꾸지 않는 것"** 이었고, 아래 검증도 그 순서로 짰습니다. 범위는 `/duel`(1갈래) 화면 하나이고, **USD 동의 관리 화면·USD 순위표 화면은 이번 범위 밖(다음 라운드)** 입니다 — `duel_consent_page.py`·`duel_leaderboard_page.py`는 한 글자도 건드리지 않았습니다.

**무엇을 만들었는지 (`web/pages/duel_page.py`, 1,112줄 → 2,093줄 · 신규 `tests/test_duel_page_usd.py` 41개):**

- **머리말 갱신** — 기존 세션 격리 규율(§0-3-8) 서술은 그대로 두고, "USD 트랙이 이 화면에 함께 들어왔다"는 6개 항목을 덧붙였습니다(원화 무수정 원칙 / 무엇을 갈랐고 무엇을 공유하는지 / 접수 시간대 혼동 금지 / **체결 시점 방향이 통화마다 정반대** / 합산 금지 / 트랙 독립).
- **§1-USD 신규 상수·문구 블록** — `CURRENCY_USD`, `ORDER_WINDOW_TEXT_USD`(규칙 계층 상수에서 생성), 상시 고지 `MANDATORY_NOTICES_USD` 4종(`NOTICE_NO_DIVIDEND_USD` · `NOTICE_FILL_TIMING_USD` · `NOTICE_BUY_ONLY_USD` · **신규 `NOTICE_NO_FX_MIX`**), 사전 고지 `NOTICE_CRAWL_FAILURE_USD`, 설명 문구 `NOTICE_WHY_SAME_DAY_USD` · `NOTICE_CASH_ROLLOVER_USD` · `NOTICE_TWR_USD`. **원화 문구 6종은 한 글자도 손대지 않았습니다.**
- **🔴 `NOTICE_FILL_TIMING_USD` — §5-16이 확정한 실제 동작을 그대로 씁니다.** "다음 거래일(D+1) 종가"가 아니라 **"주문을 넣은 바로 그날의 미국 정규장 마감가(그날이 미국 휴장일이면 그 뒤 가장 이른 거래일의 마감가)"** 이고, 그 이유(접수 시간대가 그날 미국장 개장보다 한참 전이라 **주문하는 순간 그 마감가는 아직 세상에 존재하지 않는다**)까지 함께 밝힙니다. `NOTICE_WHY_SAME_DAY_USD`는 원화 `NOTICE_WHY_NEXT_DAY`와 **결론이 정반대인** 설명이라 별도 문구로 뒀고, 두 트랙의 이유가 왜 반대인지를 한 문단으로 나란히 적었습니다. 근거는 `utils/duel_rules.py::resolve_fill_trading_day_usd()`의 docstring입니다.
- **🔴 신규 `_upcoming_trading_days_usd(from_date)`** — 원화 `_upcoming_trading_days()`와 **시작점이 다릅니다**: 원화는 `range(1, …)`(다음 날부터), USD는 `range(0, …)`(**저장일 자신부터**). §5-16에서 고친 `resolve_fill_trading_day_usd()`는 `day >= saved_date`로 후보를 고르므로 **당일이 목록에 들어 있어야만** "그날 체결"이 성립하고, 당일을 뺄지 말지를 판정하는 코드는 그 함수 어디에도 없습니다 — 즉 "당일을 후보에 넣는다"는 결정이 온전히 화면 쪽 책임입니다. 여기를 원화처럼 짜면 **함수는 멀쩡한데 인자가 틀려서 §5-16 버그가 화면에서 되살아나고**, 로그에도 화면에도 오류로 안 남습니다. 주말 제외가 근사치라는 점(미국 휴장일 캘린더가 이 저장소에 없음)과 진짜 판정은 새벽 배치가 한다는 점은 원화와 같은 톤으로 docstring에 적었습니다.
- **🔴 신규 `_order_window_state_usd()` / `_window_message_usd()`** — `resolve_order_window_usd()`(16:00:01~21:00:00, §5-13 오너 확정)를 씁니다. 화면은 원화 안내 배너와 **달러 안내 배너를 나란히 두 개** 그립니다(하나로 합치면 반드시 한쪽이 틀린 시각을 말하게 됩니다).
- **신규 `_load_us_universe()`** — `MARKET_US` 스냅샷(`data/us_stocks_latest.json`)을 읽습니다. 인덱스 생성은 '내 성적표'·'미국주식'이 이미 쓰는 `scorecard_db.build_universe_index()`를 **그대로 재사용**(두 번째 로더를 만들지 않음, §0-3-10). ⚠️ 메타데이터 시각 키가 원화와 다릅니다 — 미국은 `last_updated_at_kst`(원화는 `last_updated_at`). 원화 키를 그대로 쓰면 조용히 `None`이 되어 "언제 기준 값인지"가 화면에서 사라집니다. `price_lookup`에는 `{MARKET_US: index}`만 넘겨, 미국 티커 조회가 한국 목록을 스칠 통로 자체를 없앴습니다.
- **신규 렌더 함수 8개** — `_render_header_usd()`(상시 고지, 원화 3종 **뒤에** 덧붙임) · `_render_rules_expansion_usd()` · `_render_opt_in_usd()`(→ `opt_in_usd()`) · `_position_rows_usd()`(→ `MARKET_US`) · `_render_account_card_usd()` · `_render_positions_table_usd()` · `_render_order_form_usd()`(→ `save_order_usd()`) · `_render_orders_section_usd()` / `_render_account_orders_usd()` / `_render_pending_order_row_usd()`(→ `edit_order_usd()`·`cancel_order_usd()`) / `_render_order_history_table_usd()`.
- **재사용한 것(복제 안 함)** — `_fail` · `_parse_positive_int` · `_twr_display`(→ `compute_twr`) · `_order_status_text` · `_universe_options` · `sum_cash_balance` · `is_buy_window_open` · `DuelDbError` · `resolve_stock_query`(시장을 인자로 받는 통화 무관 함수라 미국 종목 검색을 그대로 처리) · `holdings_table_html` · `WINDOW_TITLES` · `TRADING_DAY_HORIZON` · `RECENT_ORDER_LIMIT` · `MONTHLY_DEPOSIT_DAY` · `ACCOUNT_WINDOW_TYPES`. 이 트랙이 다섯 번째로 적용한 같은 판단 기준입니다("표 이름·통화 상수·시간대·문구가 본문에 박힌 것만 새로 정의").

**§5-11-2(계좌 구조 A안)를 화면에 옮긴 방식 — 그리고 원화 화면을 지키기 위해 둔 두 경로:**

- `_render_accounts()`가 이제 `usd_accounts` / `usd_market` 을 **키워드 인자로** 추가로 받습니다. 그리고 **분기가 둘**입니다.
  1. **달러 계좌가 하나도 없으면(= 오늘 실제 사용자 전원) 2026-08-21 이전과 글자 그대로 같은 코드 경로**를 탑니다 — 같은 제목("💰 내 가상계좌 3개"), 같은 안내문, 같은 `ui.row().classes('w-full gap-4 items-stretch')`, 같은 `_render_account_card()` 호출. 원화 사용자의 화면을 바꾸지 않기 위한 **의도적인 두 경로**입니다.
  2. 달러 계좌가 있으면 창유형(M1/M3/M6)마다 한 칸을 만들어 **위쪽에 원화 카드, 아래쪽에 달러 카드**를 넣습니다. 원화 카드는 **기존 `_render_account_card()`를 그대로 호출**하고(내용·클래스 무수정), 새로 생긴 것은 그 칸을 감싸는 컨테이너뿐입니다. 컨테이너를 `display: grid`로 둔 이유도 적어 뒀습니다 — 안쪽 카드가 이미 `flex: 1 1 320px`을 달고 있어서 칸을 세로 flex로 만들면 그 320px이 **높이 기준값**으로 해석돼 빈 카드가 320px로 늘어납니다.
- **🔴 합산 숫자는 어디에도 만들지 않았습니다.** `_render_accounts()`는 `metric_card()`·`format_amount()`를 **한 번도 부르지 않고** 통화별 카드 함수에 넘기기만 합니다. 그리고 이 원칙을 문장이 아니라 **구조로 고정**하는 테스트를 넣었습니다(아래 검증 참고). 화면에는 `NOTICE_NO_FX_MIX`로 "두 통화를 더하지 않는다, 그래서 합계가 없다"는 사실 자체를 사용자에게 정직하게 밝힙니다(§0-1 — 없는 이유를 밝히지 않으면 사용자가 버그로 오해합니다).

**트랙 독립(§5-11-10 · 스키마 §14-10) — 네 가지 상태를 전부 정상으로 다룹니다:**

`_render_duel_section()`이 원화 계좌와 달러 계좌를 **각각 따로** 조회하고 **각각 따로** 소유자 이중 확인을 합니다. ① 둘 다 참여 ② 원화만 ③ 달러만 ④ 둘 다 미참여 — 넷 다 정상 상태이고, 한쪽이 없다고 다른 쪽 렌더링을 막지 않습니다. 참여하지 않은 트랙의 안내 카드는 **맨 아래**에 붙여, 이미 쓰고 있는 트랙의 흐름(계좌 → 주문 창 → 내 주문)을 중간에서 끊지 않게 했습니다. **달러 계좌 조회가 실패해도 원화 화면은 그대로 그립니다**(한쪽 장애가 다른 쪽을 삼키지 않게). `duel_opt_in_usd()`가 원화 계좌 존재 여부를 전혀 보지 않는다는 것은 `sql/duel_schema.sql` §14-10을 직접 읽어 확인했습니다(대상은 `auth.uid()` 하나뿐).

**새 URL은 만들지 않았습니다.** 기존 `/duel` 하나를 확장한 것이라 `main.py`·`web/layout.py`는 **한 글자도 건드리지 않았습니다**(테스트가 "경로가 정확히 `/duel` 하나"임을 고정합니다). 3단계 공개 게이트(`DUEL_ENABLED` / `DUEL_MENU_ADMIN_ONLY`)도 그대로이고, 달러 블록은 그 게이트 **안쪽**에 있습니다.

**검증(전부 직접 실행):**

1. `python3 -m py_compile web/pages/duel_page.py tests/test_duel_page_usd.py` — 클린. `pyflakes` — 클린.
2. **원화 회귀(베이스라인 대비) — 이번 라운드에서 가장 공들인 부분입니다.**
   - `tests/test_web_session_isolation.py::test_duel_page_wiring` · `test_duel_render_smoke` : 수정 전/후 모두 통과.
   - ⚠️ 그런데 이 두 테스트는 `check()`가 **실패를 기록만 하고 pytest를 실패시키지 않는** 구조라 "통과"만으로는 회귀 검증이 되지 않습니다. 그래서 **화면에 찍히는 ❌ 목록 자체를 수정 전/후로 캡처해 `diff` 로 대조**했고 **바이트 단위로 동일**했습니다(파일 전체 39건, 결투 두 테스트만 11건 — 전부 이번 라운드 이전부터 있던 것이고 아래 6번 참고). 대조를 위해 **오너 컴퓨터의 원본 `duel_page.py`를 다시 내려받아 실제로 되돌려 놓고** 같은 명령을 돌린 뒤 제 버전으로 복구했습니다(md5 대조 확인).
   - `tests/test_duel_public_ui.py` 단독 44개 통과(수정 전/후 동일). `tests/test_event_loop_blocking.py` 14개 통과.
3. **결투 스위트 전체** `python3 -m pytest tests/test_duel*.py -q` → **756개 전부 통과**(기존 715 + 신규 41, **회귀 0건**). 수정 전 기준선 715개를 원본 파일로 그대로 재실행해 715 통과를 먼저 확인한 뒤 비교했습니다.
4. **되돌리기 검증(revert-and-confirm) 7회** — 새 테스트가 실제로 이 라운드가 막으려는 사고들을 잡는지 하나씩 확인하고 전부 복구했습니다.
   ① `_upcoming_trading_days_usd`의 `range(0,…)` → `range(1,…)`(§5-16 버그를 화면에서 재현) → **3개 실패**.
   ② `_order_window_state_usd`가 원화 `resolve_order_window()`를 부르도록 되돌림 → **2개 실패**.
   ③ `save_order_usd()`에 원화용 `_upcoming_trading_days()`를 넘기도록 되돌림 → **2개 실패**(함수는 그대로 두고 **인자만** 틀린 경우도 잡힙니다).
   ④ `NOTICE_FILL_TIMING_USD = NOTICE_FILL_TIMING`(원화 문구 복사) → **1개 실패**.
   ⑤ `_render_accounts()`에 두 통화를 더한 "두 트랙 합계" 지표를 심음 → **2개 실패**(§5-11-2 위반이 구조 검사에 걸림).
   ⑥ 달러 참여 버튼이 원화 `opt_in()`을 부르도록 되돌림 → **4개 실패**.
   ⑦ 달러 DB 호출 하나를 `run_blocking` 없이 이벤트 루프 위에서 직접 부르도록 되돌림 → **2개 실패**.
5. **렌더 스모크는 진짜로 실행됩니다** — 합성 계좌·주문·현금으로 `_render_body()`를 끝까지 돌려, 달러 종목명에 심어둔 `<img onerror=...>`가 **글자 그대로** 나오는지(§0-3-9), 부분체결이 요청·실제 수량을 **둘 다** 보여주는지, 가격을 못 구한 종목이 0이 아니라 "가격 확인 중"인지, 원화만/달러만 참여한 사용자의 화면이 깨지지 않는지, 남의 `user_id`가 섞인 달러 계좌를 그리지 않는지를 확인합니다. Supabase에는 접속하지 않고 시세 스냅샷만 저장소의 실제 파일을 읽습니다.

**🆕 이번 라운드에서 발견했지만 범위 밖이라 손대지 않은 것 (오너 확인 필요):**

6. 🔴 **`tests/test_web_session_isolation.py`의 `check()`는 실패를 `FAILURES` 목록에 담기만 하고, 그 목록은 `main()`에서만 검사합니다.** 즉 **pytest로 돌리면 내부 검사 실패가 초록불에 묻힙니다**(예외가 난 경우에만 빨간불). 실제로 지금 결투 두 테스트 안에서 **11건이 조용히 ❌**입니다 — 대부분은 2026-08-21의 `run_blocking` 도입 부작용입니다(검사가 `fetch_x(client, …)` 모양을 찾는데 실제 코드는 `run_blocking(fetch_x, client, …)`라 "호출 0건"으로 잡힘). 검사 로직이 낡은 것이지 화면이 틀린 게 아니고, **이번 라운드 전후로 목록이 완전히 동일**합니다. 다만 이 상태를 그대로 두면 앞으로 진짜 회귀가 나도 아무도 모릅니다. 원화 테스트를 고치는 일이라 이번 범위 밖으로 두었습니다 — **다음 라운드 후보 1순위**입니다. (신규 `tests/test_duel_page_usd.py`는 이 문제가 없도록 처음부터 전부 진짜 `assert`로 짰고, `run_blocking(fn, client, …)` 패턴을 이해하는 검사를 씁니다.)
7. 🔴 `tests/test_web_session_isolation.py::test_duel_render_smoke` 안에 **`await` 를 빠뜨린 줄**이 하나 있습니다 — `page._render_duel_section(...)`이 `async def`가 된 뒤에도 그냥 호출만 하고 있어서(`RuntimeWarning: coroutine was never awaited`) "남의 계좌 혼입 방어" 검사가 실제로는 아무것도 검사하지 못합니다. 위 6번과 같은 이유로 조용히 ❌ 처리되고 있었습니다. 같은 시나리오는 신규 테스트 파일에서 달러 기준으로 제대로 검증해 뒀습니다.
8. 🔴 **화면 문구 오너 검토 요청** — 아래 세 가지는 사실관계가 §5-16과 일치하는지 오너 눈으로 한 번 확인해 주세요. ① `NOTICE_FILL_TIMING_USD`의 "**주문을 넣은 바로 그날의 미국 정규장 마감가**"라는 표현이 오너가 그림으로 확인한 시간축과 같은 말인지. ② `NOTICE_WHY_SAME_DAY_USD`가 원화와 이유가 반대라는 점을 헷갈리지 않게 설명하는지. ③ 달러 카드의 지표 이름을 "**총자산 (달러)**"로 달아 원화 카드의 "총자산"과 구분했는데, 이 정도로 충분한지(합산 숫자는 없지만 두 카드가 세로로 붙어 있어 "총자산"이 두 번 보입니다).
9. 🔵 달러 계좌 카드의 시세 기준 시각은 미국 스냅샷의 `last_updated_at_kst`(한국시간)로 표시합니다 — 원화 카드와 **기준 시각이 다른 것이 정상**이고, 그 사실을 화면에 한 줄로 밝혀 뒀습니다. 실제로 나란히 놓고 보면 어떤지 확인 부탁드립니다.
10. 🔵 **원화 트랙에는 없던 `NOTICE_NO_FX_MIX` 고지가 상시 노출 목록에 하나 늘었습니다**(원화 3종 + 달러 4종 = 화면 상단 배너 7개). 실사용해보고 배너가 너무 많다고 느끼시면, 달러 4종을 접이식으로 내리는 등 조정 가능합니다 — 다만 §2-8의 "상시 노출" 요구와 충돌하므로 **오너 결정이 필요한 사안**입니다.

**아직 안 된 것(다음 라운드)**: **USD 동의 관리 화면**(`duel_consent_page.py` 확장 또는 신규 — `save_consent_usd`·`fetch_my_consent_usd`·`revoke_consent_usd`는 §5-14에서 이미 완성돼 있고 호출부만 없습니다)과 **USD 순위표 화면**(`duel_leaderboard_page.py` 확장 또는 신규 — 원화와 **완전히 별개 표**를 읽어야 합니다, 5-11-9), 그 두 화면의 `web/layout.py` 플래그·메뉴 배선·`main.py` 배선, 그리고 그 테스트. KRW v1의 6단계(실검증)·7단계(공개 전환)도 여전히 미착수입니다.

---

### 5-19. 🆕 USD 트랙 8차 코딩 — **남은 화면 2개**(`duel_consent_page.py` 공개 동의 관리 + `duel_leaderboard_page.py` 공개 순위표) 확장 + 신규 테스트 2종 (2026-08-21, 15번째 코딩 작업)

**배경**: §5-18이 "다음 라운드"로 남겨 둔 마지막 두 화면입니다. 이것으로 **USD 트랙의 코드는 전부 끝났습니다** — 스키마(§5-12) · 규칙 상수(§5-13) · DB 접근 계층(§5-14) · 야간 배치(§5-15) · 체결 거래일 버그 수정(§5-16) · 발행 배치(§5-17) · `/duel` 화면(§5-18) · **동의 화면 + 순위표 화면(이번)**.

§5-18과 성격이 같습니다 — 두 파일 모두 **이미 배포된 화면**이라, 최우선 제약은 "USD를 잘 만드는 것"이 아니라 **"원화 트랙이 지금 하는 동작을 한 글자도 바꾸지 않는 것"** 이었습니다.

**오너가 코딩 착수 전에 확정해 준 설계 결정 4가지(그대로 따랐습니다)**:
1. **새 파일도, 새 URL도, 새 플래그도 만들지 않습니다.** `/duel/consent`·`/duel/leaderboard`를 그대로 확장하고, `DUEL_CONSENT_ENABLED`/`DUEL_CONSENT_MENU_ADMIN_ONLY`/`DUEL_LEADERBOARD_ENABLED`/`DUEL_LEADERBOARD_MENU_ADMIN_ONLY` 4개를 **원화·달러 공통으로 재사용**(§5-18에서 `/duel` 화면을 `DUEL_ENABLED` 하나로 게이팅한 것과 같은 정신 — 통화는 화면 존재 여부가 아니라 화면 **안**의 분기).
2. **동의 화면**은 `/duel` 화면(§5-18)이 이미 검증한 패턴 그대로 — USD 계좌가 하나도 없으면 예전과 **완전히 동일한 렌더 경로**, 있으면 창유형(M1/M3/M6)마다 위쪽 원화 카드 / 아래쪽 달러 카드. "아직 참여하지 않으셨습니다"는 **`mine`과 `mine_usd`가 둘 다 빌 때만**.
3. **순위표 화면**은 **세 번째 드롭다운(통화: 원화/달러)** 추가. 고른 통화의 발행표만 읽고 **절대 병합·비교하지 않습니다**(5-11-9).
4. **닉네임은 통화 공통**(`ensure_nickname`/`fetch_my_nickname`을 접미사 없이 재사용 — 5-11-10, `duel_nicknames_usd` 표는 존재하지 않음).

---

#### 🔴 이번 라운드에서 발견한 함정 — **이 트랙의 다섯·여섯 번째 "통화 리터럴이 박혀 있던 함수"**

이전 라운드들에서 "통화 무관인 줄 알았는데 실제로는 리터럴이 박혀 있던" 사례가 넷 있었습니다(`_translate_order_guard_error`(§5-14) · `format_summary_lines`(§5-15) · `resolve_fill_trading_day`(§5-16) · `resolve_bracket_for_season`(§5-17)). 이번에 둘 더 나왔습니다.

**⑤ `holding_row_cells()` — `format_amount(buy_amount, "KRW")` 로 통화가 본문에 박혀 있었습니다.**
그대로 재사용했다면 **달러 참가자의 매입금액이 "612원"으로 표시**됩니다($612.50이 아니라). 예외도 로그도 나지 않고, 사용자에게만 틀린 값이 보이는 종류입니다(§0-1 정면 위반). 오너가 착수 전에 이 자리를 미리 짚어 주셨고, 실제로 확인해 보니 그대로였습니다.
- **그런데 `holding_row_cells_usd()`를 새로 만들지는 않았습니다** — 이 함수에서 통화에 걸린 것은 `format_amount()`의 인자 **하나뿐**이고, 나머지 본문은 전부 **XSS 이스케이프(§0-3-9)** 와 **"비공개 ≠ 0"(§0-1)** 판정입니다. 복제하면 **이스케이프 경로가 두 개**가 되어 한쪽만 고치는 순간 조용히 뚫립니다. 지금까지의 복제 기준("표 이름·시간대·통화 문구가 본문에 박힌 것")과 정반대 상황이라, 여기서는 **인자로 갈랐습니다**(`currency=CURRENCY_KRW` 기본값 — 생략하면 예전과 글자 그대로 같은 원화 표기). 이 판단 자체를 회귀 테스트로 고정해 뒀습니다(`test_holdings_table_was_not_duplicated_per_currency`).

**⑥ `NOTICE_REAL_PRINCIPAL`(독립 동의 설명 문구) — 통화가 안 적혀 있어서 오히려 위험했습니다.**
문구에 "원"도 "$"도 없어서 통화 무관처럼 보이지만, 실제 판정은 **정반대**입니다:
- 원화 트랙: `duel_publish.summarize_real_principal()` → "내 성적표"에 **원화가 아닌 통화가 하나라도** 있으면 `FX_MIXED`(→ 구간 미적용), 원화 매입원가합계로 체급.
- 달러 트랙: `duel_publish_usd.summarize_real_principal_usd()` → **달러가 아닌 통화가 하나라도** 있으면 `FX_MIXED`, 달러 매입원가합계($750/$2,250/…)로 체급.

즉 원화 문구를 달러 카드에 그대로 붙이면, 국내주식만 가진 사용자가 "내 매입총합으로 체급이 정해지겠구나" 하고 동의했는데 실제로는 **전원 '구간 미적용'** 으로 떨어집니다. 그래서 `NOTICE_REAL_PRINCIPAL_USD`를 따로 뒀고(달러 경계값·"원화 종목이 섞이면 합치지 않고 구간 미적용"·"환율 시계열이 없어서 합칠 수 없음"을 전부 명시), 화면에 적은 경계값이 `BRACKET_TIERS_USD`와 실제로 같은지도 테스트로 대조합니다.

**🔵 그 밖에 발견한 것 (오너 확인 필요)**

- **✅ 2026-08-21 오너 확정 — 닉네임 공유의 부작용, 트랙별로 나눌 필요 없음.** 두 트랙에 모두 공개하면 "이 두 줄은 같은 사람"이 드러나는 부작용(같은 창유형의 원화 순위표와 달러 순위표에 같은 닉네임 문자열이 실림 — 5-11-10 "같은 사용자면 같은 닉네임"의 당연한 결과)을 오너에게 보고했고, **오너가 "트랙별로 나눌 필요는 없을 것 같다"고 확정**했습니다. 그래서 지금 구현(닉네임은 `(user_id, window_type)` 단위로 원화·달러 공유, `duel_nicknames_usd` 표를 새로 만들지 않음, 동의 화면에 `NOTICE_SHARED_NICKNAME`으로 이 사실을 미리 고지)이 **최종 방침입니다 — 추가 코드 변경 없음.** 교차 *사용자* 유출은 애초에 아니었습니다(§0-3-8은 그대로 지켜집니다 — 남의 데이터는 어느 경로로도 안 흐르고, 발행표에는 `user_id`·`account_id` 컬럼 자체가 없습니다). 공개 범위에 대한 사실을 사용자에게 미리 알리는 고지 문구만 화면에 남아 있으면 충분합니다.
- **🔵 원화 전용 사용자의 안내문 한 줄이 이제 부정확해질 수 있습니다.** 원화 전용 경로의 *"계좌마다 서로 다른 무작위 닉네임이 발급되며"* 는 원화만 쓰는 동안에는 참이지만, 달러에도 참여하면 같은 창유형끼리 이름을 공유합니다. **원화 무수정 원칙 때문에 이 문장을 손대지 않았고**, 달러 병기 화면에서는 정확한 문장(`NOTICE_SHARED_NICKNAME`)으로 대체됩니다. 원화 문장도 미리 고쳐 둘지는 오너 판단입니다.
- **🔵 둘 다 미참여일 때의 안내문("가상계좌 3개가 만들어지고")** 도 이제 최대 6개(원화 3 + 달러 3)입니다. 역시 원화 무수정을 우선해 **글자 그대로 두었습니다**(틀린 말은 아니고 불완전할 뿐). 고칠지 오너 확인 부탁드립니다.
- **✅ §5-18이 1순위 후보로 남겼던 문제(`tests/test_web_session_isolation.py`의 `check()`가 pytest를 실패시키지 않음)는 이미 해결돼 있었습니다.** 그 파일에 `_assert_no_check_failures` autouse fixture가 들어와, 이제 `check()` 실패가 pytest에서도 빨간불이 됩니다. 이번 라운드는 그 덕을 봤습니다 — 새 전역 `CURRENCY_TITLES`를 허용 목록에 등록하지 않았을 때 **곧바로 실패**해서 잡혔습니다.
- **🔵 원화 전용 경로의 `@ui.refreshable` 루프에 잠재적 늦은-바인딩 문제가 있습니다.** `for account in mine:` 안에서 `@ui.refreshable`을 바로 정의하면, 나중에 `.refresh()`로 다시 그릴 때 본문이 참조하는 이름이 **마지막 반복의 객체**로 풀립니다(첫 렌더는 정상, 재렌더만 어긋남). 예전부터 있던 모양이라 **원화 무수정 원칙에 따라 그대로 뒀고**, 새로 만든 달러 병기 경로는 팩토리(`_consent_section()`)로 그 문제가 생기지 않게 짰습니다. 고칠지 오너 판단(급하지 않음 — `.refresh()`가 잘못된 카드를 다시 그려도 데이터는 서버가 다시 읽으므로 값이 틀리지는 않고, "저장했는데 다른 카드가 새로고침되는" 표시 문제입니다).

---

#### 재사용 vs 신규 정의 — 이번 라운드의 판단표

**`web/pages/duel_consent_page.py` (783줄 → 1,234줄)**

| 함수·상수 | 판단 | 근거 |
|---|---|---|
| `consent_item_rows` · `missing_item_labels` · `all_items_checked` | **재사용** | 5-2-1의 다섯 문장은 통화 무관. 본문(독스트링 제외)에 통화 리터럴 0건 — 테스트로 고정 |
| `item_save_payload` · `final_confirm_payload` · `real_principal_payload` · `_assert_no_real_principal` | **재사용** | `save_consent_usd()`가 받는 키 집합이 원화와 **완전히 동일**(`CONSENT_ITEM_FLAGS` + `final_confirmed` + 독립 동의). FakeClient로 실제 저장 계약까지 확인 |
| `consent_state` · `reconsent_notice` · `revoke_guard` · `_render_current_state` | **재사용** | USD 동의 표가 원화와 컬럼이 같고, 3개월 판정도 같은 `duel_rules.resolve_reconsent_block()` |
| `CONSENT_ITEM_FLAGS` · `CONSENT_REAL_PRINCIPAL_FLAG` · `CONSENT_ITEM_SENTENCES` | **재사용** | `duel_db_usd`가 `duel_db`에서 그대로 import — `is` 항등성 테스트로 증명 |
| **`ensure_nickname` · `fetch_my_nickname`** | **재사용(접미사 없음)** | 🔴 5-11-10 — `duel_nicknames`는 원화·달러 공유 표. `duel_db_usd.ensure_nickname is duel_db.ensure_nickname` 확인. `*_usd` 판이 **존재하지 않는지**도 테스트로 고정 |
| `NOTICE_RESPONSIBILITY` · `NOTICE_ALL_OR_NOTHING` · `NOTICE_FINAL_CONFIRM` · `NOTICE_BRACKET_FIXED` · `NOTICE_REVOKE` · `NOTICE_REVOKE_TIMING` · `REVOKE_CONFIRM_LABEL` | **재사용** | 책임 고지·전부아니면전무·최종확인·시즌 고정·철회 결과는 통화 무관 요구사항(5-2/5-8). 시즌 경계도 공유(5-11-8) |
| `_render_account_consent_usd` | **신규** | `fetch_my_consent_usd`(표 이름) + 카드 제목 |
| `_render_consent_form_usd` | **신규** | 저장이 `_save_usd`(→ 다른 표)로 나감. 인자 하나(`save_fn=`)로 태우지 않은 이유는 `duel_publish_usd.py` 머리말과 동일 — 인자를 빠뜨리면 **달러 동의가 원화 표에 저장되는** 통로가 생김 |
| `_render_real_principal_form_usd` | **신규** | 위 함정 ⑥ — 체급 기준 통화가 정반대 |
| `_render_revoke_usd` | **신규** | `revoke_consent_usd`(표 이름) + "원화는 그대로 남습니다" 고지 |
| `_save_usd` | **신규** | `save_consent_usd`(표 이름). 닉네임 발급은 **접미사 없는 원본 그대로** |
| `_render_both_tracks` · `_consent_section` | **신규** | 창유형별 원화/달러 병기 컨테이너(§5-11-2). 원화 카드는 기존 함수를 **그대로** 호출 |
| `NOTICE_REAL_PRINCIPAL_USD` · `NOTICE_TRACKS_INDEPENDENT` · `NOTICE_SHARED_NICKNAME` | **신규 문구** | 위 함정 ⑥ + 트랙 독립(5-11-10) + 닉네임 공유 사실 고지(§0-1) |

**`web/pages/duel_leaderboard_page.py` (524줄 → 702줄)**

| 함수·상수 | 판단 | 근거 |
|---|---|---|
| `window_options` | **재사용** | `ACCOUNT_WINDOW_TYPES`가 통화 공통. 본문 통화 리터럴 0건(테스트 고정) |
| `section_cap` · `twr_display` · `rank_text` | **재사용** | 상위/하위 500·%·등수는 통화 무관. `LEADERBOARD_*`·`MIN_PARTICIPANTS_FOR_PUBLICATION`의 `_USD` 판이 **없는지**도 테스트로 고정 |
| `FIXED_NOTICE_PARAGRAPHS` · `_render_fixed_notice` | **재사용, 화면 전체에 딱 한 번** | 5-3 오너 확정 문구. 통화 선택보다 **위**(로그인 게이트보다도 위)에서 한 번만 그림 — 통화를 바꿔도 안 사라지고 안 두 번 그려짐(테스트 고정) |
| `NOTICE_HOW_RANKING_WORKS` · `NOTICE_MIN_PARTICIPANTS` · `NOTICE_DAILY` · `NOTICE_OVERLAP` · `NOTICE_EMPTY_GROUP` · `NOT_PUBLISHED_TEXT` | **재사용** | 통화 무관. 달러에만 필요한 보충(`NOTICE_BRACKET_CURRENCY_USD`)은 달러를 골랐을 때만 한 줄 더 붙임 |
| **`holding_row_cells` · `holdings_table`** | **재사용 + 통화 인자** | 🔴 함정 ⑤. 복제하면 **XSS 이스케이프 경로가 둘**이 되므로 인자로 가름. 생략 시 예전과 동일한 원화 |
| `bracket_options` | **재사용(원화 전용으로 유지)** | 그대로 |
| `bracket_options_usd` | **신규** | `BRACKET_KEYS_USD`/`bracket_label_usd()` — 오너 지시대로 화면 로컬 함수 |
| `currency_options` · `CURRENCY_TITLES` | **신규** | 통화 선택기. 통화 코드 자체는 `scorecard_db.CURRENCY_KRW/USD`가 단일 출처(`is` 확인) |
| `track_readers` | **신규** | 🔴 **통화마다 다른 것 5가지가 모여 있는 단 하나의 자리**(발행일·순위표·보유종목 조회 함수 + 체급 라벨/목록 + 금액 서식 통화). §0-3-8 검토가 "이 함수만 보면 된다"가 되게 하려는 구조. 두 번들이 **콜러블을 하나도 공유하지 않는다**는 것을 `is` 검사로 고정 |
| `_currency_changed` | **신규** | 통화가 바뀌면 체급 목록을 통째로 갈고 선택을 새 목록 첫 항목으로 리셋(원화/달러 체급 키는 '구간 미적용' 말고는 하나도 안 겹침 — 비슷한 구간을 골라 주지 않습니다, §0-1) |
| `_render_group`/`_render_section`/`_render_participant`/`_render_holdings` | **확장(인자 추가)** | `readers`를 **한 번 골라 넘깁니다** — 여러 곳에서 따로 고르면 "달러 표를 읽고 원화로 서식"이 가능해짐 |

---

#### 검증 (전부 직접 실행)

1. `python3 -m py_compile` — 수정·신규 파일 전부 클린. `pyflakes` — 신규 코드 0건(원화 쪽에 예전부터 있던 미사용 지역변수 1건은 그대로, 손대지 않음).
2. **원화 회귀 0건 — 되돌려서 대조하는 방식(§5-18과 같은 방법)**
   - 두 화면 파일의 **원본을 백업**해 두고, 수정본과 번갈아 넣어 가며 같은 명령을 돌렸습니다(md5 대조로 원상복구 확인).
   - `tests/test_web_session_isolation.py`의 **❌ 목록 자체를 캡처해 `diff`** — pytest 모드 21건, 스크립트 모드 16건이 **수정 전/후 완전히 동일**(byte-identical). 실패·오류 테스트 이름 목록도 동일(`test_macro_render_smoke` 1건 실패 + 렌더 스모크 3건 오류 — 전부 이번 라운드 이전부터 있던 것).
   - `tests/test_duel_public_ui.py`(원화 2갈래 화면의 기존 검증) **44개 그대로 통과**.
   - 저장소 **전체 스위트**를 되돌린 상태와 수정한 상태로 각각 실행: **942 통과 → 1,033 통과**(+91), 실패·오류 목록은 **완전히 동일**.
3. **결투 스위트 전체** `python3 -m pytest tests/test_duel*.py -q` → **847개 전부 통과**(기존 756 + 신규 91, **회귀 0건**).
   - 신규 `tests/test_duel_consent_page_usd.py` **45개**(706줄, 9개 절), 신규 `tests/test_duel_leaderboard_page_usd.py` **46개**(638줄, 9개 절).
4. **되돌리기 검증(revert-and-confirm) 8회** — 새 테스트가 실제로 이 라운드가 막으려는 사고를 잡는지 하나씩 심어 보고 전부 복구했습니다.
   ① `_save_usd`가 `save_consent()`(원화 표)를 부르게 되돌림 → **3개 실패**
   ② `holding_row_cells`의 `"KRW"` 하드코딩 복원 → **3개 실패**
   ③ "참여하지 않으셨습니다"를 `mine`(원화)만 보고 띄우게 되돌림 → **1개 실패**(달러만 참여한 사용자가 화면을 못 쓰는 상태)
   ④ USD 번들이 원화 발행일 함수를 쓰게 함 → **3개 실패**
   ⑤ 달러 카드의 소유자 이중 확인 제거 → **1개 실패**(§0-3-8)
   ⑥ 달러 독립 동의 설명에 원화 문구 재사용 → **2개 실패**
   ⑦ `_render_holdings`가 통화를 **다시** 고르게 함 → **2개 실패**
   ⑧ 5-3 고정 문구를 통화 그룹 안에서 한 번 더 그림 → **1개 실패**
5. **`main.py`·`web/layout.py` — 확인해 봤는데 변경 불필요.** `main.py`는 이미 `duel_consent_page`·`duel_leaderboard_page`를 import하고 있고(§5-10에서 배선 완료), `web/layout.py`의 4개 플래그와 메뉴 라벨("🔓 공개 동의 관리" / "🏆 공개 순위표 (내 밑으로 눈 깔어)")은 **통화를 언급하지 않는 문구**라 그대로 두면 원화·달러 양쪽을 덮습니다. 오너의 설계 결정 1번이 그대로 맞았습니다. 두 화면 파일 모두 "경로가 정확히 하나"임을 테스트로 고정해 뒀습니다.
6. **테스트 파일 2건을 최소 수정**했습니다(약화 아님, 근거 기록):
   - `tests/test_duel_public_ui.py` — `_render_holdings(..., readers)` / `holdings_table(rows, currency)` 시그니처 변경에 맞춰 **호출 한 곳**만 갱신. `readers`에 기본값을 두지 **않은** 것은 의도적입니다(호출부가 빠뜨리면 조용히 원화 표를 읽는 대신 `TypeError`로 시끄럽게 실패). 검증 내용은 한 글자도 안 약해졌습니다.
   - `tests/test_web_session_isolation.py` — 신규 전역 `CURRENCY_TITLES`를 `ALLOWED_MUTABLE_GLOBALS`에 **사유와 함께 등록**(§0-3-8 절차 그대로. 등록 전에는 실제로 빨간불이 떴습니다).

**⚠️ 이번에도 검증하지 못한 것(§0-1 — 할 수 있는 것만 했다고 말합니다)**: 실제 브라우저에서 통화 드롭다운을 바꿔 보는 것. 처리기(`_currency_changed`)는 페이지 함수 안의 지역 클로저라 테스트에서 직접 부를 수 없어, **그 처리기가 의지하는 위젯 API**(`ui.select.set_options(options, value=…)`)가 이 NiceGUI 판에서 실제로 도는지만 실물로 확인했습니다. 실제 Supabase RLS 동작은 6단계(실검증)의 몫입니다.

**아직 안 된 것**: **USD 트랙은 이것으로 코드가 전부 끝났습니다.** 남은 것은 원화·달러 공통으로 **6단계(실검증)** 와 **7단계(문구 최종 검토 → 3단계 공개 전환)**, 그리고 위 🔵 오너 확인 항목들입니다.

**✅ 오너 확정 (2026-08-22) — 7단계, 1갈래("덤벼라 나 자신") 3단계 공개 전환 완료.** 리밸런싱 매도 라운드(§2-3-1) + 성적표 카드 가격 버그 수정 + 예상 금액 표시 UI까지 오너가 직접 써보고 검수를 마친 뒤 확정했습니다. `web/layout.py`의 `DUEL_MENU_ADMIN_ONLY`를 `True → False` 한 글자만 바꿔(2-8·7단계에서 예고한 그대로) `/duel`(참전하기)이 이제 관리자 로그인 없이 모든 로그인 사용자에게 보입니다. **2갈래(공개 동의 `/duel/consent` · 공개 순위표 `/duel/leaderboard`)는 이 전환과 별개이며 그대로 관리자 전용으로 남아 있습니다** — §5-1의 오너 방침("일정 사람이 쌓이기 전까지 공개를 하지 않는 방향으로")이 아직 유효하기 때문입니다. `tests/test_web_session_isolation.py`의 관련 고정 검사 값도 이 전환에 맞춰 함께 뒤집었습니다.

**✅ 오너 확정 (2026-08-22, 같은 날 이어서) — 2갈래(공개 동의 · 공개 순위표)도 3단계 공개 전환.** 오너 요청으로 두 화면·발행 배치 전체를 결투 서브에이전트 + 직접 코드 재확인으로 검토한 뒤 진행했습니다. 확인한 것: ① 500명 미만 (창유형×체급) 그룹은 `duel_publish.py::split_groups_by_threshold()`가 애초에 발행하지 않고, 이를 우회하는 강제 발행 경로·화면 실시간 집계는 존재하지 않습니다(코드 근거로 재확인). ② 발행표에는 `user_id`/`account_id` 컬럼 자체가 없고 payload whitelist + `_assert_no_identity_fields()` + 컬럼 명시 select 로 삼중 방어됩니다. ③ 세션 격리(RLS + 명시 필터 + 화면 이중 확인), XSS 방어(`esc()`, 닉네임은 사용자 입력이 아니라 서버 난수)도 확인 완료. `web/layout.py`의 `DUEL_CONSENT_MENU_ADMIN_ONLY`·`DUEL_LEADERBOARD_MENU_ADMIN_ONLY`를 각각 `True → False`로 전환했습니다. 검토 중 실제 버그 하나(원화 계좌 2개 이상 시 두 번째 이후 카드 저장이 마지막 카드를 다시 그리는 새로고침 문제 — 파이썬 늦은 이름 결정)를 찾아 같이 고쳤고(USD 경로가 쓰던 `_consent_section()` 팩토리를 원화 경로에도 적용), 오너 검토 요청에 따라 책임 고지·철회 안내를 포함한 초안 문구 전체를 문장 단위 줄바꿈으로 다시 표현하고, 렌더링되지 않던 마크다운 `**` 표시(평문 라벨이라 굵게가 아니라 리터럴 별표로 보이던 기존 버그)를 제거했습니다 — 뜻은 한 글자도 바꾸지 않았습니다. 이로써 **7-2(문구 최종 검토)도 완료**로 정정합니다(위 §5-2-5·5-8 주변의 "초안"·"오너 최종 검토 대기" 표기는 각 화면 파일 안에서 정정 완료). ⚠️ **오너가 Render 대시보드에서 직접 확인·설정해야 하는 것**: `DUEL_CONSENT_ENABLED`/`DUEL_LEADERBOARD_ENABLED` 환경변수가 `true`가 아니면 이번 코드 전환과 무관하게 두 화면이 계속 "준비중"으로만 보입니다 — 저장소 안에서는 이 값을 알 수 없습니다.

### 5-20. 🔴 2026-08-23 정정 — Branch 2("내 밑으로 눈 깔어") 공개 대상을 결투 가상계좌에서 "내 성적표"(실제 자산)로 전면 전환. 이 절 전체가 은퇴됨

**무엇이 드러났나.** 바로 위 §5-10(2026-08-20)에서 완성해 §5-19(2026-08-22 오너 확정)까지 실제로 전체 공개까지 전환했던 공개 순위표는, 순위표 최상단에 **오너가 원문 그대로 고정한 문구**("실제로 공개되어있는 '내 성적표'의 데이터는 개인이 등록한 것입니다…")를 달고 있었습니다. 그런데 이 문구가 처음 이 문서에 등장한 자리(당시 5-3 근처, 2026-08-19 3차 라운드)부터, 실제 코드가 공개하는 것은 **결투 가상계좌의 성적**(순위·수익률·보유종목·수량·매입금액)이었고 "내 성적표"의 실제 자산은 **원금 구간 계산에만, 그것도 별도 독립 동의(위 §5-2-4)가 있어야만** 쓰이는 구조였습니다. 즉 **오너가 처음 지시서에 적은 고정 문구 자체가, 그 문구가 붙을 화면이 실제로 하는 일과 처음부터 어긋나 있었습니다.** 이번 세션 오너가 실사용 중(모바일 포함) `/duel/leaderboard`를 보고 "이거 가상계좌 얘기가 아니라 내 성적표 얘기 아니었어?"라고 정확히 짚어냈고, AI가 즉시 동의하거나 즉시 고치지 않고 `utils/duel_publish.py`·이 문서(당시 §5-3, 지금 §5-2-4)를 직접 재확인해 "이 불일치는 이번 세션이 만든 게 아니라 원본 지시서에 있던 것"이라는 근거를 오너에게 먼저 보고했습니다.

**오너 결정 (AskUserQuestion 2라운드 + 중간 확인, 전부 이번 세션):**
1. 공개 대상을 실제로 "내 성적표"(실제 자산)로 바꾼다 — 가상계좌 성적 공개는 **하지 않는다**("가상계좌의 공개는 할 필요도 없어", "내용의 착오를 만들 수 있으니 빼버려야해").
2. 결투 가상계좌 기능(`/duel`, 주문·매수·리밸런싱 매도) 자체는 **손대지 않고 그대로 둔다** — "나와의 결투"라는 원래 목적(내 실제 투자 방식과 다른 모의투자를 스스로 비교) 그대로 개인 연습 도구로 유지.
3. 그룹 기준은 **체급(원금 구간)만** — "내 성적표"에는 M1/M3/M6 같은 창유형 구분이 없으므로.
4. 수익률 계산은 **"내 성적표"가 이미 쓰는 방식 그대로**(매입원가 대비, TWR 아님).

**은퇴 대상 (이 문서 §5 전체가 서술하던 것, 코드는 이미 삭제·DB는 오너가 수동 DROP 예정):**
- 화면: `web/pages/duel_consent_page.py`(§5-9·§5-19), `web/pages/duel_leaderboard_page.py`(§5-10·§5-19) — **파일째 삭제**.
- 표: `duel_public_consent`, `duel_public_leaderboard`, `duel_public_holdings`, `duel_bracket_assignments`, `duel_nicknames`(§1-6~1-8) + USD 대응표(§5-12) — `sql/scorecard_public_schema.sql`로 DROP 문 작성 완료, **오너가 Supabase SQL Editor에서 직접 실행해야 실제로 지워짐(아직 미실행)**.
- 배치: `run_duel_publish_batch.py`(§5-10)·`.github/workflows/duel_publish_daily.yml`, USD 대응(§5-17) — 삭제.
- `web/layout.py`의 `DUEL_CONSENT_ENABLED`/`DUEL_CONSENT_MENU_ADMIN_ONLY`/`DUEL_LEADERBOARD_ENABLED`/`DUEL_LEADERBOARD_MENU_ADMIN_ONLY`(§5-10) — 제거.
- 테스트: `tests/test_duel_consent_page_usd.py`(§5-19)·`tests/test_duel_leaderboard_page_usd.py`(§5-19) 삭제, `tests/test_duel_public_ui.py`(§5-10) 트리밍(공개 계층 검증만 제거, `/duel` 자체 검증과 무관한 부분은 유지 + "정말로 은퇴했는가" 확인 테스트 3개 신규 추가).

**절대 안 건드린 것:** `/duel` 화면(§2-8·§5-18) 자체, `duel_accounts`/`duel_orders`/`duel_positions`/`duel_cash_ledger`/`duel_daily_snapshots`(§1-1~1-5), 야간 정산 배치(§2-5·§5-15), `DUEL_ENABLED`/`DUEL_MENU_ADMIN_ONLY`(이미 전체 공개 상태 그대로), `utils/duel_rules.py`의 통화·계좌 무관 순수 함수(체급 판정·최소 인원·시즌 계산 등은 새 모듈이 그대로 import해서 재사용 — 사본을 새로 만들지 않았습니다).

**교체 결과 — 새 모듈 `scorecard_public_*` / `scorecard_publish` 계열** (결투의 검증된 패턴을 1:1로 미러링, 계좌 축이 없어지는 만큼 오히려 더 단순해짐):
- `sql/scorecard_public_schema.sql`(신규) — 위 DROP 5종 + 신규 CREATE 5종(`scorecard_nicknames`/`scorecard_public_consent`/`scorecard_bracket_assignments`/`scorecard_public_leaderboard`/`scorecard_public_holdings`) + RLS(계좌 소유 헬퍼 대신 직접 `auth.uid()=user_id`). **오너가 아직 실행 안 함 — 이후 코드가 실제로 동작하려면 반드시 먼저 실행해야 합니다.**
- `utils/scorecard_publish.py`(신규, 858줄) — 신규 순수 함수 `resolve_portfolio_return_pct()`(포트폴리오 단위 수익률 = `total_profit / total_cost_priced`, 분모 0/None 이면 결과도 None) · `resolve_bracket_cost_basis()`(체급 입력은 무조건 합 `total_cost`, 가격 유무와 무관 — 오늘 시세 커버리지에 따라 체급이 흔들리지 않도록) 등. `duel_rules.py`의 체급·최소인원·시즌·순위 함수는 **그대로 import**.
- `utils/scorecard_publish_db.py`(신규, 1053줄) — 동의 CRUD·닉네임·발행 조회/쓰기. 계좌 루프가 없어 시그니처가 전부 `account_id` 대신 `user_id`, 창유형 인자 없음.
- `run_scorecard_publish_batch.py`(신규) + `.github/workflows/scorecard_publish_daily.yml`(신규, 독립 cron — **`.github/workflows/` 는 `device_commit_files` 로 못 넣으므로 별도 방식으로 전달 필요**, 5-15·5-17 때 USD 워크플로우를 전달했던 것과 같은 방식).
- `web/pages/scorecard_consent_page.py`(신규, 743줄) — 카드 한 장(계좌 루프 없음), **5개 항목만**(6번째 "실제 매입총합" 독립 동의는 삭제 — 이유: 공개되는 데이터 자체가 이미 실제 자산이라, 매입금액 5개 항목에 동의한 순간 원가합계는 이미 공개된 값의 단순 합이 되어 별도 동의를 세울 대상이 남지 않음).
- `web/pages/scorecard_leaderboard_page.py`(신규, 721줄) — 창유형 선택기 없음(통화·체급만), `twr_display()` → `return_display()`로 개명(더 이상 TWR이 아니므로), 순위 산정 방식 안내 문구를 매입원가 대비 수익률로 새로 작성. **맨 위 고정 문구 2문단은 글자 하나 안 건드리고 그대로 재사용** — 이번 전환으로 그 문구가 **처음으로 실제 구현과 맞는 말이 됨**.
- `web/layout.py` — `DUEL_CONSENT_*`/`DUEL_LEADERBOARD_*` 제거, `SCORECARD_CONSENT_ENABLED`/`SCORECARD_CONSENT_MENU_ADMIN_ONLY`/`SCORECARD_LEADERBOARD_ENABLED`/`SCORECARD_LEADERBOARD_MENU_ADMIN_ONLY` 신설(모두 **관리자 전용으로 시작** — `DUEL_ENABLED`는 참조하지 않음, "내 성적표"는 결투 스위치와 무관한 별개 데이터이므로).
- `main.py` — import를 `duel_consent_page`/`duel_leaderboard_page`에서 `scorecard_consent_page`/`scorecard_leaderboard_page`로 교체.

**검증 (AI가 직접 실행, 서브에이전트 보고를 그대로 믿지 않고 재확인):**
- `python -m py_compile` — 신규·변경 파일 전부(화면 2종·`layout.py`·`main.py`·신규 테스트) 클린.
- `pytest tests/test_scorecard_public_ui.py -q` → **48개 전부 통과**.
- `pytest tests/ -k duel -q` → **886 통과, 365 deselected, 오류 0**(은퇴 전 결투 스위트 기준선 1006 통과였던 것과 비교해, 줄어든 개수는 정확히 삭제된 화면·테스트 파일분과 일치).
- 저장소 **전체 스위트** → **2 failed, 1249 passed, 3 errors** — 실패·오류 목록이 이 전환 **이전부터 있던, 세션 순서에 따라 흔들리는 NiceGUI 전역 슬롯스택 문제**(`test_upload_widget_is_really_not_rendered_when_flag_is_off`·`test_macro_render_smoke`·`test_render_smoke`·`test_report_render_smoke`·`test_duel_render_smoke`)와 **byte-identical** — 이 전환이 만든 새 실패는 0건.
- 실제 파일을 직접 읽어 `esc()` 적용 자리 확인: `scorecard_consent_page.py` 551·567·569행(닉네임·시각), `scorecard_leaderboard_page.py` 357-360·545·632행(종목명·종목코드·닉네임·체급라벨) — `ui.html()` 호출은 파일 전체에서 **692행 단 한 곳**뿐이고 그 직전에 전부 `esc()`를 거칩니다.
- 악성 `stock_name`(`<img src=x onerror=alert(1)>`)을 실제 `holding_row_cells()`/`holdings_table()`에 직접 넣어 실행 → 출력 HTML에 `&lt;img …&gt;`로 이스케이프되어 있고 raw `<img onerror`는 어디에도 없음을 직접 확인.
- `main.py`·`tests/test_duel_public_ui.py`·`tests/test_web_session_isolation.py` 안에 은퇴한 화면 모듈 이름(`duel_consent_page`/`duel_leaderboard_page`)이 남아있는지 저장소 전체를 grep — 주석(설명용)·"삭제됐음을 확인하는 테스트 코드" 자리에만 남아 있고, import·호출은 전부 사라졌음을 확인.

**아직 안 끝난 것:**
1. 오너가 Supabase SQL Editor에서 `sql/scorecard_public_schema.sql` 실행(위 DROP 5종 + CREATE 5종) — 실행 전까지는 이 화면들을 열어도 DB 쪽에서 실패합니다.
2. 오너가 Render에서 `SCORECARD_CONSENT_ENABLED`/`SCORECARD_LEADERBOARD_ENABLED` 환경변수 설정(기본 꺼짐) — 결투 때와 같은 절차.
3. **6단계(실검증)**를 이 새 모듈에 대해 처음부터 다시(등록 → 5개 동의+최종확인 → 배치 실행 → 순위표에서 본인 닉네임 확인 → 철회 → 사라짐 확인).
4. `.github/workflows/scorecard_publish_daily.yml`을 GitHub에 반영 — `device_commit_files`가 `.github/workflows/` 경로를 막으므로 §5-15/§5-17 때처럼 별도 방식(오너 컴퓨터에 직접 쓰기 등)으로 전달 필요.
5. git 반영 시 **삭제된 파일**(`duel_consent_page.py`·`duel_leaderboard_page.py`·`test_duel_consent_page_usd.py`·`test_duel_leaderboard_page_usd.py`)을 `git add -A`(또는 `git rm`)로 함께 커밋해야 합니다 — 새 파일만 add하면 저장소에 죽은 코드가 남습니다.

---

## 6단계 — 실검증 (미착수)

오프라인 테스트로 대체할 수 없는 것들만. 이전 작업지시서들과 마찬가지로 **오너가 실기기로 직접 확인**해야 하는 항목입니다.

1. 스테이징에서 계좌 3개 개설 → 실제 주문 저장 → **다음 날 아침에 실제 종가로 체결됐는지** 확인. 체결가를 그날 코스피 종가와 **육안 대조**. (`SCORECARD_V2_OCR_WORK_ORDER.md` 5단계에서 "매입가 vs 현재가 육안 확인"이 가장 중요한 검증 포인트였던 것과 같은 성격입니다.)
2. 서로 다른 브라우저 세션(시크릿창)으로 **두 계정 동시 로그인** → 계좌·주문·현금이 섞이지 않는지(§0-3-8 필수 절차).
3. 정기 입금일(매월 10일) 전후 동작 — 3계좌 전부 80만원, 중복 없음.
4. 수집 실패/휴장일에 주문이 **엉뚱한 가격으로 체결되지 않는지**(2-4의 🔴 결정 사항이 정해진 뒤).
5. (5단계 포함 시) 동의 boolean 조합을 실제로 바꿔가며, **동의하지 않은 항목이 순위표에 절대 안 뜨는지**. 그리고 **철회 → 과거 기록 완전 소멸** 확인.
6. Supabase 쿼리 수·응답시간 확인 — 배치가 사용자 수에 비례해 쿼리를 늘리고 있지 않은지(§0-3-2).

---

## 7단계 — 오너 승인 → 공개 전환 (미착수)

1. **§0-3-6 절차 (2026-08-19 6차 확정, 3단계 공개)**: 기본 숨김 플래그(`DUEL_ENABLED`)로 스테이징 → `DUEL_ENABLED=true` 전환 후에도 **먼저 관리자 전용으로 공개**(기존 관리자 페이지 패턴 재사용) → 오너가 직접 써보고 이상 없으면 → `web/layout.py`의 `_MENU`에서 관리자 제한을 풀어 **전체 사용자에게 공개**. 깃허브 푸시는 이 단계들과 무관하게 평소처럼 계속합니다 — 저장소에 코드가 있는 것과 사이트에서 누가 볼 수 있는지는 별개 문제입니다.
2. 🔴 **공개 전 문구 검토(오너 필수 확인)** — 엔지니어링이 아니라 문구 작업이지만, 안 하고 배포하면 안 됩니다.
   - 매수 창·이월 규칙을 일반 사용자 언어로 설명한 문구.
   - "리밸런싱"이 아니라 **적립식 매수**임이 드러나는 문구(2-3 네이밍 주의).
   - 배당 미반영 고지, 야간 체결 고지, ~~매도 불가 고지~~ → **⚠️ 2026-08-21 정정(§2-3-1)**: 창당 1회 리밸런싱 매도 안내로 교체.
   - 동의 체크박스 5개 + 최종 확인 + 실제 매입총합 별도 동의의 **정확한 문장**. 이건 법적·신뢰 측면에서 오너가 직접 읽고 승인해야 합니다.
3. **1갈래만 먼저 공개해도 됩니다.** 2갈래(공개 순위표)는 참가자가 최소 인원을 넘을 때까지 어차피 화면에 아무것도 못 띄웁니다. 1갈래 단독 배포 → 사용자 모집 → 그 뒤 2갈래 공개가 자연스러운 순서입니다.
4. 작업 종료 시 GitHub 푸시까지 완료(§0-3-5), 작업완료서 정리(§0-3-7), `PROJECT_STATUS.md` 갱신 — **특히 service_role 파일 목록**(2단계 표 참고).

---

## 🔴 미결정 항목 모아보기 — 오너 결정 대기

**아래 항목에 임의의 값을 채워 넣고 진행하지 마세요. 지어내지 않는 것이 이 프로젝트의 최우선 규칙입니다(§0-1).**

**✅ 2026-08-19 2차 라운드에서 해소된 것**:

- ~~순위표/구간 공개 최소 참가 인원~~ → **500명**으로 확정. (5-6)
- ~~`pending` 주문 수정·취소 가능 여부~~ → **둘 다 허용, 접수 시간대 안에서 자유롭게.** (1-3, 2-4)
- ~~주문 입력 단위(금액 vs 수량)~~ → **주식 수량 기준**으로 확정, 예수금 부족 시 **부분체결**. (1-3, 2-4)
- ~~상장폐지 탐지 메커니즘~~ → **v1은 자동 탐지 없이 수동 처리.** (3-2)
- ~~매수 창의 길이~~ → **사실상 무제한**(예수금 있으면 항상 매수 가능). (2-3)
- ~~M1/M3/M6 계좌가 규칙상 동일해도 되는지~~ → **의도된 설계**(차별화는 종목 선택에서 나옴). (2-3)

**✅ 2026-08-19 3차 라운드에서 추가로 해소된 것**:

- ~~야간 배치가 종가를 못 얻었을 때 이월 vs 만료~~ → **자동 취소·실패 처리**(이월 안 함), **사전 고지 + 체크 필수.** (2-4)
- ~~휴장일 vs 수집 실패 구분~~ → **52개 지수·종목 무변동 검사 알고리즘으로 판정, 굳이 구분하지 않고 둘 다 "그날 체결 없음"으로 동일 처리.** (2-9, 신설)
- ~~거래일 귀속 날짜 경계~~ → **주문 접수는 D일 18:00:01~22:00:00, 체결은 D+1 종가로 확정.** (2-4 전면 개정)
- ~~유니버스 이탈 처리~~ → **201~500위 3개월 추적(전종목 가격 파일 폴백), 500위 밖은 마지막 종가로 강제 정리 후 현금화. 수집 범위(200)는 설정값으로 빼서 확장 가능하게.** (3-3)
- ~~상장폐지 외 기업행위 범위~~ → **가격·수량이 실제로 바뀌는 사건(분할·병합·무상증자·감자)만 대상, 나머지는 기존 로직이 자연히 커버.** (3-4)
- ~~정기 입금 반영 시각~~ → **매월 10일 00:00:01, 같은 날 저녁 접수 시간대부터 사용 가능.** (2-2)
- ~~동의 항목 간 의존관계~~ → **"일부 공개" 조합 없음 — 5개 항목은 전부 공개하거나 전부 비공개, 둘 중 하나.** (5-2)
- ~~원금 구간 경계값~~ → **8구간 확정**(100만원 미만 ~ 1억원 이상), 순위표 최상단 고정 고지 문구도 확정. (5-3)

**✅ 2026-08-19 4차 라운드에서 추가로 해소된 것**:

- ~~"매입원가합계 기준 랭킹"이 구간 기준인지 순위 기준인지~~ → **AI 해석(구간 기준)이 맞다고 확인**. 추가로 **체급은 시즌 단위로 고정**된다는 규칙이 새로 확정됨(시즌 중엔 매입원가가 변해도 체급 유지, 순위만 TWR로 갱신). (5-3)
- ~~TWR 세부 계산 규약~~ → **누적 TWR만 표시**(연환산 없음, v1). 계좌 첫날·상장폐지 상각일 처리 규약을 AI가 기본 모델로 확정, 화면에 띄워보고 조정 예정. (2-6)

**✅ 2026-08-19 5차 라운드에서 해소된 것**:

- ~~"정기 시즌"의 주기~~ → **1년으로 확정**(전체 자산 체급이라 짧은 주기로 흔들면 의미가 없다는 오너 판단). 매년 1회 그 시점의 매입원가합계로 체급 재산정, 그 사이엔 고정. (5-3)

**여전히 남은 것:**

1. **남은 UI 문구 일부** — 요구사항은 다 확정됐지만, 매수 창·이월 설명이나 크롤링 실패 사전고지 같은 몇몇 안내의 **실제 화면 문구(정확한 문장)**는 아직 초안이 없습니다. 코딩을 막지 않는 항목이라 실제 화면을 만들면서 채워도 됩니다. (7-2)

2. **✅ 2026-08-20 확정 — "옵트인 즉시 시드머니가 들어가는가?" → (B)안, 버튼 클릭 즉시 지급.** `sql/duel_schema.sql`을 실제로 짜보니, `duel_cash_ledger`(현금 원장) 테이블은 **`service_role`만 쓸 수 있고 `authenticated`(로그인한 사용자 본인)에게는 쓰기 권한을 주지 않았습니다.** 이건 실수가 아니라 의도한 안전장치입니다 — 만약 사용자가 자기 브라우저에서 직접 이 표에 쓸 수 있으면, 이론적으로 시드머니를 무한정 스스로에게 지급하는 것도 막을 방법이 없어집니다(§0-3-8 상위 원칙 — 돈이 걸린 표는 서버만 쓴다).

   **처음엔 (A, "다음 배치 때 반영")를 추천했으나, 오너가 타당한 반론을 제기해 뒤집었습니다.** 논리: 이 모듈의 부정 매매 방지 장치는 **주문 접수 시간대(D일 18:00:01~22:00:00) + D+1 종가 체결**이지, "예수금이 언제 들어오는가"가 아닙니다. 시드머니가 참여 즉시 들어오든 다음날 배치 때 들어오든, 그 돈으로 실제 주문을 넣을 수 있는 시점은 항상 "다음 접수 시간대부터"고 체결도 항상 D+1의 아직 모르는 종가로만 되므로, **자금 지급 시점은 이 안전장치와 아무 관계가 없습니다.** 즉 (A)를 추천했던 진짜 이유는 보안이 아니라 "이미 만든 배치 함수를 그대로 재사용하는 게 편해서"(§0-3-10)였는데, 그건 "가입했는데 왜 0원이지"라는 사용자 경험을 희생할 만한 근거가 못 됩니다.

   **⚠️ 2026-08-20 구현 중 이 항목의 메커니즘이 한 번 더 바뀌었습니다.** 처음엔 "버튼 누르면 서버가 `service_role` 배치 함수를 동기 호출"이라고 적었는데, 실제로 짜보니 이 방식은 **`service_role` 키를 사용자가 접속하는 앱 서버(Render) 프로세스 안에 두게 만듭니다** — 이건 이 프로젝트가 처음부터 지켜온 "service_role 키는 GitHub Actions 시크릿에만 있고, 사용자가 접속하는 서버의 환경변수에는 절대 안 둔다"는 격리 원칙(`utils/duel_db.py`/`utils/report_db.py` 상단 경고 주석)을 정면으로 깨는 방식이었습니다. 그래서 구현 단계에서 이 방식을 폐기하고 아래 방식으로 바꿨습니다.

   **✅ 최종 확정 방식 — `SECURITY DEFINER` Postgres 함수(DB 안의 특권 함수)를 RPC로 호출**: `sql/duel_schema.sql`에 `public.duel_opt_in()`이라는 함수를 새로 추가했습니다. 이 함수는 **인자를 하나도 받지 않습니다** — "누구에게 얼마를 줄지"를 호출자가 정할 수 있는 문법 자체가 없다는 뜻입니다. 대신 함수 내부에서 로그인 세션의 `auth.uid()`(RLS 정책이 쓰는 것과 똑같은 값)만 보고 "이 사람"에게만 계좌 3개+시드 3행을 만듭니다. 이 함수는 DB 소유자 권한으로 실행되도록 표시돼 있어서(`SECURITY DEFINER`) `duel_cash_ledger`에 쓸 수 있지만, `authenticated`에게는 **이 함수를 실행할 권한만** 주고 표에 대한 직접 쓰기 권한은 여전히 안 줍니다. 결과적으로: **앱 서버는 사용자 본인의 로그인 세션(anon 키)만으로 이 함수를 호출하면 되고, `service_role` 키는 앱 서버 어디에도 필요 없습니다** — 원래 지키려던 격리 원칙이 완전히 그대로 유지됩니다. 이 패턴은 이 스키마에서 완전히 새로운 게 아니라, 이미 쓰고 있던 "특권이지만 아주 좁은 일만 하는 함수"(예: 주문 계좌 소유권 확인 함수) 방식의 두 번째 사례입니다.

   실제로 로컬 PostgreSQL에 이 함수를 설치하고 여러 시나리오로 직접 검증했습니다: 로그인 안 한 상태로 호출하면 깨끗하게 거절됨, `anon` 역할은 아예 실행 권한이 없음, 같은 사용자가 두 번 세 번 눌러도 중복 지급 안 됨(멱등), 사용자 A가 사용자 B의 계좌·원장을 볼 수 없음, "다른 사람 id를 넣어서 호출"하는 문법 자체가 존재하지 않음(인자가 없으므로), 두 세션이 동시에 눌러도(경합 상황) 정확히 1번만 지급됨, 사용자가 `duel_accounts`에 가짜 시드 금액을 직접 써넣어도 실제 지급액은 여전히 DB 안의 진짜 상수값(1천만원)만 사용됨 — 전부 확인 완료.

   **🆕 이번 구현으로 새로 생긴, 아직 오너 확인이 필요한 것 하나** — 지금 스키마는 `duel_accounts`에 대해 로그인한 사용자 본인이 **직접 계좌 행을 만들 수 있는 권한**도 열어뒀습니다(1단계 초안 당시의 결정). 이제 `duel_opt_in()` 함수가 생겼으니, 정상적인 참여 절차라면 이 직접 권한을 쓸 이유가 없습니다. 이 권한을 열어둔 채로 두면 — 실제 지급액에는 영향이 없다는 걸 확인했지만(위 검증 참고, 진짜 돈은 항상 DB 상수값으로만 지급됩니다) — 사용자가 자기 계좌 행의 `seed_amount`나 `anchor_date`(개설일, TWR 계산의 시작점) 같은 **화면에 보여지는 값**을 조작해서 넣을 수 있는 여지는 남습니다. 권장: 이 직접 쓰기 권한을 다음 라운드에서 회수하고 `duel_opt_in()`을 유일한 참여 경로로 만드는 것. 급하지 않으니 다음에 확인해주시면 됩니다.

   **참고(운영 메모)**: `sql/duel_schema.sql`을 Supabase SQL Editor에 붙여넣어 실행한 직후 몇 초간 "함수가 아직 설치 안 됐다"는 안내가 뜰 수 있는데(PostgREST의 캐시가 새 함수를 인식하는 데 시간이 걸림), 화면 문구는 이미 이런 경우를 한국어 문장으로 안내하도록 만들어져 있고 저절로 풀립니다. 또한 SQL Editor 자체에서는 이 함수를 테스트할 수 없습니다(SQL Editor는 `postgres` 역할로 실행되는데, 그 안에서는 `auth.uid()`가 없어서 함수가 항상 거절하도록 짜여 있습니다 — 이건 버그가 아니라 의도된 동작입니다). 실제 테스트는 반드시 로그인한 앱 화면에서 해야 합니다.

3. **🆕 2026-08-20 야간 배치 구현 중 새로 생긴 것 — 크롤링 판정이 애매할 때(`needs_review`) 그날 주문을 어떻게 할까?** 지금은 "취소하지 않고 대기 상태 그대로 둔다, 관리자가 값을 직접 확인한 뒤 그 날짜만 수동으로 재실행해서 체결/취소 중 하나로 결론 낸다"는 보수적인 방식으로 짜여 있습니다(한번 취소하면 되돌릴 수 없으니, 애매한 상황에선 되돌릴 수 있는 쪽을 택함). **남은 질문**: 관리자가 확인을 깜빡하고 아무 조치도 안 하면 그 주문은 영원히 대기 상태로 남는데, 이게 괜찮은지, 아니면 "N일 지나면 자동으로 취소"같은 안전장치를 넣을지. 급하지 않으니 다음에 편하실 때 답해주시면 됩니다.

4. **🆕 2026-08-20 같은 라운드에서 발견 — "상위 50 종목" 순위 기준과 야간 배치 정확한 실행 시각.** 코딩 에이전트가 이 세션에는 없는 실제 데이터 파일(`data/kospi200_pegy_latest.json` 등)과 크롤링 워크플로우(`scrape.yml`)를 직접 보지 못해서 추정으로 채운 두 가지입니다 — 저장소에 다시 접근하시면 실제 파일과 대조해서 확정이 필요합니다(위 2단계 진행 로그의 "오너가 저장소를 다시 열면 꼭 확인해야 하는 것들" 참고). 코딩을 막는 항목은 아니라 지금 이대로 커밋해도 되고, 나중에 값만 바꾸면 됩니다.

5. **🆕 2026-08-20 5단계 백엔드 구현 중 새로 생긴 것 4가지 — ✅ 전부 확정** (상세 배경은 5-9절 참고):
   - ~~동의 철회 시 발행표 삭제를 즉시로 할지, 지금처럼 다음 야간 발행 배치 때까지(최대 하루) 둘지~~ → **"다음 배치 때 지우는 지금 방식 그대로"로 확정.**
   - ~~시즌 시작일~~ → **매년 3월 1일로 확정**(1월 1일에서 변경, 코드·테스트 반영 완료).
   - ~~무작위 닉네임 단어 목록(형용사+명사)의 톤·수위~~ → **숫자 접미사를 아예 없애고 형용사 2개+명사 1개 구조로 재설계, 단어 목록도 대폭 확충**(5-5절 참고). "숫자가 기계적으로 느껴진다"는 지적을 그대로 반영했습니다.
   - `bracket_key`(코드용 ASCII 식별자)와 `bracket_label()`(화면용 한글 라벨)을 분리한 것 — 이름 자체에 이견 없으면 그대로 갑니다.

6. **🆕 2026-08-20 USD 트랙 4차 코딩(배치 오케스트레이션) 중 제가 직접 도출한 설계 — 오너 확인 필요** (상세 근거는 위 5-15절 참고):
   - USD 야간 배치의 `--target-date` 기본값을 **"어제"(한국 날짜)**로 정했습니다(원화는 "오늘"). 근거: 미국 정규장 확정 종가 수집(`scrape_us.yml`, 오너 컴퓨터 실제 파일 확인)이 한국 시각 다음 날 새벽(최악 08:05)에야 끝나기 때문입니다.
   - 정기 입금($500, 매월 10일) 판정만은 `target_date`가 아니라 **배치가 실제로 도는 한국 날짜**(`--today-date`, 신규 파라미터)를 씁니다 — 현금 이벤트는 시장 이벤트와 분리해야 한다는 기존 원칙(2-2-4)을 지키기 위해서입니다.
   - `.github/workflows/duel_daily_us.yml`의 cron을 **매일(평일 아님) 03:00 UTC(= KST 정오)**로 정했습니다.
   - **이 세 가지 모두 오너가 명시적으로 확정한 값이 아닙니다** — 실제 수집 스케줄 사실로부터 이번 라운드에 제가 도출한 것이니, 저장소를 다시 여시면 한 번 확인해주세요. 급하지 않고, 코딩을 막는 항목도 아닙니다.

7. **🆕 2026-08-21 USD 트랙 6차 코딩(발행 배치) 중 제가 직접 계산한 cron — 오너 확인 필요** (상세 근거는 위 5-17절 참고):
   - `.github/workflows/duel_publish_daily_us.yml`의 cron을 **매일 03:50 UTC(= KST 12:50)**로 정했습니다. 계산: 선행 USD 체결 배치(`duel_daily_us.yml`, cron `0 3 * * *` + `timeout-minutes: 20`)의 **최악 종료 03:20 UTC** + **원화 트랙이 쓰는 여유 30분**(이 30분은 2026-08-20 오너가 직접 확정한 값 — §5-10). 뒤쪽으로는 이 배치가 타임아웃을 다 써도 04:10 UTC(= KST 13:10)에 끝나므로 다음 USD 주문 접수 시작(16:00:01 KST)보다 약 2시간 50분 앞섭니다.
   - **위 6번(체결 배치 cron 03:00 UTC)이 바뀌면 이 값도 반드시 함께 다시 계산해야 합니다** — 두 값은 "선행 종료 + 30분"으로 묶여 있고, 테스트가 그 **관계**를 고정하고 있어서 한쪽만 바꾸면 실패합니다.
   - 매일(평일 아님) 돌리는 것도 선행 체결 배치와 맞춘 판단입니다 — 발행만 평일이면 미국 금요일 마감 성적이 월요일까지 순위표에 안 실립니다.

---

## 참고 — 이번엔 명시적으로 범위 밖

- **배당금 자동 반영** — 지급 캘린더(사건별 날짜 + 확정 주당 금액)를 만들려면 DART/KIND·KRX·네이버 배당 페이지 중 하나로 **완전히 새로운 파이프라인**이 필요하고, 한국 기업은 배당락 시점에 금액이 미정인 경우가 많아 **"선언됨, 금액 미정" 상태 모델링**까지 요구됩니다. **별개 프로젝트입니다.** v1은 반영하지 않고, 화면에 그렇게 씁니다. (0단계 2번)
- ~~**매도 기능** — 이 모듈에 매도는 영원히 없습니다. 스키마 제약과 DB 트리거로 막습니다. "나중에 확장" 여지도 두지 않았습니다.~~ → **✅ 2026-08-21 오너 확정으로 정정.** 이전 라운드의 대화 착오였습니다 — 창당 1회 리밸런싱 매도가 원래 의도였고, 구현·검증까지 완료됐습니다. 상세는 위 §2-3-1 참고.
- ~~**미국주식·달러 가상거래** — 앱에 **환율 시계열이 없습니다.** 환율을 지어내지 않는 한 불가능합니다(§0-1). v1은 원화·코스피 상위 200만.~~ → **2026-08-20 재검토로 부분 해소.** "KRW·USD를 하나의 순위·체급으로 합치는 것"은 여전히 환율 없이는 불가능하지만, "내 성적표"의 통화 분리 전례를 근거로 **KRW와 절대 병합하지 않는 완전 별개 USD 트랙**이라면 환율 없이 가능하다는 게 확인돼 신규 설계(§5-11)로 반영했습니다. **아직 코드는 없고 설계만 확정된 상태입니다.**
- **실시간·장중 체결** — §0-3-1에 따라 **원칙적으로** 하지 않습니다. 현재의 한계가 아니라 프로젝트의 정의입니다.
- **비활동(휴면) 뱃지** — 채택 안 함(보류가 아니라 폐기). 매수 전용 + 가변 창 설계에서 "비활동"은 정의 불가. (0단계 5번)
- **데이터 보존·아카이빙 정책** — 지금 만들지 않음. 무기한 보관, 자동 정리 없음. 사용자 규모가 실제로 문제가 될 때 설계. (0단계 6번)
- **순위표 표시를 넘어선 타인 포트폴리오 심층 탐색**(사용자별 상세 페이지, 팔로우, 히스토리 브라우징 등) — 이번 범위 밖. 순위표에 걸린 항목의 개별 열람까지가 v1의 전부입니다. 원한다면 **별도 문서로 새 설계 라운드**를 여세요(공개 범위가 넓어지는 만큼 §0-3-8 검토를 처음부터 다시 해야 합니다).
- **외부 AI 모델 연동** — 이 모듈에는 없습니다. 따라서 §0-3-11은 지금 적용 대상이 아닙니다. 나중에 붙이면 그때 적용하세요.
- **시드머니·입금액의 사용자 설정** — v1 고정. 화면에 입력칸을 만들지 마세요.

---

## 다음에 이 문서를 다시 열 때

**2026-08-21 기준 진행 상황: 0단계(설계) ✅ + 1단계(스키마 승인) ✅ + 2단계(Branch 1) ✅ + 5단계(Branch 2, 공개 순위표) ✅ 전부 완료** — 백엔드·화면 2종(동의 관리·순위표 열람)·발행 워크플로우·`main.py` 배선(결투 화면 3개 전부)까지 코드 쪽은 다 끝났습니다. 여기에 더해 **신규 USD 트랙("달러 결투") 설계 확정**(§5-11) + **1차 코딩(스키마) 완료 + 오너가 실제 프로덕션 Supabase에 적용·확인까지 완료**(§5-12) + **2차 코딩(`duel_rules.py` USD 상수 + 닉네임 스키마 변경의 파급 수정) 완료, 주문 접수 시간대 초 단위도 오너 확정 완료**(§5-13) + **3차 코딩(신규 `utils/duel_db_usd.py` — USD 표 DB 접근 계층 + 전용 테스트 133개) 완료**(§5-14) + **4차 코딩(야간 배치 오케스트레이션 — `utils/duel_batch_usd.py`·`run_duel_daily_batch_us.py`·워크플로우 + 전용 테스트 37개) 완료**(§5-15 — 결투 스위트 602개 전부 통과, 회귀 0) + **✅ 5차 코딩(2026-08-21) — 오너가 직접 발견한 "USD 체결 거래일이 하루 늦게 잡히던 버그" 수정 완료**(§5-16 — 신규 `resolve_fill_trading_day_usd()`, 결투 스위트 **607개 전부 통과**, 회귀 0) + **✅ 6차 코딩(2026-08-21) — USD 발행 배치 완료**(§5-17 — 신규 `utils/duel_publish_usd.py`·`run_duel_publish_batch_us.py`·`.github/workflows/duel_publish_daily_us.yml`·`tests/test_duel_publish_usd.py`(108개) + `duel_rules.py`에 신규 `resolve_bracket_for_season_usd()`, 결투 스위트 **715개 전부 통과**, 회귀 0). **🔴 §5-15의 target_date=어제 기본값·정기입금 판정용 today_kst 분리·cron 시각은 여전히 오너 확인 대기입니다**(미결정 항목 6번 — §5-16의 수정은 이 스케줄링 구조를 **바꾸지 않았습니다**, 배치가 처리하는 날짜의 절대값만 하루 당겨졌을 뿐입니다). **🔴 §5-17의 USD 발행 배치 cron `50 3 * * *`(03:50 UTC = KST 12:50)도 같은 성격의 오너 확인 항목입니다** — 선행 USD 체결 배치의 종료 상한(03:20 UTC)에 원화 트랙이 쓰는 여유 30분을 더해 제가 계산한 값이고, 선행 배치 시각이 바뀌면 이 값도 함께 바뀌어야 합니다. + **✅ 7차 코딩(2026-08-21) — `/duel` 화면에 달러 결투 블록 추가 완료**(§5-18 — `web/pages/duel_page.py` 1,112줄 → 2,093줄, 같은 창유형 카드에 위쪽 KRW / 아래쪽 USD 병기, **합산 숫자 없음**(§5-11-2), 신규 `tests/test_duel_page_usd.py` 41개, 결투 스위트 **756개 전부 통과**, **원화 회귀 0건** — 원본 파일로 되돌려 ❌ 목록까지 바이트 단위 대조). + **✅ 8차 코딩(2026-08-21) — 남은 화면 2개 완료**(§5-19 — `web/pages/duel_consent_page.py` 783줄 → 1,234줄(창유형마다 위쪽 원화 / 아래쪽 달러 동의 카드, 동의는 트랙별 완전 독립, 닉네임만 공유), `web/pages/duel_leaderboard_page.py` 524줄 → 702줄(**통화 드롭다운 추가**, 고른 통화의 발행표만 읽음 — 절대 병합 안 함), 신규 `tests/test_duel_consent_page_usd.py` 45개 · `tests/test_duel_leaderboard_page_usd.py` 46개, 결투 스위트 **847개 전부 통과**, **원화 회귀 0건**, `main.py`·`web/layout.py` **변경 불필요**로 확인). **🎉 이것으로 USD 트랙의 코드는 전부 끝났습니다.** 원화·달러 공통으로 남은 것은 **6단계(실검증)**와 **7단계(문구 최종 검토 → 3단계 공개 전환)**뿐입니다.

- **1단계 ✅ 완료 (2026-08-19)** — 스키마 8종 개념(계좌/포지션/예약주문/현금원장/스냅샷/닉네임/동의/발행표) 오너 확인 완료. "여기까지는 될 것 같아" 확인 후 코딩 착수 승인.
- **2단계 진행 중 (2026-08-19, 오푸스 높음으로 2회 코딩 작업)**:
  - `sql/duel_schema.sql` — 표 10개(스냅샷 2종·발행표 2종 포함) + RLS 전부 작성. **실제 로컬 PostgreSQL 16.13에 직접 실행해서 검증**(문서 검토만이 아니라 진짜 DB에 Supabase 역할(anon/authenticated/service_role)을 흉내 낸 스텁까지 만들어서 권한별로 실제 쿼리를 돌려봄). 이 과정에서 진짜 버그 하나 발견·수정: `service_role`이 `bigserial` 시퀀스 쓰기 권한이 없어 원장 행을 못 넣던 문제. **✅ GitHub에 푸시 완료(커밋 `1ec9b21`)**.
  - `utils/duel_rules.py` — 순수 함수(체결/부분체결 계산, FIFO 예수금 배정, 매수 창·주문 접수 시간대 판정, D+1 체결일 계산, 크롤링 신선도 판정(2-9의 52개 검사), TWR 계산). `pytest tests/test_duel.py` → **55개 전부 통과**. **✅ GitHub에 푸시 완료(커밋 `1ec9b21`)**.
  - `utils/duel_db.py` — Supabase 접근 계층. 사용자 세션용(anon+RLS, 주문 저장/수정/취소·내 계좌·내 동의 저장 등)과 배치 전용(`service_role`, 월 입금·야간 체결·스냅샷 기록 등, `report_db.py`와 동일한 격리 규율)으로 절반씩 구성. **아직 GitHub에 푸시 안 됨**.
  - `tests/test_duel_db.py` — 가짜 Supabase 클라이언트로 오프라인 검증. **아직 GitHub에 푸시 안 됨**.
  - `PROJECT_STATUS.md`의 "service_role을 유일하게 쓰는 파일" 서술 2곳(모듈 파일 색인 표 + 리포트 모듈 요약 표) 모두 `duel_db.py`도 같은 패턴을 쓴다는 각주로 갱신 완료.
- **2단계 진행 (2026-08-20, 오푸스 높음으로 3번째 코딩 작업 — 즉시 옵트인)**:
  - 오너가 "매수 시간대가 이미 안전장치인데 왜 시드머니를 굳이 미루냐"고 타당한 반론 제기 → 처음 추천했던 "다음 배치 때 반영"(A안) 폐기, 즉시 지급으로 방향 확정.
  - 구현 1차 시도(서버가 `service_role` 배치 함수를 버튼 클릭 시 동기 호출)는 실제로 짜보니 **service_role 키를 앱 서버 프로세스에 두게 되는 방식**이라 이 프로젝트의 격리 원칙을 깨는 것으로 판명 — 폐기.
  - 최종적으로 **`SECURITY DEFINER` Postgres 함수(`public.duel_opt_in()`, 인자 없음, 내부에서 `auth.uid()`로만 대상자를 정함)를 RPC로 호출**하는 방식으로 구현 — `service_role` 키가 앱 서버에 전혀 필요 없는 채로 즉시 지급 달성. 위 "🔴 미결정 항목" 2번에 전체 검증 내역 기록.
  - `sql/duel_schema.sql`·`utils/duel_db.py`(신규 `opt_in()` 함수, anon 세션용 섹션)·`tests/test_duel_db.py` 수정·확장. 로컬 PostgreSQL 재검증(로그인 없이 호출 거절/`anon` 실행 권한 없음/중복 클릭해도 1회만 지급/사용자 간 격리/동시 클릭 경합 안전/가짜 seed_amount 심어도 실지급액은 DB 상수 고정 등) 전부 통과.
  - `python3 -m pytest tests/test_duel_db.py tests/test_duel.py` → **166개 전부 통과**(기존 151 + 신규 15). `py_compile` 클린. **아직 GitHub에 푸시 안 됨**.
  - **🆕 이번 라운드에서 새로 발견된, 다음에 확인해주실 것 1건** — 위 "🔴 미결정 항목" 2번 마지막 문단 참고: `duel_accounts`에 남아있는 "사용자 본인 직접 insert" 권한을 회수하고 `duel_opt_in()`을 유일한 참여 경로로 만들지 여부. 급하지 않음.
- **2단계 진행 (2026-08-20, 오푸스 높음으로 4번째 코딩 작업 — 야간 배치 오케스트레이션)**:
  - 새 파일 4개: `utils/duel_batch.py`(판정·행동 결정 — 순수 로직, 1,216줄), `run_duel_daily_batch.py`(저장소 루트 실행 스크립트 — 실제 파일·Supabase 접속 담당, 208줄), `.github/workflows/duel_daily.yml`(야간 배치 워크플로우 초안, 149줄), `tests/test_duel_batch.py`(오프라인 검증 74개).
  - `utils/duel_db.py`에 배치용 일괄조회 함수 2개 추가(`fetch_positions_for_accounts`/`fetch_daily_snapshots_for_accounts`) — 기존 함수는 무수정. `utils/duel_rules.py`는 한 글자도 안 건드림(승인된 파일 보호).
  - **"크롤링 어제 값"을 구하는 방법을 새로 설계**: 배치가 매번 `data/duel_freshness_probe_previous.json`이라는 자체 기준값 파일을 읽고(어제 값) 끝에 오늘 값으로 덮어씀 → 워크플로우가 그 파일을 커밋(`scrape_report_snapshots.yml`이 자기 출력 파일을 커밋하는 것과 같은 방식). 상위 50 명단이 매일 조금씩 바뀌는 문제는 "양쪽에 다 있는 종목만 비교, 겹침 45개 미만이면 판정 보류"로 흡수.
  - **판정→행동 표를 명확히 확정**: `ok`=체결 진행, `failed`/`failed_or_holiday`=그날 대기 주문 전부 자동취소(사전 고지된 대로), `needs_review`=**취소하지 않고 대기 상태 그대로 둠**(관리자가 나중에 `--override fill`/`--override cancel`로 그 날짜만 재실행해 결론), `no_baseline`(배치 첫 실행 등 우리 쪽 사정)=마찬가지로 대기 유지. 스냅샷은 체결 실패한 날엔 안 씀(대신 다음 정상일 스냅샷의 현금흐름에 그 사이 입금분을 이월시켜 수익률 왜곡 방지).
  - **검증**: `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_batch.py` → **240개 전부 통과**(기존 166 + 신규 74, 회귀 0). `py_compile` 전부 클린. 워크플로우 yml도 문법 파싱 확인 완료. **아직 GitHub에 푸시 안 됨**.
  - **🆕 이번 라운드에서 새로 발견된, 오너가 저장소를 다시 열면 꼭 확인해야 하는 것들**:
    1. ✅ **야간 배치 실행 시각(cron)을 실제 `scrape.yml`과 대조해 확인 완료(2026-08-20).** 오너 컴퓨터에서 실제 파일을 직접 열어봤습니다 — `scrape.yml`은 `name: "Daily Market Scraper"`, cron `'5 7 * * 1-5'`(= UTC 07:05 = KST 16:05 시작), 실측 소요 20~30분(코드 주석 확인) → 늦어도 KST 16:40 종료. 결투 배치의 cron(KST 17:10)은 그 뒤 약 30분 여유를 두고, 저녁 주문 접수 시작(18:00:01)보다 50분 앞서 끝나도록 잡혀 있었는데, **실제 파일과 대조해보니 그대로 맞았습니다.** 수정 불필요. (`.github/workflows/duel_daily.yml`의 머리말 주석도 "추정"에서 "확인 완료"로 갱신했습니다.)
    2. 🔴 **코스닥 지수가 아직 없어서 신선도 점검이 52개가 아니라 51개(코스피 지수+상위 50종목)입니다.** `PROJECT_STATUS.md`에도 이미 "코스닥 지수는 후속 과제"라고 적혀 있던 기존 한계이고, 이번에 조용히 줄인 게 아니라 실행 때마다 로그에 경고가 찍히게 만들었습니다. 코스닥 수집이 생기면 설정 한 줄만 추가하면 됩니다.
    3. 🔴 **"상위 50 종목" 선정 기준 필드가 `rank`(시가총액 순위)라고 가정했는데, 실제 데이터 파일이 이 세션에는 없어서 파일로 직접 확인은 못 했습니다.** 저장소에 다시 접근하시면 이 가정이 맞는지 한 번 확인해주세요.
  - **🆕 오너가 결정해야 할 새 미결 항목들** (급하지 않음, 아래 "🔴 미결정 항목 모아보기"에도 옮겨 적었습니다):
    - `needs_review`/기준값 없음으로 보류된 주문을 **영원히 대기 상태로 둘지, "N일 지나면 자동 취소" 규칙을 넣을지.**
    - 위 관리자 재실행(`--override`) 명령을 **누가 실행할 수 있어야 하는지**(지금은 "GitHub Actions를 수동 실행할 수 있는 사람" = 사실상 오너 본인과 동일).
    - 누적 TWR을 스냅샷 표에 **저장해둘지**(지금은 화면이 매번 계산 — 데이터가 몇 년 쌓이면 느려질 수 있어 나중에 검토할 사안, 지금 당장은 필요 없음).
- **2단계 진행 (2026-08-20, 오푸스 높음으로 5번째 코딩 작업 — 화면)**: **`web/pages/duel_page.py` 완성 — 2단계의 마지막 파일입니다.** `scorecard_page.py`와 같은 로그인 게이트·세션 격리 규율을 그대로 따랐고, `utils/duel_db.py`/`utils/duel_rules.py`는 한 줄도 안 건드리고 호출만 합니다. 화면 구성: 3종 상시 고지문 → 참여 안내/버튼(계좌 없을 때) → 계좌 3개 나란히 비교(총자산/예수금/평가액/누적 TWR/보유종목) → 매수 주문 폼(종목 검색 + 수량 + 크롤링 실패 사전고지 체크박스) → 계좌별 대기주문(수정/취소)·최근 체결 목록. `web/layout.py`에 `DUEL_ENABLED`/`DUEL_MENU_ADMIN_ONLY` 플래그와 메뉴 배선 추가(3단계 공개 전환 = 값 하나만 바꾸면 됨). `tests/test_web_session_isolation.py`에 결투 화면 배선 검사 [9] 추가.
  - **2-8 문구 오류 하나 발견·정정**: "체결 시각" 상시 고지문이 예전 초안 그대로 "그날 밤에 체결"이라고 돼 있었는데, 나중에 확정된 2-4(D+1 체결)와 어긋났습니다. 화면 작업 중 발견해서 위 2-8 문구를 D+1 기준으로 바로잡았습니다.
  - **검증**: `py_compile` 클린, `pyflakes` 클린. 이 스냅샷엔 `nicegui`/`web.auth` 등이 없어 실제 화면을 띄워보는 실행 검증은 못 했지만, 화면을 그리는 핵심 로직(계좌 3장 렌더링, TWR 계산 연결, 참여 흐름, 소유자 이중 확인)은 가짜 모듈로 감싸서 끝까지 통과하는 것까지 확인했습니다.
  - **🆕 main.py 배선 필요** — `main.py`가 이 세션엔 없어서 직접 못 고쳤습니다. 저장소 재접근 후 다른 화면들(`scorecard_page`, `admin_page`) 임포트하는 자리 옆에 `from web.pages import duel_page` 한 줄만 추가하면 됩니다(그래야 `/duel` 주소가 실제로 열립니다).
  - **🆕 오너가 확인해주실 것들** (전부 급하지 않음, 코딩을 막지 않습니다):
    1. 계좌별 잔고 추이 그래프(2-8이 언급한 "스냅샷 히스토리로 추이 표시")는 이번엔 안 만들었습니다 — 스냅샷이 며칠 안 쌓인 지금 시점엔 그릴 게 없어서입니다. 나중에 데이터가 좀 쌓이면 만들지 여부.
    2. 관리자 전용 단계에서 화면 자체도(메뉴뿐 아니라) 관리자가 아니면 본문을 안 그리게 이중으로 막아뒀습니다 — 의도한 강화이니 확인 부탁드립니다.
    3. 매수 주문 넣을 때마다 "크롤링 실패하면 취소됩니다" 체크박스를 매번 새로 체크해야 하는 엄격한 방식으로 짰습니다(2-4-5의 가장 엄격한 해석) — 실사용해보고 번거로우면 완화 가능.
    4. 메뉴/화면 문구: 그룹명 "⚔️ 결투다!", 메뉴 항목 "⚔️ 참전하기", 계좌 이름 "1개월/3개월/6개월 계좌" — 확인 부탁드립니다.
    5. 유니버스(코스피 상위 200) 이탈 종목의 3개월 유예 추적(3-3)은 배치 쪽 판단이라, 화면에서는 일단 "가격 확인 중"으로만 보여주고 화면이 따로 판단하지 않게 했습니다 — 의도한 단순화, 확인 부탁드립니다.
  - **미착수** — 5단계(공개 순위표: 동의 게이팅·닉네임 생성·체급 배정·발행 배치) 전부.

**참고 — 이번 라운드와 무관한 별도 작업**: 사이드바 메뉴 라벨 교체(`web/layout.py`, 그룹명 "보유종목" / `/scorecard` 페이지명 "내 성적표")는 이 결투 모듈과 무관한 별개 요청이었고, 오너가 이미 커밋까지 확인했습니다(위 진행 로그와 별개로 처리 완료).

**과거 요약 (0단계 설계 라운드, 참고용)**: 1차(큰 그림·계좌 3개·시드 1천만원·매월 80만원·매수 전용·이월·코스피 상위 200·원화 한정) → 2차(수량 기준 주문·부분체결·수정취소 허용·매수 창 사실상 무제한·최소 참여 500명) → 3차(주문 시간 모델 D+1 체결 전면 확정·크롤링 판정 알고리즘·유니버스 이탈 처리·기업행위 범위) → 4차(체급=구간 기준+시즌 고정 규칙) → 5차(시즌 주기 1년 확정)를 거쳐 미결 항목이 14건 → 사실상 1건까지 줄었고, 그 뒤 **1단계 스키마 승인**을 오너가 확인해 **2단계 코딩**(2026-08-19~20, 5차에 걸친 오푸스 높음 코딩 작업 — 스키마/규칙/DB계층 → 즉시옵트인 → 야간배치 → 화면)을 마쳤습니다.

**2단계 사실상 완료.** 남은 건 `main.py` 한 줄 배선(위 참고)과 위에 나열한 사소한 확인 사항들뿐입니다.

**2026-08-20, 6번째 코딩 작업 — 5단계(Branch 2) 백엔드 완료 + 오너 확인 사항 4건 전부 확정.** `sql/duel_schema.sql`(예외적 스키마 보강 2건)·`utils/duel_rules.py`(+444줄)·`utils/duel_db.py`(+662줄)·신규 `utils/duel_publish.py`(718줄)·신규 `tests/test_duel_publish.py`(120개)·`tests/test_duel_db.py` 확장까지 작성. 이어서 오너 확인을 거쳐 시즌 시작일(3월 1일)과 닉네임 형식(숫자 제거, 형용사 2개+명사 재설계)까지 반영. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_batch.py tests/test_duel_publish.py` → **361개 전부 통과, 회귀 0**(제가 직접 재실행해서 확인). 상세 내역은 위 5-5·5-9절 참고.

**2026-08-20, 7번째 코딩 작업 — 5단계(Branch 2) 화면 2종 + 발행 워크플로우 완료.** `web/pages/duel_consent_page.py`(740줄)·`web/pages/duel_leaderboard_page.py`(492줄) 신규, `utils/duel_db.py`에 발행표 읽기 전용 함수 4개 추가, `web/layout.py`에 2갈래 전용 공개 플래그 4개 추가, `run_duel_publish_batch.py`(130줄)·`.github/workflows/duel_publish_daily.yml`(138줄) 신규, `tests/test_duel_public_ui.py`(956줄, 44개) 신규. `python3 -m pytest tests/test_duel.py tests/test_duel_db.py tests/test_duel_batch.py tests/test_duel_publish.py tests/test_duel_public_ui.py` → **405개 전부 통과, 회귀 0**(제가 직접 재실행해서 확인). 상세 내역은 위 5-10절 참고.

**이어서 오너 확인 2건 처리 + main.py 배선까지 완료 — 이걸로 5단계가 완전히 끝났습니다.** (1) 발행 배치 cron을 10분 → 30분 여유로 조정(위 5-10절 끝부분 참고). (2) 플래그 이름 관련 오해 해소(내부 코드 이름일 뿐, 화면엔 이미 한글 라벨이 따로 있음). (3) `main.py`에 결투 화면 3개(`duel_page`·`duel_consent_page`·`duel_leaderboard_page`) import를 device_bash로 직접 추가 — 오너 저장소 접근 없이 오너 컴퓨터에서 바로 편집했습니다.

**다음 세션에서 제일 먼저 할 일**:
1. 아직 안 올라간 것 전부를 GitHub에 푸시 (아래 안내 참고) — §5-15의 결과물(신규 `utils/duel_batch_usd.py`·신규 `run_duel_daily_batch_us.py`·신규 `.github/workflows/duel_daily_us.yml`·신규 `tests/test_duel_batch_usd.py`)과 **§5-16의 결과물**(`utils/duel_rules.py`의 신규 `resolve_fill_trading_day_usd()`, `utils/duel_db_usd.py` 호출부·머리말 수정, `utils/duel_batch_usd.py` 머리말 문구 수정, `tests/test_duel.py` 신규 테스트 5개, `tests/test_duel_db_usd.py` 기대값 1건 수정), 그리고 **§5-17의 결과물**(신규 `utils/duel_publish_usd.py`·신규 `run_duel_publish_batch_us.py`·신규 `tests/test_duel_publish_usd.py`·`utils/duel_rules.py`의 신규 `resolve_bracket_for_season_usd()` + §11 절 머리말 갱신), 그리고 **§5-18의 결과물**(`web/pages/duel_page.py` 확장 — 달러 결투 블록 추가, 원화 부분 무수정 · 신규 `tests/test_duel_page_usd.py` 41개), 그리고 **§5-19의 결과물**(`web/pages/duel_consent_page.py` 확장 · `web/pages/duel_leaderboard_page.py` 확장(통화 드롭다운) · 신규 `tests/test_duel_consent_page_usd.py` 45개 · 신규 `tests/test_duel_leaderboard_page_usd.py` 46개 · `tests/test_duel_public_ui.py` 호출 한 곳 갱신 · `tests/test_web_session_isolation.py` 허용 목록 1건 추가), 그리고 이 문서.
   - ⚠️ **`.github/workflows/duel_publish_daily_us.yml`(신규)은 §5-15의 `duel_daily_us.yml`과 같은 방식으로 따로 반영해야 합니다** — `device_commit_files`가 `.github/workflows/` 경로를 막으므로 `device_bash` 히어독으로 오너 컴퓨터에 직접 쓴 뒤 `md5sum`으로 대조하세요. 파일 내용 자체는 이번 세션 샌드박스에 완성돼 있고 YAML 파싱까지 확인했습니다.
2. 위 "상위 50 선정 필드가 실제로 `rank`(시가총액 순위)가 맞는지"만 아직 실제 파일 대조가 안 끝났습니다.
3. 그 다음은 **6단계(실검증)** — 스테이징에서 실제로 확인해야 하는 것들(아래 6단계 참고), 그리고 **7단계(오너 승인 → 공개 전환)**의 문구 최종 검토가 남아 있습니다. ⚠️ 6단계에서 USD 주문을 실제로 넣어볼 때는 **§5-16이 고친 동작**(한국 날짜 X일 저녁에 넣은 주문의 `target_date`가 X+1이 아니라 **X**로 찍히고, 그 주문은 **X+1일 낮 배치**가 X일자 미국 마감가로 체결)이 실제로 그렇게 도는지 육안으로 한 번 확인해 주세요.
4. **🔴 USD 배치 시각 설계 오너 확인 필요(§5-15 · §5-17, 미결정 항목 6번)** — ① 체결 배치의 `--target-date` 기본값 "어제"·정기입금 판정용 `--today-date` 분리·cron `03:00 UTC`(매일)는 §5-15에서 제가 실제 수집 스케줄(scrape_us.yml)로부터 도출한 값이고, ② **발행 배치 cron `03:50 UTC`(= KST 12:50)는 §5-17에서 제가 "①의 종료 상한 03:20 UTC + 원화 트랙이 쓰는 여유 30분"으로 계산한 값**입니다 — 둘 다 오너가 확정한 값이 아닙니다. **①을 바꾸면 ②도 반드시 함께 다시 계산해야 합니다**(테스트가 그 관계를 고정하고 있어서, 한쪽만 바꾸면 `test_usd_publish_workflow_keeps_the_same_buffer_the_owner_confirmed_for_krw`가 실패합니다). 급하지 않지만 다음에 꼭 한 번 훑어봐 주세요.
5. **🔴 §5-17에서 발견한 것 — 지시서에 "통화 무관"이라고 적혀 있던 `duel_rules.resolve_bracket_for_season()`이 실제로는 원화 전용이었습니다.** 함수 본문이 유효한 체급 목록으로 `BRACKET_KEYS`(원화 9개)를 하드코딩하고 있어서, 그대로 재사용했다면 **USD 발행 배치가 첫 실행에서 `DuelRuleError`로 죽었을** 자리입니다. 신규 `resolve_bracket_for_season_usd()`로 갈랐고(이 트랙의 네 번째 예외 사례), 시즌 경계 자체는 5-11-8 확정대로 여전히 공유합니다. 코드 변경은 이미 끝났으니 **확인만** 해주시면 됩니다.
6. **🆕 USD 트랙("달러 결투") 코딩 — ✅ 전부 완료 (§5-11~§5-19).** 스키마 ✅ · `duel_rules.py` USD 상수·닉네임 파급 수정 ✅ · `utils/duel_db_usd.py` ✅ · 야간 배치 ✅ · 체결 거래일 하루 지연 버그 수정 ✅(§5-16, 오너가 직접 발견) · 발행 배치 ✅(§5-17) · `/duel` 화면(KRW/USD 병기) ✅(§5-18) · **공개 동의 관리 화면 + 공개 순위표 화면 ✅(§5-19)**. 결투 스위트 전체가 **847개 전부 통과**(회귀 0), 저장소 전체 스위트는 **1,033개 통과**(수정 전 942 + 신규 91, 실패·오류 목록 동일). **이 트랙에 더 만들 코드는 없습니다** — 다음은 6단계(실검증)·7단계(공개 전환)입니다.
7. **✅ §5-18의 1순위 후보였던 것 — 이미 해결됐습니다.** `tests/test_web_session_isolation.py` 에 `_assert_no_check_failures` autouse fixture 가 들어와, 이제 `check()` 실패가 **pytest 에서도 빨간불**입니다. §5-19 는 실제로 그 덕을 봤습니다(새 전역을 허용 목록에 등록 안 했을 때 곧바로 잡힘). 같은 파일 `test_duel_render_smoke` 의 `await` 누락은 아직 남아 있습니다 — 그 시나리오는 §5-18·§5-19 의 신규 테스트가 달러 기준으로 제대로 검증하고 있어 급하지 않습니다.
8. **🔴 §5-18의 화면 문구 오너 검토 3건** — `NOTICE_FILL_TIMING_USD`("주문을 넣은 바로 그날의 미국 정규장 마감가")·`NOTICE_WHY_SAME_DAY_USD`(원화와 이유가 정반대라는 설명)·달러 카드 지표명 "총자산 (달러)". 그리고 상시 노출 배너가 원화 3종 + 달러 4종 = 7개로 늘었습니다(§5-18 의 10번 항목). 자세한 내용은 §5-18 끝부분 참고.
9. **🔴 §5-19 오너 확인 3건** — ① **닉네임 공유의 부작용**: 같은 창유형의 원화·달러에 **둘 다** 공개 동의하면 두 순위표에 같은 닉네임이 실려 "이 두 줄은 같은 사람"이 드러납니다(5-11-10 확정의 자연스러운 귀결이고 교차 *사용자* 유출은 아니지만, 사용자가 모르면 안 되는 사실이라 동의 화면에 `NOTICE_SHARED_NICKNAME` 으로 밝혀 뒀습니다 — 문구와 방침 확인 부탁드립니다). ② 원화 전용 화면에 그대로 남겨 둔 문장 두 개("계좌마다 서로 다른 무작위 닉네임", "가상계좌 3개가 만들어지고")가 달러까지 참여하면 불완전해집니다 — 원화 무수정 원칙 때문에 손대지 않았는데, 미리 고칠지 판단 부탁드립니다. ③ 순위표 통화 선택기 라벨("🇰🇷 원화 결투 (코스피 상위 200)" / "🇺🇸 달러 결투 (미국주식)")과 달러 순위표 제목에 통화를 앞에 붙인 것이 실제로 보기에 괜찮은지. 자세한 내용은 §5-19 끝부분 참고.

10. **🔴 2026-08-23 — Branch 2 전체가 결투 가상계좌 기준에서 "내 성적표"(실제 자산) 기준으로 전면 교체됐습니다. 위 1~9번 항목은 전부 은퇴한 화면(`duel_consent_page.py`/`duel_leaderboard_page.py`) 얘기이니, 저장소를 다시 열면 이제 참고용으로만 보세요 — 실제로 살아있는 화면은 `scorecard_consent_page.py`/`scorecard_leaderboard_page.py`입니다.** 자세한 경위·오너 결정·새 설계·검증 내역은 신규 §5-20을 보고, 코드 설계 상세는 별도 신규 문서 `SCORECARD_PUBLIC_LEADERBOARD_WORK_ORDER.md`를 보세요. `/duel` 화면 자체(1갈래)는 이번 전환과 무관하며 이미 전체 공개 상태 그대로 운영 중입니다. **다음에 이 저장소를 열면 먼저 할 일**: ① 오너가 아직 실행 안 한 `sql/scorecard_public_schema.sql`을 Supabase에 적용했는지 확인, ② `SCORECARD_CONSENT_ENABLED`/`SCORECARD_LEADERBOARD_ENABLED` Render 환경변수 설정 여부 확인, ③ `.github/workflows/scorecard_publish_daily.yml`이 실제로 GitHub에 반영됐는지 확인(§5-20 끝의 "아직 안 끝난 것" 4번), ④ 그 다음이 새 모듈의 6단계(실검증).

> ⚠️ **작업 시작 전에 반드시 `git pull`.** 오너가 여러 창(웹·데스크톱·Cowork)을 오가며 서로 다른 AI 세션을 동시에 돌리는 일이 실제로 있었고, 2026-08-18에 병합 충돌이 났습니다. 코드에 손대기 전 로컬이 origin 최신인지 확인하세요 — 규칙은 `PROJECT_STATUS.md` §7-1.

> 📌 **이 모듈에서 절대 잊으면 안 되는 세 줄**
> ① 모르는 값은 절대 지어내지 않는다 — 체결가·상장폐지·배당 전부 (§0-1).
> ② 실시간은 없다 — 주문은 오늘 밤 확정 종가로 체결된다 (§0-3-1).
> ③ 공개는 발행표에서만 — 원본 테이블의 RLS는 손대지 않는다 (§0-3-8).
