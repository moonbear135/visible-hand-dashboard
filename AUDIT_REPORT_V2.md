# 코드베이스 재감사 보고서 (2026-08-06, 2차 · Opus)

> 1차 감사(`AUDIT_REPORT.md`, 2026-08-05)는 "없는 값을 지어내는" 패턴 위주로 잡았고, 그 이후 하루 만에
> 발견된 3건(퀀트 스코어 하드컷오프, VOL_PENALTY, 고성장률 PEGY 보정)은 전부 "실측값을 조용히
> 변형/절단하는" 다른 계열의 패턴이었음. 이 2차 감사는 그 계열을 집중적으로 훑은 결과.
> 이미 고친 3건은 확인 완료(문제없음) — 아래는 **새로 발견된 27건**.

마지막 갱신: 2026-08-06 (Opus 에이전트 재감사)

---

## 1. `collector_kospi200.py`

### 1-1. L421 — `abs(t_per)`로 적자 기업의 마이너스 PER을 흑자로 뒤집음 ⚠️ 최우선
```python
"t_per": abs(t_per) if (t_per is not None and t_per != 0) else None,
```
- **발견 내용**: 네이버 시총순위표의 PER 셀은 적자 기업에 음수(예: `-49.26`)를 줍니다. `abs()`가 이 부호를 지웁니다. L178/L191 상세페이지 정규식 `r'([\d\.,]+)배\s*l\s*([\d\.,]+)원'`도 앞의 `-`를 캡처하지 않아 **PER·EPS 둘 다 부호가 사라집니다.**
- **실데이터 확인**: `t_roe < 0`인 24종목 전부 양수 `t_per`+양수 `t_eps`를 가짐.
  - LG에너지솔루션: `t_roe=-5.19`, `t_per=49.26`(실제 -49.26), `t_eps=6,851`(계산값, 실제 음수), `graham_target=120,718원`
  - 삼성SDI: `t_roe=-3.15`, `t_per=75.36`, `t_pegy=1.67`, `t_fair=142,150원`, `graham=191,813원`, `growth=+45.0%`(가짜 EPS로 계산된 가짜 성장률)
  - SKC: `t_roe=-73.28`, `t_per=4.27` → 화면에 "PER 4.27배"(초저평가처럼 보임)
- **왜 문제인가**: §0-1 정면 위반(실측값 조작). 산티체크(price/eps≈per)는 부호가 양쪽 다 날아가서 항상 통과 — 검증 하네스가 못 잡음. "적자 기업은 그레이엄 넘버 산출 불가" 규칙이 24종목에서 무력화됨.
- **심각도**: 상
- **제안**: ① `abs()` 제거, 부호 보존. ② 정규식 `r'(-?[\d\.,]+)배\s*l\s*(-?[\d\.,]+)원'`으로 마이너스 캡처. ③ `t_per<=0`이면 Trailing 밸류에이션 계열 전부 None + data_issues 기록. ④ guardrail에 `t_roe<0 & t_per>0` 모순 검사 추가(재발 감지 회귀 가드).

### 1-2. L730-731 — ROE 프리미엄 심각도 무시 절벽(cliff)
```python
roe_prem  = 0.0 if f_roe is None else (0.15 if f_roe >= 12.0 else -0.10)
roic_prem = 0.0 if roic  is None else (0.10 if roic  >= 10.0 else -0.05)
```
- **왜 문제인가**: Forward ROE 11.9%→목표가 -10%, 12.0%→+15%. 0.1%p 차이로 25%p 점프. ROE 50%든 12%든 프리미엄 동일. 오늘 고친 VOL_PENALTY와 동일 패턴이며, Forward ROE를 실제 수집하기 시작한 어제부터 실제로 목표가에 영향 주기 시작함.
- **심각도**: 상
- **제안**: `compute_vol_penalty()`와 같은 형태의 `compute_roe_premium(f_roe)` 신설 — 기준선 12% 중심 절대거리 선형 스케일링(예: 0%→-0.10, 12%→0.0, 30%→+0.15 윈저라이즈).

### 1-3. L735-736 — 목표가 캡 `price×2.5`가 40종목에서 그대로 목표가가 됨
```python
target_per = min(target_pegy * growth_eff, 25.0)
f_target   = int(min(f_eps * target_per, price * 2.5))
```
- **실데이터 확인**: 200종목 중 40종목 `f_target`이 정확히 `price×2.5`(삼성전자, SK하이닉스, 기아, SK, HD한국조선해양, S-Oil 등). `t_fair`도 19종목이 캡에 붙음.
- **왜 문제인가**: 화면의 "+150.0% 상승 여력"(초록 바)은 계산 결과가 아니라 캡 상수 그 자체 — 40종목이 전부 동일 +150%. `pegy_view.py:1044` 툴팁은 "성장률·주주환원·이익창출력을 모두 고려해 계산"이라 설명 → 표시값-설명 불일치.
- **심각도**: 상
- **제안**: `f_target_capped=True`+사유 플래그. pegy_view에서 캡 걸린 종목은 "+150%" 대신 "상한 도달(＞+150%, 추정 신뢰구간 밖)" 회색/노란 배지+툴팁. 캡 자체(폭주 방지)는 유지, 25.0/2.5 근거는 주석에 SPEC §5-2 링크.

### 1-4. L626-637, 644 — 배당 수집 실패를 0으로 채우고 그대로 채점
- **실데이터 확인**: `sh_return==0`인 33종목 전부 `dps_source="no_dividend_or_not_collected"` — "무배당"과 "수집 실패"가 한 값에 섞임. 그중 19종목이 퀀트 점수 산출되어 20점 만점 중 3점을 "실측된 저배당"으로 받음.
- **심각도**: 상
- **제안**: `dps_source`를 `no_dividend_confirmed`/`not_collected`로 분리. 후자는 `dps=None`, `sh_return=None`으로 두어 scoring이 자동 배점 제외하게.

### 1-5. L352-358 / L431-433 — 시총 페이지 수집 실패를 `continue`로 삼켜 순위가 조용히 밀림
- **왜 문제인가**: 3페이지 실패 시 101~150위가 사라지고 151위가 101위로 표시. 히스테리시스 버퍼가 이를 "230위 밖 이탈"로 오판.
- **심각도**: 상
- **제안**: 페이지 실패 발생 시 `raise RuntimeError`로 중단(기존 스냅샷 유지) + `metadata.rank_source_incomplete` 플래그.

### 1-6. L295-311 — EV/EBITDA `iloc[row_idx, 1]` 고정 인덱스 (SPEC §2-1 위반) — 중
### 1-7. L314-319 / L529-537 — 우선주가 보통주 DPS/ROE를 상속받는데 마킹 없음(실측으로 둔갑) — 중
### 1-8. L747 — `value_trap = t_roe < 8.0` 이진 판정, 근거 없음 + 화면 설명("ROIC<6%")과 불일치(ROIC는 수집도 안 함) — 중
### 1-9. L723-724 — `max(growth_eff, 0.1)` 바닥값, 근거 없음 — 중
### 1-10. L742(`price×2.5`/`geff×25` 매직넘버 주석 부족), L973(`valid_ratio>=0.95` 근거 없음, 실제 0.855라 항상 DEGRADED) — 하

---

## 2. `utils/scoring.py`

### 2-1. L338-350 — 목표가 교차검증 상한 여전히 플랫 45%/60% ⚠️ 오늘 3건과 동일 패턴
```python
if price >= f_target * 1.15: quant_score = min(quant_score, _cap(45))
elif price >= f_target:      quant_score = min(quant_score, _cap(60))
```
- **왜 문제인가**: 이 파일의 다른 하드컷오프는 전부 심각도 비례로 개편됐는데 이 두 줄만 남음. 목표가 1.16배 초과와 5배 초과가 동일 45% 캡.
- **심각도**: 상
- **제안**: 절대거리 윈저라이즈로 교체 — `overshoot = price/f_target - 1.0`을 0~1.0 범위로 캡 60%→20% 선형 스케일링. **단, `f_target_capped`(1-3)면 이 블록 자체를 건너뛸 것** — 캡값과 비교하는 건 무의미.

### 2-2. PEGY 점수 구간(L195-206)과 배지 구간(L315-330) 경계값이 서로 다름 — 중
### 2-3. L218-250 — 자본효율성/배당/Trailing 점수 계단식(심각도 무시), 15/10/12/8/5/3/1 근거 없음 — 중
### 2-4. L252-258 — 변동성 점수를 UI 표시 문자열("정상"/"데이터 없음") 파싱으로 판정 + growth_eff 감점과 이중 계상 — 중
### 2-5. L159, L183-184 — `forward_available` 로컬 변수 덮어쓰기 잔재(현재는 무해, 재발 함정) — 하

---

## 3. `views/pegy_view.py`

### 3-1. L1100 — 적자 경고 배너 안에 하드코딩된 `99.99` 숫자가 18px 볼드로 표시 (1차 감사 §3-3 "PEGY 99.0" 잔재로 추정) — **상**
### 3-2. L831-864 — 1-1 부호 유실 때문에 적자 기업 24종목이 "적자 — 산출불가" 배너 아래 그레이엄 목표가를 나란히 표시(자기모순) — **상** (1-1 고치면 자동 해소, 방어적 크로스체크 추가 권장)
### 3-3. L273/L498/L1071 — "100점 만점" 문구가 실제 만점(ROIC 15점 영구 제외로 대부분 85점)과 불일치 — 중
### 3-4. L778 — `q_max_val = q_max or 100` (없는 분모를 100으로 지어냄) — 하
### 3-5. L446-450 — 렌더링 시점에 guardrail 재실행(현재 무해, 향후 분기 위험) — 하

---

## 4. `scrape_daily.py` (매크로 — 향후 미국 주식 페이지에 재사용될 코드)

### 4-1. L413-421 — 동시 충격 증폭기 플랫 1.3/1.15 ⚠️ VOL_PENALTY와 판박이 — **상**
### 4-2. L150-155 & L342-346 — 실패 시 `0.0` 대입 + 이를 막으려던 가드가 도달 불가능한 죽은 분기 — 중
### 4-3. L269 — `stock_net_base = 0.5` 순수 상수(수급 규모 미반영) — 중
### 4-4. L103-104 — `kospi_change`/`usd_change` 실패 시 `0.0`(보합으로 오인) — 중
### 4-5. L166-193 — 네이버 현재가로 FDR 종가 덮어쓰기, 날짜/괴리율 검증 없음(CSV 저장값과 점수계산 시점 값이 다른 출처) — 중

---

## 5. `views/macro_view.py` (관리자 전용)

### 5-1. L209-215 — 가중치가 `scrape_daily.py`와 다름(단일 출처 위반, 화면 표의 기여점수 합이 실제 종합점수와 안 맞음) — **상**
### 5-2. L281-297, L305-317 — 표시 "위험도"와 CSV 저장값이 서로 다른 척도(원시값 vs 시그모이드 변환값) — **상**
### 5-3. 감사이력 탭 "Target PER 10.4 고정" 문구가 현재 코드와 다름 — 하

---

## 6. `utils/guardrail.py`

### 6-1. L98 — 배당 필수 업종 키워드에 `'우B'`(업종 아닌 우선주 표기)가 섞여 오분류 — 중
### 6-2. 정합성 크로스체크 부재 — `t_roe<0 & t_per>0`(1-1을 24종목 규모로 방치한 직접 원인), `dps>0 & sh_return==0`, 목표가 캡 도달, 그레이엄 이상치 등 — 중
### 6-3. `f_per>300` 임계치가 guardrail/scoring/collector 3곳에 중복 — 하

---

## 요약

**총 27건** (이미 고친 3건 제외)

| 심각도 | 건수 |
|---|---|
| 상 | 10 |
| 중 | 12 |
| 하 | 5 |

**착수 순서 제안**: 1-1 → 3-2 → 1-4 → 1-3 → 2-1 → 3-1 → 1-2 → 1-5 → 4-1 (앞 6건은 "적자·고성장 종목의 화면 숫자"라는 한 증상군이라 한 배치로 처리 권장)

**왜 이 패턴이 반복되는가 (Opus 소견)**
1. 1차 감사는 "없는 값을 지어내는" 패턴에 집중했고, "실측값을 변형/절단하는" 패턴(abs(), min/max 캡, 계단식 대입, 부호 못 잡는 정규식)은 다루지 않았음.
2. 값을 캡/클립하는 지점에 "캡에 닿았다"는 흔적이 전혀 안 남아, 40종목이 똑같은 "+150%"를 달고도 아무도 알아챌 수 없었음. → 캡이 걸리면 `_capped=True`+사유를 항상 남기고 UI에 "🧮 계산값"류 배지로 노출하는 규칙을 SPEC에 추가 권장.
3. 같은 개념의 임계값이 여러 파일에 중복되어 조금씩 다르게 진화(PEGY 경계 점수/배지 이원화, 위험가중치 scrape_daily/macro_view 불일치, PER 300 임계 3곳, value_trap 코드/화면 설명 불일치). 미국 주식 페이지 추가 전 임계값·가중치를 `utils/constants.py`로 통합하는 리팩터가 투자 대비 효과가 가장 큼 — 크롤링이 5시간짜리가 되면 지금의 "돌려보고 화면에서 발견" 루프는 못 씀.

원본 에이전트 보고서(전체 코드 스니펫 포함)는 세션 로그 참고. 파일은 감사 중 수정하지 않음.
