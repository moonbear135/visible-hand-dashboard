# 📈 리포트 모듈 v1 신규 구축 — 실행 지시서 (오푸스, 높은 추론 강도 필수)

> ⚠️ **이 문서는 v1 빌드 당시(2026-08-12, Streamlit) 기준의 역사 기록입니다 — 지금 아키텍처가
> 아닙니다.** 이 지시서대로 새로 만들지 마세요: **리포트 모듈은 이미 완성돼 `/report`로 실서비스
> 중**입니다(2026-08-17 공개 전환). 화면은 `views/report_view.py`가 아니라
> **`web/pages/report_page.py`(NiceGUI)** 가 그립니다. 지금 아키텍처·공개 상태는
> `PROJECT_STATUS.md` §0을 보세요. 다만 **왜 이렇게 설계했는지**(배치 스냅샷 구조, service_role
> 키 취급 원칙, 벤치마크 소스 선정 근거 등)는 지금도 유효한 참고자료입니다.

> 이 문서는 **다른 세션·다른 모델(오푸스)이 이 대화의 맥락 없이 처음부터 읽어도 작업을 시작할 수 있도록**
> 만든 독립 실행 지시서입니다. 2026-08-12 오너 대화에서 확정된 내용을 정리했습니다.
> 작업 시작 전 반드시 `ENGINEERING_SPEC.md` 전체, `PROJECT_STATUS.md`, `SCORECARD_WORK_ORDER.md`,
> `sql/scorecard_schema.sql`를 먼저 읽으세요(§0-2 절차). 스타일과 원칙은 `SCORECARD_WORK_ORDER.md`와
> 동일하게 따르면 됩니다.

> 🔴 **오너 지시 (재확인): 이 모듈 코딩은 오푸스(Opus)를 높은 추론 강도로 진행할 것.** 새 모듈을
> 처음부터 설계하는 작업이라 난이도가 높습니다.

---

## 0. 한 줄 요약

"내 성적표"(4번째 모듈, 이미 완료) 밑에 **완전히 별도의 새 모듈("리포트")**을 만듭니다. 로그인한
사용자의 보유 종목 평가금액을 **매일 자동으로(로그인 여부와 무관하게) 스냅샷 저장**해두고, 그 스냅샷
하나의 테이블에서 일간/주간/월간/분기/반기/연간 리포트를 **전부 계산해서 보여줍니다**(기간별로 별도
테이블을 만들지 않음 — 아래 §2 참고). 코스피지수(한국 보유) / S&P500·나스닥(미국 보유)과 각각
벤치마크 비교도 보여줍니다.

---

## 1. 🔴 가장 중요 — 왜 "내 성적표"와 다른 배치 구조가 필요한가

"내 성적표"는 사용자가 화면을 **열 때마다** 그 순간의 현재가로 실시간 계산합니다(저장 없음). 리포트는
그럴 수 없습니다 — "어제보다 얼마나 올랐나", "이번 달엔 얼마나 벌었나"를 보여주려면 **과거 시점의
평가금액이 실제로 저장돼 있어야** 합니다. 그래서 리포트 모듈은 두 개의 서로 다른 실행 주체가 필요합니다.

1. **매일 스냅샷을 쌓는 배치 작업** (신규, GitHub Actions 스케줄) — 사용자가 앱을 열지 않아도 매일
   실행되어, **가입한 모든 사용자**의 그날 보유종목 평가금액·매입원가·벤치마크 지수값을 계산해
   Supabase에 1행씩 저장합니다.
2. **저장된 스냅샷을 읽어 리포트로 보여주는 화면** (신규, Streamlit) — "내 성적표"처럼 사용자가 열 때
   그 순간 계산하는 게 아니라, 이미 쌓인 스냅샷 테이블을 기간별로 집계해서 보여주기만 합니다.

**이 배치 작업(1번)은 "내 성적표"의 anon key 원칙과 다릅니다.** 브라우저에 노출되는 Streamlit 앱이
아니라 GitHub Actions 안에서만 도는 서버 스크립트이므로, **여기서는 `service_role` 키를 써야
합니다**(anon key + RLS로는 "가입한 모든 사용자"의 데이터를 한 번에 쓸 수 없습니다 — RLS는 "로그인한
그 사용자 본인"만 허용하도록 설계돼 있으므로). 다만 이 키는:

- **절대 Streamlit Secrets(앱)에 넣지 않습니다** — 오직 **GitHub Actions Secrets**에만 등록합니다
  (`KRX_OPENAPI_KEY`가 있는 그 자리, 이름 예: `SUPABASE_SERVICE_ROLE_KEY`). 새 배치 스크립트만
  이 환경변수를 읽습니다.
- 이 스크립트 안에서도 **읽기는 전체 사용자 holdings를 조회해야 하지만, 쓰기는 그 계산 결과인
  스냅샷 저장 한 곳으로만 국한**하세요. 실수로 holdings나 profiles를 수정하는 코드를 넣지 마세요.
- 코드 주석에 "이 파일만 service_role을 쓰는 이유"를 명확히 남기세요 — 나중에 누가 이 패턴을
  다른 곳에 복사해서 앱(브라우저) 쪽에 service_role을 넣는 실수를 막기 위해서입니다.

---

## 2. 🔴 데이터 구조 — "기간별로 따로 저장"이 아니라 "하루 1행만 저장하고 나머지는 계산"

오너 지시(2026-08-12): *"일간 주간 월간 반기 분기 년간을 다 만들껀데... 이걸 데이터 저장하려면 1개의
계정에서 생기는 데이터 량이 엄청 많이 쌓이네"* — 맞는 우려입니다. 기간마다 리포트를 미리 계산해서
저장하면 안 됩니다. 대신:

- **`portfolio_daily_snapshots`라는 테이블 하나만** 만듭니다. 사용자 1명 × 시장(KR/US) 1개 ×
  거래일 1일 = 1행. 연간 약 250여 행(거래일 기준) × 시장 2개 = 사용자당 연 500행 안팎. 10년 써도
  수천 행 수준이라 Supabase 용량 문제가 되지 않습니다.
- 주간/월간/분기/반기/연간 리포트는 **전부 이 테이블에 대한 날짜 범위 쿼리**입니다. 예: 이번 달
  리포트 = 이번 달 첫 거래일 스냅샷과 마지막(오늘) 스냅샷의 차이. 별도 테이블·별도 배치 없이
  화면 쪽 쿼리/집계 함수로만 구현하세요.
- market별로 행을 나누는 이유: "내 성적표"가 이미 원화/달러를 절대 섞지 않는다는 원칙(§0-1,
  `holdings.currency` CHECK 제약)을 지키고 있고, 벤치마크도 시장별로 다르기 때문입니다(아래 §4).

### 스키마 초안 (오푸스가 세부 조정 가능, 원칙은 유지)

```sql
create table if not exists public.portfolio_daily_snapshots (
    id                 uuid primary key default gen_random_uuid(),
    user_id            uuid not null references auth.users (id) on delete cascade,
    market             text not null check (market in ('KR', 'US')),
    snapshot_date      date not null,
    total_value        numeric(20, 6) not null,   -- 그날 종가 기준 평가금액 합계
    total_cost         numeric(20, 6) not null,   -- 그날 매입원가 합계 (holdings 스냅샷 시점 기준)
    currency           text not null check (currency in ('KRW', 'USD')),
    holdings_count     integer not null,          -- 그날 보유 종목 수 (구성 변경 감지용, §5 참고)
    benchmark_symbol   text,                       -- 'KOSPI' / 'SP500' / 'NASDAQ' 등
    benchmark_value    numeric(20, 6),             -- 그날 벤치마크 지수 종가
    created_at         timestamptz not null default now(),
    constraint snapshots_user_market_date_unique unique (user_id, market, snapshot_date)
);

create index if not exists snapshots_user_id_idx on public.portfolio_daily_snapshots (user_id);
create index if not exists snapshots_date_idx on public.portfolio_daily_snapshots (snapshot_date);
```

- RLS는 `holdings`와 동일한 패턴(`auth.uid() = user_id`, `select`만 `authenticated`에게 —
  이 테이블은 배치 스크립트(service_role, RLS 우회)만 쓰고 사용자는 읽기만 하므로 insert/update/delete
  정책은 사용자에게 줄 필요가 **없습니다**. `sql/scorecard_schema.sql`처럼 완결된 스크립트로
  `sql/report_schema.sql`에 작성하세요.
- market이 US면 currency는 USD, KR이면 KRW — `holdings`와 같은 CHECK 제약을 그대로 반영하세요.

---

## 3. 🔴 "데이터 부족"은 정직하게, 지어내지 않기 (§0-1, 오너 재확인)

오너 지시(2026-08-12): *"데이터 부족으로 뜨는건 솔직하게 데이터 부족이라고 명시하는게 맞아. 실제로
아직 며칠도 안됐는데 어정쩡한 데이터를 보내는 것만큼 보기 싫은게 없지. 그렇다고 거짓된 데이터로
신뢰를 잃는건 절대로 하면 안되는거야."*

- 이 기능을 켜는 시점부터 스냅샷이 쌓이기 시작합니다. **과거분을 소급 계산하지 마세요**(예: 오늘
  기준 현재가로 지난달 스냅샷을 역산하는 식의 추정 절대 금지).
- 각 기간 리포트는 **그 기간 전체가 실제 스냅샷으로 채워졌을 때만** 정상 표시하고, 그렇지 않으면
  "데이터 부족" 문구를 명확히 보여주세요. 예:
  - 월간 리포트: 이번 달 1일부터 스냅샷이 있어야 완전한 월간 리포트. 기능 켠 지 10일밖에 안 됐으면
    "이번 달 리포트는 OO일치 데이터만 있어 완성되지 않았습니다(정식 리포트는 다음 달부터)"처럼
    **몇 %가 아니라 사실 그대로**를 안내하세요. 절반 채워진 걸 마치 완성된 리포트처럼 보여주는
    화면 배치(작은 글씨 경고 하나만 붙이고 나머지는 정상 리포트처럼 꾸미는 것)를 하지 마세요 —
    아예 리포트 자리에 "데이터 부족" 상태를 명확한 주 컨텐츠로 보여주세요.
  - 연간/반기/분기도 동일한 원칙.
  - 일간/주간은 상대적으로 빨리 채워지니 크게 문제되지 않겠지만, 같은 원칙을 동일하게 적용하세요.
- 벤치마크 데이터가 어떤 날 수집 실패했다면(§0-1) 그 날짜 스냅샷은 `benchmark_value`를 NULL로
  두고 "벤치마크 없음"이라고 정직히 표시하세요. 전날 값을 복사해서 채우는 식의 보간(補間)을
  하지 마세요.

---

## 4. 벤치마크 비교 (오너 확정: 시장별 개별 비교)

- **한국(KR) 보유**: 코스피 지수와 비교. **이미 `market_history.csv`에 "코스피 종가" 컬럼이 매일
  자동 수집되고 있습니다** — 이 배치 작업이 새로 크롤링할 필요 없이 그 값을 **읽기 전용으로 재사용**
  하세요. ⚠️ `market_history.csv`는 §4(매크로) 파이프라인 산출물이고, **오너의 2026-08-10 중단
  지시("매크로 관련 파일은 명시 지시 없이 건드리지 말 것")가 걸려 있습니다.** 이 파일을 **읽기만
  하는 것**(수정 없음)은 이 지시 위반이 아니지만, 혹시라도 코드가 이 파일에 쓰기 작업을 하지
  않도록 특히 주의하세요. 코스닥은 이 파일에 없으므로 v1은 **코스피만** 비교하고, "코스닥 비교는
  후속 작업"이라고 문서에 남기세요(지어내서 채우지 마세요).
- **미국(US) 보유**: S&P500·나스닥 지수와 비교. **이 프로젝트에 아직 미국 지수 데이터가 없습니다.**
  > ✅ **(2026-08-17 추가 · 이 항목은 이미 완료됨)** 아래 지시대로 `collector_us_indices.py`가
  > 신설돼 `data/us_index_history.json`에 일별 종가를 누적하고 있습니다. 실제 소스는 지수 원값이
  > 아니라 **ETF 프록시(SPY·ONEQ)** 입니다(S&P DJI 재배포 제한 때문에 FRED를 일부러 미채택).
  > 다시 조사·구현하지 마세요 — 자세한 경위는 `TASK_HISTORY.md` #95~#97.

  #92/#93에서 이미 검증된 stockanalysis.com(devalue 디코더 재사용 가능한 인프라 있음)이나 다른
  신뢰할 수 있는 무료 소스에서 S&P500(^GSPC 등)·나스닥종합(^IXIC 등) **일별 종가**를 구하는 방법을
  직접 조사해서 구현하세요(§0-1 — 추측 금지, 실응답으로 확인). 기존 미국주식 수집기의 관례(딜레이,
  재시도, 실패 시 조용히 skip, raw/가공 분리)를 그대로 따르세요. 별도 파일(`data/us_index_history.json`
  같은 형태, 정확한 이름은 재량)에 누적하고 이것도 **공개 데이터**(개인정보 아님)이므로 기존
  `data/` 폴더 관례(GitHub 공개 커밋)를 따르면 됩니다 — Supabase에 넣을 필요 없습니다.
- 리포트 화면에는 "내 포트폴리오 수익률 vs 벤치마크 수익률"을 기간별로 나란히 보여주세요(예:
  이번 달 내 수익률 +3.2% vs 코스피 +1.8%).

---

## 5. 기간 수익률 계산 방식 (v1은 단순하고 정직하게)

포트폴리오 총 평가금액은 시세 변동뿐 아니라 **사용자가 중간에 종목을 추가/삭제/수정**해도 바뀝니다.
정교한 시간가중수익률(TWR)까지는 v1 범위가 아닙니다(지어내거나 어설프게 구현해서 틀린 숫자를 보여주는
것보다, 단순하고 정직한 지표를 명확한 설명과 함께 보여주는 게 낫습니다 — §0-1 정신).

- v1 기간 리포트 지표: **구간 시작 스냅샷의 (총평가금액, 총매입원가)** vs **구간 종료 스냅샷의
  (총평가금액, 총매입원가)** 단순 비교로 계산하세요.
- `holdings_count`가 구간 내에 바뀐 게 감지되면(스냅샷 간 종목 수 차이, 또는 더 정교하게 하고 싶으면
  종목 구성 자체 비교), 리포트에 **"이 기간 중 보유 종목 구성이 변경되어 수익률에 매매 영향이
  섞여 있습니다"**라는 안내 문구를 반드시 붙이세요. 이 문구를 붙이는 로직은 정확할 필요는 있지만,
  "매매로 인한 순수 효과"까지 분리 계산하려 하지 마세요(v1 범위 밖, 지어내는 리스크).

---

## 6. v1 범위

**포함:**
1. `portfolio_daily_snapshots` 테이블 + RLS (`sql/report_schema.sql`)
2. 매일 스냅샷 적재 배치 스크립트(신규 GitHub Actions 워크플로우, service_role 사용) — 가입한 모든
   사용자 × market별로 그날 총평가금액/총매입원가/벤치마크값 계산해 저장. 기존 가격 조회 로직
   (`utils/scorecard_db.py`의 `make_price_lookup()` 등)을 최대한 재사용하세요, 새로 만들지 말고.
3. 미국 지수(S&P500·나스닥) 일별 종가 수집기 신설(§4)
4. 리포트 화면(신규, "내 성적표" 메뉴 밑에 별도 메뉴/화면) — 기간 선택(일/주/월/분기/반기/연),
   최신 리포트가 기본으로 뜨고 과거 날짜/기간도 선택해 다시 볼 수 있음, 벤치마크 비교 표시,
   데이터 부족 구간은 §3 원칙대로 명확히 안내.
5. §0-3-6 원칙대로 **완전히 별도 모듈**, 완성 후 오너 승인 전까지 숨김(스테이징).

**v1에서 제외(다음으로 미룸):**
- 시간가중수익률(TWR) 등 정교한 수익률 계산
- 코스닥 벤치마크(코스피만 v1)
- 리포트 PDF/이메일 발송, 알림
- 이 프로젝트가 이미 미룬 것들(OCR 등)과 동일 순연

---

## 7. 파일 구조 (완전히 별도, §0-3-6)

기존 파일(`views/scorecard_view.py`, `utils/scorecard_db.py`, `collector_us_stocks.py` 등)은
**절대 건드리지 마세요**(단, 가격 조회 등 읽기 전용 함수를 **import해서 재사용**하는 건 권장합니다).
매크로 관련 파일(`utils/scoring.py`, `views/pegy_view.py` 등 §4 관련)도 절대 건드리지 마세요 —
`market_history.csv`는 읽기만 하세요.

새로 만들 파일(이름은 재량, 관례만 유지):
- `sql/report_schema.sql`
- `utils/report_db.py` — Supabase 배치 적재/조회 로직(스냅샷 insert는 service_role 클라이언트,
  화면에서 읽기는 기존 anon key 클라이언트 재사용)
- `collector_us_indices.py` (또는 기존 `collector_us_stocks.py`에 함수만 추가하는 대신 이렇게
  분리 — 판단은 오푸스 재량이나, §4 조사 내용을 문서에 남길 것)
- `views/report_view.py`
- `.github/workflows/scrape_report_snapshots.yml` (신규 워크플로우, 매일 장마감 후 실행 —
  기존 `scrape_us.yml`/`scrape.yml`이 끝난 뒤 시간대로 cron 잡으세요, 가격 데이터가 이미 갱신된
  뒤에 스냅샷을 찍어야 정확합니다)
- `tests/test_report.py`

---

## 8. 표준 제약사항 (재확인, 이 프로젝트 전체 공통)

- **git 명령 절대 실행 금지**(add/commit/push/pull 전부). 오너가 cmd.exe에서 직접 실행.
- 크레덴셜(anon key, service_role key 포함)을 코드/문서/로그에 실제 값으로 적지 마세요.
- 매크로(§4) 관련 파일은 오너 명시 지시 없이 절대 건드리지 마세요.
- 값을 지어내지 마세요(§0-1). 실패/누락은 정직하게 "없음"/"데이터 부족"으로.
- `py_compile`, 기존 테스트 스위트 전체(회귀 없어야 함), 신규 오프라인 테스트, `git diff -w --stat`
  범위 확인까지 스스로 검증하고 보고하세요.
- 서버 부하 배려(§0-3-2) — 새 미국 지수 수집기도 기존 관례(딜레이·재시도·차단 시 즉시중단) 그대로.

---

## 9. 착수 단계 (우선순위 순 — 한 번에 다 만들려 하지 말고 단계별로 보고)

1. **§4 벤치마크 소스 조사** — 미국 지수 일별 종가를 실제로 받아보고(실응답 확인), 방법을 문서화.
   여기서 막히면 바로 보고하세요(가짜 벤치마크로 진행하지 말 것).
2. **`sql/report_schema.sql`** 작성.
3. **`collector_us_indices.py`**(또는 재량 위치) + 오프라인 테스트.
4. **`utils/report_db.py`**: 스냅샷 적재 함수(순수 로직은 Supabase 클라이언트 없이도 테스트 가능하게
   분리) + 기간별 집계 조회 함수(§2, §5 로직) + 오프라인 테스트(합성 스냅샷 데이터로 기간 경계·
   데이터 부족 판정 로직 검증).
5. **`.github/workflows/scrape_report_snapshots.yml`** — 아직 실제 Supabase 연결 없이 코드/워크플로우
   작성까지만(§4-4 샌드박스 네트워크 제약 동일 적용, 실제 연결은 오너가 Secrets 등록 후 검증).
6. **`views/report_view.py`** — 최소 동작 버전(기간 선택 → 집계 조회 → 표시, 데이터 부족 안내 포함).
7. 여기까지 오프라인 검증 통과하면 **일단 보고하고 멈추세요.** 오너가 `SUPABASE_SERVICE_ROLE_KEY`를
   GitHub Actions Secrets에 등록하고 `sql/report_schema.sql`을 Supabase에서 실행한 뒤 실제 연결
   검증이 이어집니다(#92/#93과 같은 패턴 — 코드 완성 후 실크롤링으로 실검증).

---

## 10. 완료 후 보고 형식

`TASK_HISTORY.md`에 새 항목 추가, `PROJECT_STATUS.md`에 리포트 모듈 진행 상황을 새 섹션(예: §10)으로
추가하세요. 무엇을 만들었고, 무엇을 오프라인 테스트로 검증했고, 오너가 다음에 무엇을 해야 하는지
(Secrets 등록, SQL 실행 등)를 명확히 남기세요. `git diff -w --stat`로 변경 범위가 이 지시서의 §7
파일 목록을 벗어나지 않는지 스스로 확인하고 보고에 포함하세요.
