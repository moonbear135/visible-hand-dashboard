# 매크로 14개 위험 지표 — 실데이터 연결 조사 및 재설계 제안서

> 작성: 2026-08-09 / 상태: **조사·제안 단계 (코드 무수정)**
> 대상: `scrape_daily.py` "4. 리스크 지표 연산"(288~387줄), `utils/constants.py`의 `RISK_WEIGHTS`,
> `views/macro_view.py`, `utils/macro_scoring.py`
> 관련 원칙: `ENGINEERING_SPEC.md` §0-1(하드코딩·더미 금지), §0-3-1(후행지표 전용 + "추정 프록시
> 라벨링도 더 이상 허용 안 함"), §0-3-2(크롤링 매너 — 우회·무단 호출 금지), §0-3-3(다중 출처)
>
> ⚠️ 이 문서는 **제안서**입니다. 이 문서를 근거로 코드를 고치기 전에 오너 승인이 필요하고,
> 아래 "미확인"이라고 적힌 항목은 **구현 착수 전에 실제 호출로 먼저 확인**해야 합니다.
> (조사 단계에서 웹으로 확인 가능한 범위까지만 검증했고, 확인 못 한 것은 확인 못 했다고 적었습니다.)

---

## 1. 요약

### 1-1. 현재 상태 (문제 정의)

14개 지표 전부가 실제로는 **5개의 실측 입력값**(`kospi_change`, `usd_close/usd_change`,
`volatility`, `dist_from_high`, 3주체 순매수)만 가지고 선형식으로 파생된 **추정 프록시**입니다.

```python
# scrape_daily.py 292~303줄 (현재 코드, 발췌)
fx_base    = 0.5 + 0.3 * (usd_close - 1200) / 300     # "스왑포인트"인데 실은 환율 레벨
synth_base = 0.5 + 0.3 * (usd_close - 1300) / 200     # "합성선물 괴리"인데 실은 또 환율 레벨
ndf_base   = 0.4 + 0.5 * usd_change                    # "야간 역외환율"인데 실은 환율 등락률
short_base = 0.4 + 0.4 * (volatility / 5.0)            # "공매도 비중"인데 실은 변동성
els_base   = 0.1 + 0.7 * dist_from_high                # "ELS 낙인위험"인데 실은 고점대비 낙폭
```

즉 라벨은 "옵션 미결제약정 / 공매도 비중 / ELS 낙인 / 스왑포인트 / NDF / 합성선물 / 비차익"인데
실제로 크롤링하는 데이터는 **하나도 없습니다**. §0-3-1이 명시적으로 금지한 상태입니다.

### 1-2. 판정 결과 (14개)

| 판정 | 개수 | 현재 가중치 합 | 지표 |
| --- | --- | --- | --- |
| **A. 실측 연결 가능** | 3개 | 27.0 (26.5%) | `KOSPI_5D_Return`, `Stock_Net_Sell`, `FX_Swap_Point`(조건부) |
| **B. 근사 실측 가능(재라벨링 필요)** | 5개 | 35.0 (34.3%) | `VKOSPI_Skew`→VKOSPI, `Synthetic_Futures`→선물 베이시스, `Put_OTM_OI`, `Short_Ratio`, `Stock_Short_Balance` |
| **C. 실측 불가 → 제거 권고** | 6개 | 40.0 (39.2%) | `ELS_KnockIn`, `NDF_Night_Rate`, `Futures_Net_Sell`, `Non_Arbitrage_Ratio`, `Foreign_Broker_Dump`, `Put_Buy_Simple` |

**현재 `RISK_WEIGHTS` 합계 = 102.0** (100이 아닙니다. 직접 더해서 확인했습니다.)
다만 `utils/macro_scoring.py`의 `compute_final_score()`가
`total_weight = sum(weights[k] for k in available)` 로 **매번 재정규화**하므로,
지표를 빼도 점수 계산이 깨지지는 않습니다 (§4에서 상술).

### 1-3. 전체 그림 — 이번 조사에서 새로 드러난 3가지 큰 사실

1. **KRX 정보데이터시스템(data.krx.co.kr)이 2025-12-27부터 회원제(로그인 필수)로 전환**되었습니다.
   ("AI 봇의 무단 데이터 수집으로 인한 서버 과부하" 대응 — pykrx 이슈 #244에 인용된 기사)
   → 예전처럼 `data.krx.co.kr`을 직접 호출하는 방식은 **더 이상 정공법이 아닙니다.**
2. **정공법은 KRX OPEN API(openapi.krx.co.kr, 인증키 기반, 무료)** 입니다. 다만
   ① 인증키 발급 + **API 서비스별 개별 이용신청·승인**(각각 승인 안 나면 401),
   ② **전일 데이터는 익일 오전 8시에 업데이트**(장중 호출하면 빈 데이터),
   ③ 인증키당 **1일 10,000회** 제한, ④ 2010년 이후 데이터 제공.
3. **공매도·투자자별 선물/옵션·프로그램매매(차익/비차익) 통계는 KRX OPEN API에 아예 없습니다.**
   (서비스 목록 7개 카테고리 = 지수/주식/증권상품/채권/파생상품/일반상품/ESG 를 전수 확인)
   → 이 계열 지표는 pykrx(= data.krx 스크래핑) 말고는 무료 경로가 없고, pykrx는 로그인 차단을
   **Referer 헤더 변경으로 우회**해서 동작 중입니다. 이건 §0-3-2("로그인 우회 금지")와 정면 충돌하므로
   **오너 판단이 필요한 항목**으로 따로 표시했습니다.

---

## 2. 지표별 판정표

> 난이도: 하 = 기존 수집 데이터만으로 가능 / 중 = 신규 API 1개 연동 / 상 = 비정형 소스 역공학 필요

| # | 지표 (가중치) | 현재 프록시 공식 (= 실제로 쓰는 입력) | 판정 | 실측 소스·필드 | 지연 | 난이도 | 비고 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **FX_Swap_Point** (12.0) | `0.5+0.3*(usd_close-1200)/300` = 환율 레벨 | **A (조건부)** | 서울외국환중개(SMBS) `http://www.smbs.biz/Exchange/FxSwapUS.jsp` — "F/X Swap POINT", 기간별(1M/2M/3M/6M/1Y) 조회 + 엑셀 다운로드 | 당일 마감 후 | 중~상 | 페이지가 **로그인 없이 공개**인 것은 직접 fetch로 확인. 단 표 본문이 JS 로드라 **조회 엔드포인트는 미확인** — 착수 전 1회 확인 필요. 한국은행 ECOS에 동일 통계가 있는지는 **이번 조사에서 확정 못 함(미확인)** |
| 2 | **Put_OTM_OI** (8.0) | `0.5-0.4*kospi_change` = 지수 등락률 | **B** | KRX OPEN API `파생상품 > 옵션 일별매매정보(주식옵션外)` (`/svc/apis/drv/...`, pykrx-openapi `get_options_daily_trade()`) — KOSPI200 옵션 종목별 일별 매매정보 | T+1 오전 8시 | 중 | **미결제약정(OI) 필드 포함 여부는 로그인 없이 명세 확인 불가 = 미확인.** OI가 있으면 A로 승격, 없으면 "풋옵션 거래대금 비중"으로 재라벨링해 B 유지. OTM 구분은 종목별 행사가로 가능해 보이나 역시 미확인 |
| 3 | **Short_Ratio** (6.0) | `0.4+0.4*(volatility/5)` = 변동성 | **B (경로 이슈)** | pykrx `get_shorting_volume_by_date()` / `get_shorting_investor_volume_by_date()` (공매도 거래량·거래대금, 투자자별) | **T+2** | 중 | 값 자체는 실측 가능. 그러나 **KRX OPEN API에 공매도 서비스가 없어** pykrx(로그인 우회 스크래핑) 외 무료 경로 없음 → §0-3-2 충돌, 오너 결정 필요 |
| 4 | **ELS_KnockIn** (7.0) | `0.1+0.7*dist_from_high` = 고점대비 낙폭 | **C** | 없음 | — | — | 개별 ELS의 낙인 배리어·기초자산 현재가 대비 근접도는 **공개 데이터 없음**. SEIBro가 ELS 발행동향·미상환잔액을 공개하지만 (ㄱ) 월 단위 집계, (ㄴ) WebSquare 기반이라 크롤링 난이도 상, (ㄷ) 별도 "데이터 구매 안내" 존재. **낙인 위험 자체는 산출 불가** |
| 5 | **VKOSPI_Skew** (6.0) | `0.4+0.4*(vol/5)-0.2*kospi_change` | **B** | KRX OPEN API `지수 > 파생상품지수 시세정보` (pykrx-openapi `get_derivative_index_daily_trade()`) | T+1 오전 8시 | 중 | **스큐(풋/콜 IV 차)는 무료 공개 없음 → 그 이름으로는 불가.** 대신 VKOSPI 지수 자체는 KRX가 산출·공표하며 "파생상품지수" API 범주에 속함(지수 코드 목록 **미확인**). pykrx로는 **불가**(이슈 #266, 2026-01 오픈, `help wanted` 라벨, 미해결). → **`VKOSPI_Level`로 정직하게 재라벨링** |
| 6 | **Synthetic_Futures** (12.0) | `0.5+0.3*(usd_close-1300)/200` = **또 환율** | **B** | KRX OPEN API `파생상품 > 선물 일별매매정보(주식선물外)` 종가 + `지수 > KOSPI 시리즈 일별시세정보`의 KOSPI200 종가 | T+1 오전 8시 | 중 | 진짜 "합성선물 괴리"(콜-풋 패리티)는 **무위험이자율·잔존만기 가정**이 들어가 §0-1 계산값 예외 2번 조건(가정 금지)에 걸림 → 그대로는 불가. 대신 **선물 베이시스 = 선물 종가 − 지수 종가**는 실측 2개의 순수 뺄셈이라 예외 4조건 충족 → **`Futures_Basis`로 재라벨링** |
| 7 | **NDF_Night_Rate** (12.0) | `0.4+0.5*usd_change` = 환율 등락률 | **C** | 없음 | — | — | SMBS의 NDF 페이지(`/Exchange/NDF.jsp`)를 직접 열어 확인 — **상품 설명만 있고 시세표가 없습니다**(F/X Swap 페이지와 달리 조회 UI 자체가 없음). 공공기관의 무료 일별 NDF 종가 공표 확인 안 됨 → 유료 단말(인포맥스/로이터) 영역 |
| 8 | **Futures_Net_Sell** (6.0) | `0.5-0.3*kospi_change` | **C** | 없음(주체별 기준) | — | — | **투자자별 선물 순매수는 KRX OPEN API에 없음**(파생상품 API는 종목별 매매정보만). pykrx에도 선물 관련 함수 자체가 없음(README 함수 목록 전수 확인: 주식/지수/공매도/ETF·ETN·ELW만). 대안으로 "선물 전체 미결제약정 증감"은 가능성 있으나(#2와 동일한 미확인 사항) **주체 구분이 불가**해 현재의 외국인/기관/개인 3분할 구조와 안 맞음 |
| 9 | **Non_Arbitrage_Ratio** (6.0) | `0.5±(기관 순매수 부호)` | **C** | 없음 | — | — | 프로그램매매 차익/비차익 구분 통계는 KRX 정보데이터시스템 화면에만 있고(현재 **로그인 필수**), OPEN API 미제공 |
| 10 | **Foreign_Broker_Dump** (6.0) | `0.5±(외국인 순매수 부호)` | **C** | 없음 | — | — | 외국계 창구(회원사)별 매매 동향의 시장 전체 집계는 무료 공개 API 없음. 게다가 지금 계산식은 **#13 `Stock_Net_Sell`의 외국인 항목과 사실상 같은 숫자**라 중복 |
| 11 | **Stock_Short_Balance** (3.0) | `0.5+0.3*dist_from_high` | **B (경로 이슈)** | pykrx `get_shorting_balance_by_date()` (공매도잔고/상장주식수/공매도금액/시가총액/비중) | **T+2** | 중 | #3과 동일한 경로 문제. 참고로 pykrx 문서에도 "잔고 비율 0.01% 미만은 보고 의무가 없어 집계 누락 가능"이 명시돼 있어, 화면에 이 한계를 같이 적어야 함 |
| 12 | **Put_Buy_Simple** (3.0) | `0.4-0.3*kospi_change` | **C** | 없음 | — | — | 투자자별(특히 개인) 옵션 매매 데이터는 무료 공개 없음. #2와 개념 중복이기도 함 |
| 13 | **Stock_Net_Sell** (3.0) | 3주체 순매수 부호 ±0.3 × 개인 비중 | **A** | **이미 수집 중** — `scrape_daily.py`가 네이버에서 3주체 순매수(억원)를 크롤링해 `market_history.csv`에 저장. 교차검증용으로 pykrx `get_market_trading_value_by_investor()`도 존재 | 당일 마감 후 | **하** | **실측값을 이미 갖고 있으면서 `0.5±0.3` 상수로 뭉개는 것**이 문제. 금액 자체를 과거 분포 대비 정규화하면 즉시 정직해짐 |
| 14 | **KOSPI_5D_Return** (12.0) | 코스피 5일 모멘텀(**이미 실측**) | **A** | **이미 수집 중** — `market_history.csv`의 코스피 종가 이력 | 당일 마감 후 | **하** | 제거 후보가 아님. 문제는 실측 수익률을 다른 13개와 같은 `clip(0~1)` 프록시 스케일에 억지로 욱여넣는다는 점. 수익률 원값을 저장하고 정규화 방식만 정리하면 됨 |

### 2-1. 판정 근거 출처 (실제로 확인한 것)

- KRX OPEN API 서비스 목록 (7개 카테고리 전수): <https://openapi.krx.co.kr/contents/OPP/INFO/service/OPPINFO004.cmd>
- KRX OPEN API 옵션 일별매매정보 서비스 페이지(필드 표는 비로그인 시 미렌더링): <https://openapi.krx.co.kr/contents/OPP/USES/service/OPPUSES005_S2.cmd?BO_ID=AoTvuFpukvuBsfypkZbq>
- KRX 회원제 전환 + 로그인 필수 (2025-12-27): pykrx 이슈 #244 <https://github.com/sharebook-kr/pykrx/issues/244>
- pykrx VKOSPI 미지원 (2026-01 오픈, help wanted, 미해결): <https://github.com/sharebook-kr/pykrx/issues/266>
- pykrx 공매도 T+2 및 함수 목록: <https://github.com/sharebook-kr/pykrx> (README 전수 확인)
- KRX OPEN API 실사용기(인증키 + 서비스별 승인 2단계, 401 원인, **전일 데이터 익일 08시 업데이트**, 1일 10,000회): <https://i-whale.com/entry/KRX-시세-데이터-이제-로그인-API를-신청-해야-쓸-수-있네요->
- pykrx-openapi(파이썬 래퍼, 엔드포인트 목록 확인): <https://github.com/raccoonyy/pykrx-openapi>
- SMBS F/X Swap POINT 페이지(공개 확인): <http://www.smbs.biz/Exchange/FxSwapUS.jsp> / NDF 페이지(시세표 없음 확인): <http://www.smbs.biz/Exchange/NDF.jsp>
- SEIBro ELS 발행동향 / 데이터 구매 안내: <https://seibro.or.kr>

### 2-2. 확인하지 못한 것 (정직하게 남김)

- KRX OPEN API 선물/옵션 일별매매정보의 **출력 필드 목록** — 로그인해야 명세서 다운로드가 되어
  **미결제약정(OI) 필드 존재 여부를 확정하지 못했습니다.** (#2, #6, #8 판정에 영향)
- KRX OPEN API "파생상품지수 시세정보"에 **VKOSPI가 포함되는지** — 지수 코드 목록 미확인. (#5)
- 한국은행 ECOS에 **외환스왑레이트/스왑포인트 통계표가 있는지** — 통계표 코드를 확정하지 못했습니다.
  (ECOS는 API 키가 있어야 통계목록 조회가 가능) (#1의 2차 출처 후보)
- SMBS F/X Swap POINT의 **실제 데이터 조회 엔드포인트**와 **이용약관상 자동수집 허용 여부**. (#1)

> 위 4가지는 전부 "될 것 같다"가 아니라 **"착수 첫 단계에서 1회 호출로 확인할 것"** 으로 남깁니다.
> 확인 결과가 부정적이면 해당 지표는 그 자리에서 C(제거)로 내리는 것이 §0-1에 맞습니다.

---

## 3. A/B 지표의 갱신 주기·지연(T+n)과 "후행지표 전용" 원칙(§0-3-1)

| 소스 | 확정 시점 | 우리 수집 시각과의 관계 |
| --- | --- | --- |
| 네이버 수급 3주체 / 코스피 종가 (현행) | 장마감 후 당일 | 현행 KST 16:05 수집으로 커버됨 ✅ |
| KRX OPEN API (지수·주식·파생 전부) | **거래일 익일 08:00** | ❌ **현행 16:05 수집으로는 그날 데이터를 못 받습니다** |
| 공매도 거래·잔고 (pykrx) | **T+2** | ❌ 당일 수집 불가, 구조적 지연 |
| SMBS F/X Swap POINT | 당일 마감 후(추정, 미확인) | 확인 필요 |

**핵심 판단: 이 지연은 이 프로젝트에 오히려 유리합니다.**
§0-3-1은 "장마감 후 확정된 후행지표만 쓴다 / 실시간은 목표가 아니다"입니다. KRX OPEN API가
"장중엔 빈 데이터, 익일 08시에 전일 확정치"를 주는 건 **정확히 이 원칙이 원하는 형태**입니다.
지금처럼 16:05에 당일치를 급히 만들어내려다 프록시로 때우는 것보다, 익일 아침에 **확정 데이터**를
받아 그 날짜(전일자)로 기록하는 편이 원칙에도 데이터 품질에도 맞습니다.

**따라서 함께 제안하는 운영 변경 (구현 시점에 같이 결정 필요):**

1. 매크로 수집을 **"익일 KST 09:00에 전일자 기준으로 기록"** 하도록 분리
   (`.github/workflows/scrape.yml`의 16:05 코스피 종목 수집은 그대로 두고, 매크로만 별도 cron).
   `scrape_daily.py`는 이미 `target_date` 백필 경로가 있으므로 구조적으로 무리가 없습니다.
2. T+2인 공매도 계열은 **같은 행에 넣되 "T+2 확정치(기준일 표기)"를 반드시 화면에 명시**.
   서로 다른 지연을 가진 값을 한 줄에 섞어놓고 지연을 안 적으면 그것 자체가 §0-1 위반입니다.
3. `market_history.csv`에 지표별 **`_asof`(그 값의 기준 거래일)** 컬럼을 같이 저장하는 것을 권장.
   (§0-3-1의 "년/월/일/시/분/초까지 남긴다" 요구와 일치)
4. GitHub Actions에 `KRX_OPENAPI_KEY`를 **Secret으로 등록**해야 합니다. 키가 없으면
   해당 지표는 값을 지어내지 말고 그대로 "산출 불가"로 빠져야 합니다
   (현행 `unavailable_items` 제외 로직이 이미 그 역할을 하므로 그 패턴을 그대로 재사용).

---

## 4. C 판정 6개 제거 시 가중치 재분배 방안

### 4-1. 먼저 확인한 사실

- 현재 `RISK_WEIGHTS` 14개 합계 = **102.0** (100이 아님. 12+8+6+7+6+12+12+6+6+6+3+3+3+12)
- `utils/macro_scoring.py`의 `compute_final_score()`는
  ```python
  total_weight = sum(weights[k] for k in available)
  base_score   = sum(available[k] * (weights[k] / total_weight) for k in available)
  ```
  → **매 실행마다 재정규화**합니다. 즉 합계가 102든 100이든, 지표를 6개 빼든 **합이 깨져서
  점수가 폭주하는 사고는 발생하지 않습니다.** (이미 오늘도 `None` 지표는 자동 제외됩니다.)
- 다만 **상대 비중은 반드시 바뀝니다.** C 6개(합 40.0, 전체의 39.2%)를 빼면 남은 8개의
  상대 비중이 자동으로 1.645배(102/62)씩 커집니다. 그러니 "그냥 지워도 안전"이 아니라
  **의도한 비중인지 다시 정해야** 합니다.

### 4-2. 안 (가) — 기계적 비례 재분배 (변화 최소, 논쟁 없음)

남은 8개에 `× 100/62`을 곱해 합계 100으로 맞춥니다.

| 지표 | 현재 | 비례 재분배 후 |
| --- | --- | --- |
| FX_Swap_Point | 12.0 | 19.4 |
| Synthetic_Futures → Futures_Basis | 12.0 | 19.4 |
| KOSPI_5D_Return | 12.0 | 19.4 |
| Put_OTM_OI | 8.0 | 12.9 |
| Short_Ratio | 6.0 | 9.7 |
| VKOSPI_Skew → VKOSPI_Level | 6.0 | 9.7 |
| Stock_Short_Balance | 3.0 | 4.8 |
| Stock_Net_Sell | 3.0 | 4.8 |
| **합계** | 62.0 | **100.1** (반올림 오차 → 어느 한 항목에서 0.1 조정) |

### 4-3. 안 (나) — 실측 직접성 기준 재설계 (권장)

비례 재분배는 "환율 레벨 하나로 12점을 주던 잘못된 비중"을 그대로 물려받습니다.
실제로 **완전 실측이고 매일 확정되는 지표에 더 큰 비중**을 주는 편이 §0-1 정신에 맞습니다.

| 지표 | 제안 가중치 | 근거 |
| --- | --- | --- |
| `KOSPI_5D_Return` | 20 | 100% 실측, 지연 0, "실제로 일어난 일" |
| `Stock_Net_Sell` | 20 | 100% 실측(3주체 순매수 금액), 지연 0, 이 화면의 원래 주제(수급) |
| `FX_Swap_Point` | 15 | 달러 유동성 = 외국인 이탈 신호. 실측 전환 시 그대로 유지할 가치 |
| `VKOSPI_Level` | 15 | 시장이 매긴 향후 30일 변동성 기대치. 파생 지표 중 가장 신뢰도 높음 |
| `Futures_Basis` | 10 | 실측 2개의 순수 뺄셈. 단, 만기 근처 왜곡 주의 |
| `Short_Ratio` | 10 | 실측이나 T+2 지연 + 취득 경로 이슈 → 비중 하향 |
| `Put_OTM_OI` | 5 | OI 필드 확인 전까지는 보수적으로 낮게 |
| `Stock_Short_Balance` | 5 | 0.01% 미만 미보고 누락 한계 존재 |
| **합계** | **100** | |

### 4-4. 제거 방식에 대한 주의

- C 6개는 `RISK_WEIGHTS`에서 **키를 삭제**하고, `scrape_daily.py`의 해당 `market_scores` 항목과
  `*_base` 계산식도 함께 제거해야 합니다(가중치만 지우면 계산은 계속 돌아 CSV에 컬럼이 남습니다).
- `market_history.csv`에 이미 쌓인 과거 컬럼은 **지우지 말고 그대로 둡니다**(과거 기록 개변 금지).
  대신 `views/macro_view.py`가 현행 `RISK_WEIGHTS` 키만 렌더링하도록 되어 있는지 확인이 필요합니다.
- 제거 사실을 화면에 한 줄로 밝히는 것을 권장합니다:
  "실측 데이터가 없는 6개 지표는 추정으로 채우지 않고 제거했습니다(§0-3-1)."
  — 지표가 조용히 사라지면 그것도 정직한 표시가 아닙니다.

---

## 5. 구현 순서 제안

정렬 기준: **① 신규 외부 의존이 0인 것 → ② 승인 대기가 필요한 것(먼저 신청만 걸어둠) →
③ 역공학이 필요한 것 → ④ 원칙 충돌이 있어 오너 결정이 필요한 것**

| 단계 | 작업 | 왜 이 순서인가 |
| --- | --- | --- |
| **0** | KRX OPEN API 회원가입 + 인증키 신청 + 4개 서비스 이용신청(파생상품지수 / KOSPI 시리즈 지수 / 선물 일별매매 / 옵션 일별매매) | **승인에 하루 이상 걸립니다.** 코드 작업 전에 신청부터 걸어두면 대기 시간이 겹쳐 사라집니다 |
| **1** | `KOSPI_5D_Return`, `Stock_Net_Sell` 두 개를 **정직한 실측 정규화로 교체** | 신규 크롤링 0건. 이미 있는 데이터만으로 "완전 실측 2지표" 확보 |
| **2** | **C 6개 제거 + 가중치 재설계**(§4-3) | 1번이 끝나면 화면에 실측 지표가 남아있으므로, 제거해도 화면이 비지 않습니다 |
| **3** | 승인된 키로 **파생상품지수 → VKOSPI 코드 확인** → `VKOSPI_Level` 연결 | API 1개, 값 1개. 파생 API 계열 중 가장 단순 |
| **4** | **선물/옵션 일별매매정보 필드 확인** → `Futures_Basis` 연결, OI 있으면 `Put_OTM_OI` 연결 | 3번에서 API 호출 코드가 이미 생기므로 재사용 |
| **5** | SMBS F/X Swap POINT **엔드포인트·약관 확인 후** `FX_Swap_Point` 연결 | 역공학이 필요해 시간 예측이 어려움. 실패해도 앞 단계가 이미 끝나 있음 |
| **6** | 공매도 2종(`Short_Ratio`, `Stock_Short_Balance`) — **오너 결정 후** | pykrx의 로그인 우회 사용 여부는 기술 문제가 아니라 §0-3-2 원칙 문제라 오너가 정해야 합니다 |

각 단계는 **독립적으로 커밋 가능**하며, 중간에 끊겨도 화면이 깨지지 않습니다
(산출 불가 지표는 현행 `unavailable_items` 로직이 자동으로 배점에서 빼줍니다).

---

## 6. 다음 세션에서 1번으로 착수할 지표

### 🥇 1순위: `KOSPI_5D_Return` (가중치 12.0 → 제안 20)

- **신규 데이터 수집이 0건입니다.** `market_history.csv`에 코스피 종가가 이미 쌓여 있어
  외부 API·승인·크래딩 매너·타임아웃 어느 것도 걸리지 않습니다.
- 현재 이미 실측인데도 다른 13개 프록시와 같은 `clip(0~1)` 스케일에 묶여 있어서
  **"실측인데 프록시처럼 보이는"** 가장 아까운 케이스입니다. 여기를 먼저 풀면
  "이 화면에도 진짜 데이터가 있다"는 기준점이 하나 생깁니다.
- 가중치가 12.0(전체 3위 tie)이라 **효과 대비 비용이 가장 좋습니다.**
- 세션이 짧게 끊겨도 안전: 한 파일(`scrape_daily.py`)의 한 블록만 바뀝니다.

### 🥈 2순위: `Stock_Net_Sell` (가중치 3.0 → 제안 20)

- 역시 **이미 크롤링해서 CSV에 저장까지 하고 있는 실측값**(외국인/기관/개인 순매수 억원)인데,
  점수화 단계에서 `0.5 ± 0.3`이라는 상수로 부호만 남기고 **금액 정보를 버리고 있습니다.**
  (2026-08-06 2차 감사 4-3에서 부분적으로 손봤지만 여전히 상수 기반)
- 이 화면의 원래 주제가 "수급"인데 정작 수급 지표 가중치가 3.0(최하위)인 것도 재검토 대상입니다.
- 1순위와 **같은 함수 안, 같은 파일**을 고치므로 이어서 하면 컨텍스트 비용이 거의 0입니다.

> **다음 세션 첫 명령 제안**: "MACRO_REDESIGN_PROPOSAL.md §6의 1·2순위(KOSPI_5D_Return,
> Stock_Net_Sell)를 실측 정규화로 교체하고, 같은 커밋에서 KRX OPEN API 인증키 신청 절차를
> 오너에게 안내" — API 승인 대기와 코드 작업을 병렬로 굴리는 구성입니다.

---

## 7. 이번 작업에서 하지 않은 것 (범위 명시)

- `.py` 파일 **단 한 줄도 수정하지 않았습니다** (읽기만).
- `git add` / `commit` / `push` **실행하지 않았습니다** (오너가 직접 실행).
- 확인하지 못한 항목을 "될 것 같다"로 올려 판정하지 않았습니다 — §2-2에 미확인으로 명시했습니다.
