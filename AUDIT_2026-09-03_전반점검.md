# visible-hand-dashboard 전반 점검 리포트 (2026-09-03)

**계기**: 오늘 낮 "Duel Daily Batch #21 skipped" 문의를 조사하다가, 실제로 배포돼 작동 중인
클라우드플레어 워커(`cloudflare_worker.js`)가 저장소엔 커밋된 적이 없다는 걸 발견했습니다.
오너가 "8/29 3일짜리 스파게티 감사를 했는데도 이런 빈틈이 있으면, 비슷한 게 더 있는 거
아니냐"고 지적하셨고, 그래서 오푸스 서브에이전트 2개를 띄워 (1) 결투(duel) 모듈 죽은 코드,
(2) 저장소 전반 건강 상태를 실제 연결된 저장소(오너 PC, GitHub와 동기화된 상태) 기준으로
다시 훑었습니다. 아래는 그 결과입니다. **코드는 아무것도 건드리지 않았고**(읽기 전용 조사),
클라우드플레어 워커 커밋 1건만 오늘 이미 반영·푸시 완료된 상태입니다.

---

## 🔴 지금 사용자에게 영향 있는 것

### 1. 코스피 화면이 8일째 "⚠️ DEGRADED" 경고를 띄우고 있음 — 가장 급함
`data/kospi200_pegy_latest.json`을 커밋 20개에 걸쳐 직접 열어본 결과, 2026-08-27부터 오늘까지
**연속 8일** `status: DEGRADED`입니다. `valid_ratio`가 0.766 → **0.756**으로 계속 내려가고
있고, FAILED로 넘어가는 문턱(0.70)까지 0.056밖에 안 남았습니다.

- 판정선은 `collector_kospi200.py:88-89`의 `VALID_RATIO_SUCCESS=0.85` / `_DEGRADED=0.70`인데,
  이 값은 **코스피 200종목 시절 실측치**입니다. 8/26에 유니버스를 500종목으로 확대했는데
  이 문턱을 재조정 안 했습니다 — 그 코드 바로 위(`:81-85`)에 **"500 확대 후 첫 실행 로그로
  재조정하라"는 본인들 메모가 그대로 남아 있고, 8일째 처리가 안 됐습니다.
- 결과: `web/pages/pegy_page.py:940-945`에서 코스피 페이지에 경고 배너가 **상시 점등** 중.
  경고가 항상 떠 있으면 진짜 문제가 생겨도 아무도 구분 못 합니다.
- ⚠️ 이건 2026-08-06에 정확히 같은 이유(옛 문턱값 0.95 하드코딩)로 한 번 고쳤던 문제의
  **재발**입니다.

**필요한 결정**: 500종목 기준 실측값(0.75~0.76대가 정상 범위인지, 아니면 진짜 수집 실패가
누적된 건지)을 오너가 판단해서 문턱값을 재조정하거나 원인을 봐야 합니다.

### 2. `requirements.txt`에 `pandas`/`lxml` 버전이 안 고정돼 있음 — 잠재 폭탄
GitHub Actions는 매 실행마다 `pip install -r requirements.txt`로 **그 순간 최신 버전**을
새로 설치합니다. `collector_kospi200.py`의 재무제표 파싱(`pd.read_html`, 657·664·784행)이
최신 pandas 3.x에서 깨지는 게 이미 확인된 상태인데, 버전이 안 고정돼 있어서 **pandas가 알아서
올라가는 순간 코스피 재무제표 수집이 통째로 멈춥니다.** 위 1번(밸리드 비율 하락)과 겹치면
DEGRADED가 아니라 FAILED로 넘어갈 수 있어 우선순위가 높습니다.

### 3. 클라우드플레어 워커 도입 후, 크롤링이 매 평일 2배로 돌고 있음
워커의 "오늘 이미 실행됐으면 건너뜀" 로직은 **GitHub 자체 cron의 지연을 막을 수단이
없습니다.** 실제 로그(9/2, 9/3)를 보면 워커가 UTC 07:10/08:05에 먼저 실행시키고,
GitHub 자체 cron이 4~5시간 뒤(UTC 12:03~12:25)에 또 실행됩니다 — 코스피·미국 크롤러가
**515종목/550종목을 하루 2번씩** 전수 크롤링하는 셈입니다. 데이터 정확성 자체는 위험이
낮지만(둘 다 장마감 후 같은 종가), 외부 사이트에 부담을 2배로 주고 있고 워커 설명 주석
("중복 실행을 대부분 막아준다")과 실제 동작이 어긋납니다.
→ **권장**: 워커를 쓰기로 했으면 저장소 쪽 `scrape.yml`/`indicator_kr.yml`의 `schedule:`을
빼거나, 미국 수집기처럼 "오늘 이미 수집했으면 스스로 건너뛰는" 가드를 앞단에 두는 쪽이 안전.

### 4. 미국주식 수집기가 최근 4일 중 4일 다 실패로 표시됨
원인은 3번과 같은 "하루 2번 cron" 구조 — 두 번째 실행이 첫 번째가 커밋하기 전 시점의
코드를 체크아웃해서 push 충돌로 실패합니다. **데이터 자체는 첫 번째 실행이 이미 정상
커밋해서 안전**하지만, Actions 탭이 상시 빨간불이라 **진짜 장애가 나도 못 알아챕니다.**

### 5. GitHub Pages가 저장소에 없는 뭔가를 계속 배포 중
`dynamic/pages/pages-build-deployment`가 최근 10일간 140회 실행됐는데, `index.html`·
`CNAME`은 8/29 Streamlit 은퇴 때 이미 삭제됐습니다. **뭘 서빙하고 있는지 저장소만 봐서는
알 수 없는 상태** — 오너가 GitHub 저장소 Settings → Pages에서 직접 확인/해제해야 합니다.

### 6. Streamlit Cloud 구버전 앱 — 중지 확인 안 됨
`PROJECT_STATUS.md`에 "대시보드에서 직접 중지 필요(코드로는 불가능)"라고 5일째 미완료로
남아 있습니다. 아직 안 끄셨다면 옛 앱이 계속 떠 있을 수 있어요.

---

## 🟠 "저장소 밖에서 도는 것" 전수 점검 (클라우드플레어 워커 같은 케이스가 더 있는지)

| 서비스 | 상태 |
|---|---|
| GitHub Pages | 🟠 위 5번 — 저장소엔 없는데 실제로 돌고 있음, 확인 필요 |
| Supabase | 🟠 스키마 원본은 저장소(`sql/`)에 있지만 **실제 DB 반영은 오너가 SQL Editor에 수동 실행** — 저장소·실제 DB가 일치한다는 보장 장치가 없음. 특히 은퇴한 결투 발행표 9개(아래 참고)가 실제로 지워졌는지 확인 필요 |
| Render | 🟠 앱 코드는 저장소지만 환경변수 20여 개(`ADS_*`, `DUEL_ENABLED` 등)는 대시보드에만 있음 — 정상(비밀값이라 원래 그래야 함), 다만 "지금 실제로 뭐가 켜져 있는지"는 저장소로 재현 불가 |
| Streamlit Cloud | 🟠 위 6번 |
| Cloudflare Worker | ✅ 오늘 커밋됨. 다만 대시보드 실배포본과 파일이 진짜 동일한지는 오너 확인 필요 |
| Discord/Google Drive/DART/KRX/AdSense/Gemini | ✅ 순수 API 호출만, 커스텀 코드 없음 — 문제 없음 |
| Zapier/AWS/Vercel/Netlify/Apps Script 등 | ✅ 문서·코드 어디에도 언급 없음 |

---

## 🟡 결투(duel) 죽은 코드 — 처음 질문하셨던 그 부분

**주장 정정**: "20개"가 아니라 **41개** 함수(원화 23 + 달러 18)입니다. "읽기 전용이라
안전"이라는 근거는 저장소 어디에도 기록이 없었고, 그중 `write_public_leaderboard`·
`delete_published_*` 등은 애초에 읽기 전용이 아니라 쓰기·삭제 함수였습니다.

- **41개 전부 프로덕션 호출부 0건 확인**(AST 기반 import 해석 + grep 교차검증).
  `web/pages/duel_page.py`도 import하지 않습니다. 안전하게 지울 수 있는 게 맞습니다.
- 단, **테스트가 딸려 있어서 "함수만 지우면 끝"이 아닙니다** — `test_duel_db.py`·
  `test_duel_db_usd.py`·`test_duel_public_ui.py`에서 통째로 지울 부분과, 일부만 고쳐야
  하는 부분(예: `USER_WRITE_FUNCTIONS` 목록에서 `save_consent` 제거)이 섞여 있습니다.
- **Supabase에서 직접 확인해야 할 표 9개 + 함수 1개** (복붙용):
  ```
  duel_nicknames
  duel_public_consent
  duel_public_leaderboard
  duel_public_holdings
  duel_bracket_assignments
  duel_public_consent_usd
  duel_public_leaderboard_usd
  duel_public_holdings_usd
  duel_bracket_assignments_usd
  ```
  함수: `duel_consent_guard()`
  (`duel_nicknames_usd`는 애초에 없음 — 원화/달러가 표 하나를 공유. `duel_accounts`·
  `duel_positions`·`duel_orders`·`duel_cash_ledger`·`duel_*_snapshots`류는 지금도
  살아있는 표이니 절대 건드리지 마세요.)
- **🔴 흥미로운 발견**: `sql/duel_schema.sql`은 지금도 저 은퇴 표 9개를 `CREATE`합니다
  (주석으로 "🗑️" 표시만 달렸을 뿐 SQL 로직은 그대로). 반대로 `utils/duel_db.py`/
  `duel_db_usd.py` 파이썬 쪽엔 은퇴 표시가 한 글자도 없습니다. 게다가 `duel_db.py:3000`
  근처 TODO 주석이 **이미 삭제된 파일**(`utils/duel_publish.py`)을 "이미 만들어져 있다"고
  안내하고 있어서, 나중에 누가 이걸 보고 작업하면 없는 파일을 찾아 헤매게 됩니다.
- 2026-08-29 재감사(#H-6) 때 이 죽은 함수 중 3개(`fetch_publishable_consents` 등)의
  페이지네이션 버그를 실제로 고치고 테스트까지 새로 썼습니다 — **죽은 코드가 계속
  유지보수되고 있었다**는 뜻입니다.

**진행 여부**: 코드+테스트 정리는 제가 할 수 있습니다. DB 표 확인은 오너만 가능합니다.

---

## 🟢 미해결 백로그 (합계 21건, TASK_HISTORY·PROJECT_STATUS 전수 취합)

대부분 이미 "오너 판단 대기"로 알고 계셨을 항목들이라 여기선 **오래 방치된 것 위주**로만
짚습니다(전체 목록은 필요하시면 따로 정리해드릴게요):

- **애드센스 슬롯ID 입력 대기** — 9일째. 코드는 이미 배포돼서 값만 넣으면 켜집니다.
- **회원탈퇴 셀프서비스 여부** — 9일째 판단 보류(의도적 보류라 문제는 아님).
- **GitHub Actions 스케줄 지연의 근본 원인 미확인** — 8일째. 워커로 완화만 한 상태.
- **`data/dividend_kr_2026_raw.jsonl` 용량** — 5일 만에 9.4MB→18.5MB, 롤오버 전략 미정.
- **결투 `needs_review` 주문 자동취소 안전장치 없음** — 관리자가 깜빡하면 주문이 영원히 대기.
- **화면 제목 글자 겹침 버그** — 재현은 확인됐는데 원인 조사가 보류 중.

---

## ⚪ 코드 위생 · 기타 잡음 (급하지 않음)

- `utils/scheduler.py` — Streamlit 은퇴(8/29) 이후 호출자 0인 완전 죽은 코드. 8/29 감사가
  못 잡았습니다(그 감사는 Python 코드 위생 자체가 목적이 아니라 위험도 높은 버그 위주였음).
- `probe_indicator_universe_timing.py` + `.github/workflows/probe_indicator_timing.yml` —
  1회성 진단용이라고 이미 문서에 결론 나 있는데 안 지워짐(다른 probe 파일들은 다 지워진 것과
  대조적).
- **"코스피 상위 200" 표현이 문서에 여러 곳 남음** — `PROJECT_STATUS.md`(464·466·479행),
  `ENGINEERING_SPEC.md`(1094행). 8/26에 500종목으로 확대됐는데 문서는 안 고쳐짐. 8/29 감사가
  코드(`duel_page.py`)의 같은 오기는 잡았지만 문서는 감사 범위 밖이라 놓쳤습니다 — **워커
  누락과 정확히 같은 종류("감사 범위 밖이라 빠진 것")의 빈틈**입니다.
- `watch_data_sanity.yml`(8/30 신설)이 스케줄 워치독의 감시 대상 9개 목록에 빠져 있음 —
  이 워크플로우가 조용히 멈춰도 아무도 모르는 사각지대.
- 워커의 안전망이 결투 배치엔 안 닿는 설계 갭 — 워커가 깨운 `scrape.yml` 완료는
  `event=workflow_dispatch`라 `duel_daily.yml`이 항상 건너뜁니다. GitHub 자체 cron이
  아예 안 도는 날엔 결투 배치도 자기 cron(같은 지연 문제 대상)에만 의존하게 됩니다.
- TODO/FIXME/HACK 마커 자체는 총 6곳뿐으로 매우 깨끗합니다(위 duel TODO 3건 제외하면 사소함).
- git 저장소 자체는 매우 건강함 — working tree clean, 정리 안 된 브랜치 0개, stash 0개.

---

## 확인 못 한 것 (이 세션 한계)

- Supabase 실제 DB에 은퇴 표 9개가 진짜 남아있는지 (네트워크·서비스 키 접근 불가)
- Render/Streamlit Cloud/GitHub Pages 대시보드의 실제 설정값 (대시보드 접근 불가)
- pytest 실제 실행 결과 (읽기 전용 원칙 유지를 위해 이번엔 실행 안 함)

---

## 다음 액션 후보 (오너 판단 필요한 순서대로)

1. 코스피 `VALID_RATIO_SUCCESS`/`DEGRADED` 문턱 재조정 또는 원인 조사
2. `requirements.txt` pandas/lxml 버전 고정
3. 워커 vs GitHub cron 이중실행 정리 방식 결정
4. GitHub Pages / Streamlit Cloud 대시보드 확인
5. Supabase에서 위 표 9개 존재 여부 확인
6. (확인되면) duel 죽은 코드 41개 + 관련 테스트 정리 — 제가 진행 가능
