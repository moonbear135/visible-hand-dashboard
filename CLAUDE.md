# CLAUDE.md — 이 저장소에서 일하는 모든 AI 에이전트의 진입점

> 이 파일은 **읽으라고 시키지 않아도 자동으로 읽히는 유일한 문서**입니다.
> 그래서 여기에는 "무엇을 해라"가 아니라 **"어디를 읽고, 누가 하고, 무엇을 절대 하지 마라"**만
> 적습니다. 실제 규칙 원문은 전부 다른 문서에 있고 이 파일은 그곳을 가리키기만 합니다
> (`ENGINEERING_SPEC.md` §0-3-10 — 같은 내용을 두 곳에 두지 않습니다).

---

## 0. 30초 안에 알아야 할 것

- **프로젝트**: "잘 보면 보이는 손 (The Visible Hand)" — 한국·미국 주식의 후행지표를
  수집·검증·가공해 보여주는 서비스. NiceGUI 단일 스택, Render 배포, GitHub Actions 자동화.
- **오너는 코딩을 하지 않습니다.** 코드·문서·검증은 전부 AI가 합니다. 그래서
  "오너가 확인할 것"은 반드시 **오너가 실제로 할 수 있는 행동**(대시보드 값 입력, 화면 확인,
  SQL 실행, 승인)으로만 적습니다.
- **오너 컴퓨터의 작업 폴더는 `C:\Users\crown\visible_hand` 입니다** (Windows).
  세션은 GitHub 자격증명이 없어 **push 를 직접 못 합니다** — 커밋까지만 하고, 오너에게
  아래 두 줄을 **cmd.exe 에 그대로 복붙되는 형태로** 드리세요. 다른 우회 방법을 새로 제안하지 마세요.
  ```
  cd /d "C:\Users\crown\visible_hand"
  git push origin main
  ```
  ⚠️ 2026-09-07 이전 경로(`C:\Users\crown\.gemini\antigravity\scratch\visible_hand`)는
  `visible_hand_OLD` 로 바뀌어 더는 쓰지 않습니다. 그날 두 폴더가 서로 다른 커밋을 들고 있어
  **엉뚱한 쪽이 push 된 일이 실제로 있었습니다.** 경로가 또 바뀌면 **이 줄만** 고치세요 —
  다른 문서는 여기를 가리키기만 합니다(§0-3-10).
- **이 저장소는 여러 AI 세션을 오가며 작업됩니다.** 지금 세션의 기억은 다음 세션에 없습니다.
  문서에 남기지 않은 것은 존재하지 않는 것과 같습니다.

---

## 1. 🚦 세션 시작 절차 (예외 없음)

1. **`PROJECT_STATUS.md` §0 → §4**를 읽는다. 지금 상태와 열려있는 일이 여기 있다.
2. **`ENGINEERING_SPEC.md` §0-1 / §0-2 / §0-3**을 읽는다. 절대준수 규칙 원문이다.
3. **`AGENT_ORCHESTRATION.md`**를 읽고 이번 일이 **어느 에이전트 소관인지** 확인한다.
4. 그 에이전트 정의(`.claude/agents/<이름>.md`)를 읽고, 거기 적힌 파일만 연다.
5. **전체 파일을 처음부터 다 읽지 않는다.** 필요한 파일만 선택적으로 읽는다.

> ⛔ 1~3번을 건너뛰고 코드부터 여는 것은 이 저장소에서 **규칙 위반**입니다.
> 이미 고친 버그를 다시 진단하거나, 지우기로 한 파일을 되살리는 역행이 실제로 발생했습니다.

---

## 2. 🔴 절대준수 — 요약과 원문 위치

아래는 **목록일 뿐**입니다. 판단이 필요한 순간에는 반드시 원문(`ENGINEERING_SPEC.md`)을 여세요.

| # | 규칙 | 원문 |
|---|---|---|
| 1 | **없는 데이터를 지어내지 않는다.** 추정·평균·기본값으로 빈칸을 메우지 않는다. 실패는 화면에 실패로 보인다. 로그만 남기는 것은 조치가 아니다 | §0-1 |
| 2 | 작업 시작 전 `PROJECT_STATUS.md`, 종료 시 갱신 | §0-2 |
| 3 | **후행지표 전용.** "실시간"을 암시하는 표현·기능을 만들지 않는다 | §0-3-1 |
| 4 | **외부 서버 매너.** 네이버·DART·GitHub·Supabase·Render 등 접속하는 **모든** 대상에 여유 있는 딜레이(기준 2.0~3.0초). 차단(403/429) 시 무한 재시도 금지, 실패로 기록하고 중단 | §0-3-2 |
| 5 | 다중 출처 + raw/가공 데이터 분리 보관, 교차검증 후 병합 | §0-3-3 |
| 6 | 화면에 스택 트레이스·함수명·파일 경로 노출 금지 | §0-3-4 |
| 7 | 작업 종료 시 `git push`까지 완료 | §0-3-5 |
| 8 | 신규 기능은 독립 모듈로 + 스테이징 후 **오너 승인**을 받고 실전 반영 (긴급 복구는 예외) | §0-3-6 |
| 9 | 🔴 **사용자 개인정보·자산 데이터는 절대 섞이거나 노출되지 않는다.** 이 프로젝트 최상위 금지사항 | §0-3-8 |
| 10 | 🔴 **널리 알려진 해킹 기법에는 예외 없이 방어한다.** 보안에는 타협하지 않는다 | §0-3-9 |
| 11 | 코드는 단순하게, 중복은 만들지 않는다 (단일 출처 원칙) | §0-3-10 |
| 12 | 외부 AI 모델·API는 항상 교체 가능하게. 한 회사에 종속되지 않는다 | §0-3-11 |
| 13 | 외부 의존성은 능동적으로 점검한다 | §0-3-12 |
| 14 | 꼭 알려야 할 유의사항은 툴팁 뒤에 숨기지 않는다 | §0-3-13 |
| 15 | `TASK_HISTORY.md`는 상세히 쌓고, 닫힌 구간은 아카이브로 옮긴다 | §0-3-14 |
| 16 | 크롤링을 소비하는 배치는 **크롤링 완료 이벤트**에 연결한다. cron은 안전망일 뿐 | §0-3-15 |
| 17 | `iloc` 위치 인덱스 파싱 금지 / 특정 종목 전용 예외 코드 금지 / 더미 데이터 자동 생성 금지 / 단위 변환 임의 적용 금지 | §2-1~2-4 |
| 18 | 🔴 **재료는 받는 것이지 만드는 것이 아니다.** 재료가 있는 상태에서 파생 지표를 계산하는 건 괜찮지만, **재료 자체를 계산으로 얻어내면 안 된다.** 하나씩 느리게 크롤링하는 이유가 애널리스트 추정치를 읽기 위해서다 | §2-3-1 |

### 🔑 비밀값 취급 (위 9·10번의 실행 규칙)

- API 키·토큰·웹훅 URL·비밀번호 해시는 **환경변수 / GitHub Secrets / Render 환경변수에서만** 읽는다.
- 코드·문서·커밋 메시지·로그·화면·에러 메시지 **어디에도 값을 남기지 않는다.**
  로그에는 "켜짐/꺼짐"과 단계만 남긴다 (`main.py`의 데이터소스·애드센스 기동 로그가 표준 예시).
- URL 쿼리스트링에 키를 싣지 않는다 — 서버 로그에 남는다 (`utils/krx_openapi.py`가 헤더만 쓰는 이유).
- 값이 없으면 **기본값을 지어내지 말고 기동을 거부하거나 그 기능만 끈다**
  (`main.py`의 `NICEGUI_STORAGE_SECRET` 미설정 시 `SystemExit`이 표준 예시).

---

## 3. 🧭 누가 이 일을 하는가 — 에이전트 라우팅

전체 배치도·경계·인계 규칙은 **`AGENT_ORCHESTRATION.md`**에 있습니다. 아래는 빠른 찾아보기입니다.

| 건드리려는 것 | 담당 에이전트 |
|---|---|
| `/kr` 코스피·코스닥 PEGY, `collector_kospi200.py`, `utils/scoring.py`, `utils/guardrail.py` | `kr-stocks` |
| `/us` 미국주식, `collector_us_stocks.py`, `utils/scoring_us.py` | `us-stocks` |
| `/dividend`·`/dividend/us`, `collector_dividend_*.py`, `corp_code_mapper.py` | `dividend` |
| `/indicator` 보조지표, `collector_indicator_kr.py`, `utils/indicators.py` | `indicator` |
| `/scorecard`(+consent·leaderboard), `utils/scorecard_*.py` | `scorecard` |
| `/report` 사장님 보고서, `utils/report_db.py`, `collector_us_indices.py` | `report` |
| `/duel` 결투다!, `utils/duel_*.py`, `run_duel_daily_batch*.py` | `duel` |
| `/admin/macro` 매크로 방공망 | `macro` — 🛑 **동결. 오너의 명시적 지시 없이는 착수 금지** |
| `utils/data_source.py`·`db.py`·`data_validator.py`·`data_sanity.py`·`constants*.py` | `data-foundation` |
| `web/auth*.py`·`layout.py`·`state.py`·`blocking.py`·`components/`·개인정보·보안 | `web-security` |
| `.github/workflows/*`·`crawl_ready_gate.py`·`run_*.py`·스케줄 | `automation-ops` |
| `tests/*`·코드리뷰·전체 재감사 | `test-audit` |
| 외부 출처 재고 `DATA_SOURCES_INVENTORY.md`·새 바깥 주소·새 패키지·출처 종료/404·대체재 조사 | `data-sources` |

> 표에 없는 새 모듈을 만든다면 **담당 에이전트도 같이 만듭니다** —
> `AGENT_ORCHESTRATION.md` §2-6. 잊으면 `tests/test_agent_registry.py` 가 빨간불을 냅니다.

---

## 4. ⛔ 어떤 에이전트도 혼자 결정하지 못하는 것 (오너 결정 사항)

아래는 **작업을 멈추고 오너에게 물어야 하는** 항목입니다. 좋은 아이디어가 있어도 먼저 실행하지 않습니다.

1. **새 기능의 실전 반영** — 스테이징 확인 후 오너 승인 (§0-3-6)
2. **점수 배점·차단 기준·수식 변경** — `ENGINEERING_SPEC.md` §5 의 값
3. **공개 단계 전환** — `*_ENABLED` / `*_ADMIN_ONLY` 환경변수를 켜는 판단
4. **새 외부 데이터 출처 추가** — §0-3-2 매너 장치 설계와 약관 검토가 선행
5. **DB 스키마 변경** — `sql/*.sql`은 오너가 Supabase에서 직접 실행해야 함
6. **전체 코드리뷰·재검토 착수** — 토큰을 크게 쓰므로 **반드시 먼저 물어볼 것**
7. **파일 삭제** — 아카이브 이동은 가능, 영구 삭제는 오너 확인

---

## 5. 🏁 세션 종료 절차 (예외 없음)

1. `pytest --ignore=archive -q` 통과 확인. 실패를 남긴 채 끝내지 않는다.
   (새 파일을 만들었다면 `tests/test_agent_registry.py` 가 담당 에이전트 누락을 잡습니다.)
2. `PROJECT_STATUS.md` — "최근 작업 로그" 맨 위에 오늘 한 일 한두 줄 추가.
3. `PROJECT_STATUS.md` — "지금 열려있는 일" 정리 (완료 항목 삭제, 새 미해결 항목 추가).
4. 파일 구조가 바뀌었으면 `PROJECT_STATUS.md` §2 표도 갱신.
5. `TASK_HISTORY.md`에 번호를 붙여 상세 이력 추가.
6. **`git push`까지 완료** (§0-3-5).
7. **새 파일을 만들었으면 담당 에이전트의 `## 소유 파일` 목록에 넣는다.** 새 모듈이면
   `AGENT_ORCHESTRATION.md` §2-6 의 7단계를 따른다 — 주인 없는 파일은 테스트가 잡습니다.

---

## 6. 📚 문서 지도

| 문서 | 역할 | 언제 고치나 |
|---|---|---|
| `CLAUDE.md` (이 파일) | 진입점·라우팅·금지선 | 에이전트가 늘거나 최상위 규칙이 바뀔 때 |
| `ENGINEERING_SPEC.md` | **절대준수 지시서** — 원칙·수식·NEVER Rules | 오너가 새 원칙을 지시할 때 |
| `AGENT_ORCHESTRATION.md` | **전체 흐름 지시서** — 데이터 흐름 + 에이전트 배치·인계 | 모듈 경계가 바뀔 때 |
| `PROJECT_STATUS.md` | **현재 개발 현황 지시서** — 지금 상태·열려있는 일 | **매 세션 종료 시** |
| `TASK_HISTORY.md` | 번호별 상세 작업 이력 (#154~) | 매 작업마다 |
| `TASK_HISTORY_ARCHIVE.md` | 닫힌 구간 이력 (#1~#153) | 아카이브 이동 시에만 |
| `*_WORK_ORDER.md` | 모듈별 설계·작업지시서 (배당·결투·리포트·보조지표·성적표·미국주식) | 그 모듈 설계가 바뀔 때 |
| `.claude/agents/*.md` | 에이전트별 담당 범위·금지사항 | 담당 파일이 바뀔 때 |
